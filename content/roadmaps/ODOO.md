# The Zero-to-Hero Odoo Engineer Roadmap

*Mohammad Bilal's complete, self-paced path from absolute zero knowledge to job-ready Odoo engineering — business/ERP thinking, platform internals, modules, ORM, security, UI, integrations, OWL, attachments, PostgreSQL, testing, CLI, deployment, upgrades, functional fluency, and hire readiness — told as one continuous conversation in which each idea solves a limit left by the previous one.*

*Resources curated with Composio (web search, YouTube, GitHub) against [Odoo 18 Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html), [Architecture overview](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/01_architecture.html), [Building a Module](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html), [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html), [Security in Odoo](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html), [View architectures](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html), [Web Controllers](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html), [Web Services](https://www.odoo.com/documentation/18.0/developer/howtos/web_services.html), [Owl components](https://www.odoo.com/documentation/18.0/developer/reference/frontend/owl_components.html), [Translations](https://www.odoo.com/documentation/18.0/developer/misc/i18n/localization.html), [Source install](https://www.odoo.com/documentation/18.0/administration/on_premise/source.html), [odoo/odoo](https://github.com/odoo/odoo), [odoo/owl](https://github.com/odoo/owl), [odoo/docker](https://github.com/odoo/docker), and beginner videos such as [What is ERP?](https://www.youtube.com/watch?v=10JeksrGVjI), [Odoo Beginner's Guide](https://www.youtube.com/watch?v=QuC6rc2q2mg), [Mastering Odoo ORM](https://www.youtube.com/watch?v=5YIwP9-55Qk), and [OWL widget tutorial](https://www.youtube.com/watch?v=YJg7dvwXQF8).*

*Where this sits:* Python comfort helps once modules start (classes, inheritance). Prefer working [`CS.md`](./CS.md) foundations (especially OOP) in parallel or just ahead of Stories IV–VI. Build matching work in [`Projects.md`](../guides/Projects.md) and drill [`Interview.md`](../guides/Interview.md) when you reach Story XXIV.*

**Scope:** 24 stories · 96 phases · zero knowledge → Job Ready Odoo Engineer.

Zero Knowledge → ERP Understanding → Odoo User → Odoo Backend Developer → Full-Stack Odoo Developer → Production Odoo Engineer → Job Ready

---

## How to Read This Document

### Start here if Odoo, ERP, and business software are completely new to you

Keep one small company in mind the whole way — a bookstore, a clinic, a workshop. For every topic, ask: *what business fact does this protect or move?* Enter a few records by hand when a lab asks you to.

**Everyday words**

| Word | Meaning |
| --- | --- |
| **ERP** | One connected **business system** for sales, stock, money, people, and related work |
| **Odoo** | An **ERP platform** you can configure and extend with modules |
| **Module / Addon** | A package that **adds or changes** business features |
| **Model** | The definition of a kind of **business record** |
| **Record** | One stored item such as a **customer** or **sales order** |
| **View** | What the **user sees and clicks** for those records |

Authoring contract: [Learning Content Standard](../standards/LEARNING_CONTENT_STANDARD.md) and [Odoo Course Profile](../standards/course-profiles/odoo.md). Lessons use continuous teacher prose, selective contextual headings, and visible narrative closers after Practice (`**WHAT THIS UNLOCKS NEXT:**` / `**WHAT COMES NEXT:**`).

**Prerequisites:** Curiosity and willingness to run a local Odoo. Comfortable Python before Story IV is strongly recommended.

---

> **Integrated Git practice:** Each linked project card in [`Projects.md`](../guides/Projects.md) ends with one specific Git checkpoint.

---

## The Story Map

```text
ODOO DEVELOPMENT & ENGINEERING

Zero Knowledge
      ↓
ERP Understanding
      ↓
Odoo User
      ↓
Odoo Backend Developer
      ↓
Full-Stack Odoo Developer
      ↓
Production Odoo Engineer
      ↓
Job Ready

STORY I — UNDERSTAND THE BUSINESS BEFORE THE CODE
  "Odoo exists to run businesses. First understand the problem it solves."
  Phase 1 — What Is ERP?
    1.1 Business Processes
    1.2 Departments
    1.3 Cross-Department Workflows
    1.4 Master Data
    1.5 Transactions
    1.6 Single Source of Truth
    1.7 ERP vs CRM
    1.8 ERP vs Standalone Business Software
    1.9 PRACTICE — Model a Small Company
  Phase 2 — Understanding Odoo
    2.1 What Odoo Is
    2.2 Odoo Ecosystem
    2.3 Community Edition
    2.4 Enterprise Edition
    2.5 Odoo Apps
    2.6 Modules / Addons
    2.7 Users
    2.8 Companies
    2.9 Shared Business Records
  Phase 3 — Core Business Applications
    3.1 Contacts
    3.2 CRM
    3.3 Sales
    3.4 Purchase
    3.5 Inventory
    3.6 Accounting
    3.7 Employees / HR
    3.8 Projects
    3.9 Manufacturing
    3.10 PRACTICE — Trace Quote → Delivery → Invoice
  STORY PROJECT — Design a Company's ERP Flow

STORY II — HOW ODOO ACTUALLY WORKS
  "Before changing Odoo, understand what runs underneath it."
  Phase 4 — Odoo Architecture
    4.1 Three-Tier Architecture
    4.2 Browser / Web Client
    4.3 Odoo Application Server
    4.4 PostgreSQL
    4.5 Filestore
    4.6 Addons
    4.7 Registry
    4.8 ORM
    4.9 HTTP Layer
    4.10 LAB — Trace Browser → Server → Database
  Phase 5 — Development Environment
    5.1 Python Environment
    5.2 PostgreSQL Setup
    5.3 Odoo Source
    5.4 Configuration File
    5.5 addons_path
    5.6 Custom Addons Directory
    5.7 Developer Mode
    5.8 Debug Assets Mode
    5.9 Logging
    5.10 LAB — Run Odoo Locally
  Phase 6 — Odoo Source Code Structure
    6.1 odoo/
    6.2 addons/
    6.3 Core Framework
    6.4 Models
    6.5 HTTP
    6.6 Services
    6.7 Tools
    6.8 Registry
    6.9 Reading Official Addons
    6.10 PRACTICE — Navigate the Odoo Repository
  STORY PROJECT — Local Odoo Development Workspace

STORY III — YOUR FIRST ODOO MODULE
  "Odoo changes should live inside modules, not scattered patches."
  Phase 7 — Module Anatomy
    7.1 Addon Directory
    7.2 __manifest__.py
    7.3 __init__.py
    7.4 models/
    7.5 views/
    7.6 security/
    7.7 data/
    7.8 demo/
    7.9 static/
    7.10 controllers/
    7.11 wizard/
    7.12 report/
    7.13 i18n/
    7.14 MINI PROJECT — Hello Odoo
  Phase 8 — Module Lifecycle
    8.1 Module Discovery
    8.2 Dependencies
    8.3 Installation
    8.4 Initialization
    8.5 Upgrade
    8.6 Uninstall
    8.7 Data Load Order
    8.8 Application Flag
    8.9 Installable Flag
    8.10 LAB — Install & Upgrade Your Module
  STORY PROJECT — First Complete Addon

STORY IV — MODELING BUSINESS DATA
  "A business system begins with correctly modeled information."
  Phase 9 — Models
    9.1 models.Model
    9.2 models.AbstractModel
    9.3 models.TransientModel
    9.4 _name
    9.5 _description
    9.6 _rec_name
    9.7 _order
    9.8 Automatic Fields
    9.9 Reserved Field Names
    9.10 MINI PROJECT — Library Model
  Phase 10 — Fields
    10.1 Char
    10.2 Text
    10.3 Html
    10.4 Integer
    10.5 Float
    10.6 Boolean
    10.7 Date
    10.8 Datetime
    10.9 Selection
    10.10 Monetary
    10.11 Binary
    10.12 Image
    10.13 Field Attributes
    10.14 PRACTICE — Model Business Records
  Phase 11 — Relationships
    11.1 Many2one
    11.2 One2many
    11.3 Many2many
    11.4 Related Fields
    11.5 Relational Commands
    11.6 Command.create()
    11.7 Command.update()
    11.8 Command.delete()
    11.9 Command.unlink()
    11.10 Command.link()
    11.11 Command.clear()
    11.12 Command.set()
    11.13 DESIGN PROJECT — Business Data Graph
  Phase 12 — Computed & Dynamic Fields
    12.1 compute
    12.2 @api.depends
    12.3 Stored Computed Fields
    12.4 Non-Stored Fields
    12.5 inverse
    12.6 Custom Search Methods
    12.7 Related Fields
    12.8 Default Values
    12.9 copy
    12.10 LAB — Automatic Business Calculations
  STORY PROJECT — Business Data Management Module

STORY V — MASTERING THE ODOO ORM
  "Talk to business records through the ORM — safely, in batches, with clear intent."
  Phase 13 — Environment & Recordsets
    13.1 env
    13.2 self
    13.3 Recordsets
    13.4 Empty Recordsets
    13.5 Singleton Records
    13.6 env.user
    13.7 env.company
    13.8 env.companies
    13.9 Context
    13.10 with_context()
    13.11 with_user()
    13.12 with_company()
    13.13 sudo()
    13.14 with_env()
  Phase 14 — CRUD
    14.1 create()
    14.2 browse()
    14.3 search()
    14.4 search_fetch()
    14.5 read()
    14.6 write()
    14.7 unlink()
    14.8 copy()
    14.9 exists()
    14.10 CODING PRACTICE — CRUD Operations
  Phase 15 — Domains & Searching
    15.1 Domain Syntax
    15.2 Comparison Operators
    15.3 in / not in
    15.4 like / ilike
    15.5 child_of / parent_of
    15.6 AND
    15.7 OR
    15.8 NOT
    15.9 search_count()
    15.10 search_read()
    15.11 Ordering
    15.12 Limit / Offset
    15.13 PRACTICE — Business Queries
  Phase 16 — Recordset Operations
    16.1 Iteration
    16.2 filtered()
    16.3 filtered_domain()
    16.4 mapped()
    16.5 sorted()
    16.6 ensure_one()
    16.7 Recordset Union
    16.8 Recordset Intersection
    16.9 Recordset Difference
    16.10 Batch Operations
  Phase 17 — ORM Internals & Performance
    17.1 Prefetching
    17.2 ORM Cache
    17.3 Transactions
    17.4 Flush
    17.5 Cache Invalidation
    17.6 N+1 Queries
    17.7 Batch Creation
    17.8 Aggregation
    17.9 read_group()
    17.10 Raw SQL
    17.11 ORM vs SQL
    17.12 Security Implications of SQL
    17.13 LAB — Optimize ORM Code
  STORY PROJECT — Data-Heavy Odoo Module

STORY VI — BUSINESS RULES & WORKFLOWS
  "Business software enforces what is legal — not only what is possible."
  Phase 18 — Business Methods
    18.1 Model Methods
    18.2 Recordset Methods
    18.3 @api.model
    18.4 @api.model_create_multi
    18.5 create() Override
    18.6 write() Override
    18.7 unlink() Override
    18.8 super()
    18.9 Return Values
    18.10 Business Actions
  Phase 19 — Constraints & Validation
    19.1 Python Constraints
    19.2 @api.constrains
    19.3 SQL Constraints
    19.4 ValidationError
    19.5 UserError
    19.6 AccessError
    19.7 LAB — Protect Business Rules
  Phase 20 — Onchange & UI Logic
    20.1 @api.onchange
    20.2 Defaults
    20.3 default_get()
    20.4 Context Defaults
    20.5 Dynamic Forms
    20.6 Onchange vs Compute
  Phase 21 — States & Workflows
    21.1 State Fields
    21.2 Draft
    21.3 Confirmed
    21.4 Approved
    21.5 Done
    21.6 Cancelled
    21.7 Legal State Transitions
    21.8 Buttons
    21.9 Workflow Validation
    21.10 PROJECT — Approval Workflow
  STORY PROJECT — Business Approval System

STORY VII — BUILDING THE USER INTERFACE
  "Users never see your Python — they see views, actions, and menus."
  Phase 22 — XML Foundations
    22.1 XML Syntax
    22.2 <record>
    22.3 External IDs
    22.4 ref()
    22.5 eval
    22.6 Data Files
    22.7 XML Loading
  Phase 23 — Form & List Views
    23.1 Form Views
    23.2 List Views
    23.3 Field Display
    23.4 Buttons
    23.5 Groups
    23.6 Sheets
    23.7 Notebook
    23.8 Pages
    23.9 Widgets
    23.10 Decorations
    23.11 MINI PROJECT — CRUD Interface
  Phase 24 — Search, Kanban & Analytical Views
    24.1 Search Views
    24.2 Filters
    24.3 Group By
    24.4 Search Panels
    24.5 Kanban
    24.6 Calendar
    24.7 Pivot
    24.8 Graph
    24.9 Activity Views
    24.10 PRACTICE — Multiple Views of Same Data
  Phase 25 — Actions & Menus
    25.1 Actions
    25.2 Action Bindings
    25.3 Window Actions
    25.4 ir.actions.act_window
    25.5 URL Actions
    25.6 Server Actions
    25.7 Report Actions
    25.8 Client Actions
    25.9 Scheduled Actions
    25.10 Menus
    25.11 Context
    25.12 Domains
    25.13 Navigation
  Phase 26 — View Inheritance
    26.1 inherit_id
    26.2 XPath
    26.3 position
    26.4 Attributes
    26.5 Adding Elements
    26.6 Replacing Elements
    26.7 Moving Elements
    26.8 View Resolution
    26.9 Debugging Inherited Views
  STORY PROJECT — Complete Odoo Application UI

STORY VIII — SECURITY & MULTI-COMPANY
  "If the wrong user can see or change the wrong row, the module is unfinished."
  Phase 27 — Users & Groups
    27.1 Users
    27.2 Groups
    27.3 Group Membership
    27.4 Implied Groups
    27.5 Application Categories
    27.6 Permission Design
  Phase 28 — Access Control Lists
    28.1 ir.model.access
    28.2 ir.model.access.csv
    28.3 Read
    28.4 Write
    28.5 Create
    28.6 Unlink
    28.7 Group ACLs
    28.8 Additive Permissions
  Phase 29 — Record Rules
    29.1 ir.rule
    29.2 domain_force
    29.3 Global Rules
    29.4 Group Rules
    29.5 Rule Composition
    29.6 Row-Level Security
  Phase 30 — Security Context & Safe Privilege
    30.1 sudo()
    30.2 with_user()
    30.3 Access Checks
    30.4 check_access()
    30.5 Field Security
    30.6 View / Menu Groups
    30.7 Public Method Security
    30.8 Unsafe Raw SQL
    30.9 Security Bypass Risks
    30.10 Least Privilege
  Phase 31 — Multi-Company Development
    31.1 res.company
    31.2 company_id
    31.3 company_ids
    31.4 env.company
    31.5 with_company()
    31.6 Company-Dependent Fields
    31.7 _check_company
    31.8 Multi-Company Domains
    31.9 Cross-Company Security
  STORY PROJECT — Secure Multi-Company Module

STORY IX — EXTENDING EXISTING ODOO
  "Prefer extending official apps over forking them."
  Phase 32 — Model Inheritance
    32.1 Extension Inheritance
    32.2 _inherit
    32.3 Classical Inheritance
    32.4 Method Overriding
    32.5 Field Extension
    32.6 super()
    32.7 Multiple Inheritance
  Phase 33 — Delegation & Mixins
    33.1 _inherits
    33.2 Delegated Fields
    33.3 Delegation vs Extension
    33.4 AbstractModel
    33.5 Mixins
    33.6 Reusable Model Behavior
    33.7 Composition
  Phase 34 — View Inheritance in Real Modules
    34.1 Finding Parent Views
    34.2 External IDs
    34.3 XPath Targeting
    34.4 Adding Fields
    34.5 Modifying Attributes
    34.6 Replacing Elements
    34.7 Debugging Inheritance Conflicts
  Phase 35 — Extending Official Applications
    35.1 res.partner
    35.2 sale.order
    35.3 purchase.order
    35.4 stock.picking
    35.5 account.move
    35.6 Existing Workflows
    35.7 Extension Points
    35.8 Upgrade-Safe Customization
  STORY PROJECT — Customize an Official Odoo App

STORY X — ADVANCED BUSINESS FEATURES
  "Wizards, sequences, cron, chatter, reports, and data files run real operations."
  Phase 36 — Wizards & TransientModel
    36.1 TransientModel
    36.2 Wizard Fields
    36.3 Wizard Views
    36.4 target="new"
    36.5 active_id
    36.6 active_ids
    36.7 Context
    36.8 Multi-Step Wizards
    36.9 MINI PROJECT — Bulk Processing Wizard
  Phase 37 — Sequences
    37.1 ir.sequence
    37.2 next_by_code()
    37.3 Prefix
    37.4 Suffix
    37.5 Padding
    37.6 Date-Based Sequences
    37.7 Business References
  Phase 38 — Scheduled Actions / Cron
    38.1 ir.cron
    38.2 Scheduling
    38.3 Cron Methods
    38.4 Batch Processing
    38.5 Progress
    38.6 Failure Handling
    38.7 Idempotency
    38.8 Cron Security
  Phase 39 — Automation & Server Actions
    39.1 Automated Actions
    39.2 Triggers
    39.3 Conditions
    39.4 Server Actions
    39.5 Evaluation Context
    39.6 Action Chaining
    39.7 When to Use Code Instead
  Phase 40 — Mail, Chatter & Activities
    40.1 mail.thread
    40.2 mail.activity.mixin
    40.3 Messages
    40.4 Followers
    40.5 Tracking
    40.6 Activities
    40.7 Notifications
    40.8 Email Templates
    40.9 Sending Mail
  Phase 41 — Reports & QWeb
    41.1 QWeb
    41.2 QWeb Directives
    41.3 Report Actions
    41.4 Report Templates
    41.5 PDF Reports
    41.6 HTML Reports
    41.7 Paper Formats
    41.8 Custom Report Data
    41.9 Translatable Reports
    41.10 Barcodes
    41.11 Report Attachments
  Phase 42 — Data Files, Import & Export
    42.1 XML Data
    42.2 CSV Data
    42.3 External IDs
    42.4 env.ref()
    42.5 noupdate
    42.6 Demo Data
    42.7 Seed Data
    42.8 Import
    42.9 Export
    42.10 Data Load Order
    42.11 XML Data Operations
  STORY PROJECT — Automated Business Operations Module

STORY XI — INTERNATIONALIZATION & LOCALIZATION
  "Real ERP software serves people in different languages, countries and accounting environments."
  Phase 43 — Internationalization & Localization
    43.1 Translatable Strings
    43.2 _() Translation Function
    43.3 Translatable Fields
    43.4 i18n/
    43.5 .po Files
    43.6 .pot Templates
    43.7 Exporting Translations
    43.8 Importing Translations
    43.9 Languages
    43.10 Locale / Formatting
    43.11 Translatable QWeb
    43.12 Localization Modules
    43.13 Accounting Localization Concept
    43.14 PRACTICE — Translate a Custom Module
  STORY PROJECT — Multilingual Business Module

STORY XII — WEB DEVELOPMENT WITH ODOO
  "Controllers, website, and portal put Odoo on the public and customer web."
  Phase 44 — HTTP Controllers
    44.1 http.Controller
    44.2 @http.route
    44.3 request
    44.4 Route Types
    44.5 HTTP Methods
    44.6 Parameters
    44.7 Responses
    44.8 Authentication
    44.9 CSRF
    44.10 Sessions
    44.11 Error Handling
    44.12 Controller Security
  Phase 45 — Website Development
    45.1 Website Architecture
    45.2 Website Pages
    45.3 QWeb Templates
    45.4 Dynamic Content
    45.5 Website Controllers
    45.6 Frontend Assets
    45.7 Forms
    45.8 SEO Basics
    45.9 Multi-Website Concept
  Phase 46 — Portal Development
    46.1 Portal Users
    46.2 Portal Controllers
    46.3 Customer Pages
    46.4 Portal Layout
    46.5 Portal Security
    46.6 Customer-Owned Records
    46.7 Pagination
    46.8 Search / Filters
    46.9 Access Tokens
  STORY PROJECT — Customer Self-Service Portal

STORY XIII — APIS & INTEGRATIONS
  "Other systems will call you — and you will call them. Contracts first."
  Phase 47 — External API Concepts
    47.1 Client / Server
    47.2 Resources
    47.3 HTTP Methods
    47.4 JSON
    47.5 Authentication
    47.6 Status Codes
    47.7 Pagination
    47.8 Rate Limits
    47.9 API Contracts
  Phase 48 — External Odoo Access
    48.1 External API Architecture
    48.2 Integration Users
    48.3 Authentication
    48.4 Calling Models
    48.5 CRUD Remotely
    48.6 Domains
    48.7 Permissions
    48.8 Version Compatibility
  Phase 49 — Building API Endpoints
    49.1 API Routes
    49.2 Request Parsing
    49.3 Validation
    49.4 Serialization
    49.5 Response Design
    49.6 Authentication
    49.7 Authorization
    49.8 Pagination
    49.9 Error Responses
    49.10 API Versioning Concepts
  Phase 50 — Webhooks
    50.1 Incoming Webhooks
    50.2 Outgoing Events
    50.3 Payload Validation
    50.4 Signatures
    50.5 Secrets
    50.6 Idempotency
    50.7 Retries
    50.8 Duplicate Events
    50.9 Audit Logging
  Phase 51 — Third-Party Integrations
    51.1 REST APIs
    51.2 API Authentication
    51.3 Mapping External Data
    51.4 Data Ownership
    51.5 Synchronization
    51.6 Incremental Sync
    51.7 Scheduled Sync
    51.8 Retries / Backoff
    51.9 Rate Limiting
    51.10 Error Handling
    51.11 Integration Logs
  Phase 52 — Payment Integrations
    52.1 Payment Architecture
    52.2 Payment Providers
    52.3 Payment Transactions
    52.4 Checkout Flow
    52.5 Redirects
    52.6 Tokens
    52.7 Callbacks
    52.8 Webhooks
    52.9 Payment Status
    52.10 Failure Handling
    52.11 Refund Concepts
  STORY PROJECT — Odoo ↔ External SaaS Integration

STORY XIV — MODERN ODOO FRONTEND
  "Standard views go far. Custom interactive experiences require the web framework."
  Phase 53 — Odoo JavaScript Architecture
    53.1 Web Client
    53.2 JavaScript Modules
    53.3 Native JS Modules
    53.4 Frontend Environment
    53.5 Components
    53.6 Registries
    53.7 Services
    53.8 Hooks
    53.9 Backend ↔ Frontend Communication
    53.10 Frontend Debugging
  Phase 54 — OWL Fundamentals
    54.1 Components
    54.2 Templates
    54.3 Props
    54.4 State
    54.5 Events
    54.6 Lifecycle
    54.7 Composition
    54.8 Subcomponents
    54.9 Reactive UI
  Phase 55 — Services, Registries & Hooks
    55.1 Registries
    55.2 Registry Categories
    55.3 Services
    55.4 Service Dependencies
    55.5 useService()
    55.6 ORM Service
    55.7 RPC Service
    55.8 Notification Service
    55.9 Action Service
    55.10 Hooks
    55.11 useBus
    55.12 useAssets
    55.13 Custom Hooks
  Phase 56 — Assets & Frontend Resources
    56.1 Asset Bundles
    56.2 JavaScript Assets
    56.3 SCSS / CSS
    56.4 XML Templates
    56.5 Manifest Assets
    56.6 Lazy Loading
    56.7 Asset Debugging
  Phase 57 — Extending & Patching the Web Client
    57.1 Patching Code
    57.2 Patching Components
    57.3 Extending Components
    57.4 Extending Registries
    57.5 Custom Field Widgets
    57.6 View Extensions
    57.7 Upgrade Safety
    57.8 When Not to Patch
  Phase 58 — Client Actions & Advanced Frontend
    58.1 Client Actions
    58.2 Action Registry
    58.3 OWL Client Components
    58.4 Loading Server Data
    58.5 Custom Dashboards
    58.6 Generic Components
    58.7 Error Handling
    58.8 Error Boundaries
    58.9 Odoo Editor Concepts
    58.10 Mobile Web APIs
    58.11 Responsive Odoo UI
  STORY PROJECT — Interactive Odoo Dashboard

STORY XV — FILES, ATTACHMENTS & MEDIA
  "Business documents are files — store and secure them like business data."
  Phase 59 — Attachments & Filestore
    59.1 ir.attachment
    59.2 Binary Fields
    59.3 Image Fields
    59.4 Database vs Filestore
    59.5 Attachment Relationships
    59.6 Uploads
    59.7 Downloads
    59.8 Attachment Access
    59.9 Attachment Security
    59.10 Public / Private Files
    59.11 LAB — Secure Document Attachment Feature
  STORY PROJECT — Document-Enabled Business Module

STORY XVI — POSTGRESQL & PERFORMANCE
  "The ORM sits on PostgreSQL. Performance starts with understanding both."
  Phase 60 — Odoo Database Structure
    60.1 Models → Tables
    60.2 Fields → Columns
    60.3 Many2many Relation Tables
    60.4 Foreign Keys
    60.5 Metadata Tables
    60.6 ir_model
    60.7 ir_model_fields
    60.8 Inspecting the Schema
  Phase 61 — PostgreSQL for Odoo Developers
    61.1 SELECT
    61.2 JOIN
    61.3 GROUP BY
    61.4 Aggregates
    61.5 CTE Concepts
    61.6 Constraints
    61.7 psql
    61.8 Database Inspection
  Phase 62 — Transactions & Concurrency
    62.1 Transactions
    62.2 Commit
    62.3 Rollback
    62.4 Isolation
    62.5 Locks
    62.6 Deadlocks
    62.7 Concurrent Updates
    62.8 Savepoints
  Phase 63 — Indexes & Query Plans
    63.1 Indexes
    63.2 B-Tree Intuition
    63.3 Selectivity
    63.4 EXPLAIN
    63.5 EXPLAIN ANALYZE
    63.6 Query Plans
    63.7 Sequential Scan
    63.8 When to Add an Index
  Phase 64 — ORM Performance
    64.1 Query Counts
    64.2 Batch Operations
    64.3 Prefetch
    64.4 Cache
    64.5 N+1 Problems
    64.6 Computed Field Costs
    64.7 Stored Computations
    64.8 Efficient Domains
    64.9 Algorithms & Complexity
    64.10 Database Indexes from Models
  Phase 65 — Profiling & Optimization
    65.1 Odoo Profiler
    65.2 SQL Profiling
    65.3 Query Collector
    65.4 Periodic Collector
    65.5 Finding Bottlenecks
    65.6 Measuring Before Optimizing
    65.7 Benchmarking
    65.8 Performance Regression
  STORY PROJECT — Optimize a Slow Odoo Application

STORY XVII — TESTING & DEBUGGING
  "If you cannot prove it, you do not ship it."
  Phase 66 — Odoo Logging & Debugging
    66.1 Log Levels
    66.2 Server Logs
    66.3 Stack Traces
    66.4 Python Debugger
    66.5 Breakpoints
    66.6 Odoo Shell
    66.7 Developer Tools
    66.8 Reproducing Bugs
    66.9 Root Cause Analysis
  Phase 67 — Python Tests
    67.1 Test Module Structure
    67.2 TransactionCase
    67.3 Savepoint / Transaction Concepts
    67.4 Test Data
    67.5 setUp()
    67.6 Assertions
    67.7 Test Tags
    67.8 Running Selected Tests
  Phase 68 — Testing Business Logic
    68.1 CRUD Tests
    68.2 Computed Field Tests
    68.3 Constraint Tests
    68.4 Workflow Tests
    68.5 Cron Tests
    68.6 Edge Cases
    68.7 Regression Tests
  Phase 69 — Testing Security
    69.1 User Context
    69.2 ACL Tests
    69.3 Record Rule Tests
    69.4 sudo() Tests
    69.5 Multi-Company Tests
    69.6 Portal Access Tests
    69.7 Permission Regression
  Phase 70 — Web & Frontend Testing
    70.1 HTTP Tests
    70.2 Controller Tests
    70.3 browser_js
    70.4 Test Tours
    70.5 Integration Tours
    70.6 JavaScript Unit Tests
    70.7 HOOT
    70.8 @odoo/hoot-dom
    70.9 Web Test Helpers
    70.10 Mock Server
    70.11 Mocking RPC
    70.12 End-to-End Flows
  Phase 71 — Performance & Regression Testing
    71.1 Query Count Tests
    71.2 assertQueryCount()
    71.3 --log-sql
    71.4 Performance Budgets
    71.5 Regression Detection
    71.6 Repeatable Benchmarks
  STORY PROJECT — Fully Tested Odoo Module

STORY XVIII — CLI & DEVELOPER TOOLING
  "Your terminal is part of the Odoo toolbox."
  Phase 72 — Odoo CLI & Developer Commands
    72.1 odoo-bin / odoo
    72.2 Configuration File
    72.3 Database Selection
    72.4 -i Install
    72.5 -u Upgrade
    72.6 --addons-path
    72.7 --dev
    72.8 --log-level
    72.9 --log-sql
    72.10 --test-enable
    72.11 --test-tags
    72.12 --stop-after-init
    72.13 shell
    72.14 scaffold
    72.15 LAB — Complete CLI Development Workflow
  STORY PROJECT — Command-Line Odoo Workflow

STORY XIX — DEPLOYMENT & OPERATIONS
  "A module that cannot be deployed and restored is not finished."
  Phase 73 — Odoo Configuration
    73.1 odoo.conf
    73.2 addons_path
    73.3 db_host
    73.4 db_port
    73.5 db_user
    73.6 Database Filters
    73.7 Logging
    73.8 Proxy Mode
    73.9 Secrets
    73.10 Environment-Specific Configuration
  Phase 74 — Linux Deployment
    74.1 Linux User
    74.2 Filesystem Layout
    74.3 Python Environment
    74.4 Source Deployment
    74.5 Addons
    74.6 File Permissions
    74.7 systemd
    74.8 Service Management
  Phase 75 — Reverse Proxy & HTTPS
    75.1 Reverse Proxy
    75.2 Nginx
    75.3 Domains
    75.4 DNS
    75.5 TLS / HTTPS
    75.6 Proxy Headers
    75.7 WebSocket / Long-Lived Connections
    75.8 Secure Production Exposure
  Phase 76 — Workers & Production Configuration
    76.1 Development Mode
    76.2 Multi-Processing
    76.3 Workers
    76.4 Worker Sizing
    76.5 CPU Considerations
    76.6 Memory Limits
    76.7 Request Time Limits
    76.8 Cron Workers
    76.9 Production Configuration
  Phase 77 — PostgreSQL Operations
    77.1 Database Users
    77.2 Connections
    77.3 Connection Limits
    77.4 Maintenance
    77.5 VACUUM Concepts
    77.6 ANALYZE
    77.7 Monitoring
    77.8 Basic Tuning
  Phase 78 — Backups & Disaster Recovery
    78.1 Database Backup
    78.2 Filestore Backup
    78.3 Configuration Backup
    78.4 Restore
    78.5 Backup Scheduling
    78.6 Off-Site Backups
    78.7 Recovery Testing
    78.8 Disaster Recovery Plan
  Phase 79 — Odoo.sh
    79.1 Odoo.sh Architecture
    79.2 Projects
    79.3 Git Integration
    79.4 Branches
    79.5 Development
    79.6 Staging
    79.7 Production
    79.8 Builds
    79.9 Logs
    79.10 Shell Access
    79.11 Database Management
  Phase 80 — Docker for Odoo
    80.1 Images
    80.2 Containers
    80.3 Volumes
    80.4 Networking
    80.5 Environment Variables
    80.6 Odoo Container
    80.7 PostgreSQL Container
    80.8 Persistent Filestore
    80.9 Docker Compose
  Phase 81 — Monitoring & Production Troubleshooting
    81.1 Application Logs
    81.2 PostgreSQL Logs
    81.3 CPU
    81.4 Memory
    81.5 Disk
    81.6 Database Connections
    81.7 Slow Requests
    81.8 Failed Jobs
    81.9 Error Monitoring
    81.10 Incident Diagnosis
  STORY PROJECT — Deploy an Odoo System

STORY XX — UPGRADES, MIGRATIONS & MAINTENANCE
  "Production code must survive tomorrow's Odoo version."
  Phase 82 — Module Versioning
    82.1 Manifest Version
    82.2 Semantic Change Thinking
    82.3 Release Changes
    82.4 Compatibility
    82.5 Dependencies
    82.6 Version Control Strategy
  Phase 83 — Schema Changes
    83.1 Adding Fields
    83.2 Renaming Fields
    83.3 Changing Field Types
    83.4 Changing Relationships
    83.5 Removing Fields
    83.6 Constraints
    83.7 Safe Schema Evolution
  Phase 84 — Data Migrations
    84.1 Migration Scripts
    84.2 Pre-Migration
    84.3 Post-Migration
    84.4 Transforming Records
    84.5 Renaming Data
    84.6 Preserving Relationships
    84.7 Validation
    84.8 Large Dataset Considerations
    84.9 Rollback Planning
  Phase 85 — Odoo Version Upgrades
    85.1 Version Differences
    85.2 Deprecated APIs
    85.3 Framework Changes
    85.4 Module Compatibility
    85.5 Database Upgrade
    85.6 Upgrade Scripts
    85.7 Upgrade Utilities
    85.8 Frontend Migration
    85.9 Post-Upgrade Testing
  Phase 86 — Module Hooks
    86.1 Hooks Concept
    86.2 pre_init_hook
    86.3 post_init_hook
    86.4 uninstall_hook
    86.5 Environment During Hooks
    86.6 Appropriate Uses
    86.7 Avoiding Hook Abuse
  Phase 87 — Upgrade-Safe Customization
    87.1 Never Modify Core Directly
    87.2 Extension Points
    87.3 Model Inheritance
    87.4 View Inheritance
    87.5 Stable Overrides
    87.6 Dependency Management
    87.7 Avoiding Hard-Coding
    87.8 Future-Proof Design
  Phase 88 — Legacy Module Maintenance
    88.1 Read Before Changing
    88.2 Trace Dependencies
    88.3 Trace Execution
    88.4 Reproduce Existing Behavior
    88.5 Characterization Tests
    88.6 Backward Compatibility
    88.7 Safe Refactoring
    88.8 Deprecation
  STORY PROJECT — Upgrade an Existing Odoo Module

STORY XXI — FUNCTIONAL ODOO FOR DEVELOPERS
  "An Odoo engineer should understand the business flows their code is changing."
  Phase 89 — Core Functional Applications
    89.1 Contacts
    89.2 CRM
    89.3 Sales
    89.4 Purchase
    89.5 Inventory
    89.6 Accounting Concepts
    89.7 Invoicing
    89.8 Employees
    89.9 Recruitment
    89.10 Time Off
    89.11 Projects
    89.12 Timesheets
    89.13 Manufacturing
    89.14 Maintenance
    89.15 Helpdesk Concept
    89.16 Website
    89.17 eCommerce
    89.18 Point of Sale
    89.19 End-to-End Business Flow
  STORY PROJECT — Trace a Company End-to-End

STORY XXII — REAL ODOO ENGINEERING
  "Judge trade-offs before you write models."
  Phase 90 — Requirements → ERP Design
    90.1 Stakeholder Interviews
    90.2 Business Requirements
    90.3 Functional Requirements
    90.4 Non-Functional Requirements
    90.5 Business Process Mapping
    90.6 As-Is Process
    90.7 To-Be Process
    90.8 Data Requirements
    90.9 Roles
    90.10 Permissions
    90.11 Acceptance Criteria
  Phase 91 — Odoo Solution Architecture
    91.1 Module Boundaries
    91.2 Model Ownership
    91.3 Dependencies
    91.4 Shared Models
    91.5 Integration Boundaries
    91.6 Security Architecture
    91.7 Multi-Company Architecture
    91.8 Data Flow
    91.9 Reusability
  Phase 92 — Configuration vs Customization vs Integration
    92.1 Standard Odoo
    92.2 Configuration
    92.3 Studio Concepts
    92.4 Custom Module
    92.5 Third-Party Module
    92.6 External Integration
    92.7 Data Ownership
    92.8 Upgrade Costs
    92.9 Maintenance Costs
    92.10 Choosing the Simplest Correct Solution
  Phase 93 — Maintainable Odoo Code
    93.1 Naming
    93.2 Small Methods
    93.3 Separation of Concerns
    93.4 Cohesion
    93.5 Low Coupling
    93.6 Reusable Mixins
    93.7 Configuration over Hard-Coding
    93.8 Constants
    93.9 Error Handling
    93.10 Logging
    93.11 Documentation
    93.12 Upgrade Safety
  Phase 94 — Git, Collaboration & Code Review
    94.1 Repository
    94.2 Branches
    94.3 Commits
    94.4 Pull Requests
    94.5 Merge
    94.6 Rebase Concepts
    94.7 Release Branches
    94.8 Code Review for Correctness
    94.9 Security Review
    94.10 ORM Performance Review
    94.11 Upgrade-Safety Review
    94.12 Test Review
    94.13 Documentation Review
  STORY PROJECT — Design a Production ERP Solution

STORY XXIII — GRAND ODOO CAPSTONE
  "Stop learning isolated features. Engineer the whole ERP."
  Phase 95 — Production ERP Capstone
    95.1 Problem Discovery
    95.2 Business Requirements
    95.3 Process Maps
    95.4 Architecture
    95.5 Module Boundaries
    95.6 Data Models
    95.7 Relational Design
    95.8 ORM
    95.9 Business Rules
    95.10 Security
    95.11 Multi-Company
    95.12 Views
    95.13 Workflows
    95.14 Wizards
    95.15 Chatter & Activities
    95.16 Automation
    95.17 Reports
    95.18 Attachments
    95.19 Translations
    95.20 Website
    95.21 Portal
    95.22 API
    95.23 Webhooks
    95.24 External Integration
    95.25 OWL Dashboard
    95.26 PostgreSQL Optimization
    95.27 Automated Tests
    95.28 Security Tests
    95.29 Performance Tests
    95.30 Migration Strategy
    95.31 Backup Strategy
    95.32 Deployment
    95.33 Monitoring
    95.34 README
    95.35 Architecture Diagram
    95.36 Technical Documentation
  STORY PROJECT — COMPLETE BUSINESS ERP

STORY XXIV — INTERVIEW, PORTFOLIO & JOB READINESS
  "Now prove that you can understand, build, debug and maintain Odoo."
  Phase 96 — Odoo Engineer Job Readiness
    96.1 ERP Fundamentals
    96.2 Odoo Functional Knowledge
    96.3 Odoo Architecture
    96.4 Module Anatomy
    96.5 Python
    96.6 Models
    96.7 Fields
    96.8 Relationships
    96.9 ORM
    96.10 Recordsets
    96.11 Domains
    96.12 Computed Fields
    96.13 Constraints
    96.14 Workflows
    96.15 XML
    96.16 Views
    96.17 Actions
    96.18 Security
    96.19 Multi-Company
    96.20 Inheritance
    96.21 Mixins
    96.22 Wizards
    96.23 Cron / Automation
    96.24 Chatter
    96.25 QWeb / Reports
    96.26 Internationalization
    96.27 Controllers
    96.28 APIs
    96.29 Webhooks
    96.30 Integrations
    96.31 OWL
    96.32 JavaScript Framework
    96.33 PostgreSQL
    96.34 Performance
    96.35 Testing
    96.36 Debugging
    96.37 CLI
    96.38 Deployment
    96.39 Odoo.sh
    96.40 Docker
    96.41 Upgrades
    96.42 Migrations
    96.43 Requirements Analysis
    96.44 Solution Architecture
    96.45 Live Module Design
    96.46 Live Debugging
    96.47 Code Review
    96.48 Portfolio Walkthrough
    96.49 PORTFOLIO PROJECT 1 — Library / Asset Management
    96.50 PORTFOLIO PROJECT 2 — HR / Leave Approval
    96.51 PORTFOLIO PROJECT 3 — Sales Customization
    96.52 PORTFOLIO PROJECT 4 — Inventory Extension
    96.53 PORTFOLIO PROJECT 5 — API / SaaS Integration
    96.54 PORTFOLIO PROJECT 6 — OWL Dashboard
    96.55 PORTFOLIO PROJECT 7 — Customer Portal
    96.56 FINAL MOCK INTERVIEW

```

| Story | Question it answers |
| --- | --- |
| I. Understand the Business Before the Code | Odoo exists to run businesses. First understand the problem it solves. |
| II. How Odoo Actually Works | Before changing Odoo, understand what runs underneath it. |
| III. Your First Odoo Module | Odoo changes should live inside modules, not scattered patches. |
| IV. Modeling Business Data | A business system begins with correctly modeled information. |
| V. Mastering the Odoo ORM | Talk to business records through the ORM — safely, in batches, with clear intent. |
| VI. Business Rules & Workflows | Business software enforces what is legal — not only what is possible. |
| VII. Building the User Interface | Users never see your Python — they see views, actions, and menus. |
| VIII. Security & Multi-Company | If the wrong user can see or change the wrong row, the module is unfinished. |
| IX. Extending Existing Odoo | Prefer extending official apps over forking them. |
| X. Advanced Business Features | Wizards, sequences, cron, chatter, reports, and data files run real operations. |
| XI. Internationalization & Localization | Real ERP software serves people in different languages, countries and accounting environments. |
| XII. Web Development with Odoo | Controllers, website, and portal put Odoo on the public and customer web. |
| XIII. APIs & Integrations | Other systems will call you — and you will call them. Contracts first. |
| XIV. Modern Odoo Frontend | Standard views go far. Custom interactive experiences require the web framework. |
| XV. Files, Attachments & Media | Business documents are files — store and secure them like business data. |
| XVI. PostgreSQL & Performance | The ORM sits on PostgreSQL. Performance starts with understanding both. |
| XVII. Testing & Debugging | If you cannot prove it, you do not ship it. |
| XVIII. CLI & Developer Tooling | Your terminal is part of the Odoo toolbox. |
| XIX. Deployment & Operations | A module that cannot be deployed and restored is not finished. |
| XX. Upgrades, Migrations & Maintenance | Production code must survive tomorrow's Odoo version. |
| XXI. Functional Odoo for Developers | An Odoo engineer should understand the business flows their code is changing. |
| XXII. Real Odoo Engineering | Judge trade-offs before you write models. |
| XXIII. Grand Odoo Capstone | Stop learning isolated features. Engineer the whole ERP. |
| XXIV. Interview, Portfolio & Job Readiness | Now prove that you can understand, build, debug and maintain Odoo. |

<a id="phase-index"></a>

### Phase Index

| # | Phase | Story | Move on when you can... |
| --- | --- | --- | --- |
| 01 | [What Is ERP?](#phase-1---what-is-erp) | I | Complete: PRACTICE — Model a Small Company |
| 02 | [Understanding Odoo](#phase-2---understanding-odoo) | I | Complete: Shared Business Records |
| 03 | [Core Business Applications](#phase-3---core-business-applications) | I | Complete: PRACTICE — Trace Quote → Delivery → Invoice |
| 04 | [Odoo Architecture](#phase-4---odoo-architecture) | II | Complete: LAB — Trace Browser → Server → Database |
| 05 | [Development Environment](#phase-5---development-environment) | II | Complete: LAB — Run Odoo Locally |
| 06 | [Odoo Source Code Structure](#phase-6---odoo-source-code-structure) | II | Complete: PRACTICE — Navigate the Odoo Repository |
| 07 | [Module Anatomy](#phase-7---module-anatomy) | III | Complete: MINI PROJECT — Hello Odoo |
| 08 | [Module Lifecycle](#phase-8---module-lifecycle) | III | Complete: LAB — Install & Upgrade Your Module |
| 09 | [Models](#phase-9---models) | IV | Complete: MINI PROJECT — Library Model |
| 10 | [Fields](#phase-10---fields) | IV | Complete: PRACTICE — Model Business Records |
| 11 | [Relationships](#phase-11---relationships) | IV | Complete: DESIGN PROJECT — Business Data Graph |
| 12 | [Computed & Dynamic Fields](#phase-12---computed-dynamic-fields) | IV | Complete: LAB — Automatic Business Calculations |
| 13 | [Environment & Recordsets](#phase-13---environment-recordsets) | V | Complete: with_env() |
| 14 | [CRUD](#phase-14---crud) | V | Complete: CODING PRACTICE — CRUD Operations |
| 15 | [Domains & Searching](#phase-15---domains-searching) | V | Complete: PRACTICE — Business Queries |
| 16 | [Recordset Operations](#phase-16---recordset-operations) | V | Complete: Batch Operations |
| 17 | [ORM Internals & Performance](#phase-17---orm-internals-performance) | V | Complete: LAB — Optimize ORM Code |
| 18 | [Business Methods](#phase-18---business-methods) | VI | Complete: Business Actions |
| 19 | [Constraints & Validation](#phase-19---constraints-validation) | VI | Complete: LAB — Protect Business Rules |
| 20 | [Onchange & UI Logic](#phase-20---onchange-ui-logic) | VI | Complete: Onchange vs Compute |
| 21 | [States & Workflows](#phase-21---states-workflows) | VI | Complete: PROJECT — Approval Workflow |
| 22 | [XML Foundations](#phase-22---xml-foundations) | VII | Complete: XML Loading |
| 23 | [Form & List Views](#phase-23---form-list-views) | VII | Complete: MINI PROJECT — CRUD Interface |
| 24 | [Search, Kanban & Analytical Views](#phase-24---search-kanban-analytical-views) | VII | Complete: PRACTICE — Multiple Views of Same Data |
| 25 | [Actions & Menus](#phase-25---actions-menus) | VII | Complete: Navigation |
| 26 | [View Inheritance](#phase-26---view-inheritance) | VII | Complete: Debugging Inherited Views |
| 27 | [Users & Groups](#phase-27---users-groups) | VIII | Complete: Permission Design |
| 28 | [Access Control Lists](#phase-28---access-control-lists) | VIII | Complete: Additive Permissions |
| 29 | [Record Rules](#phase-29---record-rules) | VIII | Complete: Row-Level Security |
| 30 | [Security Context & Safe Privilege](#phase-30---security-context-safe-privilege) | VIII | Complete: Least Privilege |
| 31 | [Multi-Company Development](#phase-31---multi-company-development) | VIII | Complete: Cross-Company Security |
| 32 | [Model Inheritance](#phase-32---model-inheritance) | IX | Complete: Multiple Inheritance |
| 33 | [Delegation & Mixins](#phase-33---delegation-mixins) | IX | Complete: Composition |
| 34 | [View Inheritance in Real Modules](#phase-34---view-inheritance-in-real-modules) | IX | Complete: Debugging Inheritance Conflicts |
| 35 | [Extending Official Applications](#phase-35---extending-official-applications) | IX | Complete: Upgrade-Safe Customization |
| 36 | [Wizards & TransientModel](#phase-36---wizards-transientmodel) | X | Complete: MINI PROJECT — Bulk Processing Wizard |
| 37 | [Sequences](#phase-37---sequences) | X | Complete: Business References |
| 38 | [Scheduled Actions / Cron](#phase-38---scheduled-actions-cron) | X | Complete: Cron Security |
| 39 | [Automation & Server Actions](#phase-39---automation-server-actions) | X | Complete: When to Use Code Instead |
| 40 | [Mail, Chatter & Activities](#phase-40---mail-chatter-activities) | X | Complete: Sending Mail |
| 41 | [Reports & QWeb](#phase-41---reports-qweb) | X | Complete: Report Attachments |
| 42 | [Data Files, Import & Export](#phase-42---data-files-import-export) | X | Complete: XML Data Operations |
| 43 | [Internationalization & Localization](#phase-43---internationalization-localization) | XI | Complete: PRACTICE — Translate a Custom Module |
| 44 | [HTTP Controllers](#phase-44---http-controllers) | XII | Complete: Controller Security |
| 45 | [Website Development](#phase-45---website-development) | XII | Complete: Multi-Website Concept |
| 46 | [Portal Development](#phase-46---portal-development) | XII | Complete: Access Tokens |
| 47 | [External API Concepts](#phase-47---external-api-concepts) | XIII | Complete: API Contracts |
| 48 | [External Odoo Access](#phase-48---external-odoo-access) | XIII | Complete: Version Compatibility |
| 49 | [Building API Endpoints](#phase-49---building-api-endpoints) | XIII | Complete: API Versioning Concepts |
| 50 | [Webhooks](#phase-50---webhooks) | XIII | Complete: Audit Logging |
| 51 | [Third-Party Integrations](#phase-51---third-party-integrations) | XIII | Complete: Integration Logs |
| 52 | [Payment Integrations](#phase-52---payment-integrations) | XIII | Complete: Refund Concepts |
| 53 | [Odoo JavaScript Architecture](#phase-53---odoo-javascript-architecture) | XIV | Complete: Frontend Debugging |
| 54 | [OWL Fundamentals](#phase-54---owl-fundamentals) | XIV | Complete: Reactive UI |
| 55 | [Services, Registries & Hooks](#phase-55---services-registries-hooks) | XIV | Complete: Custom Hooks |
| 56 | [Assets & Frontend Resources](#phase-56---assets-frontend-resources) | XIV | Complete: Asset Debugging |
| 57 | [Extending & Patching the Web Client](#phase-57---extending-patching-the-web-client) | XIV | Complete: When Not to Patch |
| 58 | [Client Actions & Advanced Frontend](#phase-58---client-actions-advanced-frontend) | XIV | Complete: Responsive Odoo UI |
| 59 | [Attachments & Filestore](#phase-59---attachments-filestore) | XV | Complete: LAB — Secure Document Attachment Feature |
| 60 | [Odoo Database Structure](#phase-60---odoo-database-structure) | XVI | Complete: Inspecting the Schema |
| 61 | [PostgreSQL for Odoo Developers](#phase-61---postgresql-for-odoo-developers) | XVI | Complete: Database Inspection |
| 62 | [Transactions & Concurrency](#phase-62---transactions-concurrency) | XVI | Complete: Savepoints |
| 63 | [Indexes & Query Plans](#phase-63---indexes-query-plans) | XVI | Complete: When to Add an Index |
| 64 | [ORM Performance](#phase-64---orm-performance) | XVI | Complete: Database Indexes from Models |
| 65 | [Profiling & Optimization](#phase-65---profiling-optimization) | XVI | Complete: Performance Regression |
| 66 | [Odoo Logging & Debugging](#phase-66---odoo-logging-debugging) | XVII | Complete: Root Cause Analysis |
| 67 | [Python Tests](#phase-67---python-tests) | XVII | Complete: Running Selected Tests |
| 68 | [Testing Business Logic](#phase-68---testing-business-logic) | XVII | Complete: Regression Tests |
| 69 | [Testing Security](#phase-69---testing-security) | XVII | Complete: Permission Regression |
| 70 | [Web & Frontend Testing](#phase-70---web-frontend-testing) | XVII | Complete: End-to-End Flows |
| 71 | [Performance & Regression Testing](#phase-71---performance-regression-testing) | XVII | Complete: Repeatable Benchmarks |
| 72 | [Odoo CLI & Developer Commands](#phase-72---odoo-cli-developer-commands) | XVIII | Complete: LAB — Complete CLI Development Workflow |
| 73 | [Odoo Configuration](#phase-73---odoo-configuration) | XIX | Complete: Environment-Specific Configuration |
| 74 | [Linux Deployment](#phase-74---linux-deployment) | XIX | Complete: Service Management |
| 75 | [Reverse Proxy & HTTPS](#phase-75---reverse-proxy-https) | XIX | Complete: Secure Production Exposure |
| 76 | [Workers & Production Configuration](#phase-76---workers-production-configuration) | XIX | Complete: Production Configuration |
| 77 | [PostgreSQL Operations](#phase-77---postgresql-operations) | XIX | Complete: Basic Tuning |
| 78 | [Backups & Disaster Recovery](#phase-78---backups-disaster-recovery) | XIX | Complete: Disaster Recovery Plan |
| 79 | [Odoo.sh](#phase-79) | XIX | Complete: Database Management |
| 80 | [Docker for Odoo](#phase-80---docker-for-odoo) | XIX | Complete: Docker Compose |
| 81 | [Monitoring & Production Troubleshooting](#phase-81---monitoring-production-troubleshooting) | XIX | Complete: Incident Diagnosis |
| 82 | [Module Versioning](#phase-82---module-versioning) | XX | Complete: Version Control Strategy |
| 83 | [Schema Changes](#phase-83---schema-changes) | XX | Complete: Safe Schema Evolution |
| 84 | [Data Migrations](#phase-84---data-migrations) | XX | Complete: Rollback Planning |
| 85 | [Odoo Version Upgrades](#phase-85---odoo-version-upgrades) | XX | Complete: Post-Upgrade Testing |
| 86 | [Module Hooks](#phase-86---module-hooks) | XX | Complete: Avoiding Hook Abuse |
| 87 | [Upgrade-Safe Customization](#phase-87---upgrade-safe-customization) | XX | Complete: Future-Proof Design |
| 88 | [Legacy Module Maintenance](#phase-88---legacy-module-maintenance) | XX | Complete: Deprecation |
| 89 | [Core Functional Applications](#phase-89---core-functional-applications) | XXI | Complete: End-to-End Business Flow |
| 90 | [Requirements → ERP Design](#phase-90---requirements-erp-design) | XXII | Complete: Acceptance Criteria |
| 91 | [Odoo Solution Architecture](#phase-91---odoo-solution-architecture) | XXII | Complete: Reusability |
| 92 | [Configuration vs Customization vs Integration](#phase-92---configuration-vs-customization-vs-integration) | XXII | Complete: Choosing the Simplest Correct Solution |
| 93 | [Maintainable Odoo Code](#phase-93---maintainable-odoo-code) | XXII | Complete: Upgrade Safety |
| 94 | [Git, Collaboration & Code Review](#phase-94---git-collaboration-code-review) | XXII | Complete: Documentation Review |
| 95 | [Production ERP Capstone](#phase-95---production-erp-capstone) | XXIII | Complete: Technical Documentation |
| 96 | [Odoo Engineer Job Readiness](#phase-96---odoo-engineer-job-readiness) | XXIV | Complete: FINAL MOCK INTERVIEW |

### Hierarchy (Stories → Phases → Lessons)

#### Story I — Understand the Business Before the Code

*Odoo exists to run businesses. First understand the problem it solves.*

- **[Phase 1 — What Is ERP?](#phase-1---what-is-erp)**
  - 1.1 Business Processes
  - 1.2 Departments
  - 1.3 Cross-Department Workflows
  - 1.4 Master Data
  - 1.5 Transactions
  - 1.6 Single Source of Truth
  - 1.7 ERP vs CRM
  - 1.8 ERP vs Standalone Business Software
  - 1.9 PRACTICE — Model a Small Company
- **[Phase 2 — Understanding Odoo](#phase-2---understanding-odoo)**
  - 2.1 What Odoo Is
  - 2.2 Odoo Ecosystem
  - 2.3 Community Edition
  - 2.4 Enterprise Edition
  - 2.5 Odoo Apps
  - 2.6 Modules / Addons
  - 2.7 Users
  - 2.8 Companies
  - 2.9 Shared Business Records
- **[Phase 3 — Core Business Applications](#phase-3---core-business-applications)**
  - 3.1 Contacts
  - 3.2 CRM
  - 3.3 Sales
  - 3.4 Purchase
  - 3.5 Inventory
  - 3.6 Accounting
  - 3.7 Employees / HR
  - 3.8 Projects
  - 3.9 Manufacturing
  - 3.10 PRACTICE — Trace Quote → Delivery → Invoice
- *Story project:* Design a Company's ERP Flow

#### Story II — How Odoo Actually Works

*Before changing Odoo, understand what runs underneath it.*

- **[Phase 4 — Odoo Architecture](#phase-4---odoo-architecture)**
  - 4.1 Three-Tier Architecture
  - 4.2 Browser / Web Client
  - 4.3 Odoo Application Server
  - 4.4 PostgreSQL
  - 4.5 Filestore
  - 4.6 Addons
  - 4.7 Registry
  - 4.8 ORM
  - 4.9 HTTP Layer
  - 4.10 LAB — Trace Browser → Server → Database
- **[Phase 5 — Development Environment](#phase-5---development-environment)**
  - 5.1 Python Environment
  - 5.2 PostgreSQL Setup
  - 5.3 Odoo Source
  - 5.4 Configuration File
  - 5.5 addons_path
  - 5.6 Custom Addons Directory
  - 5.7 Developer Mode
  - 5.8 Debug Assets Mode
  - 5.9 Logging
  - 5.10 LAB — Run Odoo Locally
- **[Phase 6 — Odoo Source Code Structure](#phase-6---odoo-source-code-structure)**
  - 6.1 odoo/
  - 6.2 addons/
  - 6.3 Core Framework
  - 6.4 Models
  - 6.5 HTTP
  - 6.6 Services
  - 6.7 Tools
  - 6.8 Registry
  - 6.9 Reading Official Addons
  - 6.10 PRACTICE — Navigate the Odoo Repository
- *Story project:* Local Odoo Development Workspace

#### Story III — Your First Odoo Module

*Odoo changes should live inside modules, not scattered patches.*

- **[Phase 7 — Module Anatomy](#phase-7---module-anatomy)**
  - 7.1 Addon Directory
  - 7.2 __manifest__.py
  - 7.3 __init__.py
  - 7.4 models/
  - 7.5 views/
  - 7.6 security/
  - 7.7 data/
  - 7.8 demo/
  - 7.9 static/
  - 7.10 controllers/
  - 7.11 wizard/
  - 7.12 report/
  - 7.13 i18n/
  - 7.14 MINI PROJECT — Hello Odoo
- **[Phase 8 — Module Lifecycle](#phase-8---module-lifecycle)**
  - 8.1 Module Discovery
  - 8.2 Dependencies
  - 8.3 Installation
  - 8.4 Initialization
  - 8.5 Upgrade
  - 8.6 Uninstall
  - 8.7 Data Load Order
  - 8.8 Application Flag
  - 8.9 Installable Flag
  - 8.10 LAB — Install & Upgrade Your Module
- *Story project:* First Complete Addon

#### Story IV — Modeling Business Data

*A business system begins with correctly modeled information.*

- **[Phase 9 — Models](#phase-9---models)**
  - 9.1 models.Model
  - 9.2 models.AbstractModel
  - 9.3 models.TransientModel
  - 9.4 _name
  - 9.5 _description
  - 9.6 _rec_name
  - 9.7 _order
  - 9.8 Automatic Fields
  - 9.9 Reserved Field Names
  - 9.10 MINI PROJECT — Library Model
- **[Phase 10 — Fields](#phase-10---fields)**
  - 10.1 Char
  - 10.2 Text
  - 10.3 Html
  - 10.4 Integer
  - 10.5 Float
  - 10.6 Boolean
  - 10.7 Date
  - 10.8 Datetime
  - 10.9 Selection
  - 10.10 Monetary
  - 10.11 Binary
  - 10.12 Image
  - 10.13 Field Attributes
  - 10.14 PRACTICE — Model Business Records
- **[Phase 11 — Relationships](#phase-11---relationships)**
  - 11.1 Many2one
  - 11.2 One2many
  - 11.3 Many2many
  - 11.4 Related Fields
  - 11.5 Relational Commands
  - 11.6 Command.create()
  - 11.7 Command.update()
  - 11.8 Command.delete()
  - 11.9 Command.unlink()
  - 11.10 Command.link()
  - 11.11 Command.clear()
  - 11.12 Command.set()
  - 11.13 DESIGN PROJECT — Business Data Graph
- **[Phase 12 — Computed & Dynamic Fields](#phase-12---computed-dynamic-fields)**
  - 12.1 compute
  - 12.2 @api.depends
  - 12.3 Stored Computed Fields
  - 12.4 Non-Stored Fields
  - 12.5 inverse
  - 12.6 Custom Search Methods
  - 12.7 Related Fields
  - 12.8 Default Values
  - 12.9 copy
  - 12.10 LAB — Automatic Business Calculations
- *Story project:* Business Data Management Module

#### Story V — Mastering the Odoo ORM

*Talk to business records through the ORM — safely, in batches, with clear intent.*

- **[Phase 13 — Environment & Recordsets](#phase-13---environment-recordsets)**
  - 13.1 env
  - 13.2 self
  - 13.3 Recordsets
  - 13.4 Empty Recordsets
  - 13.5 Singleton Records
  - 13.6 env.user
  - 13.7 env.company
  - 13.8 env.companies
  - 13.9 Context
  - 13.10 with_context()
  - 13.11 with_user()
  - 13.12 with_company()
  - 13.13 sudo()
  - 13.14 with_env()
- **[Phase 14 — CRUD](#phase-14---crud)**
  - 14.1 create()
  - 14.2 browse()
  - 14.3 search()
  - 14.4 search_fetch()
  - 14.5 read()
  - 14.6 write()
  - 14.7 unlink()
  - 14.8 copy()
  - 14.9 exists()
  - 14.10 CODING PRACTICE — CRUD Operations
- **[Phase 15 — Domains & Searching](#phase-15---domains-searching)**
  - 15.1 Domain Syntax
  - 15.2 Comparison Operators
  - 15.3 in / not in
  - 15.4 like / ilike
  - 15.5 child_of / parent_of
  - 15.6 AND
  - 15.7 OR
  - 15.8 NOT
  - 15.9 search_count()
  - 15.10 search_read()
  - 15.11 Ordering
  - 15.12 Limit / Offset
  - 15.13 PRACTICE — Business Queries
- **[Phase 16 — Recordset Operations](#phase-16---recordset-operations)**
  - 16.1 Iteration
  - 16.2 filtered()
  - 16.3 filtered_domain()
  - 16.4 mapped()
  - 16.5 sorted()
  - 16.6 ensure_one()
  - 16.7 Recordset Union
  - 16.8 Recordset Intersection
  - 16.9 Recordset Difference
  - 16.10 Batch Operations
- **[Phase 17 — ORM Internals & Performance](#phase-17---orm-internals-performance)**
  - 17.1 Prefetching
  - 17.2 ORM Cache
  - 17.3 Transactions
  - 17.4 Flush
  - 17.5 Cache Invalidation
  - 17.6 N+1 Queries
  - 17.7 Batch Creation
  - 17.8 Aggregation
  - 17.9 read_group()
  - 17.10 Raw SQL
  - 17.11 ORM vs SQL
  - 17.12 Security Implications of SQL
  - 17.13 LAB — Optimize ORM Code
- *Story project:* Data-Heavy Odoo Module

#### Story VI — Business Rules & Workflows

*Business software enforces what is legal — not only what is possible.*

- **[Phase 18 — Business Methods](#phase-18---business-methods)**
  - 18.1 Model Methods
  - 18.2 Recordset Methods
  - 18.3 @api.model
  - 18.4 @api.model_create_multi
  - 18.5 create() Override
  - 18.6 write() Override
  - 18.7 unlink() Override
  - 18.8 super()
  - 18.9 Return Values
  - 18.10 Business Actions
- **[Phase 19 — Constraints & Validation](#phase-19---constraints-validation)**
  - 19.1 Python Constraints
  - 19.2 @api.constrains
  - 19.3 SQL Constraints
  - 19.4 ValidationError
  - 19.5 UserError
  - 19.6 AccessError
  - 19.7 LAB — Protect Business Rules
- **[Phase 20 — Onchange & UI Logic](#phase-20---onchange-ui-logic)**
  - 20.1 @api.onchange
  - 20.2 Defaults
  - 20.3 default_get()
  - 20.4 Context Defaults
  - 20.5 Dynamic Forms
  - 20.6 Onchange vs Compute
- **[Phase 21 — States & Workflows](#phase-21---states-workflows)**
  - 21.1 State Fields
  - 21.2 Draft
  - 21.3 Confirmed
  - 21.4 Approved
  - 21.5 Done
  - 21.6 Cancelled
  - 21.7 Legal State Transitions
  - 21.8 Buttons
  - 21.9 Workflow Validation
  - 21.10 PROJECT — Approval Workflow
- *Story project:* Business Approval System

#### Story VII — Building the User Interface

*Users never see your Python — they see views, actions, and menus.*

- **[Phase 22 — XML Foundations](#phase-22---xml-foundations)**
  - 22.1 XML Syntax
  - 22.2 <record>
  - 22.3 External IDs
  - 22.4 ref()
  - 22.5 eval
  - 22.6 Data Files
  - 22.7 XML Loading
- **[Phase 23 — Form & List Views](#phase-23---form-list-views)**
  - 23.1 Form Views
  - 23.2 List Views
  - 23.3 Field Display
  - 23.4 Buttons
  - 23.5 Groups
  - 23.6 Sheets
  - 23.7 Notebook
  - 23.8 Pages
  - 23.9 Widgets
  - 23.10 Decorations
  - 23.11 MINI PROJECT — CRUD Interface
- **[Phase 24 — Search, Kanban & Analytical Views](#phase-24---search-kanban-analytical-views)**
  - 24.1 Search Views
  - 24.2 Filters
  - 24.3 Group By
  - 24.4 Search Panels
  - 24.5 Kanban
  - 24.6 Calendar
  - 24.7 Pivot
  - 24.8 Graph
  - 24.9 Activity Views
  - 24.10 PRACTICE — Multiple Views of Same Data
- **[Phase 25 — Actions & Menus](#phase-25---actions-menus)**
  - 25.1 Actions
  - 25.2 Action Bindings
  - 25.3 Window Actions
  - 25.4 ir.actions.act_window
  - 25.5 URL Actions
  - 25.6 Server Actions
  - 25.7 Report Actions
  - 25.8 Client Actions
  - 25.9 Scheduled Actions
  - 25.10 Menus
  - 25.11 Context
  - 25.12 Domains
  - 25.13 Navigation
- **[Phase 26 — View Inheritance](#phase-26---view-inheritance)**
  - 26.1 inherit_id
  - 26.2 XPath
  - 26.3 position
  - 26.4 Attributes
  - 26.5 Adding Elements
  - 26.6 Replacing Elements
  - 26.7 Moving Elements
  - 26.8 View Resolution
  - 26.9 Debugging Inherited Views
- *Story project:* Complete Odoo Application UI

#### Story VIII — Security & Multi-Company

*If the wrong user can see or change the wrong row, the module is unfinished.*

- **[Phase 27 — Users & Groups](#phase-27---users-groups)**
  - 27.1 Users
  - 27.2 Groups
  - 27.3 Group Membership
  - 27.4 Implied Groups
  - 27.5 Application Categories
  - 27.6 Permission Design
- **[Phase 28 — Access Control Lists](#phase-28---access-control-lists)**
  - 28.1 ir.model.access
  - 28.2 ir.model.access.csv
  - 28.3 Read
  - 28.4 Write
  - 28.5 Create
  - 28.6 Unlink
  - 28.7 Group ACLs
  - 28.8 Additive Permissions
- **[Phase 29 — Record Rules](#phase-29---record-rules)**
  - 29.1 ir.rule
  - 29.2 domain_force
  - 29.3 Global Rules
  - 29.4 Group Rules
  - 29.5 Rule Composition
  - 29.6 Row-Level Security
- **[Phase 30 — Security Context & Safe Privilege](#phase-30---security-context-safe-privilege)**
  - 30.1 sudo()
  - 30.2 with_user()
  - 30.3 Access Checks
  - 30.4 check_access()
  - 30.5 Field Security
  - 30.6 View / Menu Groups
  - 30.7 Public Method Security
  - 30.8 Unsafe Raw SQL
  - 30.9 Security Bypass Risks
  - 30.10 Least Privilege
- **[Phase 31 — Multi-Company Development](#phase-31---multi-company-development)**
  - 31.1 res.company
  - 31.2 company_id
  - 31.3 company_ids
  - 31.4 env.company
  - 31.5 with_company()
  - 31.6 Company-Dependent Fields
  - 31.7 _check_company
  - 31.8 Multi-Company Domains
  - 31.9 Cross-Company Security
- *Story project:* Secure Multi-Company Module

#### Story IX — Extending Existing Odoo

*Prefer extending official apps over forking them.*

- **[Phase 32 — Model Inheritance](#phase-32---model-inheritance)**
  - 32.1 Extension Inheritance
  - 32.2 _inherit
  - 32.3 Classical Inheritance
  - 32.4 Method Overriding
  - 32.5 Field Extension
  - 32.6 super()
  - 32.7 Multiple Inheritance
- **[Phase 33 — Delegation & Mixins](#phase-33---delegation-mixins)**
  - 33.1 _inherits
  - 33.2 Delegated Fields
  - 33.3 Delegation vs Extension
  - 33.4 AbstractModel
  - 33.5 Mixins
  - 33.6 Reusable Model Behavior
  - 33.7 Composition
- **[Phase 34 — View Inheritance in Real Modules](#phase-34---view-inheritance-in-real-modules)**
  - 34.1 Finding Parent Views
  - 34.2 External IDs
  - 34.3 XPath Targeting
  - 34.4 Adding Fields
  - 34.5 Modifying Attributes
  - 34.6 Replacing Elements
  - 34.7 Debugging Inheritance Conflicts
- **[Phase 35 — Extending Official Applications](#phase-35---extending-official-applications)**
  - 35.1 res.partner
  - 35.2 sale.order
  - 35.3 purchase.order
  - 35.4 stock.picking
  - 35.5 account.move
  - 35.6 Existing Workflows
  - 35.7 Extension Points
  - 35.8 Upgrade-Safe Customization
- *Story project:* Customize an Official Odoo App

#### Story X — Advanced Business Features

*Wizards, sequences, cron, chatter, reports, and data files run real operations.*

- **[Phase 36 — Wizards & TransientModel](#phase-36---wizards-transientmodel)**
  - 36.1 TransientModel
  - 36.2 Wizard Fields
  - 36.3 Wizard Views
  - 36.4 target="new"
  - 36.5 active_id
  - 36.6 active_ids
  - 36.7 Context
  - 36.8 Multi-Step Wizards
  - 36.9 MINI PROJECT — Bulk Processing Wizard
- **[Phase 37 — Sequences](#phase-37---sequences)**
  - 37.1 ir.sequence
  - 37.2 next_by_code()
  - 37.3 Prefix
  - 37.4 Suffix
  - 37.5 Padding
  - 37.6 Date-Based Sequences
  - 37.7 Business References
- **[Phase 38 — Scheduled Actions / Cron](#phase-38---scheduled-actions-cron)**
  - 38.1 ir.cron
  - 38.2 Scheduling
  - 38.3 Cron Methods
  - 38.4 Batch Processing
  - 38.5 Progress
  - 38.6 Failure Handling
  - 38.7 Idempotency
  - 38.8 Cron Security
- **[Phase 39 — Automation & Server Actions](#phase-39---automation-server-actions)**
  - 39.1 Automated Actions
  - 39.2 Triggers
  - 39.3 Conditions
  - 39.4 Server Actions
  - 39.5 Evaluation Context
  - 39.6 Action Chaining
  - 39.7 When to Use Code Instead
- **[Phase 40 — Mail, Chatter & Activities](#phase-40---mail-chatter-activities)**
  - 40.1 mail.thread
  - 40.2 mail.activity.mixin
  - 40.3 Messages
  - 40.4 Followers
  - 40.5 Tracking
  - 40.6 Activities
  - 40.7 Notifications
  - 40.8 Email Templates
  - 40.9 Sending Mail
- **[Phase 41 — Reports & QWeb](#phase-41---reports-qweb)**
  - 41.1 QWeb
  - 41.2 QWeb Directives
  - 41.3 Report Actions
  - 41.4 Report Templates
  - 41.5 PDF Reports
  - 41.6 HTML Reports
  - 41.7 Paper Formats
  - 41.8 Custom Report Data
  - 41.9 Translatable Reports
  - 41.10 Barcodes
  - 41.11 Report Attachments
- **[Phase 42 — Data Files, Import & Export](#phase-42---data-files-import-export)**
  - 42.1 XML Data
  - 42.2 CSV Data
  - 42.3 External IDs
  - 42.4 env.ref()
  - 42.5 noupdate
  - 42.6 Demo Data
  - 42.7 Seed Data
  - 42.8 Import
  - 42.9 Export
  - 42.10 Data Load Order
  - 42.11 XML Data Operations
- *Story project:* Automated Business Operations Module

#### Story XI — Internationalization & Localization

*Real ERP software serves people in different languages, countries and accounting environments.*

- **[Phase 43 — Internationalization & Localization](#phase-43---internationalization-localization)**
  - 43.1 Translatable Strings
  - 43.2 _() Translation Function
  - 43.3 Translatable Fields
  - 43.4 i18n/
  - 43.5 .po Files
  - 43.6 .pot Templates
  - 43.7 Exporting Translations
  - 43.8 Importing Translations
  - 43.9 Languages
  - 43.10 Locale / Formatting
  - 43.11 Translatable QWeb
  - 43.12 Localization Modules
  - 43.13 Accounting Localization Concept
  - 43.14 PRACTICE — Translate a Custom Module
- *Story project:* Multilingual Business Module

#### Story XII — Web Development with Odoo

*Controllers, website, and portal put Odoo on the public and customer web.*

- **[Phase 44 — HTTP Controllers](#phase-44---http-controllers)**
  - 44.1 http.Controller
  - 44.2 @http.route
  - 44.3 request
  - 44.4 Route Types
  - 44.5 HTTP Methods
  - 44.6 Parameters
  - 44.7 Responses
  - 44.8 Authentication
  - 44.9 CSRF
  - 44.10 Sessions
  - 44.11 Error Handling
  - 44.12 Controller Security
- **[Phase 45 — Website Development](#phase-45---website-development)**
  - 45.1 Website Architecture
  - 45.2 Website Pages
  - 45.3 QWeb Templates
  - 45.4 Dynamic Content
  - 45.5 Website Controllers
  - 45.6 Frontend Assets
  - 45.7 Forms
  - 45.8 SEO Basics
  - 45.9 Multi-Website Concept
- **[Phase 46 — Portal Development](#phase-46---portal-development)**
  - 46.1 Portal Users
  - 46.2 Portal Controllers
  - 46.3 Customer Pages
  - 46.4 Portal Layout
  - 46.5 Portal Security
  - 46.6 Customer-Owned Records
  - 46.7 Pagination
  - 46.8 Search / Filters
  - 46.9 Access Tokens
- *Story project:* Customer Self-Service Portal

#### Story XIII — APIs & Integrations

*Other systems will call you — and you will call them. Contracts first.*

- **[Phase 47 — External API Concepts](#phase-47---external-api-concepts)**
  - 47.1 Client / Server
  - 47.2 Resources
  - 47.3 HTTP Methods
  - 47.4 JSON
  - 47.5 Authentication
  - 47.6 Status Codes
  - 47.7 Pagination
  - 47.8 Rate Limits
  - 47.9 API Contracts
- **[Phase 48 — External Odoo Access](#phase-48---external-odoo-access)**
  - 48.1 External API Architecture
  - 48.2 Integration Users
  - 48.3 Authentication
  - 48.4 Calling Models
  - 48.5 CRUD Remotely
  - 48.6 Domains
  - 48.7 Permissions
  - 48.8 Version Compatibility
- **[Phase 49 — Building API Endpoints](#phase-49---building-api-endpoints)**
  - 49.1 API Routes
  - 49.2 Request Parsing
  - 49.3 Validation
  - 49.4 Serialization
  - 49.5 Response Design
  - 49.6 Authentication
  - 49.7 Authorization
  - 49.8 Pagination
  - 49.9 Error Responses
  - 49.10 API Versioning Concepts
- **[Phase 50 — Webhooks](#phase-50---webhooks)**
  - 50.1 Incoming Webhooks
  - 50.2 Outgoing Events
  - 50.3 Payload Validation
  - 50.4 Signatures
  - 50.5 Secrets
  - 50.6 Idempotency
  - 50.7 Retries
  - 50.8 Duplicate Events
  - 50.9 Audit Logging
- **[Phase 51 — Third-Party Integrations](#phase-51---third-party-integrations)**
  - 51.1 REST APIs
  - 51.2 API Authentication
  - 51.3 Mapping External Data
  - 51.4 Data Ownership
  - 51.5 Synchronization
  - 51.6 Incremental Sync
  - 51.7 Scheduled Sync
  - 51.8 Retries / Backoff
  - 51.9 Rate Limiting
  - 51.10 Error Handling
  - 51.11 Integration Logs
- **[Phase 52 — Payment Integrations](#phase-52---payment-integrations)**
  - 52.1 Payment Architecture
  - 52.2 Payment Providers
  - 52.3 Payment Transactions
  - 52.4 Checkout Flow
  - 52.5 Redirects
  - 52.6 Tokens
  - 52.7 Callbacks
  - 52.8 Webhooks
  - 52.9 Payment Status
  - 52.10 Failure Handling
  - 52.11 Refund Concepts
- *Story project:* Odoo ↔ External SaaS Integration

#### Story XIV — Modern Odoo Frontend

*Standard views go far. Custom interactive experiences require the web framework.*

- **[Phase 53 — Odoo JavaScript Architecture](#phase-53---odoo-javascript-architecture)**
  - 53.1 Web Client
  - 53.2 JavaScript Modules
  - 53.3 Native JS Modules
  - 53.4 Frontend Environment
  - 53.5 Components
  - 53.6 Registries
  - 53.7 Services
  - 53.8 Hooks
  - 53.9 Backend ↔ Frontend Communication
  - 53.10 Frontend Debugging
- **[Phase 54 — OWL Fundamentals](#phase-54---owl-fundamentals)**
  - 54.1 Components
  - 54.2 Templates
  - 54.3 Props
  - 54.4 State
  - 54.5 Events
  - 54.6 Lifecycle
  - 54.7 Composition
  - 54.8 Subcomponents
  - 54.9 Reactive UI
- **[Phase 55 — Services, Registries & Hooks](#phase-55---services-registries-hooks)**
  - 55.1 Registries
  - 55.2 Registry Categories
  - 55.3 Services
  - 55.4 Service Dependencies
  - 55.5 useService()
  - 55.6 ORM Service
  - 55.7 RPC Service
  - 55.8 Notification Service
  - 55.9 Action Service
  - 55.10 Hooks
  - 55.11 useBus
  - 55.12 useAssets
  - 55.13 Custom Hooks
- **[Phase 56 — Assets & Frontend Resources](#phase-56---assets-frontend-resources)**
  - 56.1 Asset Bundles
  - 56.2 JavaScript Assets
  - 56.3 SCSS / CSS
  - 56.4 XML Templates
  - 56.5 Manifest Assets
  - 56.6 Lazy Loading
  - 56.7 Asset Debugging
- **[Phase 57 — Extending & Patching the Web Client](#phase-57---extending-patching-the-web-client)**
  - 57.1 Patching Code
  - 57.2 Patching Components
  - 57.3 Extending Components
  - 57.4 Extending Registries
  - 57.5 Custom Field Widgets
  - 57.6 View Extensions
  - 57.7 Upgrade Safety
  - 57.8 When Not to Patch
- **[Phase 58 — Client Actions & Advanced Frontend](#phase-58---client-actions-advanced-frontend)**
  - 58.1 Client Actions
  - 58.2 Action Registry
  - 58.3 OWL Client Components
  - 58.4 Loading Server Data
  - 58.5 Custom Dashboards
  - 58.6 Generic Components
  - 58.7 Error Handling
  - 58.8 Error Boundaries
  - 58.9 Odoo Editor Concepts
  - 58.10 Mobile Web APIs
  - 58.11 Responsive Odoo UI
- *Story project:* Interactive Odoo Dashboard

#### Story XV — Files, Attachments & Media

*Business documents are files — store and secure them like business data.*

- **[Phase 59 — Attachments & Filestore](#phase-59---attachments-filestore)**
  - 59.1 ir.attachment
  - 59.2 Binary Fields
  - 59.3 Image Fields
  - 59.4 Database vs Filestore
  - 59.5 Attachment Relationships
  - 59.6 Uploads
  - 59.7 Downloads
  - 59.8 Attachment Access
  - 59.9 Attachment Security
  - 59.10 Public / Private Files
  - 59.11 LAB — Secure Document Attachment Feature
- *Story project:* Document-Enabled Business Module

#### Story XVI — PostgreSQL & Performance

*The ORM sits on PostgreSQL. Performance starts with understanding both.*

- **[Phase 60 — Odoo Database Structure](#phase-60---odoo-database-structure)**
  - 60.1 Models → Tables
  - 60.2 Fields → Columns
  - 60.3 Many2many Relation Tables
  - 60.4 Foreign Keys
  - 60.5 Metadata Tables
  - 60.6 ir_model
  - 60.7 ir_model_fields
  - 60.8 Inspecting the Schema
- **[Phase 61 — PostgreSQL for Odoo Developers](#phase-61---postgresql-for-odoo-developers)**
  - 61.1 SELECT
  - 61.2 JOIN
  - 61.3 GROUP BY
  - 61.4 Aggregates
  - 61.5 CTE Concepts
  - 61.6 Constraints
  - 61.7 psql
  - 61.8 Database Inspection
- **[Phase 62 — Transactions & Concurrency](#phase-62---transactions-concurrency)**
  - 62.1 Transactions
  - 62.2 Commit
  - 62.3 Rollback
  - 62.4 Isolation
  - 62.5 Locks
  - 62.6 Deadlocks
  - 62.7 Concurrent Updates
  - 62.8 Savepoints
- **[Phase 63 — Indexes & Query Plans](#phase-63---indexes-query-plans)**
  - 63.1 Indexes
  - 63.2 B-Tree Intuition
  - 63.3 Selectivity
  - 63.4 EXPLAIN
  - 63.5 EXPLAIN ANALYZE
  - 63.6 Query Plans
  - 63.7 Sequential Scan
  - 63.8 When to Add an Index
- **[Phase 64 — ORM Performance](#phase-64---orm-performance)**
  - 64.1 Query Counts
  - 64.2 Batch Operations
  - 64.3 Prefetch
  - 64.4 Cache
  - 64.5 N+1 Problems
  - 64.6 Computed Field Costs
  - 64.7 Stored Computations
  - 64.8 Efficient Domains
  - 64.9 Algorithms & Complexity
  - 64.10 Database Indexes from Models
- **[Phase 65 — Profiling & Optimization](#phase-65---profiling-optimization)**
  - 65.1 Odoo Profiler
  - 65.2 SQL Profiling
  - 65.3 Query Collector
  - 65.4 Periodic Collector
  - 65.5 Finding Bottlenecks
  - 65.6 Measuring Before Optimizing
  - 65.7 Benchmarking
  - 65.8 Performance Regression
- *Story project:* Optimize a Slow Odoo Application

#### Story XVII — Testing & Debugging

*If you cannot prove it, you do not ship it.*

- **[Phase 66 — Odoo Logging & Debugging](#phase-66---odoo-logging-debugging)**
  - 66.1 Log Levels
  - 66.2 Server Logs
  - 66.3 Stack Traces
  - 66.4 Python Debugger
  - 66.5 Breakpoints
  - 66.6 Odoo Shell
  - 66.7 Developer Tools
  - 66.8 Reproducing Bugs
  - 66.9 Root Cause Analysis
- **[Phase 67 — Python Tests](#phase-67---python-tests)**
  - 67.1 Test Module Structure
  - 67.2 TransactionCase
  - 67.3 Savepoint / Transaction Concepts
  - 67.4 Test Data
  - 67.5 setUp()
  - 67.6 Assertions
  - 67.7 Test Tags
  - 67.8 Running Selected Tests
- **[Phase 68 — Testing Business Logic](#phase-68---testing-business-logic)**
  - 68.1 CRUD Tests
  - 68.2 Computed Field Tests
  - 68.3 Constraint Tests
  - 68.4 Workflow Tests
  - 68.5 Cron Tests
  - 68.6 Edge Cases
  - 68.7 Regression Tests
- **[Phase 69 — Testing Security](#phase-69---testing-security)**
  - 69.1 User Context
  - 69.2 ACL Tests
  - 69.3 Record Rule Tests
  - 69.4 sudo() Tests
  - 69.5 Multi-Company Tests
  - 69.6 Portal Access Tests
  - 69.7 Permission Regression
- **[Phase 70 — Web & Frontend Testing](#phase-70---web-frontend-testing)**
  - 70.1 HTTP Tests
  - 70.2 Controller Tests
  - 70.3 browser_js
  - 70.4 Test Tours
  - 70.5 Integration Tours
  - 70.6 JavaScript Unit Tests
  - 70.7 HOOT
  - 70.8 @odoo/hoot-dom
  - 70.9 Web Test Helpers
  - 70.10 Mock Server
  - 70.11 Mocking RPC
  - 70.12 End-to-End Flows
- **[Phase 71 — Performance & Regression Testing](#phase-71---performance-regression-testing)**
  - 71.1 Query Count Tests
  - 71.2 assertQueryCount()
  - 71.3 --log-sql
  - 71.4 Performance Budgets
  - 71.5 Regression Detection
  - 71.6 Repeatable Benchmarks
- *Story project:* Fully Tested Odoo Module

#### Story XVIII — CLI & Developer Tooling

*Your terminal is part of the Odoo toolbox.*

- **[Phase 72 — Odoo CLI & Developer Commands](#phase-72---odoo-cli-developer-commands)**
  - 72.1 odoo-bin / odoo
  - 72.2 Configuration File
  - 72.3 Database Selection
  - 72.4 -i Install
  - 72.5 -u Upgrade
  - 72.6 --addons-path
  - 72.7 --dev
  - 72.8 --log-level
  - 72.9 --log-sql
  - 72.10 --test-enable
  - 72.11 --test-tags
  - 72.12 --stop-after-init
  - 72.13 shell
  - 72.14 scaffold
  - 72.15 LAB — Complete CLI Development Workflow
- *Story project:* Command-Line Odoo Workflow

#### Story XIX — Deployment & Operations

*A module that cannot be deployed and restored is not finished.*

- **[Phase 73 — Odoo Configuration](#phase-73---odoo-configuration)**
  - 73.1 odoo.conf
  - 73.2 addons_path
  - 73.3 db_host
  - 73.4 db_port
  - 73.5 db_user
  - 73.6 Database Filters
  - 73.7 Logging
  - 73.8 Proxy Mode
  - 73.9 Secrets
  - 73.10 Environment-Specific Configuration
- **[Phase 74 — Linux Deployment](#phase-74---linux-deployment)**
  - 74.1 Linux User
  - 74.2 Filesystem Layout
  - 74.3 Python Environment
  - 74.4 Source Deployment
  - 74.5 Addons
  - 74.6 File Permissions
  - 74.7 systemd
  - 74.8 Service Management
- **[Phase 75 — Reverse Proxy & HTTPS](#phase-75---reverse-proxy-https)**
  - 75.1 Reverse Proxy
  - 75.2 Nginx
  - 75.3 Domains
  - 75.4 DNS
  - 75.5 TLS / HTTPS
  - 75.6 Proxy Headers
  - 75.7 WebSocket / Long-Lived Connections
  - 75.8 Secure Production Exposure
- **[Phase 76 — Workers & Production Configuration](#phase-76---workers-production-configuration)**
  - 76.1 Development Mode
  - 76.2 Multi-Processing
  - 76.3 Workers
  - 76.4 Worker Sizing
  - 76.5 CPU Considerations
  - 76.6 Memory Limits
  - 76.7 Request Time Limits
  - 76.8 Cron Workers
  - 76.9 Production Configuration
- **[Phase 77 — PostgreSQL Operations](#phase-77---postgresql-operations)**
  - 77.1 Database Users
  - 77.2 Connections
  - 77.3 Connection Limits
  - 77.4 Maintenance
  - 77.5 VACUUM Concepts
  - 77.6 ANALYZE
  - 77.7 Monitoring
  - 77.8 Basic Tuning
- **[Phase 78 — Backups & Disaster Recovery](#phase-78---backups-disaster-recovery)**
  - 78.1 Database Backup
  - 78.2 Filestore Backup
  - 78.3 Configuration Backup
  - 78.4 Restore
  - 78.5 Backup Scheduling
  - 78.6 Off-Site Backups
  - 78.7 Recovery Testing
  - 78.8 Disaster Recovery Plan
- **[Phase 79 — Odoo.sh](#phase-79)**
  - 79.1 Odoo.sh Architecture
  - 79.2 Projects
  - 79.3 Git Integration
  - 79.4 Branches
  - 79.5 Development
  - 79.6 Staging
  - 79.7 Production
  - 79.8 Builds
  - 79.9 Logs
  - 79.10 Shell Access
  - 79.11 Database Management
- **[Phase 80 — Docker for Odoo](#phase-80---docker-for-odoo)**
  - 80.1 Images
  - 80.2 Containers
  - 80.3 Volumes
  - 80.4 Networking
  - 80.5 Environment Variables
  - 80.6 Odoo Container
  - 80.7 PostgreSQL Container
  - 80.8 Persistent Filestore
  - 80.9 Docker Compose
- **[Phase 81 — Monitoring & Production Troubleshooting](#phase-81---monitoring-production-troubleshooting)**
  - 81.1 Application Logs
  - 81.2 PostgreSQL Logs
  - 81.3 CPU
  - 81.4 Memory
  - 81.5 Disk
  - 81.6 Database Connections
  - 81.7 Slow Requests
  - 81.8 Failed Jobs
  - 81.9 Error Monitoring
  - 81.10 Incident Diagnosis
- *Story project:* Deploy an Odoo System

#### Story XX — Upgrades, Migrations & Maintenance

*Production code must survive tomorrow's Odoo version.*

- **[Phase 82 — Module Versioning](#phase-82---module-versioning)**
  - 82.1 Manifest Version
  - 82.2 Semantic Change Thinking
  - 82.3 Release Changes
  - 82.4 Compatibility
  - 82.5 Dependencies
  - 82.6 Version Control Strategy
- **[Phase 83 — Schema Changes](#phase-83---schema-changes)**
  - 83.1 Adding Fields
  - 83.2 Renaming Fields
  - 83.3 Changing Field Types
  - 83.4 Changing Relationships
  - 83.5 Removing Fields
  - 83.6 Constraints
  - 83.7 Safe Schema Evolution
- **[Phase 84 — Data Migrations](#phase-84---data-migrations)**
  - 84.1 Migration Scripts
  - 84.2 Pre-Migration
  - 84.3 Post-Migration
  - 84.4 Transforming Records
  - 84.5 Renaming Data
  - 84.6 Preserving Relationships
  - 84.7 Validation
  - 84.8 Large Dataset Considerations
  - 84.9 Rollback Planning
- **[Phase 85 — Odoo Version Upgrades](#phase-85---odoo-version-upgrades)**
  - 85.1 Version Differences
  - 85.2 Deprecated APIs
  - 85.3 Framework Changes
  - 85.4 Module Compatibility
  - 85.5 Database Upgrade
  - 85.6 Upgrade Scripts
  - 85.7 Upgrade Utilities
  - 85.8 Frontend Migration
  - 85.9 Post-Upgrade Testing
- **[Phase 86 — Module Hooks](#phase-86---module-hooks)**
  - 86.1 Hooks Concept
  - 86.2 pre_init_hook
  - 86.3 post_init_hook
  - 86.4 uninstall_hook
  - 86.5 Environment During Hooks
  - 86.6 Appropriate Uses
  - 86.7 Avoiding Hook Abuse
- **[Phase 87 — Upgrade-Safe Customization](#phase-87---upgrade-safe-customization)**
  - 87.1 Never Modify Core Directly
  - 87.2 Extension Points
  - 87.3 Model Inheritance
  - 87.4 View Inheritance
  - 87.5 Stable Overrides
  - 87.6 Dependency Management
  - 87.7 Avoiding Hard-Coding
  - 87.8 Future-Proof Design
- **[Phase 88 — Legacy Module Maintenance](#phase-88---legacy-module-maintenance)**
  - 88.1 Read Before Changing
  - 88.2 Trace Dependencies
  - 88.3 Trace Execution
  - 88.4 Reproduce Existing Behavior
  - 88.5 Characterization Tests
  - 88.6 Backward Compatibility
  - 88.7 Safe Refactoring
  - 88.8 Deprecation
- *Story project:* Upgrade an Existing Odoo Module

#### Story XXI — Functional Odoo for Developers

*An Odoo engineer should understand the business flows their code is changing.*

- **[Phase 89 — Core Functional Applications](#phase-89---core-functional-applications)**
  - 89.1 Contacts
  - 89.2 CRM
  - 89.3 Sales
  - 89.4 Purchase
  - 89.5 Inventory
  - 89.6 Accounting Concepts
  - 89.7 Invoicing
  - 89.8 Employees
  - 89.9 Recruitment
  - 89.10 Time Off
  - 89.11 Projects
  - 89.12 Timesheets
  - 89.13 Manufacturing
  - 89.14 Maintenance
  - 89.15 Helpdesk Concept
  - 89.16 Website
  - 89.17 eCommerce
  - 89.18 Point of Sale
  - 89.19 End-to-End Business Flow
- *Story project:* Trace a Company End-to-End

#### Story XXII — Real Odoo Engineering

*Judge trade-offs before you write models.*

- **[Phase 90 — Requirements → ERP Design](#phase-90---requirements-erp-design)**
  - 90.1 Stakeholder Interviews
  - 90.2 Business Requirements
  - 90.3 Functional Requirements
  - 90.4 Non-Functional Requirements
  - 90.5 Business Process Mapping
  - 90.6 As-Is Process
  - 90.7 To-Be Process
  - 90.8 Data Requirements
  - 90.9 Roles
  - 90.10 Permissions
  - 90.11 Acceptance Criteria
- **[Phase 91 — Odoo Solution Architecture](#phase-91---odoo-solution-architecture)**
  - 91.1 Module Boundaries
  - 91.2 Model Ownership
  - 91.3 Dependencies
  - 91.4 Shared Models
  - 91.5 Integration Boundaries
  - 91.6 Security Architecture
  - 91.7 Multi-Company Architecture
  - 91.8 Data Flow
  - 91.9 Reusability
- **[Phase 92 — Configuration vs Customization vs Integration](#phase-92---configuration-vs-customization-vs-integration)**
  - 92.1 Standard Odoo
  - 92.2 Configuration
  - 92.3 Studio Concepts
  - 92.4 Custom Module
  - 92.5 Third-Party Module
  - 92.6 External Integration
  - 92.7 Data Ownership
  - 92.8 Upgrade Costs
  - 92.9 Maintenance Costs
  - 92.10 Choosing the Simplest Correct Solution
- **[Phase 93 — Maintainable Odoo Code](#phase-93---maintainable-odoo-code)**
  - 93.1 Naming
  - 93.2 Small Methods
  - 93.3 Separation of Concerns
  - 93.4 Cohesion
  - 93.5 Low Coupling
  - 93.6 Reusable Mixins
  - 93.7 Configuration over Hard-Coding
  - 93.8 Constants
  - 93.9 Error Handling
  - 93.10 Logging
  - 93.11 Documentation
  - 93.12 Upgrade Safety
- **[Phase 94 — Git, Collaboration & Code Review](#phase-94---git-collaboration-code-review)**
  - 94.1 Repository
  - 94.2 Branches
  - 94.3 Commits
  - 94.4 Pull Requests
  - 94.5 Merge
  - 94.6 Rebase Concepts
  - 94.7 Release Branches
  - 94.8 Code Review for Correctness
  - 94.9 Security Review
  - 94.10 ORM Performance Review
  - 94.11 Upgrade-Safety Review
  - 94.12 Test Review
  - 94.13 Documentation Review
- *Story project:* Design a Production ERP Solution

#### Story XXIII — Grand Odoo Capstone

*Stop learning isolated features. Engineer the whole ERP.*

- **[Phase 95 — Production ERP Capstone](#phase-95---production-erp-capstone)**
  - 95.1 Problem Discovery
  - 95.2 Business Requirements
  - 95.3 Process Maps
  - 95.4 Architecture
  - 95.5 Module Boundaries
  - 95.6 Data Models
  - 95.7 Relational Design
  - 95.8 ORM
  - 95.9 Business Rules
  - 95.10 Security
  - 95.11 Multi-Company
  - 95.12 Views
  - 95.13 Workflows
  - 95.14 Wizards
  - 95.15 Chatter & Activities
  - 95.16 Automation
  - 95.17 Reports
  - 95.18 Attachments
  - 95.19 Translations
  - 95.20 Website
  - 95.21 Portal
  - 95.22 API
  - 95.23 Webhooks
  - 95.24 External Integration
  - 95.25 OWL Dashboard
  - 95.26 PostgreSQL Optimization
  - 95.27 Automated Tests
  - 95.28 Security Tests
  - 95.29 Performance Tests
  - 95.30 Migration Strategy
  - 95.31 Backup Strategy
  - 95.32 Deployment
  - 95.33 Monitoring
  - 95.34 README
  - 95.35 Architecture Diagram
  - 95.36 Technical Documentation
- *Story project:* COMPLETE BUSINESS ERP

#### Story XXIV — Interview, Portfolio & Job Readiness

*Now prove that you can understand, build, debug and maintain Odoo.*

- **[Phase 96 — Odoo Engineer Job Readiness](#phase-96---odoo-engineer-job-readiness)**
  - 96.1 ERP Fundamentals
  - 96.2 Odoo Functional Knowledge
  - 96.3 Odoo Architecture
  - 96.4 Module Anatomy
  - 96.5 Python
  - 96.6 Models
  - 96.7 Fields
  - 96.8 Relationships
  - 96.9 ORM
  - 96.10 Recordsets
  - 96.11 Domains
  - 96.12 Computed Fields
  - 96.13 Constraints
  - 96.14 Workflows
  - 96.15 XML
  - 96.16 Views
  - 96.17 Actions
  - 96.18 Security
  - 96.19 Multi-Company
  - 96.20 Inheritance
  - 96.21 Mixins
  - 96.22 Wizards
  - 96.23 Cron / Automation
  - 96.24 Chatter
  - 96.25 QWeb / Reports
  - 96.26 Internationalization
  - 96.27 Controllers
  - 96.28 APIs
  - 96.29 Webhooks
  - 96.30 Integrations
  - 96.31 OWL
  - 96.32 JavaScript Framework
  - 96.33 PostgreSQL
  - 96.34 Performance
  - 96.35 Testing
  - 96.36 Debugging
  - 96.37 CLI
  - 96.38 Deployment
  - 96.39 Odoo.sh
  - 96.40 Docker
  - 96.41 Upgrades
  - 96.42 Migrations
  - 96.43 Requirements Analysis
  - 96.44 Solution Architecture
  - 96.45 Live Module Design
  - 96.46 Live Debugging
  - 96.47 Code Review
  - 96.48 Portfolio Walkthrough
  - 96.49 PORTFOLIO PROJECT 1 — Library / Asset Management
  - 96.50 PORTFOLIO PROJECT 2 — HR / Leave Approval
  - 96.51 PORTFOLIO PROJECT 3 — Sales Customization
  - 96.52 PORTFOLIO PROJECT 4 — Inventory Extension
  - 96.53 PORTFOLIO PROJECT 5 — API / SaaS Integration
  - 96.54 PORTFOLIO PROJECT 6 — OWL Dashboard
  - 96.55 PORTFOLIO PROJECT 7 — Customer Portal
  - 96.56 FINAL MOCK INTERVIEW

### Anchor Resources (bookmark these)

- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html) · [Building a Module](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html) · [Security](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [View architectures](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html) · [HTTP](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html)
- [Owl components](https://www.odoo.com/documentation/18.0/developer/reference/frontend/owl_components.html) · [odoo/odoo](https://github.com/odoo/odoo)
- **Projects bridge:** [`Projects.md`](../guides/Projects.md) · **Interview bridge:** [`Interview.md`](../guides/Interview.md)

---
---

# STORY I — UNDERSTAND THE BUSINESS BEFORE THE CODE

**Track:** Business & ERP Foundations

*Odoo exists to run businesses. First understand the problem it solves.*

---
<a id="phase-1"></a>
<a id="phase-1---what-is-erp"></a>
# PHASE 1 - What Is ERP?

**Track:** Business & ERP Foundations

**WHAT YOU WILL BE ABLE TO DO:** Explain ERP as one shared system of business truth — name processes, departments, master data, and transactions — contrast ERP with CRM, and sketch how a small company would run without spreadsheet chaos.

**WHAT YOU SHOULD KNOW FIRST:** Nothing about Odoo or ERP. Comfortable reading plain English and simple diagrams. Optional: basic Python later in the roadmap; not required yet.

**LEARNING RESOURCES:**

- [What is ERP? (YouTube)](https://www.youtube.com/watch?v=10JeksrGVjI)
- [ERP Systems Explained (YouTube)](https://www.youtube.com/watch?v=Bb4jVGiQKKA)
- [ERP explained without jargon (YouTube)](https://www.youtube.com/watch?v=EHiH7hp0PBU)
- [Odoo ERP overview (YouTube)](https://www.youtube.com/watch?v=s-4zNx7wCFk)
- [Odoo 18 — Server Framework 101 Architecture](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/01_architecture.html)
- [Odoo developer landing](https://www.odoo.com/documentation/18.0/developer.html)

## 1.1 Business Processes

You are about to learn Odoo — a platform that runs companies. Before you touch Python, XML, or a database, you need the problem Odoo was built to solve. That problem is not “missing a programming language.” It is **business work that refuses to stay in one person’s head**.

A **business process** is a repeatable sequence of steps that turns an input into a business outcome. “Customer asks for a price” → “we send a quote” → “they order” → “we deliver” → “we invoice” → “they pay” is one process. Hiring someone, buying raw materials, and closing the month’s books are other processes. Companies live or die on whether those processes are reliable, auditable, and shared.

**WHEN “JUST ASK THE OWNER” STOPS WORKING:**

In a one-person shop, the owner *is* the process. They know who ordered what, what is on the shelf, and who still owes money. The moment you add a second salesperson, a warehouse helper, or an accountant, memory stops being a system. People invent shortcuts: sticky notes, chat messages, personal spreadsheets. Each shortcut feels faster — until two people update different “truths” and the company ships the wrong product or double-invoices a customer.

ERP (Enterprise Resource Planning) exists because **resources** — money, stock, people, time — must be planned and recorded together. “Enterprise” here does not mean “only Fortune 500.” It means the whole operating unit, even if that unit is a 12-person workshop in Doha.

```text
INPUT                    PROCESS                         OUTCOME
customer need     -->    quote → order → deliver    -->  cash + happy customer
supplier catalog  -->    RFQ → PO → receive         -->  stock ready to sell
candidate resume  -->    interview → contract       -->  employee on payroll
```

Notice the pattern: every process has **states** (draft, confirmed, done) and **hand-offs** between people. Software that only stores static lists misses the point. Software that tracks state changes along a process is getting closer to ERP.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List five business processes a bakery might run in a normal week. |
| Medium | For one process (e.g. “sell a cake”), write the steps and who owns each step. |
| Hard | Mark where two people could disagree on “truth” if they used separate notebooks. |

**WHAT THIS UNLOCKS NEXT:**

Processes name *what* the company does. The next lesson asks *who* does each hand-off — and why separate departments create the very chaos ERP was invented to fix.

---

## 1.2 Departments

A process spans people. Companies group people into **departments**: Sales, Purchasing, Warehouse, Accounting, HR. Departments are useful — specialists focus — but they also create walls. Sales promises a delivery date the warehouse never saw. Accounting invoices a quantity Purchasing never ordered. HR onboards someone who never got a laptop from IT.

A **workflow** is the path a document or decision takes across those departments. In ERP language, a quotation “flows” into a sales order, which “flows” into a delivery, which “flows” into an invoice. The document changes **state**; each state change may notify or require another role.

**THE WALL BETWEEN SPREADSHEETS:**

```text
SALES                  WAREHOUSE               ACCOUNTING
sales_pipeline.xlsx    stock.xlsx              invoices.xlsx
     |                      |                       |
     |  email "ship it"     |                       |
     +--------------------->|                       |
                            |  email "shipped"      |
                            +---------------------->|
                                                    |
                         three files, three truths, endless reconciliation
```

ERP does not abolish departments. It gives them **one shared record** and a **shared sequence of states**, so hand-offs are data, not tribal knowledge.

| Department | Typical question | Needs from others |
| ---------- | ---------------- | ----------------- |
| Sales | Can we promise this? | Real stock + customer credit |
| Warehouse | What do we pick today? | Confirmed orders + locations |
| Accounting | What can we invoice? | Delivered quantities + taxes |
| Purchasing | What must we reorder? | Forecast + vendor lead times |
| HR | Who is on the team? | Roles, contracts, time off |

When you later open Odoo Apps, you will see Sales, Inventory, Accounting as separate apps — but they are **departments as modules**, not separate companies with separate databases.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Name three departments in a furniture shop and one hand-off between each pair. |
| Medium | Draw a swimlane (Sales / Warehouse / Accounting) for “customer orders a desk.” |
| Hard | Invent one failure caused by a broken hand-off, and say which shared field would have prevented it. |

**WHAT THIS UNLOCKS NEXT:**

Workflows move documents. Those documents keep naming the same customers and products over and over. If every app spells “customer” differently, the workflow still lies. That is the job of master data.

---

## 1.3 Cross-Department Workflows

A process spans people. Companies group people into **departments**: Sales, Purchasing, Warehouse, Accounting, HR. Departments are useful — specialists focus — but they also create walls. Sales promises a delivery date the warehouse never saw. Accounting invoices a quantity Purchasing never ordered. HR onboards someone who never got a laptop from IT.

A **workflow** is the path a document or decision takes across those departments. In ERP language, a quotation “flows” into a sales order, which “flows” into a delivery, which “flows” into an invoice. The document changes **state**; each state change may notify or require another role.

**THE WALL BETWEEN SPREADSHEETS:**

```text
SALES                  WAREHOUSE               ACCOUNTING
sales_pipeline.xlsx    stock.xlsx              invoices.xlsx
     |                      |                       |
     |  email "ship it"     |                       |
     +--------------------->|                       |
                            |  email "shipped"      |
                            +---------------------->|
                                                    |
                         three files, three truths, endless reconciliation
```

ERP does not abolish departments. It gives them **one shared record** and a **shared sequence of states**, so hand-offs are data, not tribal knowledge.

| Department | Typical question | Needs from others |
| ---------- | ---------------- | ----------------- |
| Sales | Can we promise this? | Real stock + customer credit |
| Warehouse | What do we pick today? | Confirmed orders + locations |
| Accounting | What can we invoice? | Delivered quantities + taxes |
| Purchasing | What must we reorder? | Forecast + vendor lead times |
| HR | Who is on the team? | Roles, contracts, time off |

When you later open Odoo Apps, you will see Sales, Inventory, Accounting as separate apps — but they are **departments as modules**, not separate companies with separate databases.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Name three departments in a furniture shop and one hand-off between each pair. |
| Medium | Draw a swimlane (Sales / Warehouse / Accounting) for “customer orders a desk.” |
| Hard | Invent one failure caused by a broken hand-off, and say which shared field would have prevented it. |

**WHAT THIS UNLOCKS NEXT:**

Workflows move documents. Those documents keep naming the same customers and products over and over. If every app spells “customer” differently, the workflow still lies. That is the job of master data.

---

## 1.4 Master Data

**Master data** is the slow-changing reference information the company agrees is true: who the customer is, what the product is, which unit of measure you sell in, which warehouse locations exist, who the employee is. Transactions (orders, deliveries, invoices) *point at* master data; they should not reinvent it.

**WHY SPELLING THE CUSTOMER TWICE DESTROYS TRUST:**

If Sales stores `Acme Trading` and Accounting stores `ACME trading W.L.L.`, reports disagree, credit limits fail, and you cannot answer “how much does this customer owe across all orders?” Master data means **one partner record**, referenced everywhere.

```text
MASTER DATA (reference)              TRANSACTIONS (events)
--------------------                 ---------------------
Partner: Acme Trading  <-----------  Quotation SO001
Product: Oak Desk      <-----------  Delivery WH/OUT/12
UoM: Unit              <-----------  Invoice INV/2026/88
Company: My Shop LLC
```

In Odoo you will meet models like `res.partner` (people and companies you deal with) and `product.product` (sellable/purchasable items). Those names can wait. The idea cannot: **identity lives in master data; activity lives in transactions.**

Good master data is boring on purpose: few fields that matter, clear ownership (who may edit a product’s cost?), and no duplicates. Bad master data looks like “we’ll clean it later” — later never comes, and every customization fights ghosts.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List six master-data entities for a bookstore (customer, book, …). |
| Medium | For “customer,” list fields that belong on the master vs fields that belong on an order. |
| Hard | Describe how a duplicate customer record would corrupt a monthly sales report. |

**WHAT THIS UNLOCKS NEXT:**

Master data alone is a directory. Businesses need a history of what *happened* — confirmed, shipped, paid. That history is made of transactions.

---

## 1.5 Transactions

A **transaction** (in the business sense) is a dated event that changes the company’s situation: a confirmed sales order, a stock move, a vendor bill, a payment. Transactions reference master data and carry quantities, amounts, and states.

```text
Time -->

[Create quote] --> [Confirm order] --> [Deliver] --> [Invoice] --> [Register payment]
   draft              sale              done         posted         reconciled
```

Two meanings of “transaction” will collide later when you write Odoo code:

1. **Business transaction** — the sale, the receipt, the invoice (what users care about).
2. **Database transaction** — a unit of work that commits or rolls back together (what PostgreSQL cares about).

For now, stay with meaning (1). ERP’s power is that confirming a sales order can **create related records** (stock moves, analytic lines) in one coherent story instead of three people typing the same numbers into three files.

**AUDIT TRAIL AS A FEATURE, NOT A SIDE EFFECT:**

Managers ask: who confirmed this? when did stock leave? which invoice matches which delivery? Spreadsheets answer with folklore. ERP answers with timestamps, users, and document links. That is why “just update the cell” is dangerous in operations software — you erase the story.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Name four transaction types a café might record in one day. |
| Medium | Trace one coffee sale from order to payment; list which master records it touches. |
| Hard | Explain why deleting a posted invoice “to fix a typo” is worse than correcting it with a credit note. |

**WHAT THIS UNLOCKS NEXT:**

You now have processes, departments, master data, and transactions. Salespeople often hear “CRM” and think that is the whole system. The next lesson draws a clean line between CRM and ERP so you never confuse a pipeline tool with a full operating system for the company.

---

## 1.6 Single Source of Truth

**Single Source of Truth** belongs to **What Is ERP?**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Single Source of Truth** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Single Source of Truth** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Single Source of Truth**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Single Source of Truth** is concrete, continue — the next lesson in this phase still depends on it.

## 1.7 ERP vs CRM

**CRM** means Customer Relationship Management: tools focused on leads, opportunities, activities, and the sales pipeline — “who might buy, and where are we in the conversation?”

**ERP** means the broader operating system for the company: sales *and* purchase *and* inventory *and* accounting *and* HR, sharing master data and workflows.

```text
CRM focus (slice)                    ERP focus (whole)
-----------------                    -----------------
Leads / Opportunities                Partners + Products + Stock + Money + People
Pipeline stages                      Quote → Delivery → Invoice → Payment
"Will they buy?"                     "Can we fulfill, bill, and account for it?"
```

Odoo includes a CRM app **inside** an ERP platform. That is not a contradiction. CRM is one process family. ERP is the decision that CRM must share the same partners, products, and eventually invoices with the rest of the company.

| Question | CRM alone | ERP (with CRM) |
| -------- | --------- | -------------- |
| Track a lead | Strong | Strong |
| Reserve stock for a won deal | Weak / external | Native |
| Invoice from the same customer record | Often export | Same partner |
| Post to the general ledger | Rarely | Accounting app |

**WHEN A STARTUP OUTGROWS “CRM ONLY”:**

A team can live in a pure CRM for months. The day they ship physical goods, buy from vendors, or need proper books, CRM-only tools force copy-paste into inventory and accounting systems. That copy-paste is the pain ERP removes.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In one sentence each, define CRM and ERP. |
| Medium | Give an example decision that needs CRM data *and* inventory data together. |
| Hard | Argue when a company should keep a separate best-of-breed CRM instead of Odoo CRM — and what integration tax they accept. |

**WHAT THIS UNLOCKS NEXT:**

Theory without a concrete company stays abstract. The next lesson forces you to model a small firm end-to-end so ERP vocabulary becomes a map you drew yourself.

---

## 1.8 ERP vs Standalone Business Software

**CRM** means Customer Relationship Management: tools focused on leads, opportunities, activities, and the sales pipeline — “who might buy, and where are we in the conversation?”

**ERP** means the broader operating system for the company: sales *and* purchase *and* inventory *and* accounting *and* HR, sharing master data and workflows.

```text
CRM focus (slice)                    ERP focus (whole)
-----------------                    -----------------
Leads / Opportunities                Partners + Products + Stock + Money + People
Pipeline stages                      Quote → Delivery → Invoice → Payment
"Will they buy?"                     "Can we fulfill, bill, and account for it?"
```

Odoo includes a CRM app **inside** an ERP platform. That is not a contradiction. CRM is one process family. ERP is the decision that CRM must share the same partners, products, and eventually invoices with the rest of the company.

| Question | CRM alone | ERP (with CRM) |
| -------- | --------- | -------------- |
| Track a lead | Strong | Strong |
| Reserve stock for a won deal | Weak / external | Native |
| Invoice from the same customer record | Often export | Same partner |
| Post to the general ledger | Rarely | Accounting app |

**WHEN A STARTUP OUTGROWS “CRM ONLY”:**

A team can live in a pure CRM for months. The day they ship physical goods, buy from vendors, or need proper books, CRM-only tools force copy-paste into inventory and accounting systems. That copy-paste is the pain ERP removes.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In one sentence each, define CRM and ERP. |
| Medium | Give an example decision that needs CRM data *and* inventory data together. |
| Hard | Argue when a company should keep a separate best-of-breed CRM instead of Odoo CRM — and what integration tax they accept. |

**WHAT THIS UNLOCKS NEXT:**

Theory without a concrete company stays abstract. The next lesson forces you to model a small firm end-to-end so ERP vocabulary becomes a map you drew yourself.

---

## 1.9 PRACTICE — Model a Small Company

Pick one fictional company and keep it for the rest of Story I. Good choices: a bookstore, a furniture workshop, a catering kitchen, or a small IT reseller. You will reuse the same names in later phases.

**BUILD THIS ARTIFACT (ONE PAGE):**

1. **Company name and what it sells**
2. **Departments** (at least Sales, Warehouse/Operations, Accounting; add Purchasing or HR if relevant)
3. **Master data list** (partners, products, locations, …)
4. **One happy-path process** from customer request to cash
5. **Three failure modes** caused by duplicate or missing shared data

```text
Example skeleton — "Gulf Desk Co."

Departments: Sales | Workshop | Stores | Accounts
Master:      Customers, Vendors, Desk models, Wood types, Bin locations
Happy path:  Lead → Quotation → Sales Order → Manufacture/Pick → Delivery → Invoice → Payment
Failures:    two spellings of same customer; stock sold twice; invoice qty ≠ delivered qty
```

You are not configuring Odoo yet. You are proving you can **see the company as a system**. Odoo developers who skip this step write modules that fit the code and fight the business.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Complete the one-page model for your chosen company. |
| Medium | Add a purchase process (reorder wood / books) that shares product master data with sales. |
| Hard | Mark which steps must be atomic together (e.g. deliver + reduce stock) vs which can wait. |

**WHAT THIS UNLOCKS NEXT:**

You can describe a company as processes, master data, and transactions. The remaining question in Story I is: what is **Odoo** specifically — editions, apps, modules, users — and how does a real database embody the model you just drew?

---


---


<a id="phase-2"></a>
<a id="phase-2---understanding-odoo"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 1:** You can explain every lesson title in Phase 1 with a concrete Odoo or business example.

> **Phase 1 complete?** [Continue to Phase 2](#phase-2)


<a id="phase-2"></a>
# PHASE 2 - Understanding Odoo

**Track:** Business & ERP Foundations

**WHAT YOU WILL BE ABLE TO DO:** Distinguish Community from Enterprise, explain apps vs modules/addons, describe users and multi-company, name the shared business records that glue apps together, and navigate a real Odoo database with intent.

**WHAT YOU SHOULD KNOW FIRST:** Phase 1 — processes, master data, transactions, ERP vs CRM, and your small-company model.

**LEARNING RESOURCES:**

- [Odoo Beginner's Guide (YouTube)](https://www.youtube.com/watch?v=QuC6rc2q2mg)
- [Odoo ERP overview (YouTube)](https://www.youtube.com/watch?v=s-4zNx7wCFk)
- [Odoo 18 — Architecture](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/01_architecture.html)
- [Odoo 18 — Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [odoo/odoo on GitHub](https://github.com/odoo/odoo)
- [odoo/docker](https://github.com/odoo/docker)

## 2.1 What Odoo Is

An **App** in Odoo is a user-facing package of business capability: CRM, Sales, Inventory, Accounting, Employees. Installing an app adds menus, models, views, and often demo data. From the user’s perspective, apps are how the ERP is assembled: turn on only what the company needs.

```text
Odoo home / app switcher

  [Contacts] [CRM] [Sales] [Purchase] [Inventory] [Accounting] [Employees] ...
       \________ shared partner & product truth underneath ________/
```

Apps are how **departments** from Phase 1 become clickable. Salespeople live in CRM/Sales; warehouse staff live in Inventory; accountants live in Accounting — yet they open the same customer.

**DEPENDENCY YOU WILL FEEL LATER:**

Installing Sales pulls in dependencies (often stock/product/partner pieces). You cannot treat apps as isolated USB sticks. Under the hood they are modules with `depends` in a manifest — Story III makes that file concrete.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Map each department from your Phase 1 model to an Odoo app name. |
| Medium | Predict which apps must be installed before “quote → delivery → invoice” works. |
| Hard | Explain why installing *every* app on a tiny company is usually a bad idea. |

**WHAT THIS UNLOCKS NEXT:**

Users say “app.” Developers say “module” or “addon.” Same family of packages — next lesson separates the vocabulary cleanly.

---

## 2.2 Odoo Ecosystem

An **App** in Odoo is a user-facing package of business capability: CRM, Sales, Inventory, Accounting, Employees. Installing an app adds menus, models, views, and often demo data. From the user’s perspective, apps are how the ERP is assembled: turn on only what the company needs.

```text
Odoo home / app switcher

  [Contacts] [CRM] [Sales] [Purchase] [Inventory] [Accounting] [Employees] ...
       \________ shared partner & product truth underneath ________/
```

Apps are how **departments** from Phase 1 become clickable. Salespeople live in CRM/Sales; warehouse staff live in Inventory; accountants live in Accounting — yet they open the same customer.

**DEPENDENCY YOU WILL FEEL LATER:**

Installing Sales pulls in dependencies (often stock/product/partner pieces). You cannot treat apps as isolated USB sticks. Under the hood they are modules with `depends` in a manifest — Story III makes that file concrete.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Map each department from your Phase 1 model to an Odoo app name. |
| Medium | Predict which apps must be installed before “quote → delivery → invoice” works. |
| Hard | Explain why installing *every* app on a tiny company is usually a bad idea. |

**WHAT THIS UNLOCKS NEXT:**

Users say “app.” Developers say “module” or “addon.” Same family of packages — next lesson separates the vocabulary cleanly.

---

## 2.3 Community Edition

Odoo ships in two well-known editions:

| | **Community** | **Enterprise** |
| --- | --- | --- |
| License | LGPL (open source core) | Proprietary extras on top of Community |
| Cost | Free to run the software | Subscription for Enterprise apps/features |
| Typical extras | Core apps, full custom module development | Studio, extra apps, hosting options, support packages |
| Developer reality | You can learn almost everything that matters for modules here | Some UI/apps differ; core ORM/module ideas are the same |

**WHAT “EDITION” MEANS FOR A LEARNER:**

You do **not** need Enterprise to become a strong Odoo developer. Models, views, security, wizards, and most workflows live in the open stack. Enterprise adds productized features and polish. When docs or screenshots differ, check the version (**18.0** in this roadmap) and the edition.

If you use [Odoo Online](https://www.odoo.com) trial databases, you may see Enterprise UI. If you run source from GitHub Community, you see Community. Both teach the same mental model: **one database, many apps, shared records**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write one sentence: why Community is enough to learn module development. |
| Medium | List two features you have heard called “Enterprise-only” and note you will verify against current docs before assuming. |
| Hard | For your Phase 1 company, decide whether Community apps alone could cover the happy path — where would you feel pressure to buy or build? |

**WHAT THIS UNLOCKS NEXT:**

Edition tells you licensing. Day-to-day, users think in **Apps** — the tiles on the home screen. That is the next layer.

---

## 2.4 Enterprise Edition

Odoo ships in two well-known editions:

| | **Community** | **Enterprise** |
| --- | --- | --- |
| License | LGPL (open source core) | Proprietary extras on top of Community |
| Cost | Free to run the software | Subscription for Enterprise apps/features |
| Typical extras | Core apps, full custom module development | Studio, extra apps, hosting options, support packages |
| Developer reality | You can learn almost everything that matters for modules here | Some UI/apps differ; core ORM/module ideas are the same |

**WHAT “EDITION” MEANS FOR A LEARNER:**

You do **not** need Enterprise to become a strong Odoo developer. Models, views, security, wizards, and most workflows live in the open stack. Enterprise adds productized features and polish. When docs or screenshots differ, check the version (**18.0** in this roadmap) and the edition.

If you use [Odoo Online](https://www.odoo.com) trial databases, you may see Enterprise UI. If you run source from GitHub Community, you see Community. Both teach the same mental model: **one database, many apps, shared records**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write one sentence: why Community is enough to learn module development. |
| Medium | List two features you have heard called “Enterprise-only” and note you will verify against current docs before assuming. |
| Hard | For your Phase 1 company, decide whether Community apps alone could cover the happy path — where would you feel pressure to buy or build? |

**WHAT THIS UNLOCKS NEXT:**

Edition tells you licensing. Day-to-day, users think in **Apps** — the tiles on the home screen. That is the next layer.

---

## 2.5 Odoo Apps

An **App** in Odoo is a user-facing package of business capability: CRM, Sales, Inventory, Accounting, Employees. Installing an app adds menus, models, views, and often demo data. From the user’s perspective, apps are how the ERP is assembled: turn on only what the company needs.

```text
Odoo home / app switcher

  [Contacts] [CRM] [Sales] [Purchase] [Inventory] [Accounting] [Employees] ...
       \________ shared partner & product truth underneath ________/
```

Apps are how **departments** from Phase 1 become clickable. Salespeople live in CRM/Sales; warehouse staff live in Inventory; accountants live in Accounting — yet they open the same customer.

**DEPENDENCY YOU WILL FEEL LATER:**

Installing Sales pulls in dependencies (often stock/product/partner pieces). You cannot treat apps as isolated USB sticks. Under the hood they are modules with `depends` in a manifest — Story III makes that file concrete.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Map each department from your Phase 1 model to an Odoo app name. |
| Medium | Predict which apps must be installed before “quote → delivery → invoice” works. |
| Hard | Explain why installing *every* app on a tiny company is usually a bad idea. |

**WHAT THIS UNLOCKS NEXT:**

Users say “app.” Developers say “module” or “addon.” Same family of packages — next lesson separates the vocabulary cleanly.

---

## 2.6 Modules / Addons

In Odoo development:

- A **module** (also called an **addon**) is a folder with a `__manifest__.py` that declares name, version, dependencies, and data files.
- Official apps are modules. Your custom feature will be a module. Third-party OCA code is modules.
- **`addons_path`** (Phase 5) tells the server which directories contain modules.

```text
addons/
  sale/                 <- official Sales app (module)
  stock/                <- Inventory
  my_company_flow/      <- YOUR module (Story III)
    __manifest__.py
    models/
    views/
    security/
```

**APP VS MODULE — PLAIN LANGUAGE:**

| Term | Audience | Meaning |
| ---- | -------- | ------- |
| App | End users | Installable business tile / feature set |
| Module / Addon | Developers | Technical package Odoo loads into the registry |

Most apps *are* modules (or a small set of modules). Custom work is almost always “write a module that depends on existing ones,” not “fork Odoo.”

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Open the [odoo/odoo](https://github.com/odoo/odoo) repo in a browser and find `addons/sale/__manifest__.py` (path may vary slightly by version layout). |
| Medium | Read the `depends` list of that manifest and name two modules Sales needs. |
| Hard | In one paragraph, explain why customization belongs in *your* module instead of editing `addons/sale` in place. |

**WHAT THIS UNLOCKS NEXT:**

Modules load into a database that has **users** and sometimes **multiple companies**. Access and company boundaries are business rules, not cosmetics.

---

## 2.7 Users

A **user** is a login that acts inside Odoo with a set of **groups** (permissions). The same human might be a salesperson (see CRM + Sales) but not an accountant (no journal access). Later stories teach ACL and record rules; here you only need: **users are not the same as partners**, and permissions matter from day one.

A **company** (`res.company`) is a legal/operating entity inside the database. Odoo supports **multi-company**: one database, several companies, with rules about which records are shared or company-specific.

```text
Database: quarry_training
  Company A: Gulf Desk Co.
  Company B: Gulf Desk Retail LLC
  User: sara@example.com  →  allowed companies A+B, default A
```

**PARTNER VS USER:**

| Concept | Model (preview) | Meaning |
| ------- | --------------- | ------- |
| Partner | `res.partner` | Customer, vendor, contact — business identity |
| User | `res.users` | Login that can open the UI / API — linked to a partner |

Your customer “Acme Trading” is a partner. Your employee Sara who logs in is a user (and usually also has a partner/employee record). Mixing these ideas causes messy data models later.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List three users your Phase 1 company would need and which apps each should open. |
| Medium | Decide whether your company needs multi-company on day one — justify yes or no. |
| Hard | Describe a risk of giving every user the admin group “just for convenience.” |

**WHAT THIS UNLOCKS NEXT:**

Users act through apps. Apps only cooperate because they share **business records**. That shared core is the next lesson — the heart of Odoo’s modular monolith.

---

## 2.8 Companies

A **user** is a login that acts inside Odoo with a set of **groups** (permissions). The same human might be a salesperson (see CRM + Sales) but not an accountant (no journal access). Later stories teach ACL and record rules; here you only need: **users are not the same as partners**, and permissions matter from day one.

A **company** (`res.company`) is a legal/operating entity inside the database. Odoo supports **multi-company**: one database, several companies, with rules about which records are shared or company-specific.

```text
Database: quarry_training
  Company A: Gulf Desk Co.
  Company B: Gulf Desk Retail LLC
  User: sara@example.com  →  allowed companies A+B, default A
```

**PARTNER VS USER:**

| Concept | Model (preview) | Meaning |
| ------- | --------------- | ------- |
| Partner | `res.partner` | Customer, vendor, contact — business identity |
| User | `res.users` | Login that can open the UI / API — linked to a partner |

Your customer “Acme Trading” is a partner. Your employee Sara who logs in is a user (and usually also has a partner/employee record). Mixing these ideas causes messy data models later.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List three users your Phase 1 company would need and which apps each should open. |
| Medium | Decide whether your company needs multi-company on day one — justify yes or no. |
| Hard | Describe a risk of giving every user the admin group “just for convenience.” |

**WHAT THIS UNLOCKS NEXT:**

Users act through apps. Apps only cooperate because they share **business records**. That shared core is the next lesson — the heart of Odoo’s modular monolith.

---

## 2.9 Shared Business Records

Odoo is not forty separate products with forty databases. It is one PostgreSQL database and one registry of models. Apps cooperate because they **reuse** core records:

| Shared idea | Typical model | Used by |
| ----------- | ------------- | ------- |
| People & orgs | `res.partner` | CRM, Sales, Purchase, Accounting, HR… |
| Products | `product.template` / `product.product` | Sales, Purchase, Inventory, Accounting |
| Company | `res.company` | Almost everything |
| Users / groups | `res.users`, `res.groups` | Security everywhere |
| Currency | `res.currency` | Sales, Accounting |

```text
                res.partner
                 /    |    \
                /     |     \
           crm.lead  sale.order  account.move
                \     |     /
                 \    |    /
                same partner_id, one truth
```

**WHY DEVELOPERS CARE BEFORE THEY CODE:**

When you build a custom module, your first design question is rarely “which cool widget?” It is “which existing model already owns this fact?” Creating a parallel `my.customer` table that duplicates `res.partner` is a classic junior mistake — reports diverge and double-entry becomes impossible.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | From your Phase 1 model, mark each entity as “shared master” or “app-specific transaction.” |
| Medium | Explain why Sales and Accounting both need the same partner record for one invoice flow. |
| Hard | Propose a custom field that *should* extend `res.partner` vs a field that belongs only on `sale.order`. |

**WHAT THIS UNLOCKS NEXT:**

Vocabulary is useless until you click through a live system. The lab forces you to explore a real Odoo database with your company model as a scavenger hunt checklist.

---


<a id="phase-3"></a>
<a id="phase-3---core-business-applications"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 2:** You can explain every lesson title in Phase 2 with a concrete Odoo or business example.

> **Phase 2 complete?** [Continue to Phase 3](#phase-3)


<a id="phase-3"></a>
# PHASE 3 - Core Business Applications

**Track:** Business & ERP Foundations

**WHAT YOU WILL BE ABLE TO DO:** Describe what Contacts, CRM, Sales, Purchase, Inventory, Accounting, and Employees each own; trace a quote through delivery to invoice on shared partners/products; and spot where custom modules usually plug in later.

**WHAT YOU SHOULD KNOW FIRST:** Phase 2 — you have opened an Odoo database and understand apps as modules over shared records. Keep your Phase 1 company model nearby.

**LEARNING RESOURCES:**

- [Odoo Beginner's Guide (YouTube)](https://www.youtube.com/watch?v=QuC6rc2q2mg)
- [Odoo ERP overview (YouTube)](https://www.youtube.com/watch?v=s-4zNx7wCFk)
- [Odoo 18 developer docs](https://www.odoo.com/documentation/18.0/developer.html)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [odoo/odoo](https://github.com/odoo/odoo) — browse `addons/` for `sale`, `stock`, `account`, `hr`, `crm`

## 3.1 Contacts

**Contacts** is the address book of the ERP. Every customer, vendor, employee contact, and individual person tends to live as a **partner**. Companies and individuals can nest (a company partner with child contact partners).

**WHY CONTACTS COME FIRST:**

Without a stable partner, CRM leads, sales orders, purchase orders, and invoices have nothing trustworthy to point at. Contacts is not “the easy app.” It is the **identity hub**.

Typical partner facts: name, address, phone, email, tax ID, language, company vs person, and links to accounting receivable/payable settings later.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Create two contacts: one company, one person under that company. |
| Medium | Decide which fields on your partner are master data vs which should wait for a sales order. |
| Hard | Explain how a vendor and a customer can be the same partner with different roles/flags. |

**WHAT THIS UNLOCKS NEXT:**

Contacts store who people are. CRM tracks whether they might buy — the pipeline before a firm order.

---

## 3.2 CRM

**CRM** manages leads and opportunities: incoming interest, stages (new → qualified → proposition → won/lost), activities (calls, meetings), and team assignment. Winning an opportunity often becomes a quotation in Sales.

```text
Lead / Opportunity stages (example)

  New --> Qualified --> Proposition --> Won --> (create quotation)
                                    \-> Lost
```

CRM answers “where is this deal in the conversation?” It should **not** become a second inventory system. Stock truth stays in Inventory; money truth stays in Accounting.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Create one opportunity for your Phase 1 customer and move it across two stages. |
| Medium | Write the rule your company would use for Lead vs Opportunity. |
| Hard | List data CRM should *not* duplicate from Inventory or Accounting. |

**WHAT THIS UNLOCKS NEXT:**

When the deal is real enough to price and commit, you leave pure pipeline thinking and enter **Sales** documents.

---

## 3.3 Sales

**Sales** owns quotations and sales orders: lines with products, quantities, prices, taxes, and terms. Confirming a quotation makes a **sales order** — a commitment that can drive delivery and invoicing.

```text
draft quotation --> sent --> sales order (confirmed) --> to invoice / to deliver
```

Sales is where your Phase 1 happy path becomes concrete. The order lines reference **products**; the header references a **partner**. Those are the shared records from Phase 2.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Create a quotation with one product line for your contact. |
| Medium | Confirm it and note which statuses / smart buttons appear. |
| Hard | Explain what should happen if the customer changes quantity after confirmation (business policy, not only UI). |

**WHAT THIS UNLOCKS NEXT:**

Sales promises goods. Someone must buy those goods or materials if you do not already hold them — that is **Purchase**.

---

## 3.4 Purchase

**Purchase** handles requests for quotation (RFQs) to vendors and **purchase orders** (POs). Receiving against a PO increases stock (with Inventory) and later creates vendor bills (with Accounting).

```text
Need stock --> RFQ to vendor --> PO confirmed --> Receive products --> Vendor bill
```

Sales and Purchase mirror each other on opposite sides of the partner graph: customers vs vendors, outbound vs inbound. Products are still the same master records when you sell finished goods you also buy.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Name one product you would purchase for your company model. |
| Medium | Sketch RFQ → PO → receive for that product. |
| Hard | Describe a mismatch risk if Purchasing uses a different product name than Sales. |

**WHAT THIS UNLOCKS NEXT:**

Buying and selling only stay honest if **Inventory** tracks what physically moved.

---

## 3.5 Inventory

**Inventory** (Stock) tracks quantities by location, incoming and outgoing shipments (**pickings**), transfers, and optionally lots/serials. A delivery order linked to a sales order is the warehouse’s view of the same commitment Sales made.

```text
Sales order confirmed
        |
        v
Delivery (stock.picking OUT)  -->  validate  -->  stock decreases
        |
        v
Accounting can invoice based on delivered qty (policy-dependent)
```

**RESERVATION AND AVAILABILITY:**

ERP inventory is not a single cell saying “12 chairs.” It is quantities per location, possibly reserved for orders, with moves that have states. That complexity exists so two salespeople cannot sell the last chair twice without the system noticing.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Find where on-hand quantity appears for your product. |
| Medium | Open a delivery related to a sales order and name its status. |
| Hard | Explain why invoicing *ordered* qty vs *delivered* qty are different business policies. |

**WHAT THIS UNLOCKS NEXT:**

Goods moved. Money must be recognized. **Accounting** turns operational documents into financial ones.

---

## 3.6 Accounting

**Accounting** posts customer invoices, vendor bills, payments, journals, and the chart of accounts. In Odoo, customer invoices are typically `account.move` records in the right journal — you will see that technical name later; for now, treat invoices as **financial transactions** linked to partners and, ideally, to sales/purchase documents.

```text
Operational world              Financial world
-----------------              ----------------
sale.order / delivery   -->    customer invoice
purchase + receipt      -->    vendor bill
bank statement          -->    payments / reconciliation
```

Accounting is where “spreadsheet ERP” usually collapses: tax, receivables aging, and audit need structured journals, not a Sales folder of PDF invoices.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Create or open a draft invoice related to your sales order (edition permitting). |
| Medium | Trace which partner and which order lines the invoice inherited. |
| Hard | Explain why validating (posting) an invoice should be harder to undo than editing a draft quotation. |

**WHAT THIS UNLOCKS NEXT:**

Money and goods need people. **Employees / HR** closes the core app tour with the human side of the company.

---

## 3.7 Employees / HR

**Employees** (HR) manages employee records, departments, jobs, and — depending on installed modules — time off, attendance, and contracts. Employees link to partners/users carefully: HR data is sensitive; not every salesperson should see salaries.

For your Phase 1 model, HR answers: who works here, in which department, and who approves what? Workflows in other apps often use employees as **responsible** users or approvers.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Create one employee matching a user role from Phase 2. |
| Medium | Assign that employee to a department that maps to an app they use. |
| Hard | List three HR fields that should have stricter access than a product’s sale price. |

**WHAT THIS UNLOCKS NEXT:**

You have met the core apps one by one. The practice that follows stitches them into the single path every Odoo consultant narrates in interviews: quote → delivery → invoice.

---

## 3.8 Projects

**Projects** belongs to **Core Business Applications**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Projects** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **Projects** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **Projects** is concrete, continue — the next lesson in this phase still depends on it.

## 3.9 Manufacturing

**Manufacturing** belongs to **Core Business Applications**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Manufacturing** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Manufacturing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Manufacturing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Manufacturing** is concrete, continue — the next lesson in this phase still depends on it.

## 3.10 PRACTICE — Trace Quote → Delivery → Invoice

Run this end-to-end in your database (or on paper if the edition blocks a step — still label each document).

```text
1. Contact exists          (Contacts)
2. Opportunity optional    (CRM)
3. Quotation → confirm     (Sales)
4. Delivery validate       (Inventory)
5. Invoice create/post     (Accounting)
6. (Optional) Payment      (Accounting)
```

For each step write: **document name/number**, **status**, **partner**, **product qty**. Then answer: where would a custom “customer PO number” field belong — partner, order, or invoice?

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Complete the happy path once and keep a written trace table. |
| Medium | Break the path on purpose (deliver partial qty) and note what invoice policy should do. |
| Hard | Draw the same path as boxes for models you predict (`res.partner`, `sale.order`, …) — refine later in Story II. |

**WHAT THIS UNLOCKS NEXT:**

Story I taught *what* the business needs and *which* Odoo apps cover it. You still cannot explain **how** a browser click becomes a PostgreSQL row. That mechanism gap opens Story II — architecture, local setup, and the source tree.

---


---


---


**CHECK YOUR UNDERSTANDING AFTER PHASE 3:** You can explain every lesson title in Phase 3 with a concrete Odoo or business example.

> **Phase 3 complete?** [Continue to Phase 4](#phase-4)


## STORY PROJECT — Design a Company's ERP Flow

**Track:** Business & ERP Foundations

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY II — HOW ODOO ACTUALLY WORKS

**Track:** Platform Internals

*Before changing Odoo, understand what runs underneath it.*

---
<a id="phase-4"></a>
<a id="phase-4---odoo-architecture"></a>
# PHASE 4 - Odoo Architecture

**Track:** Platform Internals

**WHAT YOU WILL BE ABLE TO DO:** Trace a browser action through the web client, Odoo server, PostgreSQL, and filestore; explain where addons and the registry sit; and sketch the three-tier picture without confusing “Odoo” with “the database alone.”

**WHAT YOU SHOULD KNOW FIRST:** Story I — business apps and shared records. You do not need to have compiled Odoo yet; Phase 5 installs it. Comfortable with “client / server / database” as everyday words.

**LEARNING RESOURCES:**

- [Odoo 18 — Architecture](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/01_architecture.html)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [Odoo developer landing](https://www.odoo.com/documentation/18.0/developer.html)
- [odoo/odoo](https://github.com/odoo/odoo)
- [odoo/docker](https://github.com/odoo/docker)
- [Odoo Beginner's Guide (YouTube)](https://www.youtube.com/watch?v=QuC6rc2q2mg)

## 4.1 Three-Tier Architecture

**Three-Tier Architecture** belongs to **Odoo Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Three-Tier Architecture** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Three-Tier Architecture** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Three-Tier Architecture**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Three-Tier Architecture** is concrete, continue — the next lesson in this phase still depends on it.

## 4.2 Browser / Web Client

When you open Odoo in Chrome or Firefox, you are using the **web client**: HTML, CSS, and JavaScript that draw menus, forms, lists, and kanban boards. The browser does **not** contain your sales orders permanently. It asks the server for data and sends user actions back (save, confirm, validate).

```text
BROWSER (client)
  - renders views (forms, lists, kanban)
  - holds temporary UI state
  - talks HTTP/JSON to the Odoo server
  - never is the system of record
```

In modern Odoo the client includes OWL components for richer UI. You do not need OWL yet (a later story). You only need: **the client is a guest; the server owns truth.**

**WHEN THE UI LIES:**

If two users edit the same draft order, the client that last saved wins — unless business logic and concurrency rules say otherwise. Trusting only what one browser shows is how support tickets start. Always ask: did the server accept this?

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Open Odoo, press F12 → Network, click Save on a record, and note that a request leaves the browser. |
| Medium | Explain why clearing browser cache does not delete sales orders. |
| Hard | List three things that must live on the server (not only in JS) for audit reasons. |

**WHAT THIS UNLOCKS NEXT:**

The client sends requests somewhere. That somewhere is the **Odoo server** — Python processes that understand modules, security, and the ORM.

---

## 4.3 Odoo Application Server

The **Odoo server** is a Python application. It loads addons, builds the **registry** of models, checks access rules, runs business methods, and talks to PostgreSQL. When you click “Confirm” on a quotation, the client calls a server method; the server validates, writes rows, may create a delivery, and returns updated data to the client.

```text
HTTP request
    |
    v
Odoo server (Python)
    |-- authentication / session
    |-- ACL + record rules
    |-- ORM methods (create/write/action_confirm/...)
    |-- cursors / transactions
    v
PostgreSQL (+ filestore for binaries)
```

Think of the server as the **rules engine + API** for the company database. Custom modules extend this server. They do not replace the browser.

**MULTI-WORKER MENTAL MODEL (LIGHT):**

In production you may run multiple HTTP workers. For learning, one server process is enough. The idea stays: many browsers, one logical application talking to one database name you configured.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In one sentence: what does the Odoo server do that the browser does not? |
| Medium | Map “Confirm quotation” to: client event → server method → DB writes → UI refresh. |
| Hard | Predict what happens if the server crashes after writing the order but before responding — what should a retry-safe design assume? (Qualitative answer OK.) |

**WHAT THIS UNLOCKS NEXT:**

The server still needs a place to store structured business data permanently. That place is **PostgreSQL**.

---

## 4.4 PostgreSQL

Odoo stores almost all business records in **PostgreSQL**. Models become tables (roughly); fields become columns; relational fields become foreign keys or intermediate tables. When you search customers in Contacts, the server runs SQL through the ORM — you rarely write raw SQL as a beginner, but you must know **where** the data lives.

```text
res.partner  ~  table res_partner
sale.order   ~  table sale_order
sale.order.line ~ table sale_order_line
```

**WHY POSTGRES, NOT “SQLITE FOR EVERYTHING”:**

ERP workloads need concurrent writers, solid transactions, and mature backups. PostgreSQL is the supported database for Odoo deployments. Learning Odoo includes eventually reading rows with `psql` during debugging — Phase 5 gets you connected; Phase later deepens tuning.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write: “Odoo business data lives primarily in ____.” |
| Medium | Explain why deleting a database in Postgres deletes the company’s records even if the Odoo code folder remains. |
| Hard | Distinguish database name (e.g. `odoo18`) from PostgreSQL *server* instance (host/port). |

**WHAT THIS UNLOCKS NEXT:**

Not everything fits neatly in a table cell — PDFs, images, attachments need files. That is the **filestore**.

---

## 4.5 Filestore

The **filestore** is a directory on disk where Odoo keeps binary attachments (uploaded PDFs, images, some assets). Metadata about the attachment lives in PostgreSQL; the bytes often live as files keyed by checksum/path.

```text
PostgreSQL                         Filestore directory
ir_attachment row  ------------->  /filestore/<db>/ab/abcd1234...
  name, res_model, res_id          actual PDF bytes
```

**BACKUP IMPLICATION YOU MUST NOT SKIP:**

A SQL-only dump without the filestore loses attachments. A filestore copy without the database loses meaning. Real backups capture **both**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Attach a small image to a contact; note that something beyond “a text field” was stored. |
| Medium | Explain why copying only the `addons` folder is not a backup of customer data. |
| Hard | Sketch a backup checklist: Postgres dump + filestore path + which config points to them. |

**WHAT THIS UNLOCKS NEXT:**

Database and filestore hold runtime state. **Addons** hold the code and XML that define how that state is shaped.

---

## 4.6 Addons

**Addons** (modules) are the packages on disk: official apps under Odoo’s addons tree, plus your custom folders. At startup (and on module install/upgrade), the server reads manifests, imports Python, and loads XML/CSV data.

```text
addons_path directories
    |
    +-- sale/
    +-- stock/
    +-- account/
    +-- my_flow/          (your future module)
```

Without addons, Odoo is an empty framework shell. With addons, it becomes Sales, Inventory, Accounting — and your customizations.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Name three addon folders you expect to exist in a Sales+Inventory database. |
| Medium | Explain the difference between “addon code on disk” and “module installed in this database.” |
| Hard | Why can two databases on one server have different installed module sets while sharing the same addons_path? |

**WHAT THIS UNLOCKS NEXT:**

Loaded addons populate the **registry** — the in-memory map of model names to Python classes the ORM uses.

---

## 4.7 Registry

The **registry** is Odoo’s living catalog of models for a database: `res.partner`, `sale.order`, your future `my.model`, each bound to a Python class with fields and methods. When modules install or upgrade, the registry is updated so the server knows the current shape of the business schema.

```text
Module Python classes
        |
        v
   Registry[db]
        |
        +-- "res.partner" -> ResPartner class
        +-- "sale.order"  -> SaleOrder class
        +-- "my.model"    -> MyModel class
        |
        v
   ORM uses registry to create env["sale.order"] recordsets
```

You will type `self.env["sale.order"]` constantly later. That lookup **is** the registry. No registry entry means no model, no matter what SQL tables leftover from a botched upgrade might still exist.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In your own words: registry maps ____ to ____. |
| Medium | Explain why restarting/upgrading can be required after changing model Python code. |
| Hard | Predict a failure symptom if a module failed halfway through install (registry vs database mismatch). |

**WHAT THIS UNLOCKS NEXT:**

Architecture on paper is not the same as watching one click travel the tiers. The lab makes you trace browser → server → database deliberately.

---

## 4.8 ORM

**ORM** belongs to **Odoo Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **ORM** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ORM** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ORM**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ORM** is concrete, continue — the next lesson in this phase still depends on it.

## 4.9 HTTP Layer

**HTTP Layer** belongs to **Odoo Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **HTTP Layer** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **HTTP Layer** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **HTTP Layer**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **HTTP Layer** is concrete, continue — the next lesson in this phase still depends on it.

## 4.10 LAB — Trace Browser → Server → Database

**LAB GOAL:** Prove the three tiers with evidence, not slogans.

1. Open browser DevTools → Network.  
2. Perform a visible write (create/save a contact).  
3. Identify the HTTP call to your Odoo host.  
4. On the server side (if local): watch the Odoo log line for that request (Phase 5 deepens logging).  
5. Optional if you have `psql`: `SELECT id, name FROM res_partner ORDER BY id DESC LIMIT 3;` and find your new row.  

```text
Evidence chain

[UI Save click]
    -> Network: /web/... or JSON-RPC call
    -> Server log: request / traceback if error
    -> Postgres: new/updated row in res_partner
```

Write a five-line lab note tying each tier to what you observed.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Capture one network request name/URL from a save. |
| Medium | Match that save to a SQL row (or explain what blocked you). |
| Hard | Trace a *Confirm* on a quotation across tiers — which extra tables might change? |

**WHAT THIS UNLOCKS NEXT:**

You can picture the running system. You still may not have a personal copy. Phase 5 builds the **development environment** so architecture becomes something you start and stop on your machine.

---


---


<a id="phase-5"></a>
<a id="phase-5---development-environment"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 4:** You can explain every lesson title in Phase 4 with a concrete Odoo or business example.

> **Phase 4 complete?** [Continue to Phase 5](#phase-5)


<a id="phase-5"></a>
# PHASE 5 - Development Environment

**Track:** Platform Internals

**WHAT YOU WILL BE ABLE TO DO:** Prepare a Python environment, PostgreSQL, and Odoo source; write a working configuration with `addons_path`; enable developer mode; read logs; and run Odoo locally for module work.

**WHAT YOU SHOULD KNOW FIRST:** Phase 4 architecture. Basic comfort with a terminal on your OS. Official reference: [Install from source](https://www.odoo.com/documentation/18.0/administration/on_premise/source.html).

**LEARNING RESOURCES:**

- [Odoo 18 — Install from source](https://www.odoo.com/documentation/18.0/administration/on_premise/source.html)
- [Odoo 18 — Architecture](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/01_architecture.html)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [odoo/odoo](https://github.com/odoo/odoo)
- [odoo/docker](https://github.com/odoo/docker) — alternative path
- [odoo/documentation](https://github.com/odoo/documentation)

## 5.1 Python Environment

Odoo 18 expects a supported **Python 3** version (check the source install doc for the exact range — do not guess from older blog posts). Use a **virtual environment** so project dependencies do not fight your system Python.

```text
python -m venv venv
# Windows PowerShell:
.\venv\Scripts\Activate.ps1
# Linux/macOS:
source venv/bin/activate
pip install -r requirements.txt   # from the Odoo source tree
```

**WHY VENV IS NON-NEGOTIABLE:**

Global `pip install` of Odoo’s dependency pin set is how machines rot. One venv per Odoo version keeps 16/17/18 from colliding when you maintain client projects later.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Create and activate a venv; run `python -V` and record the version. |
| Medium | Install Odoo’s `requirements.txt` into that venv (resolve OS-specific notes from official docs). |
| Hard | Explain what goes wrong if two projects share one global Django/Odoo dependency set. |

**WHAT THIS UNLOCKS NEXT:**

Python runs the server. PostgreSQL must accept connections before any database can be created.

---

## 5.2 PostgreSQL Setup

Install PostgreSQL locally (or use Docker). Create a role Odoo can use, with permission to create databases (typical for development).

```text
Example (illustrative — follow your OS docs):

  createuser --createdb odoo
  # set a password you will put in odoo.conf
```

Remember host (`localhost`), port (`5432` default), user, and password. These four values belong in the Odoo config file next.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Connect with `psql` (or a GUI) and run `SELECT version();`. |
| Medium | Create an empty database manually once — then prefer letting Odoo create DBs from the UI/CLI. |
| Hard | Document how you would reset a broken training database without deleting Postgres itself. |

**WHAT THIS UNLOCKS NEXT:**

Database engine ready. Next you need the **Odoo source** that knows how to speak to it.

---

## 5.3 Odoo Source

Clone the official repository at the branch matching your version (18.0 for this roadmap):

```text
git clone https://github.com/odoo/odoo.git --branch 18.0 --depth 1
```

You will run `odoo-bin` (or the equivalent entry point in that tree) with a config file. Community source is enough for learning modules.

**DOCKER ALTERNATIVE:**

If source install fights your OS, [odoo/docker](https://github.com/odoo/docker) can give you a running instance. Still learn where addons live and how config maps — do not treat Docker as “no config literacy.”

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Confirm `odoo-bin` exists in your clone. |
| Medium | Skim top-level folders; note `odoo/` vs `addons/` (Phase 6 deepens). |
| Hard | Pin the commit/branch you are on in your notes for reproducibility. |

**WHAT THIS UNLOCKS NEXT:**

Source without configuration still does not know your Postgres password or custom addons directory. That is the **configuration file**.

---

## 5.4 Configuration File

Create an `odoo.conf` (path yours to choose) with at least:

```ini
[options]
admin_passwd = choose_a_master_password
db_host = localhost
db_port = 5432
db_user = odoo
db_password = your_password
addons_path = /path/to/odoo/addons,/path/to/your/custom_addons
http_port = 8069
logfile = /path/to/odoo.log
```

`admin_passwd` protects database manager operations (create/drop DB) — not the same as an internal Odoo user password. Keep it secret on shared machines.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write a config skeleton with your real paths. |
| Medium | Start Odoo pointing at that config; open `http://localhost:8069`. |
| Hard | Create a training database from the DB manager and log in as admin. |

**WHAT THIS UNLOCKS NEXT:**

The most important config line for developers is **`addons_path`** — where your future modules must live.

---

## 5.5 addons_path

`addons_path` is a comma-separated list of directories Odoo scans for modules. Order can matter when the same module name appears twice (avoid duplicates). Typical layout:

```text
addons_path =
  <odoo_source>/addons,
  <odoo_source>/odoo/addons,   # depending on version layout
  <workspace>/custom_addons
```

Your Story III module will sit under `custom_addons/my_hello/` — **never** by editing core `sale/` in place.

```text
WRONG:  modify odoo/addons/sale/models/sale_order.py for a client
RIGHT:  custom_addons/client_sale_ext/ that _inherit sale.order
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Print your effective `addons_path` from config and verify folders exist. |
| Medium | Create an empty `custom_addons` directory and add it to `addons_path`. |
| Hard | Explain how a missing path entry produces “module not found” on install. |

**WHAT THIS UNLOCKS NEXT:**

Server runs; modules path is set. To see technical names and view structures in the UI, turn on **Developer Mode**.

---

## 5.6 Custom Addons Directory

`addons_path` is a comma-separated list of directories Odoo scans for modules. Order can matter when the same module name appears twice (avoid duplicates). Typical layout:

```text
addons_path =
  <odoo_source>/addons,
  <odoo_source>/odoo/addons,   # depending on version layout
  <workspace>/custom_addons
```

Your Story III module will sit under `custom_addons/my_hello/` — **never** by editing core `sale/` in place.

```text
WRONG:  modify odoo/addons/sale/models/sale_order.py for a client
RIGHT:  custom_addons/client_sale_ext/ that _inherit sale.order
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Print your effective `addons_path` from config and verify folders exist. |
| Medium | Create an empty `custom_addons` directory and add it to `addons_path`. |
| Hard | Explain how a missing path entry produces “module not found” on install. |

**WHAT THIS UNLOCKS NEXT:**

Server runs; modules path is set. To see technical names and view structures in the UI, turn on **Developer Mode**.

---

## 5.7 Developer Mode

In Odoo: Settings → activate **Developer mode** (or use `?debug=1` patterns documented for your version). Developer mode reveals:

- Technical menus (Views, Models, Actions)
- “Edit view” / debugging affordances
- Field technical names on forms (hover / debug widgets)

```text
User view:  "Customer Name"
Dev mode:   partner_id / res.partner / view xml id
```

This mode is how Story I’s business words connect to Story III’s XML ids. Leave it on while learning; teach end users to leave it off.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Enable developer mode; open a contact; find a technical field name. |
| Medium | Open Settings → Technical → Models; search `res.partner`. |
| Hard | Find the external ID of a menu item you use daily. |

**WHAT THIS UNLOCKS NEXT:**

When something fails, the UI is not enough. **Logging** is how the server narrates errors and request traces.

---

## 5.8 Debug Assets Mode

In Odoo: Settings → activate **Developer mode** (or use `?debug=1` patterns documented for your version). Developer mode reveals:

- Technical menus (Views, Models, Actions)
- “Edit view” / debugging affordances
- Field technical names on forms (hover / debug widgets)

```text
User view:  "Customer Name"
Dev mode:   partner_id / res.partner / view xml id
```

This mode is how Story I’s business words connect to Story III’s XML ids. Leave it on while learning; teach end users to leave it off.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Enable developer mode; open a contact; find a technical field name. |
| Medium | Open Settings → Technical → Models; search `res.partner`. |
| Hard | Find the external ID of a menu item you use daily. |

**WHAT THIS UNLOCKS NEXT:**

When something fails, the UI is not enough. **Logging** is how the server narrates errors and request traces.

---

## 5.9 Logging

Configure `logfile` and optionally log levels. In the terminal, Odoo prints tracebacks when a module fails to import or a method raises. Read from the **bottom** of a Python traceback up to the first of *your* files.

```text
Useful habits:

  - Reproduce once with logs visible
  - Copy the EXCEPTION line + your module frames
  - Fix, upgrade/restart, reproduce again
```

Log spam is normal. Learn to grep for your module name (`my_hello`) so core noise does not hide your bug.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Trigger a harmless error (optional) or simply locate the logfile path. |
| Medium | Grep the log for `ERROR` after a failed operation. |
| Hard | Explain the difference between a client-side JS error (browser console) and a server traceback (logfile). |

**WHAT THIS UNLOCKS NEXT:**

Pieces are ready. The lab demands a full local run — the rite of passage before reading the source tree in Phase 6.

---

## 5.10 LAB — Run Odoo Locally

**LAB CHECKLIST:**

1. venv active, requirements installed  
2. PostgreSQL accepting connections  
3. `odoo.conf` with correct `addons_path`  
4. Server starts without immediate crash  
5. Browser opens the database selector / login  
6. Developer mode on  
7. Create a note titled “my local URL + DB name”  

```text
odoo-bin -c odoo.conf
# or: python odoo-bin -c odoo.conf
```

If start fails, fix environment issues **now**. Story III assumes you can install a module into a DB you control.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Complete the checklist; keep the server running for five minutes of clicking. |
| Medium | Stop and restart the server; confirm the database still contains your data. |
| Hard | Add a second database for experiments so you never test upgrades only on “precious” data. |

**WHAT THIS UNLOCKS NEXT:**

You can run Odoo. Phase 6 teaches you to **navigate the source** so “server” stops being a black box and becomes folders you can open with intent.

---


---


<a id="phase-6"></a>
<a id="phase-6---odoo-source-code-structure"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 5:** You can explain every lesson title in Phase 5 with a concrete Odoo or business example.

> **Phase 5 complete?** [Continue to Phase 6](#phase-6)


<a id="phase-6"></a>
# PHASE 6 - Odoo Source Code Structure

**Track:** Platform Internals

**WHAT YOU WILL BE ABLE TO DO:** Orient inside the Odoo repository: core `odoo/` package vs `addons/`, where models, HTTP, and services live, how the registry is populated from code, and find files on purpose instead of by desperate search.

**WHAT YOU SHOULD KNOW FIRST:** Phase 5 — you can start Odoo locally and open developer mode. Git clone of 18.0 available on disk.

**LEARNING RESOURCES:**

- [Odoo 18 — Architecture](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/01_architecture.html)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [Backend reference / tutorials](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [odoo/odoo](https://github.com/odoo/odoo)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [odoo/documentation](https://github.com/odoo/documentation)

## 6.1 odoo/

The **`odoo/`** Python package is the framework: ORM engine, CLI, HTTP stack, modules loading, OS abstractions, testing harness pieces, and more. When people say “the ORM,” much of that machinery lives here — not inside `addons/sale`.

```text
odoo/
  __main__ / cli entry pieces
  orm / models machinery (layout evolves by version — navigate, do not memorize paths blindly)
  http.py and controllers support
  modules/ loading & registry building
  service/  server & workers
  ...
```

**RULE OF THUMB:**

Fixing a business need by patching core `odoo/` is almost always wrong for application work. Core is for framework contributors. You extend via **addons**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Open the `odoo/` directory; list five top-level subfolders in your notes. |
| Medium | Find a file whose name suggests HTTP or module loading; skim only headers/docstrings. |
| Hard | Write one paragraph: “I will change business behavior in ____, not in odoo/core, because ____.” |

**WHAT THIS UNLOCKS NEXT:**

Framework alone sells nothing. **`addons/`** is where Sales, Stock, and your future features live.

---

## 6.2 addons/

**`addons/`** holds installable modules. Each subdirectory with a manifest is a candidate module. Official business apps live here in Community source.

```text
addons/
  base/          # fundamental models (partners, users, ...) — critical
  web/           # web client pieces
  sale/
  stock/
  account/
  crm/
  hr/
  ...
```

When your `addons_path` includes this folder, Odoo can install `sale`. Your `custom_addons` mirrors this structure for private modules.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Open `addons/sale/__manifest__.py` and read `name`, `depends`, `data`. |
| Medium | Count how many XML files the sale manifest loads (approx). |
| Hard | Follow one `depends` entry into that module’s manifest — sketch a tiny dependency chain. |

**WHAT THIS UNLOCKS NEXT:**

Inside any business addon, **models/** is where Python classes declare fields and methods — the heart of backend work.

---

## 6.3 Core Framework

The **`odoo/`** Python package is the framework: ORM engine, CLI, HTTP stack, modules loading, OS abstractions, testing harness pieces, and more. When people say “the ORM,” much of that machinery lives here — not inside `addons/sale`.

```text
odoo/
  __main__ / cli entry pieces
  orm / models machinery (layout evolves by version — navigate, do not memorize paths blindly)
  http.py and controllers support
  modules/ loading & registry building
  service/  server & workers
  ...
```

**RULE OF THUMB:**

Fixing a business need by patching core `odoo/` is almost always wrong for application work. Core is for framework contributors. You extend via **addons**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Open the `odoo/` directory; list five top-level subfolders in your notes. |
| Medium | Find a file whose name suggests HTTP or module loading; skim only headers/docstrings. |
| Hard | Write one paragraph: “I will change business behavior in ____, not in odoo/core, because ____.” |

**WHAT THIS UNLOCKS NEXT:**

Framework alone sells nothing. **`addons/`** is where Sales, Stock, and your future features live.

---

## 6.4 Models

Module `models/` packages define ORM models: `_name`, fields, constraints, business methods. Example shape (Odoo 18 style):

```python
from odoo import models, fields

class SaleOrder(models.Model):
    _inherit = "sale.order"

    client_po = fields.Char(string="Customer PO")
```

Official modules use `_name` for new models and `_inherit` to extend. You will write both starting in Story III–IV. For now, practice **finding** `class SaleOrder` in `addons/sale` and noticing inheritance patterns.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Locate a model file under `addons/sale/models/` and note a `_name` or `_inherit`. |
| Medium | Find a `fields.Char` or `fields.Many2one` example; write what it represents in business words. |
| Hard | Explain why `_inherit = "sale.order"` is safer for customization than copying the whole sale module. |

**WHAT THIS UNLOCKS NEXT:**

Models are not only called from the UI. **HTTP** controllers expose routes for web pages, JSON, and integrations.

---

## 6.5 HTTP

Odoo’s HTTP layer registers **controllers** — Python classes with `@http.route` endpoints. The web client uses this heavily; later you will add webhook routes carefully (security story).

```python
# Conceptual shape — read real examples in addons, do not invent production routes yet
from odoo import http

class MyController(http.Controller):
    @http.route("/my/hello", type="http", auth="public")
    def hello(self, **kwargs):
        return "Hello"
```

For Story II, your job is orientation: know that **not everything is an ORM call from `/web`**. Some URLs hit controllers directly.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Search the codebase for `@http.route` and open one simple example. |
| Medium | Note the `auth=` value on that route (`user`, `public`, `none`) and what it implies. |
| Hard | Explain why a public route that writes partner data is dangerous. |

**WHAT THIS UNLOCKS NEXT:**

Long-running server concerns — workers, cron-like duties, startup — sit in **services**. A light pass is enough here.

---

## 6.6 Services

**Services** cover how the Odoo process runs: starting HTTP, managing workers, scheduled actions triggering server-side jobs, and related process orchestration. Exact file names shift across versions; conceptually remember:

```text
CLI start -> service boots -> registry loaded per DB -> HTTP listens -> crons fire methods
```

When your module’s scheduled action “does nothing,” the bug might be cron service/domain — not only your method body. You will debug that later; today, know the layer exists.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In docs or code, find where “worker” or “cron” is discussed at a high level. |
| Medium | Enable a scheduled action in a DB (Settings) and see it listed — do not write one yet. |
| Hard | Contrast “HTTP request lifecycle” with “cron job lifecycle” in three bullets. |

**WHAT THIS UNLOCKS NEXT:**

Models, HTTP, and services all hang off the **registry** loaded from modules — revisit it now as a source-code phenomenon, not only an architecture bubble.

---

## 6.7 Tools

**Tools** belongs to **Odoo Source Code Structure**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Tools** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Tools** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Tools**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Tools** is concrete, continue — the next lesson in this phase still depends on it.

## 6.8 Registry

Phase 4 introduced the registry as a map. In source terms, module loading imports model classes and registers them by `_name`. Installing a module may create/alter tables to match fields. Upgrading reloads Python and applies updated XML.

```text
import module models
      |
      v
register classes on Registry
      |
      v
env["model.name"] -> recordset API
```

When you change Python field definitions, databases need **`-u your_module`** (Phase 8) so registry and schema converge again.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In developer mode, open Models and pick `sale.order`; note it “comes from” modules. |
| Medium | Explain: code on disk changed, but DB not upgraded — who is stale, registry or files? |
| Hard | Predict symptoms of two modules defining the same `_name` accidentally. |

**WHAT THIS UNLOCKS NEXT:**

Orientation drills beat wandering. The practice lesson is a scavenger hunt through the repository with a checklist.

---

## 6.9 Reading Official Addons

**`addons/`** holds installable modules. Each subdirectory with a manifest is a candidate module. Official business apps live here in Community source.

```text
addons/
  base/          # fundamental models (partners, users, ...) — critical
  web/           # web client pieces
  sale/
  stock/
  account/
  crm/
  hr/
  ...
```

When your `addons_path` includes this folder, Odoo can install `sale`. Your `custom_addons` mirrors this structure for private modules.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Open `addons/sale/__manifest__.py` and read `name`, `depends`, `data`. |
| Medium | Count how many XML files the sale manifest loads (approx). |
| Hard | Follow one `depends` entry into that module’s manifest — sketch a tiny dependency chain. |

**WHAT THIS UNLOCKS NEXT:**

Inside any business addon, **models/** is where Python classes declare fields and methods — the heart of backend work.

---

## 6.10 PRACTICE — Navigate the Odoo Repository

Complete this scavenger hunt in your 18.0 tree:

| # | Find | Write down |
| - | ---- | ---------- |
| 1 | `odoo-bin` | full path |
| 2 | `addons/base/__manifest__.py` | version field |
| 3 | a `sale.order` model definition | file path |
| 4 | one `@http.route` | path string + auth |
| 5 | `web` module manifest | one dependency |
| 6 | your `custom_addons` folder | empty or ready |

Timebox: 45–60 minutes. The goal is **muscle memory for where things live**, not reading every file.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Finish rows 1–3 of the hunt. |
| Medium | Finish the full table. |
| Hard | Add row 7: locate where QWeb or OWL assets are declared for `web` — note path only. |

**WHAT THIS UNLOCKS NEXT:**

Story II ends when your machine is a real **workspace**, not a pile of installs. The story project binds config, paths, and habits before Story III asks you to author a module.

---


---


---


**CHECK YOUR UNDERSTANDING AFTER PHASE 6:** You can explain every lesson title in Phase 6 with a concrete Odoo or business example.

> **Phase 6 complete?** [Continue to Phase 7](#phase-7)


## STORY PROJECT — Local Odoo Development Workspace

**Track:** Platform Internals

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY III — YOUR FIRST ODOO MODULE

**Track:** First Module

*Odoo changes should live inside modules, not scattered patches.*

---
<a id="phase-7"></a>
<a id="phase-7---module-anatomy"></a>
# PHASE 7 - Module Anatomy

**Track:** First Module

**WHAT YOU WILL BE ABLE TO DO:** Explain every standard folder in an Odoo addon, author a minimal `__manifest__.py` and `__init__.py`, place models/views/security/data/static correctly, and ship a “Hello Odoo” module that installs cleanly.

**WHAT YOU SHOULD KNOW FIRST:** Story II workspace — local Odoo runs; `custom_addons` is on `addons_path`; developer mode works. Comfortable creating folders and editing text files.

**LEARNING RESOURCES:**

- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [Odoo 18 developer docs](https://www.odoo.com/documentation/18.0/developer.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [odoo/odoo](https://github.com/odoo/odoo) — copy structure from a tiny official module
- [Module walkthrough ideas (YouTube — ORM/dev)](https://www.youtube.com/watch?v=5YIwP9-55Qk)

## 7.1 Addon Directory

**Addon Directory** belongs to **Module Anatomy**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Addon Directory** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Addon Directory** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Addon Directory**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Addon Directory** is concrete, continue — the next lesson in this phase still depends on it.

## 7.2 __manifest__.py

Every module needs a **`__manifest__.py`** (a Python dict) describing the package to Odoo:

```python
{
    "name": "Hello Odoo",
    "version": "18.0.1.0.0",
    "summary": "Minimal training module",
    "category": "Training",
    "author": "Your Name",
    "license": "LGPL-3",
    "depends": ["base"],
    "data": [
        "security/ir.model.access.csv",
        "views/hello_views.xml",
    ],
    "installable": True,
    "application": True,
}
```

**KEYS YOU MUST RESPECT EARLY:**

| Key | Why it matters |
| --- | -------------- |
| `version` | Upgrade discipline; Odoo 18 modules commonly start with `18.0.` |
| `depends` | Other modules must load first (`base` is the usual root) |
| `data` | XML/CSV load **in list order** |
| `installable` | `False` hides broken WIP modules |
| `application` | `True` shows an app-style entry when appropriate |

Wrong paths in `data` cause install failures. Missing `depends` causes “model not found” when your XML references another app’s views.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Create `custom_addons/hello_odoo/__manifest__.py` with `depends: ["base"]`. |
| Medium | Add empty `data` list first; install later after files exist. |
| Hard | Compare your manifest to `addons/note/__manifest__.py` (or similar small module); list three keys you omitted on purpose. |

**WHAT THIS UNLOCKS NEXT:**

The manifest advertises the module. Python still needs **`__init__.py`** so imports load your model package.

---

## 7.3 __init__.py

Odoo imports your module as a Python package. Top-level `__init__.py` typically imports subpackages:

```python
# hello_odoo/__init__.py
from . import models
```

And `models/__init__.py` imports each model file:

```python
# hello_odoo/models/__init__.py
from . import hello_model
```

**IF YOU FORGET AN IMPORT:**

The file can exist on disk and still never register a model. Symptom: “model hello.model does not exist” while your `.py` looks fine. Always check the import chain before doubting the ORM.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Create both `__init__.py` files with the imports above (file may be empty until 7.3). |
| Medium | Deliberately omit a model import once in a throwaway DB and recognize the symptom — then fix it. |
| Hard | Explain why `__init__.py` matters more in Odoo modules than in some modern namespace-package layouts. |

**WHAT THIS UNLOCKS NEXT:**

Imports ready. **`models/`** is where your first business object is born.

---

## 7.4 models/

Create a simple model so the module does something real:

```python
# hello_odoo/models/hello_model.py
from odoo import models, fields

class HelloMessage(models.Model):
    _name = "hello.message"
    _description = "Hello Message"

    name = fields.Char(required=True)
    note = fields.Text()
```

`_name` is the registry key and roughly the table name (`hello_message`). `_description` shows up in technical UI. Fields declare columns the ORM manages.

```text
models/hello_model.py
        |
        v
  registry["hello.message"]
        |
        v
  table hello_message (after install)
```

You are not building Sales. You are proving **your code can enter the registry**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `HelloMessage` as above; ensure imports wire it. |
| Medium | Add a second `Char` field; plan to upgrade in Phase 8 to see schema change. |
| Hard | Predict the PostgreSQL table name Odoo will use; verify after install with developer mode or SQL. |

**WHAT THIS UNLOCKS NEXT:**

A model without screens is invisible to normal users. **`views/`** give them list/form UI.

---

## 7.5 views/

Views are XML records describing UI. Minimal list + form + action + menu:

```xml
<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="view_hello_message_list" model="ir.ui.view">
    <field name="name">hello.message.list</field>
    <field name="model">hello.message</field>
    <field name="arch" type="xml">
      <list>
        <field name="name"/>
        <field name="note"/>
      </list>
    </field>
  </record>

  <record id="view_hello_message_form" model="ir.ui.view">
    <field name="name">hello.message.form</field>
    <field name="model">hello.message</field>
    <field name="arch" type="xml">
      <form>
        <sheet>
          <group>
            <field name="name"/>
            <field name="note"/>
          </group>
        </sheet>
      </form>
    </field>
  </record>

  <record id="action_hello_message" model="ir.actions.act_window">
    <field name="name">Hello Messages</field>
    <field name="res_model">hello.message</field>
    <field name="view_mode">list,form</field>
  </record>

  <menuitem id="menu_hello_root"
            name="Hello Odoo"
            sequence="60"/>
  <menuitem id="menu_hello_message"
            name="Messages"
            parent="menu_hello_root"
            action="action_hello_message"
            sequence="10"/>
</odoo>
```

List the file in manifest `data`. In Odoo 18, list views use `<list>` (older docs say `<tree>` — follow 18.x examples when they differ).

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `views/hello_views.xml` and reference it from the manifest. |
| Medium | Change the menu label; upgrade later to see XML reload. |
| Hard | In developer mode after install, open the view record and find your XML id. |

**WHAT THIS UNLOCKS NEXT:**

UI without **security** installs as admin theater — normal users hit AccessError. Next folder fixes that.

---

## 7.6 security/

Minimum viable access: `security/ir.model.access.csv` granting your group (or `base.group_user`) model rights:

```csv
id,name,model_id:id,group_id:id,perm_read,perm_write,perm_create,perm_unlink
access_hello_message_user,hello.message.user,model_hello_message,base.group_user,1,1,1,1
```

The `model_hello_message` xml id is auto-convention from model `hello.message` (dots → underscores, `model_` prefix). Load this CSV **before** views that assume access, or at least include it in `data` before users need it — common pattern: security first in the manifest list.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add the CSV and put it first in `data`. |
| Medium | Log in as a non-admin demo user after install and open the menu. |
| Hard | Remove write permission, upgrade, and observe who can still edit (admin vs user). |

**WHAT THIS UNLOCKS NEXT:**

Security lets users in. **`data/`** (optional now, important soon) loads seed records and configuration XML.

---

## 7.7 data/

Use `data/` for master-ish records, sequences, default configs — XML or CSV that should exist after install. Example:

```xml
<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="hello_message_welcome" model="hello.message">
    <field name="name">Welcome</field>
    <field name="note">Installed by data XML</field>
  </record>
</odoo>
```

**NOUPDATE VS NOCREATE:**

Data files can create records on install. Upgrades may update them depending on XML ids and flags (`noupdate`). Phase 8 returns to load order and upgrade behavior — for Hello Odoo, one demo record is enough optional credit.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Skip data/ for the absolute minimum — or add one welcome record. |
| Medium | Give your data record a stable `id=` and find it under Technical → Records after install. |
| Hard | Predict what happens on upgrade if you change the welcome note in XML (depends on noupdate). |

**WHAT THIS UNLOCKS NEXT:**

Backend works. **`static/`** holds assets (JS/CSS/images) when the client needs them — know the folder even if Hello Odoo stays backend-only.

---

## 7.8 demo/

The `demo/` directory is conventional module anatomy. Odoo does not magically import random folders — `__init__.py` and the manifest `data`/`assets` lists decide what loads.

Open an official addon and copy the same layout. Empty folders without wiring do nothing.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **demo/** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **demo/**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **demo/** is concrete, continue — the next lesson in this phase still depends on it.

## 7.9 static/

Convention:

```text
static/
  description/
    icon.png          # app icon
  src/                # sometimes used for JS/CSS sources
  src/xml/            # OWL templates in richer modules
```

For Hello Odoo, an icon under `static/description/icon.png` makes your app recognizable. Heavy OWL work is a later story — do not block Hello Odoo on frontend tooling.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add any small PNG as `static/description/icon.png` or note “icon deferred.” |
| Medium | Browse an official module’s `static/description` and copy the idea, not the trademarked art. |
| Hard | Find how assets bundles are declared in a `web` module — read only. |

**WHAT THIS UNLOCKS NEXT:**

Anatomy pieces exist in theory. The mini project forces a module that **installs and shows a menu**.

---

## 7.10 controllers/

The `controllers/` directory is conventional module anatomy. Odoo does not magically import random folders — `__init__.py` and the manifest `data`/`assets` lists decide what loads.

Open an official addon and copy the same layout. Empty folders without wiring do nothing.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **controllers/** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **controllers/**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **controllers/** is concrete, continue — the next lesson in this phase still depends on it.

## 7.11 wizard/

The `wizard/` directory is conventional module anatomy. Odoo does not magically import random folders — `__init__.py` and the manifest `data`/`assets` lists decide what loads.

Open an official addon and copy the same layout. Empty folders without wiring do nothing.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **wizard/** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **wizard/**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **wizard/** is concrete, continue — the next lesson in this phase still depends on it.

## 7.12 report/

The `report/` directory is conventional module anatomy. Odoo does not magically import random folders — `__init__.py` and the manifest `data`/`assets` lists decide what loads.

Open an official addon and copy the same layout. Empty folders without wiring do nothing.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **report/** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **report/**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **report/** is concrete, continue — the next lesson in this phase still depends on it.

## 7.13 i18n/

The `i18n/` directory is conventional module anatomy. Odoo does not magically import random folders — `__init__.py` and the manifest `data`/`assets` lists decide what loads.

Open an official addon and copy the same layout. Empty folders without wiring do nothing.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **i18n/** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **i18n/**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **i18n/** is concrete, continue — the next lesson in this phase still depends on it.

## 7.14 MINI PROJECT — Hello Odoo

**Build `hello_odoo` with:**

1. Manifest + init imports  
2. Model `hello.message` with `name`, `note`  
3. List + form + action + menu  
4. Access CSV for internal users  
5. Install on your training database  

```text
custom_addons/hello_odoo/
  __init__.py
  __manifest__.py
  models/
    __init__.py
    hello_model.py
  views/
    hello_views.xml
  security/
    ir.model.access.csv
  static/description/icon.png   (optional)
```

Start Odoo → Apps → Update Apps List → find Hello Odoo → Install. Create one message record from the menu.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Install and create one record. |
| Medium | Break the manifest path on purpose, read the log, fix it. |
| Hard | Add a field, note that install alone may not refresh schema — Phase 8’s upgrade is next. |

**WHAT THIS UNLOCKS NEXT:**

A module that only installs once is incomplete. Real work is **dependencies, upgrade, uninstall, and data load order** — Phase 8’s lifecycle.

---


---


<a id="phase-8"></a>
<a id="phase-8---module-lifecycle"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 7:** You can explain every lesson title in Phase 7 with a concrete Odoo or business example.

> **Phase 7 complete?** [Continue to Phase 8](#phase-8)


<a id="phase-8"></a>
# PHASE 8 - Module Lifecycle

**Track:** First Module

**WHAT YOU WILL BE ABLE TO DO:** Manage `depends`, install/upgrade/uninstall with intent, predict data load order effects, and use `-i` / `-u` (or Apps UI) safely on a training database.

**WHAT YOU SHOULD KNOW FIRST:** Phase 7 — `hello_odoo` installs. You have a disposable training DB (never learn upgrades only on production).

**LEARNING RESOURCES:**

- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [Install from source](https://www.odoo.com/documentation/18.0/administration/on_premise/source.html)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [odoo/documentation](https://github.com/odoo/documentation)
- [Odoo developer landing](https://www.odoo.com/documentation/18.0/developer.html)

## 8.1 Module Discovery

**LAB SCRIPT:**

1. Ensure `hello_odoo` is installed.  
2. Add field `mood = fields.Selection([("ok", "OK"), ("great", "Great")])`.  
3. Add the field to the form view.  
4. Bump version in manifest.  
5. Run upgrade via UI or `odoo-bin -d TRAIN_DB -u hello_odoo --stop-after-init`.  
6. Open the form; set mood on a record.  
7. Check log for errors during upgrade.  

```text
Edit code -> upgrade -> verify UI/DB -> commit notes (what changed)
```

Optional stretch: add a `data/` welcome record; upgrade; observe whether values update.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Complete steps 1–6. |
| Medium | Introduce a deliberate XML typo; upgrade; fix from traceback. |
| Hard | Use a second training DB to practice `-i` vs `-u` until the difference is boring. |

**WHAT THIS UNLOCKS NEXT:**

You can baby-sit one tiny module through its life. The story project asks for a **complete first addon** — still small, but packaged as something you would show a mentor.

---


---

## 8.2 Dependencies

Manifest `depends` lists modules that must be installed first. If your views inherit `sale.order` form XML, you depend on `sale`. If you only use `res.partner`, `base` may suffice.

```python
"depends": ["base", "sale"],  # example for a sales extension
```

**DEPENDENCY GRAPH THINKING:**

```text
base
  |
  +-- web
  +-- sale --> product, ...
  +-- hello_odoo (depends base only)
```

Circular depends are forbidden. Over-depending slows installs and couples you to apps the customer did not buy. Under-depending crashes install when XML ids are missing.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Read `depends` on `hello_odoo` and on `sale`; contrast complexity. |
| Medium | Temporarily set `depends: ["sale"]` on hello (even unused) — observe install requiring Sales. Revert. |
| Hard | Design depends for a future module that adds a field on `sale.order` and a menu under Sales. |

**WHAT THIS UNLOCKS NEXT:**

Depends constrain **installation**. The install operation itself is the next lifecycle verb.

---

## 8.3 Installation

**Install** loads the module into a database for the first time: import Python, create tables for new `_name` models, load security/data/views in order, register menus.

Ways:

- UI: Apps → Install  
- CLI: `odoo-bin -c odoo.conf -d TRAIN_DB -i hello_odoo --stop-after-init`

`--stop-after-init` is ideal for scripts: apply install/upgrade, then exit.

**INSTALL IS NOT COPY FOLDER:**

Dropping a folder on disk only makes the module *discoverable*. The database rows in `ir_module_module` track installed state. Two databases can disagree while sharing addons_path (Phase 4 insight, now operational).

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Reinstall path: uninstall (next lessons) or use a fresh DB and `-i hello_odoo`. |
| Medium | Use CLI `-i` with `--stop-after-init` once; confirm exit code 0. |
| Hard | Inspect `ir.module.module` for `hello_odoo` state in developer mode. |

**WHAT THIS UNLOCKS NEXT:**

After you change code, install will not re-apply everything. You need **upgrade**.

---

## 8.4 Initialization

**Initialization** belongs to **Module Lifecycle**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Initialization** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Initialization** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Initialization**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Initialization** is concrete, continue — the next lesson in this phase still depends on it.

## 8.5 Upgrade

**Upgrade** (`-u hello_odoo`) reloads the module: update Python model fields (schema sync), reload XML/CSV per rules, refresh registry for that module’s changes.

```text
Change field in models/*.py  -->  upgrade module  -->  column appears
Change view XML              -->  upgrade module  -->  UI updates
Change only Python method body --> often restart enough; upgrade still safest habit while learning
```

Bump manifest `version` as you iterate (`18.0.1.0.1`, …) so you remember what is deployed.

**WHEN LEARNERS GET STUCK:**

They edit XML, refresh the browser, see nothing change, and blame caches. Usually the module was never upgraded. Make `-u` muscle memory.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a `Char` field; upgrade; see it on the form (add to view XML too). |
| Medium | Change a menu name in XML; upgrade; confirm UI. |
| Hard | Document a change that needed upgrade vs one that appeared after server restart only. |

**WHAT THIS UNLOCKS NEXT:**

Sometimes you must remove a module cleanly. **Uninstall** has sharper edges than beginners expect.

---

## 8.6 Uninstall

**Uninstall** removes the module’s data/models according to Odoo’s uninstall hooks — including tables for models that `_name` introduced (with constraints about dependencies). Modules that other installed modules `depends` on cannot be removed first.

```text
Safe training habit:

  - Uninstall experiments on TRAIN_DB
  - Never casually uninstall account/stock on a client DB
  - Expect custom data loss when uninstalling YOUR module
```

Uninstall is not “delete the folder.” Remove from Apps first (or CLI), then optionally delete code from disk.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | On a copy DB, uninstall `hello_odoo` and note menus disappearing. |
| Medium | Reinstall afterward; see which records survived (usually none for pure custom models). |
| Hard | Explain why uninstalling `sale` is blocked if another module depends on it. |

**WHAT THIS UNLOCKS NEXT:**

Install/upgrade both read the manifest `data` list. **Load order** decides whether security exists before views and whether record A can reference record B.

---

## 8.7 Data Load Order

Manifest `data` files execute **top to bottom**. Later files may reference XML ids defined earlier. Security CSV usually precedes views. Views precede data that needs UI? Actually data records often need models+access already present — models come from Python import at install, access from CSV, then views, then demo/data.

```python
"data": [
    "security/ir.model.access.csv",
    "views/hello_views.xml",
    "data/hello_data.xml",
],
```

**FAILURE PATTERN:**

`data/hello_data.xml` creates a record that references a menu XML id defined later → install crash. Reorder the list. Read the log’s “External ID not found” as a load-order hint.

Demo-only files often go under `demo:` key so production installs stay clean — nice-to-know for later packaging.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Verify your manifest order matches security → views → data. |
| Medium | Swap two lines to break references; fix from the logfile message. |
| Hard | Split views into two XML files and order them so action exists before menuitem that needs it (menuitem can reference action in same file — still practice splitting). |

**WHAT THIS UNLOCKS NEXT:**

Lifecycle theory sticks only when you **install and upgrade on purpose** in a lab with observable field/UI changes.

---

## 8.8 Application Flag

**Application Flag** belongs to **Module Lifecycle**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Application Flag** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Application Flag** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Application Flag**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Application Flag** is concrete, continue — the next lesson in this phase still depends on it.

## 8.9 Installable Flag

**Installable Flag** belongs to **Module Lifecycle**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Installable Flag** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **Installable Flag** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **Installable Flag** is concrete, continue — the next lesson in this phase still depends on it.

## 8.10 LAB — Install & Upgrade Your Module

**Upgrade** (`-u hello_odoo`) reloads the module: update Python model fields (schema sync), reload XML/CSV per rules, refresh registry for that module’s changes.

```text
Change field in models/*.py  -->  upgrade module  -->  column appears
Change view XML              -->  upgrade module  -->  UI updates
Change only Python method body --> often restart enough; upgrade still safest habit while learning
```

Bump manifest `version` as you iterate (`18.0.1.0.1`, …) so you remember what is deployed.

**WHEN LEARNERS GET STUCK:**

They edit XML, refresh the browser, see nothing change, and blame caches. Usually the module was never upgraded. Make `-u` muscle memory.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a `Char` field; upgrade; see it on the form (add to view XML too). |
| Medium | Change a menu name in XML; upgrade; confirm UI. |
| Hard | Document a change that needed upgrade vs one that appeared after server restart only. |

**WHAT THIS UNLOCKS NEXT:**

Sometimes you must remove a module cleanly. **Uninstall** has sharper edges than beginners expect.

---


---


**CHECK YOUR UNDERSTANDING AFTER PHASE 8:** You can explain every lesson title in Phase 8 with a concrete Odoo or business example.

> **Phase 8 complete?** [Continue to Phase 9](#phase-9)


## STORY PROJECT — First Complete Addon

**Track:** First Module

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY IV — MODELING BUSINESS DATA

**Track:** Modeling Data

*A business system begins with correctly modeled information.*

---
<a id="phase-9"></a>
<a id="phase-9---models"></a>
# PHASE 9 - Models

**Track:** Modeling Data

**WHAT YOU WILL BE ABLE TO DO:** Define a persistent Odoo model with `models.Model`, set `_name`, `_description`, and `_rec_name`, register it through the module import chain, and ship a first library book model that creates a real PostgreSQL table.

**WHAT YOU SHOULD KNOW FIRST:** Phase 8 — module skeleton (`__manifest__.py`, `models/`, import chain). Stories I–III — ERP records, local Odoo, and installing a custom addon.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Models](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#models)
- [Server Framework 101 — Models](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/04_models_and_basic_fields.html)
- [Backend tutorial overview](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [odoo/tutorials — estate sample](https://github.com/odoo/tutorials)
- [Manifest & first module (video)](https://www.youtube.com/watch?v=5YIwP9-55Qk)
- [odoo/odoo — Model base class](https://github.com/odoo/odoo)

## 9.1 models.Model

Stories I–III got you a running Odoo, a business vocabulary, and a module folder that installs without crashing. That skeleton still does not own any business facts. Until you declare a **model**, Odoo has nowhere to store a book, a loan, or a customer order of your own design.

In Odoo, a model is a Python class that describes one kind of business record. Instantiating that class is not your job — the registry builds a singleton per model. Your job is to tell Odoo the technical name, the human label, and (optionally) which field should stand for the record when it appears as a title.

**THE CLASS THAT BECOMES A TABLE:**

```python
# my_library/models/library_book.py
from odoo import models, fields


class LibraryBook(models.Model):
    _name = "library.book"
    _description = "Library Book"
    _rec_name = "name"

    name = fields.Char(string="Title", required=True)
    isbn = fields.Char(string="ISBN")
```

```text
Python class LibraryBook
        |
        v
Registry entry "library.book"
        |
        v
PostgreSQL table library_book
  id | name | isbn | create_uid | create_date | write_uid | write_date | ...
```

`models.Model` means **persistent**: rows survive restarts and live in PostgreSQL. Later you will meet `models.TransientModel` (wizards) and `models.AbstractModel` (mixins). For Story IV, almost everything you build is `models.Model`.

**_name** is the technical identity used everywhere: XML `model="library.book"`, `env["library.book"]`, security CSV rows, related fields. Convention: lowercase dotted path (`library.book`, `sale.order`). Underscores become table separators (`library_book`).

**_description** is the human label for settings, technical screens, and access lists. Without it, Odoo falls back to the technical name — fine for a prototype, noisy in a real database.

**_rec_name** chooses which field is the record's display title. Default is `name` if that field exists. A leave request might use `_rec_name = "employee_id"` so lists show the person, not an opaque number. If no suitable Char field exists, you will later override `name_get` / `display_name` — but start with a real Char field and `_rec_name`.

**REGISTRATION THROUGH THE IMPORT CHAIN:**

```text
my_library/__init__.py          -> from . import models
my_library/models/__init__.py   -> from . import library_book
my_library/models/library_book.py
```

Miss any import and the class never reaches the registry. The module installs, menus may appear later, but `library.book` simply does not exist. That failure mode is silent until the first XML or Python reference blows up.

**COMMON FIRST-MODEL MISTAKES:**

| Mistake | What you see |
| ------- | ------------ |
| Forgot `models/__init__.py` import | `KeyError: 'library.book'` or model missing in Settings → Technical |
| `_name` typo vs XML `model=` | View fails to load; model not found |
| Reused a core `_name` (`res.partner`) | You are extending/replacing a platform model — usually wrong for a first custom app |
| No `_description` | Ugly labels in ACL screens |

Upgrade with `-u my_library` after changing `_name` carefully: renaming a model does not magically rename the SQL table and all foreign keys. Treat `_name` as permanent once demo data exists.

**MODELS.MODEL VERSUS THE OTHER BASES (PREVIEW):**

| Base | Persists? | Typical use |
| ---- | --------- | ----------- |
| `models.Model` | Yes, PostgreSQL table | Books, orders, partners |
| `models.TransientModel` | Short-lived wizard rows | "Print labels" assistants (Story X) |
| `models.AbstractModel` | No table of its own | Mixins like `mail.thread` |

Your first business entities are almost always `models.Model`. If you inherit a mixin later, you still keep `_name` on the concrete model and list `_inherit` for the mixin — Phase 9 only needs the concrete case.

**HOW ODOO FINDS YOUR CLASS AT INSTALL:**

```text
1. Module listed in addons path; state -> installed / to upgrade
2. Python imports my_library -> models -> library_book
3. Metaclass registers LibraryBook under _name "library.book"
4. Registry builds / updates PostgreSQL columns for declared fields
5. XML data files may now reference model="library.book"
```

If step 2 fails, steps 4–5 never see your model. That is why the import chain is part of modeling, not "just packaging."

## 9.2 models.AbstractModel

Stories I–III got you a running Odoo, a business vocabulary, and a module folder that installs without crashing. That skeleton still does not own any business facts. Until you declare a **model**, Odoo has nowhere to store a book, a loan, or a customer order of your own design.

In Odoo, a model is a Python class that describes one kind of business record. Instantiating that class is not your job — the registry builds a singleton per model. Your job is to tell Odoo the technical name, the human label, and (optionally) which field should stand for the record when it appears as a title.

**THE CLASS THAT BECOMES A TABLE:**

```python
# my_library/models/library_book.py
from odoo import models, fields


class LibraryBook(models.Model):
    _name = "library.book"
    _description = "Library Book"
    _rec_name = "name"

    name = fields.Char(string="Title", required=True)
    isbn = fields.Char(string="ISBN")
```

```text
Python class LibraryBook
        |
        v
Registry entry "library.book"
        |
        v
PostgreSQL table library_book
  id | name | isbn | create_uid | create_date | write_uid | write_date | ...
```

`models.Model` means **persistent**: rows survive restarts and live in PostgreSQL. Later you will meet `models.TransientModel` (wizards) and `models.AbstractModel` (mixins). For Story IV, almost everything you build is `models.Model`.

**_name** is the technical identity used everywhere: XML `model="library.book"`, `env["library.book"]`, security CSV rows, related fields. Convention: lowercase dotted path (`library.book`, `sale.order`). Underscores become table separators (`library_book`).

**_description** is the human label for settings, technical screens, and access lists. Without it, Odoo falls back to the technical name — fine for a prototype, noisy in a real database.

**_rec_name** chooses which field is the record's display title. Default is `name` if that field exists. A leave request might use `_rec_name = "employee_id"` so lists show the person, not an opaque number. If no suitable Char field exists, you will later override `name_get` / `display_name` — but start with a real Char field and `_rec_name`.

**REGISTRATION THROUGH THE IMPORT CHAIN:**

```text
my_library/__init__.py          -> from . import models
my_library/models/__init__.py   -> from . import library_book
my_library/models/library_book.py
```

Miss any import and the class never reaches the registry. The module installs, menus may appear later, but `library.book` simply does not exist. That failure mode is silent until the first XML or Python reference blows up.

**COMMON FIRST-MODEL MISTAKES:**

| Mistake | What you see |
| ------- | ------------ |
| Forgot `models/__init__.py` import | `KeyError: 'library.book'` or model missing in Settings → Technical |
| `_name` typo vs XML `model=` | View fails to load; model not found |
| Reused a core `_name` (`res.partner`) | You are extending/replacing a platform model — usually wrong for a first custom app |
| No `_description` | Ugly labels in ACL screens |

Upgrade with `-u my_library` after changing `_name` carefully: renaming a model does not magically rename the SQL table and all foreign keys. Treat `_name` as permanent once demo data exists.

**MODELS.MODEL VERSUS THE OTHER BASES (PREVIEW):**

| Base | Persists? | Typical use |
| ---- | --------- | ----------- |
| `models.Model` | Yes, PostgreSQL table | Books, orders, partners |
| `models.TransientModel` | Short-lived wizard rows | "Print labels" assistants (Story X) |
| `models.AbstractModel` | No table of its own | Mixins like `mail.thread` |

Your first business entities are almost always `models.Model`. If you inherit a mixin later, you still keep `_name` on the concrete model and list `_inherit` for the mixin — Phase 9 only needs the concrete case.

**HOW ODOO FINDS YOUR CLASS AT INSTALL:**

```text
1. Module listed in addons path; state -> installed / to upgrade
2. Python imports my_library -> models -> library_book
3. Metaclass registers LibraryBook under _name "library.book"
4. Registry builds / updates PostgreSQL columns for declared fields
5. XML data files may now reference model="library.book"
```

If step 2 fails, steps 4–5 never see your model. That is why the import chain is part of modeling, not "just packaging."

## 9.3 models.TransientModel

Stories I–III got you a running Odoo, a business vocabulary, and a module folder that installs without crashing. That skeleton still does not own any business facts. Until you declare a **model**, Odoo has nowhere to store a book, a loan, or a customer order of your own design.

In Odoo, a model is a Python class that describes one kind of business record. Instantiating that class is not your job — the registry builds a singleton per model. Your job is to tell Odoo the technical name, the human label, and (optionally) which field should stand for the record when it appears as a title.

**THE CLASS THAT BECOMES A TABLE:**

```python
# my_library/models/library_book.py
from odoo import models, fields


class LibraryBook(models.Model):
    _name = "library.book"
    _description = "Library Book"
    _rec_name = "name"

    name = fields.Char(string="Title", required=True)
    isbn = fields.Char(string="ISBN")
```

```text
Python class LibraryBook
        |
        v
Registry entry "library.book"
        |
        v
PostgreSQL table library_book
  id | name | isbn | create_uid | create_date | write_uid | write_date | ...
```

`models.Model` means **persistent**: rows survive restarts and live in PostgreSQL. Later you will meet `models.TransientModel` (wizards) and `models.AbstractModel` (mixins). For Story IV, almost everything you build is `models.Model`.

**_name** is the technical identity used everywhere: XML `model="library.book"`, `env["library.book"]`, security CSV rows, related fields. Convention: lowercase dotted path (`library.book`, `sale.order`). Underscores become table separators (`library_book`).

**_description** is the human label for settings, technical screens, and access lists. Without it, Odoo falls back to the technical name — fine for a prototype, noisy in a real database.

**_rec_name** chooses which field is the record's display title. Default is `name` if that field exists. A leave request might use `_rec_name = "employee_id"` so lists show the person, not an opaque number. If no suitable Char field exists, you will later override `name_get` / `display_name` — but start with a real Char field and `_rec_name`.

**REGISTRATION THROUGH THE IMPORT CHAIN:**

```text
my_library/__init__.py          -> from . import models
my_library/models/__init__.py   -> from . import library_book
my_library/models/library_book.py
```

Miss any import and the class never reaches the registry. The module installs, menus may appear later, but `library.book` simply does not exist. That failure mode is silent until the first XML or Python reference blows up.

**COMMON FIRST-MODEL MISTAKES:**

| Mistake | What you see |
| ------- | ------------ |
| Forgot `models/__init__.py` import | `KeyError: 'library.book'` or model missing in Settings → Technical |
| `_name` typo vs XML `model=` | View fails to load; model not found |
| Reused a core `_name` (`res.partner`) | You are extending/replacing a platform model — usually wrong for a first custom app |
| No `_description` | Ugly labels in ACL screens |

Upgrade with `-u my_library` after changing `_name` carefully: renaming a model does not magically rename the SQL table and all foreign keys. Treat `_name` as permanent once demo data exists.

**MODELS.MODEL VERSUS THE OTHER BASES (PREVIEW):**

| Base | Persists? | Typical use |
| ---- | --------- | ----------- |
| `models.Model` | Yes, PostgreSQL table | Books, orders, partners |
| `models.TransientModel` | Short-lived wizard rows | "Print labels" assistants (Story X) |
| `models.AbstractModel` | No table of its own | Mixins like `mail.thread` |

Your first business entities are almost always `models.Model`. If you inherit a mixin later, you still keep `_name` on the concrete model and list `_inherit` for the mixin — Phase 9 only needs the concrete case.

**HOW ODOO FINDS YOUR CLASS AT INSTALL:**

```text
1. Module listed in addons path; state -> installed / to upgrade
2. Python imports my_library -> models -> library_book
3. Metaclass registers LibraryBook under _name "library.book"
4. Registry builds / updates PostgreSQL columns for declared fields
5. XML data files may now reference model="library.book"
```

If step 2 fails, steps 4–5 never see your model. That is why the import chain is part of modeling, not "just packaging."

## 9.4 _name

Stories I–III got you a running Odoo, a business vocabulary, and a module folder that installs without crashing. That skeleton still does not own any business facts. Until you declare a **model**, Odoo has nowhere to store a book, a loan, or a customer order of your own design.

In Odoo, a model is a Python class that describes one kind of business record. Instantiating that class is not your job — the registry builds a singleton per model. Your job is to tell Odoo the technical name, the human label, and (optionally) which field should stand for the record when it appears as a title.

**THE CLASS THAT BECOMES A TABLE:**

```python
# my_library/models/library_book.py
from odoo import models, fields


class LibraryBook(models.Model):
    _name = "library.book"
    _description = "Library Book"
    _rec_name = "name"

    name = fields.Char(string="Title", required=True)
    isbn = fields.Char(string="ISBN")
```

```text
Python class LibraryBook
        |
        v
Registry entry "library.book"
        |
        v
PostgreSQL table library_book
  id | name | isbn | create_uid | create_date | write_uid | write_date | ...
```

`models.Model` means **persistent**: rows survive restarts and live in PostgreSQL. Later you will meet `models.TransientModel` (wizards) and `models.AbstractModel` (mixins). For Story IV, almost everything you build is `models.Model`.

**_name** is the technical identity used everywhere: XML `model="library.book"`, `env["library.book"]`, security CSV rows, related fields. Convention: lowercase dotted path (`library.book`, `sale.order`). Underscores become table separators (`library_book`).

**_description** is the human label for settings, technical screens, and access lists. Without it, Odoo falls back to the technical name — fine for a prototype, noisy in a real database.

**_rec_name** chooses which field is the record's display title. Default is `name` if that field exists. A leave request might use `_rec_name = "employee_id"` so lists show the person, not an opaque number. If no suitable Char field exists, you will later override `name_get` / `display_name` — but start with a real Char field and `_rec_name`.

**REGISTRATION THROUGH THE IMPORT CHAIN:**

```text
my_library/__init__.py          -> from . import models
my_library/models/__init__.py   -> from . import library_book
my_library/models/library_book.py
```

Miss any import and the class never reaches the registry. The module installs, menus may appear later, but `library.book` simply does not exist. That failure mode is silent until the first XML or Python reference blows up.

**COMMON FIRST-MODEL MISTAKES:**

| Mistake | What you see |
| ------- | ------------ |
| Forgot `models/__init__.py` import | `KeyError: 'library.book'` or model missing in Settings → Technical |
| `_name` typo vs XML `model=` | View fails to load; model not found |
| Reused a core `_name` (`res.partner`) | You are extending/replacing a platform model — usually wrong for a first custom app |
| No `_description` | Ugly labels in ACL screens |

Upgrade with `-u my_library` after changing `_name` carefully: renaming a model does not magically rename the SQL table and all foreign keys. Treat `_name` as permanent once demo data exists.

**MODELS.MODEL VERSUS THE OTHER BASES (PREVIEW):**

| Base | Persists? | Typical use |
| ---- | --------- | ----------- |
| `models.Model` | Yes, PostgreSQL table | Books, orders, partners |
| `models.TransientModel` | Short-lived wizard rows | "Print labels" assistants (Story X) |
| `models.AbstractModel` | No table of its own | Mixins like `mail.thread` |

Your first business entities are almost always `models.Model`. If you inherit a mixin later, you still keep `_name` on the concrete model and list `_inherit` for the mixin — Phase 9 only needs the concrete case.

**HOW ODOO FINDS YOUR CLASS AT INSTALL:**

```text
1. Module listed in addons path; state -> installed / to upgrade
2. Python imports my_library -> models -> library_book
3. Metaclass registers LibraryBook under _name "library.book"
4. Registry builds / updates PostgreSQL columns for declared fields
5. XML data files may now reference model="library.book"
```

If step 2 fails, steps 4–5 never see your model. That is why the import chain is part of modeling, not "just packaging."

## 9.5 _description

Stories I–III got you a running Odoo, a business vocabulary, and a module folder that installs without crashing. That skeleton still does not own any business facts. Until you declare a **model**, Odoo has nowhere to store a book, a loan, or a customer order of your own design.

In Odoo, a model is a Python class that describes one kind of business record. Instantiating that class is not your job — the registry builds a singleton per model. Your job is to tell Odoo the technical name, the human label, and (optionally) which field should stand for the record when it appears as a title.

**THE CLASS THAT BECOMES A TABLE:**

```python
# my_library/models/library_book.py
from odoo import models, fields


class LibraryBook(models.Model):
    _name = "library.book"
    _description = "Library Book"
    _rec_name = "name"

    name = fields.Char(string="Title", required=True)
    isbn = fields.Char(string="ISBN")
```

```text
Python class LibraryBook
        |
        v
Registry entry "library.book"
        |
        v
PostgreSQL table library_book
  id | name | isbn | create_uid | create_date | write_uid | write_date | ...
```

`models.Model` means **persistent**: rows survive restarts and live in PostgreSQL. Later you will meet `models.TransientModel` (wizards) and `models.AbstractModel` (mixins). For Story IV, almost everything you build is `models.Model`.

**_name** is the technical identity used everywhere: XML `model="library.book"`, `env["library.book"]`, security CSV rows, related fields. Convention: lowercase dotted path (`library.book`, `sale.order`). Underscores become table separators (`library_book`).

**_description** is the human label for settings, technical screens, and access lists. Without it, Odoo falls back to the technical name — fine for a prototype, noisy in a real database.

**_rec_name** chooses which field is the record's display title. Default is `name` if that field exists. A leave request might use `_rec_name = "employee_id"` so lists show the person, not an opaque number. If no suitable Char field exists, you will later override `name_get` / `display_name` — but start with a real Char field and `_rec_name`.

**REGISTRATION THROUGH THE IMPORT CHAIN:**

```text
my_library/__init__.py          -> from . import models
my_library/models/__init__.py   -> from . import library_book
my_library/models/library_book.py
```

Miss any import and the class never reaches the registry. The module installs, menus may appear later, but `library.book` simply does not exist. That failure mode is silent until the first XML or Python reference blows up.

**COMMON FIRST-MODEL MISTAKES:**

| Mistake | What you see |
| ------- | ------------ |
| Forgot `models/__init__.py` import | `KeyError: 'library.book'` or model missing in Settings → Technical |
| `_name` typo vs XML `model=` | View fails to load; model not found |
| Reused a core `_name` (`res.partner`) | You are extending/replacing a platform model — usually wrong for a first custom app |
| No `_description` | Ugly labels in ACL screens |

Upgrade with `-u my_library` after changing `_name` carefully: renaming a model does not magically rename the SQL table and all foreign keys. Treat `_name` as permanent once demo data exists.

**MODELS.MODEL VERSUS THE OTHER BASES (PREVIEW):**

| Base | Persists? | Typical use |
| ---- | --------- | ----------- |
| `models.Model` | Yes, PostgreSQL table | Books, orders, partners |
| `models.TransientModel` | Short-lived wizard rows | "Print labels" assistants (Story X) |
| `models.AbstractModel` | No table of its own | Mixins like `mail.thread` |

Your first business entities are almost always `models.Model`. If you inherit a mixin later, you still keep `_name` on the concrete model and list `_inherit` for the mixin — Phase 9 only needs the concrete case.

**HOW ODOO FINDS YOUR CLASS AT INSTALL:**

```text
1. Module listed in addons path; state -> installed / to upgrade
2. Python imports my_library -> models -> library_book
3. Metaclass registers LibraryBook under _name "library.book"
4. Registry builds / updates PostgreSQL columns for declared fields
5. XML data files may now reference model="library.book"
```

If step 2 fails, steps 4–5 never see your model. That is why the import chain is part of modeling, not "just packaging."

## 9.6 _rec_name

Stories I–III got you a running Odoo, a business vocabulary, and a module folder that installs without crashing. That skeleton still does not own any business facts. Until you declare a **model**, Odoo has nowhere to store a book, a loan, or a customer order of your own design.

In Odoo, a model is a Python class that describes one kind of business record. Instantiating that class is not your job — the registry builds a singleton per model. Your job is to tell Odoo the technical name, the human label, and (optionally) which field should stand for the record when it appears as a title.

**THE CLASS THAT BECOMES A TABLE:**

```python
# my_library/models/library_book.py
from odoo import models, fields


class LibraryBook(models.Model):
    _name = "library.book"
    _description = "Library Book"
    _rec_name = "name"

    name = fields.Char(string="Title", required=True)
    isbn = fields.Char(string="ISBN")
```

```text
Python class LibraryBook
        |
        v
Registry entry "library.book"
        |
        v
PostgreSQL table library_book
  id | name | isbn | create_uid | create_date | write_uid | write_date | ...
```

`models.Model` means **persistent**: rows survive restarts and live in PostgreSQL. Later you will meet `models.TransientModel` (wizards) and `models.AbstractModel` (mixins). For Story IV, almost everything you build is `models.Model`.

**_name** is the technical identity used everywhere: XML `model="library.book"`, `env["library.book"]`, security CSV rows, related fields. Convention: lowercase dotted path (`library.book`, `sale.order`). Underscores become table separators (`library_book`).

**_description** is the human label for settings, technical screens, and access lists. Without it, Odoo falls back to the technical name — fine for a prototype, noisy in a real database.

**_rec_name** chooses which field is the record's display title. Default is `name` if that field exists. A leave request might use `_rec_name = "employee_id"` so lists show the person, not an opaque number. If no suitable Char field exists, you will later override `name_get` / `display_name` — but start with a real Char field and `_rec_name`.

**REGISTRATION THROUGH THE IMPORT CHAIN:**

```text
my_library/__init__.py          -> from . import models
my_library/models/__init__.py   -> from . import library_book
my_library/models/library_book.py
```

Miss any import and the class never reaches the registry. The module installs, menus may appear later, but `library.book` simply does not exist. That failure mode is silent until the first XML or Python reference blows up.

**COMMON FIRST-MODEL MISTAKES:**

| Mistake | What you see |
| ------- | ------------ |
| Forgot `models/__init__.py` import | `KeyError: 'library.book'` or model missing in Settings → Technical |
| `_name` typo vs XML `model=` | View fails to load; model not found |
| Reused a core `_name` (`res.partner`) | You are extending/replacing a platform model — usually wrong for a first custom app |
| No `_description` | Ugly labels in ACL screens |

Upgrade with `-u my_library` after changing `_name` carefully: renaming a model does not magically rename the SQL table and all foreign keys. Treat `_name` as permanent once demo data exists.

**MODELS.MODEL VERSUS THE OTHER BASES (PREVIEW):**

| Base | Persists? | Typical use |
| ---- | --------- | ----------- |
| `models.Model` | Yes, PostgreSQL table | Books, orders, partners |
| `models.TransientModel` | Short-lived wizard rows | "Print labels" assistants (Story X) |
| `models.AbstractModel` | No table of its own | Mixins like `mail.thread` |

Your first business entities are almost always `models.Model`. If you inherit a mixin later, you still keep `_name` on the concrete model and list `_inherit` for the mixin — Phase 9 only needs the concrete case.

**HOW ODOO FINDS YOUR CLASS AT INSTALL:**

```text
1. Module listed in addons path; state -> installed / to upgrade
2. Python imports my_library -> models -> library_book
3. Metaclass registers LibraryBook under _name "library.book"
4. Registry builds / updates PostgreSQL columns for declared fields
5. XML data files may now reference model="library.book"
```

If step 2 fails, steps 4–5 never see your model. That is why the import chain is part of modeling, not "just packaging."

## 9.7 _order

**_order** belongs to **Models**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **_order** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **_order** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **_order**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **_order** is concrete, continue — the next lesson in this phase still depends on it.

## 9.8 Automatic Fields

**Automatic Fields** belongs to **Models**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Automatic Fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Automatic Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Automatic Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Automatic Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 9.9 Reserved Field Names

**Reserved Field Names** belongs to **Models**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Reserved Field Names** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Reserved Field Names** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Reserved Field Names**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Reserved Field Names** is concrete, continue — the next lesson in this phase still depends on it.

## 9.10 MINI PROJECT — Library Model

Build the smallest durable library core inside `my_library` (or extend the module from Story III).

**DELIVERABLE:**

1. Model `library.book` with `_description = "Library Book"` and `_rec_name = "name"`.
2. Fields for this mini-project: `name` (Char, required), `isbn` (Char), `active` (Boolean, default True). More field types arrive in Phase 10 — keep this thin.
3. Import chain correct; module upgrades cleanly.
4. From the Odoo shell or a temporary server action, create two books and confirm rows in Settings → Technical → Database Structure → Models, or via `env["library.book"].search([])`.

```python
# After -u my_library, in odoo shell:
Book = env["library.book"]
Book.create({"name": "Clean Code", "isbn": "978-0132350884"})
Book.create({"name": "Domain-Driven Design", "isbn": "978-0321125217"})
print(Book.search([]).mapped("name"))
```

You still have no form view and no menu — that is intentional. Persistence comes before chrome. If you can create and find records through the ORM, the model layer is real.

**VERIFY IN THE DATABASE (OPTIONAL BUT POWERFUL):**

```text
psql -> \dt library*
       \d library_book

Expect columns: id, name, isbn, active, create_uid, create_date, write_uid, write_date
```

Seeing the table makes the abstraction concrete: the Python class is a contract; PostgreSQL is the store.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write `library.author` with `_name`, `_description`, `_rec_name = "name"`, and a required Char `name` |
| Easy | Break the import chain on purpose, upgrade, and write down the exact error — then fix it |
| Medium | Explain why `_name = "LibraryBook"` (CamelCase) is wrong for Odoo |
| Medium | Create three books via `create` / `search`; show `_rec_name` driving how they print in a many2one later (preview with `display_name`) |
| Hard | Sketch which models a lending library needs (book, copy, member, loan) without writing fields yet — name each `_name` |

**WHAT THIS UNLOCKS NEXT:**

A model without typed fields is only a named box. Business data needs columns — text, numbers, dates, money, choices — so Phase 10 fills the model with the field API.


---


<a id="phase-10"></a>
<a id="phase-10---fields"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 9:** You can explain every lesson title in Phase 9 with a concrete Odoo or business example.

> **Phase 9 complete?** [Continue to Phase 10](#phase-10)


<a id="phase-10"></a>
# PHASE 10 - Fields

**Track:** Modeling Data

**WHAT YOU WILL BE ABLE TO DO:** Choose and declare the right basic field types for business data — Char, Text, Integer, Float, Boolean, Date, Datetime, Selection, Monetary — with `string`, `required`, `default`, and `help`, and model realistic library / sales-style records.

**WHAT YOU SHOULD KNOW FIRST:** Phase 9 — a registered `models.Model` with `_name` and at least one Char field.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Fields](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#fields)
- [Basic fields tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/04_models_and_basic_fields.html)
- [Backend reference](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [First module fields walkthrough (video)](https://www.youtube.com/watch?v=5YIwP9-55Qk)
- [odoo/odoo field definitions](https://github.com/odoo/odoo)

## 10.1 Char

`fields.Char` is a short string column (name, code, email). Set `required`, `index`, `translate`, and size only when the business needs them.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Char** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Char** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Char**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Char** is concrete, continue — the next lesson in this phase still depends on it.

## 10.2 Text

`fields.Text` is unlimited text (notes, descriptions). Do not use Char+huge size as a fake memo.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Text** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Text** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Text**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Text** is concrete, continue — the next lesson in this phase still depends on it.

## 10.3 Html

`fields.Html` stores markup for HTML editors. Sanitize assumptions: who can edit, and whether portal users see it.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Html** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Html** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Html**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Html** is concrete, continue — the next lesson in this phase still depends on it.

## 10.4 Integer

`fields.Integer` is whole counts (qty of boxes, sequence-like numbers that are not `ir.sequence`).

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Integer** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Integer** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Integer**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Integer** is concrete, continue — the next lesson in this phase still depends on it.

## 10.5 Float

`fields.Float` is approximate decimals. For money use Monetary; for exact qty consider Integer or Decimal patterns used by stock.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Float** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Float** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Float**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Float** is concrete, continue — the next lesson in this phase still depends on it.

## 10.6 Boolean

`fields.Boolean` is a flag (`active`, `is_company`). Pair with record rules or attrs when it changes who may act.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Boolean** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Boolean** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Boolean**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Boolean** is concrete, continue — the next lesson in this phase still depends on it.

## 10.7 Date

`fields.Date` is a calendar day without timezone. Good for deadlines and fiscal dates.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Date** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Date** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Date**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Date** is concrete, continue — the next lesson in this phase still depends on it.

## 10.8 Datetime

`fields.Datetime` is UTC in the database; the UI converts to the user’s timezone. Mixing Date and Datetime in comparisons is a classic bug.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Datetime** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Datetime** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Datetime**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Datetime** is concrete, continue — the next lesson in this phase still depends on it.

## 10.9 Selection

`fields.Selection` is a closed list of states or types. Changing keys later is a data migration.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Selection** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Selection** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Selection**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Selection** is concrete, continue — the next lesson in this phase still depends on it.

## 10.10 Monetary

`fields.Monetary` needs a `currency_field`. Never store money as unconstrained Float if the UI must round like accounting.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Monetary** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Monetary** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Monetary**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Monetary** is concrete, continue — the next lesson in this phase still depends on it.

## 10.11 Binary

`fields.Binary` holds file bytes (often attachment-backed). Prefer attachments for multi-file chatter documents.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Binary** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Binary** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Binary**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Binary** is concrete, continue — the next lesson in this phase still depends on it.

## 10.12 Image

`fields.Image` is Binary with image widgets and optional max resolution — employee photos, product images.

Declare it on a model, show it on a form/list, and confirm the PostgreSQL column after `-u`. **Image** only pays rent when a user or a constraint needs that fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Image** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Image**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Image** is concrete, continue — the next lesson in this phase still depends on it.

## 10.13 Field Attributes

**Field Attributes** belongs to **Fields**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Field Attributes** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Field Attributes** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Field Attributes**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Field Attributes** is concrete, continue — the next lesson in this phase still depends on it.

## 10.14 PRACTICE — Model Business Records

Extend `library.book` (and optionally add `library.member`) until the fields below exist and accept demo creates from the shell.

**SCENARIO — SMALL PUBLIC LIBRARY:**

| Model | Fields to add |
| ----- | ------------- |
| `library.book` | title fields above + `list_price` Monetary + `format` Selection |
| `library.member` | `name` Char, `email` Char, `date_joined` Date, `is_active` Boolean |
| Preview only | A future `library.loan` will need Datetime checkout — name the fields on paper |

```python
env["library.book"].create({
    "name": "The Pragmatic Programmer",
    "isbn": "978-0135957059",
    "page_count": 352,
    "format": "paperback",
    "list_price": 49.99,
    "date_published": "2019-09-13",
})
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `synopsis` Text and `is_available` Boolean with a sensible default |
| Easy | Explain when you would pick Date vs Datetime for a leave request's start |
| Medium | Model a sales order header's scalar fields only (name/ref, date_order, state Selection, amount_total Monetary) — no lines yet |
| Medium | Break Monetary by omitting `currency_id`; fix it and document the error |
| Hard | Design field list for `hr.leave`-style request: employee name (Char for now), dates, days Float, state Selection — justify each type |

**WHAT THIS UNLOCKS NEXT:**

Scalar columns describe one record in isolation. Real businesses connect books to authors, orders to customers, loans to members. Phase 11 adds Many2one, One2many, Many2many, and related fields.


---


<a id="phase-11"></a>
<a id="phase-11---relationships"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 10:** You can explain every lesson title in Phase 10 with a concrete Odoo or business example.

> **Phase 10 complete?** [Continue to Phase 11](#phase-11)


<a id="phase-11"></a>
# PHASE 11 - Relationships

**Track:** Modeling Data

**WHAT YOU WILL BE ABLE TO DO:** Model Many2one, One2many, and Many2many links correctly, expose related fields for denormalized display, and draw a coherent business data graph for a library or sales domain.

**WHAT YOU SHOULD KNOW FIRST:** Phase 10 — scalar fields on registered models.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Relational fields](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#relational-fields)
- [Relational fields tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/06_basicviews.html)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [ORM relationships video](https://www.youtube.com/watch?v=ASPjB-WowBU)
- [odoo/odoo](https://github.com/odoo/odoo)

## 11.1 Many2one

Business software is a graph of records. A loan points to one member and one book copy. An order has many lines. A book has many authors and an author has many books. Odoo gives you three relational field types for that graph.

**MANY2ONE — "THIS RECORD POINTS TO ONE OTHER":**

```python
class LibraryLoan(models.Model):
    _name = "library.loan"
    _description = "Library Loan"

    member_id = fields.Many2one("library.member", string="Member", required=True, ondelete="restrict")
    book_id = fields.Many2one("library.book", string="Book", required=True, ondelete="restrict")
    date_borrowed = fields.Datetime(default=fields.Datetime.now)
```

SQL reality: a column `member_id` stores the other row's integer `id`. `ondelete` matters:

| ondelete | When the target is deleted |
| -------- | -------------------------- |
| `cascade` | Delete this row too |
| `restrict` | Block deletion if references exist |
| `set null` | Clear the FK (field must not be required) |

Prefer `restrict` for business history (do not erase loans when someone deletes a member by accident). Use `cascade` only when the child has no meaning alone (order lines when the order dies).

**ONE2MANY — "THE INVERSE LIST":**

One2many is not stored as its own column. It is the reverse search of a Many2one on the other model.

```python
class LibraryMember(models.Model):
    _name = "library.member"
    _description = "Library Member"

    name = fields.Char(required=True)
    loan_ids = fields.One2many("library.loan", "member_id", string="Loans")
```

```text
library.member                 library.loan
+----------------+             +------------------+
| id = 7         | <---------- | member_id = 7    |
| loan_ids ----+ |             | book_id = 3      |
+--------------|-+             +------------------+
               |               +------------------+
               +-------------> | member_id = 7    |
                               | book_id = 9      |
                               +------------------+
```

You almost always declare the Many2one first (the real FK), then the One2many inverse for UI and convenience.

**MANY2MANY — "SET MEMBERSHIP BOTH WAYS":**

```python
class LibraryBook(models.Model):
    _name = "library.book"
    ...
    author_ids = fields.Many2many(
        "library.author",
        "library_book_author_rel",  # relation table name
        "book_id",
        "author_id",
        string="Authors",
    )


class LibraryAuthor(models.Model):
    _name = "library.author"
    ...
    book_ids = fields.Many2many(
        "library.book",
        "library_book_author_rel",
        "author_id",
        "book_id",
        string="Books",
    )
```

Odoo creates (or uses) a relation table with two FK columns. Name the relation table explicitly on both sides so both fields share one table — otherwise you can accidentally get two relation tables and a broken graph.

**COMANDS VS ORM WRITES (PREVIEW):**

Writing One2many / Many2many from Python uses command tuples `(0,0,vals)`, `(1,id,vals)`, `(2,id)`, `(3,id)`, `(4,id)`, `(5,)`, `(6,0,[ids])`. Phase 14 practices them in CRUD. For modeling, know that `loan_ids` and `author_ids` are editable collections, not plain lists of integers.

## 11.2 One2many

Business software is a graph of records. A loan points to one member and one book copy. An order has many lines. A book has many authors and an author has many books. Odoo gives you three relational field types for that graph.

**MANY2ONE — "THIS RECORD POINTS TO ONE OTHER":**

```python
class LibraryLoan(models.Model):
    _name = "library.loan"
    _description = "Library Loan"

    member_id = fields.Many2one("library.member", string="Member", required=True, ondelete="restrict")
    book_id = fields.Many2one("library.book", string="Book", required=True, ondelete="restrict")
    date_borrowed = fields.Datetime(default=fields.Datetime.now)
```

SQL reality: a column `member_id` stores the other row's integer `id`. `ondelete` matters:

| ondelete | When the target is deleted |
| -------- | -------------------------- |
| `cascade` | Delete this row too |
| `restrict` | Block deletion if references exist |
| `set null` | Clear the FK (field must not be required) |

Prefer `restrict` for business history (do not erase loans when someone deletes a member by accident). Use `cascade` only when the child has no meaning alone (order lines when the order dies).

**ONE2MANY — "THE INVERSE LIST":**

One2many is not stored as its own column. It is the reverse search of a Many2one on the other model.

```python
class LibraryMember(models.Model):
    _name = "library.member"
    _description = "Library Member"

    name = fields.Char(required=True)
    loan_ids = fields.One2many("library.loan", "member_id", string="Loans")
```

```text
library.member                 library.loan
+----------------+             +------------------+
| id = 7         | <---------- | member_id = 7    |
| loan_ids ----+ |             | book_id = 3      |
+--------------|-+             +------------------+
               |               +------------------+
               +-------------> | member_id = 7    |
                               | book_id = 9      |
                               +------------------+
```

You almost always declare the Many2one first (the real FK), then the One2many inverse for UI and convenience.

**MANY2MANY — "SET MEMBERSHIP BOTH WAYS":**

```python
class LibraryBook(models.Model):
    _name = "library.book"
    ...
    author_ids = fields.Many2many(
        "library.author",
        "library_book_author_rel",  # relation table name
        "book_id",
        "author_id",
        string="Authors",
    )


class LibraryAuthor(models.Model):
    _name = "library.author"
    ...
    book_ids = fields.Many2many(
        "library.book",
        "library_book_author_rel",
        "author_id",
        "book_id",
        string="Books",
    )
```

Odoo creates (or uses) a relation table with two FK columns. Name the relation table explicitly on both sides so both fields share one table — otherwise you can accidentally get two relation tables and a broken graph.

**COMANDS VS ORM WRITES (PREVIEW):**

Writing One2many / Many2many from Python uses command tuples `(0,0,vals)`, `(1,id,vals)`, `(2,id)`, `(3,id)`, `(4,id)`, `(5,)`, `(6,0,[ids])`. Phase 14 practices them in CRUD. For modeling, know that `loan_ids` and `author_ids` are editable collections, not plain lists of integers.

## 11.3 Many2many

Business software is a graph of records. A loan points to one member and one book copy. An order has many lines. A book has many authors and an author has many books. Odoo gives you three relational field types for that graph.

**MANY2ONE — "THIS RECORD POINTS TO ONE OTHER":**

```python
class LibraryLoan(models.Model):
    _name = "library.loan"
    _description = "Library Loan"

    member_id = fields.Many2one("library.member", string="Member", required=True, ondelete="restrict")
    book_id = fields.Many2one("library.book", string="Book", required=True, ondelete="restrict")
    date_borrowed = fields.Datetime(default=fields.Datetime.now)
```

SQL reality: a column `member_id` stores the other row's integer `id`. `ondelete` matters:

| ondelete | When the target is deleted |
| -------- | -------------------------- |
| `cascade` | Delete this row too |
| `restrict` | Block deletion if references exist |
| `set null` | Clear the FK (field must not be required) |

Prefer `restrict` for business history (do not erase loans when someone deletes a member by accident). Use `cascade` only when the child has no meaning alone (order lines when the order dies).

**ONE2MANY — "THE INVERSE LIST":**

One2many is not stored as its own column. It is the reverse search of a Many2one on the other model.

```python
class LibraryMember(models.Model):
    _name = "library.member"
    _description = "Library Member"

    name = fields.Char(required=True)
    loan_ids = fields.One2many("library.loan", "member_id", string="Loans")
```

```text
library.member                 library.loan
+----------------+             +------------------+
| id = 7         | <---------- | member_id = 7    |
| loan_ids ----+ |             | book_id = 3      |
+--------------|-+             +------------------+
               |               +------------------+
               +-------------> | member_id = 7    |
                               | book_id = 9      |
                               +------------------+
```

You almost always declare the Many2one first (the real FK), then the One2many inverse for UI and convenience.

**MANY2MANY — "SET MEMBERSHIP BOTH WAYS":**

```python
class LibraryBook(models.Model):
    _name = "library.book"
    ...
    author_ids = fields.Many2many(
        "library.author",
        "library_book_author_rel",  # relation table name
        "book_id",
        "author_id",
        string="Authors",
    )


class LibraryAuthor(models.Model):
    _name = "library.author"
    ...
    book_ids = fields.Many2many(
        "library.book",
        "library_book_author_rel",
        "author_id",
        "book_id",
        string="Books",
    )
```

Odoo creates (or uses) a relation table with two FK columns. Name the relation table explicitly on both sides so both fields share one table — otherwise you can accidentally get two relation tables and a broken graph.

**COMANDS VS ORM WRITES (PREVIEW):**

Writing One2many / Many2many from Python uses command tuples `(0,0,vals)`, `(1,id,vals)`, `(2,id)`, `(3,id)`, `(4,id)`, `(5,)`, `(6,0,[ids])`. Phase 14 practices them in CRUD. For modeling, know that `loan_ids` and `author_ids` are editable collections, not plain lists of integers.

## 11.4 Related Fields

**RELATED — READ A VALUE THROUGH A MANY2ONE PATH:**

```python
member_email = fields.Char(related="member_id.email", string="Member Email", store=False)
```

Related fields avoid forcing users to open the member just to see an email on the loan form. `store=True` denormalizes into a real column (faster search/group by; must invalidate when the source changes — Odoo handles depends). Start with `store=False` unless you search/group on it.

Related is not a substitute for a real FK. If you need to *set* the member's email from the loan, you want an inverse or an explicit write on `member_id` — not a related Char.

**DESIGN PROJECT — DRAW THE GRAPH BEFORE MORE CODE:**

```text
library.author ---M2M--- library.book
                              ^
                              | M2O
                        library.loan
                              |
                              v M2O
                        library.member
                              |
                              v M2O (optional)
                         res.partner   (reuse Odoo contacts when ready)
```

**RULES OF THUMB:**

1. If the child cannot exist without the parent → Many2one on child + One2many on parent.
2. If both sides are independent peers → Many2many.
3. If you are tempted to copy `member_name` as Char on the loan → use related or display the Many2one; do not duplicate master data.
4. Prefer linking to `res.partner` for people/companies once you leave the learning sandbox — Story III already showed shared contacts.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `member_id` Many2one on `library.loan` and `loan_ids` One2many on `library.member` |
| Easy | Explain why One2many has no SQL column of its own |
| Medium | Implement book↔author Many2many with an explicit relation table name on both fields |
| Medium | Add `member_email = fields.Char(related="member_id.email")` and show it stays in sync when the member email changes |
| Hard | DESIGN PROJECT: full ASCII or paper ERD for library lending *or* sales order + order lines + products; mark each link M2O/O2M/M2M and choose `ondelete` |

**WHAT THIS UNLOCKS NEXT:**

Relationships and scalars still require humans to keep totals and derived values correct. Phase 12 teaches computed fields so the ORM maintains business calculations from their dependencies.


---

## 11.5 Relational Commands

**Relational Commands** belongs to **Relationships**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Relational Commands** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Relational Commands** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Relational Commands**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Relational Commands** is concrete, continue — the next lesson in this phase still depends on it.

## 11.6 Command.create()

Relational writes use `Command.*` instead of magic tuples. **Command.create()** is one of those commands on a One2many/Many2many value in `create`/`write`.

Read the ORM API table for `Command.create()`: whether it creates a new row, updates, unlinks, or only changes the relation. Prefer named commands over `(0, 0, vals)` so reviews stay readable.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Command.create()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Command.create()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Command.create()** is concrete, continue — the next lesson in this phase still depends on it.

## 11.7 Command.update()

Relational writes use `Command.*` instead of magic tuples. **Command.update()** is one of those commands on a One2many/Many2many value in `create`/`write`.

Read the ORM API table for `Command.update()`: whether it creates a new row, updates, unlinks, or only changes the relation. Prefer named commands over `(0, 0, vals)` so reviews stay readable.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Command.update()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Command.update()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Command.update()** is concrete, continue — the next lesson in this phase still depends on it.

## 11.8 Command.delete()

Relational writes use `Command.*` instead of magic tuples. **Command.delete()** is one of those commands on a One2many/Many2many value in `create`/`write`.

Read the ORM API table for `Command.delete()`: whether it creates a new row, updates, unlinks, or only changes the relation. Prefer named commands over `(0, 0, vals)` so reviews stay readable.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Command.delete()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Command.delete()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Command.delete()** is concrete, continue — the next lesson in this phase still depends on it.

## 11.9 Command.unlink()

Relational writes use `Command.*` instead of magic tuples. **Command.unlink()** is one of those commands on a One2many/Many2many value in `create`/`write`.

Read the ORM API table for `Command.unlink()`: whether it creates a new row, updates, unlinks, or only changes the relation. Prefer named commands over `(0, 0, vals)` so reviews stay readable.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Command.unlink()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Command.unlink()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Command.unlink()** is concrete, continue — the next lesson in this phase still depends on it.

## 11.10 Command.link()

Relational writes use `Command.*` instead of magic tuples. **Command.link()** is one of those commands on a One2many/Many2many value in `create`/`write`.

Read the ORM API table for `Command.link()`: whether it creates a new row, updates, unlinks, or only changes the relation. Prefer named commands over `(0, 0, vals)` so reviews stay readable.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Command.link()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Command.link()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Command.link()** is concrete, continue — the next lesson in this phase still depends on it.

## 11.11 Command.clear()

Relational writes use `Command.*` instead of magic tuples. **Command.clear()** is one of those commands on a One2many/Many2many value in `create`/`write`.

Read the ORM API table for `Command.clear()`: whether it creates a new row, updates, unlinks, or only changes the relation. Prefer named commands over `(0, 0, vals)` so reviews stay readable.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Command.clear()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Command.clear()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Command.clear()** is concrete, continue — the next lesson in this phase still depends on it.

## 11.12 Command.set()

Relational writes use `Command.*` instead of magic tuples. **Command.set()** is one of those commands on a One2many/Many2many value in `create`/`write`.

Read the ORM API table for `Command.set()`: whether it creates a new row, updates, unlinks, or only changes the relation. Prefer named commands over `(0, 0, vals)` so reviews stay readable.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Command.set()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Command.set()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Command.set()** is concrete, continue — the next lesson in this phase still depends on it.

## 11.13 DESIGN PROJECT — Business Data Graph

**RELATED — READ A VALUE THROUGH A MANY2ONE PATH:**

```python
member_email = fields.Char(related="member_id.email", string="Member Email", store=False)
```

Related fields avoid forcing users to open the member just to see an email on the loan form. `store=True` denormalizes into a real column (faster search/group by; must invalidate when the source changes — Odoo handles depends). Start with `store=False` unless you search/group on it.

Related is not a substitute for a real FK. If you need to *set* the member's email from the loan, you want an inverse or an explicit write on `member_id` — not a related Char.

**DESIGN PROJECT — DRAW THE GRAPH BEFORE MORE CODE:**

```text
library.author ---M2M--- library.book
                              ^
                              | M2O
                        library.loan
                              |
                              v M2O
                        library.member
                              |
                              v M2O (optional)
                         res.partner   (reuse Odoo contacts when ready)
```

**RULES OF THUMB:**

1. If the child cannot exist without the parent → Many2one on child + One2many on parent.
2. If both sides are independent peers → Many2many.
3. If you are tempted to copy `member_name` as Char on the loan → use related or display the Many2one; do not duplicate master data.
4. Prefer linking to `res.partner` for people/companies once you leave the learning sandbox — Story III already showed shared contacts.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `member_id` Many2one on `library.loan` and `loan_ids` One2many on `library.member` |
| Easy | Explain why One2many has no SQL column of its own |
| Medium | Implement book↔author Many2many with an explicit relation table name on both fields |
| Medium | Add `member_email = fields.Char(related="member_id.email")` and show it stays in sync when the member email changes |
| Hard | DESIGN PROJECT: full ASCII or paper ERD for library lending *or* sales order + order lines + products; mark each link M2O/O2M/M2M and choose `ondelete` |

**WHAT THIS UNLOCKS NEXT:**

Relationships and scalars still require humans to keep totals and derived values correct. Phase 12 teaches computed fields so the ORM maintains business calculations from their dependencies.


---


<a id="phase-12"></a>
<a id="phase-12---computed-dynamic-fields"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 11:** You can explain every lesson title in Phase 11 with a concrete Odoo or business example.

> **Phase 11 complete?** [Continue to Phase 12](#phase-12)


<a id="phase-12"></a>
# PHASE 12 - Computed & Dynamic Fields

**Track:** Modeling Data

**WHAT YOU WILL BE ABLE TO DO:** Implement `compute` methods with `@api.depends`, choose `store`, add `inverse` and `search` when needed, and ship automatic business calculations (loan duration, order totals) without hand-maintained duplicate numbers.

**WHAT YOU SHOULD KNOW FIRST:** Phase 11 — relational fields; Phase 10 — Monetary / Float / Integer.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Computed fields](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#computed-fields)
- [Compute / depends / store](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#decorators)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [ORM deep dive (video)](https://www.youtube.com/watch?v=ASPjB-WowBU)
- [odoo/odoo](https://github.com/odoo/odoo)

## 12.1 compute

A stored Integer `day_count` that staff update by hand will drift. A computed field recalculates from sources you declare — that is the difference between a spreadsheet cell with a formula and a cell someone typed.

**MINIMAL PATTERN:**

```python
from odoo import api, fields, models


class LibraryLoan(models.Model):
    _name = "library.loan"
    _description = "Library Loan"

    date_borrowed = fields.Datetime(required=True)
    date_returned = fields.Datetime()
    duration_days = fields.Integer(
        string="Days Out",
        compute="_compute_duration_days",
        store=True,
    )

    @api.depends("date_borrowed", "date_returned")
    def _compute_duration_days(self):
        for loan in self:
            if not loan.date_borrowed:
                loan.duration_days = 0
                continue
            end = loan.date_returned or fields.Datetime.now()
            delta = end - loan.date_borrowed
            loan.duration_days = max(delta.days, 0)
```

**@api.depends IS THE CONTRACT:**

List every field path the compute reads. Miss a dependency and the value goes stale after edits — one of the most common silent Odoo bugs.

```text
User changes date_returned
        |
        v
ORM sees depends("date_borrowed", "date_returned")
        |
        v
Calls _compute_duration_days on affected recordset
        |
        v
If store=True -> write column duration_days
```

Dependencies can walk relations: `@api.depends("line_ids.price_subtotal", "line_ids.tax_id")`. Prefer the narrowest correct set — over-broad depends recompute too often; under-broad depends lie.

**STORE TRUE VS FALSE:**

| store | Behavior | Use when |
| ----- | -------- | -------- |
| `False` (default) | Computed on read; not a DB column | Cheap calcs; rarely searched |
| `True` | Column maintained; searchable/groupable | Totals, statuses used in domains |

Stored computes still need correct `@api.depends`. They are not "set once."

**INVERSE — ALLOW WRITING A COMPUTED FIELD:**

```python
price_incl = fields.Float(
    compute="_compute_price_incl",
    inverse="_inverse_price_incl",
    store=True,
)

def _inverse_price_incl(self):
    for rec in self:
        # push user edit back into base fields
        rec.price = rec.price_incl / (1 + rec.tax_rate)
```

Without `inverse`, a computed field is read-only in the ORM sense for user writes. Add inverse only when the business truly allows editing the derived value.

**SEARCH — MAKE NON-STORED FIELDS DOMAIN-CAPABLE:**

Non-stored computed fields cannot be searched unless you provide `search=`:

```python
is_overdue = fields.Boolean(compute="_compute_is_overdue", search="_search_is_overdue")

def _search_is_overdue(self, operator, value):
    # Translate domain on is_overdue into domains on real columns
    ...
```

If you need heavy searching, `store=True` is usually simpler than a clever search method.

**COMPUTE MUST ASSIGN EVERY RECORD:**

```python
# BAD: skips records -> CacheMiss / inconsistent UI
for loan in self:
    if loan.date_borrowed:
        loan.duration_days = ...

# GOOD: every record in self gets an assignment
for loan in self:
    loan.duration_days = ...  # including 0 / False
```

## 12.2 @api.depends

A stored Integer `day_count` that staff update by hand will drift. A computed field recalculates from sources you declare — that is the difference between a spreadsheet cell with a formula and a cell someone typed.

**MINIMAL PATTERN:**

```python
from odoo import api, fields, models


class LibraryLoan(models.Model):
    _name = "library.loan"
    _description = "Library Loan"

    date_borrowed = fields.Datetime(required=True)
    date_returned = fields.Datetime()
    duration_days = fields.Integer(
        string="Days Out",
        compute="_compute_duration_days",
        store=True,
    )

    @api.depends("date_borrowed", "date_returned")
    def _compute_duration_days(self):
        for loan in self:
            if not loan.date_borrowed:
                loan.duration_days = 0
                continue
            end = loan.date_returned or fields.Datetime.now()
            delta = end - loan.date_borrowed
            loan.duration_days = max(delta.days, 0)
```

**@api.depends IS THE CONTRACT:**

List every field path the compute reads. Miss a dependency and the value goes stale after edits — one of the most common silent Odoo bugs.

```text
User changes date_returned
        |
        v
ORM sees depends("date_borrowed", "date_returned")
        |
        v
Calls _compute_duration_days on affected recordset
        |
        v
If store=True -> write column duration_days
```

Dependencies can walk relations: `@api.depends("line_ids.price_subtotal", "line_ids.tax_id")`. Prefer the narrowest correct set — over-broad depends recompute too often; under-broad depends lie.

**STORE TRUE VS FALSE:**

| store | Behavior | Use when |
| ----- | -------- | -------- |
| `False` (default) | Computed on read; not a DB column | Cheap calcs; rarely searched |
| `True` | Column maintained; searchable/groupable | Totals, statuses used in domains |

Stored computes still need correct `@api.depends`. They are not "set once."

**INVERSE — ALLOW WRITING A COMPUTED FIELD:**

```python
price_incl = fields.Float(
    compute="_compute_price_incl",
    inverse="_inverse_price_incl",
    store=True,
)

def _inverse_price_incl(self):
    for rec in self:
        # push user edit back into base fields
        rec.price = rec.price_incl / (1 + rec.tax_rate)
```

Without `inverse`, a computed field is read-only in the ORM sense for user writes. Add inverse only when the business truly allows editing the derived value.

**SEARCH — MAKE NON-STORED FIELDS DOMAIN-CAPABLE:**

Non-stored computed fields cannot be searched unless you provide `search=`:

```python
is_overdue = fields.Boolean(compute="_compute_is_overdue", search="_search_is_overdue")

def _search_is_overdue(self, operator, value):
    # Translate domain on is_overdue into domains on real columns
    ...
```

If you need heavy searching, `store=True` is usually simpler than a clever search method.

**COMPUTE MUST ASSIGN EVERY RECORD:**

```python
# BAD: skips records -> CacheMiss / inconsistent UI
for loan in self:
    if loan.date_borrowed:
        loan.duration_days = ...

# GOOD: every record in self gets an assignment
for loan in self:
    loan.duration_days = ...  # including 0 / False
```

## 12.3 Stored Computed Fields

**Stored Computed Fields** belongs to **Computed & Dynamic Fields**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Stored Computed Fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Stored Computed Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Stored Computed Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Stored Computed Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 12.4 Non-Stored Fields

**Non-Stored Fields** belongs to **Computed & Dynamic Fields**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Non-Stored Fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Non-Stored Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Non-Stored Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Non-Stored Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 12.5 inverse

A stored Integer `day_count` that staff update by hand will drift. A computed field recalculates from sources you declare — that is the difference between a spreadsheet cell with a formula and a cell someone typed.

**MINIMAL PATTERN:**

```python
from odoo import api, fields, models


class LibraryLoan(models.Model):
    _name = "library.loan"
    _description = "Library Loan"

    date_borrowed = fields.Datetime(required=True)
    date_returned = fields.Datetime()
    duration_days = fields.Integer(
        string="Days Out",
        compute="_compute_duration_days",
        store=True,
    )

    @api.depends("date_borrowed", "date_returned")
    def _compute_duration_days(self):
        for loan in self:
            if not loan.date_borrowed:
                loan.duration_days = 0
                continue
            end = loan.date_returned or fields.Datetime.now()
            delta = end - loan.date_borrowed
            loan.duration_days = max(delta.days, 0)
```

**@api.depends IS THE CONTRACT:**

List every field path the compute reads. Miss a dependency and the value goes stale after edits — one of the most common silent Odoo bugs.

```text
User changes date_returned
        |
        v
ORM sees depends("date_borrowed", "date_returned")
        |
        v
Calls _compute_duration_days on affected recordset
        |
        v
If store=True -> write column duration_days
```

Dependencies can walk relations: `@api.depends("line_ids.price_subtotal", "line_ids.tax_id")`. Prefer the narrowest correct set — over-broad depends recompute too often; under-broad depends lie.

**STORE TRUE VS FALSE:**

| store | Behavior | Use when |
| ----- | -------- | -------- |
| `False` (default) | Computed on read; not a DB column | Cheap calcs; rarely searched |
| `True` | Column maintained; searchable/groupable | Totals, statuses used in domains |

Stored computes still need correct `@api.depends`. They are not "set once."

**INVERSE — ALLOW WRITING A COMPUTED FIELD:**

```python
price_incl = fields.Float(
    compute="_compute_price_incl",
    inverse="_inverse_price_incl",
    store=True,
)

def _inverse_price_incl(self):
    for rec in self:
        # push user edit back into base fields
        rec.price = rec.price_incl / (1 + rec.tax_rate)
```

Without `inverse`, a computed field is read-only in the ORM sense for user writes. Add inverse only when the business truly allows editing the derived value.

**SEARCH — MAKE NON-STORED FIELDS DOMAIN-CAPABLE:**

Non-stored computed fields cannot be searched unless you provide `search=`:

```python
is_overdue = fields.Boolean(compute="_compute_is_overdue", search="_search_is_overdue")

def _search_is_overdue(self, operator, value):
    # Translate domain on is_overdue into domains on real columns
    ...
```

If you need heavy searching, `store=True` is usually simpler than a clever search method.

**COMPUTE MUST ASSIGN EVERY RECORD:**

```python
# BAD: skips records -> CacheMiss / inconsistent UI
for loan in self:
    if loan.date_borrowed:
        loan.duration_days = ...

# GOOD: every record in self gets an assignment
for loan in self:
    loan.duration_days = ...  # including 0 / False
```

## 12.6 Custom Search Methods

A stored Integer `day_count` that staff update by hand will drift. A computed field recalculates from sources you declare — that is the difference between a spreadsheet cell with a formula and a cell someone typed.

**MINIMAL PATTERN:**

```python
from odoo import api, fields, models


class LibraryLoan(models.Model):
    _name = "library.loan"
    _description = "Library Loan"

    date_borrowed = fields.Datetime(required=True)
    date_returned = fields.Datetime()
    duration_days = fields.Integer(
        string="Days Out",
        compute="_compute_duration_days",
        store=True,
    )

    @api.depends("date_borrowed", "date_returned")
    def _compute_duration_days(self):
        for loan in self:
            if not loan.date_borrowed:
                loan.duration_days = 0
                continue
            end = loan.date_returned or fields.Datetime.now()
            delta = end - loan.date_borrowed
            loan.duration_days = max(delta.days, 0)
```

**@api.depends IS THE CONTRACT:**

List every field path the compute reads. Miss a dependency and the value goes stale after edits — one of the most common silent Odoo bugs.

```text
User changes date_returned
        |
        v
ORM sees depends("date_borrowed", "date_returned")
        |
        v
Calls _compute_duration_days on affected recordset
        |
        v
If store=True -> write column duration_days
```

Dependencies can walk relations: `@api.depends("line_ids.price_subtotal", "line_ids.tax_id")`. Prefer the narrowest correct set — over-broad depends recompute too often; under-broad depends lie.

**STORE TRUE VS FALSE:**

| store | Behavior | Use when |
| ----- | -------- | -------- |
| `False` (default) | Computed on read; not a DB column | Cheap calcs; rarely searched |
| `True` | Column maintained; searchable/groupable | Totals, statuses used in domains |

Stored computes still need correct `@api.depends`. They are not "set once."

**INVERSE — ALLOW WRITING A COMPUTED FIELD:**

```python
price_incl = fields.Float(
    compute="_compute_price_incl",
    inverse="_inverse_price_incl",
    store=True,
)

def _inverse_price_incl(self):
    for rec in self:
        # push user edit back into base fields
        rec.price = rec.price_incl / (1 + rec.tax_rate)
```

Without `inverse`, a computed field is read-only in the ORM sense for user writes. Add inverse only when the business truly allows editing the derived value.

**SEARCH — MAKE NON-STORED FIELDS DOMAIN-CAPABLE:**

Non-stored computed fields cannot be searched unless you provide `search=`:

```python
is_overdue = fields.Boolean(compute="_compute_is_overdue", search="_search_is_overdue")

def _search_is_overdue(self, operator, value):
    # Translate domain on is_overdue into domains on real columns
    ...
```

If you need heavy searching, `store=True` is usually simpler than a clever search method.

**COMPUTE MUST ASSIGN EVERY RECORD:**

```python
# BAD: skips records -> CacheMiss / inconsistent UI
for loan in self:
    if loan.date_borrowed:
        loan.duration_days = ...

# GOOD: every record in self gets an assignment
for loan in self:
    loan.duration_days = ...  # including 0 / False
```

## 12.7 Related Fields

**Related Fields** is how Odoo connects business documents. Many2one stores an `id` on this row; One2many is the inverse list; Many2many uses a relation table; related fields copy a value along a path.

Draw the graph before coding. Wrong comodel or missing `inverse_name` produces empty lists and silent UI bugs.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Related Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Related Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Related Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 12.8 Default Values

**Default Values** belongs to **Computed & Dynamic Fields**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Default Values** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Default Values** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Default Values**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Default Values** is concrete, continue — the next lesson in this phase still depends on it.

## 12.9 copy

**copy** belongs to **Computed & Dynamic Fields**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **copy** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **copy** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **copy**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **copy** is concrete, continue — the next lesson in this phase still depends on it.

## 12.10 LAB — Automatic Business Calculations

**LAB:** On `library.loan`, implement `duration_days` (stored) and `is_overdue` (compute from due date vs today). On a mini `sale.order`-style header with `line_ids`, implement `amount_total = sum(line.price_subtotal)`.

```python
class LibrarySaleOrder(models.Model):
    _name = "library.sale.order"
    _description = "Library Sale Order"

    line_ids = fields.One2many("library.sale.order.line", "order_id")
    amount_total = fields.Monetary(
        compute="_compute_amount_total",
        store=True,
        currency_field="currency_id",
    )

    @api.depends("line_ids.price_subtotal")
    def _compute_amount_total(self):
        for order in self:
            order.amount_total = sum(order.line_ids.mapped("price_subtotal"))
```

**STORY PROJECT — Business Data Management Module**

Ship a coherent `my_library` (name yours) that includes:

1. Models: author, book, member, loan (and optional sale order + lines).
2. Scalar fields, M2O/O2M/M2M as designed in Phase 11.
3. At least two stored computes with correct `@api.depends`.
4. Demo data XML or shell script creating a small graph of records.
5. README note: what is still missing (views, security, workflows) — honesty is part of the deliverable.

Story IV ends when the **data shape** is trustworthy. Screens and ACL come later; do not fake them here.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a non-stored `display_label` Char compute that joins book name + ISBN |
| Medium | Implement stored order total from lines; change a line and prove the header updates after refresh |
| Medium | Intentionally omit a depends path; demonstrate the stale value; fix it |
| Hard | LAB: overdue flag + duration + order total in one module; write three shell assertions |
| Hard | STORY PROJECT: full Business Data Management Module as specified above |

**WHAT THIS UNLOCKS NEXT:**

You can declare rich business data — but working with thousands of records safely means understanding `env`, recordsets, context, and the ORM vocabulary for create/search/write. Story V begins there: mastering the Odoo ORM as an API, not just as model definitions.


---


---


**CHECK YOUR UNDERSTANDING AFTER PHASE 12:** You can explain every lesson title in Phase 12 with a concrete Odoo or business example.

> **Phase 12 complete?** [Continue to Phase 13](#phase-13)


## STORY PROJECT — Business Data Management Module

**Track:** Modeling Data

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY V — MASTERING THE ODOO ORM

**Track:** ORM Mastery

*Talk to business records through the ORM — safely, in batches, with clear intent.*

---
<a id="phase-13"></a>
<a id="phase-13---environment-recordsets"></a>
# PHASE 13 - Environment & Recordsets

**Track:** ORM Mastery

**WHAT YOU WILL BE ABLE TO DO:** Navigate `env` and `self`, reason about recordsets (empty, singleton, multi), read and extend `context`, and correctly use the current user and company when writing business code.

**WHAT YOU SHOULD KNOW FIRST:** Story IV — models, fields, relations, computes. Module installs and upgrades.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Environment](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#environment)
- [Recordsets](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#recordsets)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [ORM video](https://www.youtube.com/watch?v=ASPjB-WowBU)
- [odoo/odoo](https://github.com/odoo/odoo)
- [odoo/tutorials](https://github.com/odoo/tutorials)

## 13.1 env

Story IV taught you how to *declare* data. Story V teaches you how to *operate* on it. Every piece of server code in Odoo runs inside an **Environment**: a cursor, a registry, a user, a context, and caches.

```text
env
 ├── cr          database cursor (transaction)
 ├── uid         current user id
 ├── context     dict of flags and defaults
 ├── registry    all models
 └── cache       field values already fetched

env["library.book"]  ->  empty recordset of that model (the "model API")
book = env["library.book"].browse(3)  ->  recordset with id 3
books = env["library.book"].search([]) ->  recordset of 0..N records
```

Inside a model method, **`self` is a recordset** of that model — possibly empty, one record, or many. Odoo methods are written to work on batches:

```python
def action_mark_available(self):
    for book in self:
        book.is_available = True
    # or better, one write:
    # self.write({"is_available": True})
```

**RECORDSET MENTAL MODEL:**

| Expression | Meaning |
| ---------- | ------- |
| `self` | Current recordset (caller decides how many) |
| `self.ensure_one()` | Assert exactly one record; else raise |
| `bool(self)` | False if empty |
| `len(self)` | Count in memory (not always a SQL COUNT) |
| `self.ids` | List of integer ids |
| `self.env` | Environment for this recordset |

```python
books = env["library.book"].search([("is_available", "=", True)])
# books is a recordset — iterating yields singletons
for book in books:
    print(book.name)  # book is a recordset of length 1
```

Accessing a Many2one returns a recordset (possibly empty). Accessing a scalar on a multi-record recordset is forbidden unless all share the same value — another reason to loop or use `mapped`.

## 13.2 self

Story IV taught you how to *declare* data. Story V teaches you how to *operate* on it. Every piece of server code in Odoo runs inside an **Environment**: a cursor, a registry, a user, a context, and caches.

```text
env
 ├── cr          database cursor (transaction)
 ├── uid         current user id
 ├── context     dict of flags and defaults
 ├── registry    all models
 └── cache       field values already fetched

env["library.book"]  ->  empty recordset of that model (the "model API")
book = env["library.book"].browse(3)  ->  recordset with id 3
books = env["library.book"].search([]) ->  recordset of 0..N records
```

Inside a model method, **`self` is a recordset** of that model — possibly empty, one record, or many. Odoo methods are written to work on batches:

```python
def action_mark_available(self):
    for book in self:
        book.is_available = True
    # or better, one write:
    # self.write({"is_available": True})
```

**RECORDSET MENTAL MODEL:**

| Expression | Meaning |
| ---------- | ------- |
| `self` | Current recordset (caller decides how many) |
| `self.ensure_one()` | Assert exactly one record; else raise |
| `bool(self)` | False if empty |
| `len(self)` | Count in memory (not always a SQL COUNT) |
| `self.ids` | List of integer ids |
| `self.env` | Environment for this recordset |

```python
books = env["library.book"].search([("is_available", "=", True)])
# books is a recordset — iterating yields singletons
for book in books:
    print(book.name)  # book is a recordset of length 1
```

Accessing a Many2one returns a recordset (possibly empty). Accessing a scalar on a multi-record recordset is forbidden unless all share the same value — another reason to loop or use `mapped`.

## 13.3 Recordsets

Story IV taught you how to *declare* data. Story V teaches you how to *operate* on it. Every piece of server code in Odoo runs inside an **Environment**: a cursor, a registry, a user, a context, and caches.

```text
env
 ├── cr          database cursor (transaction)
 ├── uid         current user id
 ├── context     dict of flags and defaults
 ├── registry    all models
 └── cache       field values already fetched

env["library.book"]  ->  empty recordset of that model (the "model API")
book = env["library.book"].browse(3)  ->  recordset with id 3
books = env["library.book"].search([]) ->  recordset of 0..N records
```

Inside a model method, **`self` is a recordset** of that model — possibly empty, one record, or many. Odoo methods are written to work on batches:

```python
def action_mark_available(self):
    for book in self:
        book.is_available = True
    # or better, one write:
    # self.write({"is_available": True})
```

**RECORDSET MENTAL MODEL:**

| Expression | Meaning |
| ---------- | ------- |
| `self` | Current recordset (caller decides how many) |
| `self.ensure_one()` | Assert exactly one record; else raise |
| `bool(self)` | False if empty |
| `len(self)` | Count in memory (not always a SQL COUNT) |
| `self.ids` | List of integer ids |
| `self.env` | Environment for this recordset |

```python
books = env["library.book"].search([("is_available", "=", True)])
# books is a recordset — iterating yields singletons
for book in books:
    print(book.name)  # book is a recordset of length 1
```

Accessing a Many2one returns a recordset (possibly empty). Accessing a scalar on a multi-record recordset is forbidden unless all share the same value — another reason to loop or use `mapped`.

## 13.4 Empty Recordsets

Story IV taught you how to *declare* data. Story V teaches you how to *operate* on it. Every piece of server code in Odoo runs inside an **Environment**: a cursor, a registry, a user, a context, and caches.

```text
env
 ├── cr          database cursor (transaction)
 ├── uid         current user id
 ├── context     dict of flags and defaults
 ├── registry    all models
 └── cache       field values already fetched

env["library.book"]  ->  empty recordset of that model (the "model API")
book = env["library.book"].browse(3)  ->  recordset with id 3
books = env["library.book"].search([]) ->  recordset of 0..N records
```

Inside a model method, **`self` is a recordset** of that model — possibly empty, one record, or many. Odoo methods are written to work on batches:

```python
def action_mark_available(self):
    for book in self:
        book.is_available = True
    # or better, one write:
    # self.write({"is_available": True})
```

**RECORDSET MENTAL MODEL:**

| Expression | Meaning |
| ---------- | ------- |
| `self` | Current recordset (caller decides how many) |
| `self.ensure_one()` | Assert exactly one record; else raise |
| `bool(self)` | False if empty |
| `len(self)` | Count in memory (not always a SQL COUNT) |
| `self.ids` | List of integer ids |
| `self.env` | Environment for this recordset |

```python
books = env["library.book"].search([("is_available", "=", True)])
# books is a recordset — iterating yields singletons
for book in books:
    print(book.name)  # book is a recordset of length 1
```

Accessing a Many2one returns a recordset (possibly empty). Accessing a scalar on a multi-record recordset is forbidden unless all share the same value — another reason to loop or use `mapped`.

## 13.5 Singleton Records

**Singleton Records** belongs to **Environment & Recordsets**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Singleton Records** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Singleton Records** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Singleton Records**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Singleton Records** is concrete, continue — the next lesson in this phase still depends on it.

## 13.6 env.user

Story IV taught you how to *declare* data. Story V teaches you how to *operate* on it. Every piece of server code in Odoo runs inside an **Environment**: a cursor, a registry, a user, a context, and caches.

```text
env
 ├── cr          database cursor (transaction)
 ├── uid         current user id
 ├── context     dict of flags and defaults
 ├── registry    all models
 └── cache       field values already fetched

env["library.book"]  ->  empty recordset of that model (the "model API")
book = env["library.book"].browse(3)  ->  recordset with id 3
books = env["library.book"].search([]) ->  recordset of 0..N records
```

Inside a model method, **`self` is a recordset** of that model — possibly empty, one record, or many. Odoo methods are written to work on batches:

```python
def action_mark_available(self):
    for book in self:
        book.is_available = True
    # or better, one write:
    # self.write({"is_available": True})
```

**RECORDSET MENTAL MODEL:**

| Expression | Meaning |
| ---------- | ------- |
| `self` | Current recordset (caller decides how many) |
| `self.ensure_one()` | Assert exactly one record; else raise |
| `bool(self)` | False if empty |
| `len(self)` | Count in memory (not always a SQL COUNT) |
| `self.ids` | List of integer ids |
| `self.env` | Environment for this recordset |

```python
books = env["library.book"].search([("is_available", "=", True)])
# books is a recordset — iterating yields singletons
for book in books:
    print(book.name)  # book is a recordset of length 1
```

Accessing a Many2one returns a recordset (possibly empty). Accessing a scalar on a multi-record recordset is forbidden unless all share the same value — another reason to loop or use `mapped`.

## 13.7 env.company

Story IV taught you how to *declare* data. Story V teaches you how to *operate* on it. Every piece of server code in Odoo runs inside an **Environment**: a cursor, a registry, a user, a context, and caches.

```text
env
 ├── cr          database cursor (transaction)
 ├── uid         current user id
 ├── context     dict of flags and defaults
 ├── registry    all models
 └── cache       field values already fetched

env["library.book"]  ->  empty recordset of that model (the "model API")
book = env["library.book"].browse(3)  ->  recordset with id 3
books = env["library.book"].search([]) ->  recordset of 0..N records
```

Inside a model method, **`self` is a recordset** of that model — possibly empty, one record, or many. Odoo methods are written to work on batches:

```python
def action_mark_available(self):
    for book in self:
        book.is_available = True
    # or better, one write:
    # self.write({"is_available": True})
```

**RECORDSET MENTAL MODEL:**

| Expression | Meaning |
| ---------- | ------- |
| `self` | Current recordset (caller decides how many) |
| `self.ensure_one()` | Assert exactly one record; else raise |
| `bool(self)` | False if empty |
| `len(self)` | Count in memory (not always a SQL COUNT) |
| `self.ids` | List of integer ids |
| `self.env` | Environment for this recordset |

```python
books = env["library.book"].search([("is_available", "=", True)])
# books is a recordset — iterating yields singletons
for book in books:
    print(book.name)  # book is a recordset of length 1
```

Accessing a Many2one returns a recordset (possibly empty). Accessing a scalar on a multi-record recordset is forbidden unless all share the same value — another reason to loop or use `mapped`.

## 13.8 env.companies

Story IV taught you how to *declare* data. Story V teaches you how to *operate* on it. Every piece of server code in Odoo runs inside an **Environment**: a cursor, a registry, a user, a context, and caches.

```text
env
 ├── cr          database cursor (transaction)
 ├── uid         current user id
 ├── context     dict of flags and defaults
 ├── registry    all models
 └── cache       field values already fetched

env["library.book"]  ->  empty recordset of that model (the "model API")
book = env["library.book"].browse(3)  ->  recordset with id 3
books = env["library.book"].search([]) ->  recordset of 0..N records
```

Inside a model method, **`self` is a recordset** of that model — possibly empty, one record, or many. Odoo methods are written to work on batches:

```python
def action_mark_available(self):
    for book in self:
        book.is_available = True
    # or better, one write:
    # self.write({"is_available": True})
```

**RECORDSET MENTAL MODEL:**

| Expression | Meaning |
| ---------- | ------- |
| `self` | Current recordset (caller decides how many) |
| `self.ensure_one()` | Assert exactly one record; else raise |
| `bool(self)` | False if empty |
| `len(self)` | Count in memory (not always a SQL COUNT) |
| `self.ids` | List of integer ids |
| `self.env` | Environment for this recordset |

```python
books = env["library.book"].search([("is_available", "=", True)])
# books is a recordset — iterating yields singletons
for book in books:
    print(book.name)  # book is a recordset of length 1
```

Accessing a Many2one returns a recordset (possibly empty). Accessing a scalar on a multi-record recordset is forbidden unless all share the same value — another reason to loop or use `mapped`.

## 13.9 Context

**CONTEXT** is a dict that travels with the environment. It carries defaults, UI flags, and business switches.

```python
# Read
lang = self.env.context.get("lang")
# Extend without mutating shared state wrongly
self.with_context(mail_notrack=True).write({"name": "New Title"})
active_books = self.with_context(active_test=False).search([("name", "ilike", "draft")])
```

`active_test` (default True) hides `active=False` records from search. Archiving is not deletion — context decides whether archived rows appear.

**CURRENT USER:**

```python
user = self.env.user          # res.users recordset
partner = self.env.user.partner_id
if not self.env.user.has_group("base.group_system"):
    raise UserError("Only administrators can do this.")
```

`self.env.uid` is the raw id. Prefer `self.env.user` for readability.

**CURRENT COMPANY (MULTI-COMPANY):**

```python
company = self.env.company              # active company
companies = self.env.companies          # allowed companies in this env
```

Company-dependent fields and `record rules` key off this. When creating records in cron or shell, set company explicitly if the default would be wrong:

```python
env["library.book"].with_company(company).create({...})
```

**WITH_USER / SUDO (PREVIEW OF POWER AND DANGER):**

```python
self.sudo()  # bypass record rules / ACL — use rarely, never casually in HTTP
self.with_user(other_user)  # run as another user
```

Story VIII will deepen security. For now: every `sudo()` is a conscious trust decision. Prefer fixing ACL/record rules over sprinkling `sudo()`.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In a model method, log `self.env.user.name`, `self.env.company.name`, and `len(self)` |
| Easy | Show `search` with and without `active_test=False` on archived books |
| Medium | Write a method that works on a multi-record `self` using one `write`, not a Python loop of creates |
| Medium | Explain what recordset you get from an empty Many2one access (`loan.member_id`) |
| Hard | Trace a button click: which uid, company, and context keys are present — document five context keys you find |

**WHAT THIS UNLOCKS NEXT:**

Environment and recordsets are the stage. Phase 14 gives you the verbs — create, browse, search, read, write, unlink — to change business data safely.


---

## 13.10 with_context()

**CONTEXT** is a dict that travels with the environment. It carries defaults, UI flags, and business switches.

```python
# Read
lang = self.env.context.get("lang")
# Extend without mutating shared state wrongly
self.with_context(mail_notrack=True).write({"name": "New Title"})
active_books = self.with_context(active_test=False).search([("name", "ilike", "draft")])
```

`active_test` (default True) hides `active=False` records from search. Archiving is not deletion — context decides whether archived rows appear.

**CURRENT USER:**

```python
user = self.env.user          # res.users recordset
partner = self.env.user.partner_id
if not self.env.user.has_group("base.group_system"):
    raise UserError("Only administrators can do this.")
```

`self.env.uid` is the raw id. Prefer `self.env.user` for readability.

**CURRENT COMPANY (MULTI-COMPANY):**

```python
company = self.env.company              # active company
companies = self.env.companies          # allowed companies in this env
```

Company-dependent fields and `record rules` key off this. When creating records in cron or shell, set company explicitly if the default would be wrong:

```python
env["library.book"].with_company(company).create({...})
```

**WITH_USER / SUDO (PREVIEW OF POWER AND DANGER):**

```python
self.sudo()  # bypass record rules / ACL — use rarely, never casually in HTTP
self.with_user(other_user)  # run as another user
```

Story VIII will deepen security. For now: every `sudo()` is a conscious trust decision. Prefer fixing ACL/record rules over sprinkling `sudo()`.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In a model method, log `self.env.user.name`, `self.env.company.name`, and `len(self)` |
| Easy | Show `search` with and without `active_test=False` on archived books |
| Medium | Write a method that works on a multi-record `self` using one `write`, not a Python loop of creates |
| Medium | Explain what recordset you get from an empty Many2one access (`loan.member_id`) |
| Hard | Trace a button click: which uid, company, and context keys are present — document five context keys you find |

**WHAT THIS UNLOCKS NEXT:**

Environment and recordsets are the stage. Phase 14 gives you the verbs — create, browse, search, read, write, unlink — to change business data safely.


---

## 13.11 with_user()

**CONTEXT** is a dict that travels with the environment. It carries defaults, UI flags, and business switches.

```python
# Read
lang = self.env.context.get("lang")
# Extend without mutating shared state wrongly
self.with_context(mail_notrack=True).write({"name": "New Title"})
active_books = self.with_context(active_test=False).search([("name", "ilike", "draft")])
```

`active_test` (default True) hides `active=False` records from search. Archiving is not deletion — context decides whether archived rows appear.

**CURRENT USER:**

```python
user = self.env.user          # res.users recordset
partner = self.env.user.partner_id
if not self.env.user.has_group("base.group_system"):
    raise UserError("Only administrators can do this.")
```

`self.env.uid` is the raw id. Prefer `self.env.user` for readability.

**CURRENT COMPANY (MULTI-COMPANY):**

```python
company = self.env.company              # active company
companies = self.env.companies          # allowed companies in this env
```

Company-dependent fields and `record rules` key off this. When creating records in cron or shell, set company explicitly if the default would be wrong:

```python
env["library.book"].with_company(company).create({...})
```

**WITH_USER / SUDO (PREVIEW OF POWER AND DANGER):**

```python
self.sudo()  # bypass record rules / ACL — use rarely, never casually in HTTP
self.with_user(other_user)  # run as another user
```

Story VIII will deepen security. For now: every `sudo()` is a conscious trust decision. Prefer fixing ACL/record rules over sprinkling `sudo()`.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In a model method, log `self.env.user.name`, `self.env.company.name`, and `len(self)` |
| Easy | Show `search` with and without `active_test=False` on archived books |
| Medium | Write a method that works on a multi-record `self` using one `write`, not a Python loop of creates |
| Medium | Explain what recordset you get from an empty Many2one access (`loan.member_id`) |
| Hard | Trace a button click: which uid, company, and context keys are present — document five context keys you find |

**WHAT THIS UNLOCKS NEXT:**

Environment and recordsets are the stage. Phase 14 gives you the verbs — create, browse, search, read, write, unlink — to change business data safely.


---

## 13.12 with_company()

**CONTEXT** is a dict that travels with the environment. It carries defaults, UI flags, and business switches.

```python
# Read
lang = self.env.context.get("lang")
# Extend without mutating shared state wrongly
self.with_context(mail_notrack=True).write({"name": "New Title"})
active_books = self.with_context(active_test=False).search([("name", "ilike", "draft")])
```

`active_test` (default True) hides `active=False` records from search. Archiving is not deletion — context decides whether archived rows appear.

**CURRENT USER:**

```python
user = self.env.user          # res.users recordset
partner = self.env.user.partner_id
if not self.env.user.has_group("base.group_system"):
    raise UserError("Only administrators can do this.")
```

`self.env.uid` is the raw id. Prefer `self.env.user` for readability.

**CURRENT COMPANY (MULTI-COMPANY):**

```python
company = self.env.company              # active company
companies = self.env.companies          # allowed companies in this env
```

Company-dependent fields and `record rules` key off this. When creating records in cron or shell, set company explicitly if the default would be wrong:

```python
env["library.book"].with_company(company).create({...})
```

**WITH_USER / SUDO (PREVIEW OF POWER AND DANGER):**

```python
self.sudo()  # bypass record rules / ACL — use rarely, never casually in HTTP
self.with_user(other_user)  # run as another user
```

Story VIII will deepen security. For now: every `sudo()` is a conscious trust decision. Prefer fixing ACL/record rules over sprinkling `sudo()`.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | In a model method, log `self.env.user.name`, `self.env.company.name`, and `len(self)` |
| Easy | Show `search` with and without `active_test=False` on archived books |
| Medium | Write a method that works on a multi-record `self` using one `write`, not a Python loop of creates |
| Medium | Explain what recordset you get from an empty Many2one access (`loan.member_id`) |
| Hard | Trace a button click: which uid, company, and context keys are present — document five context keys you find |

**WHAT THIS UNLOCKS NEXT:**

Environment and recordsets are the stage. Phase 14 gives you the verbs — create, browse, search, read, write, unlink — to change business data safely.


---

## 13.13 sudo()

`sudo()` is an ORM entry point on a recordset (or environment). Call it with the documented signature from [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html).

Practice on a tiny recordset: print `self`, check empty vs singleton, and avoid Python loops that hide N+1 queries. Overrides must call `super()` unless you can prove you replaced the contract.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **sudo()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **sudo()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **sudo()** is concrete, continue — the next lesson in this phase still depends on it.

## 13.14 with_env()

Story IV taught you how to *declare* data. Story V teaches you how to *operate* on it. Every piece of server code in Odoo runs inside an **Environment**: a cursor, a registry, a user, a context, and caches.

```text
env
 ├── cr          database cursor (transaction)
 ├── uid         current user id
 ├── context     dict of flags and defaults
 ├── registry    all models
 └── cache       field values already fetched

env["library.book"]  ->  empty recordset of that model (the "model API")
book = env["library.book"].browse(3)  ->  recordset with id 3
books = env["library.book"].search([]) ->  recordset of 0..N records
```

Inside a model method, **`self` is a recordset** of that model — possibly empty, one record, or many. Odoo methods are written to work on batches:

```python
def action_mark_available(self):
    for book in self:
        book.is_available = True
    # or better, one write:
    # self.write({"is_available": True})
```

**RECORDSET MENTAL MODEL:**

| Expression | Meaning |
| ---------- | ------- |
| `self` | Current recordset (caller decides how many) |
| `self.ensure_one()` | Assert exactly one record; else raise |
| `bool(self)` | False if empty |
| `len(self)` | Count in memory (not always a SQL COUNT) |
| `self.ids` | List of integer ids |
| `self.env` | Environment for this recordset |

```python
books = env["library.book"].search([("is_available", "=", True)])
# books is a recordset — iterating yields singletons
for book in books:
    print(book.name)  # book is a recordset of length 1
```

Accessing a Many2one returns a recordset (possibly empty). Accessing a scalar on a multi-record recordset is forbidden unless all share the same value — another reason to loop or use `mapped`.


<a id="phase-14"></a>
<a id="phase-14---crud"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 13:** You can explain every lesson title in Phase 13 with a concrete Odoo or business example.

> **Phase 13 complete?** [Continue to Phase 14](#phase-14)


<a id="phase-14"></a>
# PHASE 14 - CRUD

**Track:** ORM Mastery

**WHAT YOU WILL BE ABLE TO DO:** Use `create`, `browse`, `search`, `read`, `write`, and `unlink` correctly on recordsets, including One2many/Many2many commands, and complete coding drills that mirror real library and sales operations.

**WHAT YOU SHOULD KNOW FIRST:** Phase 13 — env, recordsets, context.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — CRUD](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#model-reference)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [ORM video](https://www.youtube.com/watch?v=ASPjB-WowBU)
- [odoo/odoo](https://github.com/odoo/odoo)

## 14.1 create()

**CREATE — INSERT ROWS:**

```python
book = env["library.book"].create({
    "name": "Refactoring",
    "isbn": "978-0134757599",
    "format": "paperback",
})
# Multi-create (batch) — preferred when inserting many:
env["library.book"].create([
    {"name": "Book A"},
    {"name": "Book B"},
])
```

Defaults, computes, and constraints run. Override `create` later (Phase 18) for business side effects.

**BROWSE — RECORDSET FROM IDS (NO SQL BY ITSELF):**

```python
books = env["library.book"].browse([1, 2, 3])
# No search query yet — access fields to fetch
print(books.mapped("name"))
```

Browse on missing ids yields a recordset that may fail when you access fields — validate ids if they come from users.

**SEARCH — IDS FROM A DOMAIN:**

```python
books = env["library.book"].search([
    ("is_available", "=", True),
    ("format", "=", "ebook"),
], limit=50, order="name asc")
```

Phase 15 deepens domains. For now: search returns a recordset; it does not return dicts.

**READ — DICTS FOR EXTERNAL SHAPES:**

```python
data = books.read(["name", "isbn", "list_price"])
# [{'id': 1, 'name': '...', ...}, ...]
```

Prefer field access / `mapped` inside server code. Use `read` when exporting or answering RPC-like needs.

**WRITE — UPDATE:**

```python
books.write({"is_available": False})
# One SQL UPDATE for the batch when values are uniform
```

```python
loan.write({
    "book_id": book.id,
    "line_ids": [
        (0, 0, {"product_id": p.id, "qty": 2}),  # create line
        (1, line.id, {"qty": 5}),                # update line
        (2, line.id),                            # delete line
        (6, 0, [id1, id2]),                      # replace M2M ids
    ],
})
```

**UNLINK — DELETE:**

```python
loans.unlink()
```

Constraints and `ondelete` on other models fire. Prefer archive (`active=False`) for master data users might reference historically.

## 14.2 browse()

**CREATE — INSERT ROWS:**

```python
book = env["library.book"].create({
    "name": "Refactoring",
    "isbn": "978-0134757599",
    "format": "paperback",
})
# Multi-create (batch) — preferred when inserting many:
env["library.book"].create([
    {"name": "Book A"},
    {"name": "Book B"},
])
```

Defaults, computes, and constraints run. Override `create` later (Phase 18) for business side effects.

**BROWSE — RECORDSET FROM IDS (NO SQL BY ITSELF):**

```python
books = env["library.book"].browse([1, 2, 3])
# No search query yet — access fields to fetch
print(books.mapped("name"))
```

Browse on missing ids yields a recordset that may fail when you access fields — validate ids if they come from users.

**SEARCH — IDS FROM A DOMAIN:**

```python
books = env["library.book"].search([
    ("is_available", "=", True),
    ("format", "=", "ebook"),
], limit=50, order="name asc")
```

Phase 15 deepens domains. For now: search returns a recordset; it does not return dicts.

**READ — DICTS FOR EXTERNAL SHAPES:**

```python
data = books.read(["name", "isbn", "list_price"])
# [{'id': 1, 'name': '...', ...}, ...]
```

Prefer field access / `mapped` inside server code. Use `read` when exporting or answering RPC-like needs.

**WRITE — UPDATE:**

```python
books.write({"is_available": False})
# One SQL UPDATE for the batch when values are uniform
```

```python
loan.write({
    "book_id": book.id,
    "line_ids": [
        (0, 0, {"product_id": p.id, "qty": 2}),  # create line
        (1, line.id, {"qty": 5}),                # update line
        (2, line.id),                            # delete line
        (6, 0, [id1, id2]),                      # replace M2M ids
    ],
})
```

**UNLINK — DELETE:**

```python
loans.unlink()
```

Constraints and `ondelete` on other models fire. Prefer archive (`active=False`) for master data users might reference historically.

## 14.3 search()

**CREATE — INSERT ROWS:**

```python
book = env["library.book"].create({
    "name": "Refactoring",
    "isbn": "978-0134757599",
    "format": "paperback",
})
# Multi-create (batch) — preferred when inserting many:
env["library.book"].create([
    {"name": "Book A"},
    {"name": "Book B"},
])
```

Defaults, computes, and constraints run. Override `create` later (Phase 18) for business side effects.

**BROWSE — RECORDSET FROM IDS (NO SQL BY ITSELF):**

```python
books = env["library.book"].browse([1, 2, 3])
# No search query yet — access fields to fetch
print(books.mapped("name"))
```

Browse on missing ids yields a recordset that may fail when you access fields — validate ids if they come from users.

**SEARCH — IDS FROM A DOMAIN:**

```python
books = env["library.book"].search([
    ("is_available", "=", True),
    ("format", "=", "ebook"),
], limit=50, order="name asc")
```

Phase 15 deepens domains. For now: search returns a recordset; it does not return dicts.

**READ — DICTS FOR EXTERNAL SHAPES:**

```python
data = books.read(["name", "isbn", "list_price"])
# [{'id': 1, 'name': '...', ...}, ...]
```

Prefer field access / `mapped` inside server code. Use `read` when exporting or answering RPC-like needs.

**WRITE — UPDATE:**

```python
books.write({"is_available": False})
# One SQL UPDATE for the batch when values are uniform
```

```python
loan.write({
    "book_id": book.id,
    "line_ids": [
        (0, 0, {"product_id": p.id, "qty": 2}),  # create line
        (1, line.id, {"qty": 5}),                # update line
        (2, line.id),                            # delete line
        (6, 0, [id1, id2]),                      # replace M2M ids
    ],
})
```

**UNLINK — DELETE:**

```python
loans.unlink()
```

Constraints and `ondelete` on other models fire. Prefer archive (`active=False`) for master data users might reference historically.

## 14.4 search_fetch()

**CREATE — INSERT ROWS:**

```python
book = env["library.book"].create({
    "name": "Refactoring",
    "isbn": "978-0134757599",
    "format": "paperback",
})
# Multi-create (batch) — preferred when inserting many:
env["library.book"].create([
    {"name": "Book A"},
    {"name": "Book B"},
])
```

Defaults, computes, and constraints run. Override `create` later (Phase 18) for business side effects.

**BROWSE — RECORDSET FROM IDS (NO SQL BY ITSELF):**

```python
books = env["library.book"].browse([1, 2, 3])
# No search query yet — access fields to fetch
print(books.mapped("name"))
```

Browse on missing ids yields a recordset that may fail when you access fields — validate ids if they come from users.

**SEARCH — IDS FROM A DOMAIN:**

```python
books = env["library.book"].search([
    ("is_available", "=", True),
    ("format", "=", "ebook"),
], limit=50, order="name asc")
```

Phase 15 deepens domains. For now: search returns a recordset; it does not return dicts.

**READ — DICTS FOR EXTERNAL SHAPES:**

```python
data = books.read(["name", "isbn", "list_price"])
# [{'id': 1, 'name': '...', ...}, ...]
```

Prefer field access / `mapped` inside server code. Use `read` when exporting or answering RPC-like needs.

**WRITE — UPDATE:**

```python
books.write({"is_available": False})
# One SQL UPDATE for the batch when values are uniform
```

```python
loan.write({
    "book_id": book.id,
    "line_ids": [
        (0, 0, {"product_id": p.id, "qty": 2}),  # create line
        (1, line.id, {"qty": 5}),                # update line
        (2, line.id),                            # delete line
        (6, 0, [id1, id2]),                      # replace M2M ids
    ],
})
```

**UNLINK — DELETE:**

```python
loans.unlink()
```

Constraints and `ondelete` on other models fire. Prefer archive (`active=False`) for master data users might reference historically.

## 14.5 read()

**CREATE — INSERT ROWS:**

```python
book = env["library.book"].create({
    "name": "Refactoring",
    "isbn": "978-0134757599",
    "format": "paperback",
})
# Multi-create (batch) — preferred when inserting many:
env["library.book"].create([
    {"name": "Book A"},
    {"name": "Book B"},
])
```

Defaults, computes, and constraints run. Override `create` later (Phase 18) for business side effects.

**BROWSE — RECORDSET FROM IDS (NO SQL BY ITSELF):**

```python
books = env["library.book"].browse([1, 2, 3])
# No search query yet — access fields to fetch
print(books.mapped("name"))
```

Browse on missing ids yields a recordset that may fail when you access fields — validate ids if they come from users.

**SEARCH — IDS FROM A DOMAIN:**

```python
books = env["library.book"].search([
    ("is_available", "=", True),
    ("format", "=", "ebook"),
], limit=50, order="name asc")
```

Phase 15 deepens domains. For now: search returns a recordset; it does not return dicts.

**READ — DICTS FOR EXTERNAL SHAPES:**

```python
data = books.read(["name", "isbn", "list_price"])
# [{'id': 1, 'name': '...', ...}, ...]
```

Prefer field access / `mapped` inside server code. Use `read` when exporting or answering RPC-like needs.

**WRITE — UPDATE:**

```python
books.write({"is_available": False})
# One SQL UPDATE for the batch when values are uniform
```

```python
loan.write({
    "book_id": book.id,
    "line_ids": [
        (0, 0, {"product_id": p.id, "qty": 2}),  # create line
        (1, line.id, {"qty": 5}),                # update line
        (2, line.id),                            # delete line
        (6, 0, [id1, id2]),                      # replace M2M ids
    ],
})
```

**UNLINK — DELETE:**

```python
loans.unlink()
```

Constraints and `ondelete` on other models fire. Prefer archive (`active=False`) for master data users might reference historically.

## 14.6 write()

**CREATE — INSERT ROWS:**

```python
book = env["library.book"].create({
    "name": "Refactoring",
    "isbn": "978-0134757599",
    "format": "paperback",
})
# Multi-create (batch) — preferred when inserting many:
env["library.book"].create([
    {"name": "Book A"},
    {"name": "Book B"},
])
```

Defaults, computes, and constraints run. Override `create` later (Phase 18) for business side effects.

**BROWSE — RECORDSET FROM IDS (NO SQL BY ITSELF):**

```python
books = env["library.book"].browse([1, 2, 3])
# No search query yet — access fields to fetch
print(books.mapped("name"))
```

Browse on missing ids yields a recordset that may fail when you access fields — validate ids if they come from users.

**SEARCH — IDS FROM A DOMAIN:**

```python
books = env["library.book"].search([
    ("is_available", "=", True),
    ("format", "=", "ebook"),
], limit=50, order="name asc")
```

Phase 15 deepens domains. For now: search returns a recordset; it does not return dicts.

**READ — DICTS FOR EXTERNAL SHAPES:**

```python
data = books.read(["name", "isbn", "list_price"])
# [{'id': 1, 'name': '...', ...}, ...]
```

Prefer field access / `mapped` inside server code. Use `read` when exporting or answering RPC-like needs.

**WRITE — UPDATE:**

```python
books.write({"is_available": False})
# One SQL UPDATE for the batch when values are uniform
```

```python
loan.write({
    "book_id": book.id,
    "line_ids": [
        (0, 0, {"product_id": p.id, "qty": 2}),  # create line
        (1, line.id, {"qty": 5}),                # update line
        (2, line.id),                            # delete line
        (6, 0, [id1, id2]),                      # replace M2M ids
    ],
})
```

**UNLINK — DELETE:**

```python
loans.unlink()
```

Constraints and `ondelete` on other models fire. Prefer archive (`active=False`) for master data users might reference historically.

## 14.7 unlink()

**CREATE — INSERT ROWS:**

```python
book = env["library.book"].create({
    "name": "Refactoring",
    "isbn": "978-0134757599",
    "format": "paperback",
})
# Multi-create (batch) — preferred when inserting many:
env["library.book"].create([
    {"name": "Book A"},
    {"name": "Book B"},
])
```

Defaults, computes, and constraints run. Override `create` later (Phase 18) for business side effects.

**BROWSE — RECORDSET FROM IDS (NO SQL BY ITSELF):**

```python
books = env["library.book"].browse([1, 2, 3])
# No search query yet — access fields to fetch
print(books.mapped("name"))
```

Browse on missing ids yields a recordset that may fail when you access fields — validate ids if they come from users.

**SEARCH — IDS FROM A DOMAIN:**

```python
books = env["library.book"].search([
    ("is_available", "=", True),
    ("format", "=", "ebook"),
], limit=50, order="name asc")
```

Phase 15 deepens domains. For now: search returns a recordset; it does not return dicts.

**READ — DICTS FOR EXTERNAL SHAPES:**

```python
data = books.read(["name", "isbn", "list_price"])
# [{'id': 1, 'name': '...', ...}, ...]
```

Prefer field access / `mapped` inside server code. Use `read` when exporting or answering RPC-like needs.

**WRITE — UPDATE:**

```python
books.write({"is_available": False})
# One SQL UPDATE for the batch when values are uniform
```

```python
loan.write({
    "book_id": book.id,
    "line_ids": [
        (0, 0, {"product_id": p.id, "qty": 2}),  # create line
        (1, line.id, {"qty": 5}),                # update line
        (2, line.id),                            # delete line
        (6, 0, [id1, id2]),                      # replace M2M ids
    ],
})
```

**UNLINK — DELETE:**

```python
loans.unlink()
```

Constraints and `ondelete` on other models fire. Prefer archive (`active=False`) for master data users might reference historically.

## 14.8 copy()

`copy()` is an ORM entry point on a recordset (or environment). Call it with the documented signature from [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html).

Practice on a tiny recordset: print `self`, check empty vs singleton, and avoid Python loops that hide N+1 queries. Overrides must call `super()` unless you can prove you replaced the contract.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **copy()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **copy()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **copy()** is concrete, continue — the next lesson in this phase still depends on it.

## 14.9 exists()

`exists()` is an ORM entry point on a recordset (or environment). Call it with the documented signature from [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html).

Practice on a tiny recordset: print `self`, check empty vs singleton, and avoid Python loops that hide N+1 queries. Overrides must call `super()` unless you can prove you replaced the contract.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **exists()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **exists()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **exists()** is concrete, continue — the next lesson in this phase still depends on it.

## 14.10 CODING PRACTICE — CRUD Operations

Run these against your library module in `odoo-bin shell` (or a TransactionCase test):

```python
Member = env["library.member"]
Book = env["library.book"]
Loan = env["library.loan"]

member = Member.create({"name": "Ada Lovelace", "email": "ada@example.com"})
book = Book.search([("name", "ilike", "Refactoring")], limit=1)
if not book:
    book = Book.create({"name": "Refactoring"})

loan = Loan.create({
    "member_id": member.id,
    "book_id": book.id,
})
loan.write({"date_returned": fields.Datetime.now()})
print(loan.read(["member_id", "book_id", "duration_days"]))
# loan.unlink()  # only when you mean it
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `create` two members; `search` both; `write` emails; prove with `mapped("email")` |
| Easy | Contrast `browse(id)` vs `search([("id", "=", id)])` — when is browse enough? |
| Medium | Create a loan with M2O ids; then update using One2many commands on `member.loan_ids` |
| Medium | Batch-create 20 books in one `create([...])`; compare mentally to 20 single creates |
| Hard | CODING PRACTICE script: checkout → return → archive member → attempt loan (should fail or be blocked by your rules) — document behavior |

**WHAT THIS UNLOCKS NEXT:**

CRUD without expressive search is blunt. Phase 15 builds domain syntax so you can ask real business questions of the database.


---


<a id="phase-15"></a>
<a id="phase-15---domains-searching"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 14:** You can explain every lesson title in Phase 14 with a concrete Odoo or business example.

> **Phase 14 complete?** [Continue to Phase 15](#phase-15)


<a id="phase-15"></a>
# PHASE 15 - Domains & Searching

**Track:** ORM Mastery

**WHAT YOU WILL BE ABLE TO DO:** Write correct domains with operators and AND/OR/NOT composition, use `search`, `search_count`, and `search_read` for business queries, and translate English questions into Odoo domains.

**WHAT YOU SHOULD KNOW FIRST:** Phase 14 — search returns recordsets; basic domain tuples.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Domains](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#search-domains)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [ORM video](https://www.youtube.com/watch?v=ASPjB-WowBU)
- [odoo/odoo](https://github.com/odoo/odoo)

## 15.1 Domain Syntax

A **domain** is a list of conditions prefix-polished into Polish notation for AND/OR/NOT. The simple form is a list of triples:

```python
[("field", "operator", value), ("other", "=", True)]
# Implicit AND between conditions
```

**COMMON OPERATORS:**

| Op | Meaning |
| -- | ------- |
| `=`, `!=` | Equality |
| `>`, `>=`, `<`, `<=` | Comparisons |
| `like`, `ilike` | SQL LIKE (`ilike` case-insensitive); use `%` wildcards |
| `=like`, `=ilike` | Pattern without auto-wrapping |
| `in`, `not in` | Membership in a list of values |
| `child_of` | Hierarchy (categories, employees) |
| `any` / `not any` | Odoo 17+ relational subdomain (prefer docs for version) |

```python
# Available ebooks published after 2020
[
    ("is_available", "=", True),
    ("format", "=", "ebook"),
    ("date_published", ">=", "2020-01-01"),
]
```

**OR / NOT — PREFIX OPERATORS:**

```python
# name contains "clean" OR format is ebook
["|", ("name", "ilike", "clean"), ("format", "=", "ebook")]

# NOT available
["!", ("is_available", "=", True)]

# (A OR B) AND C
["&", "|", ("a", "=", 1), ("b", "=", 2), ("c", "=", 3)]
```

```text
Domain evaluation is prefix-based:

  ["|", A, B]           ->  A OR B
  ["&", A, B]           ->  A AND B   (AND is also the default between items)
  ["!", A]              ->  NOT A
  ["|", A, "&", B, C]   ->  A OR (B AND C)
```

Draw a stack when domains get nested. Off-by-one prefix mistakes are common interview and production bugs.

**RELATIONAL PATHS:**

```python
[("member_id.email", "ilike", "@example.com")]
[("author_ids.name", "ilike", "Fowler")]  # across M2M
```

Paths walk Many2one / One2many / Many2many the same way related fields do. Keep them short — deep paths (`a.b.c.d`) become slow and hard to index. If you filter on `member_id.email` often, consider a related stored field or search on `member_id` with a prior member search.

**EMPTY / FALSE GOTCHAS:**

```python
# Open loans: date_returned is not set
[("date_returned", "=", False)]
# Also works in many versions:
[("date_returned", "=", None)]
```

Do not write `[("date_returned", "!=", False)]` when you mean "has a return date" without checking null behavior — prefer positive conditions when you can.

## 15.2 Comparison Operators

A **domain** is a list of conditions prefix-polished into Polish notation for AND/OR/NOT. The simple form is a list of triples:

```python
[("field", "operator", value), ("other", "=", True)]
# Implicit AND between conditions
```

**COMMON OPERATORS:**

| Op | Meaning |
| -- | ------- |
| `=`, `!=` | Equality |
| `>`, `>=`, `<`, `<=` | Comparisons |
| `like`, `ilike` | SQL LIKE (`ilike` case-insensitive); use `%` wildcards |
| `=like`, `=ilike` | Pattern without auto-wrapping |
| `in`, `not in` | Membership in a list of values |
| `child_of` | Hierarchy (categories, employees) |
| `any` / `not any` | Odoo 17+ relational subdomain (prefer docs for version) |

```python
# Available ebooks published after 2020
[
    ("is_available", "=", True),
    ("format", "=", "ebook"),
    ("date_published", ">=", "2020-01-01"),
]
```

**OR / NOT — PREFIX OPERATORS:**

```python
# name contains "clean" OR format is ebook
["|", ("name", "ilike", "clean"), ("format", "=", "ebook")]

# NOT available
["!", ("is_available", "=", True)]

# (A OR B) AND C
["&", "|", ("a", "=", 1), ("b", "=", 2), ("c", "=", 3)]
```

```text
Domain evaluation is prefix-based:

  ["|", A, B]           ->  A OR B
  ["&", A, B]           ->  A AND B   (AND is also the default between items)
  ["!", A]              ->  NOT A
  ["|", A, "&", B, C]   ->  A OR (B AND C)
```

Draw a stack when domains get nested. Off-by-one prefix mistakes are common interview and production bugs.

**RELATIONAL PATHS:**

```python
[("member_id.email", "ilike", "@example.com")]
[("author_ids.name", "ilike", "Fowler")]  # across M2M
```

Paths walk Many2one / One2many / Many2many the same way related fields do. Keep them short — deep paths (`a.b.c.d`) become slow and hard to index. If you filter on `member_id.email` often, consider a related stored field or search on `member_id` with a prior member search.

**EMPTY / FALSE GOTCHAS:**

```python
# Open loans: date_returned is not set
[("date_returned", "=", False)]
# Also works in many versions:
[("date_returned", "=", None)]
```

Do not write `[("date_returned", "!=", False)]` when you mean "has a return date" without checking null behavior — prefer positive conditions when you can.

## 15.3 in / not in

A **domain** is a list of conditions prefix-polished into Polish notation for AND/OR/NOT. The simple form is a list of triples:

```python
[("field", "operator", value), ("other", "=", True)]
# Implicit AND between conditions
```

**COMMON OPERATORS:**

| Op | Meaning |
| -- | ------- |
| `=`, `!=` | Equality |
| `>`, `>=`, `<`, `<=` | Comparisons |
| `like`, `ilike` | SQL LIKE (`ilike` case-insensitive); use `%` wildcards |
| `=like`, `=ilike` | Pattern without auto-wrapping |
| `in`, `not in` | Membership in a list of values |
| `child_of` | Hierarchy (categories, employees) |
| `any` / `not any` | Odoo 17+ relational subdomain (prefer docs for version) |

```python
# Available ebooks published after 2020
[
    ("is_available", "=", True),
    ("format", "=", "ebook"),
    ("date_published", ">=", "2020-01-01"),
]
```

**OR / NOT — PREFIX OPERATORS:**

```python
# name contains "clean" OR format is ebook
["|", ("name", "ilike", "clean"), ("format", "=", "ebook")]

# NOT available
["!", ("is_available", "=", True)]

# (A OR B) AND C
["&", "|", ("a", "=", 1), ("b", "=", 2), ("c", "=", 3)]
```

```text
Domain evaluation is prefix-based:

  ["|", A, B]           ->  A OR B
  ["&", A, B]           ->  A AND B   (AND is also the default between items)
  ["!", A]              ->  NOT A
  ["|", A, "&", B, C]   ->  A OR (B AND C)
```

Draw a stack when domains get nested. Off-by-one prefix mistakes are common interview and production bugs.

**RELATIONAL PATHS:**

```python
[("member_id.email", "ilike", "@example.com")]
[("author_ids.name", "ilike", "Fowler")]  # across M2M
```

Paths walk Many2one / One2many / Many2many the same way related fields do. Keep them short — deep paths (`a.b.c.d`) become slow and hard to index. If you filter on `member_id.email` often, consider a related stored field or search on `member_id` with a prior member search.

**EMPTY / FALSE GOTCHAS:**

```python
# Open loans: date_returned is not set
[("date_returned", "=", False)]
# Also works in many versions:
[("date_returned", "=", None)]
```

Do not write `[("date_returned", "!=", False)]` when you mean "has a return date" without checking null behavior — prefer positive conditions when you can.

## 15.4 like / ilike

**like / ilike** belongs to **Domains & Searching**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **like / ilike** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **like / ilike** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **like / ilike**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **like / ilike** is concrete, continue — the next lesson in this phase still depends on it.

## 15.5 child_of / parent_of

**child_of / parent_of** belongs to **Domains & Searching**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **child_of / parent_of** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **child_of / parent_of** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **child_of / parent_of**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **child_of / parent_of** is concrete, continue — the next lesson in this phase still depends on it.

## 15.6 AND

A **domain** is a list of conditions prefix-polished into Polish notation for AND/OR/NOT. The simple form is a list of triples:

```python
[("field", "operator", value), ("other", "=", True)]
# Implicit AND between conditions
```

**COMMON OPERATORS:**

| Op | Meaning |
| -- | ------- |
| `=`, `!=` | Equality |
| `>`, `>=`, `<`, `<=` | Comparisons |
| `like`, `ilike` | SQL LIKE (`ilike` case-insensitive); use `%` wildcards |
| `=like`, `=ilike` | Pattern without auto-wrapping |
| `in`, `not in` | Membership in a list of values |
| `child_of` | Hierarchy (categories, employees) |
| `any` / `not any` | Odoo 17+ relational subdomain (prefer docs for version) |

```python
# Available ebooks published after 2020
[
    ("is_available", "=", True),
    ("format", "=", "ebook"),
    ("date_published", ">=", "2020-01-01"),
]
```

**OR / NOT — PREFIX OPERATORS:**

```python
# name contains "clean" OR format is ebook
["|", ("name", "ilike", "clean"), ("format", "=", "ebook")]

# NOT available
["!", ("is_available", "=", True)]

# (A OR B) AND C
["&", "|", ("a", "=", 1), ("b", "=", 2), ("c", "=", 3)]
```

```text
Domain evaluation is prefix-based:

  ["|", A, B]           ->  A OR B
  ["&", A, B]           ->  A AND B   (AND is also the default between items)
  ["!", A]              ->  NOT A
  ["|", A, "&", B, C]   ->  A OR (B AND C)
```

Draw a stack when domains get nested. Off-by-one prefix mistakes are common interview and production bugs.

**RELATIONAL PATHS:**

```python
[("member_id.email", "ilike", "@example.com")]
[("author_ids.name", "ilike", "Fowler")]  # across M2M
```

Paths walk Many2one / One2many / Many2many the same way related fields do. Keep them short — deep paths (`a.b.c.d`) become slow and hard to index. If you filter on `member_id.email` often, consider a related stored field or search on `member_id` with a prior member search.

**EMPTY / FALSE GOTCHAS:**

```python
# Open loans: date_returned is not set
[("date_returned", "=", False)]
# Also works in many versions:
[("date_returned", "=", None)]
```

Do not write `[("date_returned", "!=", False)]` when you mean "has a return date" without checking null behavior — prefer positive conditions when you can.

## 15.7 OR

A **domain** is a list of conditions prefix-polished into Polish notation for AND/OR/NOT. The simple form is a list of triples:

```python
[("field", "operator", value), ("other", "=", True)]
# Implicit AND between conditions
```

**COMMON OPERATORS:**

| Op | Meaning |
| -- | ------- |
| `=`, `!=` | Equality |
| `>`, `>=`, `<`, `<=` | Comparisons |
| `like`, `ilike` | SQL LIKE (`ilike` case-insensitive); use `%` wildcards |
| `=like`, `=ilike` | Pattern without auto-wrapping |
| `in`, `not in` | Membership in a list of values |
| `child_of` | Hierarchy (categories, employees) |
| `any` / `not any` | Odoo 17+ relational subdomain (prefer docs for version) |

```python
# Available ebooks published after 2020
[
    ("is_available", "=", True),
    ("format", "=", "ebook"),
    ("date_published", ">=", "2020-01-01"),
]
```

**OR / NOT — PREFIX OPERATORS:**

```python
# name contains "clean" OR format is ebook
["|", ("name", "ilike", "clean"), ("format", "=", "ebook")]

# NOT available
["!", ("is_available", "=", True)]

# (A OR B) AND C
["&", "|", ("a", "=", 1), ("b", "=", 2), ("c", "=", 3)]
```

```text
Domain evaluation is prefix-based:

  ["|", A, B]           ->  A OR B
  ["&", A, B]           ->  A AND B   (AND is also the default between items)
  ["!", A]              ->  NOT A
  ["|", A, "&", B, C]   ->  A OR (B AND C)
```

Draw a stack when domains get nested. Off-by-one prefix mistakes are common interview and production bugs.

**RELATIONAL PATHS:**

```python
[("member_id.email", "ilike", "@example.com")]
[("author_ids.name", "ilike", "Fowler")]  # across M2M
```

Paths walk Many2one / One2many / Many2many the same way related fields do. Keep them short — deep paths (`a.b.c.d`) become slow and hard to index. If you filter on `member_id.email` often, consider a related stored field or search on `member_id` with a prior member search.

**EMPTY / FALSE GOTCHAS:**

```python
# Open loans: date_returned is not set
[("date_returned", "=", False)]
# Also works in many versions:
[("date_returned", "=", None)]
```

Do not write `[("date_returned", "!=", False)]` when you mean "has a return date" without checking null behavior — prefer positive conditions when you can.

## 15.8 NOT

A **domain** is a list of conditions prefix-polished into Polish notation for AND/OR/NOT. The simple form is a list of triples:

```python
[("field", "operator", value), ("other", "=", True)]
# Implicit AND between conditions
```

**COMMON OPERATORS:**

| Op | Meaning |
| -- | ------- |
| `=`, `!=` | Equality |
| `>`, `>=`, `<`, `<=` | Comparisons |
| `like`, `ilike` | SQL LIKE (`ilike` case-insensitive); use `%` wildcards |
| `=like`, `=ilike` | Pattern without auto-wrapping |
| `in`, `not in` | Membership in a list of values |
| `child_of` | Hierarchy (categories, employees) |
| `any` / `not any` | Odoo 17+ relational subdomain (prefer docs for version) |

```python
# Available ebooks published after 2020
[
    ("is_available", "=", True),
    ("format", "=", "ebook"),
    ("date_published", ">=", "2020-01-01"),
]
```

**OR / NOT — PREFIX OPERATORS:**

```python
# name contains "clean" OR format is ebook
["|", ("name", "ilike", "clean"), ("format", "=", "ebook")]

# NOT available
["!", ("is_available", "=", True)]

# (A OR B) AND C
["&", "|", ("a", "=", 1), ("b", "=", 2), ("c", "=", 3)]
```

```text
Domain evaluation is prefix-based:

  ["|", A, B]           ->  A OR B
  ["&", A, B]           ->  A AND B   (AND is also the default between items)
  ["!", A]              ->  NOT A
  ["|", A, "&", B, C]   ->  A OR (B AND C)
```

Draw a stack when domains get nested. Off-by-one prefix mistakes are common interview and production bugs.

**RELATIONAL PATHS:**

```python
[("member_id.email", "ilike", "@example.com")]
[("author_ids.name", "ilike", "Fowler")]  # across M2M
```

Paths walk Many2one / One2many / Many2many the same way related fields do. Keep them short — deep paths (`a.b.c.d`) become slow and hard to index. If you filter on `member_id.email` often, consider a related stored field or search on `member_id` with a prior member search.

**EMPTY / FALSE GOTCHAS:**

```python
# Open loans: date_returned is not set
[("date_returned", "=", False)]
# Also works in many versions:
[("date_returned", "=", None)]
```

Do not write `[("date_returned", "!=", False)]` when you mean "has a return date" without checking null behavior — prefer positive conditions when you can.

## 15.9 search_count()

```python
n = env["library.loan"].search_count([("date_returned", "=", False)])
rows = env["library.book"].search_read(
    [("list_price", ">", 40)],
    ["name", "list_price", "format"],
    limit=20,
    order="list_price desc",
)
```

`search_count` avoids building a huge recordset when you only need a number. `search_read` is efficient for returning dicts (RPC, dashboards) without dangling recordsets.

**WORKED TRANSLATIONS:**

```python
# 1) Members who joined this year
Member.search([("date_joined", ">=", "2026-01-01")])

# 2) Open loans
Loan.search([("date_returned", "=", False)])

# 3) Hardcover OR ebook, AND available
Book.search([
    "&",
    "|", ("format", "=", "hardcover"), ("format", "=", "ebook"),
    ("is_available", "=", True),
])

# 4) Count overdue (if is_overdue stored or searchable)
Loan.search_count([("is_overdue", "=", True)])

# 5) Top prices as dicts
Book.search_read(
    [("list_price", ">", 0)],
    ["name", "list_price"],
    order="list_price desc",
    limit=10,
)
```

**PRACTICE — BUSINESS QUERIES:**

Translate each into a domain + API call (write them before peeking at the snippets above):

1. Members who joined this year.
2. Loans still open (`date_returned` empty).
3. Books that are hardcover **or** ebook, and available.
4. Count of overdue loans (use your compute or a date comparison).
5. `search_read` top 10 books by price for a simple report.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write domains for equality, `ilike`, and `in` on books |
| Easy | Explain why `("date_returned", "=", False)` finds open loans |
| Medium | Build a domain with `|` and `&` that matches the hardcover/ebook + available rule |
| Medium | Compare `len(search(...))` vs `search_count` on a large set — which should you use? |
| Hard | PRACTICE set: five business questions above with working shell snippets |

**WHAT THIS UNLOCKS NEXT:**

Domains fetch the right rows. Phase 16 works *inside* the recordset — filter, map, sort, and batch — without round-tripping to SQL for every tiny step.


---

## 15.10 search_read()

```python
n = env["library.loan"].search_count([("date_returned", "=", False)])
rows = env["library.book"].search_read(
    [("list_price", ">", 40)],
    ["name", "list_price", "format"],
    limit=20,
    order="list_price desc",
)
```

`search_count` avoids building a huge recordset when you only need a number. `search_read` is efficient for returning dicts (RPC, dashboards) without dangling recordsets.

**WORKED TRANSLATIONS:**

```python
# 1) Members who joined this year
Member.search([("date_joined", ">=", "2026-01-01")])

# 2) Open loans
Loan.search([("date_returned", "=", False)])

# 3) Hardcover OR ebook, AND available
Book.search([
    "&",
    "|", ("format", "=", "hardcover"), ("format", "=", "ebook"),
    ("is_available", "=", True),
])

# 4) Count overdue (if is_overdue stored or searchable)
Loan.search_count([("is_overdue", "=", True)])

# 5) Top prices as dicts
Book.search_read(
    [("list_price", ">", 0)],
    ["name", "list_price"],
    order="list_price desc",
    limit=10,
)
```

**PRACTICE — BUSINESS QUERIES:**

Translate each into a domain + API call (write them before peeking at the snippets above):

1. Members who joined this year.
2. Loans still open (`date_returned` empty).
3. Books that are hardcover **or** ebook, and available.
4. Count of overdue loans (use your compute or a date comparison).
5. `search_read` top 10 books by price for a simple report.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write domains for equality, `ilike`, and `in` on books |
| Easy | Explain why `("date_returned", "=", False)` finds open loans |
| Medium | Build a domain with `|` and `&` that matches the hardcover/ebook + available rule |
| Medium | Compare `len(search(...))` vs `search_count` on a large set — which should you use? |
| Hard | PRACTICE set: five business questions above with working shell snippets |

**WHAT THIS UNLOCKS NEXT:**

Domains fetch the right rows. Phase 16 works *inside* the recordset — filter, map, sort, and batch — without round-tripping to SQL for every tiny step.


---

## 15.11 Ordering

**Ordering** belongs to **Domains & Searching**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Ordering** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Ordering** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Ordering**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Ordering** is concrete, continue — the next lesson in this phase still depends on it.

## 15.12 Limit / Offset

**Limit / Offset** belongs to **Domains & Searching**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Limit / Offset** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Limit / Offset** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Limit / Offset**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Limit / Offset** is concrete, continue — the next lesson in this phase still depends on it.

## 15.13 PRACTICE — Business Queries

```python
n = env["library.loan"].search_count([("date_returned", "=", False)])
rows = env["library.book"].search_read(
    [("list_price", ">", 40)],
    ["name", "list_price", "format"],
    limit=20,
    order="list_price desc",
)
```

`search_count` avoids building a huge recordset when you only need a number. `search_read` is efficient for returning dicts (RPC, dashboards) without dangling recordsets.

**WORKED TRANSLATIONS:**

```python
# 1) Members who joined this year
Member.search([("date_joined", ">=", "2026-01-01")])

# 2) Open loans
Loan.search([("date_returned", "=", False)])

# 3) Hardcover OR ebook, AND available
Book.search([
    "&",
    "|", ("format", "=", "hardcover"), ("format", "=", "ebook"),
    ("is_available", "=", True),
])

# 4) Count overdue (if is_overdue stored or searchable)
Loan.search_count([("is_overdue", "=", True)])

# 5) Top prices as dicts
Book.search_read(
    [("list_price", ">", 0)],
    ["name", "list_price"],
    order="list_price desc",
    limit=10,
)
```

**PRACTICE — BUSINESS QUERIES:**

Translate each into a domain + API call (write them before peeking at the snippets above):

1. Members who joined this year.
2. Loans still open (`date_returned` empty).
3. Books that are hardcover **or** ebook, and available.
4. Count of overdue loans (use your compute or a date comparison).
5. `search_read` top 10 books by price for a simple report.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write domains for equality, `ilike`, and `in` on books |
| Easy | Explain why `("date_returned", "=", False)` finds open loans |
| Medium | Build a domain with `|` and `&` that matches the hardcover/ebook + available rule |
| Medium | Compare `len(search(...))` vs `search_count` on a large set — which should you use? |
| Hard | PRACTICE set: five business questions above with working shell snippets |

**WHAT THIS UNLOCKS NEXT:**

Domains fetch the right rows. Phase 16 works *inside* the recordset — filter, map, sort, and batch — without round-tripping to SQL for every tiny step.


---


<a id="phase-16"></a>
<a id="phase-16---recordset-operations"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 15:** You can explain every lesson title in Phase 15 with a concrete Odoo or business example.

> **Phase 15 complete?** [Continue to Phase 16](#phase-16)


<a id="phase-16"></a>
# PHASE 16 - Recordset Operations

**Track:** ORM Mastery

**WHAT YOU WILL BE ABLE TO DO:** Use `filtered`, `mapped`, `sorted`, and `ensure_one` fluently, and apply batch-friendly patterns that avoid naive Python loops over ORM chatter.

**WHAT YOU SHOULD KNOW FIRST:** Phase 15 — search/domains; Phase 13 — recordsets.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Recordsets](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#recordsets)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [ORM video](https://www.youtube.com/watch?v=ASPjB-WowBU)
- [odoo/odoo](https://github.com/odoo/odoo)

## 16.1 Iteration

**Iteration** belongs to **Recordset Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Iteration** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Iteration** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Iteration**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Iteration** is concrete, continue — the next lesson in this phase still depends on it.

## 16.2 filtered()

Once you have a recordset, you often need a subset, a projection, or a stable order — in memory, after the SQL.

**FILTERED:**

```python
loans = env["library.loan"].search([])
open_loans = loans.filtered(lambda l: not l.date_returned)
# Or field path sugar when checking truthiness of a field:
# overdue = loans.filtered("is_overdue")
```

`filtered` does not hit SQL again; it evaluates in Python on the current cache. For huge sets, put the condition in the domain instead.

**MAPPED:**

```python
names = books.mapped("name")                 # list of values
members = loans.mapped("member_id")          # union recordset of members
emails = loans.mapped("member_id.email")     # list of emails
```

`mapped` on a Many2one returns a recordset (deduplicated). On a scalar, a list. This is the idiomatic way to gather related records without manual loops.

**SORTED:**

```python
books.sorted(key=lambda b: b.list_price or 0.0, reverse=True)
books.sorted("name")
```

**ENSURE_ONE:**

```python
def action_checkout(self):
    self.ensure_one()
    # safe to treat self as a single loan
```

Buttons and RPC often pass multi-recordsets by accident. `ensure_one()` fails loud instead of corrupting data quietly.

**SET OPERATIONS:**

```python
both = books_a | books_b      # union
common = books_a & books_b    # intersection
only_a = books_a - books_b    # difference
```

Recordset set ops are on **identity (ids)**, not on field equality. Two browse recordsets of the same ids collapse cleanly.

**FILTERED VERSUS DOMAIN — WHEN TO USE WHICH:**

```text
Need subset BEFORE loading thousands of rows?
  -> put the condition in search([...])  (SQL)

Already hold a moderate recordset and need a cheap Python predicate?
  -> filtered(lambda ...)

Need related records / scalars collected?
  -> mapped("path")
```

## 16.3 filtered_domain()

Once you have a recordset, you often need a subset, a projection, or a stable order — in memory, after the SQL.

**FILTERED:**

```python
loans = env["library.loan"].search([])
open_loans = loans.filtered(lambda l: not l.date_returned)
# Or field path sugar when checking truthiness of a field:
# overdue = loans.filtered("is_overdue")
```

`filtered` does not hit SQL again; it evaluates in Python on the current cache. For huge sets, put the condition in the domain instead.

**MAPPED:**

```python
names = books.mapped("name")                 # list of values
members = loans.mapped("member_id")          # union recordset of members
emails = loans.mapped("member_id.email")     # list of emails
```

`mapped` on a Many2one returns a recordset (deduplicated). On a scalar, a list. This is the idiomatic way to gather related records without manual loops.

**SORTED:**

```python
books.sorted(key=lambda b: b.list_price or 0.0, reverse=True)
books.sorted("name")
```

**ENSURE_ONE:**

```python
def action_checkout(self):
    self.ensure_one()
    # safe to treat self as a single loan
```

Buttons and RPC often pass multi-recordsets by accident. `ensure_one()` fails loud instead of corrupting data quietly.

**SET OPERATIONS:**

```python
both = books_a | books_b      # union
common = books_a & books_b    # intersection
only_a = books_a - books_b    # difference
```

Recordset set ops are on **identity (ids)**, not on field equality. Two browse recordsets of the same ids collapse cleanly.

**FILTERED VERSUS DOMAIN — WHEN TO USE WHICH:**

```text
Need subset BEFORE loading thousands of rows?
  -> put the condition in search([...])  (SQL)

Already hold a moderate recordset and need a cheap Python predicate?
  -> filtered(lambda ...)

Need related records / scalars collected?
  -> mapped("path")
```

## 16.4 mapped()

Once you have a recordset, you often need a subset, a projection, or a stable order — in memory, after the SQL.

**FILTERED:**

```python
loans = env["library.loan"].search([])
open_loans = loans.filtered(lambda l: not l.date_returned)
# Or field path sugar when checking truthiness of a field:
# overdue = loans.filtered("is_overdue")
```

`filtered` does not hit SQL again; it evaluates in Python on the current cache. For huge sets, put the condition in the domain instead.

**MAPPED:**

```python
names = books.mapped("name")                 # list of values
members = loans.mapped("member_id")          # union recordset of members
emails = loans.mapped("member_id.email")     # list of emails
```

`mapped` on a Many2one returns a recordset (deduplicated). On a scalar, a list. This is the idiomatic way to gather related records without manual loops.

**SORTED:**

```python
books.sorted(key=lambda b: b.list_price or 0.0, reverse=True)
books.sorted("name")
```

**ENSURE_ONE:**

```python
def action_checkout(self):
    self.ensure_one()
    # safe to treat self as a single loan
```

Buttons and RPC often pass multi-recordsets by accident. `ensure_one()` fails loud instead of corrupting data quietly.

**SET OPERATIONS:**

```python
both = books_a | books_b      # union
common = books_a & books_b    # intersection
only_a = books_a - books_b    # difference
```

Recordset set ops are on **identity (ids)**, not on field equality. Two browse recordsets of the same ids collapse cleanly.

**FILTERED VERSUS DOMAIN — WHEN TO USE WHICH:**

```text
Need subset BEFORE loading thousands of rows?
  -> put the condition in search([...])  (SQL)

Already hold a moderate recordset and need a cheap Python predicate?
  -> filtered(lambda ...)

Need related records / scalars collected?
  -> mapped("path")
```

## 16.5 sorted()

Once you have a recordset, you often need a subset, a projection, or a stable order — in memory, after the SQL.

**FILTERED:**

```python
loans = env["library.loan"].search([])
open_loans = loans.filtered(lambda l: not l.date_returned)
# Or field path sugar when checking truthiness of a field:
# overdue = loans.filtered("is_overdue")
```

`filtered` does not hit SQL again; it evaluates in Python on the current cache. For huge sets, put the condition in the domain instead.

**MAPPED:**

```python
names = books.mapped("name")                 # list of values
members = loans.mapped("member_id")          # union recordset of members
emails = loans.mapped("member_id.email")     # list of emails
```

`mapped` on a Many2one returns a recordset (deduplicated). On a scalar, a list. This is the idiomatic way to gather related records without manual loops.

**SORTED:**

```python
books.sorted(key=lambda b: b.list_price or 0.0, reverse=True)
books.sorted("name")
```

**ENSURE_ONE:**

```python
def action_checkout(self):
    self.ensure_one()
    # safe to treat self as a single loan
```

Buttons and RPC often pass multi-recordsets by accident. `ensure_one()` fails loud instead of corrupting data quietly.

**SET OPERATIONS:**

```python
both = books_a | books_b      # union
common = books_a & books_b    # intersection
only_a = books_a - books_b    # difference
```

Recordset set ops are on **identity (ids)**, not on field equality. Two browse recordsets of the same ids collapse cleanly.

**FILTERED VERSUS DOMAIN — WHEN TO USE WHICH:**

```text
Need subset BEFORE loading thousands of rows?
  -> put the condition in search([...])  (SQL)

Already hold a moderate recordset and need a cheap Python predicate?
  -> filtered(lambda ...)

Need related records / scalars collected?
  -> mapped("path")
```

## 16.6 ensure_one()

Once you have a recordset, you often need a subset, a projection, or a stable order — in memory, after the SQL.

**FILTERED:**

```python
loans = env["library.loan"].search([])
open_loans = loans.filtered(lambda l: not l.date_returned)
# Or field path sugar when checking truthiness of a field:
# overdue = loans.filtered("is_overdue")
```

`filtered` does not hit SQL again; it evaluates in Python on the current cache. For huge sets, put the condition in the domain instead.

**MAPPED:**

```python
names = books.mapped("name")                 # list of values
members = loans.mapped("member_id")          # union recordset of members
emails = loans.mapped("member_id.email")     # list of emails
```

`mapped` on a Many2one returns a recordset (deduplicated). On a scalar, a list. This is the idiomatic way to gather related records without manual loops.

**SORTED:**

```python
books.sorted(key=lambda b: b.list_price or 0.0, reverse=True)
books.sorted("name")
```

**ENSURE_ONE:**

```python
def action_checkout(self):
    self.ensure_one()
    # safe to treat self as a single loan
```

Buttons and RPC often pass multi-recordsets by accident. `ensure_one()` fails loud instead of corrupting data quietly.

**SET OPERATIONS:**

```python
both = books_a | books_b      # union
common = books_a & books_b    # intersection
only_a = books_a - books_b    # difference
```

Recordset set ops are on **identity (ids)**, not on field equality. Two browse recordsets of the same ids collapse cleanly.

**FILTERED VERSUS DOMAIN — WHEN TO USE WHICH:**

```text
Need subset BEFORE loading thousands of rows?
  -> put the condition in search([...])  (SQL)

Already hold a moderate recordset and need a cheap Python predicate?
  -> filtered(lambda ...)

Need related records / scalars collected?
  -> mapped("path")
```

## 16.7 Recordset Union

**Recordset Union** belongs to **Recordset Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Recordset Union** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Recordset Union** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Recordset Union**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Recordset Union** is concrete, continue — the next lesson in this phase still depends on it.

## 16.8 Recordset Intersection

**Recordset Intersection** belongs to **Recordset Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Recordset Intersection** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Recordset Intersection** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Recordset Intersection**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Recordset Intersection** is concrete, continue — the next lesson in this phase still depends on it.

## 16.9 Recordset Difference

**Recordset Difference** belongs to **Recordset Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Recordset Difference** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Recordset Difference** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Recordset Difference**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Recordset Difference** is concrete, continue — the next lesson in this phase still depends on it.

## 16.10 Batch Operations

Prefer recordset-wide writes and creates over per-record chatter:

```python
# Weaker — N writes
for book in books:
    book.is_available = False

# Stronger — one write
books.write({"is_available": False})
```

```python
# Weaker
for vals in list_of_vals:
    env["library.book"].create(vals)

# Stronger
env["library.book"].create(list_of_vals)
```

When each row needs different values, you may still loop — but gather reads first (prefetch helps; Phase 17) and avoid searching inside the loop.

```python
# BAD — search inside loop (classic N+1)
for loan in loans:
    member = env["library.member"].search([("id", "=", loan.member_id.id)])

# GOOD — use the relation already on the recordset
for loan in loans:
    member = loan.member_id
```

**EXISTS / INDEXING HELPERS:**

```python
if loans:
    ...
if book in books:           # membership by id
    ...
books[0]                    # first record (raises if empty — check first)
books[:10]                  # slice -> recordset
```

Slicing a recordset does not always mean "LIMIT was used in SQL" if the set was already loaded — prefer `search(..., limit=10)` when you only need ten rows from the database.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | From all loans, `filtered` open ones; `mapped` member names |
| Easy | Call a method without `ensure_one` on two records; then add it and show the error |
| Medium | Sort books by price; take the top 5 with slicing `books[:5]` after sort |
| Medium | Replace a Python loop of writes with one batch `write` |
| Hard | Refactor a messy method that searches in a loop into domain + `filtered`/`mapped` |

**WHAT THIS UNLOCKS NEXT:**

Batch style helps, but the ORM's prefetch cache, flush timing, and transactions decide whether production code stays fast. Phase 17 opens the hood.


---


<a id="phase-17"></a>
<a id="phase-17---orm-internals-performance"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 16:** You can explain every lesson title in Phase 16 with a concrete Odoo or business example.

> **Phase 16 complete?** [Continue to Phase 17](#phase-17)


<a id="phase-17"></a>
# PHASE 17 - ORM Internals & Performance

**Track:** ORM Mastery

**WHAT YOU WILL BE ABLE TO DO:** Explain prefetching and caching, reason about transactions and `flush`, diagnose N+1 query patterns, and optimize a slow recordset loop with evidence — not guesswork.

**WHAT YOU SHOULD KNOW FIRST:** Phase 16 — batch recordset operations; Phase 14 — CRUD.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Performance](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [Prefetching / cache notes in ORM docs](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#field-access)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [ORM video](https://www.youtube.com/watch?v=ASPjB-WowBU)
- [odoo/odoo](https://github.com/odoo/odoo)
- [odoo/tutorials](https://github.com/odoo/tutorials)

## 17.1 Prefetching

**PREFETCH:**

When you iterate a recordset and touch `book.name`, Odoo often fetches `name` for **all ids in the prefetch set** in one query — not one query per book. That is why batch recordsets are fast *when you stay on the same field set*.

```text
books = search(200 ids)
for book in books:
    print(book.name)     # first access: SELECT name FROM ... WHERE id IN (200 ids)
    print(book.isbn)     # may trigger another batched fetch for isbn
```

Jumping into a related recordset per iteration without care can still explode:

```python
for book in books:
    for author in book.author_ids:  # can be OK if prefetch works
        print(author.name)
```

Watch for searches or `browse` of unrelated ids inside loops — prefetch cannot save you there.

**CACHE:**

The environment cache stores field values already read. Assignments update the cache and mark fields to flush. Reading after write usually sees the new value even before SQL runs.

**TRANSACTIONS:**

One HTTP request / one cron run ≈ one database transaction (simplified). Commit happens at success end; exceptions roll back. You rarely call `commit()` in business code — framework owns it. In shell, you must `env.cr.commit()` to persist experiments.

**FLUSH:**

Pending writes sit in cache until **flush** sends them to PostgreSQL — before searches that must see them, before constraints, at commit.

```python
self.name = "New"
# SQL may not have run yet
self.flush_model()          # or env.flush_all() / flush_recordset
# Now SQL is visible to subsequent queries on this cursor
```

Usually the ORM flushes automatically when needed. Manual flush matters when mixing raw SQL (`self.env.cr.execute`) with ORM writes, or when debugging "I wrote it but search cannot find it yet."

## 17.2 ORM Cache

**N+1 PATTERN:**

```python
# 1 query for loans + N queries for members if prefetch broken / forced oddly
loans = env["library.loan"].search([], limit=500)
for loan in loans:
    # BAD example: re-search
    m = env["library.member"].search([("id", "=", loan.member_id.id)])
    print(m.name)
```

```python
# BETTER
loans = env["library.loan"].search([], limit=500)
loans.mapped("member_id.name")  # uses relations + prefetch
```

**HOW TO SEE QUERIES:**

Run with `--log-level=debug_sql` or enable logging on `odoo.sql_db` temporarily. Count statements before/after a refactor. Optimization without a query count is theater.

**LAB — OPTIMIZE ORM CODE:**

1. Write a deliberately slow method: for each loan, `search` the member by id and the book by id.
2. Log SQL count (or approximate with debug_sql).
3. Replace with `loan.member_id` / `loan.book_id` and a single starting `search`.
4. Record before/after query counts in a short note.

**STORY PROJECT — Data-Heavy Odoo Module**

Extend the library (or a sales sandbox) to include:

1. ≥ 200 demo records (books/loans) via XML or a setup method.
2. A report method that returns top borrowers / most borrowed books using domains + `mapped`/`sorted` (no HTML view required).
3. Evidence that you removed at least one N+1 (paste query counts or log snippets in README).
4. One stored compute that remains correct under batch `write`.

Story V ends when you can **move data in bulk without melting PostgreSQL**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain prefetch in your own words with a 3-step ASCII diagram |
| Easy | When do you need `flush` before raw SQL SELECT? |
| Medium | Find an N+1 in sample code and fix it |
| Medium | LAB steps 1–4 with written before/after counts |
| Hard | STORY PROJECT: Data-Heavy Odoo Module deliverable |

**WHAT THIS UNLOCKS NEXT:**

You can read and write data at scale — but businesses need *rules*: methods that enforce policy, constraints that block illegal states, onchanges that guide the UI, and workflows that move a document from draft to approved. Story VI begins with business methods.


---

## 17.3 Transactions

**PREFETCH:**

When you iterate a recordset and touch `book.name`, Odoo often fetches `name` for **all ids in the prefetch set** in one query — not one query per book. That is why batch recordsets are fast *when you stay on the same field set*.

```text
books = search(200 ids)
for book in books:
    print(book.name)     # first access: SELECT name FROM ... WHERE id IN (200 ids)
    print(book.isbn)     # may trigger another batched fetch for isbn
```

Jumping into a related recordset per iteration without care can still explode:

```python
for book in books:
    for author in book.author_ids:  # can be OK if prefetch works
        print(author.name)
```

Watch for searches or `browse` of unrelated ids inside loops — prefetch cannot save you there.

**CACHE:**

The environment cache stores field values already read. Assignments update the cache and mark fields to flush. Reading after write usually sees the new value even before SQL runs.

**TRANSACTIONS:**

One HTTP request / one cron run ≈ one database transaction (simplified). Commit happens at success end; exceptions roll back. You rarely call `commit()` in business code — framework owns it. In shell, you must `env.cr.commit()` to persist experiments.

**FLUSH:**

Pending writes sit in cache until **flush** sends them to PostgreSQL — before searches that must see them, before constraints, at commit.

```python
self.name = "New"
# SQL may not have run yet
self.flush_model()          # or env.flush_all() / flush_recordset
# Now SQL is visible to subsequent queries on this cursor
```

Usually the ORM flushes automatically when needed. Manual flush matters when mixing raw SQL (`self.env.cr.execute`) with ORM writes, or when debugging "I wrote it but search cannot find it yet."

## 17.4 Flush

**PREFETCH:**

When you iterate a recordset and touch `book.name`, Odoo often fetches `name` for **all ids in the prefetch set** in one query — not one query per book. That is why batch recordsets are fast *when you stay on the same field set*.

```text
books = search(200 ids)
for book in books:
    print(book.name)     # first access: SELECT name FROM ... WHERE id IN (200 ids)
    print(book.isbn)     # may trigger another batched fetch for isbn
```

Jumping into a related recordset per iteration without care can still explode:

```python
for book in books:
    for author in book.author_ids:  # can be OK if prefetch works
        print(author.name)
```

Watch for searches or `browse` of unrelated ids inside loops — prefetch cannot save you there.

**CACHE:**

The environment cache stores field values already read. Assignments update the cache and mark fields to flush. Reading after write usually sees the new value even before SQL runs.

**TRANSACTIONS:**

One HTTP request / one cron run ≈ one database transaction (simplified). Commit happens at success end; exceptions roll back. You rarely call `commit()` in business code — framework owns it. In shell, you must `env.cr.commit()` to persist experiments.

**FLUSH:**

Pending writes sit in cache until **flush** sends them to PostgreSQL — before searches that must see them, before constraints, at commit.

```python
self.name = "New"
# SQL may not have run yet
self.flush_model()          # or env.flush_all() / flush_recordset
# Now SQL is visible to subsequent queries on this cursor
```

Usually the ORM flushes automatically when needed. Manual flush matters when mixing raw SQL (`self.env.cr.execute`) with ORM writes, or when debugging "I wrote it but search cannot find it yet."

## 17.5 Cache Invalidation

**Cache Invalidation** belongs to **ORM Internals & Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Cache Invalidation** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Cache Invalidation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Cache Invalidation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Cache Invalidation** is concrete, continue — the next lesson in this phase still depends on it.

## 17.6 N+1 Queries

**N+1 PATTERN:**

```python
# 1 query for loans + N queries for members if prefetch broken / forced oddly
loans = env["library.loan"].search([], limit=500)
for loan in loans:
    # BAD example: re-search
    m = env["library.member"].search([("id", "=", loan.member_id.id)])
    print(m.name)
```

```python
# BETTER
loans = env["library.loan"].search([], limit=500)
loans.mapped("member_id.name")  # uses relations + prefetch
```

**HOW TO SEE QUERIES:**

Run with `--log-level=debug_sql` or enable logging on `odoo.sql_db` temporarily. Count statements before/after a refactor. Optimization without a query count is theater.

**LAB — OPTIMIZE ORM CODE:**

1. Write a deliberately slow method: for each loan, `search` the member by id and the book by id.
2. Log SQL count (or approximate with debug_sql).
3. Replace with `loan.member_id` / `loan.book_id` and a single starting `search`.
4. Record before/after query counts in a short note.

**STORY PROJECT — Data-Heavy Odoo Module**

Extend the library (or a sales sandbox) to include:

1. ≥ 200 demo records (books/loans) via XML or a setup method.
2. A report method that returns top borrowers / most borrowed books using domains + `mapped`/`sorted` (no HTML view required).
3. Evidence that you removed at least one N+1 (paste query counts or log snippets in README).
4. One stored compute that remains correct under batch `write`.

Story V ends when you can **move data in bulk without melting PostgreSQL**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain prefetch in your own words with a 3-step ASCII diagram |
| Easy | When do you need `flush` before raw SQL SELECT? |
| Medium | Find an N+1 in sample code and fix it |
| Medium | LAB steps 1–4 with written before/after counts |
| Hard | STORY PROJECT: Data-Heavy Odoo Module deliverable |

**WHAT THIS UNLOCKS NEXT:**

You can read and write data at scale — but businesses need *rules*: methods that enforce policy, constraints that block illegal states, onchanges that guide the UI, and workflows that move a document from draft to approved. Story VI begins with business methods.


---

## 17.7 Batch Creation

**Batch Creation** belongs to **ORM Internals & Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Batch Creation** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Batch Creation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Batch Creation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Batch Creation** is concrete, continue — the next lesson in this phase still depends on it.

## 17.8 Aggregation

**Aggregation** belongs to **ORM Internals & Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Aggregation** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Aggregation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Aggregation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Aggregation** is concrete, continue — the next lesson in this phase still depends on it.

## 17.9 read_group()

`read_group()` is an ORM entry point on a recordset (or environment). Call it with the documented signature from [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html).

Practice on a tiny recordset: print `self`, check empty vs singleton, and avoid Python loops that hide N+1 queries. Overrides must call `super()` unless you can prove you replaced the contract.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **read_group()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **read_group()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **read_group()** is concrete, continue — the next lesson in this phase still depends on it.

## 17.10 Raw SQL

**Raw SQL** belongs to **ORM Internals & Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Raw SQL** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Raw SQL** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Raw SQL**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Raw SQL** is concrete, continue — the next lesson in this phase still depends on it.

## 17.11 ORM vs SQL

**N+1 PATTERN:**

```python
# 1 query for loans + N queries for members if prefetch broken / forced oddly
loans = env["library.loan"].search([], limit=500)
for loan in loans:
    # BAD example: re-search
    m = env["library.member"].search([("id", "=", loan.member_id.id)])
    print(m.name)
```

```python
# BETTER
loans = env["library.loan"].search([], limit=500)
loans.mapped("member_id.name")  # uses relations + prefetch
```

**HOW TO SEE QUERIES:**

Run with `--log-level=debug_sql` or enable logging on `odoo.sql_db` temporarily. Count statements before/after a refactor. Optimization without a query count is theater.

**LAB — OPTIMIZE ORM CODE:**

1. Write a deliberately slow method: for each loan, `search` the member by id and the book by id.
2. Log SQL count (or approximate with debug_sql).
3. Replace with `loan.member_id` / `loan.book_id` and a single starting `search`.
4. Record before/after query counts in a short note.

**STORY PROJECT — Data-Heavy Odoo Module**

Extend the library (or a sales sandbox) to include:

1. ≥ 200 demo records (books/loans) via XML or a setup method.
2. A report method that returns top borrowers / most borrowed books using domains + `mapped`/`sorted` (no HTML view required).
3. Evidence that you removed at least one N+1 (paste query counts or log snippets in README).
4. One stored compute that remains correct under batch `write`.

Story V ends when you can **move data in bulk without melting PostgreSQL**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain prefetch in your own words with a 3-step ASCII diagram |
| Easy | When do you need `flush` before raw SQL SELECT? |
| Medium | Find an N+1 in sample code and fix it |
| Medium | LAB steps 1–4 with written before/after counts |
| Hard | STORY PROJECT: Data-Heavy Odoo Module deliverable |

**WHAT THIS UNLOCKS NEXT:**

You can read and write data at scale — but businesses need *rules*: methods that enforce policy, constraints that block illegal states, onchanges that guide the UI, and workflows that move a document from draft to approved. Story VI begins with business methods.


---

## 17.12 Security Implications of SQL

**Security Implications of SQL** belongs to **ORM Internals & Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Security Implications of SQL** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Security Implications of SQL** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Security Implications of SQL**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Security Implications of SQL** is concrete, continue — the next lesson in this phase still depends on it.

## 17.13 LAB — Optimize ORM Code

**N+1 PATTERN:**

```python
# 1 query for loans + N queries for members if prefetch broken / forced oddly
loans = env["library.loan"].search([], limit=500)
for loan in loans:
    # BAD example: re-search
    m = env["library.member"].search([("id", "=", loan.member_id.id)])
    print(m.name)
```

```python
# BETTER
loans = env["library.loan"].search([], limit=500)
loans.mapped("member_id.name")  # uses relations + prefetch
```

**HOW TO SEE QUERIES:**

Run with `--log-level=debug_sql` or enable logging on `odoo.sql_db` temporarily. Count statements before/after a refactor. Optimization without a query count is theater.

**LAB — OPTIMIZE ORM CODE:**

1. Write a deliberately slow method: for each loan, `search` the member by id and the book by id.
2. Log SQL count (or approximate with debug_sql).
3. Replace with `loan.member_id` / `loan.book_id` and a single starting `search`.
4. Record before/after query counts in a short note.

**STORY PROJECT — Data-Heavy Odoo Module**

Extend the library (or a sales sandbox) to include:

1. ≥ 200 demo records (books/loans) via XML or a setup method.
2. A report method that returns top borrowers / most borrowed books using domains + `mapped`/`sorted` (no HTML view required).
3. Evidence that you removed at least one N+1 (paste query counts or log snippets in README).
4. One stored compute that remains correct under batch `write`.

Story V ends when you can **move data in bulk without melting PostgreSQL**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain prefetch in your own words with a 3-step ASCII diagram |
| Easy | When do you need `flush` before raw SQL SELECT? |
| Medium | Find an N+1 in sample code and fix it |
| Medium | LAB steps 1–4 with written before/after counts |
| Hard | STORY PROJECT: Data-Heavy Odoo Module deliverable |

**WHAT THIS UNLOCKS NEXT:**

You can read and write data at scale — but businesses need *rules*: methods that enforce policy, constraints that block illegal states, onchanges that guide the UI, and workflows that move a document from draft to approved. Story VI begins with business methods.


---


---


**CHECK YOUR UNDERSTANDING AFTER PHASE 17:** You can explain every lesson title in Phase 17 with a concrete Odoo or business example.

> **Phase 17 complete?** [Continue to Phase 18](#phase-18)


## STORY PROJECT — Data-Heavy Odoo Module

**Track:** ORM Mastery

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY VI — BUSINESS RULES & WORKFLOWS

**Track:** Business Logic

*Business software enforces what is legal — not only what is possible.*

---
<a id="phase-18"></a>
<a id="phase-18---business-methods"></a>
# PHASE 18 - Business Methods

**Track:** Business Logic

**WHAT YOU WILL BE ABLE TO DO:** Write model methods for business actions, override `create`/`write` with correct `super()` usage, and expose actions that transition records the way a librarian or sales manager expects.

**WHAT YOU SHOULD KNOW FIRST:** Story V — env, CRUD, domains, recordsets. Story IV — models/fields.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Model methods](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [Override create/write patterns](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#model-reference)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [ORM / backend video](https://www.youtube.com/watch?v=ASPjB-WowBU)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [odoo/odoo](https://github.com/odoo/odoo)

## 18.1 Model Methods

Not everything belongs in a compute. **Business methods** encode verbs: confirm, approve, return book, cancel. They are where you check preconditions, change several fields, talk to other models, and return UI actions.

```python
from odoo import models
from odoo.exceptions import UserError


class LibraryLoan(models.Model):
    _name = "library.loan"
    ...

    def action_return_book(self):
        for loan in self:
            if loan.date_returned:
                raise UserError("This loan is already returned.")
            loan.date_returned = fields.Datetime.now()
            loan.book_id.is_available = True
        return True
```

Name UI-triggered methods with `action_` by convention. They can return `False`/`True` or an `ir.actions` dict (open a wizard, reload, notify). Views will bind buttons later; for now call them from shell/tests.

**KEEP METHODS BATCH-SAFE:**

Assume `self` may hold many records. Either loop with clear per-record rules or write in batches when values match.

## 18.2 Recordset Methods

Not everything belongs in a compute. **Business methods** encode verbs: confirm, approve, return book, cancel. They are where you check preconditions, change several fields, talk to other models, and return UI actions.

```python
from odoo import models
from odoo.exceptions import UserError


class LibraryLoan(models.Model):
    _name = "library.loan"
    ...

    def action_return_book(self):
        for loan in self:
            if loan.date_returned:
                raise UserError("This loan is already returned.")
            loan.date_returned = fields.Datetime.now()
            loan.book_id.is_available = True
        return True
```

Name UI-triggered methods with `action_` by convention. They can return `False`/`True` or an `ir.actions` dict (open a wizard, reload, notify). Views will bind buttons later; for now call them from shell/tests.

**KEEP METHODS BATCH-SAFE:**

Assume `self` may hold many records. Either loop with clear per-record rules or write in batches when values match.

## 18.3 @api.model

Not everything belongs in a compute. **Business methods** encode verbs: confirm, approve, return book, cancel. They are where you check preconditions, change several fields, talk to other models, and return UI actions.

```python
from odoo import models
from odoo.exceptions import UserError


class LibraryLoan(models.Model):
    _name = "library.loan"
    ...

    def action_return_book(self):
        for loan in self:
            if loan.date_returned:
                raise UserError("This loan is already returned.")
            loan.date_returned = fields.Datetime.now()
            loan.book_id.is_available = True
        return True
```

Name UI-triggered methods with `action_` by convention. They can return `False`/`True` or an `ir.actions` dict (open a wizard, reload, notify). Views will bind buttons later; for now call them from shell/tests.

**KEEP METHODS BATCH-SAFE:**

Assume `self` may hold many records. Either loop with clear per-record rules or write in batches when values match.

## 18.4 @api.model_create_multi

**@api.model_create_multi** belongs to **Business Methods**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **@api.model_create_multi** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **@api.model_create_multi** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **@api.model_create_multi**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **@api.model_create_multi** is concrete, continue — the next lesson in this phase still depends on it.

## 18.5 create() Override

Overrides inject policy into every insert/update — including imports, RPC, and UI saves.

```python
from odoo import api, models
from odoo.exceptions import ValidationError


class LibraryBook(models.Model):
    _name = "library.book"
    ...

    @api.model_create_multi
    def create(self, vals_list):
        for vals in vals_list:
            if vals.get("isbn"):
                vals["isbn"] = vals["isbn"].replace("-", "").strip()
        books = super().create(vals_list)
        # post-create side effects on recordset `books`
        return books

    def write(self, vals):
        if "isbn" in vals and vals["isbn"]:
            vals = dict(vals)
            vals["isbn"] = vals["isbn"].replace("-", "").strip()
        res = super().write(vals)
        return res
```

**RULES:**

1. Prefer `@api.model_create_multi` (Odoo 18) so batch create stays batch.
2. Always call `super()` — skipping it drops base behavior (mail tracking, parent logic).
3. Put *validation that depends on final field combinations* in `@api.constrains` (Phase 19), not only in write — constrains also run when related paths require it.
4. Avoid heavy remote calls inside `write` without care — every small UI edit pays the cost.

```text
UI Save
  -> write(vals)
       -> your pre-processing
       -> super().write(vals)
            -> ORM updates cache / SQL
            -> constrains / recompute
       -> your post-processing
```

**CREATE VERSUS ACTION METHODS:**

| Hook | Fires when | Good for |
| ---- | ---------- | -------- |
| `create` / `write` | Any insert/update path | Normalization, light defaults, audit stamps |
| `action_*` | Explicit button / code call | Confirm, approve, return — intentional verbs |
| `unlink` override | Deletion | Block delete of posted docs; prefer cancel |

If confirming an order must also reserve stock, put that in `action_confirm`, not in every `write` of `state` — otherwise imports and tests surprise you.

**WORKED ACTION — CHECKOUT:**

```python
def action_checkout(self):
    self.ensure_one()
    if not self.book_id.is_available:
        raise UserError("Book is already on loan.")
    self.book_id.is_available = False
    return {
        "type": "ir.actions.client",
        "tag": "display_notification",
        "params": {
            "title": "Checked out",
            "message": f"{self.book_id.display_name} loaned to {self.member_id.display_name}",
            "type": "success",
        },
    }
```

Returning a client action is optional; returning `True` is enough until views exist.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `action_make_available` on books that sets Boolean and works on multi `self` |
| Easy | Override `create` to default `format` if missing — with `super()` |
| Medium | Normalize ISBN in both `create` and `write` without duplicating logic (extract helper) |
| Medium | Explain why skipping `super()` breaks inheritance chains |
| Hard | Business action `action_checkout` that creates a loan, marks book unavailable, and refuses if book is already out |

**WHAT THIS UNLOCKS NEXT:**

Methods can refuse with `UserError`, but data integrity also needs declarative constraints. Phase 19 adds `@api.constrains`, SQL constraints, and the right exception types.


---

## 18.6 write() Override

Overrides inject policy into every insert/update — including imports, RPC, and UI saves.

```python
from odoo import api, models
from odoo.exceptions import ValidationError


class LibraryBook(models.Model):
    _name = "library.book"
    ...

    @api.model_create_multi
    def create(self, vals_list):
        for vals in vals_list:
            if vals.get("isbn"):
                vals["isbn"] = vals["isbn"].replace("-", "").strip()
        books = super().create(vals_list)
        # post-create side effects on recordset `books`
        return books

    def write(self, vals):
        if "isbn" in vals and vals["isbn"]:
            vals = dict(vals)
            vals["isbn"] = vals["isbn"].replace("-", "").strip()
        res = super().write(vals)
        return res
```

**RULES:**

1. Prefer `@api.model_create_multi` (Odoo 18) so batch create stays batch.
2. Always call `super()` — skipping it drops base behavior (mail tracking, parent logic).
3. Put *validation that depends on final field combinations* in `@api.constrains` (Phase 19), not only in write — constrains also run when related paths require it.
4. Avoid heavy remote calls inside `write` without care — every small UI edit pays the cost.

```text
UI Save
  -> write(vals)
       -> your pre-processing
       -> super().write(vals)
            -> ORM updates cache / SQL
            -> constrains / recompute
       -> your post-processing
```

**CREATE VERSUS ACTION METHODS:**

| Hook | Fires when | Good for |
| ---- | ---------- | -------- |
| `create` / `write` | Any insert/update path | Normalization, light defaults, audit stamps |
| `action_*` | Explicit button / code call | Confirm, approve, return — intentional verbs |
| `unlink` override | Deletion | Block delete of posted docs; prefer cancel |

If confirming an order must also reserve stock, put that in `action_confirm`, not in every `write` of `state` — otherwise imports and tests surprise you.

**WORKED ACTION — CHECKOUT:**

```python
def action_checkout(self):
    self.ensure_one()
    if not self.book_id.is_available:
        raise UserError("Book is already on loan.")
    self.book_id.is_available = False
    return {
        "type": "ir.actions.client",
        "tag": "display_notification",
        "params": {
            "title": "Checked out",
            "message": f"{self.book_id.display_name} loaned to {self.member_id.display_name}",
            "type": "success",
        },
    }
```

Returning a client action is optional; returning `True` is enough until views exist.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `action_make_available` on books that sets Boolean and works on multi `self` |
| Easy | Override `create` to default `format` if missing — with `super()` |
| Medium | Normalize ISBN in both `create` and `write` without duplicating logic (extract helper) |
| Medium | Explain why skipping `super()` breaks inheritance chains |
| Hard | Business action `action_checkout` that creates a loan, marks book unavailable, and refuses if book is already out |

**WHAT THIS UNLOCKS NEXT:**

Methods can refuse with `UserError`, but data integrity also needs declarative constraints. Phase 19 adds `@api.constrains`, SQL constraints, and the right exception types.


---

## 18.7 unlink() Override

**unlink() Override** belongs to **Business Methods**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **unlink() Override** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **unlink() Override** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **unlink() Override**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **unlink() Override** is concrete, continue — the next lesson in this phase still depends on it.

## 18.8 super()

Overrides inject policy into every insert/update — including imports, RPC, and UI saves.

```python
from odoo import api, models
from odoo.exceptions import ValidationError


class LibraryBook(models.Model):
    _name = "library.book"
    ...

    @api.model_create_multi
    def create(self, vals_list):
        for vals in vals_list:
            if vals.get("isbn"):
                vals["isbn"] = vals["isbn"].replace("-", "").strip()
        books = super().create(vals_list)
        # post-create side effects on recordset `books`
        return books

    def write(self, vals):
        if "isbn" in vals and vals["isbn"]:
            vals = dict(vals)
            vals["isbn"] = vals["isbn"].replace("-", "").strip()
        res = super().write(vals)
        return res
```

**RULES:**

1. Prefer `@api.model_create_multi` (Odoo 18) so batch create stays batch.
2. Always call `super()` — skipping it drops base behavior (mail tracking, parent logic).
3. Put *validation that depends on final field combinations* in `@api.constrains` (Phase 19), not only in write — constrains also run when related paths require it.
4. Avoid heavy remote calls inside `write` without care — every small UI edit pays the cost.

```text
UI Save
  -> write(vals)
       -> your pre-processing
       -> super().write(vals)
            -> ORM updates cache / SQL
            -> constrains / recompute
       -> your post-processing
```

**CREATE VERSUS ACTION METHODS:**

| Hook | Fires when | Good for |
| ---- | ---------- | -------- |
| `create` / `write` | Any insert/update path | Normalization, light defaults, audit stamps |
| `action_*` | Explicit button / code call | Confirm, approve, return — intentional verbs |
| `unlink` override | Deletion | Block delete of posted docs; prefer cancel |

If confirming an order must also reserve stock, put that in `action_confirm`, not in every `write` of `state` — otherwise imports and tests surprise you.

**WORKED ACTION — CHECKOUT:**

```python
def action_checkout(self):
    self.ensure_one()
    if not self.book_id.is_available:
        raise UserError("Book is already on loan.")
    self.book_id.is_available = False
    return {
        "type": "ir.actions.client",
        "tag": "display_notification",
        "params": {
            "title": "Checked out",
            "message": f"{self.book_id.display_name} loaned to {self.member_id.display_name}",
            "type": "success",
        },
    }
```

Returning a client action is optional; returning `True` is enough until views exist.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `action_make_available` on books that sets Boolean and works on multi `self` |
| Easy | Override `create` to default `format` if missing — with `super()` |
| Medium | Normalize ISBN in both `create` and `write` without duplicating logic (extract helper) |
| Medium | Explain why skipping `super()` breaks inheritance chains |
| Hard | Business action `action_checkout` that creates a loan, marks book unavailable, and refuses if book is already out |

**WHAT THIS UNLOCKS NEXT:**

Methods can refuse with `UserError`, but data integrity also needs declarative constraints. Phase 19 adds `@api.constrains`, SQL constraints, and the right exception types.


---

## 18.9 Return Values

**Return Values** belongs to **Business Methods**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Return Values** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Return Values** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Return Values**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Return Values** is concrete, continue — the next lesson in this phase still depends on it.

## 18.10 Business Actions

Not everything belongs in a compute. **Business methods** encode verbs: confirm, approve, return book, cancel. They are where you check preconditions, change several fields, talk to other models, and return UI actions.

```python
from odoo import models
from odoo.exceptions import UserError


class LibraryLoan(models.Model):
    _name = "library.loan"
    ...

    def action_return_book(self):
        for loan in self:
            if loan.date_returned:
                raise UserError("This loan is already returned.")
            loan.date_returned = fields.Datetime.now()
            loan.book_id.is_available = True
        return True
```

Name UI-triggered methods with `action_` by convention. They can return `False`/`True` or an `ir.actions` dict (open a wizard, reload, notify). Views will bind buttons later; for now call them from shell/tests.

**KEEP METHODS BATCH-SAFE:**

Assume `self` may hold many records. Either loop with clear per-record rules or write in batches when values match.


<a id="phase-19"></a>
<a id="phase-19---constraints-validation"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 18:** You can explain every lesson title in Phase 18 with a concrete Odoo or business example.

> **Phase 18 complete?** [Continue to Phase 19](#phase-19)


<a id="phase-19"></a>
# PHASE 19 - Constraints & Validation

**Track:** Business Logic

**WHAT YOU WILL BE ABLE TO DO:** Protect business invariants with `@api.constrains` and SQL constraints, raise `ValidationError` vs `UserError` appropriately, and lab-test rules that block illegal library or leave-request data.

**WHAT YOU SHOULD KNOW FIRST:** Phase 18 — business methods and create/write overrides.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — Constraints](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#constraints-and-indexes)
- [Exceptions](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [odoo/odoo](https://github.com/odoo/odoo)

## 19.1 Python Constraints

**PYTHON CONSTRAINTS — MULTI-FIELD BUSINESS RULES:**

```python
from odoo import api, models
from odoo.exceptions import ValidationError


class LibraryLoan(models.Model):
    _name = "library.loan"
    ...

    @api.constrains("date_borrowed", "date_returned")
    def _check_return_after_borrow(self):
        for loan in self:
            if loan.date_returned and loan.date_borrowed and loan.date_returned < loan.date_borrowed:
                raise ValidationError("Return date cannot be before borrow date.")
```

`@api.constrains("field", ...)` runs when those fields change (and on create). Always loop `self`. Raise **ValidationError** for data that must never be stored.

**SQL CONSTRAINTS — DATABASE-ENFORCED UNIQUENESS / CHECKS:**

```python
_sql_constraints = [
    ("isbn_unique", "unique(isbn)", "ISBN must be unique."),
    ("price_positive", "CHECK(list_price >= 0)", "Price cannot be negative."),
]
```

SQL constraints catch what Python might miss (raw SQL, races). They are blunt and fast. Use both layers: SQL for simple uniqueness/checks; Python for multi-table logic.

**VALIDATIONERROR VS USERERROR:**

| Exception | Use |
| --------- | --- |
| `ValidationError` | Illegal data / invariant broken (constrains, field integrity) |
| `UserError` | Business process refusal ("You cannot approve your own request") — often from buttons |
| `AccessError` | ACL / record rules (usually raised by ORM) |

Users see both as blocking dialogs; developers and tests distinguish intent.

**CONSTRAINS VERSUS CREATE/WRITE GUARDS:**

```text
create/write override
  - Good for normalizing values early
  - Easy to forget alternate write paths

@api.constrains
  - Runs when listed fields change
  - Central place for "this data must never exist"

_sql_constraints
  - Enforced by PostgreSQL for every writer
  - Best for unique(...) and simple CHECK(...)
```

Use all three deliberately: normalize in create/write, express multi-field truth in constrains, nail uniqueness in SQL.

## 19.2 @api.constrains

**PYTHON CONSTRAINTS — MULTI-FIELD BUSINESS RULES:**

```python
from odoo import api, models
from odoo.exceptions import ValidationError


class LibraryLoan(models.Model):
    _name = "library.loan"
    ...

    @api.constrains("date_borrowed", "date_returned")
    def _check_return_after_borrow(self):
        for loan in self:
            if loan.date_returned and loan.date_borrowed and loan.date_returned < loan.date_borrowed:
                raise ValidationError("Return date cannot be before borrow date.")
```

`@api.constrains("field", ...)` runs when those fields change (and on create). Always loop `self`. Raise **ValidationError** for data that must never be stored.

**SQL CONSTRAINTS — DATABASE-ENFORCED UNIQUENESS / CHECKS:**

```python
_sql_constraints = [
    ("isbn_unique", "unique(isbn)", "ISBN must be unique."),
    ("price_positive", "CHECK(list_price >= 0)", "Price cannot be negative."),
]
```

SQL constraints catch what Python might miss (raw SQL, races). They are blunt and fast. Use both layers: SQL for simple uniqueness/checks; Python for multi-table logic.

**VALIDATIONERROR VS USERERROR:**

| Exception | Use |
| --------- | --- |
| `ValidationError` | Illegal data / invariant broken (constrains, field integrity) |
| `UserError` | Business process refusal ("You cannot approve your own request") — often from buttons |
| `AccessError` | ACL / record rules (usually raised by ORM) |

Users see both as blocking dialogs; developers and tests distinguish intent.

**CONSTRAINS VERSUS CREATE/WRITE GUARDS:**

```text
create/write override
  - Good for normalizing values early
  - Easy to forget alternate write paths

@api.constrains
  - Runs when listed fields change
  - Central place for "this data must never exist"

_sql_constraints
  - Enforced by PostgreSQL for every writer
  - Best for unique(...) and simple CHECK(...)
```

Use all three deliberately: normalize in create/write, express multi-field truth in constrains, nail uniqueness in SQL.

## 19.3 SQL Constraints

**PYTHON CONSTRAINTS — MULTI-FIELD BUSINESS RULES:**

```python
from odoo import api, models
from odoo.exceptions import ValidationError


class LibraryLoan(models.Model):
    _name = "library.loan"
    ...

    @api.constrains("date_borrowed", "date_returned")
    def _check_return_after_borrow(self):
        for loan in self:
            if loan.date_returned and loan.date_borrowed and loan.date_returned < loan.date_borrowed:
                raise ValidationError("Return date cannot be before borrow date.")
```

`@api.constrains("field", ...)` runs when those fields change (and on create). Always loop `self`. Raise **ValidationError** for data that must never be stored.

**SQL CONSTRAINTS — DATABASE-ENFORCED UNIQUENESS / CHECKS:**

```python
_sql_constraints = [
    ("isbn_unique", "unique(isbn)", "ISBN must be unique."),
    ("price_positive", "CHECK(list_price >= 0)", "Price cannot be negative."),
]
```

SQL constraints catch what Python might miss (raw SQL, races). They are blunt and fast. Use both layers: SQL for simple uniqueness/checks; Python for multi-table logic.

**VALIDATIONERROR VS USERERROR:**

| Exception | Use |
| --------- | --- |
| `ValidationError` | Illegal data / invariant broken (constrains, field integrity) |
| `UserError` | Business process refusal ("You cannot approve your own request") — often from buttons |
| `AccessError` | ACL / record rules (usually raised by ORM) |

Users see both as blocking dialogs; developers and tests distinguish intent.

**CONSTRAINS VERSUS CREATE/WRITE GUARDS:**

```text
create/write override
  - Good for normalizing values early
  - Easy to forget alternate write paths

@api.constrains
  - Runs when listed fields change
  - Central place for "this data must never exist"

_sql_constraints
  - Enforced by PostgreSQL for every writer
  - Best for unique(...) and simple CHECK(...)
```

Use all three deliberately: normalize in create/write, express multi-field truth in constrains, nail uniqueness in SQL.

## 19.4 ValidationError

**PYTHON CONSTRAINTS — MULTI-FIELD BUSINESS RULES:**

```python
from odoo import api, models
from odoo.exceptions import ValidationError


class LibraryLoan(models.Model):
    _name = "library.loan"
    ...

    @api.constrains("date_borrowed", "date_returned")
    def _check_return_after_borrow(self):
        for loan in self:
            if loan.date_returned and loan.date_borrowed and loan.date_returned < loan.date_borrowed:
                raise ValidationError("Return date cannot be before borrow date.")
```

`@api.constrains("field", ...)` runs when those fields change (and on create). Always loop `self`. Raise **ValidationError** for data that must never be stored.

**SQL CONSTRAINTS — DATABASE-ENFORCED UNIQUENESS / CHECKS:**

```python
_sql_constraints = [
    ("isbn_unique", "unique(isbn)", "ISBN must be unique."),
    ("price_positive", "CHECK(list_price >= 0)", "Price cannot be negative."),
]
```

SQL constraints catch what Python might miss (raw SQL, races). They are blunt and fast. Use both layers: SQL for simple uniqueness/checks; Python for multi-table logic.

**VALIDATIONERROR VS USERERROR:**

| Exception | Use |
| --------- | --- |
| `ValidationError` | Illegal data / invariant broken (constrains, field integrity) |
| `UserError` | Business process refusal ("You cannot approve your own request") — often from buttons |
| `AccessError` | ACL / record rules (usually raised by ORM) |

Users see both as blocking dialogs; developers and tests distinguish intent.

**CONSTRAINS VERSUS CREATE/WRITE GUARDS:**

```text
create/write override
  - Good for normalizing values early
  - Easy to forget alternate write paths

@api.constrains
  - Runs when listed fields change
  - Central place for "this data must never exist"

_sql_constraints
  - Enforced by PostgreSQL for every writer
  - Best for unique(...) and simple CHECK(...)
```

Use all three deliberately: normalize in create/write, express multi-field truth in constrains, nail uniqueness in SQL.

## 19.5 UserError

**PYTHON CONSTRAINTS — MULTI-FIELD BUSINESS RULES:**

```python
from odoo import api, models
from odoo.exceptions import ValidationError


class LibraryLoan(models.Model):
    _name = "library.loan"
    ...

    @api.constrains("date_borrowed", "date_returned")
    def _check_return_after_borrow(self):
        for loan in self:
            if loan.date_returned and loan.date_borrowed and loan.date_returned < loan.date_borrowed:
                raise ValidationError("Return date cannot be before borrow date.")
```

`@api.constrains("field", ...)` runs when those fields change (and on create). Always loop `self`. Raise **ValidationError** for data that must never be stored.

**SQL CONSTRAINTS — DATABASE-ENFORCED UNIQUENESS / CHECKS:**

```python
_sql_constraints = [
    ("isbn_unique", "unique(isbn)", "ISBN must be unique."),
    ("price_positive", "CHECK(list_price >= 0)", "Price cannot be negative."),
]
```

SQL constraints catch what Python might miss (raw SQL, races). They are blunt and fast. Use both layers: SQL for simple uniqueness/checks; Python for multi-table logic.

**VALIDATIONERROR VS USERERROR:**

| Exception | Use |
| --------- | --- |
| `ValidationError` | Illegal data / invariant broken (constrains, field integrity) |
| `UserError` | Business process refusal ("You cannot approve your own request") — often from buttons |
| `AccessError` | ACL / record rules (usually raised by ORM) |

Users see both as blocking dialogs; developers and tests distinguish intent.

**CONSTRAINS VERSUS CREATE/WRITE GUARDS:**

```text
create/write override
  - Good for normalizing values early
  - Easy to forget alternate write paths

@api.constrains
  - Runs when listed fields change
  - Central place for "this data must never exist"

_sql_constraints
  - Enforced by PostgreSQL for every writer
  - Best for unique(...) and simple CHECK(...)
```

Use all three deliberately: normalize in create/write, express multi-field truth in constrains, nail uniqueness in SQL.

## 19.6 AccessError

**AccessError** belongs to **Constraints & Validation**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **AccessError** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **AccessError** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **AccessError**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **AccessError** is concrete, continue — the next lesson in this phase still depends on it.

## 19.7 LAB — Protect Business Rules

Implement and prove (shell or unit test):

1. Unique ISBN on books (`_sql_constraints`).
2. Loan return ≥ borrow (`@api.constrains`).
3. Cannot create a loan for an unavailable book (constrain or `create` override + ValidationError).
4. Leave-request style rule: `date_to >= date_from` on a practice model.

```python
@api.constrains("book_id", "date_returned")
def _check_book_available(self):
    for loan in self:
        if loan.date_returned:
            continue  # historical closed loans stay valid
        if loan.book_id and not loan.book_id.is_available:
            raise ValidationError("Book is not available for a new loan.")
```

Think about edge cases: editing an old returned loan should not spuriously fail; constrain logic must read state carefully.

**TEST SKELETON (TransactionCase STYLE):**

```python
def test_isbn_unique(self):
    self.Book.create({"name": "A", "isbn": "111"})
    with self.assertRaises(Exception):
        self.Book.create({"name": "B", "isbn": "111"})
```

You do not need a full CI suite yet — one failing and one passing case per rule is enough to prove the lab.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add unique SQL constraint on `isbn`; try duplicate create; capture error text |
| Easy | When would you choose UserError over ValidationError in `action_approve`? |
| Medium | Constrains on leave dates + positive day count |
| Medium | Show a race where only SQL uniqueness saves you — explain |
| Hard | LAB: all four rules above with tests or reproducible shell transcript |

**WHAT THIS UNLOCKS NEXT:**

Constraints protect saved data. Users also need *while-editing* guidance — defaults and `@api.onchange` that reshape the form before save. Phase 20.


---


<a id="phase-20"></a>
<a id="phase-20---onchange-ui-logic"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 19:** You can explain every lesson title in Phase 19 with a concrete Odoo or business example.

> **Phase 19 complete?** [Continue to Phase 20](#phase-20)


<a id="phase-20"></a>
# PHASE 20 - Onchange & UI Logic

**Track:** Business Logic

**WHAT YOU WILL BE ABLE TO DO:** Use `@api.onchange` for interactive form logic, set field defaults and context defaults, and design dynamic form behavior that prepares values before the user clicks Save.

**WHAT YOU SHOULD KNOW FIRST:** Phase 19 — validation on save; Phase 10 — field defaults.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — onchange](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#onchange)
- [View / form interaction](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [Module UI video](https://www.youtube.com/watch?v=5YIwP9-55Qk)
- [odoo/odoo](https://github.com/odoo/odoo)

## 20.1 @api.onchange

`@api.onchange` runs in the **UI form** (and new-form RPC) when listed fields change. It updates other fields in the client payload **before** save. It is not a substitute for constraints — malicious or non-UI writes skip onchange.

```python
@api.onchange("book_id")
def _onchange_book_id(self):
    if self.book_id:
        self.list_price = self.book_id.list_price
    else:
        self.list_price = 0.0
```

```text
User picks book on form
        |
        v
Web client calls onchange RPC
        |
        v
_onchange_book_id runs on a virtual record
        |
        v
Returned values update widgets
        |
        v
User clicks Save -> create/write (+ constrains)
```

**LIMITS:**

- Do not rely on onchange for security or integrity.
- Prefer small, predictable updates; huge onchanges feel laggy.
- `self` in onchange may be a NewId virtual record — be careful with `self.id` and creating related rows.

**ONCHANGE THAT CLEARS DEPENDENT FIELDS:**

```python
@api.onchange("member_id")
def _onchange_member_id(self):
    self.loan_notes = False
    if self.member_id:
        self.loan_notes = f"Loan for {self.member_id.name}"
```

Clearing (`= False`) matters: otherwise stale values from a previous member stick in the form.

## 20.2 Defaults

**FIELD DEFAULT:**

```python
date_borrowed = fields.Datetime(default=fields.Datetime.now)
format = fields.Selection(..., default="paperback")
company_id = fields.Many2one("res.company", default=lambda self: self.env.company)
```

Callable defaults receive `self` as a model (empty recordset) — use `self.env`, not per-record fields.

**CONTEXT DEFAULTS** (from actions/menus you will build in Story VII):

```python
# In an action context (preview):
{"default_member_id": active_id, "default_format": "ebook"}
```

When the form opens, fields fill from `default_*` keys. This is how "Create loan from member" pre-fills the member.

**DEFAULT_GET (WHEN DEFAULTS NEED LOGIC):**

```python
@api.model
def default_get(self, fields_list):
    res = super().default_get(fields_list)
    if "date_borrowed" in fields_list:
        res["date_borrowed"] = fields.Datetime.now()
    return res
```

Use `default_get` when several fields must be decided together from context; use field `default=` for simple cases.

**DYNAMIC FORM PATTERNS (LOGIC SIDE):**

| Goal | Mechanism |
| ---- | --------- |
| Prefill price from product | onchange |
| Company-dependent currency | default lambda + related |
| Hide fields until state allows | attrs/invisible in views (Story VII) + state field |
| Compute read-only total | compute (Phase 12), not onchange |

Rule of thumb: **onchange = UX convenience**; **compute = derived truth**; **constraint = hard law**; **business method = intentional verb**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | onchange: set loan notes to include book name when `book_id` changes |
| Easy | Add a lambda default for `company_id` / currency |
| Medium | Document three `default_*` context keys you would pass from a member form action |
| Medium | Show that a shell `create` bypasses onchange — reinforce why constrains still matter |
| Hard | Design dynamic leave form logic: onchange employee → department related; defaults for dates; constrain duration |

**WHAT THIS UNLOCKS NEXT:**

Defaults and onchanges polish editing. Real documents still need a **life cycle** — draft, confirmed, approved, cancelled — with legal transitions. Phase 21.


---

## 20.3 default_get()

**default_get()** belongs to **Onchange & UI Logic**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **default_get()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **default_get()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **default_get()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **default_get()** is concrete, continue — the next lesson in this phase still depends on it.

## 20.4 Context Defaults

**FIELD DEFAULT:**

```python
date_borrowed = fields.Datetime(default=fields.Datetime.now)
format = fields.Selection(..., default="paperback")
company_id = fields.Many2one("res.company", default=lambda self: self.env.company)
```

Callable defaults receive `self` as a model (empty recordset) — use `self.env`, not per-record fields.

**CONTEXT DEFAULTS** (from actions/menus you will build in Story VII):

```python
# In an action context (preview):
{"default_member_id": active_id, "default_format": "ebook"}
```

When the form opens, fields fill from `default_*` keys. This is how "Create loan from member" pre-fills the member.

**DEFAULT_GET (WHEN DEFAULTS NEED LOGIC):**

```python
@api.model
def default_get(self, fields_list):
    res = super().default_get(fields_list)
    if "date_borrowed" in fields_list:
        res["date_borrowed"] = fields.Datetime.now()
    return res
```

Use `default_get` when several fields must be decided together from context; use field `default=` for simple cases.

**DYNAMIC FORM PATTERNS (LOGIC SIDE):**

| Goal | Mechanism |
| ---- | --------- |
| Prefill price from product | onchange |
| Company-dependent currency | default lambda + related |
| Hide fields until state allows | attrs/invisible in views (Story VII) + state field |
| Compute read-only total | compute (Phase 12), not onchange |

Rule of thumb: **onchange = UX convenience**; **compute = derived truth**; **constraint = hard law**; **business method = intentional verb**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | onchange: set loan notes to include book name when `book_id` changes |
| Easy | Add a lambda default for `company_id` / currency |
| Medium | Document three `default_*` context keys you would pass from a member form action |
| Medium | Show that a shell `create` bypasses onchange — reinforce why constrains still matter |
| Hard | Design dynamic leave form logic: onchange employee → department related; defaults for dates; constrain duration |

**WHAT THIS UNLOCKS NEXT:**

Defaults and onchanges polish editing. Real documents still need a **life cycle** — draft, confirmed, approved, cancelled — with legal transitions. Phase 21.


---

## 20.5 Dynamic Forms

**FIELD DEFAULT:**

```python
date_borrowed = fields.Datetime(default=fields.Datetime.now)
format = fields.Selection(..., default="paperback")
company_id = fields.Many2one("res.company", default=lambda self: self.env.company)
```

Callable defaults receive `self` as a model (empty recordset) — use `self.env`, not per-record fields.

**CONTEXT DEFAULTS** (from actions/menus you will build in Story VII):

```python
# In an action context (preview):
{"default_member_id": active_id, "default_format": "ebook"}
```

When the form opens, fields fill from `default_*` keys. This is how "Create loan from member" pre-fills the member.

**DEFAULT_GET (WHEN DEFAULTS NEED LOGIC):**

```python
@api.model
def default_get(self, fields_list):
    res = super().default_get(fields_list)
    if "date_borrowed" in fields_list:
        res["date_borrowed"] = fields.Datetime.now()
    return res
```

Use `default_get` when several fields must be decided together from context; use field `default=` for simple cases.

**DYNAMIC FORM PATTERNS (LOGIC SIDE):**

| Goal | Mechanism |
| ---- | --------- |
| Prefill price from product | onchange |
| Company-dependent currency | default lambda + related |
| Hide fields until state allows | attrs/invisible in views (Story VII) + state field |
| Compute read-only total | compute (Phase 12), not onchange |

Rule of thumb: **onchange = UX convenience**; **compute = derived truth**; **constraint = hard law**; **business method = intentional verb**.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | onchange: set loan notes to include book name when `book_id` changes |
| Easy | Add a lambda default for `company_id` / currency |
| Medium | Document three `default_*` context keys you would pass from a member form action |
| Medium | Show that a shell `create` bypasses onchange — reinforce why constrains still matter |
| Hard | Design dynamic leave form logic: onchange employee → department related; defaults for dates; constrain duration |

**WHAT THIS UNLOCKS NEXT:**

Defaults and onchanges polish editing. Real documents still need a **life cycle** — draft, confirmed, approved, cancelled — with legal transitions. Phase 21.


---

## 20.6 Onchange vs Compute

`@api.onchange` runs in the **UI form** (and new-form RPC) when listed fields change. It updates other fields in the client payload **before** save. It is not a substitute for constraints — malicious or non-UI writes skip onchange.

```python
@api.onchange("book_id")
def _onchange_book_id(self):
    if self.book_id:
        self.list_price = self.book_id.list_price
    else:
        self.list_price = 0.0
```

```text
User picks book on form
        |
        v
Web client calls onchange RPC
        |
        v
_onchange_book_id runs on a virtual record
        |
        v
Returned values update widgets
        |
        v
User clicks Save -> create/write (+ constrains)
```

**LIMITS:**

- Do not rely on onchange for security or integrity.
- Prefer small, predictable updates; huge onchanges feel laggy.
- `self` in onchange may be a NewId virtual record — be careful with `self.id` and creating related rows.

**ONCHANGE THAT CLEARS DEPENDENT FIELDS:**

```python
@api.onchange("member_id")
def _onchange_member_id(self):
    self.loan_notes = False
    if self.member_id:
        self.loan_notes = f"Loan for {self.member_id.name}"
```

Clearing (`= False`) matters: otherwise stale values from a previous member stick in the form.


<a id="phase-21"></a>
<a id="phase-21---states-workflows"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 20:** You can explain every lesson title in Phase 20 with a concrete Odoo or business example.

> **Phase 20 complete?** [Continue to Phase 21](#phase-21)


<a id="phase-21"></a>
# PHASE 21 - States & Workflows

**Track:** Business Logic

**WHAT YOU WILL BE ABLE TO DO:** Model document states with Selection fields, implement safe transitions (draft → confirmed → approved / cancelled), block illegal moves, and deliver an approval workflow project for leave requests or purchases.

**WHAT YOU SHOULD KNOW FIRST:** Phase 18 — action methods; Phase 19 — constraints/UserError; Phase 20 — defaults/onchange.

**LEARNING RESOURCES:**

- [Odoo 18 ORM — fields / methods](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)
- [Backend tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/backend.html)
- [odoo/tutorials](https://github.com/odoo/tutorials)
- [Backend workflow patterns (video)](https://www.youtube.com/watch?v=ASPjB-WowBU)
- [odoo/odoo](https://github.com/odoo/odoo)
- [First module patterns (video)](https://www.youtube.com/watch?v=5YIwP9-55Qk)

## 21.1 State Fields

**State Fields** belongs to **States & Workflows**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **State Fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **State Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **State Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **State Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 21.2 Draft

Most business documents are state machines. A leave request should not jump from draft to paid salary impact without confirmation. Encode state as Selection — not as booleans like `is_confirmed` + `is_cancelled` that allow impossible combinations.

```python
state = fields.Selection(
    [
        ("draft", "Draft"),
        ("confirmed", "Confirmed"),
        ("approved", "Approved"),
        ("cancelled", "Cancelled"),
    ],
    default="draft",
    string="Status",
    required=True,
    copy=False,
    tracking=True,  # if mail.thread mixin later
)
```

```text
                 confirm                approve
   [draft] ----------------> [confirmed] ----------------> [approved]
      |                         |
      | cancel                  | cancel
      v                         v
              [cancelled] <-----+

Approved -> Cancelled may be forbidden or require a special reset action.
Never allow Approved -> Draft without an explicit "reset" policy.
```

```python
def action_confirm(self):
    for rec in self:
        if rec.state != "draft":
            raise UserError("Only draft requests can be confirmed.")
        rec.state = "confirmed"

def action_approve(self):
    for rec in self:
        if rec.state != "confirmed":
            raise UserError("Only confirmed requests can be approved.")
        if rec.employee_id.user_id == self.env.user:
            raise UserError("You cannot approve your own request.")
        rec.state = "approved"

def action_cancel(self):
    for rec in self:
        if rec.state == "approved":
            raise UserError("Approved requests cannot be cancelled; reset first.")
        rec.state = "cancelled"

def action_reset_to_draft(self):
    self.ensure_one()
    if not self.env.user.has_group("base.group_system"):
        raise UserError("Only managers can reset.")
    self.state = "draft"
```

Optional: `_sql_constraints` cannot easily express transition graphs — keep transitions in methods. Optionally forbid direct `write({"state": ...})` from non-method paths by checking context flags, but start with disciplined actions and readonly states in views (Story VII).

## 21.3 Confirmed

Most business documents are state machines. A leave request should not jump from draft to paid salary impact without confirmation. Encode state as Selection — not as booleans like `is_confirmed` + `is_cancelled` that allow impossible combinations.

```python
state = fields.Selection(
    [
        ("draft", "Draft"),
        ("confirmed", "Confirmed"),
        ("approved", "Approved"),
        ("cancelled", "Cancelled"),
    ],
    default="draft",
    string="Status",
    required=True,
    copy=False,
    tracking=True,  # if mail.thread mixin later
)
```

```text
                 confirm                approve
   [draft] ----------------> [confirmed] ----------------> [approved]
      |                         |
      | cancel                  | cancel
      v                         v
              [cancelled] <-----+

Approved -> Cancelled may be forbidden or require a special reset action.
Never allow Approved -> Draft without an explicit "reset" policy.
```

```python
def action_confirm(self):
    for rec in self:
        if rec.state != "draft":
            raise UserError("Only draft requests can be confirmed.")
        rec.state = "confirmed"

def action_approve(self):
    for rec in self:
        if rec.state != "confirmed":
            raise UserError("Only confirmed requests can be approved.")
        if rec.employee_id.user_id == self.env.user:
            raise UserError("You cannot approve your own request.")
        rec.state = "approved"

def action_cancel(self):
    for rec in self:
        if rec.state == "approved":
            raise UserError("Approved requests cannot be cancelled; reset first.")
        rec.state = "cancelled"

def action_reset_to_draft(self):
    self.ensure_one()
    if not self.env.user.has_group("base.group_system"):
        raise UserError("Only managers can reset.")
    self.state = "draft"
```

Optional: `_sql_constraints` cannot easily express transition graphs — keep transitions in methods. Optionally forbid direct `write({"state": ...})` from non-method paths by checking context flags, but start with disciplined actions and readonly states in views (Story VII).

## 21.4 Approved

Most business documents are state machines. A leave request should not jump from draft to paid salary impact without confirmation. Encode state as Selection — not as booleans like `is_confirmed` + `is_cancelled` that allow impossible combinations.

```python
state = fields.Selection(
    [
        ("draft", "Draft"),
        ("confirmed", "Confirmed"),
        ("approved", "Approved"),
        ("cancelled", "Cancelled"),
    ],
    default="draft",
    string="Status",
    required=True,
    copy=False,
    tracking=True,  # if mail.thread mixin later
)
```

```text
                 confirm                approve
   [draft] ----------------> [confirmed] ----------------> [approved]
      |                         |
      | cancel                  | cancel
      v                         v
              [cancelled] <-----+

Approved -> Cancelled may be forbidden or require a special reset action.
Never allow Approved -> Draft without an explicit "reset" policy.
```

```python
def action_confirm(self):
    for rec in self:
        if rec.state != "draft":
            raise UserError("Only draft requests can be confirmed.")
        rec.state = "confirmed"

def action_approve(self):
    for rec in self:
        if rec.state != "confirmed":
            raise UserError("Only confirmed requests can be approved.")
        if rec.employee_id.user_id == self.env.user:
            raise UserError("You cannot approve your own request.")
        rec.state = "approved"

def action_cancel(self):
    for rec in self:
        if rec.state == "approved":
            raise UserError("Approved requests cannot be cancelled; reset first.")
        rec.state = "cancelled"

def action_reset_to_draft(self):
    self.ensure_one()
    if not self.env.user.has_group("base.group_system"):
        raise UserError("Only managers can reset.")
    self.state = "draft"
```

Optional: `_sql_constraints` cannot easily express transition graphs — keep transitions in methods. Optionally forbid direct `write({"state": ...})` from non-method paths by checking context flags, but start with disciplined actions and readonly states in views (Story VII).

## 21.5 Done

**Done** is a typical document state. Encode it as a Selection value and only move there through methods that check invariants (stock reserved, manager signed, not already cancelled).

Buttons call those methods; users should not type the state by hand. Illegal transitions raise `UserError`.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Done** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Done**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Done** is concrete, continue — the next lesson in this phase still depends on it.

## 21.6 Cancelled

Most business documents are state machines. A leave request should not jump from draft to paid salary impact without confirmation. Encode state as Selection — not as booleans like `is_confirmed` + `is_cancelled` that allow impossible combinations.

```python
state = fields.Selection(
    [
        ("draft", "Draft"),
        ("confirmed", "Confirmed"),
        ("approved", "Approved"),
        ("cancelled", "Cancelled"),
    ],
    default="draft",
    string="Status",
    required=True,
    copy=False,
    tracking=True,  # if mail.thread mixin later
)
```

```text
                 confirm                approve
   [draft] ----------------> [confirmed] ----------------> [approved]
      |                         |
      | cancel                  | cancel
      v                         v
              [cancelled] <-----+

Approved -> Cancelled may be forbidden or require a special reset action.
Never allow Approved -> Draft without an explicit "reset" policy.
```

```python
def action_confirm(self):
    for rec in self:
        if rec.state != "draft":
            raise UserError("Only draft requests can be confirmed.")
        rec.state = "confirmed"

def action_approve(self):
    for rec in self:
        if rec.state != "confirmed":
            raise UserError("Only confirmed requests can be approved.")
        if rec.employee_id.user_id == self.env.user:
            raise UserError("You cannot approve your own request.")
        rec.state = "approved"

def action_cancel(self):
    for rec in self:
        if rec.state == "approved":
            raise UserError("Approved requests cannot be cancelled; reset first.")
        rec.state = "cancelled"

def action_reset_to_draft(self):
    self.ensure_one()
    if not self.env.user.has_group("base.group_system"):
        raise UserError("Only managers can reset.")
    self.state = "draft"
```

Optional: `_sql_constraints` cannot easily express transition graphs — keep transitions in methods. Optionally forbid direct `write({"state": ...})` from non-method paths by checking context flags, but start with disciplined actions and readonly states in views (Story VII).

## 21.7 Legal State Transitions

Most business documents are state machines. A leave request should not jump from draft to paid salary impact without confirmation. Encode state as Selection — not as booleans like `is_confirmed` + `is_cancelled` that allow impossible combinations.

```python
state = fields.Selection(
    [
        ("draft", "Draft"),
        ("confirmed", "Confirmed"),
        ("approved", "Approved"),
        ("cancelled", "Cancelled"),
    ],
    default="draft",
    string="Status",
    required=True,
    copy=False,
    tracking=True,  # if mail.thread mixin later
)
```

```text
                 confirm                approve
   [draft] ----------------> [confirmed] ----------------> [approved]
      |                         |
      | cancel                  | cancel
      v                         v
              [cancelled] <-----+

Approved -> Cancelled may be forbidden or require a special reset action.
Never allow Approved -> Draft without an explicit "reset" policy.
```

```python
def action_confirm(self):
    for rec in self:
        if rec.state != "draft":
            raise UserError("Only draft requests can be confirmed.")
        rec.state = "confirmed"

def action_approve(self):
    for rec in self:
        if rec.state != "confirmed":
            raise UserError("Only confirmed requests can be approved.")
        if rec.employee_id.user_id == self.env.user:
            raise UserError("You cannot approve your own request.")
        rec.state = "approved"

def action_cancel(self):
    for rec in self:
        if rec.state == "approved":
            raise UserError("Approved requests cannot be cancelled; reset first.")
        rec.state = "cancelled"

def action_reset_to_draft(self):
    self.ensure_one()
    if not self.env.user.has_group("base.group_system"):
        raise UserError("Only managers can reset.")
    self.state = "draft"
```

Optional: `_sql_constraints` cannot easily express transition graphs — keep transitions in methods. Optionally forbid direct `write({"state": ...})` from non-method paths by checking context flags, but start with disciplined actions and readonly states in views (Story VII).

## 21.8 Buttons

**Buttons** belongs to **States & Workflows**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Buttons** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Buttons** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Buttons**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Buttons** is concrete, continue — the next lesson in this phase still depends on it.

## 21.9 Workflow Validation

**PROJECT — Approval Workflow (leave request or purchase):**

Model `library.leave.request` (or `training.purchase.request`) with:

| Field | Type |
| ----- | ---- |
| employee / requester | Many2one |
| date_from, date_to | Date |
| days | Float compute |
| state | Selection workflow |
| notes | Text |

Methods: confirm, approve, cancel, reset (manager). Constraints: date order, positive days. onchange: optional prefills. Demo: two users — requester cannot approve own record.

**STORY PROJECT — Business Approval System**

Package Story VI into one module slice:

1. Document model with explicit state machine.
2. Business actions for every legal transition; illegal ones raise UserError.
3. Constraints from Phase 19 still hold at every state.
4. Short README state diagram (ASCII) + test script or TransactionCase covering happy path and three refusals.
5. Note explicitly what Story VII must add (buttons on forms, statusbar widget, groups for approvers).

```text
Story IV  — what the data *is*
Story V   — how to *move* data safely at scale
Story VI  — what the business *allows* to happen
Story VII — how users *see and click* that policy
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Draw the state diagram for sales order draft/sent/sale/cancel |
| Easy | Implement `action_confirm` with a state guard |
| Medium | Block self-approval; prove with two users in shell (`with_user`) |
| Medium | PROJECT leave/purchase approval with compute days + constraints |
| Hard | STORY PROJECT: Business Approval System with tests for happy path + refusals |

**WHAT THIS UNLOCKS NEXT:**

You can enforce a real approval life cycle in Python — but staff still have no screens, status bars, or menus to click. That limitation opens **Story VII — Building the User Interface**: XML views, actions, and menus that expose the models and workflows you just built.

## 21.10 PROJECT — Approval Workflow

**PROJECT — Approval Workflow (leave request or purchase):**

Model `library.leave.request` (or `training.purchase.request`) with:

| Field | Type |
| ----- | ---- |
| employee / requester | Many2one |
| date_from, date_to | Date |
| days | Float compute |
| state | Selection workflow |
| notes | Text |

Methods: confirm, approve, cancel, reset (manager). Constraints: date order, positive days. onchange: optional prefills. Demo: two users — requester cannot approve own record.

**STORY PROJECT — Business Approval System**

Package Story VI into one module slice:

1. Document model with explicit state machine.
2. Business actions for every legal transition; illegal ones raise UserError.
3. Constraints from Phase 19 still hold at every state.
4. Short README state diagram (ASCII) + test script or TransactionCase covering happy path and three refusals.
5. Note explicitly what Story VII must add (buttons on forms, statusbar widget, groups for approvers).

```text
Story IV  — what the data *is*
Story V   — how to *move* data safely at scale
Story VI  — what the business *allows* to happen
Story VII — how users *see and click* that policy
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Draw the state diagram for sales order draft/sent/sale/cancel |
| Easy | Implement `action_confirm` with a state guard |
| Medium | Block self-approval; prove with two users in shell (`with_user`) |
| Medium | PROJECT leave/purchase approval with compute days + constraints |
| Hard | STORY PROJECT: Business Approval System with tests for happy path + refusals |

**WHAT THIS UNLOCKS NEXT:**

You can enforce a real approval life cycle in Python — but staff still have no screens, status bars, or menus to click. That limitation opens **Story VII — Building the User Interface**: XML views, actions, and menus that expose the models and workflows you just built.


---


**CHECK YOUR UNDERSTANDING AFTER PHASE 21:** You can explain every lesson title in Phase 21 with a concrete Odoo or business example.

> **Phase 21 complete?** [Continue to Phase 22](#phase-22)


## STORY PROJECT — Business Approval System

**Track:** Business Logic

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY VII — BUILDING THE USER INTERFACE

**Track:** User Interface

*Users never see your Python — they see views, actions, and menus.*

---
<a id="phase-22"></a>
<a id="phase-22---xml-foundations"></a>
# PHASE 22 - XML Foundations

**Track:** User Interface

**WHAT YOU WILL BE ABLE TO DO:** Author Odoo data XML that creates stable `ir.model.data` external IDs, load in the right order through the manifest, and debug noupdate / missing-ref failures without guessing.

**WHAT YOU SHOULD KNOW FIRST:** Phases 9–21 — models, fields, ORM, and business methods. You can define a model and persist records in Python; users still have no declarative screen wiring.

**LEARNING RESOURCES:**

- [View architectures (Odoo 18)](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [Server Framework 101 — Security intro (data loading context)](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/04_securityintro.html)
- [odoo/odoo — base data XML patterns](https://github.com/odoo/odoo)
- [Odoo XML data files overview](https://www.youtube.com/watch?v=mzg3EGD_6Gw)

## 22.1 XML Syntax

You already own business truth in Python models. The next wall is not “how do I `create()` a book?” — it is “how does Odoo remember *this* form, *this* menu, *this* group across databases and upgrades?” That memory is not a random primary key. It is an **external ID**: a namespaced string stored in `ir.model.data` that maps `module.xml_id` → database row.

**WHAT AN XML RECORD ACTUALLY IS:**

Every `<record>` in a data file is an instruction to the ORM: find-or-create a row in some model, set fields, and register (or reuse) an external ID.

```xml
<record id="view_library_book_form" model="ir.ui.view">
    <field name="name">library.book.form</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <form string="Book">
            <sheet>
                <group>
                    <field name="name"/>
                    <field name="isbn"/>
                </group>
            </sheet>
        </form>
    </field>
</record>
```

- `id="view_library_book_form"` becomes the **XML ID** `your_module.view_library_book_form`.
- `model="ir.ui.view"` is the target model (not your business model alone — views, actions, menus, groups, sequences are all data rows).
- Nested `<field>` tags set columns on that row. `type="xml"` means the content is stored as an XML architecture, not escaped text.

**EXTERNAL IDS ARE THE PUBLIC API OF YOUR MODULE:**

```text
Module library_app installs:

  XML:  id="view_library_book_form"
           |
           v
  ir.model.data
    module = library_app
    name   = view_library_book_form
    model  = ir.ui.view
    res_id = 42          <-- this integer changes per database
           |
           v
  ir.ui.view (id=42) arch=...

Other XML can say:
  ref="library_app.view_library_book_form"   <-- stable across DBs
Never hardcode res_id=42.
```

`ref="module.xml_id"` resolves at load time. If the target is missing, install/`-u` fails loudly — that is good. Silent wrong IDs are worse.

**EVAL AND FUNCTION CALLS IN DATA:**

```xml
<field name="groups_id" eval="[(4, ref('base.group_user'))]"/>
<field name="binding_model_id" ref="model_library_book"/>
```

`eval` runs a restricted Python expression with helpers like `ref()`, `time`, and command tuples for x2many writes. Prefer `ref=` on simple Many2one fields; use `eval` when you need commands or computed values.

**NOUPDATE AND UPGRADES:**

`<odoo noupdate="1">` (or `<data noupdate="1">`) means: on **update**, do not overwrite existing rows for these XML IDs. First install still creates them. Use noupdate for:

- security groups users may have customized,
- sequences / demo-ish master data you must not reset,
- record rules a deployer tweaked in production.

Do **not** put evolving view arches under noupdate while you are actively developing — you will `-u` and wonder why XML changes never appear.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write a `<record>` for `ir.ui.view` with a stable `id` and explain what lands in `ir.model.data`. |
| Medium | Cross-reference two records with `ref=` (view → action). Predict the error if load order is reversed. |
| Hard | Decide which of: groups, form views, demo books should use `noupdate="1"` and justify each choice. |

**WHAT THIS UNLOCKS NEXT:**

A single record is not a module. Odoo loads **files** in a declared order. Wrong order means `ref` failures even when every ID is spelled correctly — that is Phase 22.2.

---

## 22.2 <record>

You already own business truth in Python models. The next wall is not “how do I `create()` a book?” — it is “how does Odoo remember *this* form, *this* menu, *this* group across databases and upgrades?” That memory is not a random primary key. It is an **external ID**: a namespaced string stored in `ir.model.data` that maps `module.xml_id` → database row.

**WHAT AN XML RECORD ACTUALLY IS:**

Every `<record>` in a data file is an instruction to the ORM: find-or-create a row in some model, set fields, and register (or reuse) an external ID.

```xml
<record id="view_library_book_form" model="ir.ui.view">
    <field name="name">library.book.form</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <form string="Book">
            <sheet>
                <group>
                    <field name="name"/>
                    <field name="isbn"/>
                </group>
            </sheet>
        </form>
    </field>
</record>
```

- `id="view_library_book_form"` becomes the **XML ID** `your_module.view_library_book_form`.
- `model="ir.ui.view"` is the target model (not your business model alone — views, actions, menus, groups, sequences are all data rows).
- Nested `<field>` tags set columns on that row. `type="xml"` means the content is stored as an XML architecture, not escaped text.

**EXTERNAL IDS ARE THE PUBLIC API OF YOUR MODULE:**

```text
Module library_app installs:

  XML:  id="view_library_book_form"
           |
           v
  ir.model.data
    module = library_app
    name   = view_library_book_form
    model  = ir.ui.view
    res_id = 42          <-- this integer changes per database
           |
           v
  ir.ui.view (id=42) arch=...

Other XML can say:
  ref="library_app.view_library_book_form"   <-- stable across DBs
Never hardcode res_id=42.
```

`ref="module.xml_id"` resolves at load time. If the target is missing, install/`-u` fails loudly — that is good. Silent wrong IDs are worse.

**EVAL AND FUNCTION CALLS IN DATA:**

```xml
<field name="groups_id" eval="[(4, ref('base.group_user'))]"/>
<field name="binding_model_id" ref="model_library_book"/>
```

`eval` runs a restricted Python expression with helpers like `ref()`, `time`, and command tuples for x2many writes. Prefer `ref=` on simple Many2one fields; use `eval` when you need commands or computed values.

**NOUPDATE AND UPGRADES:**

`<odoo noupdate="1">` (or `<data noupdate="1">`) means: on **update**, do not overwrite existing rows for these XML IDs. First install still creates them. Use noupdate for:

- security groups users may have customized,
- sequences / demo-ish master data you must not reset,
- record rules a deployer tweaked in production.

Do **not** put evolving view arches under noupdate while you are actively developing — you will `-u` and wonder why XML changes never appear.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write a `<record>` for `ir.ui.view` with a stable `id` and explain what lands in `ir.model.data`. |
| Medium | Cross-reference two records with `ref=` (view → action). Predict the error if load order is reversed. |
| Hard | Decide which of: groups, form views, demo books should use `noupdate="1"` and justify each choice. |

**WHAT THIS UNLOCKS NEXT:**

A single record is not a module. Odoo loads **files** in a declared order. Wrong order means `ref` failures even when every ID is spelled correctly — that is Phase 22.2.

---

## 22.3 External IDs

You already own business truth in Python models. The next wall is not “how do I `create()` a book?” — it is “how does Odoo remember *this* form, *this* menu, *this* group across databases and upgrades?” That memory is not a random primary key. It is an **external ID**: a namespaced string stored in `ir.model.data` that maps `module.xml_id` → database row.

**WHAT AN XML RECORD ACTUALLY IS:**

Every `<record>` in a data file is an instruction to the ORM: find-or-create a row in some model, set fields, and register (or reuse) an external ID.

```xml
<record id="view_library_book_form" model="ir.ui.view">
    <field name="name">library.book.form</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <form string="Book">
            <sheet>
                <group>
                    <field name="name"/>
                    <field name="isbn"/>
                </group>
            </sheet>
        </form>
    </field>
</record>
```

- `id="view_library_book_form"` becomes the **XML ID** `your_module.view_library_book_form`.
- `model="ir.ui.view"` is the target model (not your business model alone — views, actions, menus, groups, sequences are all data rows).
- Nested `<field>` tags set columns on that row. `type="xml"` means the content is stored as an XML architecture, not escaped text.

**EXTERNAL IDS ARE THE PUBLIC API OF YOUR MODULE:**

```text
Module library_app installs:

  XML:  id="view_library_book_form"
           |
           v
  ir.model.data
    module = library_app
    name   = view_library_book_form
    model  = ir.ui.view
    res_id = 42          <-- this integer changes per database
           |
           v
  ir.ui.view (id=42) arch=...

Other XML can say:
  ref="library_app.view_library_book_form"   <-- stable across DBs
Never hardcode res_id=42.
```

`ref="module.xml_id"` resolves at load time. If the target is missing, install/`-u` fails loudly — that is good. Silent wrong IDs are worse.

**EVAL AND FUNCTION CALLS IN DATA:**

```xml
<field name="groups_id" eval="[(4, ref('base.group_user'))]"/>
<field name="binding_model_id" ref="model_library_book"/>
```

`eval` runs a restricted Python expression with helpers like `ref()`, `time`, and command tuples for x2many writes. Prefer `ref=` on simple Many2one fields; use `eval` when you need commands or computed values.

**NOUPDATE AND UPGRADES:**

`<odoo noupdate="1">` (or `<data noupdate="1">`) means: on **update**, do not overwrite existing rows for these XML IDs. First install still creates them. Use noupdate for:

- security groups users may have customized,
- sequences / demo-ish master data you must not reset,
- record rules a deployer tweaked in production.

Do **not** put evolving view arches under noupdate while you are actively developing — you will `-u` and wonder why XML changes never appear.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write a `<record>` for `ir.ui.view` with a stable `id` and explain what lands in `ir.model.data`. |
| Medium | Cross-reference two records with `ref=` (view → action). Predict the error if load order is reversed. |
| Hard | Decide which of: groups, form views, demo books should use `noupdate="1"` and justify each choice. |

**WHAT THIS UNLOCKS NEXT:**

A single record is not a module. Odoo loads **files** in a declared order. Wrong order means `ref` failures even when every ID is spelled correctly — that is Phase 22.2.

---

## 22.4 ref()

**ref()** belongs to **XML Foundations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **ref()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ref()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ref()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ref()** is concrete, continue — the next lesson in this phase still depends on it.

## 22.5 eval

**eval** belongs to **XML Foundations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **eval** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **eval** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **eval**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **eval** is concrete, continue — the next lesson in this phase still depends on it.

## 22.6 Data Files

The manifest’s `'data': [...]` list is a **pipeline**, not a bag. Odoo processes files top to bottom on install and update. Dependencies between XML IDs must respect that order — and module `depends` must ensure foreign modules’ IDs exist first.

**TYPICAL UI DATA LAYOUT:**

```text
library_app/
  __manifest__.py
    'data': [
      'security/library_security.xml',   # groups first
      'security/ir.model.access.csv',
      'views/library_book_views.xml',    # views before actions that need them
      'views/library_menus.xml',         # menus last (need actions)
      'data/library_sequence.xml',
    ]
  security/
  views/
  data/
```

**WHY ORDER BITES:**

```text
menus.xml loads first, contains:
  <field name="action" ref="library_app.action_library_book"/>

actions still undefined
        |
        v
ValueError: External ID not found: library_app.action_library_book
```

Fix: list the action file before the menu file — or keep action + menu in one file with action defined first.

**CSV VS XML:**

Access rights usually live in `ir.model.access.csv` (tabular ACL). Groups, rules, views, actions, menus live in XML. Both create external IDs. CSV rows use `id` and `model_id:id` / `group_id:id` columns that resolve XML IDs the same way.

**INSTALL VS UPDATE MENTAL MODEL:**

```text
odoo-bin -i library_app
  create DB rows + ir.model.data for every XML/CSV id

odoo-bin -u library_app
  for each XML id:
    if noupdate and row exists -> skip field writes
    else -> write fields again (views refresh, etc.)
```

During development, `-u` after every view change is normal. In production, treat `-u` as a release step with backup — views and security reload together.

**DEMO DATA:**

`'demo': [...]` loads only when the database was created with demo data enabled. Never put required security or menus only in demo files.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Sketch a manifest `data` list for groups → ACL → views → actions → menus. |
| Medium | Break a module on purpose with reversed refs; fix by reordering files only. |
| Hard | Audit an existing addon’s data list and mark every cross-file `ref=` dependency. |

**WHAT THIS UNLOCKS NEXT:**

Stable IDs and load order give you a place to hang UI. Phase 23 builds the screens users actually edit: **form** and **list** views bound to your models.

---


---

## 22.7 XML Loading

The manifest’s `'data': [...]` list is a **pipeline**, not a bag. Odoo processes files top to bottom on install and update. Dependencies between XML IDs must respect that order — and module `depends` must ensure foreign modules’ IDs exist first.

**TYPICAL UI DATA LAYOUT:**

```text
library_app/
  __manifest__.py
    'data': [
      'security/library_security.xml',   # groups first
      'security/ir.model.access.csv',
      'views/library_book_views.xml',    # views before actions that need them
      'views/library_menus.xml',         # menus last (need actions)
      'data/library_sequence.xml',
    ]
  security/
  views/
  data/
```

**WHY ORDER BITES:**

```text
menus.xml loads first, contains:
  <field name="action" ref="library_app.action_library_book"/>

actions still undefined
        |
        v
ValueError: External ID not found: library_app.action_library_book
```

Fix: list the action file before the menu file — or keep action + menu in one file with action defined first.

**CSV VS XML:**

Access rights usually live in `ir.model.access.csv` (tabular ACL). Groups, rules, views, actions, menus live in XML. Both create external IDs. CSV rows use `id` and `model_id:id` / `group_id:id` columns that resolve XML IDs the same way.

**INSTALL VS UPDATE MENTAL MODEL:**

```text
odoo-bin -i library_app
  create DB rows + ir.model.data for every XML/CSV id

odoo-bin -u library_app
  for each XML id:
    if noupdate and row exists -> skip field writes
    else -> write fields again (views refresh, etc.)
```

During development, `-u` after every view change is normal. In production, treat `-u` as a release step with backup — views and security reload together.

**DEMO DATA:**

`'demo': [...]` loads only when the database was created with demo data enabled. Never put required security or menus only in demo files.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Sketch a manifest `data` list for groups → ACL → views → actions → menus. |
| Medium | Break a module on purpose with reversed refs; fix by reordering files only. |
| Hard | Audit an existing addon’s data list and mark every cross-file `ref=` dependency. |

**WHAT THIS UNLOCKS NEXT:**

Stable IDs and load order give you a place to hang UI. Phase 23 builds the screens users actually edit: **form** and **list** views bound to your models.

---


---


<a id="phase-23"></a>
<a id="phase-23---form-list-views"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 22:** You can explain every lesson title in Phase 22 with a concrete Odoo or business example.

> **Phase 22 complete?** [Continue to Phase 23](#phase-23)


<a id="phase-23"></a>
# PHASE 23 - Form & List Views

**Track:** User Interface

**WHAT YOU WILL BE ABLE TO DO:** Design Odoo 18 form and list architectures — sheet layout, groups, notebooks, headers/buttons, list columns — so a business user can create, read, update, and navigate records without shell access.

**WHAT YOU SHOULD KNOW FIRST:** Phase 22 — XML records, external IDs, data load order. Your models from Stories IV–VI already exist.

**LEARNING RESOURCES:**

- [View architectures (Odoo 18)](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [Form / list UI walkthrough](https://www.youtube.com/watch?v=hwfhO2IGhmc)
- [odoo/odoo — sale form XML](https://github.com/odoo/odoo)

## 23.1 Form Views

A **form view** is the detail/edit surface for one record. In Odoo 18 the architecture is XML inside `ir.ui.view`; the web client (OWL) renders widgets bound to field names on the model.

**ANATOMY OF A WORKING FORM:**

```xml
<record id="view_library_book_form" model="ir.ui.view">
    <field name="name">library.book.form</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <form string="Book">
            <header>
                <button name="action_make_available" type="object"
                        string="Make Available"
                        invisible="state != 'loaned'"/>
                <field name="state" widget="statusbar"
                       statusbar_visible="draft,available,loaned"/>
            </header>
            <sheet>
                <div class="oe_title">
                    <h1><field name="name" placeholder="Book title"/></h1>
                </div>
                <group>
                    <group string="Identity">
                        <field name="isbn"/>
                        <field name="author_id"/>
                    </group>
                    <group string="Loan">
                        <field name="borrower_id"/>
                        <field name="due_date"/>
                    </group>
                </group>
                <notebook>
                    <page string="Notes" name="notes">
                        <field name="notes" nolabel="1"/>
                    </page>
                    <page string="Loans" name="loans">
                        <field name="loan_ids">
                            <list>
                                <field name="date_start"/>
                                <field name="partner_id"/>
                            </list>
                        </field>
                    </page>
                </notebook>
            </sheet>
        </form>
    </field>
</record>
```

**LAYOUT CONTRACT:**

```text
<form>
  <header>     statusbar + workflow buttons (object / action)
  <sheet>      main business fields
    <group>    two-column grids; nest group in group for 2x2
    <notebook> tabs for secondary density
      <page>
  optional: chatter (mail.thread) outside sheet in many official apps
```

- **`type="object"`** buttons call a Python method on the model (`def action_make_available(self):`).
- **`type="action"`** buttons open an `ir.actions.*` by XML ID.
- Odoo 18 prefers Python expressions in `invisible` / `readonly` / `required` (e.g. `state != 'draft'`) over legacy `attrs` dicts on new code.

**FIELD WIDGETS CHANGE THE JOB, NOT THE COLUMN:**

| Widget / option | When |
| --------------- | ---- |
| `widget="statusbar"` | Selection / many2one stages in the header |
| `widget="many2many_tags"` | Compact M2M |
| `widget="monetary"` | Amount + currency |
| `widget="badge"` | Soft status chips on lists/forms |
| `nolabel="1"` | Full-width text / HTML |

Domains on fields (`domain="[('active','=',True)]"`) limit *what you can pick*, not server security — Phase 28–29 still own authorization.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Form with two `group` columns and three fields. |
| Medium | Header button + statusbar; hide button unless `state` matches. |
| Hard | Notebook with an embedded one2many list editable inline. |

**WHAT THIS UNLOCKS NEXT:**

Forms edit one record. Operators live in queues of many records — that is the **list** view.

---

## 23.2 List Views

In Odoo 18 the list root is `<list>` (historically `<tree>` still appears in older modules). Lists are for scanning, multi-edit, and opening forms.

```xml
<record id="view_library_book_list" model="ir.ui.view">
    <field name="name">library.book.list</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <list string="Books" default_order="name"
              decoration-muted="state == 'archived'"
              decoration-danger="state == 'lost'">
            <field name="name"/>
            <field name="isbn" optional="hide"/>
            <field name="author_id"/>
            <field name="state" widget="badge"/>
            <field name="due_date"/>
        </list>
    </field>
</record>
```

- **`editable="bottom"`** or `"top"` enables inline create/edit without opening the form — powerful for line models, dangerous for complex workflows.
- **`optional="hide"` / `"show"`** lets users toggle columns.
- **`decoration-*`** apply row styling from expressions (presentation only).

**BUTTONS ON LISTS:**

```xml
<button name="action_renew" type="object" string="Renew" icon="fa-refresh"/>
```

Row buttons operate on that record’s recordset (`self` length 1). Prefer server actions / multi for bulk.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List with four columns and `default_order`. |
| Medium | Add `optional` column + decoration for overdue due dates. |
| Hard | Editable list for loan lines with a constrained many2one. |

**WHAT THIS UNLOCKS NEXT:**

Form + list alone are enough for a first CRUD UI — ship that mini interface next.

---

## 23.3 Field Display

**Field Display** belongs to **Form & List Views**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Field Display** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Field Display** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Field Display**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Field Display** is concrete, continue — the next lesson in this phase still depends on it.

## 23.4 Buttons

A **form view** is the detail/edit surface for one record. In Odoo 18 the architecture is XML inside `ir.ui.view`; the web client (OWL) renders widgets bound to field names on the model.

**ANATOMY OF A WORKING FORM:**

```xml
<record id="view_library_book_form" model="ir.ui.view">
    <field name="name">library.book.form</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <form string="Book">
            <header>
                <button name="action_make_available" type="object"
                        string="Make Available"
                        invisible="state != 'loaned'"/>
                <field name="state" widget="statusbar"
                       statusbar_visible="draft,available,loaned"/>
            </header>
            <sheet>
                <div class="oe_title">
                    <h1><field name="name" placeholder="Book title"/></h1>
                </div>
                <group>
                    <group string="Identity">
                        <field name="isbn"/>
                        <field name="author_id"/>
                    </group>
                    <group string="Loan">
                        <field name="borrower_id"/>
                        <field name="due_date"/>
                    </group>
                </group>
                <notebook>
                    <page string="Notes" name="notes">
                        <field name="notes" nolabel="1"/>
                    </page>
                    <page string="Loans" name="loans">
                        <field name="loan_ids">
                            <list>
                                <field name="date_start"/>
                                <field name="partner_id"/>
                            </list>
                        </field>
                    </page>
                </notebook>
            </sheet>
        </form>
    </field>
</record>
```

**LAYOUT CONTRACT:**

```text
<form>
  <header>     statusbar + workflow buttons (object / action)
  <sheet>      main business fields
    <group>    two-column grids; nest group in group for 2x2
    <notebook> tabs for secondary density
      <page>
  optional: chatter (mail.thread) outside sheet in many official apps
```

- **`type="object"`** buttons call a Python method on the model (`def action_make_available(self):`).
- **`type="action"`** buttons open an `ir.actions.*` by XML ID.
- Odoo 18 prefers Python expressions in `invisible` / `readonly` / `required` (e.g. `state != 'draft'`) over legacy `attrs` dicts on new code.

**FIELD WIDGETS CHANGE THE JOB, NOT THE COLUMN:**

| Widget / option | When |
| --------------- | ---- |
| `widget="statusbar"` | Selection / many2one stages in the header |
| `widget="many2many_tags"` | Compact M2M |
| `widget="monetary"` | Amount + currency |
| `widget="badge"` | Soft status chips on lists/forms |
| `nolabel="1"` | Full-width text / HTML |

Domains on fields (`domain="[('active','=',True)]"`) limit *what you can pick*, not server security — Phase 28–29 still own authorization.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Form with two `group` columns and three fields. |
| Medium | Header button + statusbar; hide button unless `state` matches. |
| Hard | Notebook with an embedded one2many list editable inline. |

**WHAT THIS UNLOCKS NEXT:**

Forms edit one record. Operators live in queues of many records — that is the **list** view.

---

## 23.5 Groups

A **form view** is the detail/edit surface for one record. In Odoo 18 the architecture is XML inside `ir.ui.view`; the web client (OWL) renders widgets bound to field names on the model.

**ANATOMY OF A WORKING FORM:**

```xml
<record id="view_library_book_form" model="ir.ui.view">
    <field name="name">library.book.form</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <form string="Book">
            <header>
                <button name="action_make_available" type="object"
                        string="Make Available"
                        invisible="state != 'loaned'"/>
                <field name="state" widget="statusbar"
                       statusbar_visible="draft,available,loaned"/>
            </header>
            <sheet>
                <div class="oe_title">
                    <h1><field name="name" placeholder="Book title"/></h1>
                </div>
                <group>
                    <group string="Identity">
                        <field name="isbn"/>
                        <field name="author_id"/>
                    </group>
                    <group string="Loan">
                        <field name="borrower_id"/>
                        <field name="due_date"/>
                    </group>
                </group>
                <notebook>
                    <page string="Notes" name="notes">
                        <field name="notes" nolabel="1"/>
                    </page>
                    <page string="Loans" name="loans">
                        <field name="loan_ids">
                            <list>
                                <field name="date_start"/>
                                <field name="partner_id"/>
                            </list>
                        </field>
                    </page>
                </notebook>
            </sheet>
        </form>
    </field>
</record>
```

**LAYOUT CONTRACT:**

```text
<form>
  <header>     statusbar + workflow buttons (object / action)
  <sheet>      main business fields
    <group>    two-column grids; nest group in group for 2x2
    <notebook> tabs for secondary density
      <page>
  optional: chatter (mail.thread) outside sheet in many official apps
```

- **`type="object"`** buttons call a Python method on the model (`def action_make_available(self):`).
- **`type="action"`** buttons open an `ir.actions.*` by XML ID.
- Odoo 18 prefers Python expressions in `invisible` / `readonly` / `required` (e.g. `state != 'draft'`) over legacy `attrs` dicts on new code.

**FIELD WIDGETS CHANGE THE JOB, NOT THE COLUMN:**

| Widget / option | When |
| --------------- | ---- |
| `widget="statusbar"` | Selection / many2one stages in the header |
| `widget="many2many_tags"` | Compact M2M |
| `widget="monetary"` | Amount + currency |
| `widget="badge"` | Soft status chips on lists/forms |
| `nolabel="1"` | Full-width text / HTML |

Domains on fields (`domain="[('active','=',True)]"`) limit *what you can pick*, not server security — Phase 28–29 still own authorization.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Form with two `group` columns and three fields. |
| Medium | Header button + statusbar; hide button unless `state` matches. |
| Hard | Notebook with an embedded one2many list editable inline. |

**WHAT THIS UNLOCKS NEXT:**

Forms edit one record. Operators live in queues of many records — that is the **list** view.

---

## 23.6 Sheets

**Sheets** belongs to **Form & List Views**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Sheets** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Sheets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Sheets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Sheets** is concrete, continue — the next lesson in this phase still depends on it.

## 23.7 Notebook

A **form view** is the detail/edit surface for one record. In Odoo 18 the architecture is XML inside `ir.ui.view`; the web client (OWL) renders widgets bound to field names on the model.

**ANATOMY OF A WORKING FORM:**

```xml
<record id="view_library_book_form" model="ir.ui.view">
    <field name="name">library.book.form</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <form string="Book">
            <header>
                <button name="action_make_available" type="object"
                        string="Make Available"
                        invisible="state != 'loaned'"/>
                <field name="state" widget="statusbar"
                       statusbar_visible="draft,available,loaned"/>
            </header>
            <sheet>
                <div class="oe_title">
                    <h1><field name="name" placeholder="Book title"/></h1>
                </div>
                <group>
                    <group string="Identity">
                        <field name="isbn"/>
                        <field name="author_id"/>
                    </group>
                    <group string="Loan">
                        <field name="borrower_id"/>
                        <field name="due_date"/>
                    </group>
                </group>
                <notebook>
                    <page string="Notes" name="notes">
                        <field name="notes" nolabel="1"/>
                    </page>
                    <page string="Loans" name="loans">
                        <field name="loan_ids">
                            <list>
                                <field name="date_start"/>
                                <field name="partner_id"/>
                            </list>
                        </field>
                    </page>
                </notebook>
            </sheet>
        </form>
    </field>
</record>
```

**LAYOUT CONTRACT:**

```text
<form>
  <header>     statusbar + workflow buttons (object / action)
  <sheet>      main business fields
    <group>    two-column grids; nest group in group for 2x2
    <notebook> tabs for secondary density
      <page>
  optional: chatter (mail.thread) outside sheet in many official apps
```

- **`type="object"`** buttons call a Python method on the model (`def action_make_available(self):`).
- **`type="action"`** buttons open an `ir.actions.*` by XML ID.
- Odoo 18 prefers Python expressions in `invisible` / `readonly` / `required` (e.g. `state != 'draft'`) over legacy `attrs` dicts on new code.

**FIELD WIDGETS CHANGE THE JOB, NOT THE COLUMN:**

| Widget / option | When |
| --------------- | ---- |
| `widget="statusbar"` | Selection / many2one stages in the header |
| `widget="many2many_tags"` | Compact M2M |
| `widget="monetary"` | Amount + currency |
| `widget="badge"` | Soft status chips on lists/forms |
| `nolabel="1"` | Full-width text / HTML |

Domains on fields (`domain="[('active','=',True)]"`) limit *what you can pick*, not server security — Phase 28–29 still own authorization.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Form with two `group` columns and three fields. |
| Medium | Header button + statusbar; hide button unless `state` matches. |
| Hard | Notebook with an embedded one2many list editable inline. |

**WHAT THIS UNLOCKS NEXT:**

Forms edit one record. Operators live in queues of many records — that is the **list** view.

---

## 23.8 Pages

**Pages** belongs to **Form & List Views**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Pages** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Pages** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Pages**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Pages** is concrete, continue — the next lesson in this phase still depends on it.

## 23.9 Widgets

**Widgets** belongs to **Form & List Views**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Widgets** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Widgets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Widgets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Widgets** is concrete, continue — the next lesson in this phase still depends on it.

## 23.10 Decorations

**Decorations** belongs to **Form & List Views**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Decorations** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Decorations** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Decorations**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Decorations** is concrete, continue — the next lesson in this phase still depends on it.

## 23.11 MINI PROJECT — CRUD Interface

Build a minimal but real UI for one model you already have (e.g. `library.book` or `estate.property`):

1. Form with header/statusbar or a simple state field, sheet groups, and one notebook page.
2. List with at least five meaningful columns and one decoration.
3. Both registered as `ir.ui.view` records with stable XML IDs.
4. Install / `-u` and create three records from the UI (you may temporarily open via Technical → Views if menus wait until Phase 25).

**Acceptance:** a non-developer can create and edit a record without the shell; field names on the form match the model; no traceback on save.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Screenshot form + list after creating one record. |
| Medium | Add a one2many page and create a child line from the form. |
| Hard | Make a field readonly when state is done using an Odoo 18 expression. |

**WHAT THIS UNLOCKS NEXT:**

CRUD without search filters forces users to scroll forever. Phase 24 adds **search**, **kanban**, and analytical views on the same model.

---


---


<a id="phase-24"></a>
<a id="phase-24---search-kanban-analytical-views"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 23:** You can explain every lesson title in Phase 23 with a concrete Odoo or business example.

> **Phase 23 complete?** [Continue to Phase 24](#phase-24)


<a id="phase-24"></a>
# PHASE 24 - Search, Kanban & Analytical Views

**Track:** User Interface

**WHAT YOU WILL BE ABLE TO DO:** Attach search, kanban, calendar, pivot, and graph views to the same model so one dataset serves operations, planning, and light analytics.

**WHAT YOU SHOULD KNOW FIRST:** Phase 23 — form and list arches. Domains from the ORM story.

**LEARNING RESOURCES:**

- [View architectures (Odoo 18)](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [Multiple view types demo](https://www.youtube.com/watch?v=r11U1pqGqQQ)
- [odoo/odoo — CRM kanban / search examples](https://github.com/odoo/odoo)

## 24.1 Search Views

A **search view** does not display records. It defines the control strip above list/kanban: fields to search, preset filters, and group-by options.

```xml
<record id="view_library_book_search" model="ir.ui.view">
    <field name="name">library.book.search</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <search string="Books">
            <field name="name"/>
            <field name="isbn"/>
            <field name="author_id"/>
            <filter name="filter_available" string="Available"
                    domain="[('state', '=', 'available')]"/>
            <filter name="filter_loaned" string="Loaned"
                    domain="[('state', '=', 'loaned')]"/>
            <separator/>
            <filter name="filter_my_authors" string="My Authors"
                    domain="[('author_id.user_id', '=', uid)]"/>
            <group expand="0" string="Group By">
                <filter name="groupby_author" string="Author"
                        context="{'group_by': 'author_id'}"/>
                <filter name="groupby_state" string="State"
                        context="{'group_by': 'state'}"/>
            </group>
            <searchpanel>
                <field name="state" enable_counters="1"/>
            </searchpanel>
        </search>
    </field>
</record>
```

**FILTER VS DOMAIN ON THE ACTION:**

```text
Action domain     = always-on constraint (e.g. only active books in this menu)
Search filter     = optional facet the user toggles
Both AND together when the filter is on.
```

`uid` in domains is the current user id. Prefer named filters over teaching users to write domains by hand.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Search view with two field searches and one domain filter. |
| Medium | Add group-by for many2one + selection. |
| Hard | Searchpanel on state with counters; verify filter + action domain interaction. |

**WHAT THIS UNLOCKS NEXT:**

Search shapes *which* rows. Kanban and friends change *how* those rows are experienced.

---

## 24.2 Filters

A **search view** does not display records. It defines the control strip above list/kanban: fields to search, preset filters, and group-by options.

```xml
<record id="view_library_book_search" model="ir.ui.view">
    <field name="name">library.book.search</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <search string="Books">
            <field name="name"/>
            <field name="isbn"/>
            <field name="author_id"/>
            <filter name="filter_available" string="Available"
                    domain="[('state', '=', 'available')]"/>
            <filter name="filter_loaned" string="Loaned"
                    domain="[('state', '=', 'loaned')]"/>
            <separator/>
            <filter name="filter_my_authors" string="My Authors"
                    domain="[('author_id.user_id', '=', uid)]"/>
            <group expand="0" string="Group By">
                <filter name="groupby_author" string="Author"
                        context="{'group_by': 'author_id'}"/>
                <filter name="groupby_state" string="State"
                        context="{'group_by': 'state'}"/>
            </group>
            <searchpanel>
                <field name="state" enable_counters="1"/>
            </searchpanel>
        </search>
    </field>
</record>
```

**FILTER VS DOMAIN ON THE ACTION:**

```text
Action domain     = always-on constraint (e.g. only active books in this menu)
Search filter     = optional facet the user toggles
Both AND together when the filter is on.
```

`uid` in domains is the current user id. Prefer named filters over teaching users to write domains by hand.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Search view with two field searches and one domain filter. |
| Medium | Add group-by for many2one + selection. |
| Hard | Searchpanel on state with counters; verify filter + action domain interaction. |

**WHAT THIS UNLOCKS NEXT:**

Search shapes *which* rows. Kanban and friends change *how* those rows are experienced.

---

## 24.3 Group By

A **search view** does not display records. It defines the control strip above list/kanban: fields to search, preset filters, and group-by options.

```xml
<record id="view_library_book_search" model="ir.ui.view">
    <field name="name">library.book.search</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <search string="Books">
            <field name="name"/>
            <field name="isbn"/>
            <field name="author_id"/>
            <filter name="filter_available" string="Available"
                    domain="[('state', '=', 'available')]"/>
            <filter name="filter_loaned" string="Loaned"
                    domain="[('state', '=', 'loaned')]"/>
            <separator/>
            <filter name="filter_my_authors" string="My Authors"
                    domain="[('author_id.user_id', '=', uid)]"/>
            <group expand="0" string="Group By">
                <filter name="groupby_author" string="Author"
                        context="{'group_by': 'author_id'}"/>
                <filter name="groupby_state" string="State"
                        context="{'group_by': 'state'}"/>
            </group>
            <searchpanel>
                <field name="state" enable_counters="1"/>
            </searchpanel>
        </search>
    </field>
</record>
```

**FILTER VS DOMAIN ON THE ACTION:**

```text
Action domain     = always-on constraint (e.g. only active books in this menu)
Search filter     = optional facet the user toggles
Both AND together when the filter is on.
```

`uid` in domains is the current user id. Prefer named filters over teaching users to write domains by hand.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Search view with two field searches and one domain filter. |
| Medium | Add group-by for many2one + selection. |
| Hard | Searchpanel on state with counters; verify filter + action domain interaction. |

**WHAT THIS UNLOCKS NEXT:**

Search shapes *which* rows. Kanban and friends change *how* those rows are experienced.

---

## 24.4 Search Panels

A **search view** does not display records. It defines the control strip above list/kanban: fields to search, preset filters, and group-by options.

```xml
<record id="view_library_book_search" model="ir.ui.view">
    <field name="name">library.book.search</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <search string="Books">
            <field name="name"/>
            <field name="isbn"/>
            <field name="author_id"/>
            <filter name="filter_available" string="Available"
                    domain="[('state', '=', 'available')]"/>
            <filter name="filter_loaned" string="Loaned"
                    domain="[('state', '=', 'loaned')]"/>
            <separator/>
            <filter name="filter_my_authors" string="My Authors"
                    domain="[('author_id.user_id', '=', uid)]"/>
            <group expand="0" string="Group By">
                <filter name="groupby_author" string="Author"
                        context="{'group_by': 'author_id'}"/>
                <filter name="groupby_state" string="State"
                        context="{'group_by': 'state'}"/>
            </group>
            <searchpanel>
                <field name="state" enable_counters="1"/>
            </searchpanel>
        </search>
    </field>
</record>
```

**FILTER VS DOMAIN ON THE ACTION:**

```text
Action domain     = always-on constraint (e.g. only active books in this menu)
Search filter     = optional facet the user toggles
Both AND together when the filter is on.
```

`uid` in domains is the current user id. Prefer named filters over teaching users to write domains by hand.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Search view with two field searches and one domain filter. |
| Medium | Add group-by for many2one + selection. |
| Hard | Searchpanel on state with counters; verify filter + action domain interaction. |

**WHAT THIS UNLOCKS NEXT:**

Search shapes *which* rows. Kanban and friends change *how* those rows are experienced.

---

## 24.5 Kanban

**Kanban** — cards by stage (CRM DNA):

```xml
<kanban default_group_by="state" class="o_kanban_small_column">
    <field name="name"/>
    <field name="author_id"/>
    <field name="state"/>
    <templates>
        <t t-name="card">
            <field name="name" class="fw-bold"/>
            <field name="author_id"/>
        </t>
    </templates>
</kanban>
```

Drag between columns typically writes the group-by field — ensure that write is allowed by ACL/rules and business constraints.

**Calendar** — needs a date/datetime field:

```xml
<calendar date_start="due_date" color="author_id" mode="month">
    <field name="name"/>
</calendar>
```

**Pivot / Graph** — aggregates for managers:

```xml
<pivot string="Loans">
    <field name="author_id" type="row"/>
    <field name="state" type="col"/>
    <field name="id" type="measure"/>
</pivot>

<graph string="Books by State" type="bar">
    <field name="state"/>
    <field name="id" type="measure"/>
</graph>
```

Measures must be aggregable (stored numeric / countable). Computed non-stored fields often disappoint here — store or use SQL views when analytics matter.

**SAME MODEL, MANY ARCHES:**

```text
library.book
  ├── form     (edit one)
  ├── list     (scan many)
  ├── search   (filter/group controls)
  ├── kanban   (by state)
  ├── calendar (by due_date)
  ├── pivot    (counts by author/state)
  └── graph    (bar by state)

Window action (Phase 25) picks view_mode = "list,form,kanban,..."
```

**PRACTICE — MULTIPLE VIEWS OF SAME DATA:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add search + kanban to the Phase 23 model. |
| Medium | Calendar on a date field + list/form still working. |
| Hard | Pivot + graph that answer “how many loans per state this month?” |

**WHAT THIS UNLOCKS NEXT:**

Views sitting in Technical menus help developers only. Phase 25 wires **actions and menus** so normal users navigate.

---


---

## 24.6 Calendar

**Kanban** — cards by stage (CRM DNA):

```xml
<kanban default_group_by="state" class="o_kanban_small_column">
    <field name="name"/>
    <field name="author_id"/>
    <field name="state"/>
    <templates>
        <t t-name="card">
            <field name="name" class="fw-bold"/>
            <field name="author_id"/>
        </t>
    </templates>
</kanban>
```

Drag between columns typically writes the group-by field — ensure that write is allowed by ACL/rules and business constraints.

**Calendar** — needs a date/datetime field:

```xml
<calendar date_start="due_date" color="author_id" mode="month">
    <field name="name"/>
</calendar>
```

**Pivot / Graph** — aggregates for managers:

```xml
<pivot string="Loans">
    <field name="author_id" type="row"/>
    <field name="state" type="col"/>
    <field name="id" type="measure"/>
</pivot>

<graph string="Books by State" type="bar">
    <field name="state"/>
    <field name="id" type="measure"/>
</graph>
```

Measures must be aggregable (stored numeric / countable). Computed non-stored fields often disappoint here — store or use SQL views when analytics matter.

**SAME MODEL, MANY ARCHES:**

```text
library.book
  ├── form     (edit one)
  ├── list     (scan many)
  ├── search   (filter/group controls)
  ├── kanban   (by state)
  ├── calendar (by due_date)
  ├── pivot    (counts by author/state)
  └── graph    (bar by state)

Window action (Phase 25) picks view_mode = "list,form,kanban,..."
```

**PRACTICE — MULTIPLE VIEWS OF SAME DATA:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add search + kanban to the Phase 23 model. |
| Medium | Calendar on a date field + list/form still working. |
| Hard | Pivot + graph that answer “how many loans per state this month?” |

**WHAT THIS UNLOCKS NEXT:**

Views sitting in Technical menus help developers only. Phase 25 wires **actions and menus** so normal users navigate.

---


---

## 24.7 Pivot

**Kanban** — cards by stage (CRM DNA):

```xml
<kanban default_group_by="state" class="o_kanban_small_column">
    <field name="name"/>
    <field name="author_id"/>
    <field name="state"/>
    <templates>
        <t t-name="card">
            <field name="name" class="fw-bold"/>
            <field name="author_id"/>
        </t>
    </templates>
</kanban>
```

Drag between columns typically writes the group-by field — ensure that write is allowed by ACL/rules and business constraints.

**Calendar** — needs a date/datetime field:

```xml
<calendar date_start="due_date" color="author_id" mode="month">
    <field name="name"/>
</calendar>
```

**Pivot / Graph** — aggregates for managers:

```xml
<pivot string="Loans">
    <field name="author_id" type="row"/>
    <field name="state" type="col"/>
    <field name="id" type="measure"/>
</pivot>

<graph string="Books by State" type="bar">
    <field name="state"/>
    <field name="id" type="measure"/>
</graph>
```

Measures must be aggregable (stored numeric / countable). Computed non-stored fields often disappoint here — store or use SQL views when analytics matter.

**SAME MODEL, MANY ARCHES:**

```text
library.book
  ├── form     (edit one)
  ├── list     (scan many)
  ├── search   (filter/group controls)
  ├── kanban   (by state)
  ├── calendar (by due_date)
  ├── pivot    (counts by author/state)
  └── graph    (bar by state)

Window action (Phase 25) picks view_mode = "list,form,kanban,..."
```

**PRACTICE — MULTIPLE VIEWS OF SAME DATA:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add search + kanban to the Phase 23 model. |
| Medium | Calendar on a date field + list/form still working. |
| Hard | Pivot + graph that answer “how many loans per state this month?” |

**WHAT THIS UNLOCKS NEXT:**

Views sitting in Technical menus help developers only. Phase 25 wires **actions and menus** so normal users navigate.

---


---

## 24.8 Graph

**Kanban** — cards by stage (CRM DNA):

```xml
<kanban default_group_by="state" class="o_kanban_small_column">
    <field name="name"/>
    <field name="author_id"/>
    <field name="state"/>
    <templates>
        <t t-name="card">
            <field name="name" class="fw-bold"/>
            <field name="author_id"/>
        </t>
    </templates>
</kanban>
```

Drag between columns typically writes the group-by field — ensure that write is allowed by ACL/rules and business constraints.

**Calendar** — needs a date/datetime field:

```xml
<calendar date_start="due_date" color="author_id" mode="month">
    <field name="name"/>
</calendar>
```

**Pivot / Graph** — aggregates for managers:

```xml
<pivot string="Loans">
    <field name="author_id" type="row"/>
    <field name="state" type="col"/>
    <field name="id" type="measure"/>
</pivot>

<graph string="Books by State" type="bar">
    <field name="state"/>
    <field name="id" type="measure"/>
</graph>
```

Measures must be aggregable (stored numeric / countable). Computed non-stored fields often disappoint here — store or use SQL views when analytics matter.

**SAME MODEL, MANY ARCHES:**

```text
library.book
  ├── form     (edit one)
  ├── list     (scan many)
  ├── search   (filter/group controls)
  ├── kanban   (by state)
  ├── calendar (by due_date)
  ├── pivot    (counts by author/state)
  └── graph    (bar by state)

Window action (Phase 25) picks view_mode = "list,form,kanban,..."
```

**PRACTICE — MULTIPLE VIEWS OF SAME DATA:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add search + kanban to the Phase 23 model. |
| Medium | Calendar on a date field + list/form still working. |
| Hard | Pivot + graph that answer “how many loans per state this month?” |

**WHAT THIS UNLOCKS NEXT:**

Views sitting in Technical menus help developers only. Phase 25 wires **actions and menus** so normal users navigate.

---


---

## 24.9 Activity Views

A **search view** does not display records. It defines the control strip above list/kanban: fields to search, preset filters, and group-by options.

```xml
<record id="view_library_book_search" model="ir.ui.view">
    <field name="name">library.book.search</field>
    <field name="model">library.book</field>
    <field name="arch" type="xml">
        <search string="Books">
            <field name="name"/>
            <field name="isbn"/>
            <field name="author_id"/>
            <filter name="filter_available" string="Available"
                    domain="[('state', '=', 'available')]"/>
            <filter name="filter_loaned" string="Loaned"
                    domain="[('state', '=', 'loaned')]"/>
            <separator/>
            <filter name="filter_my_authors" string="My Authors"
                    domain="[('author_id.user_id', '=', uid)]"/>
            <group expand="0" string="Group By">
                <filter name="groupby_author" string="Author"
                        context="{'group_by': 'author_id'}"/>
                <filter name="groupby_state" string="State"
                        context="{'group_by': 'state'}"/>
            </group>
            <searchpanel>
                <field name="state" enable_counters="1"/>
            </searchpanel>
        </search>
    </field>
</record>
```

**FILTER VS DOMAIN ON THE ACTION:**

```text
Action domain     = always-on constraint (e.g. only active books in this menu)
Search filter     = optional facet the user toggles
Both AND together when the filter is on.
```

`uid` in domains is the current user id. Prefer named filters over teaching users to write domains by hand.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Search view with two field searches and one domain filter. |
| Medium | Add group-by for many2one + selection. |
| Hard | Searchpanel on state with counters; verify filter + action domain interaction. |

**WHAT THIS UNLOCKS NEXT:**

Search shapes *which* rows. Kanban and friends change *how* those rows are experienced.

---

## 24.10 PRACTICE — Multiple Views of Same Data

**PRACTICE — Multiple Views of Same Data** belongs to **Search, Kanban & Analytical Views**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **PRACTICE — Multiple Views of Same Data** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **PRACTICE — Multiple Views of Same Data** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **PRACTICE — Multiple Views of Same Data** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 24:** You can explain every lesson title in Phase 24 with a concrete Odoo or business example.

> **Phase 24 complete?** [Continue to Phase 25](#phase-25)


<a id="phase-25"></a>
# PHASE 25 - Actions & Menus

**Track:** User Interface

**WHAT YOU WILL BE ABLE TO DO:** Connect models to window actions and menu trees with domains, contexts, and view modes so navigation opens the right records in the right views.

**WHAT YOU SHOULD KNOW FIRST:** Phases 22–24 — XML IDs, form/list/search/kanban views.

**LEARNING RESOURCES:**

- [View architectures + actions usage](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [Menus and actions](https://www.youtube.com/watch?v=mzg3EGD_6Gw)
- [odoo/odoo — menu XML in addons](https://github.com/odoo/odoo)

## 25.1 Actions

`ir.actions.act_window` is the glue: “open this model, with these views, under this domain, with this context.”

```xml
<record id="action_library_book" model="ir.actions.act_window">
    <field name="name">Books</field>
    <field name="res_model">library.book</field>
    <field name="view_mode">list,form,kanban,graph</field>
    <field name="search_view_id" ref="view_library_book_search"/>
    <field name="domain">[('active', '=', True)]</field>
    <field name="context">{
        {
            'search_default_filter_available': 1,
            'default_state': 'draft',
        }
    </field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create your first book.
        </p>
    </field>
</record>
```

**DOMAIN VS CONTEXT:**

| Mechanism | Effect |
| --------- | ------ |
| `domain` on action | Permanent filter for this menu entry |
| `search_default_<filter_name>` | Turns on a search filter by default |
| `default_<field>` | Prefills new records |
| `group_by` in context | Opens already grouped |

```text
Menu "Available Books"
  action.domain = [('state','=','available')]

Menu "All Books"
  action.domain = []
  search_default_filter_available optional

Same model, different windows — do not fork models for menu filters.
```

You can pin explicit view IDs:

```xml
<field name="view_ids" eval="[
    (5, 0, 0),
    (0, 0, {'view_mode': 'list', 'view_id': ref('view_library_book_list')}),
    (0, 0, {'view_mode': 'form', 'view_id': ref('view_library_book_form')}),
]"/>
```

Usually `view_mode` + lowest-sequence views for the model are enough.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `act_window` with `list,form` and empty domain. |
| Medium | Two actions on the same model with different domains/contexts. |
| Hard | `search_default_` + `default_state` and prove both in the UI. |

**WHAT THIS UNLOCKS NEXT:**

Actions without menus are orphan technical records. Menus place them in the app switcher / sidebar.

---

## 25.2 Action Bindings

**Action Bindings** belongs to **Actions & Menus**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Action Bindings** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Action Bindings** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Action Bindings**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Action Bindings** is concrete, continue — the next lesson in this phase still depends on it.

## 25.3 Window Actions

`ir.actions.act_window` is the glue: “open this model, with these views, under this domain, with this context.”

```xml
<record id="action_library_book" model="ir.actions.act_window">
    <field name="name">Books</field>
    <field name="res_model">library.book</field>
    <field name="view_mode">list,form,kanban,graph</field>
    <field name="search_view_id" ref="view_library_book_search"/>
    <field name="domain">[('active', '=', True)]</field>
    <field name="context">{
        {
            'search_default_filter_available': 1,
            'default_state': 'draft',
        }
    </field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create your first book.
        </p>
    </field>
</record>
```

**DOMAIN VS CONTEXT:**

| Mechanism | Effect |
| --------- | ------ |
| `domain` on action | Permanent filter for this menu entry |
| `search_default_<filter_name>` | Turns on a search filter by default |
| `default_<field>` | Prefills new records |
| `group_by` in context | Opens already grouped |

```text
Menu "Available Books"
  action.domain = [('state','=','available')]

Menu "All Books"
  action.domain = []
  search_default_filter_available optional

Same model, different windows — do not fork models for menu filters.
```

You can pin explicit view IDs:

```xml
<field name="view_ids" eval="[
    (5, 0, 0),
    (0, 0, {'view_mode': 'list', 'view_id': ref('view_library_book_list')}),
    (0, 0, {'view_mode': 'form', 'view_id': ref('view_library_book_form')}),
]"/>
```

Usually `view_mode` + lowest-sequence views for the model are enough.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `act_window` with `list,form` and empty domain. |
| Medium | Two actions on the same model with different domains/contexts. |
| Hard | `search_default_` + `default_state` and prove both in the UI. |

**WHAT THIS UNLOCKS NEXT:**

Actions without menus are orphan technical records. Menus place them in the app switcher / sidebar.

---

## 25.4 ir.actions.act_window

`ir.actions.act_window` is the glue: “open this model, with these views, under this domain, with this context.”

```xml
<record id="action_library_book" model="ir.actions.act_window">
    <field name="name">Books</field>
    <field name="res_model">library.book</field>
    <field name="view_mode">list,form,kanban,graph</field>
    <field name="search_view_id" ref="view_library_book_search"/>
    <field name="domain">[('active', '=', True)]</field>
    <field name="context">{
        {
            'search_default_filter_available': 1,
            'default_state': 'draft',
        }
    </field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create your first book.
        </p>
    </field>
</record>
```

**DOMAIN VS CONTEXT:**

| Mechanism | Effect |
| --------- | ------ |
| `domain` on action | Permanent filter for this menu entry |
| `search_default_<filter_name>` | Turns on a search filter by default |
| `default_<field>` | Prefills new records |
| `group_by` in context | Opens already grouped |

```text
Menu "Available Books"
  action.domain = [('state','=','available')]

Menu "All Books"
  action.domain = []
  search_default_filter_available optional

Same model, different windows — do not fork models for menu filters.
```

You can pin explicit view IDs:

```xml
<field name="view_ids" eval="[
    (5, 0, 0),
    (0, 0, {'view_mode': 'list', 'view_id': ref('view_library_book_list')}),
    (0, 0, {'view_mode': 'form', 'view_id': ref('view_library_book_form')}),
]"/>
```

Usually `view_mode` + lowest-sequence views for the model are enough.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `act_window` with `list,form` and empty domain. |
| Medium | Two actions on the same model with different domains/contexts. |
| Hard | `search_default_` + `default_state` and prove both in the UI. |

**WHAT THIS UNLOCKS NEXT:**

Actions without menus are orphan technical records. Menus place them in the app switcher / sidebar.

---

## 25.5 URL Actions

`ir.actions.act_window` is the glue: “open this model, with these views, under this domain, with this context.”

```xml
<record id="action_library_book" model="ir.actions.act_window">
    <field name="name">Books</field>
    <field name="res_model">library.book</field>
    <field name="view_mode">list,form,kanban,graph</field>
    <field name="search_view_id" ref="view_library_book_search"/>
    <field name="domain">[('active', '=', True)]</field>
    <field name="context">{
        {
            'search_default_filter_available': 1,
            'default_state': 'draft',
        }
    </field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create your first book.
        </p>
    </field>
</record>
```

**DOMAIN VS CONTEXT:**

| Mechanism | Effect |
| --------- | ------ |
| `domain` on action | Permanent filter for this menu entry |
| `search_default_<filter_name>` | Turns on a search filter by default |
| `default_<field>` | Prefills new records |
| `group_by` in context | Opens already grouped |

```text
Menu "Available Books"
  action.domain = [('state','=','available')]

Menu "All Books"
  action.domain = []
  search_default_filter_available optional

Same model, different windows — do not fork models for menu filters.
```

You can pin explicit view IDs:

```xml
<field name="view_ids" eval="[
    (5, 0, 0),
    (0, 0, {'view_mode': 'list', 'view_id': ref('view_library_book_list')}),
    (0, 0, {'view_mode': 'form', 'view_id': ref('view_library_book_form')}),
]"/>
```

Usually `view_mode` + lowest-sequence views for the model are enough.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `act_window` with `list,form` and empty domain. |
| Medium | Two actions on the same model with different domains/contexts. |
| Hard | `search_default_` + `default_state` and prove both in the UI. |

**WHAT THIS UNLOCKS NEXT:**

Actions without menus are orphan technical records. Menus place them in the app switcher / sidebar.

---

## 25.6 Server Actions

`ir.actions.act_window` is the glue: “open this model, with these views, under this domain, with this context.”

```xml
<record id="action_library_book" model="ir.actions.act_window">
    <field name="name">Books</field>
    <field name="res_model">library.book</field>
    <field name="view_mode">list,form,kanban,graph</field>
    <field name="search_view_id" ref="view_library_book_search"/>
    <field name="domain">[('active', '=', True)]</field>
    <field name="context">{
        {
            'search_default_filter_available': 1,
            'default_state': 'draft',
        }
    </field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create your first book.
        </p>
    </field>
</record>
```

**DOMAIN VS CONTEXT:**

| Mechanism | Effect |
| --------- | ------ |
| `domain` on action | Permanent filter for this menu entry |
| `search_default_<filter_name>` | Turns on a search filter by default |
| `default_<field>` | Prefills new records |
| `group_by` in context | Opens already grouped |

```text
Menu "Available Books"
  action.domain = [('state','=','available')]

Menu "All Books"
  action.domain = []
  search_default_filter_available optional

Same model, different windows — do not fork models for menu filters.
```

You can pin explicit view IDs:

```xml
<field name="view_ids" eval="[
    (5, 0, 0),
    (0, 0, {'view_mode': 'list', 'view_id': ref('view_library_book_list')}),
    (0, 0, {'view_mode': 'form', 'view_id': ref('view_library_book_form')}),
]"/>
```

Usually `view_mode` + lowest-sequence views for the model are enough.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `act_window` with `list,form` and empty domain. |
| Medium | Two actions on the same model with different domains/contexts. |
| Hard | `search_default_` + `default_state` and prove both in the UI. |

**WHAT THIS UNLOCKS NEXT:**

Actions without menus are orphan technical records. Menus place them in the app switcher / sidebar.

---

## 25.7 Report Actions

`ir.actions.act_window` is the glue: “open this model, with these views, under this domain, with this context.”

```xml
<record id="action_library_book" model="ir.actions.act_window">
    <field name="name">Books</field>
    <field name="res_model">library.book</field>
    <field name="view_mode">list,form,kanban,graph</field>
    <field name="search_view_id" ref="view_library_book_search"/>
    <field name="domain">[('active', '=', True)]</field>
    <field name="context">{
        {
            'search_default_filter_available': 1,
            'default_state': 'draft',
        }
    </field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create your first book.
        </p>
    </field>
</record>
```

**DOMAIN VS CONTEXT:**

| Mechanism | Effect |
| --------- | ------ |
| `domain` on action | Permanent filter for this menu entry |
| `search_default_<filter_name>` | Turns on a search filter by default |
| `default_<field>` | Prefills new records |
| `group_by` in context | Opens already grouped |

```text
Menu "Available Books"
  action.domain = [('state','=','available')]

Menu "All Books"
  action.domain = []
  search_default_filter_available optional

Same model, different windows — do not fork models for menu filters.
```

You can pin explicit view IDs:

```xml
<field name="view_ids" eval="[
    (5, 0, 0),
    (0, 0, {'view_mode': 'list', 'view_id': ref('view_library_book_list')}),
    (0, 0, {'view_mode': 'form', 'view_id': ref('view_library_book_form')}),
]"/>
```

Usually `view_mode` + lowest-sequence views for the model are enough.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `act_window` with `list,form` and empty domain. |
| Medium | Two actions on the same model with different domains/contexts. |
| Hard | `search_default_` + `default_state` and prove both in the UI. |

**WHAT THIS UNLOCKS NEXT:**

Actions without menus are orphan technical records. Menus place them in the app switcher / sidebar.

---

## 25.8 Client Actions

`ir.actions.act_window` is the glue: “open this model, with these views, under this domain, with this context.”

```xml
<record id="action_library_book" model="ir.actions.act_window">
    <field name="name">Books</field>
    <field name="res_model">library.book</field>
    <field name="view_mode">list,form,kanban,graph</field>
    <field name="search_view_id" ref="view_library_book_search"/>
    <field name="domain">[('active', '=', True)]</field>
    <field name="context">{
        {
            'search_default_filter_available': 1,
            'default_state': 'draft',
        }
    </field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create your first book.
        </p>
    </field>
</record>
```

**DOMAIN VS CONTEXT:**

| Mechanism | Effect |
| --------- | ------ |
| `domain` on action | Permanent filter for this menu entry |
| `search_default_<filter_name>` | Turns on a search filter by default |
| `default_<field>` | Prefills new records |
| `group_by` in context | Opens already grouped |

```text
Menu "Available Books"
  action.domain = [('state','=','available')]

Menu "All Books"
  action.domain = []
  search_default_filter_available optional

Same model, different windows — do not fork models for menu filters.
```

You can pin explicit view IDs:

```xml
<field name="view_ids" eval="[
    (5, 0, 0),
    (0, 0, {'view_mode': 'list', 'view_id': ref('view_library_book_list')}),
    (0, 0, {'view_mode': 'form', 'view_id': ref('view_library_book_form')}),
]"/>
```

Usually `view_mode` + lowest-sequence views for the model are enough.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `act_window` with `list,form` and empty domain. |
| Medium | Two actions on the same model with different domains/contexts. |
| Hard | `search_default_` + `default_state` and prove both in the UI. |

**WHAT THIS UNLOCKS NEXT:**

Actions without menus are orphan technical records. Menus place them in the app switcher / sidebar.

---

## 25.9 Scheduled Actions

`ir.actions.act_window` is the glue: “open this model, with these views, under this domain, with this context.”

```xml
<record id="action_library_book" model="ir.actions.act_window">
    <field name="name">Books</field>
    <field name="res_model">library.book</field>
    <field name="view_mode">list,form,kanban,graph</field>
    <field name="search_view_id" ref="view_library_book_search"/>
    <field name="domain">[('active', '=', True)]</field>
    <field name="context">{
        {
            'search_default_filter_available': 1,
            'default_state': 'draft',
        }
    </field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create your first book.
        </p>
    </field>
</record>
```

**DOMAIN VS CONTEXT:**

| Mechanism | Effect |
| --------- | ------ |
| `domain` on action | Permanent filter for this menu entry |
| `search_default_<filter_name>` | Turns on a search filter by default |
| `default_<field>` | Prefills new records |
| `group_by` in context | Opens already grouped |

```text
Menu "Available Books"
  action.domain = [('state','=','available')]

Menu "All Books"
  action.domain = []
  search_default_filter_available optional

Same model, different windows — do not fork models for menu filters.
```

You can pin explicit view IDs:

```xml
<field name="view_ids" eval="[
    (5, 0, 0),
    (0, 0, {'view_mode': 'list', 'view_id': ref('view_library_book_list')}),
    (0, 0, {'view_mode': 'form', 'view_id': ref('view_library_book_form')}),
]"/>
```

Usually `view_mode` + lowest-sequence views for the model are enough.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `act_window` with `list,form` and empty domain. |
| Medium | Two actions on the same model with different domains/contexts. |
| Hard | `search_default_` + `default_state` and prove both in the UI. |

**WHAT THIS UNLOCKS NEXT:**

Actions without menus are orphan technical records. Menus place them in the app switcher / sidebar.

---

## 25.10 Menus

```xml
<menuitem id="menu_library_root"
          name="Library"
          sequence="10"
          web_icon="library_app,static/description/icon.png"/>

<menuitem id="menu_library_books"
          name="Books"
          parent="menu_library_root"
          action="action_library_book"
          sequence="10"
          groups="library_app.group_library_user"/>
```

- Root menuitem without `parent` becomes an app (with `web_icon`).
- Child menus usually set `action=...`.
- **`groups=` on menus hides navigation** — it is **visibility**, not security. RPC and ORM still need ACL/record rules (Story VIII).

**SEQUENCE AND PARENT CHAINS:**

```text
Library (root)
  ├── Books          -> action_library_book
  ├── Authors        -> action_library_author
  └── Configuration
        └── Book Categories -> action_library_category
```

Keep Configuration nested; do not dump every action at root.

**SERVER ACTIONS / BINDINGS (PREVIEW):**

Actions can bind to the Action menu on a model (`binding_model_id`). Full wizard flows arrive in Story X; for now know that not every action is a window action.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Root + one child menu opening your books action. |
| Medium | Nested Configuration menu with groups restricted to managers. |
| Hard | Two menus, one domain-limited; verify a user without the group cannot see the manager menu (still not enough for data security). |

**WHAT THIS UNLOCKS NEXT:**

Your own screens work. Real projects extend **standard** screens without copying them — Phase 26 is view inheritance with XPath.

---


---

## 25.11 Context

`ir.actions.act_window` is the glue: “open this model, with these views, under this domain, with this context.”

```xml
<record id="action_library_book" model="ir.actions.act_window">
    <field name="name">Books</field>
    <field name="res_model">library.book</field>
    <field name="view_mode">list,form,kanban,graph</field>
    <field name="search_view_id" ref="view_library_book_search"/>
    <field name="domain">[('active', '=', True)]</field>
    <field name="context">{
        {
            'search_default_filter_available': 1,
            'default_state': 'draft',
        }
    </field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create your first book.
        </p>
    </field>
</record>
```

**DOMAIN VS CONTEXT:**

| Mechanism | Effect |
| --------- | ------ |
| `domain` on action | Permanent filter for this menu entry |
| `search_default_<filter_name>` | Turns on a search filter by default |
| `default_<field>` | Prefills new records |
| `group_by` in context | Opens already grouped |

```text
Menu "Available Books"
  action.domain = [('state','=','available')]

Menu "All Books"
  action.domain = []
  search_default_filter_available optional

Same model, different windows — do not fork models for menu filters.
```

You can pin explicit view IDs:

```xml
<field name="view_ids" eval="[
    (5, 0, 0),
    (0, 0, {'view_mode': 'list', 'view_id': ref('view_library_book_list')}),
    (0, 0, {'view_mode': 'form', 'view_id': ref('view_library_book_form')}),
]"/>
```

Usually `view_mode` + lowest-sequence views for the model are enough.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `act_window` with `list,form` and empty domain. |
| Medium | Two actions on the same model with different domains/contexts. |
| Hard | `search_default_` + `default_state` and prove both in the UI. |

**WHAT THIS UNLOCKS NEXT:**

Actions without menus are orphan technical records. Menus place them in the app switcher / sidebar.

---

## 25.12 Domains

**Domains** belongs to **Actions & Menus**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Domains** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Domains** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Domains**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Domains** is concrete, continue — the next lesson in this phase still depends on it.

## 25.13 Navigation

```xml
<menuitem id="menu_library_root"
          name="Library"
          sequence="10"
          web_icon="library_app,static/description/icon.png"/>

<menuitem id="menu_library_books"
          name="Books"
          parent="menu_library_root"
          action="action_library_book"
          sequence="10"
          groups="library_app.group_library_user"/>
```

- Root menuitem without `parent` becomes an app (with `web_icon`).
- Child menus usually set `action=...`.
- **`groups=` on menus hides navigation** — it is **visibility**, not security. RPC and ORM still need ACL/record rules (Story VIII).

**SEQUENCE AND PARENT CHAINS:**

```text
Library (root)
  ├── Books          -> action_library_book
  ├── Authors        -> action_library_author
  └── Configuration
        └── Book Categories -> action_library_category
```

Keep Configuration nested; do not dump every action at root.

**SERVER ACTIONS / BINDINGS (PREVIEW):**

Actions can bind to the Action menu on a model (`binding_model_id`). Full wizard flows arrive in Story X; for now know that not every action is a window action.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Root + one child menu opening your books action. |
| Medium | Nested Configuration menu with groups restricted to managers. |
| Hard | Two menus, one domain-limited; verify a user without the group cannot see the manager menu (still not enough for data security). |

**WHAT THIS UNLOCKS NEXT:**

Your own screens work. Real projects extend **standard** screens without copying them — Phase 26 is view inheritance with XPath.

---


---


<a id="phase-26"></a>
<a id="phase-26---view-inheritance"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 25:** You can explain every lesson title in Phase 25 with a concrete Odoo or business example.

> **Phase 25 complete?** [Continue to Phase 26](#phase-26)


<a id="phase-26"></a>
# PHASE 26 - View Inheritance

**Track:** User Interface

**WHAT YOU WILL BE ABLE TO DO:** Extend existing `ir.ui.view` arches with `inherit_id` and XPath `position` semantics so upgrades do not require forked copies of official forms.

**WHAT YOU SHOULD KNOW FIRST:** Phases 22–25 — you can ship a full custom UI. Official apps already ship theirs.

**LEARNING RESOURCES:**

- [View architectures — inheritance](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [XPath inheritance deep dive](https://www.youtube.com/watch?v=hwfhO2IGhmc)
- [odoo/odoo — inherit patterns in sale/purchase](https://github.com/odoo/odoo)

## 26.1 inherit_id

View inheritance creates a new `ir.ui.view` row that **patches** a parent arch. Odoo merges patches in module dependency order into the arch the client receives.

```xml
<record id="view_partner_form_library" model="ir.ui.view">
    <field name="name">res.partner.form.library</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">
        <xpath expr="//field[@name='phone']" position="after">
            <field name="library_card_number"/>
        </xpath>
    </field>
</record>
```

**POSITIONS:**

| `position` | Effect |
| ---------- | ------ |
| `inside` | Append as last child of matched node |
| `before` / `after` | Sibling insertion |
| `replace` | Swap the matched node for your content |
| `attributes` | Change XML attributes without replacing the node |
| `move` | Relocate an existing node (Odoo version-aware; use carefully) |

```xml
<xpath expr="//field[@name='email']" position="attributes">
    <attribute name="required">1</attribute>
</xpath>
```

**XPATH DISCIPLINE:**

```text
Brittle:  //group[3]/field[2]
Stable:   //field[@name='partner_id']
Better:   //page[@name='order_lines']//field[@name='product_id']
```

Prefer anchors with `@name` on pages/fields. Index-based paths break when upstream inserts a group.

**MERGE PICTURE:**

```text
base.view_partner_form
        +
library.view_partner_form_library   (after phone)
        +
another.extension                   (inside sheet)
        =
single merged arch --> one form in UI
```

If XPath matches nothing, `-u` raises — fix the expr, do not catch and ignore.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Inherit partner form; add a Char after `phone`. |
| Medium | `position="attributes"` to make a field readonly when appropriate. |
| Hard | Two modules inherit the same anchor; predict merge order from `depends`. |

**WHAT THIS UNLOCKS NEXT:**

Surgical patches scale only if you practice on real density — next, apply inheritance as the primary way to customize, then close Story VII with a full UI project.

---

## 26.2 XPath

View inheritance creates a new `ir.ui.view` row that **patches** a parent arch. Odoo merges patches in module dependency order into the arch the client receives.

```xml
<record id="view_partner_form_library" model="ir.ui.view">
    <field name="name">res.partner.form.library</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">
        <xpath expr="//field[@name='phone']" position="after">
            <field name="library_card_number"/>
        </xpath>
    </field>
</record>
```

**POSITIONS:**

| `position` | Effect |
| ---------- | ------ |
| `inside` | Append as last child of matched node |
| `before` / `after` | Sibling insertion |
| `replace` | Swap the matched node for your content |
| `attributes` | Change XML attributes without replacing the node |
| `move` | Relocate an existing node (Odoo version-aware; use carefully) |

```xml
<xpath expr="//field[@name='email']" position="attributes">
    <attribute name="required">1</attribute>
</xpath>
```

**XPATH DISCIPLINE:**

```text
Brittle:  //group[3]/field[2]
Stable:   //field[@name='partner_id']
Better:   //page[@name='order_lines']//field[@name='product_id']
```

Prefer anchors with `@name` on pages/fields. Index-based paths break when upstream inserts a group.

**MERGE PICTURE:**

```text
base.view_partner_form
        +
library.view_partner_form_library   (after phone)
        +
another.extension                   (inside sheet)
        =
single merged arch --> one form in UI
```

If XPath matches nothing, `-u` raises — fix the expr, do not catch and ignore.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Inherit partner form; add a Char after `phone`. |
| Medium | `position="attributes"` to make a field readonly when appropriate. |
| Hard | Two modules inherit the same anchor; predict merge order from `depends`. |

**WHAT THIS UNLOCKS NEXT:**

Surgical patches scale only if you practice on real density — next, apply inheritance as the primary way to customize, then close Story VII with a full UI project.

---

## 26.3 position

View inheritance creates a new `ir.ui.view` row that **patches** a parent arch. Odoo merges patches in module dependency order into the arch the client receives.

```xml
<record id="view_partner_form_library" model="ir.ui.view">
    <field name="name">res.partner.form.library</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">
        <xpath expr="//field[@name='phone']" position="after">
            <field name="library_card_number"/>
        </xpath>
    </field>
</record>
```

**POSITIONS:**

| `position` | Effect |
| ---------- | ------ |
| `inside` | Append as last child of matched node |
| `before` / `after` | Sibling insertion |
| `replace` | Swap the matched node for your content |
| `attributes` | Change XML attributes without replacing the node |
| `move` | Relocate an existing node (Odoo version-aware; use carefully) |

```xml
<xpath expr="//field[@name='email']" position="attributes">
    <attribute name="required">1</attribute>
</xpath>
```

**XPATH DISCIPLINE:**

```text
Brittle:  //group[3]/field[2]
Stable:   //field[@name='partner_id']
Better:   //page[@name='order_lines']//field[@name='product_id']
```

Prefer anchors with `@name` on pages/fields. Index-based paths break when upstream inserts a group.

**MERGE PICTURE:**

```text
base.view_partner_form
        +
library.view_partner_form_library   (after phone)
        +
another.extension                   (inside sheet)
        =
single merged arch --> one form in UI
```

If XPath matches nothing, `-u` raises — fix the expr, do not catch and ignore.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Inherit partner form; add a Char after `phone`. |
| Medium | `position="attributes"` to make a field readonly when appropriate. |
| Hard | Two modules inherit the same anchor; predict merge order from `depends`. |

**WHAT THIS UNLOCKS NEXT:**

Surgical patches scale only if you practice on real density — next, apply inheritance as the primary way to customize, then close Story VII with a full UI project.

---

## 26.4 Attributes

**Attributes** belongs to **View Inheritance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Attributes** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Attributes** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Attributes**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Attributes** is concrete, continue — the next lesson in this phase still depends on it.

## 26.5 Adding Elements

**Adding Elements** belongs to **View Inheritance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Adding Elements** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Adding Elements** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Adding Elements**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Adding Elements** is concrete, continue — the next lesson in this phase still depends on it.

## 26.6 Replacing Elements

**Replacing Elements** belongs to **View Inheritance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Replacing Elements** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Replacing Elements** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Replacing Elements**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Replacing Elements** is concrete, continue — the next lesson in this phase still depends on it.

## 26.7 Moving Elements

**Moving Elements** belongs to **View Inheritance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Moving Elements** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Moving Elements** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Moving Elements**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Moving Elements** is concrete, continue — the next lesson in this phase still depends on it.

## 26.8 View Resolution

**View Resolution** belongs to **View Inheritance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **View Resolution** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **View Resolution** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **View Resolution**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **View Resolution** is concrete, continue — the next lesson in this phase still depends on it.

## 26.9 Debugging Inherited Views

**Debugging Inherited Views** belongs to **View Inheritance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Debugging Inherited Views** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Debugging Inherited Views** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Debugging Inherited Views**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Debugging Inherited Views** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 26:** You can explain every lesson title in Phase 26 with a concrete Odoo or business example.

> **Phase 26 complete?** [Continue to Phase 27](#phase-27)


## STORY PROJECT — Complete Odoo Application UI

**Track:** User Interface

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY VIII — SECURITY & MULTI-COMPANY

**Track:** Security

*If the wrong user can see or change the wrong row, the module is unfinished.*

---
<a id="phase-27"></a>
<a id="phase-27---users-groups"></a>
# PHASE 27 - Users & Groups

**Track:** Security

**WHAT YOU WILL BE ABLE TO DO:** Model responsibility with `res.groups` hierarchies (`implied_ids`), assign users correctly, and separate UI visibility groups from privilege design.

**WHAT YOU SHOULD KNOW FIRST:** Story VII — menus/views exist. Stories IV–VI — models hold data worth protecting.

**LEARNING RESOURCES:**

- [Access rights (applications)](https://www.odoo.com/documentation/18.0/applications/general/users/access_rights.html)
- [Security reference](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [Server Framework 101 — security intro](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/04_securityintro.html)
- [Restrict data access tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/restrict_data_access.html)
- [Users & groups walkthrough](https://www.youtube.com/watch?v=mzg3EGD_6Gw)

## 27.1 Users

In Odoo, a **user** (`res.users`) is a login bound to a partner. **Groups** (`res.groups`) are roles. Permissions attach to groups; users inherit the union of their groups’ rights (plus implied groups).

```xml
<record id="module_category_library" model="ir.module.category">
    <field name="name">Library</field>
    <field name="sequence">30</field>
</record>

<record id="group_library_user" model="res.groups">
    <field name="name">User</field>
    <field name="category_id" ref="module_category_library"/>
    <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
</record>

<record id="group_library_manager" model="res.groups">
    <field name="name">Administrator</field>
    <field name="category_id" ref="module_category_library"/>
    <field name="implied_ids" eval="[(4, ref('group_library_user'))]"/>
    <field name="users" eval="[(4, ref('base.user_admin'))]"/>
</record>
```

**IMPLIED_IDS CASCADE:**

```text
Library / Administrator
    implies --> Library / User
                    implies --> Internal User (base.group_user)

Grant Manager => automatically receives User rights.
Do not duplicate every ACL row for Manager if User already has the baseline
(still common to give Manager extra unlink, etc.).
```

**CATEGORIES** drive the Settings → Users access rights matrix UI (Application section). Without a category, groups still work but are harder to administer.

**USERS VS SETTINGS:**

- Prefer granting groups in data for system users; humans get groups via the Users form.
- `base.group_system` (Settings) is extremely privileged — not a substitute for app manager groups.
- Portal / public users are separate (`base.group_portal`, public) — do not casually imply them into internal ACL.

**UI GROUPS VS SECURITY GROUPS:**

```text
menuitem groups="library.group_library_manager"
button  groups="library.group_library_manager"

  => hides widgets for non-members

ir.model.access / ir.rule still decide whether RPC succeeds.
Hidden button + open ACL = attacker calls method anyway.
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Define User + Manager groups with `implied_ids`. |
| Medium | Put groups in an `ir.module.category` and assign a test user only User. |
| Hard | Map every menu/button `groups=` and confirm each has a matching ACL story. |

**WHAT THIS UNLOCKS NEXT:**

Groups without ACL rows often mean AccessError on first click — Phase 28 adds **model-level** CRUD rights.

---


---

## 27.2 Groups

In Odoo, a **user** (`res.users`) is a login bound to a partner. **Groups** (`res.groups`) are roles. Permissions attach to groups; users inherit the union of their groups’ rights (plus implied groups).

```xml
<record id="module_category_library" model="ir.module.category">
    <field name="name">Library</field>
    <field name="sequence">30</field>
</record>

<record id="group_library_user" model="res.groups">
    <field name="name">User</field>
    <field name="category_id" ref="module_category_library"/>
    <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
</record>

<record id="group_library_manager" model="res.groups">
    <field name="name">Administrator</field>
    <field name="category_id" ref="module_category_library"/>
    <field name="implied_ids" eval="[(4, ref('group_library_user'))]"/>
    <field name="users" eval="[(4, ref('base.user_admin'))]"/>
</record>
```

**IMPLIED_IDS CASCADE:**

```text
Library / Administrator
    implies --> Library / User
                    implies --> Internal User (base.group_user)

Grant Manager => automatically receives User rights.
Do not duplicate every ACL row for Manager if User already has the baseline
(still common to give Manager extra unlink, etc.).
```

**CATEGORIES** drive the Settings → Users access rights matrix UI (Application section). Without a category, groups still work but are harder to administer.

**USERS VS SETTINGS:**

- Prefer granting groups in data for system users; humans get groups via the Users form.
- `base.group_system` (Settings) is extremely privileged — not a substitute for app manager groups.
- Portal / public users are separate (`base.group_portal`, public) — do not casually imply them into internal ACL.

**UI GROUPS VS SECURITY GROUPS:**

```text
menuitem groups="library.group_library_manager"
button  groups="library.group_library_manager"

  => hides widgets for non-members

ir.model.access / ir.rule still decide whether RPC succeeds.
Hidden button + open ACL = attacker calls method anyway.
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Define User + Manager groups with `implied_ids`. |
| Medium | Put groups in an `ir.module.category` and assign a test user only User. |
| Hard | Map every menu/button `groups=` and confirm each has a matching ACL story. |

**WHAT THIS UNLOCKS NEXT:**

Groups without ACL rows often mean AccessError on first click — Phase 28 adds **model-level** CRUD rights.

---


---

## 27.3 Group Membership

**Group Membership** belongs to **Users & Groups**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Group Membership** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Group Membership** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Group Membership**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Group Membership** is concrete, continue — the next lesson in this phase still depends on it.

## 27.4 Implied Groups

In Odoo, a **user** (`res.users`) is a login bound to a partner. **Groups** (`res.groups`) are roles. Permissions attach to groups; users inherit the union of their groups’ rights (plus implied groups).

```xml
<record id="module_category_library" model="ir.module.category">
    <field name="name">Library</field>
    <field name="sequence">30</field>
</record>

<record id="group_library_user" model="res.groups">
    <field name="name">User</field>
    <field name="category_id" ref="module_category_library"/>
    <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
</record>

<record id="group_library_manager" model="res.groups">
    <field name="name">Administrator</field>
    <field name="category_id" ref="module_category_library"/>
    <field name="implied_ids" eval="[(4, ref('group_library_user'))]"/>
    <field name="users" eval="[(4, ref('base.user_admin'))]"/>
</record>
```

**IMPLIED_IDS CASCADE:**

```text
Library / Administrator
    implies --> Library / User
                    implies --> Internal User (base.group_user)

Grant Manager => automatically receives User rights.
Do not duplicate every ACL row for Manager if User already has the baseline
(still common to give Manager extra unlink, etc.).
```

**CATEGORIES** drive the Settings → Users access rights matrix UI (Application section). Without a category, groups still work but are harder to administer.

**USERS VS SETTINGS:**

- Prefer granting groups in data for system users; humans get groups via the Users form.
- `base.group_system` (Settings) is extremely privileged — not a substitute for app manager groups.
- Portal / public users are separate (`base.group_portal`, public) — do not casually imply them into internal ACL.

**UI GROUPS VS SECURITY GROUPS:**

```text
menuitem groups="library.group_library_manager"
button  groups="library.group_library_manager"

  => hides widgets for non-members

ir.model.access / ir.rule still decide whether RPC succeeds.
Hidden button + open ACL = attacker calls method anyway.
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Define User + Manager groups with `implied_ids`. |
| Medium | Put groups in an `ir.module.category` and assign a test user only User. |
| Hard | Map every menu/button `groups=` and confirm each has a matching ACL story. |

**WHAT THIS UNLOCKS NEXT:**

Groups without ACL rows often mean AccessError on first click — Phase 28 adds **model-level** CRUD rights.

---


---

## 27.5 Application Categories

**Application Categories** belongs to **Users & Groups**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Application Categories** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Application Categories** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Application Categories**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Application Categories** is concrete, continue — the next lesson in this phase still depends on it.

## 27.6 Permission Design

**Permission Design** belongs to **Users & Groups**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Permission Design** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Permission Design** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Permission Design**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Permission Design** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 27:** You can explain every lesson title in Phase 27 with a concrete Odoo or business example.

> **Phase 27 complete?** [Continue to Phase 28](#phase-28)


<a id="phase-28"></a>
# PHASE 28 - Access Control Lists

**Track:** Security

**WHAT YOU WILL BE ABLE TO DO:** Author `ir.model.access` CSV matrices that grant least-privilege CRUD per group, and diagnose AccessError as missing model rights (not “Odoo is broken”).

**WHAT YOU SHOULD KNOW FIRST:** Phase 27 — groups exist. Models from earlier stories.

**LEARNING RESOURCES:**

- [Security reference — access rights](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [Restrict data access](https://www.odoo.com/documentation/18.0/developer/tutorials/restrict_data_access.html)
- [Server Framework 101 — security](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/04_securityintro.html)
- [ACL configuration](https://www.youtube.com/watch?v=hwfhO2IGhmc)

## 28.1 ir.model.access

**ACL answers:** *May this group perform create/read/write/unlink on this model at all?* It does **not** answer *which rows*.

```csv
id,name,model_id:id,group_id:id,perm_read,perm_write,perm_create,perm_unlink
access_library_book_user,library.book.user,model_library_book,library_app.group_library_user,1,1,1,0
access_library_book_manager,library.book.manager,model_library_book,library_app.group_library_manager,1,1,1,1
access_library_loan_user,library.loan.user,model_library_loan,library_app.group_library_user,1,1,1,0
```

- `model_id:id` uses the XML ID Odoo auto-creates for models: `model_<name_with_underscores>` (e.g. `library.book` → `model_library_book`), usually in your module or as `module.model_...`.
- Empty `group_id` means global (everyone) — rare and dangerous; prefer explicit groups.
- Missing row for a group ⇒ **AccessError** when that user touches the model.

**ACCESSERROR MENTAL MODEL:**

```text
User clicks Books menu
  -> act_window reads library.book
  -> ACL check: perm_read for user's groups?
        |
        +-- no  -> AccessError (often empty screen / popup)
        +-- yes -> continue to record rules (Phase 29)
```

Always test with a **non-admin** user. Admin/`sudo` paths hide ACL mistakes until production.

**LEAST PRIVILEGE PATTERNS:**

| Role | read | write | create | unlink |
| ---- | ---- | ----- | ------ | ------ |
| Officer | 1 | 1 | 1 | 0 |
| Manager | 1 | 1 | 1 | 1 |
| Portal readonly | 1 | 0 | 0 | 0 |

Transient wizard models need ACL too — forgotten wizard access is a classic install bug.

**ACL VS RECORD RULES (BOOKMARK THIS):**

```text
ACL          = gate to the table (model)
Record rule  = filter on rows (domain)
Both required for real multi-user apps.

                  +------------------+
 User request --> | ACL: model CRUD? | --no--> AccessError
                  +--------+---------+
                           | yes
                           v
                  +------------------+
                  | Rules: which     | --none match--> empty recordset
                  | rows?            | --match--> operate on rows
                  +------------------+
```

**FIELD-LEVEL `groups=` IS NOT A REPLACEMENT:**

```python
secret_note = fields.Char(groups="library_app.group_library_manager")
```

Non-managers omit the field from the default read/write field set — useful, still not a substitute for missing ACL on a dedicated sensitive model. Do not store payroll in a Char on `res.partner` with only `groups=` and call it done.

**MANIFEST LOADING:**

Keep `security/ir.model.access.csv` in `'data'` after group XML (groups must exist for `group_id:id`). On `-u`, CSV lines upsert by XML id — changing permissions in CSV is how you ship privilege fixes.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | CSV granting User read/write/create and Manager full CRUD. |
| Medium | Reproduce AccessError by removing a CSV line; fix it. |
| Hard | Audit five models for portal vs internal rights; no global ACL. |

**WHAT THIS UNLOCKS NEXT:**

Everyone in Sales can read `sale.order` — but each rep must see **their** orders. Phase 29 is row-level **record rules**.

---


---

## 28.2 ir.model.access.csv

**ACL answers:** *May this group perform create/read/write/unlink on this model at all?* It does **not** answer *which rows*.

```csv
id,name,model_id:id,group_id:id,perm_read,perm_write,perm_create,perm_unlink
access_library_book_user,library.book.user,model_library_book,library_app.group_library_user,1,1,1,0
access_library_book_manager,library.book.manager,model_library_book,library_app.group_library_manager,1,1,1,1
access_library_loan_user,library.loan.user,model_library_loan,library_app.group_library_user,1,1,1,0
```

- `model_id:id` uses the XML ID Odoo auto-creates for models: `model_<name_with_underscores>` (e.g. `library.book` → `model_library_book`), usually in your module or as `module.model_...`.
- Empty `group_id` means global (everyone) — rare and dangerous; prefer explicit groups.
- Missing row for a group ⇒ **AccessError** when that user touches the model.

**ACCESSERROR MENTAL MODEL:**

```text
User clicks Books menu
  -> act_window reads library.book
  -> ACL check: perm_read for user's groups?
        |
        +-- no  -> AccessError (often empty screen / popup)
        +-- yes -> continue to record rules (Phase 29)
```

Always test with a **non-admin** user. Admin/`sudo` paths hide ACL mistakes until production.

**LEAST PRIVILEGE PATTERNS:**

| Role | read | write | create | unlink |
| ---- | ---- | ----- | ------ | ------ |
| Officer | 1 | 1 | 1 | 0 |
| Manager | 1 | 1 | 1 | 1 |
| Portal readonly | 1 | 0 | 0 | 0 |

Transient wizard models need ACL too — forgotten wizard access is a classic install bug.

**ACL VS RECORD RULES (BOOKMARK THIS):**

```text
ACL          = gate to the table (model)
Record rule  = filter on rows (domain)
Both required for real multi-user apps.

                  +------------------+
 User request --> | ACL: model CRUD? | --no--> AccessError
                  +--------+---------+
                           | yes
                           v
                  +------------------+
                  | Rules: which     | --none match--> empty recordset
                  | rows?            | --match--> operate on rows
                  +------------------+
```

**FIELD-LEVEL `groups=` IS NOT A REPLACEMENT:**

```python
secret_note = fields.Char(groups="library_app.group_library_manager")
```

Non-managers omit the field from the default read/write field set — useful, still not a substitute for missing ACL on a dedicated sensitive model. Do not store payroll in a Char on `res.partner` with only `groups=` and call it done.

**MANIFEST LOADING:**

Keep `security/ir.model.access.csv` in `'data'` after group XML (groups must exist for `group_id:id`). On `-u`, CSV lines upsert by XML id — changing permissions in CSV is how you ship privilege fixes.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | CSV granting User read/write/create and Manager full CRUD. |
| Medium | Reproduce AccessError by removing a CSV line; fix it. |
| Hard | Audit five models for portal vs internal rights; no global ACL. |

**WHAT THIS UNLOCKS NEXT:**

Everyone in Sales can read `sale.order` — but each rep must see **their** orders. Phase 29 is row-level **record rules**.

---


---

## 28.3 Read

ACL **Read** is a 1/0 column in `ir.model.access.csv` (perm_read, perm_write, perm_create, perm_unlink).

Groups are additive: if any group grants the bit, the user may do it at model level. Record rules still filter rows. Never 'fix' AccessError by `sudo()` in a user button.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Read** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Read**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Read** is concrete, continue — the next lesson in this phase still depends on it.

## 28.4 Write

ACL **Write** is a 1/0 column in `ir.model.access.csv` (perm_read, perm_write, perm_create, perm_unlink).

Groups are additive: if any group grants the bit, the user may do it at model level. Record rules still filter rows. Never 'fix' AccessError by `sudo()` in a user button.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Write** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Write**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Write** is concrete, continue — the next lesson in this phase still depends on it.

## 28.5 Create

ACL **Create** is a 1/0 column in `ir.model.access.csv` (perm_read, perm_write, perm_create, perm_unlink).

Groups are additive: if any group grants the bit, the user may do it at model level. Record rules still filter rows. Never 'fix' AccessError by `sudo()` in a user button.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Create** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Create**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Create** is concrete, continue — the next lesson in this phase still depends on it.

## 28.6 Unlink

ACL **Unlink** is a 1/0 column in `ir.model.access.csv` (perm_read, perm_write, perm_create, perm_unlink).

Groups are additive: if any group grants the bit, the user may do it at model level. Record rules still filter rows. Never 'fix' AccessError by `sudo()` in a user button.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Unlink** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Unlink**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Unlink** is concrete, continue — the next lesson in this phase still depends on it.

## 28.7 Group ACLs

**Group ACLs** belongs to **Access Control Lists**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Group ACLs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Group ACLs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Group ACLs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Group ACLs** is concrete, continue — the next lesson in this phase still depends on it.

## 28.8 Additive Permissions

**Additive Permissions** belongs to **Access Control Lists**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Additive Permissions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Additive Permissions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Additive Permissions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Additive Permissions** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 28:** You can explain every lesson title in Phase 28 with a concrete Odoo or business example.

> **Phase 28 complete?** [Continue to Phase 29](#phase-29)


<a id="phase-29"></a>
# PHASE 29 - Record Rules

**Track:** Security

**WHAT YOU WILL BE ABLE TO DO:** Write `ir.rule` domains that enforce row-level security, explain global AND vs group OR combination, and debug “empty list for user / full list for admin” failures.

**WHAT YOU SHOULD KNOW FIRST:** Phase 28 — ACL allows model access. Domains from the ORM story.

**LEARNING RESOURCES:**

- [Security reference — record rules](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [Restrict data access tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/restrict_data_access.html)
- [Record rules deep dive](https://www.youtube.com/watch?v=r11U1pqGqQQ)
- [odoo/odoo — base security rules](https://github.com/odoo/odoo)

## 29.1 ir.rule

```xml
<record id="rule_library_book_officer_own" model="ir.rule">
    <field name="name">Books: officer sees own</field>
    <field name="model_id" ref="model_library_book"/>
    <field name="domain_force">[('officer_id', '=', user.id)]</field>
    <field name="groups" eval="[(4, ref('library_app.group_library_user'))]"/>
    <field name="perm_read" eval="True"/>
    <field name="perm_write" eval="True"/>
    <field name="perm_create" eval="True"/>
    <field name="perm_unlink" eval="False"/>
</record>
```

`domain_force` is a **string** evaluated with `user`, `time`, and company helpers available. Standard library `user` is the current `res.users` record.

**COMBINATION RULES (CRITICAL):**

```text
Global rules (no groups on ir.rule):
  ALL global rules AND together

Group-specific rules:
  For the user's groups, rules for a given operation OR together
  Then AND with globals

Example:
  Global:  company_id in company_ids
  Group A: officer_id = user.id
  Group B: state = 'published'   (user in A and B)

  Effective ≈ global AND (own OR published)
```

Misunderstanding OR/AND causes “manager suddenly sees nothing” after adding a strict global rule.

**PERM FLAGS ON RULES:**

A rule can apply only to certain operations. You might allow read broadly but restrict write/unlink more tightly — use separate rules rather than one overloaded domain when clarity suffers.

**VISIBILITY ≠ SECURITY (AGAIN):**

```text
List view decoration / invisible field
  -> cosmetic

Action domain
  -> UX default window scope (bypassable via other actions / RPC)

Record rule
  -> ORM enforces on search/read/write/unlink
```

If security matters, put it in **ACL + rules**, not in view XML alone.

**DEBUG PLAYBOOK:**

1. Reproduce with a dedicated test user (only intended groups).
2. As admin, check Settings → Technical → Security → Record Rules for the model.
3. In shell: `env['library.book'].with_user(test_user).search([])` vs `sudo()`.
4. Confirm ACL first — empty + AccessError is ACL; empty + no error often means rules filtered everything.

**WORKED FAILURE — “ADMIN SEES ROWS, USER SEES NONE”:**

```text
Symptom: Manager list shows 40 books; Officer list shows 0; no AccessError.

Check ACL: Officer has perm_read=1 on library.book     OK
Check action domain: []                                OK
Check rules:
  Global: [('company_id','in', company_ids)]           OK (same company)
  Group Officer: [('officer_id','=', user.id)]         <-- officer_id empty on all demo books

Cause: domain correct, data wrong — or rule too strict for how records are created.
Fix: set officer_id on create defaults, OR widen domain with '|', ('officer_id','=',False) if intentional.
```

**MULTI-COMPANY PREVIEW:**

Company isolation is usually a **global** `ir.rule` on transactional models. Phase 31 installs that pattern properly with `company_id` fields — do not invent a one-off company domain per menu and call it security.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Rule: users see only records where `create_uid = user.id`. |
| Medium | Global company rule + group own-document rule; explain the combo. |
| Hard | Debug a user who sees zero rows; write the root cause (ACL vs rule vs domain). |

**WHAT THIS UNLOCKS NEXT:**

Legitimate code sometimes must bypass or switch users — `sudo()` and `with_user()` — without becoming a backdoor. Phase 30.

---


---

## 29.2 domain_force

**domain_force** belongs to **Record Rules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **domain_force** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **domain_force** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **domain_force**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **domain_force** is concrete, continue — the next lesson in this phase still depends on it.

## 29.3 Global Rules

**Global Rules** belongs to **Record Rules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Global Rules** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Global Rules** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Global Rules**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Global Rules** is concrete, continue — the next lesson in this phase still depends on it.

## 29.4 Group Rules

**Group Rules** belongs to **Record Rules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Group Rules** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Group Rules** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Group Rules**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Group Rules** is concrete, continue — the next lesson in this phase still depends on it.

## 29.5 Rule Composition

```xml
<record id="rule_library_book_officer_own" model="ir.rule">
    <field name="name">Books: officer sees own</field>
    <field name="model_id" ref="model_library_book"/>
    <field name="domain_force">[('officer_id', '=', user.id)]</field>
    <field name="groups" eval="[(4, ref('library_app.group_library_user'))]"/>
    <field name="perm_read" eval="True"/>
    <field name="perm_write" eval="True"/>
    <field name="perm_create" eval="True"/>
    <field name="perm_unlink" eval="False"/>
</record>
```

`domain_force` is a **string** evaluated with `user`, `time`, and company helpers available. Standard library `user` is the current `res.users` record.

**COMBINATION RULES (CRITICAL):**

```text
Global rules (no groups on ir.rule):
  ALL global rules AND together

Group-specific rules:
  For the user's groups, rules for a given operation OR together
  Then AND with globals

Example:
  Global:  company_id in company_ids
  Group A: officer_id = user.id
  Group B: state = 'published'   (user in A and B)

  Effective ≈ global AND (own OR published)
```

Misunderstanding OR/AND causes “manager suddenly sees nothing” after adding a strict global rule.

**PERM FLAGS ON RULES:**

A rule can apply only to certain operations. You might allow read broadly but restrict write/unlink more tightly — use separate rules rather than one overloaded domain when clarity suffers.

**VISIBILITY ≠ SECURITY (AGAIN):**

```text
List view decoration / invisible field
  -> cosmetic

Action domain
  -> UX default window scope (bypassable via other actions / RPC)

Record rule
  -> ORM enforces on search/read/write/unlink
```

If security matters, put it in **ACL + rules**, not in view XML alone.

**DEBUG PLAYBOOK:**

1. Reproduce with a dedicated test user (only intended groups).
2. As admin, check Settings → Technical → Security → Record Rules for the model.
3. In shell: `env['library.book'].with_user(test_user).search([])` vs `sudo()`.
4. Confirm ACL first — empty + AccessError is ACL; empty + no error often means rules filtered everything.

**WORKED FAILURE — “ADMIN SEES ROWS, USER SEES NONE”:**

```text
Symptom: Manager list shows 40 books; Officer list shows 0; no AccessError.

Check ACL: Officer has perm_read=1 on library.book     OK
Check action domain: []                                OK
Check rules:
  Global: [('company_id','in', company_ids)]           OK (same company)
  Group Officer: [('officer_id','=', user.id)]         <-- officer_id empty on all demo books

Cause: domain correct, data wrong — or rule too strict for how records are created.
Fix: set officer_id on create defaults, OR widen domain with '|', ('officer_id','=',False) if intentional.
```

**MULTI-COMPANY PREVIEW:**

Company isolation is usually a **global** `ir.rule` on transactional models. Phase 31 installs that pattern properly with `company_id` fields — do not invent a one-off company domain per menu and call it security.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Rule: users see only records where `create_uid = user.id`. |
| Medium | Global company rule + group own-document rule; explain the combo. |
| Hard | Debug a user who sees zero rows; write the root cause (ACL vs rule vs domain). |

**WHAT THIS UNLOCKS NEXT:**

Legitimate code sometimes must bypass or switch users — `sudo()` and `with_user()` — without becoming a backdoor. Phase 30.

---


---

## 29.6 Row-Level Security

```xml
<record id="rule_library_book_officer_own" model="ir.rule">
    <field name="name">Books: officer sees own</field>
    <field name="model_id" ref="model_library_book"/>
    <field name="domain_force">[('officer_id', '=', user.id)]</field>
    <field name="groups" eval="[(4, ref('library_app.group_library_user'))]"/>
    <field name="perm_read" eval="True"/>
    <field name="perm_write" eval="True"/>
    <field name="perm_create" eval="True"/>
    <field name="perm_unlink" eval="False"/>
</record>
```

`domain_force` is a **string** evaluated with `user`, `time`, and company helpers available. Standard library `user` is the current `res.users` record.

**COMBINATION RULES (CRITICAL):**

```text
Global rules (no groups on ir.rule):
  ALL global rules AND together

Group-specific rules:
  For the user's groups, rules for a given operation OR together
  Then AND with globals

Example:
  Global:  company_id in company_ids
  Group A: officer_id = user.id
  Group B: state = 'published'   (user in A and B)

  Effective ≈ global AND (own OR published)
```

Misunderstanding OR/AND causes “manager suddenly sees nothing” after adding a strict global rule.

**PERM FLAGS ON RULES:**

A rule can apply only to certain operations. You might allow read broadly but restrict write/unlink more tightly — use separate rules rather than one overloaded domain when clarity suffers.

**VISIBILITY ≠ SECURITY (AGAIN):**

```text
List view decoration / invisible field
  -> cosmetic

Action domain
  -> UX default window scope (bypassable via other actions / RPC)

Record rule
  -> ORM enforces on search/read/write/unlink
```

If security matters, put it in **ACL + rules**, not in view XML alone.

**DEBUG PLAYBOOK:**

1. Reproduce with a dedicated test user (only intended groups).
2. As admin, check Settings → Technical → Security → Record Rules for the model.
3. In shell: `env['library.book'].with_user(test_user).search([])` vs `sudo()`.
4. Confirm ACL first — empty + AccessError is ACL; empty + no error often means rules filtered everything.

**WORKED FAILURE — “ADMIN SEES ROWS, USER SEES NONE”:**

```text
Symptom: Manager list shows 40 books; Officer list shows 0; no AccessError.

Check ACL: Officer has perm_read=1 on library.book     OK
Check action domain: []                                OK
Check rules:
  Global: [('company_id','in', company_ids)]           OK (same company)
  Group Officer: [('officer_id','=', user.id)]         <-- officer_id empty on all demo books

Cause: domain correct, data wrong — or rule too strict for how records are created.
Fix: set officer_id on create defaults, OR widen domain with '|', ('officer_id','=',False) if intentional.
```

**MULTI-COMPANY PREVIEW:**

Company isolation is usually a **global** `ir.rule` on transactional models. Phase 31 installs that pattern properly with `company_id` fields — do not invent a one-off company domain per menu and call it security.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Rule: users see only records where `create_uid = user.id`. |
| Medium | Global company rule + group own-document rule; explain the combo. |
| Hard | Debug a user who sees zero rows; write the root cause (ACL vs rule vs domain). |

**WHAT THIS UNLOCKS NEXT:**

Legitimate code sometimes must bypass or switch users — `sudo()` and `with_user()` — without becoming a backdoor. Phase 30.

---


---


<a id="phase-30"></a>
<a id="phase-30---security-context-safe-privilege"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 29:** You can explain every lesson title in Phase 29 with a concrete Odoo or business example.

> **Phase 29 complete?** [Continue to Phase 30](#phase-30)


<a id="phase-30"></a>
# PHASE 30 - Security Context & Safe Privilege

**Track:** Security

**WHAT YOU WILL BE ABLE TO DO:** Use `sudo()` and `with_user()` deliberately, keep security context narrow, and refuse patterns that launder privileges across request boundaries.

**WHAT YOU SHOULD KNOW FIRST:** Phases 27–29 — groups, ACL, record rules. ORM environment from Story V.

**LEARNING RESOURCES:**

- [Security reference](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [Restrict data access](https://www.odoo.com/documentation/18.0/developer/tutorials/restrict_data_access.html)
- [Security context pitfalls](https://www.youtube.com/watch?v=r11U1pqGqQQ)
- [odoo/odoo — Environment / sudo usage in core](https://github.com/odoo/odoo)

## 30.1 sudo()

`sudo()` rebuilds the environment as superuser: ACLs and record rules stop applying. Use it for system rows (mail, cron, computed stored fields that must write privileged data) — never as a shortcut in a portal controller.

Prefer `with_user(integration_user)` when you still want *some* rules. Every `sudo()` in a PR needs a one-line justification.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **sudo()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **sudo()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **sudo()** is concrete, continue — the next lesson in this phase still depends on it.

## 30.2 with_user()

**with_user()** belongs to **Security Context & Safe Privilege**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **with_user()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **with_user()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **with_user()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **with_user()** is concrete, continue — the next lesson in this phase still depends on it.

## 30.3 Access Checks

**Access Checks** belongs to **Security Context & Safe Privilege**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Access Checks** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Access Checks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Access Checks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Access Checks** is concrete, continue — the next lesson in this phase still depends on it.

## 30.4 check_access()

**check_access()** belongs to **Security Context & Safe Privilege**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **check_access()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **check_access()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **check_access()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **check_access()** is concrete, continue — the next lesson in this phase still depends on it.

## 30.5 Field Security

**Field Security** belongs to **Security Context & Safe Privilege**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Field Security** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Field Security** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Field Security**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Field Security** is concrete, continue — the next lesson in this phase still depends on it.

## 30.6 View / Menu Groups

**View / Menu Groups** belongs to **Security Context & Safe Privilege**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **View / Menu Groups** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **View / Menu Groups** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **View / Menu Groups**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **View / Menu Groups** is concrete, continue — the next lesson in this phase still depends on it.

## 30.7 Public Method Security

**Public Method Security** belongs to **Security Context & Safe Privilege**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Public Method Security** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Public Method Security** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Public Method Security**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Public Method Security** is concrete, continue — the next lesson in this phase still depends on it.

## 30.8 Unsafe Raw SQL

**Unsafe Raw SQL** belongs to **Security Context & Safe Privilege**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Unsafe Raw SQL** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Unsafe Raw SQL** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Unsafe Raw SQL**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Unsafe Raw SQL** is concrete, continue — the next lesson in this phase still depends on it.

## 30.9 Security Bypass Risks

**Security Bypass Risks** belongs to **Security Context & Safe Privilege**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Security Bypass Risks** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Security Bypass Risks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Security Bypass Risks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Security Bypass Risks** is concrete, continue — the next lesson in this phase still depends on it.

## 30.10 Least Privilege

Every recordset carries an **Environment**: cursor, uid, context, su flag. Access checks use that environment.

```python
# Dangerous: entire method runs as superuser
def action_confirm(self):
    self.sudo().write({"state": "done"})
    self.sudo().message_post(body="Confirmed")  # still sudo
```

```python
# Better: narrow sudo to the line that needs it
def action_confirm(self):
    self.check_access_rights("write")
    self.check_access_rule("write")
    sensitive = self.env["res.company"].sudo().browse(self.company_id.id)
    # ... read something the user cannot ...
    self.write({"state": "done"})  # as the real user
```

**SUDO():**

- Sets superuser mode on the environment: **ACL and record rules are skipped**.
- Required for cron jobs, some install hooks, crossing intentional trust boundaries.
- Default in controllers with `auth='none'` / public is *not* an excuse to write business data with sudo blindly — map to a real user when possible.

**WITH_USER(USER):**

```python
as_officer = self.env["library.book"].with_user(officer_user)
as_officer.search([])  # evaluates rules as officer_user
```

Use `with_user` for impersonation tests and for “run this workflow as the document owner” without full superuser. It still respects that user’s ACL/rules (unless chained with sudo).

**CONTEXT IS NOT SECURITY:**

```python
self.with_context(force_company=1)  # affects defaults / company-dependent fields
# Does NOT replace record rules.
```

Attackers control context from RPC more than beginners expect. Never put authorization decisions only in context flags the client can set.

**FAILURE PATTERN — SUDO IN A LOOP OVER USER INPUT:**

```text
Portal user posts book_id=17
Controller:
  request.env['library.book'].sudo().browse(book_id).unlink()

=> Portal just deleted any book. ACL/rules never ran.
```

Fix: operate as `request.env.user` (or a dedicated portal group with strict rules), verify ownership, then mutate.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Rewrite a method that sudo-writes everything into a narrow sudo read + user write. |
| Medium | Unit-style shell test: `with_user` sees subset; `sudo` sees all. |
| Hard | Find and fix a controller/cron that sudo-writes from external input. |

**WHAT THIS UNLOCKS NEXT:**

One database, several legal entities — Phase 31 **multi-company** fields, company-dependent data, and cross-company rules.

---


---


<a id="phase-31"></a>
<a id="phase-31---multi-company-development"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 30:** You can explain every lesson title in Phase 30 with a concrete Odoo or business example.

> **Phase 30 complete?** [Continue to Phase 31](#phase-31)


<a id="phase-31"></a>
# PHASE 31 - Multi-Company Development

**Track:** Security

**WHAT YOU WILL BE ABLE TO DO:** Add `company_id` / `company_ids` correctly, apply company record-rule patterns, understand company-dependent fields, and prevent cross-company leakage.

**WHAT YOU SHOULD KNOW FIRST:** Phases 27–30 — full security stack. `res.company` exists in every DB.

**LEARNING RESOURCES:**

- [Security reference](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [Access rights / multi-company UX](https://www.odoo.com/documentation/18.0/applications/general/users/access_rights.html)
- [Restrict data access](https://www.odoo.com/documentation/18.0/developer/tutorials/restrict_data_access.html)
- [Multi-company patterns](https://www.youtube.com/watch?v=hwfhO2IGhmc)
- [odoo/odoo — company rules in base](https://github.com/odoo/odoo)

## 31.1 res.company

Multi-company is not a checkbox — it is a data model:

```python
class LibraryBook(models.Model):
    _name = "library.book"
    _check_company_auto = True

    name = fields.Char(required=True)
    company_id = fields.Many2one(
        "res.company",
        required=True,
        default=lambda self: self.env.company,
        index=True,
    )
    author_id = fields.Many2one("res.partner", check_company=True)
```

- **`env.company`** — current working company.
- **`env.companies` / `user.company_ids`** — companies the user may switch into.
- **`check_company=True`** — relational consistency: related records must share company (or be company-empty when allowed).
- **`_check_company_auto`** — enables automatic consistency checks on writes.

**STANDARD RECORD RULE PATTERN:**

```xml
<field name="domain_force">[
    '|',
    ('company_id', '=', False),
    ('company_id', 'in', company_ids),
]</field>
```

`company_ids` in rule evaluation refers to the user’s allowed companies. Empty `company_id` often means “shared across companies” — use deliberately (products sometimes; accounting rarely).

**COMPANY-DEPENDENT FIELDS:**

Some fields store **per-company values** (property-like / `company_dependent=True`). Reading them respects the active company. Writing in the wrong company silently affects another slice of data — always test with company switching.

```text
User active company = Qatar Co
  read product.standard_price  -> Qatar value

Switch to UAE Co
  read product.standard_price  -> UAE value

Same product row, different property bag.
```

**CROSS-COMPANY FAILURES:**

```text
Bad:  sale order company A, invoice company B, no checks
Bad:  record rule missing on custom model that holds money
Bad:  sudo() search without company domain in a multi-co cron

Good: company_id on transactional models
Good: check_company on money/partner links
Good: rules using company_ids
Good: tests with two companies + two users
```

**STORY PROJECT — SECURE MULTI-COMPANY MODULE**

Deliver a small module (or extend your library) that proves isolation:

| Check | Evidence |
| ----- | -------- |
| Groups User/Manager | XML + Settings matrix |
| ACL CSV | Non-admin AccessError without rights |
| Record rules | User A cannot read User B rows (or company B rows) |
| `company_id` on main model | Created records default to `env.company` |
| Company rule | User limited to company_ids sees only those rows |
| No sudo laundering | Business methods pass `with_user` tests |
| Menu `groups=` | Matched by real ACL (not UI-only) |

Write a short SECURITY.md in the module: who can do what, in one page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `company_id` defaulting to `env.company`. |
| Medium | Install the standard company rule domain; test with two companies. |
| Hard | Full story project: two companies, two users, zero cross leakage on your model. |

**WHAT THIS UNLOCKS NEXT:**

Your app is secure and usable. Story IX teaches how to **extend official Odoo apps** — `_inherit`, `_inherits`, real view patches, and Sales/Purchase/Inventory customizations — without breaking upgrades.

---


---

## 31.2 company_id

Multi-company is not a checkbox — it is a data model:

```python
class LibraryBook(models.Model):
    _name = "library.book"
    _check_company_auto = True

    name = fields.Char(required=True)
    company_id = fields.Many2one(
        "res.company",
        required=True,
        default=lambda self: self.env.company,
        index=True,
    )
    author_id = fields.Many2one("res.partner", check_company=True)
```

- **`env.company`** — current working company.
- **`env.companies` / `user.company_ids`** — companies the user may switch into.
- **`check_company=True`** — relational consistency: related records must share company (or be company-empty when allowed).
- **`_check_company_auto`** — enables automatic consistency checks on writes.

**STANDARD RECORD RULE PATTERN:**

```xml
<field name="domain_force">[
    '|',
    ('company_id', '=', False),
    ('company_id', 'in', company_ids),
]</field>
```

`company_ids` in rule evaluation refers to the user’s allowed companies. Empty `company_id` often means “shared across companies” — use deliberately (products sometimes; accounting rarely).

**COMPANY-DEPENDENT FIELDS:**

Some fields store **per-company values** (property-like / `company_dependent=True`). Reading them respects the active company. Writing in the wrong company silently affects another slice of data — always test with company switching.

```text
User active company = Qatar Co
  read product.standard_price  -> Qatar value

Switch to UAE Co
  read product.standard_price  -> UAE value

Same product row, different property bag.
```

**CROSS-COMPANY FAILURES:**

```text
Bad:  sale order company A, invoice company B, no checks
Bad:  record rule missing on custom model that holds money
Bad:  sudo() search without company domain in a multi-co cron

Good: company_id on transactional models
Good: check_company on money/partner links
Good: rules using company_ids
Good: tests with two companies + two users
```

**STORY PROJECT — SECURE MULTI-COMPANY MODULE**

Deliver a small module (or extend your library) that proves isolation:

| Check | Evidence |
| ----- | -------- |
| Groups User/Manager | XML + Settings matrix |
| ACL CSV | Non-admin AccessError without rights |
| Record rules | User A cannot read User B rows (or company B rows) |
| `company_id` on main model | Created records default to `env.company` |
| Company rule | User limited to company_ids sees only those rows |
| No sudo laundering | Business methods pass `with_user` tests |
| Menu `groups=` | Matched by real ACL (not UI-only) |

Write a short SECURITY.md in the module: who can do what, in one page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `company_id` defaulting to `env.company`. |
| Medium | Install the standard company rule domain; test with two companies. |
| Hard | Full story project: two companies, two users, zero cross leakage on your model. |

**WHAT THIS UNLOCKS NEXT:**

Your app is secure and usable. Story IX teaches how to **extend official Odoo apps** — `_inherit`, `_inherits`, real view patches, and Sales/Purchase/Inventory customizations — without breaking upgrades.

---


---

## 31.3 company_ids

Multi-company is not a checkbox — it is a data model:

```python
class LibraryBook(models.Model):
    _name = "library.book"
    _check_company_auto = True

    name = fields.Char(required=True)
    company_id = fields.Many2one(
        "res.company",
        required=True,
        default=lambda self: self.env.company,
        index=True,
    )
    author_id = fields.Many2one("res.partner", check_company=True)
```

- **`env.company`** — current working company.
- **`env.companies` / `user.company_ids`** — companies the user may switch into.
- **`check_company=True`** — relational consistency: related records must share company (or be company-empty when allowed).
- **`_check_company_auto`** — enables automatic consistency checks on writes.

**STANDARD RECORD RULE PATTERN:**

```xml
<field name="domain_force">[
    '|',
    ('company_id', '=', False),
    ('company_id', 'in', company_ids),
]</field>
```

`company_ids` in rule evaluation refers to the user’s allowed companies. Empty `company_id` often means “shared across companies” — use deliberately (products sometimes; accounting rarely).

**COMPANY-DEPENDENT FIELDS:**

Some fields store **per-company values** (property-like / `company_dependent=True`). Reading them respects the active company. Writing in the wrong company silently affects another slice of data — always test with company switching.

```text
User active company = Qatar Co
  read product.standard_price  -> Qatar value

Switch to UAE Co
  read product.standard_price  -> UAE value

Same product row, different property bag.
```

**CROSS-COMPANY FAILURES:**

```text
Bad:  sale order company A, invoice company B, no checks
Bad:  record rule missing on custom model that holds money
Bad:  sudo() search without company domain in a multi-co cron

Good: company_id on transactional models
Good: check_company on money/partner links
Good: rules using company_ids
Good: tests with two companies + two users
```

**STORY PROJECT — SECURE MULTI-COMPANY MODULE**

Deliver a small module (or extend your library) that proves isolation:

| Check | Evidence |
| ----- | -------- |
| Groups User/Manager | XML + Settings matrix |
| ACL CSV | Non-admin AccessError without rights |
| Record rules | User A cannot read User B rows (or company B rows) |
| `company_id` on main model | Created records default to `env.company` |
| Company rule | User limited to company_ids sees only those rows |
| No sudo laundering | Business methods pass `with_user` tests |
| Menu `groups=` | Matched by real ACL (not UI-only) |

Write a short SECURITY.md in the module: who can do what, in one page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `company_id` defaulting to `env.company`. |
| Medium | Install the standard company rule domain; test with two companies. |
| Hard | Full story project: two companies, two users, zero cross leakage on your model. |

**WHAT THIS UNLOCKS NEXT:**

Your app is secure and usable. Story IX teaches how to **extend official Odoo apps** — `_inherit`, `_inherits`, real view patches, and Sales/Purchase/Inventory customizations — without breaking upgrades.

---


---

## 31.4 env.company

Multi-company is not a checkbox — it is a data model:

```python
class LibraryBook(models.Model):
    _name = "library.book"
    _check_company_auto = True

    name = fields.Char(required=True)
    company_id = fields.Many2one(
        "res.company",
        required=True,
        default=lambda self: self.env.company,
        index=True,
    )
    author_id = fields.Many2one("res.partner", check_company=True)
```

- **`env.company`** — current working company.
- **`env.companies` / `user.company_ids`** — companies the user may switch into.
- **`check_company=True`** — relational consistency: related records must share company (or be company-empty when allowed).
- **`_check_company_auto`** — enables automatic consistency checks on writes.

**STANDARD RECORD RULE PATTERN:**

```xml
<field name="domain_force">[
    '|',
    ('company_id', '=', False),
    ('company_id', 'in', company_ids),
]</field>
```

`company_ids` in rule evaluation refers to the user’s allowed companies. Empty `company_id` often means “shared across companies” — use deliberately (products sometimes; accounting rarely).

**COMPANY-DEPENDENT FIELDS:**

Some fields store **per-company values** (property-like / `company_dependent=True`). Reading them respects the active company. Writing in the wrong company silently affects another slice of data — always test with company switching.

```text
User active company = Qatar Co
  read product.standard_price  -> Qatar value

Switch to UAE Co
  read product.standard_price  -> UAE value

Same product row, different property bag.
```

**CROSS-COMPANY FAILURES:**

```text
Bad:  sale order company A, invoice company B, no checks
Bad:  record rule missing on custom model that holds money
Bad:  sudo() search without company domain in a multi-co cron

Good: company_id on transactional models
Good: check_company on money/partner links
Good: rules using company_ids
Good: tests with two companies + two users
```

**STORY PROJECT — SECURE MULTI-COMPANY MODULE**

Deliver a small module (or extend your library) that proves isolation:

| Check | Evidence |
| ----- | -------- |
| Groups User/Manager | XML + Settings matrix |
| ACL CSV | Non-admin AccessError without rights |
| Record rules | User A cannot read User B rows (or company B rows) |
| `company_id` on main model | Created records default to `env.company` |
| Company rule | User limited to company_ids sees only those rows |
| No sudo laundering | Business methods pass `with_user` tests |
| Menu `groups=` | Matched by real ACL (not UI-only) |

Write a short SECURITY.md in the module: who can do what, in one page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `company_id` defaulting to `env.company`. |
| Medium | Install the standard company rule domain; test with two companies. |
| Hard | Full story project: two companies, two users, zero cross leakage on your model. |

**WHAT THIS UNLOCKS NEXT:**

Your app is secure and usable. Story IX teaches how to **extend official Odoo apps** — `_inherit`, `_inherits`, real view patches, and Sales/Purchase/Inventory customizations — without breaking upgrades.

---


---

## 31.5 with_company()

Multi-company is not a checkbox — it is a data model:

```python
class LibraryBook(models.Model):
    _name = "library.book"
    _check_company_auto = True

    name = fields.Char(required=True)
    company_id = fields.Many2one(
        "res.company",
        required=True,
        default=lambda self: self.env.company,
        index=True,
    )
    author_id = fields.Many2one("res.partner", check_company=True)
```

- **`env.company`** — current working company.
- **`env.companies` / `user.company_ids`** — companies the user may switch into.
- **`check_company=True`** — relational consistency: related records must share company (or be company-empty when allowed).
- **`_check_company_auto`** — enables automatic consistency checks on writes.

**STANDARD RECORD RULE PATTERN:**

```xml
<field name="domain_force">[
    '|',
    ('company_id', '=', False),
    ('company_id', 'in', company_ids),
]</field>
```

`company_ids` in rule evaluation refers to the user’s allowed companies. Empty `company_id` often means “shared across companies” — use deliberately (products sometimes; accounting rarely).

**COMPANY-DEPENDENT FIELDS:**

Some fields store **per-company values** (property-like / `company_dependent=True`). Reading them respects the active company. Writing in the wrong company silently affects another slice of data — always test with company switching.

```text
User active company = Qatar Co
  read product.standard_price  -> Qatar value

Switch to UAE Co
  read product.standard_price  -> UAE value

Same product row, different property bag.
```

**CROSS-COMPANY FAILURES:**

```text
Bad:  sale order company A, invoice company B, no checks
Bad:  record rule missing on custom model that holds money
Bad:  sudo() search without company domain in a multi-co cron

Good: company_id on transactional models
Good: check_company on money/partner links
Good: rules using company_ids
Good: tests with two companies + two users
```

**STORY PROJECT — SECURE MULTI-COMPANY MODULE**

Deliver a small module (or extend your library) that proves isolation:

| Check | Evidence |
| ----- | -------- |
| Groups User/Manager | XML + Settings matrix |
| ACL CSV | Non-admin AccessError without rights |
| Record rules | User A cannot read User B rows (or company B rows) |
| `company_id` on main model | Created records default to `env.company` |
| Company rule | User limited to company_ids sees only those rows |
| No sudo laundering | Business methods pass `with_user` tests |
| Menu `groups=` | Matched by real ACL (not UI-only) |

Write a short SECURITY.md in the module: who can do what, in one page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `company_id` defaulting to `env.company`. |
| Medium | Install the standard company rule domain; test with two companies. |
| Hard | Full story project: two companies, two users, zero cross leakage on your model. |

**WHAT THIS UNLOCKS NEXT:**

Your app is secure and usable. Story IX teaches how to **extend official Odoo apps** — `_inherit`, `_inherits`, real view patches, and Sales/Purchase/Inventory customizations — without breaking upgrades.

---


---

## 31.6 Company-Dependent Fields

Multi-company is not a checkbox — it is a data model:

```python
class LibraryBook(models.Model):
    _name = "library.book"
    _check_company_auto = True

    name = fields.Char(required=True)
    company_id = fields.Many2one(
        "res.company",
        required=True,
        default=lambda self: self.env.company,
        index=True,
    )
    author_id = fields.Many2one("res.partner", check_company=True)
```

- **`env.company`** — current working company.
- **`env.companies` / `user.company_ids`** — companies the user may switch into.
- **`check_company=True`** — relational consistency: related records must share company (or be company-empty when allowed).
- **`_check_company_auto`** — enables automatic consistency checks on writes.

**STANDARD RECORD RULE PATTERN:**

```xml
<field name="domain_force">[
    '|',
    ('company_id', '=', False),
    ('company_id', 'in', company_ids),
]</field>
```

`company_ids` in rule evaluation refers to the user’s allowed companies. Empty `company_id` often means “shared across companies” — use deliberately (products sometimes; accounting rarely).

**COMPANY-DEPENDENT FIELDS:**

Some fields store **per-company values** (property-like / `company_dependent=True`). Reading them respects the active company. Writing in the wrong company silently affects another slice of data — always test with company switching.

```text
User active company = Qatar Co
  read product.standard_price  -> Qatar value

Switch to UAE Co
  read product.standard_price  -> UAE value

Same product row, different property bag.
```

**CROSS-COMPANY FAILURES:**

```text
Bad:  sale order company A, invoice company B, no checks
Bad:  record rule missing on custom model that holds money
Bad:  sudo() search without company domain in a multi-co cron

Good: company_id on transactional models
Good: check_company on money/partner links
Good: rules using company_ids
Good: tests with two companies + two users
```

**STORY PROJECT — SECURE MULTI-COMPANY MODULE**

Deliver a small module (or extend your library) that proves isolation:

| Check | Evidence |
| ----- | -------- |
| Groups User/Manager | XML + Settings matrix |
| ACL CSV | Non-admin AccessError without rights |
| Record rules | User A cannot read User B rows (or company B rows) |
| `company_id` on main model | Created records default to `env.company` |
| Company rule | User limited to company_ids sees only those rows |
| No sudo laundering | Business methods pass `with_user` tests |
| Menu `groups=` | Matched by real ACL (not UI-only) |

Write a short SECURITY.md in the module: who can do what, in one page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `company_id` defaulting to `env.company`. |
| Medium | Install the standard company rule domain; test with two companies. |
| Hard | Full story project: two companies, two users, zero cross leakage on your model. |

**WHAT THIS UNLOCKS NEXT:**

Your app is secure and usable. Story IX teaches how to **extend official Odoo apps** — `_inherit`, `_inherits`, real view patches, and Sales/Purchase/Inventory customizations — without breaking upgrades.

---


---

## 31.7 _check_company

Multi-company is not a checkbox — it is a data model:

```python
class LibraryBook(models.Model):
    _name = "library.book"
    _check_company_auto = True

    name = fields.Char(required=True)
    company_id = fields.Many2one(
        "res.company",
        required=True,
        default=lambda self: self.env.company,
        index=True,
    )
    author_id = fields.Many2one("res.partner", check_company=True)
```

- **`env.company`** — current working company.
- **`env.companies` / `user.company_ids`** — companies the user may switch into.
- **`check_company=True`** — relational consistency: related records must share company (or be company-empty when allowed).
- **`_check_company_auto`** — enables automatic consistency checks on writes.

**STANDARD RECORD RULE PATTERN:**

```xml
<field name="domain_force">[
    '|',
    ('company_id', '=', False),
    ('company_id', 'in', company_ids),
]</field>
```

`company_ids` in rule evaluation refers to the user’s allowed companies. Empty `company_id` often means “shared across companies” — use deliberately (products sometimes; accounting rarely).

**COMPANY-DEPENDENT FIELDS:**

Some fields store **per-company values** (property-like / `company_dependent=True`). Reading them respects the active company. Writing in the wrong company silently affects another slice of data — always test with company switching.

```text
User active company = Qatar Co
  read product.standard_price  -> Qatar value

Switch to UAE Co
  read product.standard_price  -> UAE value

Same product row, different property bag.
```

**CROSS-COMPANY FAILURES:**

```text
Bad:  sale order company A, invoice company B, no checks
Bad:  record rule missing on custom model that holds money
Bad:  sudo() search without company domain in a multi-co cron

Good: company_id on transactional models
Good: check_company on money/partner links
Good: rules using company_ids
Good: tests with two companies + two users
```

**STORY PROJECT — SECURE MULTI-COMPANY MODULE**

Deliver a small module (or extend your library) that proves isolation:

| Check | Evidence |
| ----- | -------- |
| Groups User/Manager | XML + Settings matrix |
| ACL CSV | Non-admin AccessError without rights |
| Record rules | User A cannot read User B rows (or company B rows) |
| `company_id` on main model | Created records default to `env.company` |
| Company rule | User limited to company_ids sees only those rows |
| No sudo laundering | Business methods pass `with_user` tests |
| Menu `groups=` | Matched by real ACL (not UI-only) |

Write a short SECURITY.md in the module: who can do what, in one page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `company_id` defaulting to `env.company`. |
| Medium | Install the standard company rule domain; test with two companies. |
| Hard | Full story project: two companies, two users, zero cross leakage on your model. |

**WHAT THIS UNLOCKS NEXT:**

Your app is secure and usable. Story IX teaches how to **extend official Odoo apps** — `_inherit`, `_inherits`, real view patches, and Sales/Purchase/Inventory customizations — without breaking upgrades.

---


---

## 31.8 Multi-Company Domains

Multi-company is not a checkbox — it is a data model:

```python
class LibraryBook(models.Model):
    _name = "library.book"
    _check_company_auto = True

    name = fields.Char(required=True)
    company_id = fields.Many2one(
        "res.company",
        required=True,
        default=lambda self: self.env.company,
        index=True,
    )
    author_id = fields.Many2one("res.partner", check_company=True)
```

- **`env.company`** — current working company.
- **`env.companies` / `user.company_ids`** — companies the user may switch into.
- **`check_company=True`** — relational consistency: related records must share company (or be company-empty when allowed).
- **`_check_company_auto`** — enables automatic consistency checks on writes.

**STANDARD RECORD RULE PATTERN:**

```xml
<field name="domain_force">[
    '|',
    ('company_id', '=', False),
    ('company_id', 'in', company_ids),
]</field>
```

`company_ids` in rule evaluation refers to the user’s allowed companies. Empty `company_id` often means “shared across companies” — use deliberately (products sometimes; accounting rarely).

**COMPANY-DEPENDENT FIELDS:**

Some fields store **per-company values** (property-like / `company_dependent=True`). Reading them respects the active company. Writing in the wrong company silently affects another slice of data — always test with company switching.

```text
User active company = Qatar Co
  read product.standard_price  -> Qatar value

Switch to UAE Co
  read product.standard_price  -> UAE value

Same product row, different property bag.
```

**CROSS-COMPANY FAILURES:**

```text
Bad:  sale order company A, invoice company B, no checks
Bad:  record rule missing on custom model that holds money
Bad:  sudo() search without company domain in a multi-co cron

Good: company_id on transactional models
Good: check_company on money/partner links
Good: rules using company_ids
Good: tests with two companies + two users
```

**STORY PROJECT — SECURE MULTI-COMPANY MODULE**

Deliver a small module (or extend your library) that proves isolation:

| Check | Evidence |
| ----- | -------- |
| Groups User/Manager | XML + Settings matrix |
| ACL CSV | Non-admin AccessError without rights |
| Record rules | User A cannot read User B rows (or company B rows) |
| `company_id` on main model | Created records default to `env.company` |
| Company rule | User limited to company_ids sees only those rows |
| No sudo laundering | Business methods pass `with_user` tests |
| Menu `groups=` | Matched by real ACL (not UI-only) |

Write a short SECURITY.md in the module: who can do what, in one page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `company_id` defaulting to `env.company`. |
| Medium | Install the standard company rule domain; test with two companies. |
| Hard | Full story project: two companies, two users, zero cross leakage on your model. |

**WHAT THIS UNLOCKS NEXT:**

Your app is secure and usable. Story IX teaches how to **extend official Odoo apps** — `_inherit`, `_inherits`, real view patches, and Sales/Purchase/Inventory customizations — without breaking upgrades.

---


---

## 31.9 Cross-Company Security

Multi-company is not a checkbox — it is a data model:

```python
class LibraryBook(models.Model):
    _name = "library.book"
    _check_company_auto = True

    name = fields.Char(required=True)
    company_id = fields.Many2one(
        "res.company",
        required=True,
        default=lambda self: self.env.company,
        index=True,
    )
    author_id = fields.Many2one("res.partner", check_company=True)
```

- **`env.company`** — current working company.
- **`env.companies` / `user.company_ids`** — companies the user may switch into.
- **`check_company=True`** — relational consistency: related records must share company (or be company-empty when allowed).
- **`_check_company_auto`** — enables automatic consistency checks on writes.

**STANDARD RECORD RULE PATTERN:**

```xml
<field name="domain_force">[
    '|',
    ('company_id', '=', False),
    ('company_id', 'in', company_ids),
]</field>
```

`company_ids` in rule evaluation refers to the user’s allowed companies. Empty `company_id` often means “shared across companies” — use deliberately (products sometimes; accounting rarely).

**COMPANY-DEPENDENT FIELDS:**

Some fields store **per-company values** (property-like / `company_dependent=True`). Reading them respects the active company. Writing in the wrong company silently affects another slice of data — always test with company switching.

```text
User active company = Qatar Co
  read product.standard_price  -> Qatar value

Switch to UAE Co
  read product.standard_price  -> UAE value

Same product row, different property bag.
```

**CROSS-COMPANY FAILURES:**

```text
Bad:  sale order company A, invoice company B, no checks
Bad:  record rule missing on custom model that holds money
Bad:  sudo() search without company domain in a multi-co cron

Good: company_id on transactional models
Good: check_company on money/partner links
Good: rules using company_ids
Good: tests with two companies + two users
```

**STORY PROJECT — SECURE MULTI-COMPANY MODULE**

Deliver a small module (or extend your library) that proves isolation:

| Check | Evidence |
| ----- | -------- |
| Groups User/Manager | XML + Settings matrix |
| ACL CSV | Non-admin AccessError without rights |
| Record rules | User A cannot read User B rows (or company B rows) |
| `company_id` on main model | Created records default to `env.company` |
| Company rule | User limited to company_ids sees only those rows |
| No sudo laundering | Business methods pass `with_user` tests |
| Menu `groups=` | Matched by real ACL (not UI-only) |

Write a short SECURITY.md in the module: who can do what, in one page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `company_id` defaulting to `env.company`. |
| Medium | Install the standard company rule domain; test with two companies. |
| Hard | Full story project: two companies, two users, zero cross leakage on your model. |

**WHAT THIS UNLOCKS NEXT:**

Your app is secure and usable. Story IX teaches how to **extend official Odoo apps** — `_inherit`, `_inherits`, real view patches, and Sales/Purchase/Inventory customizations — without breaking upgrades.

---


---


---


**CHECK YOUR UNDERSTANDING AFTER PHASE 31:** You can explain every lesson title in Phase 31 with a concrete Odoo or business example.

> **Phase 31 complete?** [Continue to Phase 32](#phase-32)


## STORY PROJECT — Secure Multi-Company Module

**Track:** Security

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY IX — EXTENDING EXISTING ODOO

**Track:** Extension

*Prefer extending official apps over forking them.*

---
<a id="phase-32"></a>
<a id="phase-32---model-inheritance"></a>
# PHASE 32 - Model Inheritance

**Track:** Extension

**WHAT YOU WILL BE ABLE TO DO:** Extend existing models with `_inherit`, choose classical vs extension patterns, and override methods safely with `super()` under Odoo’s multi-module MRO.

**WHAT YOU SHOULD KNOW FIRST:** Stories IV–VI (models/ORM) and VII–VIII (views/security). You can build standalone models; production work extends `sale.order`, `res.partner`, etc.

**LEARNING RESOURCES:**

- [odoo/odoo — inheritance patterns in addons](https://github.com/odoo/odoo)
- [View architectures (pair with model inherit)](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [Inherit vs inherits](https://www.youtube.com/watch?v=mzg3EGD_6Gw)
- [Security reference — field groups still apply](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)

## 32.1 Extension Inheritance

Odoo inheritance is registry merging, not only Python `class Child(Parent)`.

**EXTENSION (MOST COMMON):**

Same `_name` as an existing model (often omitted when equal to inherit), plus `_inherit`:

```python
class SaleOrder(models.Model):
    _inherit = "sale.order"

    library_pickup = fields.Boolean(
        string="Library Pickup",
        help="Deliver to library desk instead of shipping.",
    )

    def action_confirm(self):
        res = super().action_confirm()
        pickup = self.filtered("library_pickup")
        if pickup:
            pickup._notify_library_desk()
        return res

    def _notify_library_desk(self):
        for order in self:
            order.message_post(body="Library pickup requested.")
```

Result: **same table**, merged fields/methods. Every module depending on `sale` that also inherits `sale.order` participates in one MRO.

**CLASSICAL INHERITANCE (NEW MODEL, SHARED TABLE BEHAVIOR):**

```python
class LibraryOffer(models.Model):
    _name = "library.offer"
    _inherit = ["mail.thread", "mail.activity.mixin"]
    _description = "Library Offer"

    name = fields.Char(required=True)
```

Here `_name` is new; `_inherit` pulls **mixin / parent** behavior (often abstract models). Distinct from delegation (`_inherits`) in Phase 33.

**SUPER() DISCIPLINE:**

```text
Module A overrides action_confirm -> super -> core
Module B overrides action_confirm -> super -> A -> core

Depends order + MRO decide who runs first.
Skipping super() drops other modules' and core behavior
(confirm without stock moves, etc.).
```

Always call `super()` unless you intentionally replace the workflow (document why). For `create`, prefer `@api.model_create_multi` and call super on the batch.

**SECURITY NOTE:**

Extending a model reuses its ACL. New sensitive fields are still readable to anyone who can read the model — use `groups=` on field definitions for field-level restriction where appropriate, plus UI inherit.

```python
salary_extra = fields.Float(groups="hr.group_hr_manager")
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `_inherit` `res.partner` with one Char field. |
| Medium | Override a method with `super()` and a side effect. |
| Hard | Two modules override the same method; predict order from `depends`. |

**WHAT THIS UNLOCKS NEXT:**

Sometimes you must not widen `res.partner` for every app. Phase 33 **`_inherits`** delegates identity to a parent row instead.

---


---

## 32.2 _inherit

Odoo inheritance is registry merging, not only Python `class Child(Parent)`.

**EXTENSION (MOST COMMON):**

Same `_name` as an existing model (often omitted when equal to inherit), plus `_inherit`:

```python
class SaleOrder(models.Model):
    _inherit = "sale.order"

    library_pickup = fields.Boolean(
        string="Library Pickup",
        help="Deliver to library desk instead of shipping.",
    )

    def action_confirm(self):
        res = super().action_confirm()
        pickup = self.filtered("library_pickup")
        if pickup:
            pickup._notify_library_desk()
        return res

    def _notify_library_desk(self):
        for order in self:
            order.message_post(body="Library pickup requested.")
```

Result: **same table**, merged fields/methods. Every module depending on `sale` that also inherits `sale.order` participates in one MRO.

**CLASSICAL INHERITANCE (NEW MODEL, SHARED TABLE BEHAVIOR):**

```python
class LibraryOffer(models.Model):
    _name = "library.offer"
    _inherit = ["mail.thread", "mail.activity.mixin"]
    _description = "Library Offer"

    name = fields.Char(required=True)
```

Here `_name` is new; `_inherit` pulls **mixin / parent** behavior (often abstract models). Distinct from delegation (`_inherits`) in Phase 33.

**SUPER() DISCIPLINE:**

```text
Module A overrides action_confirm -> super -> core
Module B overrides action_confirm -> super -> A -> core

Depends order + MRO decide who runs first.
Skipping super() drops other modules' and core behavior
(confirm without stock moves, etc.).
```

Always call `super()` unless you intentionally replace the workflow (document why). For `create`, prefer `@api.model_create_multi` and call super on the batch.

**SECURITY NOTE:**

Extending a model reuses its ACL. New sensitive fields are still readable to anyone who can read the model — use `groups=` on field definitions for field-level restriction where appropriate, plus UI inherit.

```python
salary_extra = fields.Float(groups="hr.group_hr_manager")
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `_inherit` `res.partner` with one Char field. |
| Medium | Override a method with `super()` and a side effect. |
| Hard | Two modules override the same method; predict order from `depends`. |

**WHAT THIS UNLOCKS NEXT:**

Sometimes you must not widen `res.partner` for every app. Phase 33 **`_inherits`** delegates identity to a parent row instead.

---


---

## 32.3 Classical Inheritance

Odoo inheritance is registry merging, not only Python `class Child(Parent)`.

**EXTENSION (MOST COMMON):**

Same `_name` as an existing model (often omitted when equal to inherit), plus `_inherit`:

```python
class SaleOrder(models.Model):
    _inherit = "sale.order"

    library_pickup = fields.Boolean(
        string="Library Pickup",
        help="Deliver to library desk instead of shipping.",
    )

    def action_confirm(self):
        res = super().action_confirm()
        pickup = self.filtered("library_pickup")
        if pickup:
            pickup._notify_library_desk()
        return res

    def _notify_library_desk(self):
        for order in self:
            order.message_post(body="Library pickup requested.")
```

Result: **same table**, merged fields/methods. Every module depending on `sale` that also inherits `sale.order` participates in one MRO.

**CLASSICAL INHERITANCE (NEW MODEL, SHARED TABLE BEHAVIOR):**

```python
class LibraryOffer(models.Model):
    _name = "library.offer"
    _inherit = ["mail.thread", "mail.activity.mixin"]
    _description = "Library Offer"

    name = fields.Char(required=True)
```

Here `_name` is new; `_inherit` pulls **mixin / parent** behavior (often abstract models). Distinct from delegation (`_inherits`) in Phase 33.

**SUPER() DISCIPLINE:**

```text
Module A overrides action_confirm -> super -> core
Module B overrides action_confirm -> super -> A -> core

Depends order + MRO decide who runs first.
Skipping super() drops other modules' and core behavior
(confirm without stock moves, etc.).
```

Always call `super()` unless you intentionally replace the workflow (document why). For `create`, prefer `@api.model_create_multi` and call super on the batch.

**SECURITY NOTE:**

Extending a model reuses its ACL. New sensitive fields are still readable to anyone who can read the model — use `groups=` on field definitions for field-level restriction where appropriate, plus UI inherit.

```python
salary_extra = fields.Float(groups="hr.group_hr_manager")
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `_inherit` `res.partner` with one Char field. |
| Medium | Override a method with `super()` and a side effect. |
| Hard | Two modules override the same method; predict order from `depends`. |

**WHAT THIS UNLOCKS NEXT:**

Sometimes you must not widen `res.partner` for every app. Phase 33 **`_inherits`** delegates identity to a parent row instead.

---


---

## 32.4 Method Overriding

**Method Overriding** belongs to **Model Inheritance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Method Overriding** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Method Overriding** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Method Overriding**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Method Overriding** is concrete, continue — the next lesson in this phase still depends on it.

## 32.5 Field Extension

Odoo inheritance is registry merging, not only Python `class Child(Parent)`.

**EXTENSION (MOST COMMON):**

Same `_name` as an existing model (often omitted when equal to inherit), plus `_inherit`:

```python
class SaleOrder(models.Model):
    _inherit = "sale.order"

    library_pickup = fields.Boolean(
        string="Library Pickup",
        help="Deliver to library desk instead of shipping.",
    )

    def action_confirm(self):
        res = super().action_confirm()
        pickup = self.filtered("library_pickup")
        if pickup:
            pickup._notify_library_desk()
        return res

    def _notify_library_desk(self):
        for order in self:
            order.message_post(body="Library pickup requested.")
```

Result: **same table**, merged fields/methods. Every module depending on `sale` that also inherits `sale.order` participates in one MRO.

**CLASSICAL INHERITANCE (NEW MODEL, SHARED TABLE BEHAVIOR):**

```python
class LibraryOffer(models.Model):
    _name = "library.offer"
    _inherit = ["mail.thread", "mail.activity.mixin"]
    _description = "Library Offer"

    name = fields.Char(required=True)
```

Here `_name` is new; `_inherit` pulls **mixin / parent** behavior (often abstract models). Distinct from delegation (`_inherits`) in Phase 33.

**SUPER() DISCIPLINE:**

```text
Module A overrides action_confirm -> super -> core
Module B overrides action_confirm -> super -> A -> core

Depends order + MRO decide who runs first.
Skipping super() drops other modules' and core behavior
(confirm without stock moves, etc.).
```

Always call `super()` unless you intentionally replace the workflow (document why). For `create`, prefer `@api.model_create_multi` and call super on the batch.

**SECURITY NOTE:**

Extending a model reuses its ACL. New sensitive fields are still readable to anyone who can read the model — use `groups=` on field definitions for field-level restriction where appropriate, plus UI inherit.

```python
salary_extra = fields.Float(groups="hr.group_hr_manager")
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | `_inherit` `res.partner` with one Char field. |
| Medium | Override a method with `super()` and a side effect. |
| Hard | Two modules override the same method; predict order from `depends`. |

**WHAT THIS UNLOCKS NEXT:**

Sometimes you must not widen `res.partner` for every app. Phase 33 **`_inherits`** delegates identity to a parent row instead.

---


---

## 32.6 super()

**super()** belongs to **Model Inheritance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **super()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **super()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **super()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **super()** is concrete, continue — the next lesson in this phase still depends on it.

## 32.7 Multiple Inheritance

**Multiple Inheritance** belongs to **Model Inheritance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Multiple Inheritance** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Multiple Inheritance** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Multiple Inheritance**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Multiple Inheritance** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 32:** You can explain every lesson title in Phase 32 with a concrete Odoo or business example.

> **Phase 32 complete?** [Continue to Phase 33](#phase-33)


<a id="phase-33"></a>
# PHASE 33 - Delegation & Mixins

**Track:** Extension

**WHAT YOU WILL BE ABLE TO DO:** Compose models with `_inherits` delegation, create parent rows correctly, and choose `_inherit` vs `_inherits` with a clear criterion.

**WHAT YOU SHOULD KNOW FIRST:** Phase 32 — extension inheritance. Relational fields (Many2one) from Story IV.

**LEARNING RESOURCES:**

- [odoo/odoo — product.template / product.product delegation](https://github.com/odoo/odoo)
- [_inherit vs _inherits explained](https://www.youtube.com/watch?v=hwfhO2IGhmc)
- [Security still applies on both tables](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)

## 33.1 _inherits

**Delegation** creates a **new** model/table that links to a parent via a required Many2one. Parent fields are readable/writable through the child as if they were local.

```python
class LibraryMember(models.Model):
    _name = "library.member"
    _description = "Library Member"
    _inherits = {"res.partner": "partner_id"}

    partner_id = fields.Many2one(
        "res.partner",
        required=True,
        ondelete="cascade",
        delegate=True,
    )
    card_number = fields.Char(required=True, copy=False)
    membership_level = fields.Selection(
        [("bronze", "Bronze"), ("silver", "Silver"), ("gold", "Gold")],
        default="bronze",
    )
```

```text
library.member                         res.partner
+------------------+                   +----------------+
| id               |                   | id             |
| partner_id  -----+------------------>| name           |
| card_number      |                   | email          |
| membership_level |                   | phone          |
+------------------+                   +----------------+

member.email  ==  member.partner_id.email   (delegated)
create({name: "Ada", card_number: "L-1"})
  -> creates partner + member
```

**_INHERIT VS _INHERITS:**

| | `_inherit` extension | `_inherits` delegation |
| - | -------------------- | ---------------------- |
| Table | Same as parent model | New table + parent row |
| Use when | Field/behavior belongs on the official model for all | Specialized role with its own identity |
| Example | `sale.order` pickup flag | `product.product` → `product.template`; member → partner |
| Risk | Polluting core model with niche columns | Create/unlink cascades; extra joins |

**ACL:** both models need access rights. Delegated writes still touch `res.partner` — partner ACL/rules apply when the ORM writes through.

**WHEN NOT TO DELEGATE:**

If you only need a couple of related fields, a Many2one + `related=` fields may be simpler. Use `_inherits` when the child *is-a* specialized partner/product with its own lifecycle and screens.

**DECISION TREE:**

```text
Need extra behavior on an official document used everywhere?
  -> _inherit extension (same _name) + view xpath

Need a specialized role with its own menu, ACL, and identity,
while reusing partner/product fields?
  -> _inherits delegation (new _name + parent Many2one)

Need shared methods only (chatter, activities)?
  -> _inherit abstract mixins (mail.thread, ...) with a NEW _name

Need a one-off related display field?
  -> Many2one + related=  (no inheritance ceremony)
```

**UNLINK / ONDELETE:**

`ondelete="cascade"` on `partner_id` means deleting the member can delete the partner — catastrophic if that partner is also a customer. Prefer `restrict` or careful business methods when the parent is shared master data. Official `product.product` / `product.template` carefully owns that lifecycle; copy the pattern, do not invent cascades casually.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Sketch member→partner tables and which fields live where. |
| Medium | Implement `_inherits` member; create from UI and find both rows in DB. |
| Hard | Decide inherit vs inherits for “Patient” in a clinic app; write three sentences justifying. |

**WHAT THIS UNLOCKS NEXT:**

Model extension alone does not change screens. Phase 34 applies **view inheritance in real modules** — denser XPath against official arches.

---


---

## 33.2 Delegated Fields

**Delegated Fields** belongs to **Delegation & Mixins**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Delegated Fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Delegated Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Delegated Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Delegated Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 33.3 Delegation vs Extension

**Delegation vs Extension** belongs to **Delegation & Mixins**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Delegation vs Extension** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Delegation vs Extension** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Delegation vs Extension**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Delegation vs Extension** is concrete, continue — the next lesson in this phase still depends on it.

## 33.4 AbstractModel

**AbstractModel** belongs to **Delegation & Mixins**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **AbstractModel** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **AbstractModel** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **AbstractModel**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **AbstractModel** is concrete, continue — the next lesson in this phase still depends on it.

## 33.5 Mixins

**Mixins** belongs to **Delegation & Mixins**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Mixins** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Mixins** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Mixins**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Mixins** is concrete, continue — the next lesson in this phase still depends on it.

## 33.6 Reusable Model Behavior

**Reusable Model Behavior** belongs to **Delegation & Mixins**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Reusable Model Behavior** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Reusable Model Behavior** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Reusable Model Behavior**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Reusable Model Behavior** is concrete, continue — the next lesson in this phase still depends on it.

## 33.7 Composition

**Delegation** creates a **new** model/table that links to a parent via a required Many2one. Parent fields are readable/writable through the child as if they were local.

```python
class LibraryMember(models.Model):
    _name = "library.member"
    _description = "Library Member"
    _inherits = {"res.partner": "partner_id"}

    partner_id = fields.Many2one(
        "res.partner",
        required=True,
        ondelete="cascade",
        delegate=True,
    )
    card_number = fields.Char(required=True, copy=False)
    membership_level = fields.Selection(
        [("bronze", "Bronze"), ("silver", "Silver"), ("gold", "Gold")],
        default="bronze",
    )
```

```text
library.member                         res.partner
+------------------+                   +----------------+
| id               |                   | id             |
| partner_id  -----+------------------>| name           |
| card_number      |                   | email          |
| membership_level |                   | phone          |
+------------------+                   +----------------+

member.email  ==  member.partner_id.email   (delegated)
create({name: "Ada", card_number: "L-1"})
  -> creates partner + member
```

**_INHERIT VS _INHERITS:**

| | `_inherit` extension | `_inherits` delegation |
| - | -------------------- | ---------------------- |
| Table | Same as parent model | New table + parent row |
| Use when | Field/behavior belongs on the official model for all | Specialized role with its own identity |
| Example | `sale.order` pickup flag | `product.product` → `product.template`; member → partner |
| Risk | Polluting core model with niche columns | Create/unlink cascades; extra joins |

**ACL:** both models need access rights. Delegated writes still touch `res.partner` — partner ACL/rules apply when the ORM writes through.

**WHEN NOT TO DELEGATE:**

If you only need a couple of related fields, a Many2one + `related=` fields may be simpler. Use `_inherits` when the child *is-a* specialized partner/product with its own lifecycle and screens.

**DECISION TREE:**

```text
Need extra behavior on an official document used everywhere?
  -> _inherit extension (same _name) + view xpath

Need a specialized role with its own menu, ACL, and identity,
while reusing partner/product fields?
  -> _inherits delegation (new _name + parent Many2one)

Need shared methods only (chatter, activities)?
  -> _inherit abstract mixins (mail.thread, ...) with a NEW _name

Need a one-off related display field?
  -> Many2one + related=  (no inheritance ceremony)
```

**UNLINK / ONDELETE:**

`ondelete="cascade"` on `partner_id` means deleting the member can delete the partner — catastrophic if that partner is also a customer. Prefer `restrict` or careful business methods when the parent is shared master data. Official `product.product` / `product.template` carefully owns that lifecycle; copy the pattern, do not invent cascades casually.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Sketch member→partner tables and which fields live where. |
| Medium | Implement `_inherits` member; create from UI and find both rows in DB. |
| Hard | Decide inherit vs inherits for “Patient” in a clinic app; write three sentences justifying. |

**WHAT THIS UNLOCKS NEXT:**

Model extension alone does not change screens. Phase 34 applies **view inheritance in real modules** — denser XPath against official arches.

---


---


<a id="phase-34"></a>
<a id="phase-34---view-inheritance-in-real-modules"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 33:** You can explain every lesson title in Phase 33 with a concrete Odoo or business example.

> **Phase 33 complete?** [Continue to Phase 34](#phase-34)


<a id="phase-34"></a>
# PHASE 34 - View Inheritance in Real Modules

**Track:** Extension

**WHAT YOU WILL BE ABLE TO DO:** Patch dense official form/list/search arches with reliable XPath, coordinate primary/secondary inherits, and survive upstream structure changes safely.

**WHAT YOU SHOULD KNOW FIRST:** Phase 26 — XPath basics. Phase 32 — fields exist on the extended model.

**LEARNING RESOURCES:**

- [View architectures](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [sale / purchase / stock view XML in odoo/odoo](https://github.com/odoo/odoo)
- [Real-module inheritance](https://www.youtube.com/watch?v=r11U1pqGqQQ)

## 34.1 Finding Parent Views

**Finding Parent Views** belongs to **View Inheritance in Real Modules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Finding Parent Views** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Finding Parent Views** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Finding Parent Views**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Finding Parent Views** is concrete, continue — the next lesson in this phase still depends on it.

## 34.2 External IDs

**External IDs** belongs to **View Inheritance in Real Modules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **External IDs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **External IDs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **External IDs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **External IDs** is concrete, continue — the next lesson in this phase still depends on it.

## 34.3 XPath Targeting

**XPath Targeting** belongs to **View Inheritance in Real Modules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **XPath Targeting** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **XPath Targeting** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **XPath Targeting**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **XPath Targeting** is concrete, continue — the next lesson in this phase still depends on it.

## 34.4 Adding Fields

**Adding Fields** belongs to **View Inheritance in Real Modules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Adding Fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Adding Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Adding Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Adding Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 34.5 Modifying Attributes

**Modifying Attributes** belongs to **View Inheritance in Real Modules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Modifying Attributes** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Modifying Attributes** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Modifying Attributes**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Modifying Attributes** is concrete, continue — the next lesson in this phase still depends on it.

## 34.6 Replacing Elements

**Replacing Elements** belongs to **View Inheritance in Real Modules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Replacing Elements** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Replacing Elements** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Replacing Elements**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Replacing Elements** is concrete, continue — the next lesson in this phase still depends on it.

## 34.7 Debugging Inheritance Conflicts

**Debugging Inheritance Conflicts** belongs to **View Inheritance in Real Modules**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Debugging Inheritance Conflicts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Debugging Inheritance Conflicts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Debugging Inheritance Conflicts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Debugging Inheritance Conflicts** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 34:** You can explain every lesson title in Phase 34 with a concrete Odoo or business example.

> **Phase 34 complete?** [Continue to Phase 35](#phase-35)


<a id="phase-35"></a>
# PHASE 35 - Extending Official Applications

**Track:** Extension

**WHAT YOU WILL BE ABLE TO DO:** Customize `sale.order`, purchase, and stock workflows with fields, super() hooks, view inherits, and security that respects official flows.

**WHAT YOU SHOULD KNOW FIRST:** Phases 32–34 — model + view inheritance. Story VIII — do not ship customizations without ACL awareness.

**LEARNING RESOURCES:**

- [View architectures](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [Security reference](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [odoo/odoo addons: sale, purchase, stock](https://github.com/odoo/odoo)
- [Extending business flows](https://www.youtube.com/watch?v=mzg3EGD_6Gw)

## 35.1 res.partner

**res.partner** belongs to **Extending Official Applications**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **res.partner** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **res.partner** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **res.partner**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **res.partner** is concrete, continue — the next lesson in this phase still depends on it.

## 35.2 sale.order

**sale.order** belongs to **Extending Official Applications**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **sale.order** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **sale.order** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **sale.order**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **sale.order** is concrete, continue — the next lesson in this phase still depends on it.

## 35.3 purchase.order

**SALES — HOOK THE LIFECYCLE, DO NOT FORK IT:**

```python
class SaleOrder(models.Model):
    _inherit = "sale.order"

    client_po_ref = fields.Char(string="Client PO")

    def action_confirm(self):
        for order in self:
            if order.library_pickup and not order.client_po_ref:
                raise UserError("Client PO required for library pickup orders.")
        return super().action_confirm()
```

Extend lines similarly (`sale.order.line`) for product-level flags. Prefer constraints (`@api.constrains`) for invariant checks that must hold beyond one button.

**PURCHASE — MIRROR DISCIPLINE:**

```python
class PurchaseOrder(models.Model):
    _inherit = "purchase.order"

    laboratory_required = fields.Boolean()

    def button_approve(self, force=False):
        res = super().button_approve(force=force)
        self.filtered("laboratory_required")._create_lab_activity()
        return res
```

Know the official method names (`button_confirm`, `button_approve`, `button_cancel`) from the version you run — read the source, do not invent hooks.

**INVENTORY / STOCK:**

```python
class StockMove(models.Model):
    _inherit = "stock.move"

    def _action_done(self, cancel_backorder=False):
        res = super()._action_done(cancel_backorder=cancel_backorder)
        # never swallow errors from super; stock quant integrity depends on it
        return res
```

Stock is unforgiving: skipping `super()` or sudo-writing quants is how production inventory corrupts. Extend carefully; test with reserved quantities.

**DEPENDS AND DATA:**

```python
# __manifest__.py
{
    "name": "Library Sales Bridge",
    "depends": ["sale_management", "stock", "library_app"],
    "data": [
        "views/sale_order_views.xml",
        "security/ir.model.access.csv",  # only if you add NEW models
    ],
}
```

New *models* need ACL. Pure field extension on `sale.order` reuses sale’s access — still hide sensitive fields with `groups=` when needed.

**CROSS-APP CONSISTENCY:**

```text
sale.order (library_pickup)
    |
    v  on confirm
stock.picking / moves  (carry a related flag if warehouse must see it)
    |
    v
custom report / activity for library desk
```

Pass information through fields on official documents rather than parallel shadow tables when the business document *is* the sales order.

**STORY PROJECT — CUSTOMIZE AN OFFICIAL ODOO APP**

Build `library_sale_bridge` (name flexible) that:

1. Depends on `sale_management` (and stock if you touch deliveries).
2. Adds ≥2 fields on `sale.order` and/or `sale.order.line`.
3. Overrides one workflow method with `super()` + validation or side effect.
4. Inherits `sale.view_order_form` (and list/search) via XPath — no full form copy.
5. Adds a search filter for the customization.
6. Includes a short README: upgrade notes + which XPath anchors you rely on.
7. Passes a manual test: confirm order as a sales user (non-admin); AccessError-free; stock/sale still completes.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a Char on `sale.order` + form XPath. |
| Medium | `action_confirm` guard with `UserError` + super. |
| Hard | Full story project including list filter and non-admin test. |

**WHAT THIS UNLOCKS NEXT:**

You can build UIs, lock them down across companies, and extend official apps without forks. Story X starts **advanced business features** — wizards, sequences, cron, automated actions, chatter, reports — the automation layer on top of stable models and screens.

---


---

## 35.4 stock.picking

**SALES — HOOK THE LIFECYCLE, DO NOT FORK IT:**

```python
class SaleOrder(models.Model):
    _inherit = "sale.order"

    client_po_ref = fields.Char(string="Client PO")

    def action_confirm(self):
        for order in self:
            if order.library_pickup and not order.client_po_ref:
                raise UserError("Client PO required for library pickup orders.")
        return super().action_confirm()
```

Extend lines similarly (`sale.order.line`) for product-level flags. Prefer constraints (`@api.constrains`) for invariant checks that must hold beyond one button.

**PURCHASE — MIRROR DISCIPLINE:**

```python
class PurchaseOrder(models.Model):
    _inherit = "purchase.order"

    laboratory_required = fields.Boolean()

    def button_approve(self, force=False):
        res = super().button_approve(force=force)
        self.filtered("laboratory_required")._create_lab_activity()
        return res
```

Know the official method names (`button_confirm`, `button_approve`, `button_cancel`) from the version you run — read the source, do not invent hooks.

**INVENTORY / STOCK:**

```python
class StockMove(models.Model):
    _inherit = "stock.move"

    def _action_done(self, cancel_backorder=False):
        res = super()._action_done(cancel_backorder=cancel_backorder)
        # never swallow errors from super; stock quant integrity depends on it
        return res
```

Stock is unforgiving: skipping `super()` or sudo-writing quants is how production inventory corrupts. Extend carefully; test with reserved quantities.

**DEPENDS AND DATA:**

```python
# __manifest__.py
{
    "name": "Library Sales Bridge",
    "depends": ["sale_management", "stock", "library_app"],
    "data": [
        "views/sale_order_views.xml",
        "security/ir.model.access.csv",  # only if you add NEW models
    ],
}
```

New *models* need ACL. Pure field extension on `sale.order` reuses sale’s access — still hide sensitive fields with `groups=` when needed.

**CROSS-APP CONSISTENCY:**

```text
sale.order (library_pickup)
    |
    v  on confirm
stock.picking / moves  (carry a related flag if warehouse must see it)
    |
    v
custom report / activity for library desk
```

Pass information through fields on official documents rather than parallel shadow tables when the business document *is* the sales order.

**STORY PROJECT — CUSTOMIZE AN OFFICIAL ODOO APP**

Build `library_sale_bridge` (name flexible) that:

1. Depends on `sale_management` (and stock if you touch deliveries).
2. Adds ≥2 fields on `sale.order` and/or `sale.order.line`.
3. Overrides one workflow method with `super()` + validation or side effect.
4. Inherits `sale.view_order_form` (and list/search) via XPath — no full form copy.
5. Adds a search filter for the customization.
6. Includes a short README: upgrade notes + which XPath anchors you rely on.
7. Passes a manual test: confirm order as a sales user (non-admin); AccessError-free; stock/sale still completes.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a Char on `sale.order` + form XPath. |
| Medium | `action_confirm` guard with `UserError` + super. |
| Hard | Full story project including list filter and non-admin test. |

**WHAT THIS UNLOCKS NEXT:**

You can build UIs, lock them down across companies, and extend official apps without forks. Story X starts **advanced business features** — wizards, sequences, cron, automated actions, chatter, reports — the automation layer on top of stable models and screens.

---


---

## 35.5 account.move

**account.move** belongs to **Extending Official Applications**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **account.move** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **account.move** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **account.move**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **account.move** is concrete, continue — the next lesson in this phase still depends on it.

## 35.6 Existing Workflows

**Existing Workflows** belongs to **Extending Official Applications**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Existing Workflows** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Existing Workflows** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Existing Workflows**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Existing Workflows** is concrete, continue — the next lesson in this phase still depends on it.

## 35.7 Extension Points

**SALES — HOOK THE LIFECYCLE, DO NOT FORK IT:**

```python
class SaleOrder(models.Model):
    _inherit = "sale.order"

    client_po_ref = fields.Char(string="Client PO")

    def action_confirm(self):
        for order in self:
            if order.library_pickup and not order.client_po_ref:
                raise UserError("Client PO required for library pickup orders.")
        return super().action_confirm()
```

Extend lines similarly (`sale.order.line`) for product-level flags. Prefer constraints (`@api.constrains`) for invariant checks that must hold beyond one button.

**PURCHASE — MIRROR DISCIPLINE:**

```python
class PurchaseOrder(models.Model):
    _inherit = "purchase.order"

    laboratory_required = fields.Boolean()

    def button_approve(self, force=False):
        res = super().button_approve(force=force)
        self.filtered("laboratory_required")._create_lab_activity()
        return res
```

Know the official method names (`button_confirm`, `button_approve`, `button_cancel`) from the version you run — read the source, do not invent hooks.

**INVENTORY / STOCK:**

```python
class StockMove(models.Model):
    _inherit = "stock.move"

    def _action_done(self, cancel_backorder=False):
        res = super()._action_done(cancel_backorder=cancel_backorder)
        # never swallow errors from super; stock quant integrity depends on it
        return res
```

Stock is unforgiving: skipping `super()` or sudo-writing quants is how production inventory corrupts. Extend carefully; test with reserved quantities.

**DEPENDS AND DATA:**

```python
# __manifest__.py
{
    "name": "Library Sales Bridge",
    "depends": ["sale_management", "stock", "library_app"],
    "data": [
        "views/sale_order_views.xml",
        "security/ir.model.access.csv",  # only if you add NEW models
    ],
}
```

New *models* need ACL. Pure field extension on `sale.order` reuses sale’s access — still hide sensitive fields with `groups=` when needed.

**CROSS-APP CONSISTENCY:**

```text
sale.order (library_pickup)
    |
    v  on confirm
stock.picking / moves  (carry a related flag if warehouse must see it)
    |
    v
custom report / activity for library desk
```

Pass information through fields on official documents rather than parallel shadow tables when the business document *is* the sales order.

**STORY PROJECT — CUSTOMIZE AN OFFICIAL ODOO APP**

Build `library_sale_bridge` (name flexible) that:

1. Depends on `sale_management` (and stock if you touch deliveries).
2. Adds ≥2 fields on `sale.order` and/or `sale.order.line`.
3. Overrides one workflow method with `super()` + validation or side effect.
4. Inherits `sale.view_order_form` (and list/search) via XPath — no full form copy.
5. Adds a search filter for the customization.
6. Includes a short README: upgrade notes + which XPath anchors you rely on.
7. Passes a manual test: confirm order as a sales user (non-admin); AccessError-free; stock/sale still completes.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a Char on `sale.order` + form XPath. |
| Medium | `action_confirm` guard with `UserError` + super. |
| Hard | Full story project including list filter and non-admin test. |

**WHAT THIS UNLOCKS NEXT:**

You can build UIs, lock them down across companies, and extend official apps without forks. Story X starts **advanced business features** — wizards, sequences, cron, automated actions, chatter, reports — the automation layer on top of stable models and screens.

---


---

## 35.8 Upgrade-Safe Customization

**Upgrade-Safe Customization** belongs to **Extending Official Applications**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Upgrade-Safe Customization** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Upgrade-Safe Customization** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Upgrade-Safe Customization**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Upgrade-Safe Customization** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 35:** You can explain every lesson title in Phase 35 with a concrete Odoo or business example.

> **Phase 35 complete?** [Continue to Phase 36](#phase-36)


## STORY PROJECT — Customize an Official Odoo App

**Track:** Extension

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY X — ADVANCED BUSINESS FEATURES

**Track:** Advanced Features

*Wizards, sequences, cron, chatter, reports, and data files run real operations.*

---
<a id="phase-36"></a>
<a id="phase-36---wizards-transientmodel"></a>
# PHASE 36 - Wizards & TransientModel

**Track:** Advanced Features

**WHAT YOU WILL BE ABLE TO DO:** Build multi-step dialogs with `models.TransientModel`, collect input from list/form selections via `active_ids`, apply business changes once, and close the dialog without polluting permanent tables.

**WHAT YOU SHOULD KNOW FIRST:** Phase 35 — you can extend Sales / Purchase / Inventory with fields and methods. Phase 23 — form views and buttons. Phase 25 — `ir.actions.act_window` and bindings. Phase 14 — `create` / `write` on real models.

**LEARNING RESOURCES:**

- [ORM — Transient models (Odoo 18)](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#transient-models)
- [Actions — window target `new`](https://www.odoo.com/documentation/18.0/developer/reference/backend/actions.html)
- [View architectures](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [odoo/odoo](https://github.com/odoo/odoo) — search `TransientModel` under `addons/` for real wizards
- [Server Framework 101](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101.html)

## 36.1 TransientModel

`models.TransientModel` is still an ORM model. It has fields, constraints, `create`/`write`, and a PostgreSQL table. The difference is **lifecycle and access**:

- Records are meant to be **short-lived**.
- Odoo regularly **vacuums** old transient rows (`_transient_vacuum`), using limits such as `_transient_max_hours` (default around one hour) and optional `_transient_max_count`.
- Access is simplified: users can create; they normally only see **their own** transient records. The superuser sees all.

```python
from odoo import api, fields, models


class PickingScheduleWizard(models.TransientModel):
    _name = "stock.picking.schedule.wizard"
    _description = "Reschedule selected pickings"

    scheduled_date = fields.Datetime(required=True)
    picking_ids = fields.Many2many("stock.picking", string="Pickings")

    @api.model
    def default_get(self, fields_list):
        res = super().default_get(fields_list)
        active_ids = self.env.context.get("active_ids", [])
        if self.env.context.get("active_model") == "stock.picking" and active_ids:
            res["picking_ids"] = [(6, 0, active_ids)]
        return res

    def action_apply(self):
        self.ensure_one()
        if not self.picking_ids:
            raise models.UserError("Select at least one picking.")
        self.picking_ids.write({"scheduled_date": self.scheduled_date})
        return {"type": "ir.actions.act_window_close"}
```

**HOW `active_ids` REACH THE WIZARD:**

When a user selects rows in a list and opens an Action, the client puts `active_model`, `active_id`, and `active_ids` into the action **context**. Your wizard’s `default_get` (or field defaults) reads that context. Never assume the user is still looking at the same list when `action_apply` runs — trust the Many2many you stored on the wizard, or re-browse `active_ids` carefully.

**TRANSIENT VS ABSTRACT VS MODEL:**

| Class | Stored? | Typical use |
| ----- | ------- | ----------- |
| `Model` | Yes, permanent | Orders, partners, products |
| `TransientModel` | Yes, vacuumed | Wizards, assistants |
| `AbstractModel` | No table | Mixins (`mail.thread`) |

**WHERE WIZARDS FAIL:**

- Putting long-term audit data only on the transient row (it will vanish).
- Doing heavy work without batching (`for picking in self.picking_ids: picking.do_something()` with N queries).
- Returning nothing useful — prefer `act_window_close`, a reload action, or a notification client action so the user sees the result.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write a TransientModel with one required Char and an `action_apply` that raises if empty. |
| Medium | Implement `default_get` that copies `active_ids` into a Many2many. |
| Hard | Add an `@api.constrains` that rejects a date in the past; show the message in the dialog. |

**WHAT THIS UNLOCKS NEXT:**

A model without a view is invisible. The next lesson wires the modal action and list binding.

---

## 36.2 Wizard Fields

Open the wizard with an `ir.actions.act_window` whose `res_model` is the transient model, `view_mode` is `form`, and **`target` is `new`** (modal dialog). Bind it to the host model so it appears under **Action** on list/form.

```xml
<record id="action_picking_schedule_wizard" model="ir.actions.act_window">
    <field name="name">Reschedule</field>
    <field name="res_model">stock.picking.schedule.wizard</field>
    <field name="view_mode">form</field>
    <field name="target">new</field>
    <field name="binding_model_id" ref="stock.model_stock_picking"/>
    <field name="binding_view_types">list,form</field>
</record>

<record id="view_picking_schedule_wizard_form" model="ir.ui.view">
    <field name="name">stock.picking.schedule.wizard.form</field>
    <field name="model">stock.picking.schedule.wizard</field>
    <field name="arch" type="xml">
        <form string="Reschedule pickings">
            <group>
                <field name="scheduled_date"/>
                <field name="picking_ids" widget="many2many_tags"/>
            </group>
            <footer>
                <button name="action_apply" type="object" string="Apply" class="btn-primary"/>
                <button string="Cancel" special="cancel"/>
            </footer>
        </form>
    </field>
</record>
```

**MULTI-STEP WIZARDS:**

Occasionally `action_apply` returns *another* `act_window` on a second transient model (step 2). Keep that rare. Most business dialogs are one screen. If you need a long assistant, consider a dedicated client action later (Story XIII) — but learn the TransientModel pattern first; it is what core Odoo uses constantly.

**SECURITY NOTE:**

Wizard apply methods run as the logged-in user. They must not `sudo()` casually to “make the write work.” If the user cannot write the picking, the wizard should fail with `AccessError` — that is correct. Grant rights on the real model, not a silent elevation inside Apply.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add Cancel (`special="cancel"`) and primary Apply buttons. |
| Medium | Bind the wizard only to list view; verify form Action menu does not show it. |
| Hard | Return a `display_notification` client action after apply, then close — so users see “20 pickings updated.” |

**WHAT THIS UNLOCKS NEXT:**

Wizards collect decisions. Business documents still need **human-readable numbers** — SO001, INV/2026/0042 — that stay unique under concurrency. That is sequences.

---


---

## 36.3 Wizard Views

Open the wizard with an `ir.actions.act_window` whose `res_model` is the transient model, `view_mode` is `form`, and **`target` is `new`** (modal dialog). Bind it to the host model so it appears under **Action** on list/form.

```xml
<record id="action_picking_schedule_wizard" model="ir.actions.act_window">
    <field name="name">Reschedule</field>
    <field name="res_model">stock.picking.schedule.wizard</field>
    <field name="view_mode">form</field>
    <field name="target">new</field>
    <field name="binding_model_id" ref="stock.model_stock_picking"/>
    <field name="binding_view_types">list,form</field>
</record>

<record id="view_picking_schedule_wizard_form" model="ir.ui.view">
    <field name="name">stock.picking.schedule.wizard.form</field>
    <field name="model">stock.picking.schedule.wizard</field>
    <field name="arch" type="xml">
        <form string="Reschedule pickings">
            <group>
                <field name="scheduled_date"/>
                <field name="picking_ids" widget="many2many_tags"/>
            </group>
            <footer>
                <button name="action_apply" type="object" string="Apply" class="btn-primary"/>
                <button string="Cancel" special="cancel"/>
            </footer>
        </form>
    </field>
</record>
```

**MULTI-STEP WIZARDS:**

Occasionally `action_apply` returns *another* `act_window` on a second transient model (step 2). Keep that rare. Most business dialogs are one screen. If you need a long assistant, consider a dedicated client action later (Story XIII) — but learn the TransientModel pattern first; it is what core Odoo uses constantly.

**SECURITY NOTE:**

Wizard apply methods run as the logged-in user. They must not `sudo()` casually to “make the write work.” If the user cannot write the picking, the wizard should fail with `AccessError` — that is correct. Grant rights on the real model, not a silent elevation inside Apply.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add Cancel (`special="cancel"`) and primary Apply buttons. |
| Medium | Bind the wizard only to list view; verify form Action menu does not show it. |
| Hard | Return a `display_notification` client action after apply, then close — so users see “20 pickings updated.” |

**WHAT THIS UNLOCKS NEXT:**

Wizards collect decisions. Business documents still need **human-readable numbers** — SO001, INV/2026/0042 — that stay unique under concurrency. That is sequences.

---


---

## 36.4 target="new"

Open the wizard with an `ir.actions.act_window` whose `res_model` is the transient model, `view_mode` is `form`, and **`target` is `new`** (modal dialog). Bind it to the host model so it appears under **Action** on list/form.

```xml
<record id="action_picking_schedule_wizard" model="ir.actions.act_window">
    <field name="name">Reschedule</field>
    <field name="res_model">stock.picking.schedule.wizard</field>
    <field name="view_mode">form</field>
    <field name="target">new</field>
    <field name="binding_model_id" ref="stock.model_stock_picking"/>
    <field name="binding_view_types">list,form</field>
</record>

<record id="view_picking_schedule_wizard_form" model="ir.ui.view">
    <field name="name">stock.picking.schedule.wizard.form</field>
    <field name="model">stock.picking.schedule.wizard</field>
    <field name="arch" type="xml">
        <form string="Reschedule pickings">
            <group>
                <field name="scheduled_date"/>
                <field name="picking_ids" widget="many2many_tags"/>
            </group>
            <footer>
                <button name="action_apply" type="object" string="Apply" class="btn-primary"/>
                <button string="Cancel" special="cancel"/>
            </footer>
        </form>
    </field>
</record>
```

**MULTI-STEP WIZARDS:**

Occasionally `action_apply` returns *another* `act_window` on a second transient model (step 2). Keep that rare. Most business dialogs are one screen. If you need a long assistant, consider a dedicated client action later (Story XIII) — but learn the TransientModel pattern first; it is what core Odoo uses constantly.

**SECURITY NOTE:**

Wizard apply methods run as the logged-in user. They must not `sudo()` casually to “make the write work.” If the user cannot write the picking, the wizard should fail with `AccessError` — that is correct. Grant rights on the real model, not a silent elevation inside Apply.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add Cancel (`special="cancel"`) and primary Apply buttons. |
| Medium | Bind the wizard only to list view; verify form Action menu does not show it. |
| Hard | Return a `display_notification` client action after apply, then close — so users see “20 pickings updated.” |

**WHAT THIS UNLOCKS NEXT:**

Wizards collect decisions. Business documents still need **human-readable numbers** — SO001, INV/2026/0042 — that stay unique under concurrency. That is sequences.

---


---

## 36.5 active_id

**active_id** belongs to **Wizards & TransientModel**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **active_id** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **active_id** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **active_id**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **active_id** is concrete, continue — the next lesson in this phase still depends on it.

## 36.6 active_ids

**active_ids** belongs to **Wizards & TransientModel**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **active_ids** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **active_ids** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **active_ids**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **active_ids** is concrete, continue — the next lesson in this phase still depends on it.

## 36.7 Context

**Context** belongs to **Wizards & TransientModel**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Context** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Context** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Context**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Context** is concrete, continue — the next lesson in this phase still depends on it.

## 36.8 Multi-Step Wizards

**Multi-Step Wizards** belongs to **Wizards & TransientModel**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Multi-Step Wizards** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Multi-Step Wizards** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Multi-Step Wizards**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Multi-Step Wizards** is concrete, continue — the next lesson in this phase still depends on it.

## 36.9 MINI PROJECT — Bulk Processing Wizard

**MINI PROJECT — Bulk Processing Wizard** belongs to **Wizards & TransientModel**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **MINI PROJECT — Bulk Processing Wizard** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **MINI PROJECT — Bulk Processing Wizard** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **MINI PROJECT — Bulk Processing Wizard** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 36:** You can explain every lesson title in Phase 36 with a concrete Odoo or business example.

> **Phase 36 complete?** [Continue to Phase 37](#phase-37)


<a id="phase-37"></a>
# PHASE 37 - Sequences

**Track:** Advanced Features

**WHAT YOU WILL BE ABLE TO DO:** Configure `ir.sequence` for document names, call `next_by_code` / `next_by_id` safely, and reason about gaps, company-specific sequences, and concurrency.

**WHAT YOU SHOULD KNOW FIRST:** Phase 36 — wizards. Phase 14 — `create` overrides. Phase 31 — multi-company basics (`company_id`).

**LEARNING RESOURCES:**

- [ORM API (Odoo 18)](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [Data files](https://www.odoo.com/documentation/18.0/developer/reference/backend/data.html)
- [odoo/odoo](https://github.com/odoo/odoo) — `addons/base/models/ir_sequence.py`
- Settings → Technical → Sequences (developer mode)

## 37.1 ir.sequence

**ir.sequence** belongs to **Sequences**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **ir.sequence** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ir.sequence** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ir.sequence**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ir.sequence** is concrete, continue — the next lesson in this phase still depends on it.

## 37.2 next_by_code()

**next_by_code()** belongs to **Sequences**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **next_by_code()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **next_by_code()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **next_by_code()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **next_by_code()** is concrete, continue — the next lesson in this phase still depends on it.

## 37.3 Prefix

**Prefix** belongs to **Sequences**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Prefix** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Prefix** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Prefix**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Prefix** is concrete, continue — the next lesson in this phase still depends on it.

## 37.4 Suffix

**Suffix** belongs to **Sequences**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Suffix** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Suffix** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Suffix**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Suffix** is concrete, continue — the next lesson in this phase still depends on it.

## 37.5 Padding

**Padding** belongs to **Sequences**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Padding** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Padding** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Padding**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Padding** is concrete, continue — the next lesson in this phase still depends on it.

## 37.6 Date-Based Sequences

**Date-Based Sequences** belongs to **Sequences**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Date-Based Sequences** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Date-Based Sequences** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Date-Based Sequences**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Date-Based Sequences** is concrete, continue — the next lesson in this phase still depends on it.

## 37.7 Business References

**Business References** belongs to **Sequences**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Business References** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Business References** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Business References**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Business References** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 37:** You can explain every lesson title in Phase 37 with a concrete Odoo or business example.

> **Phase 37 complete?** [Continue to Phase 38](#phase-38)


<a id="phase-38"></a>
# PHASE 38 - Scheduled Actions / Cron

**Track:** Advanced Features

**WHAT YOU WILL BE ABLE TO DO:** Define `ir.cron` jobs that call model methods on a schedule, keep jobs idempotent and short, choose the right user, and avoid “cron that locks the database.”

**WHAT YOU SHOULD KNOW FIRST:** Phase 37 — sequences. Phase 18 — business methods. Phase 30 — `sudo()` discipline. Phase 17 — ORM performance instincts.

**LEARNING RESOURCES:**

- [ORM / automation patterns in core](https://github.com/odoo/odoo) — search `ir.cron` XML under `addons/`
- [Odoo 18 developer reference](https://www.odoo.com/documentation/18.0/developer/reference/backend.html)
- Settings → Technical → Automation → Scheduled Actions (developer mode)

## 38.1 ir.cron

**ir.cron** belongs to **Scheduled Actions / Cron**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **ir.cron** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ir.cron** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ir.cron**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ir.cron** is concrete, continue — the next lesson in this phase still depends on it.

## 38.2 Scheduling

**Scheduling** belongs to **Scheduled Actions / Cron**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Scheduling** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Scheduling** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Scheduling**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Scheduling** is concrete, continue — the next lesson in this phase still depends on it.

## 38.3 Cron Methods

**Cron Methods** belongs to **Scheduled Actions / Cron**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Cron Methods** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Cron Methods** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Cron Methods**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Cron Methods** is concrete, continue — the next lesson in this phase still depends on it.

## 38.4 Batch Processing

**Batch Processing** belongs to **Scheduled Actions / Cron**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Batch Processing** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Batch Processing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Batch Processing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Batch Processing** is concrete, continue — the next lesson in this phase still depends on it.

## 38.5 Progress

**Progress** belongs to **Scheduled Actions / Cron**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Progress** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Progress** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Progress**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Progress** is concrete, continue — the next lesson in this phase still depends on it.

## 38.6 Failure Handling

**Failure Handling** belongs to **Scheduled Actions / Cron**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Failure Handling** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Failure Handling** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Failure Handling**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Failure Handling** is concrete, continue — the next lesson in this phase still depends on it.

## 38.7 Idempotency

**Idempotency** belongs to **Scheduled Actions / Cron**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Idempotency** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Idempotency** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Idempotency**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Idempotency** is concrete, continue — the next lesson in this phase still depends on it.

## 38.8 Cron Security

**Cron Security** belongs to **Scheduled Actions / Cron**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Cron Security** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Cron Security** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Cron Security**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Cron Security** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 38:** You can explain every lesson title in Phase 38 with a concrete Odoo or business example.

> **Phase 38 complete?** [Continue to Phase 39](#phase-39)


<a id="phase-39"></a>
# PHASE 39 - Automation & Server Actions

**Track:** Advanced Features

**WHAT YOU WILL BE ABLE TO DO:** Explain base automation (automated actions): triggers, domains, and server actions; know when configuration is enough and when to move logic into a module method.

**WHAT YOU SHOULD KNOW FIRST:** Phase 38 — cron. Phase 21 — states/workflows. Phase 18 — server-side methods. Phase 28–29 — who may change what.

**LEARNING RESOURCES:**

- Settings → Technical → Automation (developer mode) on an Odoo 18 database
- [odoo/odoo](https://github.com/odoo/odoo) — `base_automation` module
- [Odoo 18 developer docs](https://www.odoo.com/documentation/18.0/developer.html)

## 39.1 Automated Actions

**Automated Actions** belongs to **Automation & Server Actions**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Automated Actions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Automated Actions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Automated Actions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Automated Actions** is concrete, continue — the next lesson in this phase still depends on it.

## 39.2 Triggers

**Triggers** belongs to **Automation & Server Actions**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Triggers** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Triggers** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Triggers**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Triggers** is concrete, continue — the next lesson in this phase still depends on it.

## 39.3 Conditions

**Conditions** belongs to **Automation & Server Actions**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Conditions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Conditions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Conditions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Conditions** is concrete, continue — the next lesson in this phase still depends on it.

## 39.4 Server Actions

**Server Actions** belongs to **Automation & Server Actions**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Server Actions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Server Actions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Server Actions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Server Actions** is concrete, continue — the next lesson in this phase still depends on it.

## 39.5 Evaluation Context

**Evaluation Context** belongs to **Automation & Server Actions**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Evaluation Context** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Evaluation Context** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Evaluation Context**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Evaluation Context** is concrete, continue — the next lesson in this phase still depends on it.

## 39.6 Action Chaining

**Action Chaining** belongs to **Automation & Server Actions**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Action Chaining** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Action Chaining** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Action Chaining**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Action Chaining** is concrete, continue — the next lesson in this phase still depends on it.

## 39.7 When to Use Code Instead

**When to Use Code Instead** belongs to **Automation & Server Actions**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **When to Use Code Instead** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **When to Use Code Instead** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **When to Use Code Instead**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **When to Use Code Instead** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 39:** You can explain every lesson title in Phase 39 with a concrete Odoo or business example.

> **Phase 39 complete?** [Continue to Phase 40](#phase-40)


<a id="phase-40"></a>
# PHASE 40 - Mail, Chatter & Activities

**Track:** Advanced Features

**WHAT YOU WILL BE ABLE TO DO:** Mixin `mail.thread` / `mail.activity.mixin`, post messages, manage followers, schedule activities, and keep portal/internal visibility straight.

**WHAT YOU SHOULD KNOW FIRST:** Phase 39 — automations that create activities. Phase 23 — form views. Phase 28–29 — ACL and record rules. Phase 32 — `_inherit` extension.

**LEARNING RESOURCES:**

- [odoo/odoo `mail` module](https://github.com/odoo/odoo/tree/18.0/addons/mail)
- [OWL / web client mention of messaging](https://www.odoo.com/documentation/18.0/developer/howtos/frontend_owl_components.html) — portal/web context later
- Open any Sales Order form in Odoo 18 and use the chatter deliberately

## 40.1 mail.thread

Business records are not only fields. People argue on them, attach PDFs, @mention colleagues, and need a history that survives employee turnover. Odoo’s answer is the **chatter**: a message stream on the document.

Inherit the abstract models (mixins):

```python
class LibraryLoan(models.Model):
    _name = "library.loan"
    _inherit = ["mail.thread", "mail.activity.mixin"]
    _description = "Loan"

    name = fields.Char(required=True, tracking=True)
    partner_id = fields.Many2one("res.partner", required=True, tracking=True)
    state = fields.Selection(
        [("draft", "Draft"), ("borrowed", "Borrowed"), ("returned", "Returned")],
        default="draft",
        tracking=True,
    )
```

In the form view, add the chatter:

```xml
<form>
    <sheet><!-- fields --></sheet>
    <chatter/>
</form>
```

`tracking=True` on fields posts subtype messages when values change — the audit trail users expect on important documents.

**POSTING FROM CODE:**

```python
def action_borrow(self):
    for loan in self:
        loan.state = "borrowed"
        loan.message_post(
            body="Loan confirmed for %s" % loan.partner_id.display_name,
            message_type="comment",
            subtype_xmlid="mail.mt_comment",
        )
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `mail.thread` + chatter to a custom model and post one message from a button. |
| Medium | Track three fields; change them and read the history. |
| Hard | Explain `_mail_post_access` (who may post) vs model write rights — when a user can read but not comment. |

**WHAT THIS UNLOCKS NEXT:**

Messages need an audience. Followers decide who is notified.

---

## 40.2 mail.activity.mixin

Business records are not only fields. People argue on them, attach PDFs, @mention colleagues, and need a history that survives employee turnover. Odoo’s answer is the **chatter**: a message stream on the document.

Inherit the abstract models (mixins):

```python
class LibraryLoan(models.Model):
    _name = "library.loan"
    _inherit = ["mail.thread", "mail.activity.mixin"]
    _description = "Loan"

    name = fields.Char(required=True, tracking=True)
    partner_id = fields.Many2one("res.partner", required=True, tracking=True)
    state = fields.Selection(
        [("draft", "Draft"), ("borrowed", "Borrowed"), ("returned", "Returned")],
        default="draft",
        tracking=True,
    )
```

In the form view, add the chatter:

```xml
<form>
    <sheet><!-- fields --></sheet>
    <chatter/>
</form>
```

`tracking=True` on fields posts subtype messages when values change — the audit trail users expect on important documents.

**POSTING FROM CODE:**

```python
def action_borrow(self):
    for loan in self:
        loan.state = "borrowed"
        loan.message_post(
            body="Loan confirmed for %s" % loan.partner_id.display_name,
            message_type="comment",
            subtype_xmlid="mail.mt_comment",
        )
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `mail.thread` + chatter to a custom model and post one message from a button. |
| Medium | Track three fields; change them and read the history. |
| Hard | Explain `_mail_post_access` (who may post) vs model write rights — when a user can read but not comment. |

**WHAT THIS UNLOCKS NEXT:**

Messages need an audience. Followers decide who is notified.

---

## 40.3 Messages

**Followers** subscribe partners/users to a document. When a message posts with the right subtype, followers may get inbox/email notifications.

```text
library.loan #42
  followers: Ada (salesperson), Library Bot
        |
        v
message_post(...)
        |
        v
mail.mail / notifications for followers (respecting subtypes & muted)
```

**INTERNAL VS SHARED WITH CUSTOMER:**

Not every note should email the customer. Odoo distinguishes note/comment subtypes and has patterns for **internal** notes vs customer-visible messages. Mis-posting a cost negotiation into a customer-visible thread is a real business incident — treat visibility as a feature, not a default.

**SECURITY:**

Chatter does not replace record rules. If a user cannot `read` the loan, they should not see its messages. Portal users (Story XI) get carefully scoped access — never “sudo and dump the whole thread.”

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add yourself as follower on a sales order; post a note; observe the inbox. |
| Medium | From code, `message_subscribe` a responsible user when state becomes borrowed. |
| Hard | Design when customers become followers vs when only internal users follow. |

**WHAT THIS UNLOCKS NEXT:**

Messages are history. **Activities** are future work — calls, to-dos, deadlines on the record.

---

## 40.4 Followers

**Followers** subscribe partners/users to a document. When a message posts with the right subtype, followers may get inbox/email notifications.

```text
library.loan #42
  followers: Ada (salesperson), Library Bot
        |
        v
message_post(...)
        |
        v
mail.mail / notifications for followers (respecting subtypes & muted)
```

**INTERNAL VS SHARED WITH CUSTOMER:**

Not every note should email the customer. Odoo distinguishes note/comment subtypes and has patterns for **internal** notes vs customer-visible messages. Mis-posting a cost negotiation into a customer-visible thread is a real business incident — treat visibility as a feature, not a default.

**SECURITY:**

Chatter does not replace record rules. If a user cannot `read` the loan, they should not see its messages. Portal users (Story XI) get carefully scoped access — never “sudo and dump the whole thread.”

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add yourself as follower on a sales order; post a note; observe the inbox. |
| Medium | From code, `message_subscribe` a responsible user when state becomes borrowed. |
| Hard | Design when customers become followers vs when only internal users follow. |

**WHAT THIS UNLOCKS NEXT:**

Messages are history. **Activities** are future work — calls, to-dos, deadlines on the record.

---

## 40.5 Tracking

**Tracking** belongs to **Mail, Chatter & Activities**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Tracking** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Tracking** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Tracking**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Tracking** is concrete, continue — the next lesson in this phase still depends on it.

## 40.6 Activities

`mail.activity.mixin` adds activities: “Call customer by Friday,” linked to a user, activity type, and date. Activities appear on the record and in users’ activity views.

```python
def action_borrow(self):
    self.ensure_one()
    self.state = "borrowed"
    self.activity_schedule(
        "mail.mail_activity_data_call",
        user_id=self.env.user.id,
        summary="Remind borrower 2 days before due",
        date_deadline=self.due_date,
    )
```

Automations (Phase 39) often create activities. Cron (Phase 38) often finds overdue activities or overdue documents. Together they form Odoo’s lightweight workflow layer without a separate BPM engine.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Schedule an activity manually from the chatter UI. |
| Medium | Schedule one from Python on state change. |
| Hard | Write a cron that searches overdue activities and posts a digest message to a manager. |

**WHAT THIS UNLOCKS NEXT:**

Collaboration is on-screen. Businesses still need **printable PDFs** — invoices, picks, loan slips. That is QWeb reports.

---


---

## 40.7 Notifications

**Notifications** belongs to **Mail, Chatter & Activities**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Notifications** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Notifications** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Notifications**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Notifications** is concrete, continue — the next lesson in this phase still depends on it.

## 40.8 Email Templates

**Email Templates** belongs to **Mail, Chatter & Activities**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Email Templates** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Email Templates** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Email Templates**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Email Templates** is concrete, continue — the next lesson in this phase still depends on it.

## 40.9 Sending Mail

Business records are not only fields. People argue on them, attach PDFs, @mention colleagues, and need a history that survives employee turnover. Odoo’s answer is the **chatter**: a message stream on the document.

Inherit the abstract models (mixins):

```python
class LibraryLoan(models.Model):
    _name = "library.loan"
    _inherit = ["mail.thread", "mail.activity.mixin"]
    _description = "Loan"

    name = fields.Char(required=True, tracking=True)
    partner_id = fields.Many2one("res.partner", required=True, tracking=True)
    state = fields.Selection(
        [("draft", "Draft"), ("borrowed", "Borrowed"), ("returned", "Returned")],
        default="draft",
        tracking=True,
    )
```

In the form view, add the chatter:

```xml
<form>
    <sheet><!-- fields --></sheet>
    <chatter/>
</form>
```

`tracking=True` on fields posts subtype messages when values change — the audit trail users expect on important documents.

**POSTING FROM CODE:**

```python
def action_borrow(self):
    for loan in self:
        loan.state = "borrowed"
        loan.message_post(
            body="Loan confirmed for %s" % loan.partner_id.display_name,
            message_type="comment",
            subtype_xmlid="mail.mt_comment",
        )
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add `mail.thread` + chatter to a custom model and post one message from a button. |
| Medium | Track three fields; change them and read the history. |
| Hard | Explain `_mail_post_access` (who may post) vs model write rights — when a user can read but not comment. |

**WHAT THIS UNLOCKS NEXT:**

Messages need an audience. Followers decide who is notified.

---


<a id="phase-41"></a>
<a id="phase-41---reports-qweb"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 40:** You can explain every lesson title in Phase 40 with a concrete Odoo or business example.

> **Phase 40 complete?** [Continue to Phase 41](#phase-41)


<a id="phase-41"></a>
# PHASE 41 - Reports & QWeb

**Track:** Advanced Features

**WHAT YOU WILL BE ABLE TO DO:** Author QWeb report templates, bind `ir.actions.report`, generate PDFs for recordsets, and keep report logic thin (data on the model, layout in QWeb).

**WHAT YOU SHOULD KNOW FIRST:** Phase 40 — chatter on documents. Phase 22 — XML data files. Phase 23 — views (QWeb is another XML dialect). Phase 37 — document names on PDFs.

**LEARNING RESOURCES:**

- [QWeb / reports in Odoo docs](https://www.odoo.com/documentation/18.0/developer/reference/backend.html)
- [View architectures](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [odoo/odoo](https://github.com/odoo/odoo) — browse `report_` templates in `sale`, `account`, `stock`
- [Website apps overview](https://www.odoo.com/documentation/18.0/applications/websites.html) — QWeb also powers website (Phase 44)

## 41.1 QWeb

**QWeb** is Odoo’s XML templating engine. For reports, a template renders HTML that wkhtmltopdf (or the configured PDF engine) turns into a PDF. The template receives `docs` (the recordset being printed) and can call model methods.

```xml
<template id="report_library_loan_document">
    <t t-call="web.html_container">
        <t t-foreach="docs" t-as="doc">
            <t t-call="web.external_layout">
                <div class="page">
                    <h2>Loan <span t-field="doc.name"/></h2>
                    <p>Borrower: <span t-field="doc.partner_id"/></p>
                    <p>Due: <span t-field="doc.due_date"/></p>
                </div>
            </t>
        </t>
    </t>
</template>
```

```xml
<record id="action_report_library_loan" model="ir.actions.report">
    <field name="name">Loan Slip</field>
    <field name="model">library.loan</field>
    <field name="report_type">qweb-pdf</field>
    <field name="report_name">my_library.report_library_loan_document</field>
    <field name="report_file">my_library.report_library_loan_document</field>
    <field name="binding_model_id" ref="model_library_loan"/>
    <field name="binding_type">report</field>
</record>
```

**KEEP LOGIC OUT OF THE TEMPLATE:**

Bad: heavy Python-like calculations only in QWeb. Better: computed fields or report helper methods on the model, then `t-field` / `t-esc` in the template. Reports that embed business rules become untestable.

**EXTERNAL LAYOUT:**

Company header/footer come from layout templates. Inherit carefully; do not copy-paste entire invoice reports when you only need one extra line (use `inherit_id` on QWeb templates).

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a PDF report action bound to your custom model. |
| Medium | Add a table of related lines with `t-foreach`. |
| Hard | Inherit a standard report (e.g. sale order) to add one field without rewriting the whole template. |

**WHAT THIS UNLOCKS NEXT:**

Reports print live data. Companies also move data in bulk — CSV/XML imports and module data files. That discipline is Phase 42.

---


---

## 41.2 QWeb Directives

**QWeb** is Odoo’s XML templating engine. For reports, a template renders HTML that wkhtmltopdf (or the configured PDF engine) turns into a PDF. The template receives `docs` (the recordset being printed) and can call model methods.

```xml
<template id="report_library_loan_document">
    <t t-call="web.html_container">
        <t t-foreach="docs" t-as="doc">
            <t t-call="web.external_layout">
                <div class="page">
                    <h2>Loan <span t-field="doc.name"/></h2>
                    <p>Borrower: <span t-field="doc.partner_id"/></p>
                    <p>Due: <span t-field="doc.due_date"/></p>
                </div>
            </t>
        </t>
    </t>
</template>
```

```xml
<record id="action_report_library_loan" model="ir.actions.report">
    <field name="name">Loan Slip</field>
    <field name="model">library.loan</field>
    <field name="report_type">qweb-pdf</field>
    <field name="report_name">my_library.report_library_loan_document</field>
    <field name="report_file">my_library.report_library_loan_document</field>
    <field name="binding_model_id" ref="model_library_loan"/>
    <field name="binding_type">report</field>
</record>
```

**KEEP LOGIC OUT OF THE TEMPLATE:**

Bad: heavy Python-like calculations only in QWeb. Better: computed fields or report helper methods on the model, then `t-field` / `t-esc` in the template. Reports that embed business rules become untestable.

**EXTERNAL LAYOUT:**

Company header/footer come from layout templates. Inherit carefully; do not copy-paste entire invoice reports when you only need one extra line (use `inherit_id` on QWeb templates).

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a PDF report action bound to your custom model. |
| Medium | Add a table of related lines with `t-foreach`. |
| Hard | Inherit a standard report (e.g. sale order) to add one field without rewriting the whole template. |

**WHAT THIS UNLOCKS NEXT:**

Reports print live data. Companies also move data in bulk — CSV/XML imports and module data files. That discipline is Phase 42.

---


---

## 41.3 Report Actions

**Report Actions** belongs to **Reports & QWeb**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Report Actions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Report Actions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Report Actions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Report Actions** is concrete, continue — the next lesson in this phase still depends on it.

## 41.4 Report Templates

**Report Templates** belongs to **Reports & QWeb**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Report Templates** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Report Templates** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Report Templates**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Report Templates** is concrete, continue — the next lesson in this phase still depends on it.

## 41.5 PDF Reports

**PDF Reports** belongs to **Reports & QWeb**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **PDF Reports** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **PDF Reports** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **PDF Reports**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **PDF Reports** is concrete, continue — the next lesson in this phase still depends on it.

## 41.6 HTML Reports

**HTML Reports** belongs to **Reports & QWeb**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **HTML Reports** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **HTML Reports** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **HTML Reports**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **HTML Reports** is concrete, continue — the next lesson in this phase still depends on it.

## 41.7 Paper Formats

**Paper Formats** belongs to **Reports & QWeb**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Paper Formats** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Paper Formats** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Paper Formats**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Paper Formats** is concrete, continue — the next lesson in this phase still depends on it.

## 41.8 Custom Report Data

**Custom Report Data** belongs to **Reports & QWeb**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Custom Report Data** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Custom Report Data** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Custom Report Data**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Custom Report Data** is concrete, continue — the next lesson in this phase still depends on it.

## 41.9 Translatable Reports

**Translatable Reports** belongs to **Reports & QWeb**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Translatable Reports** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Translatable Reports** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Translatable Reports**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Translatable Reports** is concrete, continue — the next lesson in this phase still depends on it.

## 41.10 Barcodes

**Barcodes** belongs to **Reports & QWeb**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Barcodes** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Barcodes** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Barcodes**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Barcodes** is concrete, continue — the next lesson in this phase still depends on it.

## 41.11 Report Attachments

**Report Attachments** belongs to **Reports & QWeb**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Report Attachments** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Report Attachments** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Report Attachments**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Report Attachments** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 41:** You can explain every lesson title in Phase 41 with a concrete Odoo or business example.

> **Phase 41 complete?** [Continue to Phase 42](#phase-42)


<a id="phase-42"></a>
# PHASE 42 - Data Files, Import & Export

**Track:** Advanced Features

**WHAT YOU WILL BE ABLE TO DO:** Use UI import/export responsibly, author module XML/CSV data with external IDs, and apply `noupdate` so upgrades do not wipe production configuration.

**WHAT YOU SHOULD KNOW FIRST:** Phase 41 — reports. Phase 22 — XML foundations / external IDs. Phase 8 — module install vs upgrade. Phase 37 — sequences marked `noupdate`.

**LEARNING RESOURCES:**

- [Data files (Odoo 18)](https://www.odoo.com/documentation/18.0/developer/reference/backend/data.html)
- [External IDs / export in UI](https://www.odoo.com/documentation/18.0/developer.html)
- [odoo/odoo](https://github.com/odoo/odoo) — sample `data/*.xml` in any official addon

## 42.1 XML Data

1. **Operational import** — a user uploads CSV of products into a live database (UI import).
2. **Module data** — XML/CSV shipped in an addon, loaded on `-i` / `-u`, identified by **external IDs** (`module.xml_id`).

```text
UI import                         Module data
---------                         -----------
One database, now                 Every database that installs the module
Easy for ops                      Versioned in Git
Easy to get wrong silently        Needs noupdate strategy
```

**EXTERNAL IDS ARE THE CONTRACT:**

`<record id="product_oak_desk" model="product.product">` creates `my_module.product_oak_desk`. Later upgrades find that row by XML ID instead of inventing duplicates. Never rely on “search by name and hope.”

**NOUPDATE:**

```xml
<data noupdate="1">
    <record id="seq_library_loan" model="ir.sequence">
        <!-- production counter must not reset on -u -->
    </record>
</data>
```

Use `noupdate="1"` for sequences, user-configurable defaults, and anything ops may customize. Leave `noupdate="0"` (default) for views and code-adjacent XML you *do* want refreshed on upgrade — understanding the trade-off is the skill.

**EXPORT FOR MIGRATIONS:**

UI export with external IDs helps move config between DBs. Treat imported security and automation as code review candidates.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Export five partners to CSV; re-import into a test DB. |
| Medium | Ship a demo `data/library_demo.xml` with external IDs for two loans. |
| Hard | Mark a sequence `noupdate="1"`, change prefix in XML, run `-u`, and explain whether the DB changed. |

**WHAT THIS UNLOCKS NEXT:**

Story X’s tools — wizards, sequences, cron, automation, chatter, reports, data files — combine into an **Automated Business Operations** module. Build that before you expose HTTP to the world.

---


---

## 42.2 CSV Data

1. **Operational import** — a user uploads CSV of products into a live database (UI import).
2. **Module data** — XML/CSV shipped in an addon, loaded on `-i` / `-u`, identified by **external IDs** (`module.xml_id`).

```text
UI import                         Module data
---------                         -----------
One database, now                 Every database that installs the module
Easy for ops                      Versioned in Git
Easy to get wrong silently        Needs noupdate strategy
```

**EXTERNAL IDS ARE THE CONTRACT:**

`<record id="product_oak_desk" model="product.product">` creates `my_module.product_oak_desk`. Later upgrades find that row by XML ID instead of inventing duplicates. Never rely on “search by name and hope.”

**NOUPDATE:**

```xml
<data noupdate="1">
    <record id="seq_library_loan" model="ir.sequence">
        <!-- production counter must not reset on -u -->
    </record>
</data>
```

Use `noupdate="1"` for sequences, user-configurable defaults, and anything ops may customize. Leave `noupdate="0"` (default) for views and code-adjacent XML you *do* want refreshed on upgrade — understanding the trade-off is the skill.

**EXPORT FOR MIGRATIONS:**

UI export with external IDs helps move config between DBs. Treat imported security and automation as code review candidates.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Export five partners to CSV; re-import into a test DB. |
| Medium | Ship a demo `data/library_demo.xml` with external IDs for two loans. |
| Hard | Mark a sequence `noupdate="1"`, change prefix in XML, run `-u`, and explain whether the DB changed. |

**WHAT THIS UNLOCKS NEXT:**

Story X’s tools — wizards, sequences, cron, automation, chatter, reports, data files — combine into an **Automated Business Operations** module. Build that before you expose HTTP to the world.

---


---

## 42.3 External IDs

**External IDs** belongs to **Data Files, Import & Export**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **External IDs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **External IDs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **External IDs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **External IDs** is concrete, continue — the next lesson in this phase still depends on it.

## 42.4 env.ref()

**env.ref()** belongs to **Data Files, Import & Export**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **env.ref()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **env.ref()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **env.ref()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **env.ref()** is concrete, continue — the next lesson in this phase still depends on it.

## 42.5 noupdate

**noupdate** belongs to **Data Files, Import & Export**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **noupdate** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **noupdate** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **noupdate**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **noupdate** is concrete, continue — the next lesson in this phase still depends on it.

## 42.6 Demo Data

1. **Operational import** — a user uploads CSV of products into a live database (UI import).
2. **Module data** — XML/CSV shipped in an addon, loaded on `-i` / `-u`, identified by **external IDs** (`module.xml_id`).

```text
UI import                         Module data
---------                         -----------
One database, now                 Every database that installs the module
Easy for ops                      Versioned in Git
Easy to get wrong silently        Needs noupdate strategy
```

**EXTERNAL IDS ARE THE CONTRACT:**

`<record id="product_oak_desk" model="product.product">` creates `my_module.product_oak_desk`. Later upgrades find that row by XML ID instead of inventing duplicates. Never rely on “search by name and hope.”

**NOUPDATE:**

```xml
<data noupdate="1">
    <record id="seq_library_loan" model="ir.sequence">
        <!-- production counter must not reset on -u -->
    </record>
</data>
```

Use `noupdate="1"` for sequences, user-configurable defaults, and anything ops may customize. Leave `noupdate="0"` (default) for views and code-adjacent XML you *do* want refreshed on upgrade — understanding the trade-off is the skill.

**EXPORT FOR MIGRATIONS:**

UI export with external IDs helps move config between DBs. Treat imported security and automation as code review candidates.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Export five partners to CSV; re-import into a test DB. |
| Medium | Ship a demo `data/library_demo.xml` with external IDs for two loans. |
| Hard | Mark a sequence `noupdate="1"`, change prefix in XML, run `-u`, and explain whether the DB changed. |

**WHAT THIS UNLOCKS NEXT:**

Story X’s tools — wizards, sequences, cron, automation, chatter, reports, data files — combine into an **Automated Business Operations** module. Build that before you expose HTTP to the world.

---


---

## 42.7 Seed Data

1. **Operational import** — a user uploads CSV of products into a live database (UI import).
2. **Module data** — XML/CSV shipped in an addon, loaded on `-i` / `-u`, identified by **external IDs** (`module.xml_id`).

```text
UI import                         Module data
---------                         -----------
One database, now                 Every database that installs the module
Easy for ops                      Versioned in Git
Easy to get wrong silently        Needs noupdate strategy
```

**EXTERNAL IDS ARE THE CONTRACT:**

`<record id="product_oak_desk" model="product.product">` creates `my_module.product_oak_desk`. Later upgrades find that row by XML ID instead of inventing duplicates. Never rely on “search by name and hope.”

**NOUPDATE:**

```xml
<data noupdate="1">
    <record id="seq_library_loan" model="ir.sequence">
        <!-- production counter must not reset on -u -->
    </record>
</data>
```

Use `noupdate="1"` for sequences, user-configurable defaults, and anything ops may customize. Leave `noupdate="0"` (default) for views and code-adjacent XML you *do* want refreshed on upgrade — understanding the trade-off is the skill.

**EXPORT FOR MIGRATIONS:**

UI export with external IDs helps move config between DBs. Treat imported security and automation as code review candidates.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Export five partners to CSV; re-import into a test DB. |
| Medium | Ship a demo `data/library_demo.xml` with external IDs for two loans. |
| Hard | Mark a sequence `noupdate="1"`, change prefix in XML, run `-u`, and explain whether the DB changed. |

**WHAT THIS UNLOCKS NEXT:**

Story X’s tools — wizards, sequences, cron, automation, chatter, reports, data files — combine into an **Automated Business Operations** module. Build that before you expose HTTP to the world.

---


---

## 42.8 Import

**Import** belongs to **Data Files, Import & Export**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Import** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Import** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Import**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Import** is concrete, continue — the next lesson in this phase still depends on it.

## 42.9 Export

**Export** belongs to **Data Files, Import & Export**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Export** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Export** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Export**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Export** is concrete, continue — the next lesson in this phase still depends on it.

## 42.10 Data Load Order

1. **Operational import** — a user uploads CSV of products into a live database (UI import).
2. **Module data** — XML/CSV shipped in an addon, loaded on `-i` / `-u`, identified by **external IDs** (`module.xml_id`).

```text
UI import                         Module data
---------                         -----------
One database, now                 Every database that installs the module
Easy for ops                      Versioned in Git
Easy to get wrong silently        Needs noupdate strategy
```

**EXTERNAL IDS ARE THE CONTRACT:**

`<record id="product_oak_desk" model="product.product">` creates `my_module.product_oak_desk`. Later upgrades find that row by XML ID instead of inventing duplicates. Never rely on “search by name and hope.”

**NOUPDATE:**

```xml
<data noupdate="1">
    <record id="seq_library_loan" model="ir.sequence">
        <!-- production counter must not reset on -u -->
    </record>
</data>
```

Use `noupdate="1"` for sequences, user-configurable defaults, and anything ops may customize. Leave `noupdate="0"` (default) for views and code-adjacent XML you *do* want refreshed on upgrade — understanding the trade-off is the skill.

**EXPORT FOR MIGRATIONS:**

UI export with external IDs helps move config between DBs. Treat imported security and automation as code review candidates.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Export five partners to CSV; re-import into a test DB. |
| Medium | Ship a demo `data/library_demo.xml` with external IDs for two loans. |
| Hard | Mark a sequence `noupdate="1"`, change prefix in XML, run `-u`, and explain whether the DB changed. |

**WHAT THIS UNLOCKS NEXT:**

Story X’s tools — wizards, sequences, cron, automation, chatter, reports, data files — combine into an **Automated Business Operations** module. Build that before you expose HTTP to the world.

---


---

## 42.11 XML Data Operations

1. **Operational import** — a user uploads CSV of products into a live database (UI import).
2. **Module data** — XML/CSV shipped in an addon, loaded on `-i` / `-u`, identified by **external IDs** (`module.xml_id`).

```text
UI import                         Module data
---------                         -----------
One database, now                 Every database that installs the module
Easy for ops                      Versioned in Git
Easy to get wrong silently        Needs noupdate strategy
```

**EXTERNAL IDS ARE THE CONTRACT:**

`<record id="product_oak_desk" model="product.product">` creates `my_module.product_oak_desk`. Later upgrades find that row by XML ID instead of inventing duplicates. Never rely on “search by name and hope.”

**NOUPDATE:**

```xml
<data noupdate="1">
    <record id="seq_library_loan" model="ir.sequence">
        <!-- production counter must not reset on -u -->
    </record>
</data>
```

Use `noupdate="1"` for sequences, user-configurable defaults, and anything ops may customize. Leave `noupdate="0"` (default) for views and code-adjacent XML you *do* want refreshed on upgrade — understanding the trade-off is the skill.

**EXPORT FOR MIGRATIONS:**

UI export with external IDs helps move config between DBs. Treat imported security and automation as code review candidates.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Export five partners to CSV; re-import into a test DB. |
| Medium | Ship a demo `data/library_demo.xml` with external IDs for two loans. |
| Hard | Mark a sequence `noupdate="1"`, change prefix in XML, run `-u`, and explain whether the DB changed. |

**WHAT THIS UNLOCKS NEXT:**

Story X’s tools — wizards, sequences, cron, automation, chatter, reports, data files — combine into an **Automated Business Operations** module. Build that before you expose HTTP to the world.

---


---


---


**CHECK YOUR UNDERSTANDING AFTER PHASE 42:** You can explain every lesson title in Phase 42 with a concrete Odoo or business example.

> **Phase 42 complete?** [Continue to Phase 43](#phase-43)


## STORY PROJECT — Automated Business Operations Module

**Track:** Advanced Features

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XI — INTERNATIONALIZATION & LOCALIZATION

**Track:** i18n & Localization

*Real ERP software serves people in different languages, countries and accounting environments.*

---
<a id="phase-43"></a>
<a id="phase-43---internationalization-localization"></a>
# PHASE 43 - Internationalization & Localization

**Track:** i18n & Localization

Real ERP software serves people in different languages, countries and accounting environments.

**WHAT YOU WILL BE ABLE TO DO:** Work confidently with every topic under **Internationalization & Localization** — not as a glossary, but as mechanisms you can implement and debug.

**WHAT YOU SHOULD KNOW FIRST:** Earlier stories through the phase before this one. Keep one demo company in mind.

**LEARNING RESOURCES:**

- [Odoo 18 Developer docs](https://www.odoo.com/documentation/18.0/developer.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 43.1 Translatable Strings

Odoo stores many user-facing strings as **translatable**: field labels, selection labels, help text, report titles, and `_()`-wrapped Python messages. Without translation, every non-English user sees English chrome — or worse, mixed languages after a partial import.

A translatable string is not “any string in the database.” It is a string registered in Odoo’s translation tables (`ir.translation` historically; newer versions use the translation API on fields and code exports). When a user switches language, Odoo looks up the translated value for that `lang` and falls back to the source language when missing.

```text
source string (en_US)
      |
      +--→ export .pot
      |
      +--→ translate .po
      |
      +--→ import → language-specific UI
```

**PRACTICE:** List five strings in your custom module that must be translated (labels, errors, report title, menu, button).

**WHAT THIS UNLOCKS NEXT:** You need the `_()` function so Python runtime messages join that same system.


## 43.2 _() Translation Function

In Python code, wrap user-visible messages with `_()` from `odoo`:

```python
from odoo import _, models
from odoo.exceptions import UserError

class SaleOrder(models.Model):
    _inherit = "sale.order"

    def action_custom_block(self):
        raise UserError(_("This order cannot be confirmed yet."))
```

`_()` marks the string for export into `.pot` / `.po`. Do **not** translate by concatenating fragments (`_("Hello ") + name`) — translators need a full sentence. Use placeholders: `_("Hello %s", name)` (Odoo’s lazy gettext style).

Model field `string=` and `help=` are usually auto-exported; code messages are not unless `_()` wraps them.

**PRACTICE:** Convert three hardcoded `UserError("...")` strings in a sample module to `_()`.

**WHAT THIS UNLOCKS NEXT:** Some **fields** themselves are multilingual (product names, website pages).


## 43.3 Translatable Fields

Fields can be marked `translate=True` so each language stores its own value (product name, website snippet, helpdesk description). That is different from translating the *label* of the field.

```python
name = fields.Char(required=True, translate=True)
```

Writes happen in the current user’s language context unless you `with_context(lang=...)`. Reports and website pages must pick the right `lang` or customers see the wrong catalog name.

**PRACTICE:** Add a `translate=True` Char on a demo model; enter EN and AR values; switch languages and verify.

**WHAT THIS UNLOCKS NEXT:** Translations live as files under `i18n/`.


## 43.4 i18n/

Every installable module may ship an `i18n/` directory. Odoo loads translations from there on install/upgrade when languages are installed. Keep `.po` files in version control so translators and developers share one source of truth.

```text
my_module/
  i18n/
    my_module.pot   # template (optional but useful)
    ar.po
    fr.po
```

**PRACTICE:** Create `i18n/` in your Hello Odoo module even if empty — confirm the path is conventional.

**WHAT THIS UNLOCKS NEXT:** `.po` and `.pot` file roles.


## 43.5 .po Files

A `.po` file maps `msgid` (source) to `msgstr` (translation) for one language. Headers record language code and plural rules. Fuzzy entries are incomplete — do not ship them as final.

Edit with Poedit or a text editor; keep encoding UTF-8. Never hand-edit compiled `.mo` if your workflow uses them — prefer Odoo’s import path.

**PRACTICE:** Open any official addon’s `i18n/fr.po` and find one `msgid` you recognize from the UI.

**WHAT THIS UNLOCKS NEXT:** `.pot` templates as the export baseline.


## 43.6 .pot Templates

A `.pot` file is the **template**: all source `msgid`s with empty `msgstr`. Translators copy it to `xx.po`. Regenerating `.pot` after code changes reveals new strings and obsolete ones.

**PRACTICE:** Document your team rule: who regenerates `.pot`, and how often (per release vs per PR).

**WHAT THIS UNLOCKS NEXT:** Exporting translations from a running database.


## 43.7 Exporting Translations

In developer mode, Odoo can export translations for a module/language. Prefer exporting after UI strings stabilize. Commit the resulting `.po` updates so staging/production installs stay consistent.

**PRACTICE:** Export translations for one custom module into `i18n/` and inspect the new `msgid`s.

**WHAT THIS UNLOCKS NEXT:** Importing those files back cleanly.


## 43.8 Importing Translations

Import loads `.po` content into the database for the selected language. After import, upgrade the module or restart as your environment requires, then verify menus and errors in that language.

Conflict rule: database overrides vs file loads — know your deployment path (Odoo.sh vs source) so you do not lose translator work.

**PRACTICE:** Import a small `xx.po` with three strings; confirm they appear after language switch.

**WHAT THIS UNLOCKS NEXT:** Installing **Languages** and user language preferences.


## 43.9 Languages

Settings → Translations → Languages installs language packs. Users pick a language on their preference. Company and partner languages matter for reports and portal.

**PRACTICE:** Install a second language on a local DB and switch your admin user.

**WHAT THIS UNLOCKS NEXT:** Locale formatting (dates, numbers, currencies).


## 43.10 Locale / Formatting

Locale is not translation. It controls date/number/currency formatting. A French user may want `fr_FR` formatting even when reading English labels during training — or the reverse. Monetary widgets and reports must respect company currency **and** language/locale context.

**PRACTICE:** Compare the same invoice PDF with two languages; note which parts translate vs which format.

**WHAT THIS UNLOCKS NEXT:** QWeb/report translation.


## 43.11 Translatable QWeb

QWeb templates use `t-translation` / language-aware rendering for reports and website. Wrap visible text properly; avoid splitting sentences across nodes. Test PDF output per language — layout often breaks when German or Arabic strings grow.

**PRACTICE:** Translate one report title and one column header; regenerate PDF in two languages.

**WHAT THIS UNLOCKS NEXT:** Localization **modules** (l10n_*) beyond UI strings.


## 43.12 Localization Modules

`l10n_*` modules package country-specific charts of accounts, taxes, and fiscal positions. They are not “just translations.” Installing the wrong localization corrupts accounting assumptions.

**PRACTICE:** Read the README of one `l10n_` module relevant to your region; list what it installs.

**WHAT THIS UNLOCKS NEXT:** Accounting localization as a concept for developers who touch `account.move`.


## 43.13 Accounting Localization Concept

Accounting localization defines legal/fiscal defaults. Custom modules that hard-code tax IDs or account codes become upgrade hazards. Prefer configurable accounts, xmlids from `l10n_*`, or company-dependent settings.

**PRACTICE:** Write a one-pager: three localization facts your custom invoicing module must not hard-code.

**WHAT THIS UNLOCKS NEXT:** Practice translating a full custom module end-to-end.


## 43.14 PRACTICE — Translate a Custom Module

**PRACTICE — Translate a Custom Module**

1. Pick your library/todo addon.
2. Wrap all UserError/ValidationError messages with `_()`.
3. Mark at least one Char `translate=True`.
4. Export `.pot`, create `ar.po` or `fr.po` with ten real translations.
5. Import, switch language, screenshot menus + one error + one form field.

**Acceptance:** a second language user can complete the happy path without English leftovers on your screens.

**WHAT THIS UNLOCKS NEXT:** Story XII — HTTP, website, and portal (public users also need language-aware pages).


**CHECK YOUR UNDERSTANDING AFTER PHASE 43:** You can explain every lesson title in Phase 43 with a concrete Odoo or business example.

> **Phase 43 complete?** [Continue to Phase 44](#phase-44)


## STORY PROJECT — Multilingual Business Module

**Track:** i18n & Localization

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XII — WEB DEVELOPMENT WITH ODOO

**Track:** Web & Portal

*Controllers, website, and portal put Odoo on the public and customer web.*

---
<a id="phase-44"></a>
<a id="phase-44---http-controllers"></a>
# PHASE 44 - HTTP Controllers

**Track:** Web & Portal

**WHAT YOU WILL BE ABLE TO DO:** Define `http.Controller` routes with `@http.route`, use `request`, choose `auth` modes (`user`, `public`, `bearer`, `none`), return HTTP/JSON responses, and treat CSRF as mandatory for browser form posts.

**WHAT YOU SHOULD KNOW FIRST:** Story X project complete (or equivalent comfort with models/security). Phase 28–30 — ACL, rules, sudo discipline. Networks intuition: HTTP verbs, cookies, sessions.

**LEARNING RESOURCES:**

- [Web Controllers — Odoo 18](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html)
- [Web Services howto](https://www.odoo.com/documentation/18.0/developer/howtos/web_services.html)
- [odoo/odoo](https://github.com/odoo/odoo) — `odoo/http.py` and controllers under `addons/website`, `addons/portal`

## 44.1 http.Controller

The web client talks JSON-RPC to the ORM for normal screens. Sometimes you need a **custom URL**: `/library/status`, a webhook receiver, a public brochure page, a JSON API for a mobile app.

```python
from odoo import http
from odoo.http import request


class LibraryController(http.Controller):

    @http.route("/library/status", type="http", auth="public", website=True, methods=["GET"])
    def status(self, **kwargs):
        count = request.env["library.loan"].sudo().search_count([("state", "=", "borrowed")])
        return request.render("my_library.portal_status_template", {"borrowed": count})
```

**ANATOMY OF `@http.route`:**

| Argument | Meaning |
| -------- | ------- |
| path | URL pattern(s) |
| `type` | `'http'` (forms/pages) or `'json'` (JSON-RPC style body) |
| `auth` | Who runs the code — see below |
| `methods` | Limit verbs (`GET`, `POST`, …) |
| `csrf` | CSRF check for HTTP routes (default **True** for `http`) |
| `cors` | CORS header if browsers call you cross-origin |
| `readonly` | Prefer read-only DB cursor when safe |

**`request`** is the thread-local wrapper: `request.env`, `request.session`, `request.httprequest`, helpers like `make_json_response`, `render`, CSRF helpers.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a GET route that returns plain text “ok” with `auth="user"`. |
| Medium | Return JSON with `make_json_response` listing fields the user is allowed to see (no sudo). |
| Hard | Override another module’s controller correctly by re-decorating `@http.route()` as the docs require. |

**WHAT THIS UNLOCKS NEXT:**

Routes without a clear **auth** story are security holes. Next: the auth modes and CSRF in depth.

---

## 44.2 @http.route

The web client talks JSON-RPC to the ORM for normal screens. Sometimes you need a **custom URL**: `/library/status`, a webhook receiver, a public brochure page, a JSON API for a mobile app.

```python
from odoo import http
from odoo.http import request


class LibraryController(http.Controller):

    @http.route("/library/status", type="http", auth="public", website=True, methods=["GET"])
    def status(self, **kwargs):
        count = request.env["library.loan"].sudo().search_count([("state", "=", "borrowed")])
        return request.render("my_library.portal_status_template", {"borrowed": count})
```

**ANATOMY OF `@http.route`:**

| Argument | Meaning |
| -------- | ------- |
| path | URL pattern(s) |
| `type` | `'http'` (forms/pages) or `'json'` (JSON-RPC style body) |
| `auth` | Who runs the code — see below |
| `methods` | Limit verbs (`GET`, `POST`, …) |
| `csrf` | CSRF check for HTTP routes (default **True** for `http`) |
| `cors` | CORS header if browsers call you cross-origin |
| `readonly` | Prefer read-only DB cursor when safe |

**`request`** is the thread-local wrapper: `request.env`, `request.session`, `request.httprequest`, helpers like `make_json_response`, `render`, CSRF helpers.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a GET route that returns plain text “ok” with `auth="user"`. |
| Medium | Return JSON with `make_json_response` listing fields the user is allowed to see (no sudo). |
| Hard | Override another module’s controller correctly by re-decorating `@http.route()` as the docs require. |

**WHAT THIS UNLOCKS NEXT:**

Routes without a clear **auth** story are security holes. Next: the auth modes and CSRF in depth.

---

## 44.3 request

**request** belongs to **HTTP Controllers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **request** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **request** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **request**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **request** is concrete, continue — the next lesson in this phase still depends on it.

## 44.4 Route Types

**Route Types** belongs to **HTTP Controllers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Route Types** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Route Types** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Route Types**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Route Types** is concrete, continue — the next lesson in this phase still depends on it.

## 44.5 HTTP Methods

The web client talks JSON-RPC to the ORM for normal screens. Sometimes you need a **custom URL**: `/library/status`, a webhook receiver, a public brochure page, a JSON API for a mobile app.

```python
from odoo import http
from odoo.http import request


class LibraryController(http.Controller):

    @http.route("/library/status", type="http", auth="public", website=True, methods=["GET"])
    def status(self, **kwargs):
        count = request.env["library.loan"].sudo().search_count([("state", "=", "borrowed")])
        return request.render("my_library.portal_status_template", {"borrowed": count})
```

**ANATOMY OF `@http.route`:**

| Argument | Meaning |
| -------- | ------- |
| path | URL pattern(s) |
| `type` | `'http'` (forms/pages) or `'json'` (JSON-RPC style body) |
| `auth` | Who runs the code — see below |
| `methods` | Limit verbs (`GET`, `POST`, …) |
| `csrf` | CSRF check for HTTP routes (default **True** for `http`) |
| `cors` | CORS header if browsers call you cross-origin |
| `readonly` | Prefer read-only DB cursor when safe |

**`request`** is the thread-local wrapper: `request.env`, `request.session`, `request.httprequest`, helpers like `make_json_response`, `render`, CSRF helpers.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Add a GET route that returns plain text “ok” with `auth="user"`. |
| Medium | Return JSON with `make_json_response` listing fields the user is allowed to see (no sudo). |
| Hard | Override another module’s controller correctly by re-decorating `@http.route()` as the docs require. |

**WHAT THIS UNLOCKS NEXT:**

Routes without a clear **auth** story are security holes. Next: the auth modes and CSRF in depth.

---

## 44.6 Parameters

**Parameters** belongs to **HTTP Controllers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Parameters** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Parameters** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Parameters**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Parameters** is concrete, continue — the next lesson in this phase still depends on it.

## 44.7 Responses

**Responses** belongs to **HTTP Controllers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Responses** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Responses** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Responses**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Responses** is concrete, continue — the next lesson in this phase still depends on it.

## 44.8 Authentication

**AUTH MODES (ODOO 18):**

| `auth` | Behavior |
| ------ | -------- |
| `user` | Must be logged in; `request.env` is that user |
| `public` | May be anonymous; runs as the shared **Public** user if guest |
| `bearer` | API token via `Authorization: Bearer …`, else session user |
| `none` | No DB user facilities — framework/login style; rare in business modules |

```text
Browser form POST without CSRF token
        |
        v
HttpDispatcher checks csrf (default on for type=http)
        |
        X  rejected — forged cross-site post blocked
```

**CSRF — WHY IT EXISTS:**

If a user is logged into Odoo and visits an attacker’s page, the attacker could auto-POST to `/your/route` using the victim’s cookies. CSRF tokens bind the form to the session (`request.csrf_token()`). **Do not set `csrf=False` unless** you have another strong authenticity check (shared secret signature, verified webhook HMAC, etc.) and you understand the risk.

JSON routes default CSRF off because they are not classic HTML form posts — that does **not** mean “open to the world.” Still require `auth` and authorization.

**PUBLIC + SUDO IS A LOADED GUN:**

`auth="public"` plus `sudo()` is how partner data leaks. Prefer:

- `auth="user"` for private APIs
- Portal security groups for customers (Phase 45)
- Explicit record rules
- Narrow sudo only after validating an unguessable token

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Render a QWeb form that includes a CSRF token field. |
| Medium | Attempt POST without token on a csrf-protected route; observe failure. |
| Hard | Design a webhook route with `csrf=False` **and** HMAC verification (preview of Phase 49). |

**WHAT THIS UNLOCKS NEXT:**

Controllers can render pages. The **website** app adds page management, menus, and public-facing QWeb layouts.

---


---

## 44.9 CSRF

**AUTH MODES (ODOO 18):**

| `auth` | Behavior |
| ------ | -------- |
| `user` | Must be logged in; `request.env` is that user |
| `public` | May be anonymous; runs as the shared **Public** user if guest |
| `bearer` | API token via `Authorization: Bearer …`, else session user |
| `none` | No DB user facilities — framework/login style; rare in business modules |

```text
Browser form POST without CSRF token
        |
        v
HttpDispatcher checks csrf (default on for type=http)
        |
        X  rejected — forged cross-site post blocked
```

**CSRF — WHY IT EXISTS:**

If a user is logged into Odoo and visits an attacker’s page, the attacker could auto-POST to `/your/route` using the victim’s cookies. CSRF tokens bind the form to the session (`request.csrf_token()`). **Do not set `csrf=False` unless** you have another strong authenticity check (shared secret signature, verified webhook HMAC, etc.) and you understand the risk.

JSON routes default CSRF off because they are not classic HTML form posts — that does **not** mean “open to the world.” Still require `auth` and authorization.

**PUBLIC + SUDO IS A LOADED GUN:**

`auth="public"` plus `sudo()` is how partner data leaks. Prefer:

- `auth="user"` for private APIs
- Portal security groups for customers (Phase 45)
- Explicit record rules
- Narrow sudo only after validating an unguessable token

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Render a QWeb form that includes a CSRF token field. |
| Medium | Attempt POST without token on a csrf-protected route; observe failure. |
| Hard | Design a webhook route with `csrf=False` **and** HMAC verification (preview of Phase 49). |

**WHAT THIS UNLOCKS NEXT:**

Controllers can render pages. The **website** app adds page management, menus, and public-facing QWeb layouts.

---


---

## 44.10 Sessions

**Sessions** on HTTP routes is security, not ceremony. `@http.route` `auth` values (`user`, `public`, `none`) change who you are. CSRF tokens protect cookie-authenticated POST. Sessions bind the browser to `env.user`.

Public JSON endpoints need their own auth story (tokens, signatures). Copying `auth='public'` from a website page onto a data API is a leak.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Sessions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Sessions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Sessions** is concrete, continue — the next lesson in this phase still depends on it.

## 44.11 Error Handling

**Error Handling** belongs to **HTTP Controllers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Error Handling** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Error Handling** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Error Handling**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Error Handling** is concrete, continue — the next lesson in this phase still depends on it.

## 44.12 Controller Security

**Controller Security** belongs to **HTTP Controllers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Controller Security** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Controller Security** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Controller Security**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Controller Security** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 44:** You can explain every lesson title in Phase 44 with a concrete Odoo or business example.

> **Phase 44 complete?** [Continue to Phase 45](#phase-45)


<a id="phase-45"></a>
# PHASE 45 - Website Development

**Track:** Web & Portal

**WHAT YOU WILL BE ABLE TO DO:** Build website pages with QWeb, serve dynamic content through website controllers, and keep public templates free of private fields.

**WHAT YOU SHOULD KNOW FIRST:** Phase 43 — controllers, auth, CSRF. Phase 41 — QWeb basics. Phase 7 — module structure (`views/`, templates).

**LEARNING RESOURCES:**

- [Odoo Websites applications](https://www.odoo.com/documentation/18.0/applications/websites.html)
- [HTTP controllers](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html)
- [odoo/odoo `website` module](https://github.com/odoo/odoo/tree/18.0/addons/website)

## 45.1 Website Architecture

**Website Architecture** belongs to **Website Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Website Architecture** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Website Architecture** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Website Architecture**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Website Architecture** is concrete, continue — the next lesson in this phase still depends on it.

## 45.2 Website Pages

The website is not a separate CMS database. It is Odoo modules rendering QWeb for visitors, with `website=True` on routes and website-aware menus. Editors can drag blocks in Website Builder; developers add structured dynamic pages when the builder is not enough.

```python
@http.route("/books", type="http", auth="public", website=True)
def book_catalog(self, **kwargs):
    books = request.env["product.template"].search([("sale_ok", "=", True)], limit=50)
    return request.render("my_library.book_catalog_page", {"books": books})
```

```xml
<template id="book_catalog_page" name="Book catalog">
    <t t-call="website.layout">
        <div class="container">
            <h1>Books</h1>
            <t t-foreach="books" t-as="book">
                <div t-field="book.name"/>
            </t>
        </div>
    </t>
</template>
```

**DYNAMIC CONTENT RULES:**

- Assume the visitor is hostile or curious.
- Never expose cost, internal notes, or other companies’ data on `auth="public"`.
- Prefer dedicated published fields (`is_published`, website description) over dumping internal form fields.

**WEBSITE VS BACKEND VIEWS:**

Backend form/list XML is for employees. Website templates wrap `website.layout` and must look acceptable on mobile. Do not reuse backend view architecture as a public page.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Create a `/hello` website route rendering a name. |
| Medium | List published products only; hide unpublished. |
| Hard | Add a POST form with CSRF that creates a `crm.lead` as public (check field allow-list). |

**WHAT THIS UNLOCKS NEXT:**

Anonymous pages are marketing. Logged-in **customers** need to see *their* orders and invoices — portal, not the backend.

---


---

## 45.3 QWeb Templates

**QWeb Templates** belongs to **Website Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **QWeb Templates** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **QWeb Templates** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **QWeb Templates**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **QWeb Templates** is concrete, continue — the next lesson in this phase still depends on it.

## 45.4 Dynamic Content

**Dynamic Content** belongs to **Website Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Dynamic Content** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Dynamic Content** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Dynamic Content**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Dynamic Content** is concrete, continue — the next lesson in this phase still depends on it.

## 45.5 Website Controllers

**Website Controllers** belongs to **Website Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Website Controllers** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Website Controllers** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Website Controllers**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Website Controllers** is concrete, continue — the next lesson in this phase still depends on it.

## 45.6 Frontend Assets

**Frontend Assets** belongs to **Website Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Frontend Assets** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Frontend Assets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Frontend Assets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Frontend Assets** is concrete, continue — the next lesson in this phase still depends on it.

## 45.7 Forms

**Forms** belongs to **Website Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Forms** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Forms** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Forms**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Forms** is concrete, continue — the next lesson in this phase still depends on it.

## 45.8 SEO Basics

**SEO Basics** belongs to **Website Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **SEO Basics** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **SEO Basics** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **SEO Basics**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **SEO Basics** is concrete, continue — the next lesson in this phase still depends on it.

## 45.9 Multi-Website Concept

**Multi-Website Concept** belongs to **Website Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Multi-Website Concept** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Multi-Website Concept** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Multi-Website Concept**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Multi-Website Concept** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 45:** You can explain every lesson title in Phase 45 with a concrete Odoo or business example.

> **Phase 45 complete?** [Continue to Phase 46](#phase-46)


<a id="phase-46"></a>
# PHASE 46 - Portal Development

**Track:** Web & Portal

**WHAT YOU WILL BE ABLE TO DO:** Build portal pages for portal users, enforce portal security (share controllers + record rules), and expose customer records without granting internal groups.

**WHAT YOU SHOULD KNOW FIRST:** Phase 44 — website pages. Phase 29 — record rules. Phase 27 — users/groups (`base.group_portal`). Phase 43 — auth modes.

**LEARNING RESOURCES:**

- [HTTP controllers](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html)
- [Security in Odoo](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [odoo/odoo `portal` module](https://github.com/odoo/odoo/tree/18.0/addons/portal)
- [Frontend OWL howto](https://www.odoo.com/documentation/18.0/developer/howtos/frontend_owl_components.html) — portal UX may use web components later

## 46.1 Portal Users

A **portal user** logs in with limited rights: typically `base.group_portal`, not `base.group_user`. They use `/my` style pages, not the backend web client menus. They should see **their** quotations, invoices, tickets — never the whole company.

```text
Internal user          Portal user
--------------         -----------
Backend apps           /my portal home
group_user             group_portal
Wide record rules      Partner-scoped rules
```

```python
from odoo.addons.portal.controllers.portal import CustomerPortal
from odoo.exceptions import AccessError, MissingError


class LibraryPortal(CustomerPortal):

    @http.route(["/my/loans", "/my/loans/page/<int:page>"], type="http", auth="user", website=True)
    def portal_my_loans(self, page=1, **kwargs):
        partner = request.env.user.partner_id
        loans = request.env["library.loan"].search([("partner_id", "=", partner.id)])
        return request.render("my_library.portal_my_loans", {"loans": loans})
```

**PORTAL SECURITY — NON-NEGOTIABLES:**

1. **Record rules** on `library.loan` for portal groups: `[('partner_id','=', user.partner_id.id)]` (adjust to your model).
2. Controllers must not `sudo()` and then filter “for convenience” while forgetting an edge case — prefer letting the ORM enforce rules.
3. When using sudo for a composed page, **re-check** ownership (`if loan.partner_id != request.env.user.partner_id: raise AccessError`).
4. Never trust hidden form fields like `loan_id` without verifying access (`_document_check_access` patterns in `CustomerPortal`).
5. ACL: portal usually needs **read** (and limited write) on specific models — not admin.

```python
def _loan_get(self, loan_id):
    loan = request.env["library.loan"].browse(loan_id).exists()
    if not loan or loan.partner_id != request.env.user.partner_id:
        raise AccessError("This loan is not available.")
    return loan
```

**CUSTOMER RECORDS:**

Portal pages show customer-facing fields. Internal chatter notes must not leak. Use message subtypes / portal templates that render only shared messages.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Enable portal on a test partner; log in and open `/my`. |
| Medium | Add `/my/loans` listing only that partner’s loans via record rules. |
| Hard | Add a detail route `/my/loans/<id>` that rejects another partner’s id even if guessed. |

**WHAT THIS UNLOCKS NEXT:**

Story XI ends with a **Customer Self-Service Portal** project. After that, machines — not browsers — need access: Story XII APIs and integrations.

---


---

## 46.2 Portal Controllers

A **portal user** logs in with limited rights: typically `base.group_portal`, not `base.group_user`. They use `/my` style pages, not the backend web client menus. They should see **their** quotations, invoices, tickets — never the whole company.

```text
Internal user          Portal user
--------------         -----------
Backend apps           /my portal home
group_user             group_portal
Wide record rules      Partner-scoped rules
```

```python
from odoo.addons.portal.controllers.portal import CustomerPortal
from odoo.exceptions import AccessError, MissingError


class LibraryPortal(CustomerPortal):

    @http.route(["/my/loans", "/my/loans/page/<int:page>"], type="http", auth="user", website=True)
    def portal_my_loans(self, page=1, **kwargs):
        partner = request.env.user.partner_id
        loans = request.env["library.loan"].search([("partner_id", "=", partner.id)])
        return request.render("my_library.portal_my_loans", {"loans": loans})
```

**PORTAL SECURITY — NON-NEGOTIABLES:**

1. **Record rules** on `library.loan` for portal groups: `[('partner_id','=', user.partner_id.id)]` (adjust to your model).
2. Controllers must not `sudo()` and then filter “for convenience” while forgetting an edge case — prefer letting the ORM enforce rules.
3. When using sudo for a composed page, **re-check** ownership (`if loan.partner_id != request.env.user.partner_id: raise AccessError`).
4. Never trust hidden form fields like `loan_id` without verifying access (`_document_check_access` patterns in `CustomerPortal`).
5. ACL: portal usually needs **read** (and limited write) on specific models — not admin.

```python
def _loan_get(self, loan_id):
    loan = request.env["library.loan"].browse(loan_id).exists()
    if not loan or loan.partner_id != request.env.user.partner_id:
        raise AccessError("This loan is not available.")
    return loan
```

**CUSTOMER RECORDS:**

Portal pages show customer-facing fields. Internal chatter notes must not leak. Use message subtypes / portal templates that render only shared messages.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Enable portal on a test partner; log in and open `/my`. |
| Medium | Add `/my/loans` listing only that partner’s loans via record rules. |
| Hard | Add a detail route `/my/loans/<id>` that rejects another partner’s id even if guessed. |

**WHAT THIS UNLOCKS NEXT:**

Story XI ends with a **Customer Self-Service Portal** project. After that, machines — not browsers — need access: Story XII APIs and integrations.

---


---

## 46.3 Customer Pages

**Customer Pages** belongs to **Portal Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Customer Pages** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Customer Pages** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Customer Pages**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Customer Pages** is concrete, continue — the next lesson in this phase still depends on it.

## 46.4 Portal Layout

A **portal user** logs in with limited rights: typically `base.group_portal`, not `base.group_user`. They use `/my` style pages, not the backend web client menus. They should see **their** quotations, invoices, tickets — never the whole company.

```text
Internal user          Portal user
--------------         -----------
Backend apps           /my portal home
group_user             group_portal
Wide record rules      Partner-scoped rules
```

```python
from odoo.addons.portal.controllers.portal import CustomerPortal
from odoo.exceptions import AccessError, MissingError


class LibraryPortal(CustomerPortal):

    @http.route(["/my/loans", "/my/loans/page/<int:page>"], type="http", auth="user", website=True)
    def portal_my_loans(self, page=1, **kwargs):
        partner = request.env.user.partner_id
        loans = request.env["library.loan"].search([("partner_id", "=", partner.id)])
        return request.render("my_library.portal_my_loans", {"loans": loans})
```

**PORTAL SECURITY — NON-NEGOTIABLES:**

1. **Record rules** on `library.loan` for portal groups: `[('partner_id','=', user.partner_id.id)]` (adjust to your model).
2. Controllers must not `sudo()` and then filter “for convenience” while forgetting an edge case — prefer letting the ORM enforce rules.
3. When using sudo for a composed page, **re-check** ownership (`if loan.partner_id != request.env.user.partner_id: raise AccessError`).
4. Never trust hidden form fields like `loan_id` without verifying access (`_document_check_access` patterns in `CustomerPortal`).
5. ACL: portal usually needs **read** (and limited write) on specific models — not admin.

```python
def _loan_get(self, loan_id):
    loan = request.env["library.loan"].browse(loan_id).exists()
    if not loan or loan.partner_id != request.env.user.partner_id:
        raise AccessError("This loan is not available.")
    return loan
```

**CUSTOMER RECORDS:**

Portal pages show customer-facing fields. Internal chatter notes must not leak. Use message subtypes / portal templates that render only shared messages.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Enable portal on a test partner; log in and open `/my`. |
| Medium | Add `/my/loans` listing only that partner’s loans via record rules. |
| Hard | Add a detail route `/my/loans/<id>` that rejects another partner’s id even if guessed. |

**WHAT THIS UNLOCKS NEXT:**

Story XI ends with a **Customer Self-Service Portal** project. After that, machines — not browsers — need access: Story XII APIs and integrations.

---


---

## 46.5 Portal Security

A **portal user** logs in with limited rights: typically `base.group_portal`, not `base.group_user`. They use `/my` style pages, not the backend web client menus. They should see **their** quotations, invoices, tickets — never the whole company.

```text
Internal user          Portal user
--------------         -----------
Backend apps           /my portal home
group_user             group_portal
Wide record rules      Partner-scoped rules
```

```python
from odoo.addons.portal.controllers.portal import CustomerPortal
from odoo.exceptions import AccessError, MissingError


class LibraryPortal(CustomerPortal):

    @http.route(["/my/loans", "/my/loans/page/<int:page>"], type="http", auth="user", website=True)
    def portal_my_loans(self, page=1, **kwargs):
        partner = request.env.user.partner_id
        loans = request.env["library.loan"].search([("partner_id", "=", partner.id)])
        return request.render("my_library.portal_my_loans", {"loans": loans})
```

**PORTAL SECURITY — NON-NEGOTIABLES:**

1. **Record rules** on `library.loan` for portal groups: `[('partner_id','=', user.partner_id.id)]` (adjust to your model).
2. Controllers must not `sudo()` and then filter “for convenience” while forgetting an edge case — prefer letting the ORM enforce rules.
3. When using sudo for a composed page, **re-check** ownership (`if loan.partner_id != request.env.user.partner_id: raise AccessError`).
4. Never trust hidden form fields like `loan_id` without verifying access (`_document_check_access` patterns in `CustomerPortal`).
5. ACL: portal usually needs **read** (and limited write) on specific models — not admin.

```python
def _loan_get(self, loan_id):
    loan = request.env["library.loan"].browse(loan_id).exists()
    if not loan or loan.partner_id != request.env.user.partner_id:
        raise AccessError("This loan is not available.")
    return loan
```

**CUSTOMER RECORDS:**

Portal pages show customer-facing fields. Internal chatter notes must not leak. Use message subtypes / portal templates that render only shared messages.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Enable portal on a test partner; log in and open `/my`. |
| Medium | Add `/my/loans` listing only that partner’s loans via record rules. |
| Hard | Add a detail route `/my/loans/<id>` that rejects another partner’s id even if guessed. |

**WHAT THIS UNLOCKS NEXT:**

Story XI ends with a **Customer Self-Service Portal** project. After that, machines — not browsers — need access: Story XII APIs and integrations.

---


---

## 46.6 Customer-Owned Records

**Customer-Owned Records** belongs to **Portal Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Customer-Owned Records** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Customer-Owned Records** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Customer-Owned Records**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Customer-Owned Records** is concrete, continue — the next lesson in this phase still depends on it.

## 46.7 Pagination

**Pagination** belongs to **Portal Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Pagination** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Pagination** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Pagination**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Pagination** is concrete, continue — the next lesson in this phase still depends on it.

## 46.8 Search / Filters

**Search / Filters** belongs to **Portal Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Search / Filters** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Search / Filters** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Search / Filters**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Search / Filters** is concrete, continue — the next lesson in this phase still depends on it.

## 46.9 Access Tokens

**Access Tokens** belongs to **Portal Development**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Access Tokens** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Access Tokens** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Access Tokens**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Access Tokens** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 46:** You can explain every lesson title in Phase 46 with a concrete Odoo or business example.

> **Phase 46 complete?** [Continue to Phase 47](#phase-47)


## STORY PROJECT — Customer Self-Service Portal

**Track:** Web & Portal

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XIII — APIS & INTEGRATIONS

**Track:** APIs & Integrations

*Other systems will call you — and you will call them. Contracts first.*

---
<a id="phase-47"></a>
<a id="phase-47---external-api-concepts"></a>
# PHASE 47 - External API Concepts

**Track:** APIs & Integrations

**WHAT YOU WILL BE ABLE TO DO:** Describe integration contracts (identity, auth, resources, errors, idempotency, versioning) before writing code, and choose RPC vs custom REST vs webhook shapes deliberately.

**WHAT YOU SHOULD KNOW FIRST:** Story XI — HTTP auth/CSRF/portal boundaries. Phase 28–30 — security context. Phase 38 — idempotent jobs.

**LEARNING RESOURCES:**

- [Web Services — Odoo 18](https://www.odoo.com/documentation/18.0/developer/howtos/web_services.html)
- [HTTP controllers](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html)
- [Security in Odoo](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [External API tutorial family on odoo.com](https://www.odoo.com/documentation/18.0/developer.html)

## 47.1 Client / Server

**Client / Server** belongs to **External API Concepts**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Client / Server** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Client / Server** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Client / Server**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Client / Server** is concrete, continue — the next lesson in this phase still depends on it.

## 47.2 Resources

**Resources** belongs to **External API Concepts**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Resources** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Resources** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Resources**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Resources** is concrete, continue — the next lesson in this phase still depends on it.

## 47.3 HTTP Methods

You can already build portals and controllers. The next temptation is to let a mobile app or middleware “talk to the database.” That phrase is dangerous. An integration is a **product surface**: the same business operations humans do in the UI, exposed to machines, with failures, retries, and attackers included.

**WHAT BREAKS WHEN YOU SKIP A CONTRACT:**

```text
Mobile app  --POST /create_order-->  Odoo
   timeout (user retries)
        |
        v
Two orders, two invoices, one angry customer
```

Without identity, authority, idempotency, and error contracts, every network glitch becomes a business incident.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Name three systems that might call Odoo in a real company. |
| Medium | For “create sales order from ecommerce,” list what must never duplicate. |
| Hard | Explain why SQL from an external ETL tool bypasses Odoo security. |

**WHAT THIS UNLOCKS NEXT:**

You feel the risk. Next we write the **contract checklist** that every integration design must answer before code.

---

## 47.4 JSON

**JSON** belongs to **External API Concepts**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **JSON** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **JSON** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **JSON**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **JSON** is concrete, continue — the next lesson in this phase still depends on it.

## 47.5 Authentication

**Authentication** belongs to **External API Concepts**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Authentication** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Authentication** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Authentication**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Authentication** is concrete, continue — the next lesson in this phase still depends on it.

## 47.6 Status Codes

**Status Codes** belongs to **External API Concepts**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Status Codes** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Status Codes** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Status Codes**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Status Codes** is concrete, continue — the next lesson in this phase still depends on it.

## 47.7 Pagination

**Pagination** belongs to **External API Concepts**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Pagination** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Pagination** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Pagination**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Pagination** is concrete, continue — the next lesson in this phase still depends on it.

## 47.8 Rate Limits

**Rate Limits** belongs to **External API Concepts**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Rate Limits** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Rate Limits** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Rate Limits**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Rate Limits** is concrete, continue — the next lesson in this phase still depends on it.

## 47.9 API Contracts

**API Contracts** belongs to **External API Concepts**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **API Contracts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **API Contracts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **API Contracts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **API Contracts** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 47:** You can explain every lesson title in Phase 47 with a concrete Odoo or business example.

> **Phase 47 complete?** [Continue to Phase 48](#phase-48)


<a id="phase-48"></a>
# PHASE 48 - External Odoo Access

**Track:** APIs & Integrations

**WHAT YOU WILL BE ABLE TO DO:** Authenticate via XML-RPC/JSON-RPC, call `execute_kw` / `execute`, use a least-privilege integration user (and API keys where available), and avoid embedding admin passwords in apps.

**WHAT YOU SHOULD KNOW FIRST:** Phase 46 — contracts. Phase 28–29 — groups and rules. [Web Services howto](https://www.odoo.com/documentation/18.0/developer/howtos/web_services.html).

**LEARNING RESOURCES:**

- [Web Services — Odoo 18](https://www.odoo.com/documentation/18.0/developer/howtos/web_services.html)
- [odoo/odoo](https://github.com/odoo/odoo) — external API examples in documentation sources
- Prefer official docs over random “Odoo REST” blog posts

## 48.1 External API Architecture

**External API Architecture** belongs to **External Odoo Access**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **External API Architecture** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **External API Architecture** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **External API Architecture**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **External API Architecture** is concrete, continue — the next lesson in this phase still depends on it.

## 48.2 Integration Users

**Integration Users** belongs to **External Odoo Access**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Integration Users** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Integration Users** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Integration Users**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Integration Users** is concrete, continue — the next lesson in this phase still depends on it.

## 48.3 Authentication

**Authentication** belongs to **External Odoo Access**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Authentication** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Authentication** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Authentication**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Authentication** is concrete, continue — the next lesson in this phase still depends on it.

## 48.4 Calling Models

**Calling Models** belongs to **External Odoo Access**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Calling Models** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Calling Models** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Calling Models**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Calling Models** is concrete, continue — the next lesson in this phase still depends on it.

## 48.5 CRUD Remotely

**CRUD Remotely** belongs to **External Odoo Access**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **CRUD Remotely** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **CRUD Remotely** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **CRUD Remotely**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **CRUD Remotely** is concrete, continue — the next lesson in this phase still depends on it.

## 48.6 Domains

**Domains** belongs to **External Odoo Access**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Domains** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Domains** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Domains**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Domains** is concrete, continue — the next lesson in this phase still depends on it.

## 48.7 Permissions

**Permissions** belongs to **External Odoo Access**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Permissions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Permissions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Permissions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Permissions** is concrete, continue — the next lesson in this phase still depends on it.

## 48.8 Version Compatibility

**Version Compatibility** belongs to **External Odoo Access**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Version Compatibility** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Version Compatibility** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Version Compatibility**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Version Compatibility** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 48:** You can explain every lesson title in Phase 48 with a concrete Odoo or business example.

> **Phase 48 complete?** [Continue to Phase 49](#phase-49)


<a id="phase-49"></a>
# PHASE 49 - Building API Endpoints

**Track:** APIs & Integrations

**WHAT YOU WILL BE ABLE TO DO:** Design versioned JSON HTTP APIs with `@http.route`, authenticate callers, validate payloads, and map errors to stable HTTP statuses.

**WHAT YOU SHOULD KNOW FIRST:** Phase 47 — RPC. Phase 43 — controllers, auth, CSRF. Phase 46 — contract checklist.

**LEARNING RESOURCES:**

- [HTTP controllers — Odoo 18](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html) — note `auth='bearer'`
- [Web Services](https://www.odoo.com/documentation/18.0/developer/howtos/web_services.html)

## 49.1 API Routes

**API Routes** belongs to **Building API Endpoints**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **API Routes** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **API Routes** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **API Routes**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **API Routes** is concrete, continue — the next lesson in this phase still depends on it.

## 49.2 Request Parsing

**Request Parsing** belongs to **Building API Endpoints**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Request Parsing** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Request Parsing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Request Parsing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Request Parsing** is concrete, continue — the next lesson in this phase still depends on it.

## 49.3 Validation

**Validation** belongs to **Building API Endpoints**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Validation** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Validation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Validation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Validation** is concrete, continue — the next lesson in this phase still depends on it.

## 49.4 Serialization

**Serialization** belongs to **Building API Endpoints**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Serialization** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Serialization** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Serialization**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Serialization** is concrete, continue — the next lesson in this phase still depends on it.

## 49.5 Response Design

**Response Design** belongs to **Building API Endpoints**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Response Design** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Response Design** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Response Design**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Response Design** is concrete, continue — the next lesson in this phase still depends on it.

## 49.6 Authentication

**Authentication** on HTTP routes is security, not ceremony. `@http.route` `auth` values (`user`, `public`, `none`) change who you are. CSRF tokens protect cookie-authenticated POST. Sessions bind the browser to `env.user`.

Public JSON endpoints need their own auth story (tokens, signatures). Copying `auth='public'` from a website page onto a data API is a leak.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Authentication** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Authentication**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Authentication** is concrete, continue — the next lesson in this phase still depends on it.

## 49.7 Authorization

**Authorization** belongs to **Building API Endpoints**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Authorization** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Authorization** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Authorization**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Authorization** is concrete, continue — the next lesson in this phase still depends on it.

## 49.8 Pagination

**Pagination** belongs to **Building API Endpoints**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Pagination** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Pagination** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Pagination**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Pagination** is concrete, continue — the next lesson in this phase still depends on it.

## 49.9 Error Responses

**Error Responses** belongs to **Building API Endpoints**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Error Responses** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Error Responses** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Error Responses**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Error Responses** is concrete, continue — the next lesson in this phase still depends on it.

## 49.10 API Versioning Concepts

**API Versioning Concepts** belongs to **Building API Endpoints**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **API Versioning Concepts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **API Versioning Concepts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **API Versioning Concepts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **API Versioning Concepts** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 49:** You can explain every lesson title in Phase 49 with a concrete Odoo or business example.

> **Phase 49 complete?** [Continue to Phase 50](#phase-50)


<a id="phase-50"></a>
# PHASE 50 - Webhooks

**Track:** APIs & Integrations

**WHAT YOU WILL BE ABLE TO DO:** Receive incoming webhooks, verify signatures, process events **idempotently**, and design outgoing event notifications without losing or duplicating critical side effects.

**WHAT YOU SHOULD KNOW FIRST:** Phase 48 — custom endpoints. Phase 38 — idempotent cron. Phase 43 — CSRF exceptions only with compensating controls.

**LEARNING RESOURCES:**

- [HTTP controllers](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html)
- Provider docs for signature headers (Stripe, GitHub, etc.) — prefer vendor official docs
- [odoo/odoo payment modules](https://github.com/odoo/odoo/tree/18.0/addons) — webhook handlers as reference patterns

## 50.1 Incoming Webhooks

A webhook is an HTTP POST from a third party: “payment succeeded,” “shipment scanned.” Attackers can POST too. Authenticity comes from **signatures** (HMAC of the body with a shared secret) or mTLS — not from hiding the URL.

```text
Provider POST /webhooks/carrier
  Header: X-Signature: hex(hmac(secret, body))
        |
        v
Recompute HMAC; compare with hmac.compare_digest
        |
        +--> mismatch: 401, stop
        +--> match: parse event id; idempotency store
                |
                +--> already processed: 200 OK (no redo)
                +--> new: apply business change; store event id; 200
```

```python
import hashlib
import hmac
import json

from odoo import http
from odoo.http import request


class CarrierWebhook(http.Controller):

    @http.route("/webhooks/carrier", type="http", auth="public", methods=["POST"], csrf=False)
    def carrier_hook(self, **kwargs):
        secret = request.env["ir.config_parameter"].sudo().get_param("carrier.webhook_secret") or ""
        body = request.httprequest.data or b""
        sig = request.httprequest.headers.get("X-Signature", "")
        expected = hmac.new(secret.encode(), body, hashlib.sha256).hexdigest()
        if not secret or not hmac.compare_digest(expected, sig):
            return request.make_json_response({"error": "invalid signature"}, status=401)

        try:
            payload = json.loads(body.decode() or "{}")
        except json.JSONDecodeError:
            return request.make_json_response({"error": "invalid json"}, status=400)

        event_id = payload.get("id")
        Event = request.env["my.webhook.event"].sudo()
        if event_id and Event.search_count([("event_id", "=", event_id)]):
            # Already applied — acknowledge so the provider stops retrying.
            return request.make_json_response({"status": "duplicate"}, status=200)

        # Apply business logic ONCE (write stock, update picking, …) then record the event id.
        # ... domain-specific work here ...
        if event_id:
            Event.create({"event_id": event_id, "name": payload.get("type", "event")})
        return request.make_json_response({"status": "ok"}, status=200)
```

**IDEMPOTENCY IS MANDATORY:**

Providers retry. Your handler will run twice. Use a unique event id table, or an idempotency key, or a natural key (“payment intent X already done”). Return 200 for duplicates after the first success so providers stop retrying — without re-applying side effects.

**OUTGOING EVENTS:**

When Odoo is the source of truth, notify others from `create`/`write` carefully (queues/cron better than slow HTTP in the request). Sign your payloads; document retry policy; expect consumers to be idempotent too.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Write a pseudo-handler checklist: verify → dedupe → mutate → persist event id → 200. |
| Medium | Implement HMAC verify + duplicate table for a demo payload. |
| Hard | Simulate retries in a test: second delivery must not double-write stock. |

**WHAT THIS UNLOCKS NEXT:**

Webhooks are one sync style. Broader **SaaS integrations** add polling, mapping, retries, and error handling (Phase 50).

---


---

## 50.2 Outgoing Events

**Outgoing Events** belongs to **Webhooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Outgoing Events** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Outgoing Events** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Outgoing Events**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Outgoing Events** is concrete, continue — the next lesson in this phase still depends on it.

## 50.3 Payload Validation

**Payload Validation** belongs to **Webhooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Payload Validation** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Payload Validation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Payload Validation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Payload Validation** is concrete, continue — the next lesson in this phase still depends on it.

## 50.4 Signatures

**Signatures** belongs to **Webhooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Signatures** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Signatures** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Signatures**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Signatures** is concrete, continue — the next lesson in this phase still depends on it.

## 50.5 Secrets

**Secrets** belongs to **Webhooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Secrets** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Secrets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Secrets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Secrets** is concrete, continue — the next lesson in this phase still depends on it.

## 50.6 Idempotency

**Idempotency** belongs to **Webhooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Idempotency** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Idempotency** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Idempotency**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Idempotency** is concrete, continue — the next lesson in this phase still depends on it.

## 50.7 Retries

**Retries** belongs to **Webhooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Retries** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Retries** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Retries**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Retries** is concrete, continue — the next lesson in this phase still depends on it.

## 50.8 Duplicate Events

**Duplicate Events** belongs to **Webhooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Duplicate Events** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Duplicate Events** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Duplicate Events**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Duplicate Events** is concrete, continue — the next lesson in this phase still depends on it.

## 50.9 Audit Logging

**Audit Logging** belongs to **Webhooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Audit Logging** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Audit Logging** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Audit Logging**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Audit Logging** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 50:** You can explain every lesson title in Phase 50 with a concrete Odoo or business example.

> **Phase 50 complete?** [Continue to Phase 51](#phase-51)


<a id="phase-51"></a>
# PHASE 51 - Third-Party Integrations

**Track:** APIs & Integrations

**WHAT YOU WILL BE ABLE TO DO:** Structure REST integrations with authentication, field mapping, sync jobs, retries/backoff, and visible error handling — without scattering HTTP calls across every model method.

**WHAT YOU SHOULD KNOW FIRST:** Phase 49 — webhooks/idempotency. Phase 38 — cron batches. Phase 46 — contracts.

**LEARNING RESOURCES:**

- [Web Services](https://www.odoo.com/documentation/18.0/developer/howtos/web_services.html)
- [HTTP](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- Official REST docs of the vendor you integrate (not SEO blog spam)
- [odoo/odoo](https://github.com/odoo/odoo) — study how payment_* and connector-style addons isolate HTTP

## 51.1 REST APIs

A webhook (Phase 49) is a push. Most SaaS syncs are also **pull or push-on-schedule**: Odoo calls a REST API with a token, maps fields, stores an external id, and retries when the network lies. The first design mistake is putting `requests.get(...)` inside every `create`/`write`. That couples your business model to one vendor’s downtime.

**KEEP HTTP BEHIND A SERVICE BOUNDARY:**

```text
library.partner / res.partner  (business facts)
        |
        v
my_module.integration.client   (auth, rate limit, HTTP verbs)
        |
        v
Vendor REST API
```

Authentication patterns you will actually meet:

| Pattern | Typical use | Store secrets in |
| ------- | ----------- | ---------------- |
| API key / bearer token | Server-to-server SaaS | `ir.config_parameter` or vault — never git |
| OAuth2 client credentials | Many modern SaaS | Token + refresh; rotate |
| Basic auth | Legacy APIs | Same as API key — least privilege account |
| Mutual TLS | High-security B2B | Ops-managed certs |

```python
import requests
from odoo import models, api
from odoo.exceptions import UserError


class VendorClient(models.AbstractModel):
    _name = "vendor.api.client"
    _description = "HTTP client for Vendor SaaS"

    def _base_url(self):
        return self.env["ir.config_parameter"].sudo().get_param("vendor.api_url")

    def _headers(self):
        token = self.env["ir.config_parameter"].sudo().get_param("vendor.api_token")
        if not token:
            raise UserError("Vendor API token is not configured.")
        return {"Authorization": f"Bearer {token}", "Accept": "application/json"}

    def request(self, method, path, **kwargs):
        url = f"{self._base_url().rstrip('/')}/{path.lstrip('/')}"
        timeout = kwargs.pop("timeout", 30)
        response = requests.request(method, url, headers=self._headers(), timeout=timeout, **kwargs)
        return response
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List three secrets that must never appear in a repo or screenshot. |
| Medium | Sketch `_headers()` for API key vs OAuth2 client-credentials (refresh step included). |
| Hard | Argue why calling the vendor from `res.partner.create` is worse than a dedicated client + cron. |

**WHAT THIS UNLOCKS NEXT:**

Auth gets you in the door. Synchronization needs **mapping**, **external ids**, and a strategy for “who owns the truth.”

---

## 51.2 API Authentication

A webhook (Phase 49) is a push. Most SaaS syncs are also **pull or push-on-schedule**: Odoo calls a REST API with a token, maps fields, stores an external id, and retries when the network lies. The first design mistake is putting `requests.get(...)` inside every `create`/`write`. That couples your business model to one vendor’s downtime.

**KEEP HTTP BEHIND A SERVICE BOUNDARY:**

```text
library.partner / res.partner  (business facts)
        |
        v
my_module.integration.client   (auth, rate limit, HTTP verbs)
        |
        v
Vendor REST API
```

Authentication patterns you will actually meet:

| Pattern | Typical use | Store secrets in |
| ------- | ----------- | ---------------- |
| API key / bearer token | Server-to-server SaaS | `ir.config_parameter` or vault — never git |
| OAuth2 client credentials | Many modern SaaS | Token + refresh; rotate |
| Basic auth | Legacy APIs | Same as API key — least privilege account |
| Mutual TLS | High-security B2B | Ops-managed certs |

```python
import requests
from odoo import models, api
from odoo.exceptions import UserError


class VendorClient(models.AbstractModel):
    _name = "vendor.api.client"
    _description = "HTTP client for Vendor SaaS"

    def _base_url(self):
        return self.env["ir.config_parameter"].sudo().get_param("vendor.api_url")

    def _headers(self):
        token = self.env["ir.config_parameter"].sudo().get_param("vendor.api_token")
        if not token:
            raise UserError("Vendor API token is not configured.")
        return {"Authorization": f"Bearer {token}", "Accept": "application/json"}

    def request(self, method, path, **kwargs):
        url = f"{self._base_url().rstrip('/')}/{path.lstrip('/')}"
        timeout = kwargs.pop("timeout", 30)
        response = requests.request(method, url, headers=self._headers(), timeout=timeout, **kwargs)
        return response
```

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List three secrets that must never appear in a repo or screenshot. |
| Medium | Sketch `_headers()` for API key vs OAuth2 client-credentials (refresh step included). |
| Hard | Argue why calling the vendor from `res.partner.create` is worse than a dedicated client + cron. |

**WHAT THIS UNLOCKS NEXT:**

Auth gets you in the door. Synchronization needs **mapping**, **external ids**, and a strategy for “who owns the truth.”

---

## 51.3 Mapping External Data

Two systems almost never share the same field names. Odoo’s `email` might be Vendor’s `primaryEmail`. Odoo’s `vat` might be optional while Vendor requires `taxId`. Mapping is a product decision: which system is master for each field?

**EXTERNAL IDS ARE THE JOIN KEY:**

```python
class ResPartner(models.Model):
    _inherit = "res.partner"

    vendor_id = fields.Char(index=True, copy=False)
    vendor_sync_state = fields.Selection(
        [
            ("idle", "Idle"),
            ("pending", "Pending"),
            ("error", "Error"),
        ],
        default="idle",
    )
    vendor_last_error = fields.Text()
    vendor_need_sync = fields.Boolean(default=False)
```

```text
Push (Odoo → Vendor):
  pending partners --> map fields --> POST/PUT --> store vendor_id

Pull (Vendor → Odoo):
  list remote changes --> match vendor_id --> write allow-listed fields

Conflict:
  last-write-wins is crude; field-level ownership is clearer
  (Odoo owns phone; Vendor owns marketing opt-in)
```

Prefer **upsert by external id** (`PUT /contacts/{vendor_id}`) over blind `POST` create. Blind creates duplicate contacts every retry.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Draw a table: Odoo field → Vendor field → who is master. |
| Medium | Add `vendor_id` + sync state fields to a training model. |
| Hard | Specify conflict rules for email changes on both sides in the same hour. |

**WHAT THIS UNLOCKS NEXT:**

Mapping assumes the HTTP call succeeds. Networks fail. Phase 50.3 is retries and error handling that do not amplify outages.

---

## 51.4 Data Ownership

**Data Ownership** belongs to **Third-Party Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Data Ownership** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Data Ownership** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Data Ownership**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Data Ownership** is concrete, continue — the next lesson in this phase still depends on it.

## 51.5 Synchronization

Two systems almost never share the same field names. Odoo’s `email` might be Vendor’s `primaryEmail`. Odoo’s `vat` might be optional while Vendor requires `taxId`. Mapping is a product decision: which system is master for each field?

**EXTERNAL IDS ARE THE JOIN KEY:**

```python
class ResPartner(models.Model):
    _inherit = "res.partner"

    vendor_id = fields.Char(index=True, copy=False)
    vendor_sync_state = fields.Selection(
        [
            ("idle", "Idle"),
            ("pending", "Pending"),
            ("error", "Error"),
        ],
        default="idle",
    )
    vendor_last_error = fields.Text()
    vendor_need_sync = fields.Boolean(default=False)
```

```text
Push (Odoo → Vendor):
  pending partners --> map fields --> POST/PUT --> store vendor_id

Pull (Vendor → Odoo):
  list remote changes --> match vendor_id --> write allow-listed fields

Conflict:
  last-write-wins is crude; field-level ownership is clearer
  (Odoo owns phone; Vendor owns marketing opt-in)
```

Prefer **upsert by external id** (`PUT /contacts/{vendor_id}`) over blind `POST` create. Blind creates duplicate contacts every retry.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Draw a table: Odoo field → Vendor field → who is master. |
| Medium | Add `vendor_id` + sync state fields to a training model. |
| Hard | Specify conflict rules for email changes on both sides in the same hour. |

**WHAT THIS UNLOCKS NEXT:**

Mapping assumes the HTTP call succeeds. Networks fail. Phase 50.3 is retries and error handling that do not amplify outages.

---

## 51.6 Incremental Sync

**Incremental Sync** belongs to **Third-Party Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Incremental Sync** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Incremental Sync** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Incremental Sync**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Incremental Sync** is concrete, continue — the next lesson in this phase still depends on it.

## 51.7 Scheduled Sync

**Scheduled Sync** belongs to **Third-Party Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Scheduled Sync** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Scheduled Sync** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Scheduled Sync**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Scheduled Sync** is concrete, continue — the next lesson in this phase still depends on it.

## 51.8 Retries / Backoff

```text
Attempt 1  -->  timeout
wait 2s
Attempt 2  -->  502
wait 4s
Attempt 3  -->  200 OK
```

| HTTP outcome | Retry? | Why |
| ------------ | ------ | --- |
| 408 / 429 / 5xx / timeout | Yes (bounded) | Transient |
| 401 / 403 | No (alert ops) | Auth/config broken |
| 400 / 422 | No (fix payload) | Your mapping is wrong |
| 404 on PUT by id | Maybe create-once | Resource deleted remotely |

```python
import time
import logging
_logger = logging.getLogger(__name__)


def request_with_retry(client, method, path, attempts=3, **kwargs):
    delay = 2
    last = None
    for attempt in range(1, attempts + 1):
        response = client.request(method, path, **kwargs)
        last = response
        if response.status_code in (408, 429, 500, 502, 503, 504):
            _logger.warning("vendor %s %s attempt %s -> %s", method, path, attempt, response.status_code)
            if attempt < attempts:
                time.sleep(delay)
                delay *= 2
                continue
        return response
    return last
```

**CRON BATCHES BEAT REQUEST-PATH HTTP:**

```python
def cron_push_partners(self):
    partners = self.search([("vendor_need_sync", "=", True)], limit=50)
    Client = self.env["vendor.api.client"]
    for partner in partners:
        try:
            # map + upsert; set vendor_id; clear need_sync
            partner.vendor_sync_state = "idle"
            partner.vendor_last_error = False
            partner.vendor_need_sync = False
        except Exception as exc:
            partner.vendor_sync_state = "error"
            partner.vendor_last_error = str(exc)[:2000]
            partner.message_post(body=f"Vendor sync failed: {exc}")
```

Surface failures on the record (status field + chatter). Log correlation ids, never tokens or personal data dumps. Make re-sync idempotent so ops can click “Retry” safely.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Sketch models/fields for syncing contacts to a fictional CRM API. |
| Medium | Write a cron that pushes only `need_sync=True` records with `limit=50`. |
| Hard | Design retry: which HTTP codes retry, which poison-pill to a dead-letter status. |

**WHAT THIS UNLOCKS NEXT:**

Contact sync mistakes are embarrassing. Payment mistakes are legal and financial. **Payment integrations** close Story XII with stricter rules.

---


---

## 51.9 Rate Limiting

**Rate Limiting** belongs to **Third-Party Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Rate Limiting** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Rate Limiting** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Rate Limiting**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Rate Limiting** is concrete, continue — the next lesson in this phase still depends on it.

## 51.10 Error Handling

```text
Attempt 1  -->  timeout
wait 2s
Attempt 2  -->  502
wait 4s
Attempt 3  -->  200 OK
```

| HTTP outcome | Retry? | Why |
| ------------ | ------ | --- |
| 408 / 429 / 5xx / timeout | Yes (bounded) | Transient |
| 401 / 403 | No (alert ops) | Auth/config broken |
| 400 / 422 | No (fix payload) | Your mapping is wrong |
| 404 on PUT by id | Maybe create-once | Resource deleted remotely |

```python
import time
import logging
_logger = logging.getLogger(__name__)


def request_with_retry(client, method, path, attempts=3, **kwargs):
    delay = 2
    last = None
    for attempt in range(1, attempts + 1):
        response = client.request(method, path, **kwargs)
        last = response
        if response.status_code in (408, 429, 500, 502, 503, 504):
            _logger.warning("vendor %s %s attempt %s -> %s", method, path, attempt, response.status_code)
            if attempt < attempts:
                time.sleep(delay)
                delay *= 2
                continue
        return response
    return last
```

**CRON BATCHES BEAT REQUEST-PATH HTTP:**

```python
def cron_push_partners(self):
    partners = self.search([("vendor_need_sync", "=", True)], limit=50)
    Client = self.env["vendor.api.client"]
    for partner in partners:
        try:
            # map + upsert; set vendor_id; clear need_sync
            partner.vendor_sync_state = "idle"
            partner.vendor_last_error = False
            partner.vendor_need_sync = False
        except Exception as exc:
            partner.vendor_sync_state = "error"
            partner.vendor_last_error = str(exc)[:2000]
            partner.message_post(body=f"Vendor sync failed: {exc}")
```

Surface failures on the record (status field + chatter). Log correlation ids, never tokens or personal data dumps. Make re-sync idempotent so ops can click “Retry” safely.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Sketch models/fields for syncing contacts to a fictional CRM API. |
| Medium | Write a cron that pushes only `need_sync=True` records with `limit=50`. |
| Hard | Design retry: which HTTP codes retry, which poison-pill to a dead-letter status. |

**WHAT THIS UNLOCKS NEXT:**

Contact sync mistakes are embarrassing. Payment mistakes are legal and financial. **Payment integrations** close Story XII with stricter rules.

---


---

## 51.11 Integration Logs

**Integration Logs** belongs to **Third-Party Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Integration Logs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Integration Logs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Integration Logs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Integration Logs** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 51:** You can explain every lesson title in Phase 51 with a concrete Odoo or business example.

> **Phase 51 complete?** [Continue to Phase 52](#phase-52)


<a id="phase-52"></a>
# PHASE 52 - Payment Integrations

**Track:** APIs & Integrations

**WHAT YOU WILL BE ABLE TO DO:** Explain Odoo’s `payment.provider` / `payment.transaction` flow, keep PCI scope small, verify provider webhooks idempotently, and reconcile payment state safely.

**WHAT YOU SHOULD KNOW FIRST:** Phase 49 — signed idempotent webhooks. Phase 50 — integration boundaries. Phase 43 — public routes carefully.

**LEARNING RESOURCES:**

- [odoo/odoo `payment` and `payment_*` addons](https://github.com/odoo/odoo/tree/18.0/addons) — especially `payment_stripe` as a reference
- Official provider docs (Stripe/PayPal/etc.) for webhooks and test mode
- Prefer Odoo core patterns over random “create payment acquirer” blog posts when they conflict

## 52.1 Payment Architecture

**Payment Architecture** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Payment Architecture** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Payment Architecture** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Payment Architecture**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Payment Architecture** is concrete, continue — the next lesson in this phase still depends on it.

## 52.2 Payment Providers

**Payment Providers** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Payment Providers** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Payment Providers** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Payment Providers**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Payment Providers** is concrete, continue — the next lesson in this phase still depends on it.

## 52.3 Payment Transactions

**Payment Transactions** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Payment Transactions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Payment Transactions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Payment Transactions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Payment Transactions** is concrete, continue — the next lesson in this phase still depends on it.

## 52.4 Checkout Flow

**Checkout Flow** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Checkout Flow** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Checkout Flow** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Checkout Flow**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Checkout Flow** is concrete, continue — the next lesson in this phase still depends on it.

## 52.5 Redirects

**Redirects** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Redirects** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Redirects** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Redirects**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Redirects** is concrete, continue — the next lesson in this phase still depends on it.

## 52.6 Tokens

**Tokens** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Tokens** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Tokens** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Tokens**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Tokens** is concrete, continue — the next lesson in this phase still depends on it.

## 52.7 Callbacks

**Callbacks** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Callbacks** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Callbacks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Callbacks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Callbacks** is concrete, continue — the next lesson in this phase still depends on it.

## 52.8 Webhooks

**Webhooks** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Webhooks** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Webhooks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Webhooks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Webhooks** is concrete, continue — the next lesson in this phase still depends on it.

## 52.9 Payment Status

**Payment Status** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Payment Status** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Payment Status** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Payment Status**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Payment Status** is concrete, continue — the next lesson in this phase still depends on it.

## 52.10 Failure Handling

**Failure Handling** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Failure Handling** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Failure Handling** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Failure Handling**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Failure Handling** is concrete, continue — the next lesson in this phase still depends on it.

## 52.11 Refund Concepts

**Refund Concepts** belongs to **Payment Integrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Refund Concepts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Refund Concepts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Refund Concepts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Refund Concepts** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 52:** You can explain every lesson title in Phase 52 with a concrete Odoo or business example.

> **Phase 52 complete?** [Continue to Phase 53](#phase-53)


## STORY PROJECT — Odoo ↔ External SaaS Integration

**Track:** APIs & Integrations

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XIV — MODERN ODOO FRONTEND

**Track:** OWL Frontend

*Standard views go far. Custom interactive experiences require the web framework.*

---
<a id="phase-53"></a>
<a id="phase-53---odoo-javascript-architecture"></a>
# PHASE 53 - Odoo JavaScript Architecture

**Track:** OWL Frontend

**WHAT YOU WILL BE ABLE TO DO:** Map how the Odoo web client loads — assets, registries, services, and OWL components — and decide when XML views are enough versus when you need JavaScript.

**WHAT YOU SHOULD KNOW FIRST:** Story XII — controllers, RPC, and integrations. Story VII — views, actions, and menus. You can ship business screens in XML; this story starts when the screen must *behave* in ways XML cannot express.

**LEARNING RESOURCES:**

- [OWL components (Odoo 18)](https://www.odoo.com/documentation/18.0/developer/reference/frontend/owl_components.html)
- [Discover the JS framework — OWL components tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/discover_js_framework/01_owl_components.html)
- [Services reference](https://www.odoo.com/documentation/18.0/developer/reference/frontend/services.html)
- [OWL introduction (YouTube)](https://www.youtube.com/watch?v=YJg7dvwXQF8)
- [Odoo architecture overview (YouTube)](https://www.youtube.com/watch?v=WnsYmsq4Qr8)
- [ged-odoo/odoo-js-training-public](https://github.com/ged-odoo/odoo-js-training-public)
- [odoo/owl](https://github.com/odoo/owl) · [odoo/odoo web client](https://github.com/odoo/odoo)

## 53.1 Web Client

Story XII closed with Odoo talking to the outside world — RPC, webhooks, payments. Those integrations still land in **views** and **actions** you already know how to declare. For many products that is enough: a form, a list, a button that calls a Python method, a statusbar that moves draft → confirmed.

Then a real requirement shows up that XML alone cannot carry:

- a live KPI strip that refreshes without reloading the whole form
- a drag-and-drop planner that is not a kanban card rename
- a dashboard that composes charts, filters, and RPC calls into one full-screen experience
- a small UX hint that must react instantly to keystrokes before any server round-trip

You are no longer only describing *what fields appear*. You are programming the **browser half** of Odoo.

**THE WEB CLIENT IS AN APPLICATION, NOT A PAGE:**

```text
BROWSER                                              SERVER
+----------------------------------+                 +------------------+
| Web Client (Owl + services)      |   RPC / HTTP    | Odoo Python      |
|  registries | components | assets| <-------------> | ORM + controllers|
+----------------------------------+                 +--------+---------+
                                                               |
                                                               v
                                                         PostgreSQL
```

The client boots once, loads **asset bundles** (JS, CSS, XML templates), starts **services** (notification, rpc, orm, user, router…), and mounts a tree of **OWL components**. Clicking a menu does not download a brand-new website for every screen the way a 2005 ERP might. It asks the client to open an **action**, which mounts the right view or custom component.

That mental model matters because bugs feel different here. A missing CSS class is often “asset not in the right bundle.” A silent RPC failure is often “service used wrong” or “component destroyed mid-await.” An upgrade break is often “you patched a private method that moved.”

## 53.2 JavaScript Modules

**JavaScript Modules** belongs to **Odoo JavaScript Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **JavaScript Modules** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **JavaScript Modules** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **JavaScript Modules**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **JavaScript Modules** is concrete, continue — the next lesson in this phase still depends on it.

## 53.3 Native JS Modules

**Native JS Modules** belongs to **Odoo JavaScript Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Native JS Modules** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Native JS Modules** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Native JS Modules**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Native JS Modules** is concrete, continue — the next lesson in this phase still depends on it.

## 53.4 Frontend Environment

**Frontend Environment** belongs to **Odoo JavaScript Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Frontend Environment** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Frontend Environment** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Frontend Environment**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Frontend Environment** is concrete, continue — the next lesson in this phase still depends on it.

## 53.5 Components

**Components** belongs to **Odoo JavaScript Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Components** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Components** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Components**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Components** is concrete, continue — the next lesson in this phase still depends on it.

## 53.6 Registries

**Registries** belongs to **Odoo JavaScript Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Registries** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Registries** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Registries**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Registries** is concrete, continue — the next lesson in this phase still depends on it.

## 53.7 Services

When you open `/web`, roughly this happens (simplified, Odoo 18-shaped):

```text
1. Browser requests the web client shell
2. Asset bundles for backend load (JS modules + XML templates + CSS)
3. Owl environment is created
4. Services registry starts (notification, rpc, orm, user, action, ...)
5. WebClient component mounts (navbar, menus, content area)
6. User clicks a menu → action service loads an action descriptor
7. act_window → standard view controllers
   ir.actions.client → your registered Owl action component
```

**ES MODULES AND `@odoo-module`:**

Modern Odoo JS files are ES modules. The magic comment `/** @odoo-module **/` tells the asset pipeline to treat the file as a module with imports/exports. Imports like `@web/core/registry` or `@odoo/owl` resolve through Odoo’s alias map — you are not configuring Webpack by hand in an addon.

**LEGACY FOOTGUNS:**

| Old habit | Modern replacement |
| --------- | ------------------ |
| jQuery `$(...).on` in random JS | Owl event handlers / patch |
| `odoo.define(...)` AMD (very old) | ES modules + registries |
| Inline `<script>` in form views | Assets + components |
| Copying minified `web.assets` | Never |

If a tutorial still teaches `odoo.define` as the default for new Odoo 18 work, close the tab. Read Odoo 18 frontend docs and `ged-odoo/odoo-js-training-public` instead.

**WHEN XML IS ENOUGH — AND WHEN IT IS NOT:**

| Need | Prefer | Why |
| ---- | ------ | --- |
| Show/hide fields, buttons, domains | XML views + attrs / modifiers | Declarative, upgrade-friendly |
| Call server business logic | `type="object"` button → Python | Keeps rules on the server |
| Light polish (colors, icons) | SCSS in assets | Still declarative packaging |
| Interactive widget / dashboard / custom UX | OWL component + registry | Real client behavior |

The expensive mistake is forking the web client or pasting jQuery from Odoo 13 Stack Overflow into a modern database. Odoo 14+ rebuilt the client around **OWL**. Your job in Story XIII is to extend that architecture the way Odoo intends: modules declare assets, register pieces, patch narrowly, and keep business truth on the server.

**WHERE TO LOOK IN THE REPO (ORIENTATION, NOT MEMORIZATION):**

```text
addons/web/static/src/
  core/          # registries, services, utils, components
  views/         # form, list, kanban controllers & renderers
  webclient/     # shell: menus, action service, navbar
  search/        # search model / facets
```

You will not rewrite those folders. You will **depend on `web`**, ship files under your module’s `static/src/`, and hook into the same registries the core uses.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Name three reasons a form might still stay pure XML instead of OWL. |
| Medium | Draw the browser → RPC → ORM path for “user clicks Confirm on a sales order.” Mark where OWL lives. |
| Hard | Open `addons/web/static/src` in a source checkout and list five registry category names you find (grep `registry.category`). |

**WHAT THIS UNLOCKS NEXT:**

You can picture the client as an app with registries and services. You still cannot build a piece of UI inside it. That starts with OWL itself — components, templates, props, state, and lifecycle.

---


---

## 53.8 Hooks

**Hooks** belongs to **Odoo JavaScript Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Hooks** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Hooks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Hooks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Hooks** is concrete, continue — the next lesson in this phase still depends on it.

## 53.9 Backend ↔ Frontend Communication

**Backend ↔ Frontend Communication** belongs to **Odoo JavaScript Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Backend ↔ Frontend Communication** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Backend ↔ Frontend Communication** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Backend ↔ Frontend Communication**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Backend ↔ Frontend Communication** is concrete, continue — the next lesson in this phase still depends on it.

## 53.10 Frontend Debugging

**Frontend Debugging** belongs to **Odoo JavaScript Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Frontend Debugging** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Frontend Debugging** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Frontend Debugging**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Frontend Debugging** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 53:** You can explain every lesson title in Phase 53 with a concrete Odoo or business example.

> **Phase 53 complete?** [Continue to Phase 54](#phase-54)


<a id="phase-54"></a>
# PHASE 54 - OWL Fundamentals

**Track:** OWL Frontend

**WHAT YOU WILL BE ABLE TO DO:** Build a small OWL component with an XML template, typed props, reactive state, and correct lifecycle hooks — following Odoo’s `setup()` convention.

**WHAT YOU SHOULD KNOW FIRST:** Phase 52 — the web client is an Owl application loaded through assets and registries. Basic JavaScript classes and `async`/`await`.

**LEARNING RESOURCES:**

- [OWL components reference](https://www.odoo.com/documentation/18.0/developer/reference/frontend/owl_components.html)
- [Discover JS framework — 01 Owl components](https://www.odoo.com/documentation/18.0/developer/tutorials/discover_js_framework/01_owl_components.html)
- [Frontend OWL components howto](https://www.odoo.com/documentation/18.0/developer/howtos/frontend_owl_components.html)
- [OWL introduction (YouTube)](https://www.youtube.com/watch?v=YJg7dvwXQF8)
- [odoo/owl](https://github.com/odoo/owl)
- [ged-odoo/odoo-js-training-public](https://github.com/ged-odoo/odoo-js-training-public)

## 54.1 Components

**OWL** (Odoo Web Library) is Odoo’s component system — loosely inspired by ideas you may know from Vue or React, but with **QWeb-style templates** and Odoo-specific packaging. A component is a JavaScript class that owns a template, optional props, and optional local state. The framework mounts it into the DOM and re-renders when reactive state changes.

**THE SMALLEST HONEST COMPONENT:**

```javascript
/** @odoo-module **/
import { Component, useState } from "@odoo/owl";

export class LoanCounter extends Component {
    static template = "library_mgmt.LoanCounter";
    static props = {
        title: { type: String, optional: true },
    };

    setup() {
        this.state = useState({ count: 0 });
    }

    increment() {
        this.state.count++;
    }
}
```

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="library_mgmt.LoanCounter">
        <div class="o_loan_counter" t-on-click="increment">
            <t t-esc="props.title or 'Loans'"/>:
            <t t-esc="state.count"/>
        </div>
    </t>
</templates>
```

Three packaging rules from the official docs that save upgrade pain:

1. Prefer **XML template files** over inline `xml\`...\`` so strings can be translated and kept next to the component.
2. Name templates `addon_name.ComponentName` to avoid collisions across modules.
3. Initialize in **`setup()`**, never in `constructor`. Odoo’s patching and inheritance patterns assume `setup`.

**TEMPLATE DIRECTIVES YOU WILL USE CONSTANTLY:**

| Directive | Job |
| --------- | --- |
| `t-esc` | Escape and print a value |
| `t-out` | Output markup carefully (know the XSS risk) |
| `t-if` / `t-elif` / `t-else` | Conditional blocks |
| `t-foreach` / `t-as` | Loop |
| `t-on-click` | Event handler bound to a method |
| `t-att-class` / `t-attf-href` | Dynamic attributes |

Think of the template as the **view**, the class as the **controller**, and the server as the **source of business truth**. OWL is not where you hide access rules.

## 54.2 Templates

**OWL** (Odoo Web Library) is Odoo’s component system — loosely inspired by ideas you may know from Vue or React, but with **QWeb-style templates** and Odoo-specific packaging. A component is a JavaScript class that owns a template, optional props, and optional local state. The framework mounts it into the DOM and re-renders when reactive state changes.

**THE SMALLEST HONEST COMPONENT:**

```javascript
/** @odoo-module **/
import { Component, useState } from "@odoo/owl";

export class LoanCounter extends Component {
    static template = "library_mgmt.LoanCounter";
    static props = {
        title: { type: String, optional: true },
    };

    setup() {
        this.state = useState({ count: 0 });
    }

    increment() {
        this.state.count++;
    }
}
```

```xml
<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="library_mgmt.LoanCounter">
        <div class="o_loan_counter" t-on-click="increment">
            <t t-esc="props.title or 'Loans'"/>:
            <t t-esc="state.count"/>
        </div>
    </t>
</templates>
```

Three packaging rules from the official docs that save upgrade pain:

1. Prefer **XML template files** over inline `xml\`...\`` so strings can be translated and kept next to the component.
2. Name templates `addon_name.ComponentName` to avoid collisions across modules.
3. Initialize in **`setup()`**, never in `constructor`. Odoo’s patching and inheritance patterns assume `setup`.

**TEMPLATE DIRECTIVES YOU WILL USE CONSTANTLY:**

| Directive | Job |
| --------- | --- |
| `t-esc` | Escape and print a value |
| `t-out` | Output markup carefully (know the XSS risk) |
| `t-if` / `t-elif` / `t-else` | Conditional blocks |
| `t-foreach` / `t-as` | Loop |
| `t-on-click` | Event handler bound to a method |
| `t-att-class` / `t-attf-href` | Dynamic attributes |

Think of the template as the **view**, the class as the **controller**, and the server as the **source of business truth**. OWL is not where you hide access rules.

## 54.3 Props

**Props** are inputs from the parent (or from the registry/action that mounts you). Treat them as read-only for the child. **State** is local, mutable data owned by the component — created with `useState` so Owl can re-render when it changes.

**PROPS ARE THE CONTRACT; STATE IS THE SCRATCHPAD:**

```text
Parent / Action
   |  props: { partnerId, title }
   v
LoanCounter
   |  state: { count, loading, error }
   |  may call orm/rpc in setup / handlers
   v
DOM
```

Validate props with `static props = { ... }`. Optional props need `optional: true`. Missing required props fail loudly in debug — that is a gift.

**LIFECYCLE HOOKS (THE ONES YOU REACH FOR FIRST):**

| Hook | When it runs | Typical use |
| ---- | ------------ | ----------- |
| `setup()` | Component construction | Services, state, event buses |
| `onWillStart` | Before first render (async OK) | Initial RPC / orm read |
| `onMounted` | After first paint | Focus, measure DOM, start timers |
| `onWillUpdateProps` | Props about to change | Reset derived state |
| `onWillUnmount` | About to leave the DOM | Clear intervals, abort work |

```javascript
/** @odoo-module **/
import { Component, onWillStart, onWillUnmount, useState } from "@odoo/owl";
import { useService } from "@web/core/utils/hooks";

export class PartnerLoanBadge extends Component {
    static template = "library_mgmt.PartnerLoanBadge";
    static props = { partnerId: Number };

    setup() {
        this.orm = useService("orm");
        this.state = useState({ openLoans: 0, loading: true });
        onWillStart(async () => {
            this.state.openLoans = await this.orm.searchCount("library.loan", [
                ["partner_id", "=", this.props.partnerId],
                ["state", "=", "open"],
            ]);
            this.state.loading = false;
        });
        // If you start a setInterval, clear it in onWillUnmount.
        onWillUnmount(() => {});
    }
}
```

**ASYNC SAFETY:**

If the user navigates away while `await orm.searchCount(...)` is in flight, the component may already be destroyed. Prefer Odoo services marked async-aware, keep handlers idempotent, and do not write into `this.state` blindly after long waits without a mounted check pattern your version documents. Leaking intervals is the classic dashboard bug.

**BUSINESS RULE STILL LIVES ON THE SERVER:**

OWL can compute a pretty total for display. It must not be the only place that enforces “you may not confirm this loan.” A crafted RPC or another client could bypass the browser. Use OWL for interaction; use Python constraints, methods, and record rules for truth.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List three differences between props and `useState` values. |
| Medium | Write a component that shows loading → value → empty for a `searchCount` result (code on paper is fine). |
| Hard | Explain why translating strings belongs in XML templates and why `setup()` beats `constructor` in Odoo. |

**WHAT THIS UNLOCKS NEXT:**

A lone component still cannot talk to the rest of the client cleanly. Notifications, RPC, the current user, and action navigation live in **services**, discovered through **registries** and **hooks**.

---


---

## 54.4 State

**Props** are inputs from the parent (or from the registry/action that mounts you). Treat them as read-only for the child. **State** is local, mutable data owned by the component — created with `useState` so Owl can re-render when it changes.

**PROPS ARE THE CONTRACT; STATE IS THE SCRATCHPAD:**

```text
Parent / Action
   |  props: { partnerId, title }
   v
LoanCounter
   |  state: { count, loading, error }
   |  may call orm/rpc in setup / handlers
   v
DOM
```

Validate props with `static props = { ... }`. Optional props need `optional: true`. Missing required props fail loudly in debug — that is a gift.

**LIFECYCLE HOOKS (THE ONES YOU REACH FOR FIRST):**

| Hook | When it runs | Typical use |
| ---- | ------------ | ----------- |
| `setup()` | Component construction | Services, state, event buses |
| `onWillStart` | Before first render (async OK) | Initial RPC / orm read |
| `onMounted` | After first paint | Focus, measure DOM, start timers |
| `onWillUpdateProps` | Props about to change | Reset derived state |
| `onWillUnmount` | About to leave the DOM | Clear intervals, abort work |

```javascript
/** @odoo-module **/
import { Component, onWillStart, onWillUnmount, useState } from "@odoo/owl";
import { useService } from "@web/core/utils/hooks";

export class PartnerLoanBadge extends Component {
    static template = "library_mgmt.PartnerLoanBadge";
    static props = { partnerId: Number };

    setup() {
        this.orm = useService("orm");
        this.state = useState({ openLoans: 0, loading: true });
        onWillStart(async () => {
            this.state.openLoans = await this.orm.searchCount("library.loan", [
                ["partner_id", "=", this.props.partnerId],
                ["state", "=", "open"],
            ]);
            this.state.loading = false;
        });
        // If you start a setInterval, clear it in onWillUnmount.
        onWillUnmount(() => {});
    }
}
```

**ASYNC SAFETY:**

If the user navigates away while `await orm.searchCount(...)` is in flight, the component may already be destroyed. Prefer Odoo services marked async-aware, keep handlers idempotent, and do not write into `this.state` blindly after long waits without a mounted check pattern your version documents. Leaking intervals is the classic dashboard bug.

**BUSINESS RULE STILL LIVES ON THE SERVER:**

OWL can compute a pretty total for display. It must not be the only place that enforces “you may not confirm this loan.” A crafted RPC or another client could bypass the browser. Use OWL for interaction; use Python constraints, methods, and record rules for truth.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List three differences between props and `useState` values. |
| Medium | Write a component that shows loading → value → empty for a `searchCount` result (code on paper is fine). |
| Hard | Explain why translating strings belongs in XML templates and why `setup()` beats `constructor` in Odoo. |

**WHAT THIS UNLOCKS NEXT:**

A lone component still cannot talk to the rest of the client cleanly. Notifications, RPC, the current user, and action navigation live in **services**, discovered through **registries** and **hooks**.

---


---

## 54.5 Events

**Events** belongs to **OWL Fundamentals**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Events** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Events** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Events**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Events** is concrete, continue — the next lesson in this phase still depends on it.

## 54.6 Lifecycle

**Props** are inputs from the parent (or from the registry/action that mounts you). Treat them as read-only for the child. **State** is local, mutable data owned by the component — created with `useState` so Owl can re-render when it changes.

**PROPS ARE THE CONTRACT; STATE IS THE SCRATCHPAD:**

```text
Parent / Action
   |  props: { partnerId, title }
   v
LoanCounter
   |  state: { count, loading, error }
   |  may call orm/rpc in setup / handlers
   v
DOM
```

Validate props with `static props = { ... }`. Optional props need `optional: true`. Missing required props fail loudly in debug — that is a gift.

**LIFECYCLE HOOKS (THE ONES YOU REACH FOR FIRST):**

| Hook | When it runs | Typical use |
| ---- | ------------ | ----------- |
| `setup()` | Component construction | Services, state, event buses |
| `onWillStart` | Before first render (async OK) | Initial RPC / orm read |
| `onMounted` | After first paint | Focus, measure DOM, start timers |
| `onWillUpdateProps` | Props about to change | Reset derived state |
| `onWillUnmount` | About to leave the DOM | Clear intervals, abort work |

```javascript
/** @odoo-module **/
import { Component, onWillStart, onWillUnmount, useState } from "@odoo/owl";
import { useService } from "@web/core/utils/hooks";

export class PartnerLoanBadge extends Component {
    static template = "library_mgmt.PartnerLoanBadge";
    static props = { partnerId: Number };

    setup() {
        this.orm = useService("orm");
        this.state = useState({ openLoans: 0, loading: true });
        onWillStart(async () => {
            this.state.openLoans = await this.orm.searchCount("library.loan", [
                ["partner_id", "=", this.props.partnerId],
                ["state", "=", "open"],
            ]);
            this.state.loading = false;
        });
        // If you start a setInterval, clear it in onWillUnmount.
        onWillUnmount(() => {});
    }
}
```

**ASYNC SAFETY:**

If the user navigates away while `await orm.searchCount(...)` is in flight, the component may already be destroyed. Prefer Odoo services marked async-aware, keep handlers idempotent, and do not write into `this.state` blindly after long waits without a mounted check pattern your version documents. Leaking intervals is the classic dashboard bug.

**BUSINESS RULE STILL LIVES ON THE SERVER:**

OWL can compute a pretty total for display. It must not be the only place that enforces “you may not confirm this loan.” A crafted RPC or another client could bypass the browser. Use OWL for interaction; use Python constraints, methods, and record rules for truth.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | List three differences between props and `useState` values. |
| Medium | Write a component that shows loading → value → empty for a `searchCount` result (code on paper is fine). |
| Hard | Explain why translating strings belongs in XML templates and why `setup()` beats `constructor` in Odoo. |

**WHAT THIS UNLOCKS NEXT:**

A lone component still cannot talk to the rest of the client cleanly. Notifications, RPC, the current user, and action navigation live in **services**, discovered through **registries** and **hooks**.

---


---

## 54.7 Composition

**Composition** belongs to **OWL Fundamentals**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Composition** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Composition** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Composition**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Composition** is concrete, continue — the next lesson in this phase still depends on it.

## 54.8 Subcomponents

**Subcomponents** belongs to **OWL Fundamentals**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Subcomponents** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Subcomponents** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Subcomponents**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Subcomponents** is concrete, continue — the next lesson in this phase still depends on it.

## 54.9 Reactive UI

**Reactive UI** belongs to **OWL Fundamentals**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Reactive UI** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Reactive UI** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Reactive UI**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Reactive UI** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 54:** You can explain every lesson title in Phase 54 with a concrete Odoo or business example.

> **Phase 54 complete?** [Continue to Phase 55](#phase-55)


<a id="phase-55"></a>
# PHASE 55 - Services, Registries & Hooks

**Track:** OWL Frontend

**WHAT YOU WILL BE ABLE TO DO:** Register and consume web client services with `useService`, place components and actions in the correct registry categories, and use common Owl/Odoo hooks without inventing global singletons.

**WHAT YOU SHOULD KNOW FIRST:** Phase 53 — components, templates, props, state, lifecycle.

**LEARNING RESOURCES:**

- [Services reference](https://www.odoo.com/documentation/18.0/developer/reference/frontend/services.html)
- [OWL components](https://www.odoo.com/documentation/18.0/developer/reference/frontend/owl_components.html)
- [Discover JS framework tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/discover_js_framework/01_owl_components.html)
- [ged-odoo/odoo-js-training-public](https://github.com/ged-odoo/odoo-js-training-public)
- [odoo/odoo — web/core](https://github.com/odoo/odoo/tree/18.0/addons/web/static/src/core)

## 55.1 Registries

A **registry** is a named catalog. Categories you will meet early:

| Category | Typical content |
| -------- | --------------- |
| `services` | Long-lived services |
| `actions` | Client actions (full-screen custom UI) |
| `view_widgets` / field widgets | Field UI extensions |
| `main_components` | Shell-level mounts |
| `effects` | Effect implementations |

```javascript
/** @odoo-module **/
import { registry } from "@web/core/registry";
import { LibraryDashboard } from "./library_dashboard";

registry.category("actions").add("library_mgmt.dashboard", LibraryDashboard);
```

Registration is how your module **publishes** a capability without editing core files. The key string becomes part of your public contract — keep it namespaced (`module.thing`).

**HOOKS ARE HOW COMPONENTS SUBSCRIBE TO THE FRAMEWORK:**

- Owl hooks: `useState`, `onWillStart`, `onMounted`, `onWillUnmount`, …
- Odoo hooks: `useService`, plus view-specific hooks you will meet when extending form/list

Rule of thumb: if you need something global and stateful, it is probably a **service**. If you need UI composition, it is a **component** registered somewhere. If you need one-line access inside `setup`, it is a **hook**.

**ANTI-PATTERNS THIS PHASE KILLS:**

```text
Bad:  window.odooLibraryState = {}
Bad:  fetch('/web/dataset/call_kw/...') by hand in five widgets
Bad:  importing a service file and calling its internals as a singleton
Good: useService("orm") + registry.category("actions").add(...)
```

## 55.2 Registry Categories

**Registry Categories** belongs to **Services, Registries & Hooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Registry Categories** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Registry Categories** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Registry Categories**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Registry Categories** is concrete, continue — the next lesson in this phase still depends on it.

## 55.3 Services

A **service** is a long-lived feature of the web client: notifications, RPC, ORM helpers, router, user info, effects (the famous rainbow man), titles, cookies. Components should not invent `window.myGlobalRpc`. They should **declare a dependency** and receive the service through the environment.

At startup, Odoo walks the `services` registry, starts each service (honoring `dependencies`), and stores the result on `env.services`.

```javascript
/** @odoo-module **/
import { registry } from "@web/core/registry";

const tickService = {
    dependencies: ["notification"],
    start(env, { notification }) {
        // Side-effect-only services may return nothing (null).
        let n = 0;
        const id = setInterval(() => {
            n += 1;
            notification.add(`Library heartbeat ${n}`, { type: "info" });
        }, 60000);
        return {
            stop() {
                clearInterval(id);
            },
        };
    },
};

registry.category("services").add("library_tick", tickService);
```

**USING A SERVICE FROM A COMPONENT:**

```javascript
import { useService } from "@web/core/utils/hooks";

setup() {
    this.notification = useService("notification");
    this.orm = useService("orm");
    this.action = useService("action");
}
```

| Service (examples) | You reach for it when… |
| ------------------ | ---------------------- |
| `notification` | Toast a success / warning |
| `orm` | Call `searchRead` / `call` the way the client prefers |
| `rpc` | Lower-level `/web/dataset` style calls when needed |
| `action` | `doAction` to open another act_window / client action |
| `user` | Current user context bits |
| `router` | URL / hash coordination |
| `effect` | Celebratory UI feedback (use sparingly) |

Services are also a **testing seam**: JS tests can disable or replace services so side effects do not poison assertions. That is why “just attach to `window`” is not only ugly — it fights the framework.

## 55.4 Service Dependencies

Prefer `useService("orm")` for model operations from Owl:

```javascript
const rows = await this.orm.searchRead(
    "library.loan",
    [["state", "=", "open"]],
    ["id", "name", "partner_id", "due_date"],
    { limit: 80 },
);
await this.orm.call("library.loan", "action_confirm", [ids]);
```

Use lower-level `rpc` when you must hit a custom JSON route. Do not invent a third HTTP client. Consistency matters when debugging network panels and when services’ async flags protect destroyed components.

**ENV AS THE SHARED WORLD:**

Services receive `env`. Components reach services through hooks that read `env`. That shared environment is why replacing a service in tests works. Global singletons erase that boundary.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain services vs components in one paragraph. |
| Medium | Sketch a service that wraps `notification` to standardize library toast titles. |
| Hard | Find three `registry.category(...)` calls in `web` or an OCA module and write what each category is for. |

**WHAT THIS UNLOCKS NEXT:**

You know how to register and consume pieces. Those pieces still never load unless the **asset bundle** includes your JS/XML/SCSS. Manifest assets are the shipping department of the frontend.

---


---

## 55.5 useService()

**useService()** belongs to **Services, Registries & Hooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **useService()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **useService()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **useService()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **useService()** is concrete, continue — the next lesson in this phase still depends on it.

## 55.6 ORM Service

Prefer `useService("orm")` for model operations from Owl:

```javascript
const rows = await this.orm.searchRead(
    "library.loan",
    [["state", "=", "open"]],
    ["id", "name", "partner_id", "due_date"],
    { limit: 80 },
);
await this.orm.call("library.loan", "action_confirm", [ids]);
```

Use lower-level `rpc` when you must hit a custom JSON route. Do not invent a third HTTP client. Consistency matters when debugging network panels and when services’ async flags protect destroyed components.

**ENV AS THE SHARED WORLD:**

Services receive `env`. Components reach services through hooks that read `env`. That shared environment is why replacing a service in tests works. Global singletons erase that boundary.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain services vs components in one paragraph. |
| Medium | Sketch a service that wraps `notification` to standardize library toast titles. |
| Hard | Find three `registry.category(...)` calls in `web` or an OCA module and write what each category is for. |

**WHAT THIS UNLOCKS NEXT:**

You know how to register and consume pieces. Those pieces still never load unless the **asset bundle** includes your JS/XML/SCSS. Manifest assets are the shipping department of the frontend.

---


---

## 55.7 RPC Service

Prefer `useService("orm")` for model operations from Owl:

```javascript
const rows = await this.orm.searchRead(
    "library.loan",
    [["state", "=", "open"]],
    ["id", "name", "partner_id", "due_date"],
    { limit: 80 },
);
await this.orm.call("library.loan", "action_confirm", [ids]);
```

Use lower-level `rpc` when you must hit a custom JSON route. Do not invent a third HTTP client. Consistency matters when debugging network panels and when services’ async flags protect destroyed components.

**ENV AS THE SHARED WORLD:**

Services receive `env`. Components reach services through hooks that read `env`. That shared environment is why replacing a service in tests works. Global singletons erase that boundary.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain services vs components in one paragraph. |
| Medium | Sketch a service that wraps `notification` to standardize library toast titles. |
| Hard | Find three `registry.category(...)` calls in `web` or an OCA module and write what each category is for. |

**WHAT THIS UNLOCKS NEXT:**

You know how to register and consume pieces. Those pieces still never load unless the **asset bundle** includes your JS/XML/SCSS. Manifest assets are the shipping department of the frontend.

---


---

## 55.8 Notification Service

Prefer `useService("orm")` for model operations from Owl:

```javascript
const rows = await this.orm.searchRead(
    "library.loan",
    [["state", "=", "open"]],
    ["id", "name", "partner_id", "due_date"],
    { limit: 80 },
);
await this.orm.call("library.loan", "action_confirm", [ids]);
```

Use lower-level `rpc` when you must hit a custom JSON route. Do not invent a third HTTP client. Consistency matters when debugging network panels and when services’ async flags protect destroyed components.

**ENV AS THE SHARED WORLD:**

Services receive `env`. Components reach services through hooks that read `env`. That shared environment is why replacing a service in tests works. Global singletons erase that boundary.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain services vs components in one paragraph. |
| Medium | Sketch a service that wraps `notification` to standardize library toast titles. |
| Hard | Find three `registry.category(...)` calls in `web` or an OCA module and write what each category is for. |

**WHAT THIS UNLOCKS NEXT:**

You know how to register and consume pieces. Those pieces still never load unless the **asset bundle** includes your JS/XML/SCSS. Manifest assets are the shipping department of the frontend.

---


---

## 55.9 Action Service

Prefer `useService("orm")` for model operations from Owl:

```javascript
const rows = await this.orm.searchRead(
    "library.loan",
    [["state", "=", "open"]],
    ["id", "name", "partner_id", "due_date"],
    { limit: 80 },
);
await this.orm.call("library.loan", "action_confirm", [ids]);
```

Use lower-level `rpc` when you must hit a custom JSON route. Do not invent a third HTTP client. Consistency matters when debugging network panels and when services’ async flags protect destroyed components.

**ENV AS THE SHARED WORLD:**

Services receive `env`. Components reach services through hooks that read `env`. That shared environment is why replacing a service in tests works. Global singletons erase that boundary.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain services vs components in one paragraph. |
| Medium | Sketch a service that wraps `notification` to standardize library toast titles. |
| Hard | Find three `registry.category(...)` calls in `web` or an OCA module and write what each category is for. |

**WHAT THIS UNLOCKS NEXT:**

You know how to register and consume pieces. Those pieces still never load unless the **asset bundle** includes your JS/XML/SCSS. Manifest assets are the shipping department of the frontend.

---


---

## 55.10 Hooks

A **registry** is a named catalog. Categories you will meet early:

| Category | Typical content |
| -------- | --------------- |
| `services` | Long-lived services |
| `actions` | Client actions (full-screen custom UI) |
| `view_widgets` / field widgets | Field UI extensions |
| `main_components` | Shell-level mounts |
| `effects` | Effect implementations |

```javascript
/** @odoo-module **/
import { registry } from "@web/core/registry";
import { LibraryDashboard } from "./library_dashboard";

registry.category("actions").add("library_mgmt.dashboard", LibraryDashboard);
```

Registration is how your module **publishes** a capability without editing core files. The key string becomes part of your public contract — keep it namespaced (`module.thing`).

**HOOKS ARE HOW COMPONENTS SUBSCRIBE TO THE FRAMEWORK:**

- Owl hooks: `useState`, `onWillStart`, `onMounted`, `onWillUnmount`, …
- Odoo hooks: `useService`, plus view-specific hooks you will meet when extending form/list

Rule of thumb: if you need something global and stateful, it is probably a **service**. If you need UI composition, it is a **component** registered somewhere. If you need one-line access inside `setup`, it is a **hook**.

**ANTI-PATTERNS THIS PHASE KILLS:**

```text
Bad:  window.odooLibraryState = {}
Bad:  fetch('/web/dataset/call_kw/...') by hand in five widgets
Bad:  importing a service file and calling its internals as a singleton
Good: useService("orm") + registry.category("actions").add(...)
```

## 55.11 useBus

**useBus** belongs to **Services, Registries & Hooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **useBus** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **useBus** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **useBus**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **useBus** is concrete, continue — the next lesson in this phase still depends on it.

## 55.12 useAssets

**useAssets** belongs to **Services, Registries & Hooks**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **useAssets** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **useAssets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **useAssets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **useAssets** is concrete, continue — the next lesson in this phase still depends on it.

## 55.13 Custom Hooks

A **registry** is a named catalog. Categories you will meet early:

| Category | Typical content |
| -------- | --------------- |
| `services` | Long-lived services |
| `actions` | Client actions (full-screen custom UI) |
| `view_widgets` / field widgets | Field UI extensions |
| `main_components` | Shell-level mounts |
| `effects` | Effect implementations |

```javascript
/** @odoo-module **/
import { registry } from "@web/core/registry";
import { LibraryDashboard } from "./library_dashboard";

registry.category("actions").add("library_mgmt.dashboard", LibraryDashboard);
```

Registration is how your module **publishes** a capability without editing core files. The key string becomes part of your public contract — keep it namespaced (`module.thing`).

**HOOKS ARE HOW COMPONENTS SUBSCRIBE TO THE FRAMEWORK:**

- Owl hooks: `useState`, `onWillStart`, `onMounted`, `onWillUnmount`, …
- Odoo hooks: `useService`, plus view-specific hooks you will meet when extending form/list

Rule of thumb: if you need something global and stateful, it is probably a **service**. If you need UI composition, it is a **component** registered somewhere. If you need one-line access inside `setup`, it is a **hook**.

**ANTI-PATTERNS THIS PHASE KILLS:**

```text
Bad:  window.odooLibraryState = {}
Bad:  fetch('/web/dataset/call_kw/...') by hand in five widgets
Bad:  importing a service file and calling its internals as a singleton
Good: useService("orm") + registry.category("actions").add(...)
```


<a id="phase-56"></a>
<a id="phase-56---assets-frontend-resources"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 55:** You can explain every lesson title in Phase 55 with a concrete Odoo or business example.

> **Phase 55 complete?** [Continue to Phase 56](#phase-56)


<a id="phase-56"></a>
# PHASE 56 - Assets & Frontend Resources

**Track:** OWL Frontend

**WHAT YOU WILL BE ABLE TO DO:** Declare backend (and related) asset bundles in `__manifest__.py`, place static files correctly, control load order with directives, and debug “my JS never runs” failures.

**WHAT YOU SHOULD KNOW FIRST:** Phases 52–54 — architecture, OWL components, registries/services. Phase 7 — module anatomy (`static/`).

**LEARNING RESOURCES:**

- [Assets reference](https://www.odoo.com/documentation/18.0/developer/reference/frontend/assets.html)
- [OWL components — file layout](https://www.odoo.com/documentation/18.0/developer/reference/frontend/owl_components.html)
- [Discover JS framework tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/discover_js_framework/01_owl_components.html)
- [Architecture overview (YouTube)](https://www.youtube.com/watch?v=WnsYmsq4Qr8)
- [ged-odoo/odoo-js-training-public](https://github.com/ged-odoo/odoo-js-training-public)

## 56.1 Asset Bundles

Odoo does not magically import every file under `static/`. You **declare** which files belong to which **bundle**. Common backend work uses `web.assets_backend`. Website work uses frontend bundles. Putting backend OWL code in the website bundle is a classic “works on my machine / invisible in form view” bug.

**MANIFEST SHAPE:**

```python
# __manifest__.py (excerpt)
{
    "name": "Library Management",
    "depends": ["web", "base"],
    "assets": {
        "web.assets_backend": [
            "library_mgmt/static/src/**/*",
            # or explicit files:
            # "library_mgmt/static/src/components/loan_counter/loan_counter.js",
            # "library_mgmt/static/src/components/loan_counter/loan_counter.xml",
            # "library_mgmt/static/src/components/loan_counter/loan_counter.scss",
        ],
    },
}
```

**FILE LAYOUT THAT MATCHES ODOO’S HABIT:**

```text
library_mgmt/
  static/
    src/
      components/
        loan_counter/
          loan_counter.js
          loan_counter.xml
          loan_counter.scss
      services/
        library_tick_service.js
      actions/
        library_dashboard.js
        library_dashboard.xml
```

Keep component triplets together. Use `/** @odoo-module **/` at the top of JS files so the bundler treats them as ES modules.

**ORDER DIRECTIVES:**

Manifest list entries can include tuples like `'before'`, `'after'`, `'replace'` when you must inject relative to another file. Reach for them when a SCSS variable must exist before your rules, or when you intentionally replace a file — not as a first instinct.

**DEBUGGING “ASSET NEVER LOADS”:**

1. Confirm the file path matches the module directory name exactly.
2. Upgrade the module (`-u library_mgmt`) after manifest asset changes.
3. Use developer mode / `--dev=assets` during iteration so bundles rebuild more helpfully.
4. Hard-refresh the browser (asset hashes and caching lie to tired developers).
5. Check you targeted `web.assets_backend` for backend UI.

**QWEB REPORTS ≠ OWL TEMPLATES:**

Report QWeb (PDF/HTML print) and Owl XML templates both look like XML with `t-` directives, but they live in different pipelines. Do not drop a report template into `static/src` and expect a component to find it — or the reverse.

## 56.2 JavaScript Assets

**JavaScript Assets** belongs to **Assets & Frontend Resources**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **JavaScript Assets** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **JavaScript Assets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **JavaScript Assets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **JavaScript Assets** is concrete, continue — the next lesson in this phase still depends on it.

## 56.3 SCSS / CSS

**SCSS / CSS** belongs to **Assets & Frontend Resources**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **SCSS / CSS** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **SCSS / CSS** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **SCSS / CSS**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **SCSS / CSS** is concrete, continue — the next lesson in this phase still depends on it.

## 56.4 XML Templates

**XML Templates** belongs to **Assets & Frontend Resources**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **XML Templates** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **XML Templates** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **XML Templates**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **XML Templates** is concrete, continue — the next lesson in this phase still depends on it.

## 56.5 Manifest Assets

**Manifest Assets** belongs to **Assets & Frontend Resources**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Manifest Assets** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Manifest Assets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Manifest Assets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Manifest Assets** is concrete, continue — the next lesson in this phase still depends on it.

## 56.6 Lazy Loading

**Lazy Loading** belongs to **Assets & Frontend Resources**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Lazy Loading** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Lazy Loading** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Lazy Loading**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Lazy Loading** is concrete, continue — the next lesson in this phase still depends on it.

## 56.7 Asset Debugging

**Asset Debugging** belongs to **Assets & Frontend Resources**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Asset Debugging** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Asset Debugging** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Asset Debugging**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Asset Debugging** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 56:** You can explain every lesson title in Phase 56 with a concrete Odoo or business example.

> **Phase 56 complete?** [Continue to Phase 57](#phase-57)


<a id="phase-57"></a>
# PHASE 57 - Extending & Patching the Web Client

**Track:** OWL Frontend

**WHAT YOU WILL BE ABLE TO DO:** Patch existing Owl components and view controllers with `patch()`, choose patch over fork, and avoid upgrade-hostile overrides.

**WHAT YOU SHOULD KNOW FIRST:** Phases 53–55 — components, services/registries, assets. Story IX — server-side `_inherit` mindset (same discipline, different layer).

**LEARNING RESOURCES:**

- [Frontend OWL components howto](https://www.odoo.com/documentation/18.0/developer/howtos/frontend_owl_components.html)
- [OWL components reference](https://www.odoo.com/documentation/18.0/developer/reference/frontend/owl_components.html)
- [OWL intro (YouTube)](https://www.youtube.com/watch?v=YJg7dvwXQF8)
- [ged-odoo/odoo-js-training-public](https://github.com/ged-odoo/odoo-js-training-public)
- [odoo/odoo web views](https://github.com/odoo/odoo/tree/18.0/addons/web/static/src/views)

## 57.1 Patching Code

**Patching Code** belongs to **Extending & Patching the Web Client**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Patching Code** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Patching Code** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Patching Code**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Patching Code** is concrete, continue — the next lesson in this phase still depends on it.

## 57.2 Patching Components

**Patching Components** belongs to **Extending & Patching the Web Client**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Patching Components** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Patching Components** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Patching Components**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Patching Components** is concrete, continue — the next lesson in this phase still depends on it.

## 57.3 Extending Components

**Extending Components** belongs to **Extending & Patching the Web Client**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Extending Components** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Extending Components** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Extending Components**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Extending Components** is concrete, continue — the next lesson in this phase still depends on it.

## 57.4 Extending Registries

**Extending Registries** belongs to **Extending & Patching the Web Client**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Extending Registries** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Extending Registries** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Extending Registries**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Extending Registries** is concrete, continue — the next lesson in this phase still depends on it.

## 57.5 Custom Field Widgets

**Custom Field Widgets** belongs to **Extending & Patching the Web Client**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Custom Field Widgets** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Custom Field Widgets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Custom Field Widgets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Custom Field Widgets** is concrete, continue — the next lesson in this phase still depends on it.

## 57.6 View Extensions

**View Extensions** belongs to **Extending & Patching the Web Client**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **View Extensions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **View Extensions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **View Extensions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **View Extensions** is concrete, continue — the next lesson in this phase still depends on it.

## 57.7 Upgrade Safety

**Upgrade Safety** belongs to **Extending & Patching the Web Client**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Upgrade Safety** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Upgrade Safety** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Upgrade Safety**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Upgrade Safety** is concrete, continue — the next lesson in this phase still depends on it.

## 57.8 When Not to Patch

**When Not to Patch** belongs to **Extending & Patching the Web Client**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **When Not to Patch** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **When Not to Patch** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **When Not to Patch**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **When Not to Patch** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 57:** You can explain every lesson title in Phase 57 with a concrete Odoo or business example.

> **Phase 57 complete?** [Continue to Phase 58](#phase-58)


<a id="phase-58"></a>
# PHASE 58 - Client Actions & Advanced Frontend

**Track:** OWL Frontend

**WHAT YOU WILL BE ABLE TO DO:** Register an Owl component as a client action, bind it with `ir.actions.client` and a menu, and load data through `orm`/`rpc` services with loading and error states.

**WHAT YOU SHOULD KNOW FIRST:** Phases 54–56 — registries, assets, patching. Phase 25 — actions & menus (server-side action records).

**LEARNING RESOURCES:**

- [Frontend OWL components howto](https://www.odoo.com/documentation/18.0/developer/howtos/frontend_owl_components.html)
- [Services — orm / notification](https://www.odoo.com/documentation/18.0/developer/reference/frontend/services.html)
- [Discover JS framework](https://www.odoo.com/documentation/18.0/developer/tutorials/discover_js_framework/01_owl_components.html)
- [OWL intro (YouTube)](https://www.youtube.com/watch?v=YJg7dvwXQF8)
- [ged-odoo/odoo-js-training-public](https://github.com/ged-odoo/odoo-js-training-public)

## 58.1 Client Actions

A **client action** tells the web client: “mount this JavaScript action implementation,” not “open this model’s views.” Dashboards, configuration studios, and custom workplaces live here.

**THREE MOVING PARTS:**

```text
1) Owl component  -> registry.category("actions").add("library_mgmt.dashboard", Comp)
2) ir.actions.client XML  -> tag = library_mgmt.dashboard
3) Menu  -> action = that client action
(+ assets so the JS/XML actually load)
```

```javascript
/** @odoo-module **/
import { Component, onWillStart, useState } from "@odoo/owl";
import { registry } from "@web/core/registry";
import { useService } from "@web/core/utils/hooks";

export class LibraryDashboard extends Component {
    static template = "library_mgmt.LibraryDashboard";
    static props = { "*": true }; // action props vary; tighten when you know the shape

    setup() {
        this.orm = useService("orm");
        this.action = useService("action");
        this.notification = useService("notification");
        this.state = useState({
            loading: true,
            openLoans: 0,
            overdue: 0,
            error: null,
        });
        onWillStart(async () => {
            try {
                this.state.openLoans = await this.orm.searchCount("library.loan", [
                    ["state", "=", "open"],
                ]);
                this.state.overdue = await this.orm.searchCount("library.loan", [
                    ["state", "=", "open"],
                    ["due_date", "<", new Date().toISOString().slice(0, 10)],
                ]);
            } catch (e) {
                this.state.error = e.message || String(e);
            } finally {
                this.state.loading = false;
            }
        });
    }

    openOverdue() {
        this.action.doAction({
            type: "ir.actions.act_window",
            name: "Overdue loans",
            res_model: "library.loan",
            views: [[false, "list"], [false, "form"]],
            domain: [
                ["state", "=", "open"],
                ["due_date", "<", new Date().toISOString().slice(0, 10)],
            ],
        });
    }
}

registry.category("actions").add("library_mgmt.dashboard", LibraryDashboard);
```

```xml
<record id="action_library_dashboard" model="ir.actions.client">
    <field name="name">Library Dashboard</field>
    <field name="tag">library_mgmt.dashboard</field>
</record>

<menuitem id="menu_library_dashboard"
          name="Dashboard"
          parent="menu_library_root"
          action="action_library_dashboard"
          sequence="1"/>
```

**UX STATES THAT SEPARATE TOYS FROM PRODUCTS:**

| State | User sees |
| ----- | --------- |
| Loading | Skeleton / spinner |
| Empty | Honest empty copy + CTA |
| Error | Message + retry; no blank screen |
| Access denied | Friendly denial (server still enforces) |
| Ready | KPIs + buttons that `doAction` into real views |

## 58.2 Action Registry

**Action Registry** belongs to **Client Actions & Advanced Frontend**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Action Registry** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Action Registry** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Action Registry**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Action Registry** is concrete, continue — the next lesson in this phase still depends on it.

## 58.3 OWL Client Components

A **client action** tells the web client: “mount this JavaScript action implementation,” not “open this model’s views.” Dashboards, configuration studios, and custom workplaces live here.

**THREE MOVING PARTS:**

```text
1) Owl component  -> registry.category("actions").add("library_mgmt.dashboard", Comp)
2) ir.actions.client XML  -> tag = library_mgmt.dashboard
3) Menu  -> action = that client action
(+ assets so the JS/XML actually load)
```

```javascript
/** @odoo-module **/
import { Component, onWillStart, useState } from "@odoo/owl";
import { registry } from "@web/core/registry";
import { useService } from "@web/core/utils/hooks";

export class LibraryDashboard extends Component {
    static template = "library_mgmt.LibraryDashboard";
    static props = { "*": true }; // action props vary; tighten when you know the shape

    setup() {
        this.orm = useService("orm");
        this.action = useService("action");
        this.notification = useService("notification");
        this.state = useState({
            loading: true,
            openLoans: 0,
            overdue: 0,
            error: null,
        });
        onWillStart(async () => {
            try {
                this.state.openLoans = await this.orm.searchCount("library.loan", [
                    ["state", "=", "open"],
                ]);
                this.state.overdue = await this.orm.searchCount("library.loan", [
                    ["state", "=", "open"],
                    ["due_date", "<", new Date().toISOString().slice(0, 10)],
                ]);
            } catch (e) {
                this.state.error = e.message || String(e);
            } finally {
                this.state.loading = false;
            }
        });
    }

    openOverdue() {
        this.action.doAction({
            type: "ir.actions.act_window",
            name: "Overdue loans",
            res_model: "library.loan",
            views: [[false, "list"], [false, "form"]],
            domain: [
                ["state", "=", "open"],
                ["due_date", "<", new Date().toISOString().slice(0, 10)],
            ],
        });
    }
}

registry.category("actions").add("library_mgmt.dashboard", LibraryDashboard);
```

```xml
<record id="action_library_dashboard" model="ir.actions.client">
    <field name="name">Library Dashboard</field>
    <field name="tag">library_mgmt.dashboard</field>
</record>

<menuitem id="menu_library_dashboard"
          name="Dashboard"
          parent="menu_library_root"
          action="action_library_dashboard"
          sequence="1"/>
```

**UX STATES THAT SEPARATE TOYS FROM PRODUCTS:**

| State | User sees |
| ----- | --------- |
| Loading | Skeleton / spinner |
| Empty | Honest empty copy + CTA |
| Error | Message + retry; no blank screen |
| Access denied | Friendly denial (server still enforces) |
| Ready | KPIs + buttons that `doAction` into real views |

## 58.4 Loading Server Data

If the dashboard needs five counts, prefer one Python method returning a dict over five `searchCount` round trips from Owl:

```python
# library.loan
def get_dashboard_kpis(self):
    self.check_access_rights("read")
    Loan = self.env["library.loan"]
    return {
        "open": Loan.search_count([("state", "=", "open")]),
        "overdue": Loan.search_count([
            ("state", "=", "open"),
            ("due_date", "<", fields.Date.context_today(self)),
        ]),
    }
```

Call it with `orm.call`. Still enforce ACL inside. The browser displays; the server decides.

Never trust the browser as ACL. If `searchCount` succeeds for a user who should see nothing, your **record rules** are wrong — fix security on the server (Story VIII), then keep the dashboard honest.

**STORY PROJECT — INTERACTIVE ODOO DASHBOARD:**

Build a small backend dashboard module that:

1. Registers a client action with Owl.
2. Declares assets correctly.
3. Loads at least two KPIs via `orm`.
4. Offers a button that opens a filtered `act_window`.
5. Handles loading, empty, and error states.
6. Does **not** bypass ACL with reckless `sudo` on the server just to make the widget pretty.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Explain `ir.actions.client` vs `ir.actions.act_window` in two sentences. |
| Medium | Wire tag ↔ registry key ↔ menu for a hello-world client action. |
| Hard | Add a refresh button that reloads KPIs without leaving the action. |

**WHAT THIS UNLOCKS NEXT:**

Story XIII made the browser half of Odoo programmable. Those KPIs and lists still live in **PostgreSQL**. When the dashboard is slow at 200k loans, XML and Owl cannot save you — you need the database story: schema, plans, transactions, and ORM performance.

---


---

## 58.5 Custom Dashboards

**Custom Dashboards** belongs to **Client Actions & Advanced Frontend**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Custom Dashboards** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Custom Dashboards** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Custom Dashboards**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Custom Dashboards** is concrete, continue — the next lesson in this phase still depends on it.

## 58.6 Generic Components

**Generic Components** belongs to **Client Actions & Advanced Frontend**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Generic Components** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Generic Components** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Generic Components**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Generic Components** is concrete, continue — the next lesson in this phase still depends on it.

## 58.7 Error Handling

**Error Handling** belongs to **Client Actions & Advanced Frontend**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Error Handling** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Error Handling** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Error Handling**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Error Handling** is concrete, continue — the next lesson in this phase still depends on it.

## 58.8 Error Boundaries

**Error Boundaries** belongs to **Client Actions & Advanced Frontend**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Error Boundaries** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Error Boundaries** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Error Boundaries**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Error Boundaries** is concrete, continue — the next lesson in this phase still depends on it.

## 58.9 Odoo Editor Concepts

**Odoo Editor Concepts** belongs to **Client Actions & Advanced Frontend**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Odoo Editor Concepts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Odoo Editor Concepts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Odoo Editor Concepts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Odoo Editor Concepts** is concrete, continue — the next lesson in this phase still depends on it.

## 58.10 Mobile Web APIs

**Mobile Web APIs** belongs to **Client Actions & Advanced Frontend**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Mobile Web APIs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Mobile Web APIs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Mobile Web APIs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Mobile Web APIs** is concrete, continue — the next lesson in this phase still depends on it.

## 58.11 Responsive Odoo UI

**Responsive Odoo UI** belongs to **Client Actions & Advanced Frontend**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Responsive Odoo UI** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Responsive Odoo UI** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Responsive Odoo UI**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Responsive Odoo UI** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 58:** You can explain every lesson title in Phase 58 with a concrete Odoo or business example.

> **Phase 58 complete?** [Continue to Phase 59](#phase-59)


## STORY PROJECT — Interactive Odoo Dashboard

**Track:** OWL Frontend

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XV — FILES, ATTACHMENTS & MEDIA

**Track:** Attachments & Media

*Business documents are files — store and secure them like business data.*

---
<a id="phase-59"></a>
<a id="phase-59---attachments-filestore"></a>
# PHASE 59 - Attachments & Filestore

**Track:** Attachments & Media

Business documents are files — store and secure them like business data.

**WHAT YOU WILL BE ABLE TO DO:** Work confidently with every topic under **Attachments & Filestore** — not as a glossary, but as mechanisms you can implement and debug.

**WHAT YOU SHOULD KNOW FIRST:** Earlier stories through the phase before this one. Keep one demo company in mind.

**LEARNING RESOURCES:**

- [Odoo 18 Developer docs](https://www.odoo.com/documentation/18.0/developer.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 59.1 ir.attachment

`ir.attachment` is Odoo’s central file record: name, mimetype, `res_model` / `res_id` link to a business document, and either DB bytes or filestore index. Almost every “upload” in Odoo creates or updates an attachment row.

```text
sale.order (42)
      |
      +-- ir.attachment(res_model='sale.order', res_id=42)
               |
               +-- filestore hash path  OR  db_datas
```

**PRACTICE:** Upload a PDF on a record with chatter; find the `ir.attachment` row (developer mode).

**WHAT THIS UNLOCKS NEXT:** Binary fields vs attachment records.


## 59.2 Binary Fields

`fields.Binary` stores file payload (often via attachment under the hood depending on version/field flags). Use Binary for single-file fields on a form (ID scan). Prefer attachments + chatter for multi-document collaboration.

**PRACTICE:** Add a Binary field to a demo model; upload and download once.

**WHAT THIS UNLOCKS NEXT:** Image fields and resizing widgets.


## 59.3 Image Fields

`fields.Image` specializes Binary for pictures with optional max sizes. Product images and employee photos use this path. Large originals belong in filestore with sensible limits — do not dump 40MB photos into PostgreSQL blindly.

**PRACTICE:** Add an Image field; verify the form widget and that a thumbnail appears in kanban if configured.

**WHAT THIS UNLOCKS NEXT:** Database vs filestore trade-offs.


## 59.4 Database vs Filestore

Small binaries may live in the database; production Odoo typically uses the **filestore** directory keyed by checksum. Backups must include **both** PostgreSQL dump **and** filestore, or restores look fine until someone opens a PDF and it 404s.

**PRACTICE:** Locate `data_dir` / filestore path in your config; note its size after a few uploads.

**WHAT THIS UNLOCKS NEXT:** Linking attachments to models correctly.


## 59.5 Attachment Relationships

Set `res_model` and `res_id` (and optionally `res_field`) so access rules can follow the parent record. Orphan attachments are a security and storage mess.

**PRACTICE:** Create an attachment via ORM with `res_model`/`res_id` set; confirm it appears on the record’s attachment box.

**WHAT THIS UNLOCKS NEXT:** Upload flows from UI and code.


## 59.6 Uploads

UI uploads go through controllers and create attachments in the user transaction. From Python, create `ir.attachment` with `datas` (base64) or use helpers. Always run as the correct user — `sudo()` uploads can bypass intended access.

**PRACTICE:** Write a model method that attaches a generated text file to `self`.

**WHAT THIS UNLOCKS NEXT:** Secure downloads.


## 59.7 Downloads

Downloads must enforce the same ACLs/record rules as the parent document. A guessable `/web/content/...` URL without checks is a data leak. Prefer Odoo’s standard content routes and attachment access helpers.

**PRACTICE:** As a restricted user, try to download another company’s attachment; confirm AccessError / empty.

**WHAT THIS UNLOCKS NEXT:** Attachment access model.


## 59.8 Attachment Access

Attachment access follows related document rights when linked correctly. Public attachments (`public=True`) are intentional exceptions for website assets — treat that flag as a security decision, not a convenience.

**PRACTICE:** List attachments where `public` is true on a demo DB; justify each.

**WHAT THIS UNLOCKS NEXT:** Hardening attachment security.


## 59.9 Attachment Security

Checklist: no public flag by default; record rules on parent; virus scanning at the edge for portal uploads; size limits; mimetype allow-lists for portal; never trust client-provided mimetype alone for execution paths.

**PRACTICE:** Write a portal upload allow-list policy for your capstone (pdf/png only, max MB).

**WHAT THIS UNLOCKS NEXT:** Public vs private files in website/portal.


## 59.10 Public / Private Files

Website needs some public assets; portal needs private customer files. Mixing them is how invoices leak. Use separate controllers, access tokens when required, and tests for both happy and hostile access.

**PRACTICE:** Design a matrix: asset type × public/portal/internal × auth mechanism.

**WHAT THIS UNLOCKS NEXT:** Lab — ship a secure document feature on a business model.


## 59.11 LAB — Secure Document Attachment Feature

**LAB — Secure Document Attachment Feature**

Add attachments to a business model with: ACL, record rule, portal-safe download route or standard widget, max size validation, and a TransactionCase that proves a second user cannot read the file.

**Acceptance:** hostile user gets AccessError; owner downloads successfully; filestore backup note appears in README.

**WHAT THIS UNLOCKS NEXT:** Story XVI — PostgreSQL and performance (attachments stress storage and queries too).


**CHECK YOUR UNDERSTANDING AFTER PHASE 59:** You can explain every lesson title in Phase 59 with a concrete Odoo or business example.

> **Phase 59 complete?** [Continue to Phase 60](#phase-60)


## STORY PROJECT — Document-Enabled Business Module

**Track:** Attachments & Media

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XVI — POSTGRESQL & PERFORMANCE

**Track:** PostgreSQL & Performance

*The ORM sits on PostgreSQL. Performance starts with understanding both.*

---
<a id="phase-60"></a>
<a id="phase-60---odoo-database-structure"></a>
# PHASE 60 - Odoo Database Structure

**Track:** PostgreSQL & Performance

**WHAT YOU WILL BE ABLE TO DO:** Map Odoo models to PostgreSQL tables, recognize `ir_*` metadata tables, Many2many relation tables, and explain why hand-editing registry rows is dangerous.

**WHAT YOU SHOULD KNOW FIRST:** Stories IV–V — models, fields, ORM. Story XIII — the client reads data the server loads from PostgreSQL. Phase 4 — three-tier architecture.

**LEARNING RESOURCES:**

- [Odoo data model / backend data](https://www.odoo.com/documentation/18.0/developer/reference/backend/data.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [Architecture tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/server_framework_101/01_architecture.html)
- [odoo/odoo](https://github.com/odoo/odoo)
- [Architecture overview (YouTube)](https://www.youtube.com/watch?v=WnsYmsq4Qr8)

## 60.1 Models → Tables

Every `models.Model` with `_auto = True` (the default) gets a PostgreSQL table. By convention the table name is the model name with dots → underscores: `library.book` → `library_book`. You can override with `_table`, but fighting the convention needs a reason.

**WHAT ELSE IS IN THE DATABASE:**

```text
BUSINESS TABLES              METADATA / SYSTEM
library_book                 ir_model
library_loan                 ir_model_fields
res_partner                  ir_ui_view
                             ir_rule
                             ir_model_access
                             ir_attachment  (+ filestore files)
                             ir_sequence
```

**Many2many** fields create (or reuse) a relation table, often named like `library_book_tag_rel` with two foreign-key columns. **One2many** is not a reverse column on the parent — it is a many2one on the child that the ORM presents as a collection.

**Translations & JSONB:** modern Odoo versions store translated field values in ways that involve JSONB columns — inspecting `\d library_book` in `psql` teaches more than memorizing version trivia. Attachments metadata live in `ir_attachment`; large file bytes often live in the **filestore** on disk, not as giant bytea columns you casually SELECT.

**ir_* IS THE ZONING OFFICE:**

| Table | Role |
| ----- | ---- |
| `ir_model` | Which models exist for this database |
| `ir_model_fields` | Field definitions visible to UI / ORM |
| `ir_ui_view` | View architectures (arch_db) |
| `ir_rule` | Record rules |
| `ir_module_module` | Installed module state |

Deleting “junk” rows from `ir_ui_view` or `ir_model_fields` with raw SQL to “fix a stuck upgrade” is how databases enter the twilight zone. Prefer ORM, module uninstall, and official upgrade paths (Story XVII).

**READ-ONLY INSPECTION HABITS:**

```sql
-- Orientation only — not a substitute for ORM security
SELECT id, name, model FROM ir_model WHERE model LIKE 'library.%';
SELECT name, ttype, store FROM ir_model_fields WHERE model = 'library.book';
SELECT id, name FROM library_book ORDER BY id DESC LIMIT 10;
```

Use inspection to **learn and diagnose**. Use the ORM to **mutate business data** so constraints, mail chatter, computed fields, and access rules still run.

## 60.2 Fields → Columns

| Column pattern | Meaning |
| -------------- | ------- |
| `id` | Integer primary key (usual) |
| `create_uid` / `create_date` | Audit: who/when created |
| `write_uid` / `write_date` | Audit: who/when last wrote |
| `company_id` | Multi-company ownership when present |
| `active` | Archive pattern (`False` hides without delete) |

Relational columns for Many2one are typically `<field>_id`. Looking at `\d library_loan` should feel like reading the Python model with types lowered into SQL.

**EXTERNAL IDS LIVE IN SQL TOO:**

`ir_model_data` maps XML ids (`library_mgmt.book_demo_1`) to database ids. That is how `env.ref(...)` works. When a module uninstalls, related `ir_model_data` rows and owned records follow uninstall rules — another reason not to invent “orphan” rows with raw INSERT that no XML id owns.

**DEMO vs EMPTY DB:**

Demo data populates tables so UI tours feel alive. Performance work needs **volume**, not demo — seed scripts or copies of anonymized staging. Schema literacy without data volume still leaves you blind to seq scans.

**STORY BRIDGE:**

Story XIII’s dashboard counted loans with `orm.searchCount`. Those counts are SQL `COUNT` under the hood against tables you can now name. When counts get slow, you will know which relation to index — Phase 61 — instead of blaming Owl.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Predict the default table name for `library.loan.line`. |
| Medium | In a dev DB, `\d` your custom model and sketch columns vs fields. |
| Hard | Find the M2M relation table for a Many2many you own; explain each column. |

**WHAT THIS UNLOCKS NEXT:**

You can name tables. You still need enough PostgreSQL literacy to inspect indexes, permissions at the DB level (separate from Odoo ACL), and query plans without pretending SQL replaces the ORM.

---


---

## 60.3 Many2many Relation Tables

Every `models.Model` with `_auto = True` (the default) gets a PostgreSQL table. By convention the table name is the model name with dots → underscores: `library.book` → `library_book`. You can override with `_table`, but fighting the convention needs a reason.

**WHAT ELSE IS IN THE DATABASE:**

```text
BUSINESS TABLES              METADATA / SYSTEM
library_book                 ir_model
library_loan                 ir_model_fields
res_partner                  ir_ui_view
                             ir_rule
                             ir_model_access
                             ir_attachment  (+ filestore files)
                             ir_sequence
```

**Many2many** fields create (or reuse) a relation table, often named like `library_book_tag_rel` with two foreign-key columns. **One2many** is not a reverse column on the parent — it is a many2one on the child that the ORM presents as a collection.

**Translations & JSONB:** modern Odoo versions store translated field values in ways that involve JSONB columns — inspecting `\d library_book` in `psql` teaches more than memorizing version trivia. Attachments metadata live in `ir_attachment`; large file bytes often live in the **filestore** on disk, not as giant bytea columns you casually SELECT.

**ir_* IS THE ZONING OFFICE:**

| Table | Role |
| ----- | ---- |
| `ir_model` | Which models exist for this database |
| `ir_model_fields` | Field definitions visible to UI / ORM |
| `ir_ui_view` | View architectures (arch_db) |
| `ir_rule` | Record rules |
| `ir_module_module` | Installed module state |

Deleting “junk” rows from `ir_ui_view` or `ir_model_fields` with raw SQL to “fix a stuck upgrade” is how databases enter the twilight zone. Prefer ORM, module uninstall, and official upgrade paths (Story XVII).

**READ-ONLY INSPECTION HABITS:**

```sql
-- Orientation only — not a substitute for ORM security
SELECT id, name, model FROM ir_model WHERE model LIKE 'library.%';
SELECT name, ttype, store FROM ir_model_fields WHERE model = 'library.book';
SELECT id, name FROM library_book ORDER BY id DESC LIMIT 10;
```

Use inspection to **learn and diagnose**. Use the ORM to **mutate business data** so constraints, mail chatter, computed fields, and access rules still run.

## 60.4 Foreign Keys

**Foreign Keys** belongs to **Odoo Database Structure**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Foreign Keys** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Foreign Keys** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Foreign Keys**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Foreign Keys** is concrete, continue — the next lesson in this phase still depends on it.

## 60.5 Metadata Tables

Every `models.Model` with `_auto = True` (the default) gets a PostgreSQL table. By convention the table name is the model name with dots → underscores: `library.book` → `library_book`. You can override with `_table`, but fighting the convention needs a reason.

**WHAT ELSE IS IN THE DATABASE:**

```text
BUSINESS TABLES              METADATA / SYSTEM
library_book                 ir_model
library_loan                 ir_model_fields
res_partner                  ir_ui_view
                             ir_rule
                             ir_model_access
                             ir_attachment  (+ filestore files)
                             ir_sequence
```

**Many2many** fields create (or reuse) a relation table, often named like `library_book_tag_rel` with two foreign-key columns. **One2many** is not a reverse column on the parent — it is a many2one on the child that the ORM presents as a collection.

**Translations & JSONB:** modern Odoo versions store translated field values in ways that involve JSONB columns — inspecting `\d library_book` in `psql` teaches more than memorizing version trivia. Attachments metadata live in `ir_attachment`; large file bytes often live in the **filestore** on disk, not as giant bytea columns you casually SELECT.

**ir_* IS THE ZONING OFFICE:**

| Table | Role |
| ----- | ---- |
| `ir_model` | Which models exist for this database |
| `ir_model_fields` | Field definitions visible to UI / ORM |
| `ir_ui_view` | View architectures (arch_db) |
| `ir_rule` | Record rules |
| `ir_module_module` | Installed module state |

Deleting “junk” rows from `ir_ui_view` or `ir_model_fields` with raw SQL to “fix a stuck upgrade” is how databases enter the twilight zone. Prefer ORM, module uninstall, and official upgrade paths (Story XVII).

**READ-ONLY INSPECTION HABITS:**

```sql
-- Orientation only — not a substitute for ORM security
SELECT id, name, model FROM ir_model WHERE model LIKE 'library.%';
SELECT name, ttype, store FROM ir_model_fields WHERE model = 'library.book';
SELECT id, name FROM library_book ORDER BY id DESC LIMIT 10;
```

Use inspection to **learn and diagnose**. Use the ORM to **mutate business data** so constraints, mail chatter, computed fields, and access rules still run.

## 60.6 ir_model

**ir_model** belongs to **Odoo Database Structure**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **ir_model** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ir_model** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ir_model**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ir_model** is concrete, continue — the next lesson in this phase still depends on it.

## 60.7 ir_model_fields

**ir_model_fields** belongs to **Odoo Database Structure**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **ir_model_fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ir_model_fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ir_model_fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ir_model_fields** is concrete, continue — the next lesson in this phase still depends on it.

## 60.8 Inspecting the Schema

**Inspecting the Schema** belongs to **Odoo Database Structure**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Inspecting the Schema** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Inspecting the Schema** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Inspecting the Schema**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Inspecting the Schema** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 60:** You can explain every lesson title in Phase 60 with a concrete Odoo or business example.

> **Phase 60 complete?** [Continue to Phase 61](#phase-61)


<a id="phase-61"></a>
# PHASE 61 - PostgreSQL for Odoo Developers

**Track:** PostgreSQL & Performance

**WHAT YOU WILL BE ABLE TO DO:** Use `psql` (or an equivalent) to inspect schemas, read basic query results, and explain the boundary between DBA SQL and Odoo ORM responsibilities.

**WHAT YOU SHOULD KNOW FIRST:** Phase 58 — table mapping and `ir_*`. Comfortable running a local Odoo with PostgreSQL (Phase 5).

**LEARNING RESOURCES:**

- [PostgreSQL EXPLAIN](https://www.postgresql.org/docs/current/sql-explain.html)
- [Odoo ORM performance notes](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#performance)
- [Deploy / PostgreSQL notes](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html)
- [Architecture (YouTube)](https://www.youtube.com/watch?v=WnsYmsq4Qr8)
- [odoo/odoo](https://github.com/odoo/odoo)

## 61.1 SELECT

**SELECT** belongs to **PostgreSQL for Odoo Developers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **SELECT** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **SELECT** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **SELECT**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **SELECT** is concrete, continue — the next lesson in this phase still depends on it.

## 61.2 JOIN

**JOIN** belongs to **PostgreSQL for Odoo Developers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **JOIN** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **JOIN** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **JOIN**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **JOIN** is concrete, continue — the next lesson in this phase still depends on it.

## 61.3 GROUP BY

**GROUP BY** belongs to **PostgreSQL for Odoo Developers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **GROUP BY** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **GROUP BY** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **GROUP BY**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **GROUP BY** is concrete, continue — the next lesson in this phase still depends on it.

## 61.4 Aggregates

**Aggregates** belongs to **PostgreSQL for Odoo Developers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Aggregates** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Aggregates** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Aggregates**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Aggregates** is concrete, continue — the next lesson in this phase still depends on it.

## 61.5 CTE Concepts

**CTE Concepts** belongs to **PostgreSQL for Odoo Developers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **CTE Concepts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **CTE Concepts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **CTE Concepts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **CTE Concepts** is concrete, continue — the next lesson in this phase still depends on it.

## 61.6 Constraints

**Constraints** belongs to **PostgreSQL for Odoo Developers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Constraints** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Constraints** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Constraints**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Constraints** is concrete, continue — the next lesson in this phase still depends on it.

## 61.7 psql

**psql** belongs to **PostgreSQL for Odoo Developers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **psql** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **psql** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **psql**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **psql** is concrete, continue — the next lesson in this phase still depends on it.

## 61.8 Database Inspection

**Database Inspection** belongs to **PostgreSQL for Odoo Developers**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Database Inspection** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Database Inspection** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Database Inspection**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Database Inspection** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 61:** You can explain every lesson title in Phase 61 with a concrete Odoo or business example.

> **Phase 61 complete?** [Continue to Phase 62](#phase-62)


<a id="phase-62"></a>
# PHASE 62 - Transactions & Concurrency

**Track:** PostgreSQL & Performance

**WHAT YOU WILL BE ABLE TO DO:** Explain how an Odoo request maps to a database transaction, what flushing means, and how concurrent updates produce lock waits or inconsistent reads — without inventing DIY locking everywhere.

**WHAT YOU SHOULD KNOW FIRST:** Phase 59 — SQL literacy boundary. Phase 13/17 — environment, cursor, prefetch/flush ideas from the ORM story.

**LEARNING RESOURCES:**

- [ORM API — transaction / environment](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [PostgreSQL concurrency control](https://www.postgresql.org/docs/current/mvcc-intro.html)
- [Performance reference](https://www.odoo.com/documentation/18.0/developer/reference/backend/performance.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 62.1 Transactions

**Transactions** belongs to **Transactions & Concurrency**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Transactions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Transactions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Transactions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Transactions** is concrete, continue — the next lesson in this phase still depends on it.

## 62.2 Commit

**Commit** belongs to **Transactions & Concurrency**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Commit** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Commit** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Commit**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Commit** is concrete, continue — the next lesson in this phase still depends on it.

## 62.3 Rollback

**Rollback** belongs to **Transactions & Concurrency**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Rollback** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Rollback** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Rollback**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Rollback** is concrete, continue — the next lesson in this phase still depends on it.

## 62.4 Isolation

PostgreSQL’s default isolation (typically Read Committed) means each statement sees a snapshot of committed data. Long-running Python that reads, waits on HTTP, then writes can make decisions on stale facts. Patterns:

1. Re-read critical rows before mutating (`browse` + check state again).
2. Use SQL constraints as backstops (`unique`, check).
3. Design methods to be **idempotent** where possible (confirm twice should not double-decrement).

Cursor management helpers appear in cron and webhook code. Misused, they commit half a business operation. Prefer staying inside the request transaction until you understand the failure modes.

**LOCK WAITS YOU WILL SEE IN LOGS:**

```text
could not obtain lock on row in relation "library_loan"
canceling statement due to lock timeout
```

Someone held a row too long. Find the long transaction (slow method, external HTTP inside the critical section, forgotten shell session with an open transaction). Fix the design; do not raise lock timeouts as a “performance feature.”

**TESTS PREVIEW:**

`TransactionCase` (Phase 65) leans on this same transactional world — savepoints and rollbacks — so your concurrency literacy becomes your test literacy.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Difference between flush and commit in one sentence each. |
| Medium | Describe a double-confirm race on stock qty=1 and how a constraint helps. |
| Hard | Trace a webhook handler that commits too early; list the failure mode. |

**WHAT THIS UNLOCKS NEXT:**

Concurrency explains *when* things wait. Indexes and query plans explain *why a single user still waits* on an unindexed domain.

---


---

## 62.5 Locks

**Locks** belongs to **Transactions & Concurrency**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Locks** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Locks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Locks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Locks** is concrete, continue — the next lesson in this phase still depends on it.

## 62.6 Deadlocks

**Deadlocks** belongs to **Transactions & Concurrency**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Deadlocks** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Deadlocks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Deadlocks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Deadlocks** is concrete, continue — the next lesson in this phase still depends on it.

## 62.7 Concurrent Updates

**Concurrent Updates** belongs to **Transactions & Concurrency**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Concurrent Updates** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Concurrent Updates** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Concurrent Updates**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Concurrent Updates** is concrete, continue — the next lesson in this phase still depends on it.

## 62.8 Savepoints

**Savepoints** belongs to **Transactions & Concurrency**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Savepoints** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Savepoints** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Savepoints**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Savepoints** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 62:** You can explain every lesson title in Phase 62 with a concrete Odoo or business example.

> **Phase 62 complete?** [Continue to Phase 63](#phase-63)


<a id="phase-63"></a>
# PHASE 63 - Indexes & Query Plans

**Track:** PostgreSQL & Performance

**WHAT YOU WILL BE ABLE TO DO:** Read a basic `EXPLAIN (ANALYZE, BUFFERS)` plan, relate slow domains to missing indexes, and add `index=True` / SQL indexes deliberately — not on every column.

**WHAT YOU SHOULD KNOW FIRST:** Phases 58–60 — schema, SQL literacy, transactions. Phase 15 — domains.

**LEARNING RESOURCES:**

- [PostgreSQL EXPLAIN](https://www.postgresql.org/docs/current/sql-explain.html)
- [ORM performance](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#performance)
- [Odoo performance / profiling](https://www.odoo.com/documentation/18.0/developer/reference/backend/performance.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 63.1 Indexes

An **index** is a separate structure that lets PostgreSQL find rows without scanning the whole table. Useful for domains you run constantly (`partner_id = …`, `state = …`, email lookups). Harmful when sprayed onto low-selectivity columns you never filter — every write maintains every index.

**IN ODOO FIELDS:**

```python
partner_id = fields.Many2one("res.partner", required=True, index=True)
state = fields.Selection([...], default="draft", index=True)
isbn = fields.Char(index=True)  # if you search by ISBN constantly
```

Many2one fields often deserve indexes because joins and domains hit them constantly. Storing a computed field you filter/group on (`store=True`) is sometimes the real fix — an unstored compute cannot be indexed as a normal column.

**EXPLAIN WITHOUT FEAR:**

```sql
EXPLAIN (ANALYZE, BUFFERS)
SELECT id, name
FROM library_loan
WHERE state = 'open' AND partner_id = 42;
```

Things to notice in plans:

| Plan idea | Plain language |
| --------- | -------------- |
| Seq Scan | Read most/all of the table |
| Index Scan / Index Only Scan | Use an index to find rows |
| Bitmap Heap Scan | Combine index hits, then fetch heaps |
| Nested Loop / Hash Join | How tables combine |
| high actual time | Where seconds go |

`ANALYZE` runs the query. Use it on **copies / staging**, not carelessly on heavy production writers. `BUFFERS` shows cache vs disk feel.

**MATCH INDEXES TO DOMAINS:**

```text
Domain: [("state", "=", "open"), ("partner_id", "=", pid)]
Ask:   Is there an index that leads with partner_id or a composite that fits?
Also:  Is the action's domain applied at search, or do we search([]) and filter in Python?
```

Filtering 200k rows in Python after a wide SELECT is not an indexing problem — it is an ORM problem (Phase 62).

## 63.2 B-Tree Intuition

**B-Tree Intuition** belongs to **Indexes & Query Plans**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **B-Tree Intuition** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **B-Tree Intuition** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **B-Tree Intuition**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **B-Tree Intuition** is concrete, continue — the next lesson in this phase still depends on it.

## 63.3 Selectivity

**Selectivity** belongs to **Indexes & Query Plans**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Selectivity** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Selectivity** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Selectivity**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Selectivity** is concrete, continue — the next lesson in this phase still depends on it.

## 63.4 EXPLAIN

A simplified story of a bad plan:

```text
Seq Scan on library_loan  (cost=0.00..18500 rows=12000 width=64)
  Filter: ((state = 'open') AND (partner_id = 42))
  Rows Removed by Filter: 188000
Planning Time: 0.2 ms
Execution Time: 95 ms
```

After a useful index on `partner_id` (or a composite that fits):

```text
Index Scan using library_loan_partner_id_index on library_loan
  Index Cond: (partner_id = 42)
  Filter: (state = 'open')
Execution Time: 0.8 ms
```

**WHAT “COST” MEANS:**

PostgreSQL’s cost is a planner estimate (arbitrary units), not wall-clock truth. `ANALYZE` gives actual times. Trust **actual time** and **rows** first; use cost to understand the planner’s bet.

**PARTIAL INDEXES (ADVANCED):**

If 95% of loans are `returned` and you always query `state = 'open'`, a partial index can stay small:

```sql
CREATE INDEX library_loan_open_partner
  ON library_loan (partner_id)
  WHERE state = 'open';
```

In Odoo you more often declare `index=True` on fields and let the ORM create standard indexes. Partial indexes may appear in carefully reviewed SQL init hooks — document them; upgrades must know they exist.

**STATISTICS:**

Planner quality depends on `ANALYZE` statistics. If EXPLAIN estimates 100 rows and reality is 100000, your index choice fights bad stats. Ops depth continues in Story XVI.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | When would `index=True` on a boolean field be pointless? |
| Medium | Take one slow list view domain; propose one index and justify with EXPLAIN. |
| Hard | Compare Seq Scan vs Index Scan on the same query before/after an index on staging; paste both plans in your notes. |

**WHAT THIS UNLOCKS NEXT:**

Good indexes still lose to bad ORM patterns — N+1 loops, unbounded searches, and repeated flushes. Phase 62 attacks the Python side.

---


---

## 63.5 EXPLAIN ANALYZE

A simplified story of a bad plan:

```text
Seq Scan on library_loan  (cost=0.00..18500 rows=12000 width=64)
  Filter: ((state = 'open') AND (partner_id = 42))
  Rows Removed by Filter: 188000
Planning Time: 0.2 ms
Execution Time: 95 ms
```

After a useful index on `partner_id` (or a composite that fits):

```text
Index Scan using library_loan_partner_id_index on library_loan
  Index Cond: (partner_id = 42)
  Filter: (state = 'open')
Execution Time: 0.8 ms
```

**WHAT “COST” MEANS:**

PostgreSQL’s cost is a planner estimate (arbitrary units), not wall-clock truth. `ANALYZE` gives actual times. Trust **actual time** and **rows** first; use cost to understand the planner’s bet.

**PARTIAL INDEXES (ADVANCED):**

If 95% of loans are `returned` and you always query `state = 'open'`, a partial index can stay small:

```sql
CREATE INDEX library_loan_open_partner
  ON library_loan (partner_id)
  WHERE state = 'open';
```

In Odoo you more often declare `index=True` on fields and let the ORM create standard indexes. Partial indexes may appear in carefully reviewed SQL init hooks — document them; upgrades must know they exist.

**STATISTICS:**

Planner quality depends on `ANALYZE` statistics. If EXPLAIN estimates 100 rows and reality is 100000, your index choice fights bad stats. Ops depth continues in Story XVI.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | When would `index=True` on a boolean field be pointless? |
| Medium | Take one slow list view domain; propose one index and justify with EXPLAIN. |
| Hard | Compare Seq Scan vs Index Scan on the same query before/after an index on staging; paste both plans in your notes. |

**WHAT THIS UNLOCKS NEXT:**

Good indexes still lose to bad ORM patterns — N+1 loops, unbounded searches, and repeated flushes. Phase 62 attacks the Python side.

---


---

## 63.6 Query Plans

**Query Plans** belongs to **Indexes & Query Plans**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Query Plans** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Query Plans** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Query Plans**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Query Plans** is concrete, continue — the next lesson in this phase still depends on it.

## 63.7 Sequential Scan

**Sequential Scan** belongs to **Indexes & Query Plans**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Sequential Scan** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Sequential Scan** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Sequential Scan**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Sequential Scan** is concrete, continue — the next lesson in this phase still depends on it.

## 63.8 When to Add an Index

**When to Add an Index** belongs to **Indexes & Query Plans**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **When to Add an Index** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **When to Add an Index** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **When to Add an Index**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **When to Add an Index** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 63:** You can explain every lesson title in Phase 63 with a concrete Odoo or business example.

> **Phase 63 complete?** [Continue to Phase 64](#phase-64)


<a id="phase-64"></a>
# PHASE 64 - ORM Performance

**Track:** PostgreSQL & Performance

**WHAT YOU WILL BE ABLE TO DO:** Spot and fix classic Odoo N+1 patterns, use `mapped`/`filtered` wisely, prefetch intentionally, paginate searches, and know when `read_group` beats loading every row.

**WHAT YOU SHOULD KNOW FIRST:** Phase 61 — indexes/plans. Story V — recordsets, mapped/filtered, prefetch/cache.

**LEARNING RESOURCES:**

- [ORM performance section](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#performance)
- [Odoo performance profiling](https://www.odoo.com/documentation/18.0/developer/reference/backend/performance.html)
- [odoo/odoo](https://github.com/odoo/odoo)
- [OWL/architecture videos only as context](https://www.youtube.com/watch?v=WnsYmsq4Qr8)

## 64.1 Query Counts

**Query Counts** belongs to **ORM Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Query Counts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Query Counts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Query Counts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Query Counts** is concrete, continue — the next lesson in this phase still depends on it.

## 64.2 Batch Operations

The ORM is optimized for **recordsets**, not for Python `for` loops that each trigger SQL.

**N+1 — THE LIBRARY LOAN TRAP:**

```python
# Bad: may query partner (and more) per loan
for loan in self.search([("state", "=", "open")]):
    print(loan.partner_id.name)

# Better: prefetch partners in batch, or:
loans = self.search([("state", "=", "open")])
names = loans.mapped("partner_id.name")
```

`mapped` / browsing related fields on a recordset usually triggers batch reads. Searching inside a loop does not:

```python
# Bad
for partner in partners:
    count = self.env["library.loan"].search_count([
        ("partner_id", "=", partner.id),
        ("state", "=", "open"),
    ])

# Better direction: one search, then group in memory / read_group
Loan = self.env["library.loan"]
loans = Loan.search([
    ("partner_id", "in", partners.ids),
    ("state", "=", "open"),
])
# or read_group to aggregate in SQL
```

**UNBOUNDED SEARCH IS A PRODUCTION INCIDENT:**

```python
# Dangerous in cron / report
self.search([])  # million rows later...

self.search(domain, limit=200)  # intentional batches
```

Always ask: *What is the maximum rows this can return in three years?*

**STORED COMPUTES & SEARCHABILITY:**

If a list view filters on a value, storing the compute (with correct `@api.depends`) can move work to write-time and make indexes possible. Non-stored computes recalculate when read — fine for rare displays, painful for search/group.

**READ_GROUP FOR AGGREGATES:**

Dashboards that need counts per state should not load every loan into Owl and count in JavaScript. Prefer server `read_group` / carefully designed methods that return aggregates, still under ACL.

**RAW SQL?**

Sometimes a report uses `_auto = False` SQL views or `cr.execute` with joins. That is an advanced, reviewed decision — and it still must not become a backdoor past record rules for interactive users. Prefer ORM until measurement says otherwise (Phase 63).

## 64.3 Prefetch

**Prefetch:** When you read `loan.partner_id.name` for many loans in one recordset, Odoo tries to fetch partners in batches. Breaking the recordset into one-id searches destroys that.

**Cache:** Field values sit in cache on the recordset. After writes, flushes, or invalidations, prefer short methods with clear write → read order.

**Flush storms:** Touching many computed stored fields can schedule extensive recomputation. Profile before blaming “Postgres is slow” when Python is recomputing half the database.

**BATCH CREATE/WRITE:**

```python
Loan.create([{...}, {...}, {...}])
records.write({"state": "cancelled"})  # batch when legal
```

Batching is not a license to skip validation.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Rewrite a partner-name loop using `mapped`. |
| Medium | Find an N+1 in a sample compute; fix it. |
| Hard | Design a KPI method that returns counts via `read_group` instead of search+len. |

**WHAT THIS UNLOCKS NEXT:**

You know common fixes. Without **measurement**, you will optimize the wrong layer. Profiling makes performance empirical.

---


---

## 64.4 Cache

**Prefetch:** When you read `loan.partner_id.name` for many loans in one recordset, Odoo tries to fetch partners in batches. Breaking the recordset into one-id searches destroys that.

**Cache:** Field values sit in cache on the recordset. After writes, flushes, or invalidations, prefer short methods with clear write → read order.

**Flush storms:** Touching many computed stored fields can schedule extensive recomputation. Profile before blaming “Postgres is slow” when Python is recomputing half the database.

**BATCH CREATE/WRITE:**

```python
Loan.create([{...}, {...}, {...}])
records.write({"state": "cancelled"})  # batch when legal
```

Batching is not a license to skip validation.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Rewrite a partner-name loop using `mapped`. |
| Medium | Find an N+1 in a sample compute; fix it. |
| Hard | Design a KPI method that returns counts via `read_group` instead of search+len. |

**WHAT THIS UNLOCKS NEXT:**

You know common fixes. Without **measurement**, you will optimize the wrong layer. Profiling makes performance empirical.

---


---

## 64.5 N+1 Problems

**N+1 Problems** belongs to **ORM Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **N+1 Problems** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **N+1 Problems** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **N+1 Problems**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **N+1 Problems** is concrete, continue — the next lesson in this phase still depends on it.

## 64.6 Computed Field Costs

**Prefetch:** When you read `loan.partner_id.name` for many loans in one recordset, Odoo tries to fetch partners in batches. Breaking the recordset into one-id searches destroys that.

**Cache:** Field values sit in cache on the recordset. After writes, flushes, or invalidations, prefer short methods with clear write → read order.

**Flush storms:** Touching many computed stored fields can schedule extensive recomputation. Profile before blaming “Postgres is slow” when Python is recomputing half the database.

**BATCH CREATE/WRITE:**

```python
Loan.create([{...}, {...}, {...}])
records.write({"state": "cancelled"})  # batch when legal
```

Batching is not a license to skip validation.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Rewrite a partner-name loop using `mapped`. |
| Medium | Find an N+1 in a sample compute; fix it. |
| Hard | Design a KPI method that returns counts via `read_group` instead of search+len. |

**WHAT THIS UNLOCKS NEXT:**

You know common fixes. Without **measurement**, you will optimize the wrong layer. Profiling makes performance empirical.

---


---

## 64.7 Stored Computations

**Stored Computations** belongs to **ORM Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Stored Computations** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Stored Computations** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Stored Computations**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Stored Computations** is concrete, continue — the next lesson in this phase still depends on it.

## 64.8 Efficient Domains

**Efficient Domains** belongs to **ORM Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Efficient Domains** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Efficient Domains** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Efficient Domains**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Efficient Domains** is concrete, continue — the next lesson in this phase still depends on it.

## 64.9 Algorithms & Complexity

**Algorithms & Complexity** belongs to **ORM Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Algorithms & Complexity** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Algorithms & Complexity** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Algorithms & Complexity**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Algorithms & Complexity** is concrete, continue — the next lesson in this phase still depends on it.

## 64.10 Database Indexes from Models

**Database Indexes from Models** belongs to **ORM Performance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Database Indexes from Models** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Database Indexes from Models** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Database Indexes from Models**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Database Indexes from Models** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 64:** You can explain every lesson title in Phase 64 with a concrete Odoo or business example.

> **Phase 64 complete?** [Continue to Phase 65](#phase-65)


<a id="phase-65"></a>
# PHASE 65 - Profiling & Optimization

**Track:** PostgreSQL & Performance

**WHAT YOU WILL BE ABLE TO DO:** Enable Odoo’s profiler, read speedscope-oriented results at a practical level, combine SQL logs with EXPLAIN, and apply a measure → fix → remeasure loop.

**WHAT YOU SHOULD KNOW FIRST:** Phases 61–62 — plans and ORM patterns.

**LEARNING RESOURCES:**

- [Odoo Performance / Profiling](https://www.odoo.com/documentation/18.0/developer/reference/backend/performance.html)
- [ORM performance](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#performance)
- [PostgreSQL EXPLAIN](https://www.postgresql.org/docs/current/sql-explain.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 65.1 Odoo Profiler

**Odoo Profiler** belongs to **Profiling & Optimization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Odoo Profiler** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Odoo Profiler** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Odoo Profiler**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Odoo Profiler** is concrete, continue — the next lesson in this phase still depends on it.

## 65.2 SQL Profiling

**SQL Profiling** belongs to **Profiling & Optimization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **SQL Profiling** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **SQL Profiling** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **SQL Profiling**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **SQL Profiling** is concrete, continue — the next lesson in this phase still depends on it.

## 65.3 Query Collector

**Query Collector** belongs to **Profiling & Optimization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Query Collector** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Query Collector** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Query Collector**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Query Collector** is concrete, continue — the next lesson in this phase still depends on it.

## 65.4 Periodic Collector

**Periodic Collector** belongs to **Profiling & Optimization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Periodic Collector** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Periodic Collector** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Periodic Collector**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Periodic Collector** is concrete, continue — the next lesson in this phase still depends on it.

## 65.5 Finding Bottlenecks

**Finding Bottlenecks** belongs to **Profiling & Optimization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Finding Bottlenecks** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Finding Bottlenecks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Finding Bottlenecks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Finding Bottlenecks** is concrete, continue — the next lesson in this phase still depends on it.

## 65.6 Measuring Before Optimizing

Guessing “it must be the index” wastes days. Odoo ships an integrated **profiler** that can record SQL and stack samples into `ir.profile` and open results in speedscope-style views.

**ENABLEMENT (DEV / STAGING HABIT):**

1. Developer mode on.
2. Enable profiling globally for a limited time (UI tools / settings — follow current 18.0 docs).
3. Enable profiling on your session with SQL + traces collectors.
4. Reproduce the slow action once (warm cache considerations apply).
5. Open the profile, look for hot SQL and hot Python frames.
6. Disable profiling — it is not free.

Collectors (from docs): SQL, periodic traces, QWeb, sync (heavy). Default SQL + periodic traces are the usual starting pair.

**ALSO USE:**

| Tool | When |
| ---- | ---- |
| `--log-level=debug_sql` | See ORM SQL in logs (noisy; short sessions) |
| `EXPLAIN (ANALYZE, BUFFERS)` | Validate a specific query shape |
| Browser network tab | Separate client OWL cost from server time |
| `--workers` realities | Later in deploy story; don’t confuse prod worker limits with algo bugs |

**OPTIMIZATION LOOP:**

```text
1. Reproduce with representative data volume
2. Profile / EXPLAIN
3. Change ONE thing (index, domain, mapped, stored field, pagination)
4. Remeasure
5. Keep a before/after note for the PR
```

## 65.7 Benchmarking

**Benchmarking** belongs to **Profiling & Optimization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Benchmarking** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Benchmarking** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Benchmarking**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Benchmarking** is concrete, continue — the next lesson in this phase still depends on it.

## 65.8 Performance Regression

**Performance Regression** belongs to **Profiling & Optimization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Performance Regression** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Performance Regression** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Performance Regression**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Performance Regression** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 65:** You can explain every lesson title in Phase 65 with a concrete Odoo or business example.

> **Phase 65 complete?** [Continue to Phase 66](#phase-66)


## STORY PROJECT — Optimize a Slow Odoo Application

**Track:** PostgreSQL & Performance

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XVII — TESTING & DEBUGGING

**Track:** Testing & Debugging

*If you cannot prove it, you do not ship it.*

---
<a id="phase-66"></a>
<a id="phase-66---odoo-logging-debugging"></a>
# PHASE 66 - Odoo Logging & Debugging

**Track:** Testing & Debugging

**WHAT YOU WILL BE ABLE TO DO:** Configure meaningful log levels, place strategic logs, use debugger breakpoints on Python paths, and debug AccessError / ValidationError without cargo-cult printing.

**WHAT YOU SHOULD KNOW FIRST:** Stories IV–VIII — ORM and security. Story XIV — how to read SQL when performance is the bug.

**LEARNING RESOURCES:**

- [CLI / logging notes in Odoo docs](https://www.odoo.com/documentation/18.0/developer/reference/cli.html)
- [Testing reference](https://www.odoo.com/documentation/18.0/developer/reference/backend/testing.html)
- [Security reference](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [odoo/odoo](https://github.com/odoo/odoo)
- [Architecture (YouTube)](https://www.youtube.com/watch?v=WnsYmsq4Qr8)

## 66.1 Log Levels

**Log Levels** belongs to **Odoo Logging & Debugging**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Log Levels** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Log Levels** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Log Levels**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Log Levels** is concrete, continue — the next lesson in this phase still depends on it.

## 66.2 Server Logs

**Server Logs** belongs to **Odoo Logging & Debugging**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Server Logs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Server Logs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Server Logs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Server Logs** is concrete, continue — the next lesson in this phase still depends on it.

## 66.3 Stack Traces

**Stack Traces** belongs to **Odoo Logging & Debugging**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Stack Traces** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Stack Traces** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Stack Traces**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Stack Traces** is concrete, continue — the next lesson in this phase still depends on it.

## 66.4 Python Debugger

**Python Debugger** belongs to **Odoo Logging & Debugging**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Python Debugger** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Python Debugger** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Python Debugger**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Python Debugger** is concrete, continue — the next lesson in this phase still depends on it.

## 66.5 Breakpoints

**Breakpoints** belongs to **Odoo Logging & Debugging**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Breakpoints** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Breakpoints** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Breakpoints**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Breakpoints** is concrete, continue — the next lesson in this phase still depends on it.

## 66.6 Odoo Shell

**Odoo Shell** belongs to **Odoo Logging & Debugging**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Odoo Shell** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Odoo Shell** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Odoo Shell**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Odoo Shell** is concrete, continue — the next lesson in this phase still depends on it.

## 66.7 Developer Tools

**Developer Tools** belongs to **Odoo Logging & Debugging**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Developer Tools** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Developer Tools** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Developer Tools**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Developer Tools** is concrete, continue — the next lesson in this phase still depends on it.

## 66.8 Reproducing Bugs

**Reproducing Bugs** belongs to **Odoo Logging & Debugging**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Reproducing Bugs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Reproducing Bugs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Reproducing Bugs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Reproducing Bugs** is concrete, continue — the next lesson in this phase still depends on it.

## 66.9 Root Cause Analysis

**Root Cause Analysis** belongs to **Odoo Logging & Debugging**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Root Cause Analysis** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Root Cause Analysis** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Root Cause Analysis**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Root Cause Analysis** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 66:** You can explain every lesson title in Phase 66 with a concrete Odoo or business example.

> **Phase 66 complete?** [Continue to Phase 67](#phase-67)


<a id="phase-67"></a>
# PHASE 67 - Python Tests

**Track:** Testing & Debugging

**WHAT YOU WILL BE ABLE TO DO:** Structure a `tests/` package, write `TransactionCase` tests with assertions, understand savepoint isolation between methods, and tag tests for install vs post-install runs.

**WHAT YOU SHOULD KNOW FIRST:** Phase 64 — logging/debug. Story VI — constraints and workflows you can assert.

**LEARNING RESOURCES:**

- [Testing Odoo](https://www.odoo.com/documentation/18.0/developer/reference/backend/testing.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [odoo/odoo tests helpers](https://github.com/odoo/odoo/tree/18.0/odoo/tests)
- [ged-odoo training](https://github.com/ged-odoo/odoo-js-training-public) (JS side later)

## 67.1 Test Module Structure

**Test Module Structure** belongs to **Python Tests**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Test Module Structure** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Test Module Structure** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Test Module Structure**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Test Module Structure** is concrete, continue — the next lesson in this phase still depends on it.

## 67.2 TransactionCase

Odoo’s Python tests build on `unittest`. Standard layout:

```text
library_mgmt/
  tests/
    __init__.py          # imports test modules
    test_loan_workflow.py
```

```python
# tests/__init__.py
from . import test_loan_workflow
```

**TransactionCase (modern mental model):**

Each test method runs inside a transaction mechanism that **rolls back** so tests do not permanently dirty the database. Internally, Odoo uses **savepoints** so methods stay isolated while sharing class-level setup efficiently. Older docs talked about `SavepointCase`; today you should be fluent in **`TransactionCase`** and understand that **savepoint isolation** is the concept keeping tests independent.

```python
from odoo.tests import TransactionCase, tagged
from odoo.exceptions import ValidationError, UserError

@tagged("post_install", "-at_install")
class TestLoanWorkflow(TransactionCase):

    @classmethod
    def setUpClass(cls):
        super().setUpClass()
        cls.partner = cls.env["res.partner"].create({"name": "Test Reader"})
        cls.book = cls.env["library.book"].create({"name": "Test Book"})

    def test_confirm_sets_open(self):
        loan = self.env["library.loan"].create({
            "partner_id": self.partner.id,
            "book_id": self.book.id,
        })
        loan.action_confirm()
        self.assertEqual(loan.state, "open")

    def test_confirm_requires_partner(self):
        loan = self.env["library.loan"].new({"book_id": self.book.id})
        with self.assertRaises(ValidationError):
            loan._check_partner()  # or whatever constraint path you expose
```

**RULES THAT KEEP CI GREEN:**

| Rule | Why |
| ---- | --- |
| Methods start with `test_` | Otherwise they never run |
| Import test modules from `tests/__init__.py` | Otherwise they never collect |
| Prefer `setUpClass` for shared demo rows | Faster than recreating everything per method when safe |
| Assert outcomes, not implementation trivia | Tests survive refactors |
| Use `Form` helper when onchanges matter | Closer to UI behavior |

**TAGS:**

Default tags often include `standard` and `at_install`. Use `@tagged('-at_install', 'post_install')` when the test needs a fuller registry (common for anything touching extra modules).

**SingleTransactionCase** exists for special suites that intentionally share one transaction across methods — know it exists; do not reach for it first.

## 67.3 Savepoint / Transaction Concepts

**Savepoint / Transaction Concepts** belongs to **Python Tests**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Savepoint / Transaction Concepts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Savepoint / Transaction Concepts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Savepoint / Transaction Concepts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Savepoint / Transaction Concepts** is concrete, continue — the next lesson in this phase still depends on it.

## 67.4 Test Data

**Test Data** belongs to **Python Tests**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Test Data** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Test Data** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Test Data**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Test Data** is concrete, continue — the next lesson in this phase still depends on it.

## 67.5 setUp()

**setUp()** belongs to **Python Tests**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **setUp()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **setUp()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **setUp()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **setUp()** is concrete, continue — the next lesson in this phase still depends on it.

## 67.6 Assertions

Odoo’s Python tests build on `unittest`. Standard layout:

```text
library_mgmt/
  tests/
    __init__.py          # imports test modules
    test_loan_workflow.py
```

```python
# tests/__init__.py
from . import test_loan_workflow
```

**TransactionCase (modern mental model):**

Each test method runs inside a transaction mechanism that **rolls back** so tests do not permanently dirty the database. Internally, Odoo uses **savepoints** so methods stay isolated while sharing class-level setup efficiently. Older docs talked about `SavepointCase`; today you should be fluent in **`TransactionCase`** and understand that **savepoint isolation** is the concept keeping tests independent.

```python
from odoo.tests import TransactionCase, tagged
from odoo.exceptions import ValidationError, UserError

@tagged("post_install", "-at_install")
class TestLoanWorkflow(TransactionCase):

    @classmethod
    def setUpClass(cls):
        super().setUpClass()
        cls.partner = cls.env["res.partner"].create({"name": "Test Reader"})
        cls.book = cls.env["library.book"].create({"name": "Test Book"})

    def test_confirm_sets_open(self):
        loan = self.env["library.loan"].create({
            "partner_id": self.partner.id,
            "book_id": self.book.id,
        })
        loan.action_confirm()
        self.assertEqual(loan.state, "open")

    def test_confirm_requires_partner(self):
        loan = self.env["library.loan"].new({"book_id": self.book.id})
        with self.assertRaises(ValidationError):
            loan._check_partner()  # or whatever constraint path you expose
```

**RULES THAT KEEP CI GREEN:**

| Rule | Why |
| ---- | --- |
| Methods start with `test_` | Otherwise they never run |
| Import test modules from `tests/__init__.py` | Otherwise they never collect |
| Prefer `setUpClass` for shared demo rows | Faster than recreating everything per method when safe |
| Assert outcomes, not implementation trivia | Tests survive refactors |
| Use `Form` helper when onchanges matter | Closer to UI behavior |

**TAGS:**

Default tags often include `standard` and `at_install`. Use `@tagged('-at_install', 'post_install')` when the test needs a fuller registry (common for anything touching extra modules).

**SingleTransactionCase** exists for special suites that intentionally share one transaction across methods — know it exists; do not reach for it first.

## 67.7 Test Tags

**Test Tags** belongs to **Python Tests**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Test Tags** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Test Tags** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Test Tags**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Test Tags** is concrete, continue — the next lesson in this phase still depends on it.

## 67.8 Running Selected Tests

**Running Selected Tests** belongs to **Python Tests**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Running Selected Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Running Selected Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Running Selected Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Running Selected Tests** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 67:** You can explain every lesson title in Phase 67 with a concrete Odoo or business example.

> **Phase 67 complete?** [Continue to Phase 68](#phase-68)


<a id="phase-68"></a>
# PHASE 68 - Testing Business Logic

**Track:** Testing & Debugging

**WHAT YOU WILL BE ABLE TO DO:** Design tests for state transitions, constraints, computes, and wizards so business invariants stay enforced under refactor.

**WHAT YOU SHOULD KNOW FIRST:** Phase 65 — TransactionCase. Story VI — methods, constraints, states.

**LEARNING RESOURCES:**

- [Testing Odoo — Form helper](https://www.odoo.com/documentation/18.0/developer/reference/backend/testing.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 68.1 CRUD Tests

**CRUD Tests** belongs to **Testing Business Logic**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **CRUD Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **CRUD Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **CRUD Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **CRUD Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 68.2 Computed Field Tests

**Computed Field Tests** belongs to **Testing Business Logic**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Computed Field Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Computed Field Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Computed Field Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Computed Field Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 68.3 Constraint Tests

**Constraint Tests** belongs to **Testing Business Logic**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Constraint Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Constraint Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Constraint Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Constraint Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 68.4 Workflow Tests

**Workflow Tests** belongs to **Testing Business Logic**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Workflow Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Workflow Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Workflow Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Workflow Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 68.5 Cron Tests

**Cron Tests** belongs to **Testing Business Logic**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Cron Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Cron Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Cron Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Cron Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 68.6 Edge Cases

**Edge Cases** belongs to **Testing Business Logic**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Edge Cases** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Edge Cases** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Edge Cases**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Edge Cases** is concrete, continue — the next lesson in this phase still depends on it.

## 68.7 Regression Tests

**Regression Tests** belongs to **Testing Business Logic**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Regression Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Regression Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Regression Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Regression Tests** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 68:** You can explain every lesson title in Phase 68 with a concrete Odoo or business example.

> **Phase 68 complete?** [Continue to Phase 69](#phase-69)


<a id="phase-69"></a>
# PHASE 69 - Testing Security

**Track:** Testing & Debugging

**WHAT YOU WILL BE ABLE TO DO:** Write tests that prove groups can or cannot CRUD models, that record rules isolate rows, and that `AccessError` appears when it should — including multi-company gotchas.

**WHAT YOU SHOULD KNOW FIRST:** Phase 66 — business tests. Story VIII — users, ACL, record rules, sudo discipline.

**LEARNING RESOURCES:**

- [Testing Odoo](https://www.odoo.com/documentation/18.0/developer/reference/backend/testing.html)
- [Security in Odoo](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [Restrict access tutorial](https://www.odoo.com/documentation/18.0/developer/tutorials/restrict_data_access.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 69.1 User Context

**User Context** belongs to **Testing Security**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **User Context** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **User Context** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **User Context**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **User Context** is concrete, continue — the next lesson in this phase still depends on it.

## 69.2 ACL Tests

**ACL Tests** belongs to **Testing Security**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **ACL Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ACL Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ACL Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ACL Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 69.3 Record Rule Tests

**Record Rule Tests** belongs to **Testing Security**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Record Rule Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Record Rule Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Record Rule Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Record Rule Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 69.4 sudo() Tests

`sudo()` rebuilds the environment as superuser: ACLs and record rules stop applying. Use it for system rows (mail, cron, computed stored fields that must write privileged data) — never as a shortcut in a portal controller.

Prefer `with_user(integration_user)` when you still want *some* rules. Every `sudo()` in a PR needs a one-line justification.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **sudo() Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **sudo() Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **sudo() Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 69.5 Multi-Company Tests

**Multi-Company Tests** belongs to **Testing Security**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Multi-Company Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Multi-Company Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Multi-Company Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Multi-Company Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 69.6 Portal Access Tests

**Portal Access Tests** belongs to **Testing Security**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Portal Access Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Portal Access Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Portal Access Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Portal Access Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 69.7 Permission Regression

**Permission Regression** belongs to **Testing Security**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Permission Regression** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Permission Regression** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Permission Regression**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Permission Regression** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 69:** You can explain every lesson title in Phase 69 with a concrete Odoo or business example.

> **Phase 69 complete?** [Continue to Phase 70](#phase-70)


<a id="phase-70"></a>
# PHASE 70 - Web & Frontend Testing

**Track:** Testing & Debugging

**WHAT YOU WILL BE ABLE TO DO:** Use `HttpCase` / tour-style and controller tests at a practical level, know when JS unit tests vs tours vs Python tests fit, and protect critical web/portal paths.

**WHAT YOU SHOULD KNOW FIRST:** Phase 67 — security tests. Stories XI–XIII — controllers, portal, OWL client actions.

**LEARNING RESOURCES:**

- [Testing Odoo — HttpCase & tours](https://www.odoo.com/documentation/18.0/developer/reference/backend/testing.html)
- [JS framework / tours in Odoo docs](https://www.odoo.com/documentation/18.0/developer/reference/frontend.html)
- [HTTP controllers](https://www.odoo.com/documentation/18.0/developer/reference/backend/http.html)
- [odoo/odoo](https://github.com/odoo/odoo)
- [ged-odoo/odoo-js-training-public](https://github.com/ged-odoo/odoo-js-training-public)

## 70.1 HTTP Tests

**HTTP Tests** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **HTTP Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **HTTP Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **HTTP Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **HTTP Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 70.2 Controller Tests

**Controller Tests** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Controller Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Controller Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Controller Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Controller Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 70.3 browser_js

**browser_js** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **browser_js** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **browser_js** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **browser_js**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **browser_js** is concrete, continue — the next lesson in this phase still depends on it.

## 70.4 Test Tours

A tour is a scripted click-path. Keep tours for:

- install → open client action → see KPI (Story XIII dashboard)
- portal user sees only own documents
- payment or webhook acknowledgment screens

Do **not** tour every CRUD. TransactionCase already owns CRUD. Tours own “Python and JS still speak to each other after a refactor.”

When a tour flakes, check asset readiness, demo data assumptions, and selectors bound to translated strings. Prefer stable CSS classes you own (`o_library_dashboard`) over brittle text matches.

**STORY PROJECT — FULLY TESTED MODULE:**

Ship (or extend) a module with:

1. TransactionCase workflow tests (happy + failure).
2. At least two security tests (`AccessError` / record rule).
3. One HttpCase or controller test for a critical path.
4. README section: how to run tests (`odoo-bin -i/-u … --test-enable / tagged`).
5. No reliance on raw SQL writes to “set up state” that bypasses constraints — use ORM setup helpers.

That closes Story XV: you can debug, prove business logic, prove security, and smoke-test web flows. Combined with Stories XIII–XIV, you are ready to put the system into production-shaped environments — workers, proxies, backups — without treating “it works on my laptop” as a deployment strategy.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Decide for three features whether unit, JS, or tour fits best. |
| Medium | Write an HttpCase that authenticates and hits `/web` successfully. |
| Hard | Add a tour or `browser_js` check that your client action renders a KPI. |

**WHAT THIS UNLOCKS NEXT:**

Stories XIII–XV made you full-stack inside one Odoo database: Owl UX, PostgreSQL performance, and automated proof. Running that system for real users — workers, HTTPS, backups, monitoring — is a different job. Story XVI starts **deployment and operations**.

---


## 70.5 Integration Tours

A tour is a scripted click-path. Keep tours for:

- install → open client action → see KPI (Story XIII dashboard)
- portal user sees only own documents
- payment or webhook acknowledgment screens

Do **not** tour every CRUD. TransactionCase already owns CRUD. Tours own “Python and JS still speak to each other after a refactor.”

When a tour flakes, check asset readiness, demo data assumptions, and selectors bound to translated strings. Prefer stable CSS classes you own (`o_library_dashboard`) over brittle text matches.

**STORY PROJECT — FULLY TESTED MODULE:**

Ship (or extend) a module with:

1. TransactionCase workflow tests (happy + failure).
2. At least two security tests (`AccessError` / record rule).
3. One HttpCase or controller test for a critical path.
4. README section: how to run tests (`odoo-bin -i/-u … --test-enable / tagged`).
5. No reliance on raw SQL writes to “set up state” that bypasses constraints — use ORM setup helpers.

That closes Story XV: you can debug, prove business logic, prove security, and smoke-test web flows. Combined with Stories XIII–XIV, you are ready to put the system into production-shaped environments — workers, proxies, backups — without treating “it works on my laptop” as a deployment strategy.

**PRACTICE:**

| Difficulty | Task |
| ---------- | ---- |
| Easy | Decide for three features whether unit, JS, or tour fits best. |
| Medium | Write an HttpCase that authenticates and hits `/web` successfully. |
| Hard | Add a tour or `browser_js` check that your client action renders a KPI. |

**WHAT THIS UNLOCKS NEXT:**

Stories XIII–XV made you full-stack inside one Odoo database: Owl UX, PostgreSQL performance, and automated proof. Running that system for real users — workers, HTTPS, backups, monitoring — is a different job. Story XVI starts **deployment and operations**.

---


## 70.6 JavaScript Unit Tests

**JavaScript Unit Tests** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **JavaScript Unit Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **JavaScript Unit Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **JavaScript Unit Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **JavaScript Unit Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 70.7 HOOT

**HOOT** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **HOOT** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **HOOT** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **HOOT**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **HOOT** is concrete, continue — the next lesson in this phase still depends on it.

## 70.8 @odoo/hoot-dom

**@odoo/hoot-dom** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **@odoo/hoot-dom** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **@odoo/hoot-dom** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **@odoo/hoot-dom**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **@odoo/hoot-dom** is concrete, continue — the next lesson in this phase still depends on it.

## 70.9 Web Test Helpers

**Web Test Helpers** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Web Test Helpers** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Web Test Helpers** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Web Test Helpers**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Web Test Helpers** is concrete, continue — the next lesson in this phase still depends on it.

## 70.10 Mock Server

**Mock Server** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Mock Server** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Mock Server** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Mock Server**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Mock Server** is concrete, continue — the next lesson in this phase still depends on it.

## 70.11 Mocking RPC

**Mocking RPC** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Mocking RPC** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Mocking RPC** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Mocking RPC**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Mocking RPC** is concrete, continue — the next lesson in this phase still depends on it.

## 70.12 End-to-End Flows

**End-to-End Flows** belongs to **Web & Frontend Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **End-to-End Flows** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **End-to-End Flows** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **End-to-End Flows**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **End-to-End Flows** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 70:** You can explain every lesson title in Phase 70 with a concrete Odoo or business example.

> **Phase 70 complete?** [Continue to Phase 71](#phase-71)


<a id="phase-71"></a>
# PHASE 71 - Performance & Regression Testing

**Track:** Testing & Debugging

If you cannot prove it, you do not ship it.

**WHAT YOU WILL BE ABLE TO DO:** Work confidently with every topic under **Performance & Regression Testing** — not as a glossary, but as mechanisms you can implement and debug.

**WHAT YOU SHOULD KNOW FIRST:** Earlier stories through the phase before this one. Keep one demo company in mind.

**LEARNING RESOURCES:**

- [Odoo 18 Developer docs](https://www.odoo.com/documentation/18.0/developer.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 71.1 Query Count Tests

**Query Count Tests** belongs to **Performance & Regression Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Query Count Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Query Count Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Query Count Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Query Count Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 71.2 assertQueryCount()

**assertQueryCount()** belongs to **Performance & Regression Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **assertQueryCount()** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **assertQueryCount()** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **assertQueryCount()**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **assertQueryCount()** is concrete, continue — the next lesson in this phase still depends on it.

## 71.3 --log-sql

**--log-sql** belongs to **Performance & Regression Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **--log-sql** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **--log-sql** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **--log-sql**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **--log-sql** is concrete, continue — the next lesson in this phase still depends on it.

## 71.4 Performance Budgets

**Performance Budgets** belongs to **Performance & Regression Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Performance Budgets** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Performance Budgets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Performance Budgets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Performance Budgets** is concrete, continue — the next lesson in this phase still depends on it.

## 71.5 Regression Detection

**Regression Detection** belongs to **Performance & Regression Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Regression Detection** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Regression Detection** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Regression Detection**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Regression Detection** is concrete, continue — the next lesson in this phase still depends on it.

## 71.6 Repeatable Benchmarks

**Repeatable Benchmarks** belongs to **Performance & Regression Testing**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Repeatable Benchmarks** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Repeatable Benchmarks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Repeatable Benchmarks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Repeatable Benchmarks** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 71:** You can explain every lesson title in Phase 71 with a concrete Odoo or business example.

> **Phase 71 complete?** [Continue to Phase 72](#phase-72)


## STORY PROJECT — Fully Tested Odoo Module

**Track:** Testing & Debugging

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XVIII — CLI & DEVELOPER TOOLING

**Track:** CLI & Tooling

*Your terminal is part of the Odoo toolbox.*

---
<a id="phase-72"></a>
<a id="phase-72---odoo-cli-developer-commands"></a>
# PHASE 72 - Odoo CLI & Developer Commands

**Track:** CLI & Tooling

Your terminal is part of the Odoo toolbox.

**WHAT YOU WILL BE ABLE TO DO:** Work confidently with every topic under **Odoo CLI & Developer Commands** — not as a glossary, but as mechanisms you can implement and debug.

**WHAT YOU SHOULD KNOW FIRST:** Earlier stories through the phase before this one. Keep one demo company in mind.

**LEARNING RESOURCES:**

- [Odoo 18 Developer docs](https://www.odoo.com/documentation/18.0/developer.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 72.1 odoo-bin / odoo

`odoo-bin` (or the `odoo` console script) is the process entrypoint: it loads config, connects to PostgreSQL, builds the registry, and serves HTTP. Everything else — install, upgrade, tests, shell — is a flag combination on this binary.

**PRACTICE:** Run `odoo-bin --help` and note install/upgrade/test/shell flags.

**WHAT THIS UNLOCKS NEXT:** Configuration file vs CLI flags.


## 72.2 Configuration File

`-c odoo.conf` centralizes `addons_path`, db credentials, and logging. Flags override file values for one-off commands — useful in CI, dangerous if you forget which won.

**PRACTICE:** Diff a long CLI invocation vs the same settings in `odoo.conf`; pick one source of truth for daily work.

**WHAT THIS UNLOCKS NEXT:** Selecting the database.


## 72.3 Database Selection

`-d DATABASE` selects the DB. Wrong database + `-u` is a classic disaster. Use distinct DB names per project (`odoo_lib_dev`, `odoo_lib_test`).

**PRACTICE:** List databases with `psql -l` and map each to a purpose.

**WHAT THIS UNLOCKS NEXT:** `-i` install.


## 72.4 -i Install

`-i module1,module2` installs modules then continues (unless `--stop-after-init`). Dependencies install first via manifest `depends`.

**PRACTICE:** Install your custom module on a fresh DB with `-i` and `--stop-after-init`.

**WHAT THIS UNLOCKS NEXT:** `-u` upgrade.


## 72.5 -u Upgrade

`-u module` upgrades: reloads Python, applies XML (respecting noupdate), runs migrations. After model/view changes, upgrade is mandatory — restart alone is not enough for data files.

**PRACTICE:** Change a view arch; upgrade with `-u`; confirm without upgrade the old arch remains.

**WHAT THIS UNLOCKS NEXT:** `--addons-path`.


## 72.6 --addons-path

`--addons-path` is a comma-separated list of addon roots (core `addons`, enterprise if any, your `extra-addons`). Order matters for duplicates. Misconfigured paths mean “module not found” ghosts.

**PRACTICE:** Print the effective addons path from your conf; ensure your custom folder is listed.

**WHAT THIS UNLOCKS NEXT:** `--dev` helpers.


## 72.7 --dev

`--dev=all` (or subsets like `xml,reload`) speeds UI/XML iteration. Never use full dev mode as a production substitute — workers, caching, and security assumptions differ.

**PRACTICE:** Edit a form view with `--dev=xml` and note reload behavior.

**WHAT THIS UNLOCKS NEXT:** `--log-level`.


## 72.8 --log-level

`--log-level=debug` (or logger-specific levels) exposes ORM and HTTP detail. Pair with targeted loggers when `debug` is too noisy.

**PRACTICE:** Reproduce a bug once at `info` and once at `debug`; note the extra lines that matter.

**WHAT THIS UNLOCKS NEXT:** `--log-sql`.


## 72.9 --log-sql

`--log-sql` prints SQL. Use it to catch N+1 patterns during a single UI action. Turn it off after — logs explode.

**PRACTICE:** Open a list view with `--log-sql`; count SELECTs before/after a prefetch fix.

**WHAT THIS UNLOCKS NEXT:** `--test-enable`.


## 72.10 --test-enable

`--test-enable` runs module tests on install/upgrade paths used in CI. Combine with `--stop-after-init` for headless pipelines.

**PRACTICE:** Run your module tests via CLI once; save the command in README.

**WHAT THIS UNLOCKS NEXT:** `--test-tags`.


## 72.11 --test-tags

`--test-tags` filters tests (standard/at_install/post_install and custom tags). Fast feedback beats full suite while iterating one feature.

**PRACTICE:** Tag one test `/my_module` and run only that tag.

**WHAT THIS UNLOCKS NEXT:** `--stop-after-init`.


## 72.12 --stop-after-init

`--stop-after-init` exits after registry init / install / tests — essential for scripts and CI so the HTTP server does not hang the job.

**PRACTICE:** Add `--stop-after-init` to your install command and confirm the process exits 0.

**WHAT THIS UNLOCKS NEXT:** `shell`.


## 72.13 shell

`odoo-bin shell -d DB` opens a Python shell with `env` bound — ideal for inspecting records, domains, and one-off repairs (still respect security; prefer explicit `with_user`).

**PRACTICE:** In shell, `env['res.partner'].search([], limit=5)` and print names.

**WHAT THIS UNLOCKS NEXT:** `scaffold`.


## 72.14 scaffold

`odoo-bin scaffold my_module /path/to/extra-addons` creates a skeleton addon. Treat it as a starting point — still fill security ACLs before install on shared DBs.

**PRACTICE:** Scaffold a throwaway module; compare its layout to Phase 7 anatomy.

**WHAT THIS UNLOCKS NEXT:** Lab — full CLI workflow.


## 72.15 LAB — Complete CLI Development Workflow

**LAB — Complete CLI Development Workflow**

Document and run: create DB → scaffold/install module → develop → `-u` → `--test-enable --stop-after-init` → shell smoke check. Paste the exact commands into your module README.

**Acceptance:** a teammate can reproduce install/test using only the README.

**WHAT THIS UNLOCKS NEXT:** Story XIX — production deployment (CLI habits become ops habits).


**CHECK YOUR UNDERSTANDING AFTER PHASE 72:** You can explain every lesson title in Phase 72 with a concrete Odoo or business example.

> **Phase 72 complete?** [Continue to Phase 73](#phase-73)


## STORY PROJECT — Command-Line Odoo Workflow

**Track:** CLI & Tooling

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XIX — DEPLOYMENT & OPERATIONS

**Track:** Deployment

*A module that cannot be deployed and restored is not finished.*

---
<a id="phase-73"></a>
<a id="phase-73---odoo-configuration"></a>
# PHASE 73 - Odoo Configuration

**Track:** Deployment

**WHAT YOU WILL BE ABLE TO DO:** Author a production-minded `odoo.conf` (or equivalent CLI flags): database connection, `addons_path`, `admin_passwd`, `dbfilter`, logging, data/filestore paths, and the difference between “works on my machine” and “safe on the internet.”

**WHAT YOU SHOULD KNOW FIRST:** Phase 5 — local config and `addons_path`. Phase 8 — install/upgrade lifecycle. Phase 68 — you can test a module before you trust it in production.

**LEARNING RESOURCES:**

- [Source install (Odoo 18)](https://www.odoo.com/documentation/18.0/administration/on_premise/source.html)
- [System configuration / deploy](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html)
- [CLI reference](https://www.odoo.com/documentation/18.0/developer/reference/cli.html)
- [odoo/odoo](https://github.com/odoo/odoo) — read `odoo/tools/config.py` for option names

## 73.1 odoo.conf

**odoo.conf** belongs to **Odoo Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **odoo.conf** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **odoo.conf** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **odoo.conf**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **odoo.conf** is concrete, continue — the next lesson in this phase still depends on it.

## 73.2 addons_path

**addons_path** belongs to **Odoo Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **addons_path** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **addons_path** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **addons_path**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **addons_path** is concrete, continue — the next lesson in this phase still depends on it.

## 73.3 db_host

Odoo is multi-tenant: one server process can serve many PostgreSQL databases. Logged-in backend users pick a DB at login. **Anonymous** website/portal traffic has no login yet — Odoo must decide which DB owns `https://shop.example.com/`.

That decision is `dbfilter`: a regex, optionally with `%h` (hostname) or `%d` (first subdomain). Official guidance: for production multi-DB with website, set `dbfilter` or features break. Once one hostname maps to one DB, lock down listing.

```text
Request Host: shop.acme.com
dbfilter = ^%d$
        |
        v
Candidate DB name: "shop"
        |
        v
Load website + portal for that DB only
```

**Single-company production tip:** set `dbfilter` to an exact name (`^mycompany$`) even if you only have one DB. Ambiguity is how accidental cross-DB mistakes start.

## 73.4 db_port

**db_port** belongs to **Odoo Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **db_port** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **db_port** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **db_port**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **db_port** is concrete, continue — the next lesson in this phase still depends on it.

## 73.5 db_user

**db_user** belongs to **Odoo Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **db_user** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **db_user** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **db_user**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **db_user** is concrete, continue — the next lesson in this phase still depends on it.

## 73.6 Database Filters

**Database Filters** belongs to **Odoo Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Database Filters** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Database Filters** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Database Filters**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Database Filters** is concrete, continue — the next lesson in this phase still depends on it.

## 73.7 Logging

**Logging** belongs to **Odoo Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Logging** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Logging** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Logging**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Logging** is concrete, continue — the next lesson in this phase still depends on it.

## 73.8 Proxy Mode

**Proxy Mode** belongs to **Odoo Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Proxy Mode** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Proxy Mode** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Proxy Mode**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Proxy Mode** is concrete, continue — the next lesson in this phase still depends on it.

## 73.9 Secrets

Treat **dev**, **staging**, and **prod** as different configs that share module code:

- Different `db_name` / `dbfilter`
- Different `admin_passwd` and DB passwords
- Staging may allow `list_db = True`; prod should not
- Never commit real secrets into Git — use env-specific files or secret managers

`addons_path` should point at **version-pinned** code (Git tags/branches), not “whatever was last on the laptop.”

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write an `odoo.conf` for a single DB named `training` with custom addons under `/opt/odoo/custom`, logging to a file. |
| Medium | Explain in writing the difference between `admin_passwd` and the Odoo user `admin`. |
| Hard | Design a three-file layout (`odoo.dev.conf`, `odoo.staging.conf`, `odoo.prod.conf`) listing which keys differ and why. |

**WHAT THIS UNLOCKS NEXT:** Config names the paths and secrets — but someone still has to install Odoo on a real Linux host, run it as a service user, and keep it alive across reboots. That is Linux deployment.

---

<a id="phase-70"></a>

## 73.10 Environment-Specific Configuration

Treat **dev**, **staging**, and **prod** as different configs that share module code:

- Different `db_name` / `dbfilter`
- Different `admin_passwd` and DB passwords
- Staging may allow `list_db = True`; prod should not
- Never commit real secrets into Git — use env-specific files or secret managers

`addons_path` should point at **version-pinned** code (Git tags/branches), not “whatever was last on the laptop.”

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write an `odoo.conf` for a single DB named `training` with custom addons under `/opt/odoo/custom`, logging to a file. |
| Medium | Explain in writing the difference between `admin_passwd` and the Odoo user `admin`. |
| Hard | Design a three-file layout (`odoo.dev.conf`, `odoo.staging.conf`, `odoo.prod.conf`) listing which keys differ and why. |

**WHAT THIS UNLOCKS NEXT:** Config names the paths and secrets — but someone still has to install Odoo on a real Linux host, run it as a service user, and keep it alive across reboots. That is Linux deployment.

---

<a id="phase-70"></a>


<a id="phase-74"></a>
<a id="phase-74---linux-deployment"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 73:** You can explain every lesson title in Phase 73 with a concrete Odoo or business example.

> **Phase 73 complete?** [Continue to Phase 74](#phase-74)


<a id="phase-74"></a>
# PHASE 74 - Linux Deployment

**Track:** Deployment

**WHAT YOU WILL BE ABLE TO DO:** Describe a sane on-premise layout: dedicated OS user, Python venv or packaged install, PostgreSQL role (not superuser), systemd service, and a repeatable start/stop/upgrade routine aligned with the [source install](https://www.odoo.com/documentation/18.0/administration/on_premise/source.html) docs.

**WHAT YOU SHOULD KNOW FIRST:** Phase 69 — configuration as policy. Phase 5 — you have already run Odoo locally.

**LEARNING RESOURCES:**

- [Source install](https://www.odoo.com/documentation/18.0/administration/on_premise/source.html)
- [Deploy / system configuration](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html)
- [odoo/odoo](https://github.com/odoo/odoo) — `setup/debinstall.sh`, debian packaging hints

## 74.1 Linux User

**Linux User** belongs to **Linux Deployment**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Linux User** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Linux User** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Linux User**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Linux User** is concrete, continue — the next lesson in this phase still depends on it.

## 74.2 Filesystem Layout

**Filesystem Layout** belongs to **Linux Deployment**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Filesystem Layout** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Filesystem Layout** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Filesystem Layout**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Filesystem Layout** is concrete, continue — the next lesson in this phase still depends on it.

## 74.3 Python Environment

**Python Environment** belongs to **Linux Deployment**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Python Environment** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Python Environment** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Python Environment**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Python Environment** is concrete, continue — the next lesson in this phase still depends on it.

## 74.4 Source Deployment

**Source Deployment** belongs to **Linux Deployment**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Source Deployment** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Source Deployment** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Source Deployment**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Source Deployment** is concrete, continue — the next lesson in this phase still depends on it.

## 74.5 Addons

**Addons** belongs to **Linux Deployment**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Addons** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Addons** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Addons**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Addons** is concrete, continue — the next lesson in this phase still depends on it.

## 74.6 File Permissions

- Filestore under `data_dir` must be writable by the service user.
- Config files with passwords: mode `640`, owned by `root:odoo` or `odoo:odoo`.
- Do not run Odoo as root.
- Separate “deploy key” Git access from personal developer SSH keys when possible.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Draw the directory layout above and label which paths belong in backups. |
| Medium | Write a systemd unit for your lab machine and verify `systemctl status` after start. |
| Hard | Document a runbook: “deploy new addon version” with pull, restart, `-u`, smoke tests, and rollback. |

**WHAT THIS UNLOCKS NEXT:** Binding Odoo to port 8069 on localhost is not how the internet should talk to it. You need a reverse proxy and HTTPS.

---

<a id="phase-71"></a>

## 74.7 systemd

A minimal unit sketch:

```ini
[Unit]
Description=Odoo 18
After=network.target postgresql.service

[Service]
Type=simple
User=odoo
Group=odoo
ExecStart=/opt/odoo/venv/bin/python /opt/odoo/odoo/odoo-bin -c /etc/odoo/odoo.conf
Restart=on-failure
LimitNOFILE=65535

[Install]
WantedBy=multi-user.target
```

Operators then use:

```bash
sudo systemctl enable --now odoo
sudo systemctl status odoo
sudo journalctl -u odoo -f
```

**Upgrade ritual (module code):** pull Git → restart or rolling restart → run `-u module` in a maintenance window on a known DB — never “edit production files by hand and hope.”

**Install ritual (new server):** OS packages → Postgres role → clone → venv → conf → systemd → create DB → install base apps → deploy custom addons → verify HTTPS (next phases).

## 74.8 Service Management

**Service Management** belongs to **Linux Deployment**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Service Management** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Service Management** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Service Management**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Service Management** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 74:** You can explain every lesson title in Phase 74 with a concrete Odoo or business example.

> **Phase 74 complete?** [Continue to Phase 75](#phase-75)


<a id="phase-75"></a>
# PHASE 75 - Reverse Proxy & HTTPS

**Track:** Deployment

**WHAT YOU WILL BE ABLE TO DO:** Put nginx (or similar) in front of Odoo for TLS termination, HTTP→HTTPS redirect, long timeouts, and websocket/livechat upstreams — with `proxy_mode = True` only when the proxy is trusted.

**WHAT YOU SHOULD KNOW FIRST:** Phase 69 — `proxy_mode` and `dbfilter`. Phase 70 — Odoo listening on localhost:8069.

**LEARNING RESOURCES:**

- [Deploy — HTTPS section](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html)
- [Security recommendations](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html) (same deploy doc)
- Official nginx samples in the deploy documentation

## 75.1 Reverse Proxy

From the official nginx sketch:

- `upstream odoo` → `127.0.0.1:8069`
- `upstream odoochat` → `127.0.0.1:8072` for websocket/livechat
- Long `proxy_*_timeout` values (Odoo examples use ~720s) for long requests/reports
- Redirect port 80 → 443
- Forward headers: Host, X-Forwarded-Host, X-Forwarded-For, X-Forwarded-Proto
- Optional: serve `/web/static/` with longer cache headers

**COMMON FAILURES:**

| Symptom | Likely cause |
| --- | --- |
| Mixed content / wrong URLs | `proxy_mode` off or missing `X-Forwarded-Proto` |
| Livechat/websocket dies | chat upstream or Upgrade headers missing |
| Timeouts on PDF/report | proxy read timeout too low |
| Wrong DB on website | `dbfilter` not aligned with hostname |

Certificates: Let’s Encrypt via certbot is common for labs; enterprises may use their own PKI. Renewals must be automated — expired certs look like “Odoo is down.”

## 75.2 Nginx

**Nginx** belongs to **Reverse Proxy & HTTPS**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Nginx** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Nginx** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Nginx**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Nginx** is concrete, continue — the next lesson in this phase still depends on it.

## 75.3 Domains

**Domains** belongs to **Reverse Proxy & HTTPS**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Domains** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Domains** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Domains**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Domains** is concrete, continue — the next lesson in this phase still depends on it.

## 75.4 DNS

**DNS** belongs to **Reverse Proxy & HTTPS**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **DNS** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **DNS** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **DNS**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **DNS** is concrete, continue — the next lesson in this phase still depends on it.

## 75.5 TLS / HTTPS

**TLS / HTTPS** belongs to **Reverse Proxy & HTTPS**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **TLS / HTTPS** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **TLS / HTTPS** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **TLS / HTTPS**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **TLS / HTTPS** is concrete, continue — the next lesson in this phase still depends on it.

## 75.6 Proxy Headers

From the official nginx sketch:

- `upstream odoo` → `127.0.0.1:8069`
- `upstream odoochat` → `127.0.0.1:8072` for websocket/livechat
- Long `proxy_*_timeout` values (Odoo examples use ~720s) for long requests/reports
- Redirect port 80 → 443
- Forward headers: Host, X-Forwarded-Host, X-Forwarded-For, X-Forwarded-Proto
- Optional: serve `/web/static/` with longer cache headers

**COMMON FAILURES:**

| Symptom | Likely cause |
| --- | --- |
| Mixed content / wrong URLs | `proxy_mode` off or missing `X-Forwarded-Proto` |
| Livechat/websocket dies | chat upstream or Upgrade headers missing |
| Timeouts on PDF/report | proxy read timeout too low |
| Wrong DB on website | `dbfilter` not aligned with hostname |

Certificates: Let’s Encrypt via certbot is common for labs; enterprises may use their own PKI. Renewals must be automated — expired certs look like “Odoo is down.”

## 75.7 WebSocket / Long-Lived Connections

**WebSocket / Long-Lived Connections** belongs to **Reverse Proxy & HTTPS**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **WebSocket / Long-Lived Connections** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **WebSocket / Long-Lived Connections** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **WebSocket / Long-Lived Connections**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **WebSocket / Long-Lived Connections** is concrete, continue — the next lesson in this phase still depends on it.

## 75.8 Secure Production Exposure

**Secure Production Exposure** belongs to **Reverse Proxy & HTTPS**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Secure Production Exposure** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Secure Production Exposure** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Secure Production Exposure**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Secure Production Exposure** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 75:** You can explain every lesson title in Phase 75 with a concrete Odoo or business example.

> **Phase 75 complete?** [Continue to Phase 76](#phase-76)


<a id="phase-76"></a>
# PHASE 76 - Workers & Production Configuration

**Track:** Deployment

**WHAT YOU WILL BE ABLE TO DO:** Choose multi-processing workers vs the default multi-threaded server; apply official rules of thumb for worker count and RAM; set memory/time/request limits; and explain cron and livechat workers.

**WHAT YOU SHOULD KNOW FIRST:** Phase 71 — proxy in front. Phase 60–63 — transactions and performance (workers amplify bad queries).

**LEARNING RESOURCES:**

- [Builtin server / workers (deploy docs)](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html)
- [CLI options](https://www.odoo.com/documentation/18.0/developer/reference/cli.html)

## 76.1 Development Mode

**Development Mode** belongs to **Workers & Production Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Development Mode** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Development Mode** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Development Mode**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Development Mode** is concrete, continue — the next lesson in this phase still depends on it.

## 76.2 Multi-Processing

**Multi-Processing** belongs to **Workers & Production Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Multi-Processing** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Multi-Processing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Multi-Processing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Multi-Processing** is concrete, continue — the next lesson in this phase still depends on it.

## 76.3 Workers

**Workers** belongs to **Workers & Production Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Workers** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Workers** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Workers**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Workers** is concrete, continue — the next lesson in this phase still depends on it.

## 76.4 Worker Sizing

From Odoo’s deploy documentation:

- **Worker count rule of thumb:** `(#CPU × 2) + 1`
- Rough concurrency: **1 worker ≈ 6 concurrent users**
- Cron needs CPU too — count it in capacity planning
- RAM estimate assumes ~20% heavy / ~80% light requests  
  - Heavy worker ≈ ~1 GB  
  - Light worker ≈ ~150 MB  
  - `Needed RAM ≈ #workers × (0.8×150MB + 0.2×1024MB)`

**Worked example (from docs):** 4 CPU, ~60 concurrent users → theoretical workers ≈ 10 by users, ≈ 9 by CPU formula → use **8 HTTP workers + 1 cron**, ~3 GB for Odoo processes alone (Postgres and OS need more).

```ini
[options]
workers = 8
max_cron_threads = 1
limit_memory_soft = 629145600
limit_memory_hard = 1677721600
limit_request = 8192
limit_time_cpu = 600
limit_time_real = 1200
```

**What the limits do:**

| Limit | Role |
| --- | --- |
| `limit_memory_soft` | Worker recycled after request when RSS exceeds soft limit |
| `limit_memory_hard` | Worker killed immediately if hard limit exceeded |
| `limit_request` | Recycle after N requests (fights slow leaks) |
| `limit_time_cpu` / `limit_time_real` | Kill runaway CPU / wall-clock requests |

These are **safety rails**, not substitutes for fixing N+1 queries (Story XIV).

## 76.5 CPU Considerations

**CPU Considerations** belongs to **Workers & Production Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **CPU Considerations** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **CPU Considerations** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **CPU Considerations**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **CPU Considerations** is concrete, continue — the next lesson in this phase still depends on it.

## 76.6 Memory Limits

**Memory Limits** belongs to **Workers & Production Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Memory Limits** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Memory Limits** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Memory Limits**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Memory Limits** is concrete, continue — the next lesson in this phase still depends on it.

## 76.7 Request Time Limits

**Request Time Limits** belongs to **Workers & Production Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Request Time Limits** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Request Time Limits** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Request Time Limits**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Request Time Limits** is concrete, continue — the next lesson in this phase still depends on it.

## 76.8 Cron Workers

**Cron Workers** belongs to **Workers & Production Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Cron Workers** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Cron Workers** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Cron Workers**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Cron Workers** is concrete, continue — the next lesson in this phase still depends on it.

## 76.9 Production Configuration

**Production Configuration** belongs to **Workers & Production Configuration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Production Configuration** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Production Configuration** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Production Configuration**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Production Configuration** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 76:** You can explain every lesson title in Phase 76 with a concrete Odoo or business example.

> **Phase 76 complete?** [Continue to Phase 77](#phase-77)


<a id="phase-77"></a>
# PHASE 77 - PostgreSQL Operations

**Track:** Deployment

**WHAT YOU WILL BE ABLE TO DO:** Operate the Postgres side of Odoo: roles and privileges, connection limits vs worker counts, basic vacuum/analyze awareness, and when Odoo and Postgres live on separate hosts.

**WHAT YOU SHOULD KNOW FIRST:** Phase 58–61 — schema and plans. Phase 72 — each worker opens DB connections.

**LEARNING RESOURCES:**

- [Deploy — PostgreSQL section](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html)
- [PostgreSQL docs — routine vacuuming](https://www.postgresql.org/docs/current/routine-vacuuming.html)

## 77.1 Database Users

Every ORM write becomes SQL. Workers multiply connections. Official sample configs raise `max_connections` thoughtfully (example values around 80 on a small dedicated Postgres). Rough planning:

```text
max_connections ≳ (odoo workers × connections per worker) + cron + admin + margin
```

If Postgres refuses connections, users see errors that look like “Odoo is down.”

**Roles:** create an `odoo` role that owns the application database. Do **not** use a superuser. For locking down the DB manager: no `CREATEDB` on the app role and a different owner strategy — see deploy docs.

Configure remote Postgres carefully (`pg_hba.conf`, `listen_addresses`); prefer SSL (`db_sslmode` in Odoo: `require` / `verify-full` as appropriate). Prefer a private network over public exposure.

## 77.2 Connections

**Connections** belongs to **PostgreSQL Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Connections** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Connections** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Connections**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Connections** is concrete, continue — the next lesson in this phase still depends on it.

## 77.3 Connection Limits

**Connection Limits** belongs to **PostgreSQL Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Connection Limits** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Connection Limits** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Connection Limits**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Connection Limits** is concrete, continue — the next lesson in this phase still depends on it.

## 77.4 Maintenance

- **Autovacuum** usually handles dead tuples; giant bulk deletes/imports may need attention.
- **ANALYZE** keeps the planner honest after large data shape changes (Story XIV).
- Watch disk for WAL and data growth — a full disk stops Odoo cold.
- Extensions and custom SQL in modules must be upgrade-safe (Story XVII).

**Operator checklist weekly:** connections in use, slow queries, disk %, backup success (next phase), checkpoint/WAL warnings.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | List why the Odoo DB user must not be a superuser. |
| Medium | Given `workers = 8`, propose a `max_connections` floor and justify. |
| Hard | Write a one-page ops note: separate DB host, SSL mode, and firewall rules. |

**WHAT THIS UNLOCKS NEXT:** A perfect Postgres that you cannot restore after ransomware or human error is still a single point of failure. Backups.

---

<a id="phase-74"></a>

## 77.5 VACUUM Concepts

**VACUUM Concepts** belongs to **PostgreSQL Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **VACUUM Concepts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **VACUUM Concepts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **VACUUM Concepts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **VACUUM Concepts** is concrete, continue — the next lesson in this phase still depends on it.

## 77.6 ANALYZE

**ANALYZE** belongs to **PostgreSQL Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **ANALYZE** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ANALYZE** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ANALYZE**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ANALYZE** is concrete, continue — the next lesson in this phase still depends on it.

## 77.7 Monitoring

**Monitoring** belongs to **PostgreSQL Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Monitoring** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Monitoring** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Monitoring**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Monitoring** is concrete, continue — the next lesson in this phase still depends on it.

## 77.8 Basic Tuning

**Basic Tuning** belongs to **PostgreSQL Operations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Basic Tuning** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Basic Tuning** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Basic Tuning**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Basic Tuning** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 77:** You can explain every lesson title in Phase 77 with a concrete Odoo or business example.

> **Phase 77 complete?** [Continue to Phase 78](#phase-78)


<a id="phase-78"></a>
# PHASE 78 - Backups & Disaster Recovery

**Track:** Deployment

**WHAT YOU WILL BE ABLE TO DO:** Design and test backups that include **both** the PostgreSQL dump **and** the filestore; practice restore on a spare host; define RPO/RTO in plain language.

**WHAT YOU SHOULD KNOW FIRST:** Phase 69 — `data_dir`. Phase 73 — Postgres ownership.

**LEARNING RESOURCES:**

- [Deploy documentation](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html) (security + ops context)
- [PostgreSQL backup/restore](https://www.postgresql.org/docs/current/backup.html)
- Odoo database manager backup (dev/small) vs `pg_dump` (production discipline)

## 78.1 Database Backup

**Database Backup** belongs to **Backups & Disaster Recovery**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Database Backup** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Database Backup** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Database Backup**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Database Backup** is concrete, continue — the next lesson in this phase still depends on it.

## 78.2 Filestore Backup

**Filestore Backup** belongs to **Backups & Disaster Recovery**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Filestore Backup** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Filestore Backup** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Filestore Backup**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Filestore Backup** is concrete, continue — the next lesson in this phase still depends on it.

## 78.3 Configuration Backup

**Configuration Backup** belongs to **Backups & Disaster Recovery**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Configuration Backup** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Configuration Backup** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Configuration Backup**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Configuration Backup** is concrete, continue — the next lesson in this phase still depends on it.

## 78.4 Restore

1. Provision Postgres + empty data_dir.
2. Restore dump into a new DB name.
3. Extract filestore into `data_dir/filestore/<dbname>/`.
4. Point `odoo.conf` at the restored DB; start Odoo.
5. Smoke-test: login, open a record with an attachment, run a report, confirm cron.

Document who can restore, where keys live, and how long the last drill took.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain why filestore must join DB backups. |
| Medium | Write a backup script outline and a restore checklist. |
| Hard | Define RPO/RTO for a company that invoices daily, and size backup frequency accordingly. |

**WHAT THIS UNLOCKS NEXT:** Not every team runs bare metal. Odoo.sh is the official PaaS path many projects use instead of self-managed Linux.

---

<a id="phase-75"></a>

## 78.5 Backup Scheduling

**Backup Scheduling** belongs to **Backups & Disaster Recovery**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Backup Scheduling** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Backup Scheduling** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Backup Scheduling**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Backup Scheduling** is concrete, continue — the next lesson in this phase still depends on it.

## 78.6 Off-Site Backups

**Off-Site Backups** belongs to **Backups & Disaster Recovery**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Off-Site Backups** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Off-Site Backups** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Off-Site Backups**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Off-Site Backups** is concrete, continue — the next lesson in this phase still depends on it.

## 78.7 Recovery Testing

**Recovery Testing** belongs to **Backups & Disaster Recovery**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Recovery Testing** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Recovery Testing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Recovery Testing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Recovery Testing** is concrete, continue — the next lesson in this phase still depends on it.

## 78.8 Disaster Recovery Plan

**Disaster Recovery Plan** belongs to **Backups & Disaster Recovery**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Disaster Recovery Plan** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Disaster Recovery Plan** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Disaster Recovery Plan**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Disaster Recovery Plan** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 78:** You can explain every lesson title in Phase 78 with a concrete Odoo or business example.

> **Phase 78 complete?** [Continue to Phase 79](#phase-79)


<a id="phase-79"></a>
# PHASE 79 - Odoo.sh

**Track:** Deployment

**WHAT YOU WILL BE ABLE TO DO:** Explain what Odoo.sh provides (Git-based builds, staging/production databases, CI-ish checks) versus self-hosted Linux, and when each fits.

**WHAT YOU SHOULD KNOW FIRST:** Phase 70–74 — you understand what Odoo.sh is automating for you.

**LEARNING RESOURCES:**

- [Odoo.sh documentation hub](https://www.odoo.com/documentation/18.0/administration/odoo_sh.html) (follow current official pages for branches, builds, and databases)
- [Odoo developer docs](https://www.odoo.com/documentation/18.0/developer.html)

## 79.1 Odoo.sh Architecture

**Odoo.sh Architecture** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Odoo.sh Architecture** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Odoo.sh Architecture** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Odoo.sh Architecture**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Odoo.sh Architecture** is concrete, continue — the next lesson in this phase still depends on it.

## 79.2 Projects

**Projects** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Projects** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **Projects** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **Projects** is concrete, continue — the next lesson in this phase still depends on it.

## 79.3 Git Integration

Odoo.sh is Odoo’s cloud platform: you connect a Git repository; pushes build servers; branches map to environments (typically production + staging/dev databases). You still write the same modules — the platform owns much of OS, Postgres, HTTPS, and backup plumbing.

```text
feature branch  -->  build  -->  test DB
staging branch  -->  build  -->  staging DB (often prod copy)
production      -->  build  -->  live DB
```

You trade control for speed: less SSH DIY, more platform conventions (manifest versions, test status, build logs).

## 79.4 Branches

**Branches** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Branches** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Branches** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Branches**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Branches** is concrete, continue — the next lesson in this phase still depends on it.

## 79.5 Development

**Development** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Development** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Development** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Development**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Development** is concrete, continue — the next lesson in this phase still depends on it.

## 79.6 Staging

**Staging** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Staging** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Staging** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Staging**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Staging** is concrete, continue — the next lesson in this phase still depends on it.

## 79.7 Production

**Production** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Production** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Production** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Production**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Production** is concrete, continue — the next lesson in this phase still depends on it.

## 79.8 Builds

**Builds** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Builds** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Builds** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Builds**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Builds** is concrete, continue — the next lesson in this phase still depends on it.

## 79.9 Logs

**Logs** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Logs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Logs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Logs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Logs** is concrete, continue — the next lesson in this phase still depends on it.

## 79.10 Shell Access

**Shell Access** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Shell Access** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Shell Access** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Shell Access**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Shell Access** is concrete, continue — the next lesson in this phase still depends on it.

## 79.11 Database Management

**Database Management** belongs to **Odoo.sh**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Database Management** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Database Management** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Database Management**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Database Management** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 79:** You can explain every lesson title in Phase 79 with a concrete Odoo or business example.

> **Phase 79 complete?** [Continue to Phase 80](#phase-80)


<a id="phase-80"></a>
# PHASE 80 - Docker for Odoo

**Track:** Deployment

**WHAT YOU WILL BE ABLE TO DO:** Reason about official [odoo/docker](https://github.com/odoo/docker) images: Odoo container + Postgres container, volumes for filestore and DB data, env vars, and the fact that default images lean on the **threaded** server unless you configure otherwise.

**WHAT YOU SHOULD KNOW FIRST:** Phase 70–74 — same concerns, different packaging.

**LEARNING RESOURCES:**

- [odoo/docker](https://github.com/odoo/docker)
- [Deploy docs — workers note for Docker](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html)
- [Source install](https://www.odoo.com/documentation/18.0/administration/on_premise/source.html) for non-container comparison

## 80.1 Images

**Images** belongs to **Docker for Odoo**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Images** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Images** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Images**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Images** is concrete, continue — the next lesson in this phase still depends on it.

## 80.2 Containers

A typical compose mental model:

```text
[odoo container]  --TCP-->  [postgres container]
       |                         |
   volume: filestore         volume: pgdata
       |
   bind/mount: custom addons
```

You still need: backups of **both** volumes, HTTPS in front (Traefik/nginx/Caddy), resource limits, and upgrade discipline. Docker does not remove `dbfilter`, secrets, or worker sizing — it changes how you start processes.

Official images are a starting point; read their docs for env vars (`HOST`, `USER`, `PASSWORD`, etc.) and mounted addon paths. Default worker mode for containers is often threaded (`workers = 0`) per deploy docs — production containers on Linux can enable multiprocessing if the image/process setup allows, with the same RAM math as Phase 72.

## 80.3 Volumes

**Volumes** belongs to **Docker for Odoo**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Volumes** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Volumes** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Volumes**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Volumes** is concrete, continue — the next lesson in this phase still depends on it.

## 80.4 Networking

**Networking** belongs to **Docker for Odoo**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Networking** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Networking** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Networking**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Networking** is concrete, continue — the next lesson in this phase still depends on it.

## 80.5 Environment Variables

**Environment Variables** belongs to **Docker for Odoo**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Environment Variables** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Environment Variables** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Environment Variables**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Environment Variables** is concrete, continue — the next lesson in this phase still depends on it.

## 80.6 Odoo Container

**Odoo Container** belongs to **Docker for Odoo**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Odoo Container** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Odoo Container** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Odoo Container**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Odoo Container** is concrete, continue — the next lesson in this phase still depends on it.

## 80.7 PostgreSQL Container

**PostgreSQL Container** belongs to **Docker for Odoo**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **PostgreSQL Container** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **PostgreSQL Container** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **PostgreSQL Container**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **PostgreSQL Container** is concrete, continue — the next lesson in this phase still depends on it.

## 80.8 Persistent Filestore

**Persistent Filestore** belongs to **Docker for Odoo**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Persistent Filestore** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Persistent Filestore** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Persistent Filestore**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Persistent Filestore** is concrete, continue — the next lesson in this phase still depends on it.

## 80.9 Docker Compose

**Docker Compose** belongs to **Docker for Odoo**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Docker Compose** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Docker Compose** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Docker Compose**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Docker Compose** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 80:** You can explain every lesson title in Phase 80 with a concrete Odoo or business example.

> **Phase 80 complete?** [Continue to Phase 81](#phase-81)


<a id="phase-81"></a>
# PHASE 81 - Monitoring & Production Troubleshooting

**Track:** Deployment

**WHAT YOU WILL BE ABLE TO DO:** Read Odoo and nginx/Postgres logs under load; watch worker memory kills, HTTP 5xx rates, disk, and backup job success; set a minimal alert set for production.

**WHAT YOU SHOULD KNOW FIRST:** Phase 64 — logging mindset. Phase 72 — worker limits leave fingerprints in logs.

**LEARNING RESOURCES:**

- [CLI / logging](https://www.odoo.com/documentation/18.0/developer/reference/cli.html)
- [Deploy documentation](https://www.odoo.com/documentation/18.0/administration/on_premise/deploy.html)

## 81.1 Application Logs

Configure `logfile` and a sensible `log_level` (`info` for prod; temporarily `debug` for incidents). Correlate:

```text
nginx access/error  →  odoo.log worker events  →  postgres log
        |                      |                      |
   502/504 timing        traceback / MemoryError   fatal / connection
```

Workers recycled by `limit_memory_*` or time limits often precede “random” user errors. Cron failures hide in logs until invoices stop generating.

## 81.2 PostgreSQL Logs

Configure `logfile` and a sensible `log_level` (`info` for prod; temporarily `debug` for incidents). Correlate:

```text
nginx access/error  →  odoo.log worker events  →  postgres log
        |                      |                      |
   502/504 timing        traceback / MemoryError   fatal / connection
```

Workers recycled by `limit_memory_*` or time limits often precede “random” user errors. Cron failures hide in logs until invoices stop generating.

## 81.3 CPU

**CPU** belongs to **Monitoring & Production Troubleshooting**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **CPU** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **CPU** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **CPU**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **CPU** is concrete, continue — the next lesson in this phase still depends on it.

## 81.4 Memory

**Memory** belongs to **Monitoring & Production Troubleshooting**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Memory** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Memory** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Memory**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Memory** is concrete, continue — the next lesson in this phase still depends on it.

## 81.5 Disk

**Disk** belongs to **Monitoring & Production Troubleshooting**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Disk** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Disk** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Disk**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Disk** is concrete, continue — the next lesson in this phase still depends on it.

## 81.6 Database Connections

**Database Connections** belongs to **Monitoring & Production Troubleshooting**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Database Connections** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Database Connections** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Database Connections**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Database Connections** is concrete, continue — the next lesson in this phase still depends on it.

## 81.7 Slow Requests

**Slow Requests** belongs to **Monitoring & Production Troubleshooting**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Slow Requests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Slow Requests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Slow Requests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Slow Requests** is concrete, continue — the next lesson in this phase still depends on it.

## 81.8 Failed Jobs

**Failed Jobs** belongs to **Monitoring & Production Troubleshooting**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Failed Jobs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Failed Jobs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Failed Jobs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Failed Jobs** is concrete, continue — the next lesson in this phase still depends on it.

## 81.9 Error Monitoring

| Signal | Why |
| --- | --- |
| Process up (systemd/container) | Cold downtime |
| HTTP 5xx rate | User-visible failure |
| Disk % on data + PG | Silent death when full |
| Backup job success | Future you |
| CPU / RAM / load | Capacity before meltdown |
| Certificate expiry | Avoidable outage |

Optional deeper: slow query logs, pg_stat_activity, APM — after basics work.

**Alerting rule:** pages should be rare and actionable. Email on backup failure; page on site down.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Given a traceback timestamp, list which three log files you open first. |
| Medium | Design a five-alert starter pack for a 2-vCPU Odoo VM. |
| Hard | Walk an incident: “reports timeout after 10 minutes” using limits from Phase 72 and proxy timeouts from Phase 71. |

**WHAT THIS UNLOCKS NEXT:** You can keep a system alive — Story XVI project asks you to deploy one end-to-end. After that, Story XVII faces the other half of operations: **upgrades** without destroying live data.

---

<a id="story-project---deploy-an-odoo-system"></a>

## 81.10 Incident Diagnosis

**Incident Diagnosis** belongs to **Monitoring & Production Troubleshooting**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Incident Diagnosis** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Incident Diagnosis** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Incident Diagnosis**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Incident Diagnosis** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 81:** You can explain every lesson title in Phase 81 with a concrete Odoo or business example.

> **Phase 81 complete?** [Continue to Phase 82](#phase-82)


## STORY PROJECT — Deploy an Odoo System

**Track:** Deployment

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XX — UPGRADES, MIGRATIONS & MAINTENANCE

**Track:** Upgrades

*Production code must survive tomorrow's Odoo version.*

---
<a id="phase-82"></a>
<a id="phase-82---module-versioning"></a>
# PHASE 82 - Module Versioning

**Track:** Upgrades

**WHAT YOU WILL BE ABLE TO DO:** Use `__manifest__.py` `version` intentionally (series + module revision), know when `-u` runs, and relate version bumps to migration script folders.

**WHAT YOU SHOULD KNOW FIRST:** Phase 8 — module lifecycle. Phase 65–66 — tests that should pass before and after `-u`.

**LEARNING RESOURCES:**

- [Module manifests](https://www.odoo.com/documentation/18.0/developer/reference/backend/module.html)
- [Upgrades reference](https://www.odoo.com/documentation/18.0/developer/reference/upgrades.html)
- [Upgrade scripts](https://www.odoo.com/documentation/18.0/developer/reference/upgrades/upgrade_scripts.html)

## 82.1 Manifest Version

In the manifest:

```python
{
    "name": "Library",
    "version": "18.0.1.2.0",
    "depends": ["base", "mail"],
    ...
}
```

Common Odoo convention: `odoo_series.major.minor.patch` (exact house style varies by team — pick one and stay consistent). The series prefix (`18.0`) signals which Odoo generation the module targets.

When you change Python models, XML, or security, you typically:

1. Bump `version`
2. Ship code
3. Run `odoo-bin -u library -d thedb` (or install update from Apps)

`-u` reloads module data/updates according to rules (`noupdate`, etc.) and can trigger **migration scripts** keyed to version directories (Phase 80).

**Without a bump:** teammates and CI cannot tell “new code needs upgrade.”  
**With a careless bump and no migrations:** `-u` may create columns but leave old data stranded.

## 82.2 Semantic Change Thinking

**Semantic Change Thinking** belongs to **Module Versioning**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Semantic Change Thinking** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Semantic Change Thinking** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Semantic Change Thinking**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Semantic Change Thinking** is concrete, continue — the next lesson in this phase still depends on it.

## 82.3 Release Changes

**Release Changes** belongs to **Module Versioning**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Release Changes** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Release Changes** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Release Changes**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Release Changes** is concrete, continue — the next lesson in this phase still depends on it.

## 82.4 Compatibility

**Compatibility** belongs to **Module Versioning**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Compatibility** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Compatibility** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Compatibility**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Compatibility** is concrete, continue — the next lesson in this phase still depends on it.

## 82.5 Dependencies

**Dependencies** belongs to **Module Versioning**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Dependencies** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Dependencies** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Dependencies**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Dependencies** is concrete, continue — the next lesson in this phase still depends on it.

## 82.6 Version Control Strategy

In the manifest:

```python
{
    "name": "Library",
    "version": "18.0.1.2.0",
    "depends": ["base", "mail"],
    ...
}
```

Common Odoo convention: `odoo_series.major.minor.patch` (exact house style varies by team — pick one and stay consistent). The series prefix (`18.0`) signals which Odoo generation the module targets.

When you change Python models, XML, or security, you typically:

1. Bump `version`
2. Ship code
3. Run `odoo-bin -u library -d thedb` (or install update from Apps)

`-u` reloads module data/updates according to rules (`noupdate`, etc.) and can trigger **migration scripts** keyed to version directories (Phase 80).

**Without a bump:** teammates and CI cannot tell “new code needs upgrade.”  
**With a careless bump and no migrations:** `-u` may create columns but leave old data stranded.


<a id="phase-83"></a>
<a id="phase-83---schema-changes"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 82:** You can explain every lesson title in Phase 82 with a concrete Odoo or business example.

> **Phase 82 complete?** [Continue to Phase 83](#phase-83)


<a id="phase-83"></a>
# PHASE 83 - Schema Changes

**Track:** Upgrades

**WHAT YOU WILL BE ABLE TO DO:** Evolve fields safely: add columns, rename with data preservation, change types carefully, and avoid “delete field in prod on Friday” disasters.

**WHAT YOU SHOULD KNOW FIRST:** Phase 9–12 — fields. Phase 78 — versioning.

**LEARNING RESOURCES:**

- [ORM fields](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [Upgrade scripts](https://www.odoo.com/documentation/18.0/developer/reference/upgrades/upgrade_scripts.html)

## 83.1 Adding Fields

**Adding Fields** belongs to **Schema Changes**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Adding Fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Adding Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Adding Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Adding Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 83.2 Renaming Fields

**Renaming Fields** belongs to **Schema Changes**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Renaming Fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Renaming Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Renaming Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Renaming Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 83.3 Changing Field Types

**Changing Field Types** belongs to **Schema Changes**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Changing Field Types** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Changing Field Types** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Changing Field Types**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Changing Field Types** is concrete, continue — the next lesson in this phase still depends on it.

## 83.4 Changing Relationships

**Changing Relationships** belongs to **Schema Changes**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Changing Relationships** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Changing Relationships** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Changing Relationships**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Changing Relationships** is concrete, continue — the next lesson in this phase still depends on it.

## 83.5 Removing Fields

**Removing Fields** belongs to **Schema Changes**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Removing Fields** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Removing Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Removing Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Removing Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 83.6 Constraints

**Constraints** belongs to **Schema Changes**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Constraints** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Constraints** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Constraints**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Constraints** is concrete, continue — the next lesson in this phase still depends on it.

## 83.7 Safe Schema Evolution

When you add `priority = fields.Selection(...)`, an upgrade usually creates the column. When you **remove** a field from Python, Odoo may leave the column or clean it depending on version/tools — never assume production data is disposable.

**Safe patterns:**

| Change | Approach |
| --- | --- |
| Add field | Add field + bump version + `-u`; set defaults carefully |
| Rename field | Add new field → migrate data (Phase 80) → deprecate old → remove later |
| Change Selection keys | Map old keys to new in migration; do not silently orphan values |
| Char → Many2one | Create relation, migrate values, then switch UI |
| Shorten size / change type | Validate existing data first on a clone |

**Dangerous patterns:** drop column with live reports depending on it; reuse a column name for a different meaning; change `company_id` semantics casually.

```text
BAD:  rename field in Python only, hope values follow
GOOD: pre-migrate copies old_column → new_column, then model uses new name
```


<a id="phase-84"></a>
<a id="phase-84---data-migrations"></a>

**CHECK YOUR UNDERSTANDING AFTER PHASE 83:** You can explain every lesson title in Phase 83 with a concrete Odoo or business example.

> **Phase 83 complete?** [Continue to Phase 84](#phase-84)


<a id="phase-84"></a>
# PHASE 84 - Data Migrations

**Track:** Upgrades

**WHAT YOU WILL BE ABLE TO DO:** Write and test upgrade scripts (`migrations/` / official upgrade script API) that move data idempotently where possible, and know pre vs post migrate timing.

**WHAT YOU SHOULD KNOW FIRST:** Phase 79 — schema strategy. Phase 65 — TransactionCase for business rules after migrate.

**LEARNING RESOURCES:**

- [Upgrade scripts](https://www.odoo.com/documentation/18.0/developer/reference/upgrades/upgrade_scripts.html)
- [Upgrade utils](https://www.odoo.com/documentation/18.0/developer/reference/upgrades/upgrade_utils.html)
- [Upgrades index](https://www.odoo.com/documentation/18.0/developer/reference/upgrades.html)

## 84.1 Migration Scripts

**Migration Scripts** belongs to **Data Migrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Migration Scripts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Migration Scripts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Migration Scripts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Migration Scripts** is concrete, continue — the next lesson in this phase still depends on it.

## 84.2 Pre-Migration

**Pre-Migration** belongs to **Data Migrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Pre-Migration** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Pre-Migration** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Pre-Migration**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Pre-Migration** is concrete, continue — the next lesson in this phase still depends on it.

## 84.3 Post-Migration

**Post-Migration** belongs to **Data Migrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Post-Migration** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Post-Migration** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Post-Migration**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Post-Migration** is concrete, continue — the next lesson in this phase still depends on it.

## 84.4 Transforming Records

**Transforming Records** belongs to **Data Migrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Transforming Records** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Transforming Records** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Transforming Records**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Transforming Records** is concrete, continue — the next lesson in this phase still depends on it.

## 84.5 Renaming Data

**Renaming Data** belongs to **Data Migrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Renaming Data** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Renaming Data** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Renaming Data**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Renaming Data** is concrete, continue — the next lesson in this phase still depends on it.

## 84.6 Preserving Relationships

**Preserving Relationships** belongs to **Data Migrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Preserving Relationships** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Preserving Relationships** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Preserving Relationships**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Preserving Relationships** is concrete, continue — the next lesson in this phase still depends on it.

## 84.7 Validation

**Validation** belongs to **Data Migrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Validation** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Validation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Validation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Validation** is concrete, continue — the next lesson in this phase still depends on it.

## 84.8 Large Dataset Considerations

**Large Dataset Considerations** belongs to **Data Migrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Large Dataset Considerations** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Large Dataset Considerations** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Large Dataset Considerations**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Large Dataset Considerations** is concrete, continue — the next lesson in this phase still depends on it.

## 84.9 Rollback Planning

**Rollback Planning** belongs to **Data Migrations**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Rollback Planning** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Rollback Planning** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Rollback Planning**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Rollback Planning** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 84:** You can explain every lesson title in Phase 84 with a concrete Odoo or business example.

> **Phase 84 complete?** [Continue to Phase 85](#phase-85)


<a id="phase-85"></a>
# PHASE 85 - Odoo Version Upgrades

**Track:** Upgrades

**WHAT YOU WILL BE ABLE TO DO:** Plan a major Odoo version upgrade: inventory custom modules, check deprecated APIs, use official upgrade paths/tools where applicable, and rehearse on staging.

**WHAT YOU SHOULD KNOW FIRST:** Phase 78–80 — module-level discipline. Phase 82 will deepen upgrade-safe coding.

**LEARNING RESOURCES:**

- [Upgrades reference](https://www.odoo.com/documentation/18.0/developer/reference/upgrades.html)
- [Odoo 18 developer docs](https://www.odoo.com/documentation/18.0/developer.html)
- Platform upgrade offerings / Odoo.sh upgrade docs (official pages for your edition)

## 85.1 Version Differences

**Version Differences** belongs to **Odoo Version Upgrades**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Version Differences** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Version Differences** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Version Differences**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Version Differences** is concrete, continue — the next lesson in this phase still depends on it.

## 85.2 Deprecated APIs

**Deprecated APIs** belongs to **Odoo Version Upgrades**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Deprecated APIs** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Deprecated APIs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Deprecated APIs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Deprecated APIs** is concrete, continue — the next lesson in this phase still depends on it.

## 85.3 Framework Changes

**Framework Changes** belongs to **Odoo Version Upgrades**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Framework Changes** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Framework Changes** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Framework Changes**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Framework Changes** is concrete, continue — the next lesson in this phase still depends on it.

## 85.4 Module Compatibility

**Module Compatibility** belongs to **Odoo Version Upgrades**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Module Compatibility** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Module Compatibility** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Module Compatibility**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Module Compatibility** is concrete, continue — the next lesson in this phase still depends on it.

## 85.5 Database Upgrade

**Database Upgrade** belongs to **Odoo Version Upgrades**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Database Upgrade** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Database Upgrade** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Database Upgrade**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Database Upgrade** is concrete, continue — the next lesson in this phase still depends on it.

## 85.6 Upgrade Scripts

**Upgrade Scripts** belongs to **Odoo Version Upgrades**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Upgrade Scripts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Upgrade Scripts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Upgrade Scripts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Upgrade Scripts** is concrete, continue — the next lesson in this phase still depends on it.

## 85.7 Upgrade Utilities

**Upgrade Utilities** belongs to **Odoo Version Upgrades**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Upgrade Utilities** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Upgrade Utilities** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Upgrade Utilities**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Upgrade Utilities** is concrete, continue — the next lesson in this phase still depends on it.

## 85.8 Frontend Migration

**Frontend Migration** belongs to **Odoo Version Upgrades**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Frontend Migration** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Frontend Migration** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Frontend Migration**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Frontend Migration** is concrete, continue — the next lesson in this phase still depends on it.

## 85.9 Post-Upgrade Testing

**Post-Upgrade Testing** belongs to **Odoo Version Upgrades**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Post-Upgrade Testing** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Post-Upgrade Testing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Post-Upgrade Testing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Post-Upgrade Testing** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 85:** You can explain every lesson title in Phase 85 with a concrete Odoo or business example.

> **Phase 85 complete?** [Continue to Phase 86](#phase-86)


<a id="phase-86"></a>
# PHASE 86 - Module Hooks

**Track:** Upgrades

Production code must survive tomorrow's Odoo version.

**WHAT YOU WILL BE ABLE TO DO:** Work confidently with every topic under **Module Hooks** — not as a glossary, but as mechanisms you can implement and debug.

**WHAT YOU SHOULD KNOW FIRST:** Earlier stories through the phase before this one. Keep one demo company in mind.

**LEARNING RESOURCES:**

- [Odoo 18 Developer docs](https://www.odoo.com/documentation/18.0/developer.html)
- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [odoo/odoo](https://github.com/odoo/odoo)

## 86.1 Hooks Concept

Module hooks are Python callables registered in `__manifest__.py` that run at specific lifecycle moments: before init, after init, and on uninstall. They exist for work XML cannot express — but they are easy to abuse into unmaintainable side effects.

**PRACTICE:** Read an official module that uses `post_init_hook`; note why XML was insufficient.

**WHAT THIS UNLOCKS NEXT:** `pre_init_hook` specifically.


## 86.2 pre_init_hook

`pre_init_hook(cr)` runs before the module’s schema/data load. Typical uses: PostgreSQL prep, cleaning incompatible leftovers. You get a cursor, not a full `env` — keep it minimal and SQL-safe.

**PRACTICE:** Sketch a pre_init that aborts install if a forbidden table exists (pseudo-code).

**WHAT THIS UNLOCKS NEXT:** `post_init_hook`.


## 86.3 post_init_hook

`post_init_hook(env)` (or `cr, registry` in older signatures — match your Odoo version) runs after install. Use for seeding computed state, aligning sequences, or one-time links. Prefer XML data when possible so noupdate/upgrade semantics stay clear.

**PRACTICE:** Write a post_init that sets a default config parameter via `env['ir.config_parameter']`.

**WHAT THIS UNLOCKS NEXT:** `uninstall_hook`.


## 86.4 uninstall_hook

`uninstall_hook` cleans external artifacts the ORM will not remove (files, remote webhooks). Do not assume you can casually delete business records users still need — uninstall is rare and dangerous in production.

**PRACTICE:** List three uninstall side effects that would anger a customer; design the hook to avoid them.

**WHAT THIS UNLOCKS NEXT:** What environment you actually have in hooks.


## 86.5 Environment During Hooks

Know whether your hook receives `cr` only or full `env`. Misusing models before the registry is ready causes subtle failures. Keep hooks boring and logged.

**PRACTICE:** Add logging at start/end of a post_init_hook.

**WHAT THIS UNLOCKS NEXT:** Appropriate uses vs abuse.


## 86.6 Appropriate Uses

Appropriate: one-time technical alignment, data repair that migrations will own later, registering cron defaults. Inappropriate: entire business workflows, calling out to unpaid external APIs, hiding schema changes from migration scripts.

**PRACTICE:** Classify five hook ideas as OK / move-to-migration / move-to-XML.

**WHAT THIS UNLOCKS NEXT:** Avoiding hook abuse patterns.


## 86.7 Avoiding Hook Abuse

Hook abuse symptoms: nobody knows when code runs; installs differ from upgrades; tests skip hooks; production-only surprises. Prefer `migrations/` scripts for schema/data evolution and keep hooks tiny.

**PRACTICE:** Add a comment block in manifest documenting each hook’s purpose and owner.

**WHAT THIS UNLOCKS NEXT:** Upgrade-safe customization discipline (next phase).


**CHECK YOUR UNDERSTANDING AFTER PHASE 86:** You can explain every lesson title in Phase 86 with a concrete Odoo or business example.

> **Phase 86 complete?** [Continue to Phase 87](#phase-87)


<a id="phase-87"></a>
# PHASE 87 - Upgrade-Safe Customization

**Track:** Upgrades

**WHAT YOU WILL BE ABLE TO DO:** Prefer extension (`_inherit`, xpath, intentional APIs) over editing core files; avoid private method dependence; keep overrides thin and tested.

**WHAT YOU SHOULD KNOW FIRST:** Phases 32–35 — inheritance. Phase 81 — what breaks on upgrades.

**LEARNING RESOURCES:**

- [Inheritance](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html#inheritance)
- [View inheritance](https://www.odoo.com/documentation/18.0/developer/reference/user_interface/view_architectures.html)
- [odoo/odoo](https://github.com/odoo/odoo) — study how official modules extend each other

## 87.1 Never Modify Core Directly

**Never Modify Core Directly** belongs to **Upgrade-Safe Customization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Never Modify Core Directly** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Never Modify Core Directly** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Never Modify Core Directly**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Never Modify Core Directly** is concrete, continue — the next lesson in this phase still depends on it.

## 87.2 Extension Points

**Extension Points** belongs to **Upgrade-Safe Customization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Extension Points** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Extension Points** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Extension Points**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Extension Points** is concrete, continue — the next lesson in this phase still depends on it.

## 87.3 Model Inheritance

**Model Inheritance** belongs to **Upgrade-Safe Customization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Model Inheritance** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Model Inheritance** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Model Inheritance**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Model Inheritance** is concrete, continue — the next lesson in this phase still depends on it.

## 87.4 View Inheritance

**View Inheritance** belongs to **Upgrade-Safe Customization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **View Inheritance** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **View Inheritance** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **View Inheritance**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **View Inheritance** is concrete, continue — the next lesson in this phase still depends on it.

## 87.5 Stable Overrides

- Call `super()` unless you have a documented reason not to
- Prefer hooking documented methods / adding new methods called from buttons you own
- If you must override, write TransactionCase covering the flow (Story XV)
- JS patches: same rule — patch narrowly (Story XIII)

Upgrade-safe code is not “clever.” It is **boring, tested, and upstream-friendly.**

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Find one override in a sample module and judge if it calls `super()`. |
| Medium | Refactor a hypothetical core edit into an extension module outline. |
| Hard | Review a module that overrides `create` on `account.move` and list upgrade risks. |

**WHAT THIS UNLOCKS NEXT:** Real projects inherit **legacy** modules written before these rules. Debugging them is Phase 83.

---

<a id="phase-83"></a>

## 87.6 Dependency Management

**Dependency Management** belongs to **Upgrade-Safe Customization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Dependency Management** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Dependency Management** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Dependency Management**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Dependency Management** is concrete, continue — the next lesson in this phase still depends on it.

## 87.7 Avoiding Hard-Coding

**Avoiding Hard-Coding** belongs to **Upgrade-Safe Customization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Avoiding Hard-Coding** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Avoiding Hard-Coding** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Avoiding Hard-Coding**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Avoiding Hard-Coding** is concrete, continue — the next lesson in this phase still depends on it.

## 87.8 Future-Proof Design

**Future-Proof Design** belongs to **Upgrade-Safe Customization**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Future-Proof Design** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Future-Proof Design** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Future-Proof Design**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Future-Proof Design** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 87:** You can explain every lesson title in Phase 87 with a concrete Odoo or business example.

> **Phase 87 complete?** [Continue to Phase 88](#phase-88)


<a id="phase-88"></a>
# PHASE 88 - Legacy Module Maintenance

**Track:** Upgrades

**WHAT YOU WILL BE ABLE TO DO:** Triage old modules: map depends, find brittle inherits, isolate monkey patches, add characterization tests before changing behavior.

**WHAT YOU SHOULD KNOW FIRST:** Phase 64 — debugging. Phase 82 — what “good” looks like.

**LEARNING RESOURCES:**

- [Testing](https://www.odoo.com/documentation/18.0/developer/reference/backend/testing.html)
- [Logging / CLI](https://www.odoo.com/documentation/18.0/developer/reference/cli.html)

## 88.1 Read Before Changing

**Read Before Changing** belongs to **Legacy Module Maintenance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Read Before Changing** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Read Before Changing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Read Before Changing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Read Before Changing** is concrete, continue — the next lesson in this phase still depends on it.

## 88.2 Trace Dependencies

**Trace Dependencies** belongs to **Legacy Module Maintenance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Trace Dependencies** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Trace Dependencies** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Trace Dependencies**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Trace Dependencies** is concrete, continue — the next lesson in this phase still depends on it.

## 88.3 Trace Execution

**Trace Execution** belongs to **Legacy Module Maintenance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Trace Execution** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Trace Execution** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Trace Execution**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Trace Execution** is concrete, continue — the next lesson in this phase still depends on it.

## 88.4 Reproduce Existing Behavior

**Reproduce Existing Behavior** belongs to **Legacy Module Maintenance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Reproduce Existing Behavior** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Reproduce Existing Behavior** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Reproduce Existing Behavior**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Reproduce Existing Behavior** is concrete, continue — the next lesson in this phase still depends on it.

## 88.5 Characterization Tests

**Characterization Tests** belongs to **Legacy Module Maintenance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Characterization Tests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Characterization Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Characterization Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Characterization Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 88.6 Backward Compatibility

**Backward Compatibility** belongs to **Legacy Module Maintenance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Backward Compatibility** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Backward Compatibility** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Backward Compatibility**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Backward Compatibility** is concrete, continue — the next lesson in this phase still depends on it.

## 88.7 Safe Refactoring

**Safe Refactoring** belongs to **Legacy Module Maintenance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Safe Refactoring** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Safe Refactoring** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Safe Refactoring**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Safe Refactoring** is concrete, continue — the next lesson in this phase still depends on it.

## 88.8 Deprecation

**Deprecation** belongs to **Legacy Module Maintenance**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Deprecation** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Deprecation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Deprecation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Deprecation** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 88:** You can explain every lesson title in Phase 88 with a concrete Odoo or business example.

> **Phase 88 complete?** [Continue to Phase 89](#phase-89)


## STORY PROJECT — Upgrade an Existing Odoo Module

**Track:** Upgrades

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XXI — FUNCTIONAL ODOO FOR DEVELOPERS

**Track:** Functional Fluency

*An Odoo engineer should understand the business flows their code is changing.*

---
<a id="phase-89"></a>
<a id="phase-89---core-functional-applications"></a>
# PHASE 89 - Core Functional Applications

**Track:** Functional Fluency

**WHAT YOU WILL BE ABLE TO DO:** Explain, in business language, how CRM, Sales, Purchase, Inventory, Accounting concepts, and HR connect — enough to design with users, not only with models.

**WHAT YOU SHOULD KNOW FIRST:** Phase 3 — core apps. Phase 84 — requirements mapping.

**LEARNING RESOURCES:**

- Odoo 18 Applications documentation: CRM, Sales, Purchase, Inventory, Accounting, Employees
- [Developer docs](https://www.odoo.com/documentation/18.0/developer.html) when you need model names behind the UI
- Hands-on: run demo data and click the flows

## 89.1 Contacts

Functional fluency: **Contacts** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Contacts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Contacts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Contacts** is concrete, continue — the next lesson in this phase still depends on it.

## 89.2 CRM

Functional fluency: **CRM** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **CRM** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **CRM**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **CRM** is concrete, continue — the next lesson in this phase still depends on it.

## 89.3 Sales

Functional fluency: **Sales** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Sales** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Sales**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Sales** is concrete, continue — the next lesson in this phase still depends on it.

## 89.4 Purchase

Functional fluency: **Purchase** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Purchase** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Purchase**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Purchase** is concrete, continue — the next lesson in this phase still depends on it.

## 89.5 Inventory

Functional fluency: **Inventory** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Inventory** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Inventory**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Inventory** is concrete, continue — the next lesson in this phase still depends on it.

## 89.6 Accounting Concepts

Functional fluency: **Accounting Concepts** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Accounting Concepts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Accounting Concepts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Accounting Concepts** is concrete, continue — the next lesson in this phase still depends on it.

## 89.7 Invoicing

Functional fluency: **Invoicing** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Invoicing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Invoicing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Invoicing** is concrete, continue — the next lesson in this phase still depends on it.

## 89.8 Employees

Functional fluency: **Employees** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Employees** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Employees**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Employees** is concrete, continue — the next lesson in this phase still depends on it.

## 89.9 Recruitment

Functional fluency: **Recruitment** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Recruitment** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Recruitment**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Recruitment** is concrete, continue — the next lesson in this phase still depends on it.

## 89.10 Time Off

Functional fluency: **Time Off** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Time Off** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Time Off**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Time Off** is concrete, continue — the next lesson in this phase still depends on it.

## 89.11 Projects

Functional fluency: **Projects** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **Projects** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **Projects** is concrete, continue — the next lesson in this phase still depends on it.

## 89.12 Timesheets

Functional fluency: **Timesheets** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Timesheets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Timesheets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Timesheets** is concrete, continue — the next lesson in this phase still depends on it.

## 89.13 Manufacturing

Functional fluency: **Manufacturing** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Manufacturing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Manufacturing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Manufacturing** is concrete, continue — the next lesson in this phase still depends on it.

## 89.14 Maintenance

Functional fluency: **Maintenance** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Maintenance** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Maintenance**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Maintenance** is concrete, continue — the next lesson in this phase still depends on it.

## 89.15 Helpdesk Concept

Functional fluency: **Helpdesk Concept** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Helpdesk Concept** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Helpdesk Concept**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Helpdesk Concept** is concrete, continue — the next lesson in this phase still depends on it.

## 89.16 Website

Functional fluency: **Website** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Website** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Website**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Website** is concrete, continue — the next lesson in this phase still depends on it.

## 89.17 eCommerce

Functional fluency: **eCommerce** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **eCommerce** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **eCommerce**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **eCommerce** is concrete, continue — the next lesson in this phase still depends on it.

## 89.18 Point of Sale

Functional fluency: **Point of Sale** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Point of Sale** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Point of Sale**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Point of Sale** is concrete, continue — the next lesson in this phase still depends on it.

## 89.19 End-to-End Business Flow

Functional fluency: **End-to-End Business Flow** is an official Odoo application (or a flow across apps). Click it on a demo database until you can name the main documents and what confirming them creates next.

Developers who skip this invent duplicate models. Your job is to extend the document that already owns the fact.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **End-to-End Business Flow** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **End-to-End Business Flow**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **End-to-End Business Flow** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 89:** You can explain every lesson title in Phase 89 with a concrete Odoo or business example.

> **Phase 89 complete?** [Continue to Phase 90](#phase-90)


## STORY PROJECT — Trace a Company End-to-End

**Track:** Functional Fluency

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XXII — REAL ODOO ENGINEERING

**Track:** Engineering Practice

*Judge trade-offs before you write models.*

---
<a id="phase-90"></a>
<a id="phase-90---requirements-erp-design"></a>
# PHASE 90 - Requirements → ERP Design

**Track:** Engineering Practice

**WHAT YOU WILL BE ABLE TO DO:** Interview stakeholders, map processes to Odoo apps, identify master data vs transactions, and produce a design that names modules and ownership — before writing models.

**WHAT YOU SHOULD KNOW FIRST:** Story I — ERP thinking. Phase 3 — core apps flow. Stories IV–IX — what custom modules can do.

**LEARNING RESOURCES:**

- [Odoo functional apps overview](https://www.odoo.com/documentation/18.0/) (Applications section)
- [`Projects.md`](../guides/Projects.md) — process-map style deliverables
- [`Interview.md`](../guides/Interview.md) — explaining design under pressure

## 90.1 Stakeholder Interviews

**Stakeholder Interviews** belongs to **Requirements → ERP Design**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Stakeholder Interviews** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Stakeholder Interviews** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Stakeholder Interviews**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Stakeholder Interviews** is concrete, continue — the next lesson in this phase still depends on it.

## 90.2 Business Requirements

| Statement | Engineer hears |
| --- | --- |
| “Like Excel but shared” | Need audit trail, concurrency, permissions |
| “Just one more field” | Often a new process hiding |
| “Copy our old system” | Migrate process debt |
| “Make it flexible” | Undefined rules — push for examples |

Always collect **examples**: three real documents, three edge cases, one “this must never happen.”

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Turn “manage company cars” into master data vs transactions. |
| Medium | Interview script for a leave-approval ERP feature (10 questions). |
| Hard | Write a one-page design for quote→delivery→invoice with two companies sharing a catalog. |

**WHAT THIS UNLOCKS NEXT:** Design implies **module architecture** — how addons split and depend.

---

<a id="phase-85"></a>

## 90.3 Functional Requirements

| Statement | Engineer hears |
| --- | --- |
| “Like Excel but shared” | Need audit trail, concurrency, permissions |
| “Just one more field” | Often a new process hiding |
| “Copy our old system” | Migrate process debt |
| “Make it flexible” | Undefined rules — push for examples |

Always collect **examples**: three real documents, three edge cases, one “this must never happen.”

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Turn “manage company cars” into master data vs transactions. |
| Medium | Interview script for a leave-approval ERP feature (10 questions). |
| Hard | Write a one-page design for quote→delivery→invoice with two companies sharing a catalog. |

**WHAT THIS UNLOCKS NEXT:** Design implies **module architecture** — how addons split and depend.

---

<a id="phase-85"></a>

## 90.4 Non-Functional Requirements

| Statement | Engineer hears |
| --- | --- |
| “Like Excel but shared” | Need audit trail, concurrency, permissions |
| “Just one more field” | Often a new process hiding |
| “Copy our old system” | Migrate process debt |
| “Make it flexible” | Undefined rules — push for examples |

Always collect **examples**: three real documents, three edge cases, one “this must never happen.”

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Turn “manage company cars” into master data vs transactions. |
| Medium | Interview script for a leave-approval ERP feature (10 questions). |
| Hard | Write a one-page design for quote→delivery→invoice with two companies sharing a catalog. |

**WHAT THIS UNLOCKS NEXT:** Design implies **module architecture** — how addons split and depend.

---

<a id="phase-85"></a>

## 90.5 Business Process Mapping

**Business Process Mapping** belongs to **Requirements → ERP Design**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Business Process Mapping** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Business Process Mapping** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Business Process Mapping**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Business Process Mapping** is concrete, continue — the next lesson in this phase still depends on it.

## 90.6 As-Is Process

A client says: “We need a better approval for vendor bills.” A junior opens `models/` and invents `x_approval`. An engineer asks:

- Who requests, who approves, who pays?
- Is this Accounting, Purchase, or a custom committee?
- Does standard Odoo already cover it with approvals / studio / tiers?
- What reporting must remain true for auditors?

**Design artifacts (lightweight):**

```text
Process map:  request → approve → post → pay
Master data:  vendors, products, analytic accounts
Transactions: bills, payments
Apps:         purchase? account? hr? custom?
Risks:        multi-company, tax, existing custom modules
```

Only then: module boundaries (Phase 85) and config vs code (Phase 86).

## 90.7 To-Be Process

**To-Be Process** belongs to **Requirements → ERP Design**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **To-Be Process** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **To-Be Process** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **To-Be Process**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **To-Be Process** is concrete, continue — the next lesson in this phase still depends on it.

## 90.8 Data Requirements

| Statement | Engineer hears |
| --- | --- |
| “Like Excel but shared” | Need audit trail, concurrency, permissions |
| “Just one more field” | Often a new process hiding |
| “Copy our old system” | Migrate process debt |
| “Make it flexible” | Undefined rules — push for examples |

Always collect **examples**: three real documents, three edge cases, one “this must never happen.”

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Turn “manage company cars” into master data vs transactions. |
| Medium | Interview script for a leave-approval ERP feature (10 questions). |
| Hard | Write a one-page design for quote→delivery→invoice with two companies sharing a catalog. |

**WHAT THIS UNLOCKS NEXT:** Design implies **module architecture** — how addons split and depend.

---

<a id="phase-85"></a>

## 90.9 Roles

**Roles** belongs to **Requirements → ERP Design**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Roles** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Roles** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Roles**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Roles** is concrete, continue — the next lesson in this phase still depends on it.

## 90.10 Permissions

**Permissions** belongs to **Requirements → ERP Design**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Permissions** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Permissions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Permissions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Permissions** is concrete, continue — the next lesson in this phase still depends on it.

## 90.11 Acceptance Criteria

**Acceptance Criteria** belongs to **Requirements → ERP Design**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Acceptance Criteria** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Acceptance Criteria** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Acceptance Criteria**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Acceptance Criteria** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 90:** You can explain every lesson title in Phase 90 with a concrete Odoo or business example.

> **Phase 90 complete?** [Continue to Phase 91](#phase-91)


<a id="phase-91"></a>
# PHASE 91 - Odoo Solution Architecture

**Track:** Engineering Practice

**WHAT YOU WILL BE ABLE TO DO:** Split features into addons with a clean `depends` graph; avoid god-modules; separate glue from core business.

**WHAT YOU SHOULD KNOW FIRST:** Phase 7–8 — anatomy and depends. Phase 84 — requirements map.

**LEARNING RESOURCES:**

- [Modules](https://www.odoo.com/documentation/18.0/developer/reference/backend/module.html)
- Study `depends` in [odoo/odoo](https://github.com/odoo/odoo) official apps

## 91.1 Module Boundaries

**Module Boundaries** belongs to **Odoo Solution Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Module Boundaries** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Module Boundaries** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Module Boundaries**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Module Boundaries** is concrete, continue — the next lesson in this phase still depends on it.

## 91.2 Model Ownership

**Model Ownership** belongs to **Odoo Solution Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Model Ownership** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Model Ownership** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Model Ownership**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Model Ownership** is concrete, continue — the next lesson in this phase still depends on it.

## 91.3 Dependencies

**Dependencies** belongs to **Odoo Solution Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Dependencies** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Dependencies** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Dependencies**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Dependencies** is concrete, continue — the next lesson in this phase still depends on it.

## 91.4 Shared Models

**Shared Models** belongs to **Odoo Solution Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Shared Models** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Shared Models** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Shared Models**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Shared Models** is concrete, continue — the next lesson in this phase still depends on it.

## 91.5 Integration Boundaries

**Integration Boundaries** belongs to **Odoo Solution Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Integration Boundaries** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Integration Boundaries** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Integration Boundaries**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Integration Boundaries** is concrete, continue — the next lesson in this phase still depends on it.

## 91.6 Security Architecture

```text
BAD:  everything_custom  depends on  sale, stock, account, hr, website, ...
GOOD:
  my_partner_ext          → base
  my_sale_approval        → sale, my_partner_ext
  my_sale_stock_bridge    → my_sale_approval, stock
```

Small modules:

- Install only what a DB needs
- Upgrade blast radius stays small
- Tests stay focused
- Teams can own areas

**Glue modules** connect two apps without forcing every DB to install both stacks until needed.

## 91.7 Multi-Company Architecture

```text
BAD:  everything_custom  depends on  sale, stock, account, hr, website, ...
GOOD:
  my_partner_ext          → base
  my_sale_approval        → sale, my_partner_ext
  my_sale_stock_bridge    → my_sale_approval, stock
```

Small modules:

- Install only what a DB needs
- Upgrade blast radius stays small
- Tests stay focused
- Teams can own areas

**Glue modules** connect two apps without forcing every DB to install both stacks until needed.

## 91.8 Data Flow

**Data Flow** belongs to **Odoo Solution Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Data Flow** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Data Flow** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Data Flow**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Data Flow** is concrete, continue — the next lesson in this phase still depends on it.

## 91.9 Reusability

**Reusability** belongs to **Odoo Solution Architecture**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Reusability** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Reusability** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Reusability**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Reusability** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 91:** You can explain every lesson title in Phase 91 with a concrete Odoo or business example.

> **Phase 91 complete?** [Continue to Phase 92](#phase-92)


<a id="phase-92"></a>
# PHASE 92 - Configuration vs Customization vs Integration

**Track:** Engineering Practice

**WHAT YOU WILL BE ABLE TO DO:** Decide when settings, automated actions, studio/optional tools, or standard workflows beat writing Python — and when they do not.

**WHAT YOU SHOULD KNOW FIRST:** Phase 39 — automated actions. Phase 84 — requirements.

**LEARNING RESOURCES:**

- Odoo Applications documentation for the apps you configure
- [Automated actions / studio](https://www.odoo.com/documentation/18.0/) (edition-dependent features — verify what your edition includes)

## 92.1 Standard Odoo

**Standard Odoo** belongs to **Configuration vs Customization vs Integration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Standard Odoo** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Standard Odoo** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Standard Odoo**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Standard Odoo** is concrete, continue — the next lesson in this phase still depends on it.

## 92.2 Configuration

Prefer configuration when:

- Standard fields/workflows already express the rule
- Business users must change thresholds without deploys
- The rule is simple (email on stage change, required field via form)

Prefer **customization** when:

- Invariants must be enforced server-side with tests
- Performance-sensitive logic
- Complex multi-model transactions
- Security must not rely on a UI-only rule
- You need upgrade-safe, reviewable code in Git

```text
“Discount over 20% needs manager”
  → try standard approval / activity / group button first
  → customize if audit + multi-company + pricing engine demand it
```

## 92.3 Studio Concepts

**Studio Concepts** belongs to **Configuration vs Customization vs Integration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Studio Concepts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Studio Concepts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Studio Concepts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Studio Concepts** is concrete, continue — the next lesson in this phase still depends on it.

## 92.4 Custom Module

**Custom Module** belongs to **Configuration vs Customization vs Integration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Custom Module** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Custom Module** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Custom Module**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Custom Module** is concrete, continue — the next lesson in this phase still depends on it.

## 92.5 Third-Party Module

**Third-Party Module** belongs to **Configuration vs Customization vs Integration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Third-Party Module** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Third-Party Module** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Third-Party Module**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Third-Party Module** is concrete, continue — the next lesson in this phase still depends on it.

## 92.6 External Integration

- Auth, retries, idempotency (Phase 49)
- Mapping and reconciliation
- Partial failure UX
- Monitoring and replay tools
- Duplicate prevention

**Bad compromise:** half-building a payment gateway inside Odoo “to avoid Stripe fees.”  
**Good compromise:** Odoo owns orders/invoices; PSP owns card data; webhooks reconcile.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | For “sync HubSpot leads,” say who owns the lead and why. |
| Medium | Design ownership for inventory between Odoo and an external WMS. |
| Hard | Write an ADR: customize vs integrate for tax calculation in 3 countries. |

**WHAT THIS UNLOCKS NEXT:** Whatever you build, someone else must read it next year — maintainable Odoo code.

---

<a id="phase-88"></a>

## 92.7 Data Ownership

**Data Ownership** belongs to **Configuration vs Customization vs Integration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Data Ownership** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Data Ownership** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Data Ownership**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Data Ownership** is concrete, continue — the next lesson in this phase still depends on it.

## 92.8 Upgrade Costs

- Auth, retries, idempotency (Phase 49)
- Mapping and reconciliation
- Partial failure UX
- Monitoring and replay tools
- Duplicate prevention

**Bad compromise:** half-building a payment gateway inside Odoo “to avoid Stripe fees.”  
**Good compromise:** Odoo owns orders/invoices; PSP owns card data; webhooks reconcile.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | For “sync HubSpot leads,” say who owns the lead and why. |
| Medium | Design ownership for inventory between Odoo and an external WMS. |
| Hard | Write an ADR: customize vs integrate for tax calculation in 3 countries. |

**WHAT THIS UNLOCKS NEXT:** Whatever you build, someone else must read it next year — maintainable Odoo code.

---

<a id="phase-88"></a>

## 92.9 Maintenance Costs

- Auth, retries, idempotency (Phase 49)
- Mapping and reconciliation
- Partial failure UX
- Monitoring and replay tools
- Duplicate prevention

**Bad compromise:** half-building a payment gateway inside Odoo “to avoid Stripe fees.”  
**Good compromise:** Odoo owns orders/invoices; PSP owns card data; webhooks reconcile.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | For “sync HubSpot leads,” say who owns the lead and why. |
| Medium | Design ownership for inventory between Odoo and an external WMS. |
| Hard | Write an ADR: customize vs integrate for tax calculation in 3 countries. |

**WHAT THIS UNLOCKS NEXT:** Whatever you build, someone else must read it next year — maintainable Odoo code.

---

<a id="phase-88"></a>

## 92.10 Choosing the Simplest Correct Solution

**Choosing the Simplest Correct Solution** belongs to **Configuration vs Customization vs Integration**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Choosing the Simplest Correct Solution** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Choosing the Simplest Correct Solution** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Choosing the Simplest Correct Solution**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Choosing the Simplest Correct Solution** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 92:** You can explain every lesson title in Phase 92 with a concrete Odoo or business example.

> **Phase 92 complete?** [Continue to Phase 93](#phase-93)


<a id="phase-93"></a>
# PHASE 93 - Maintainable Odoo Code

**Track:** Engineering Practice

**WHAT YOU WILL BE ABLE TO DO:** Write modules that future teammates can navigate: clear models, thin controllers, tested business methods, honest names, and no cleverness that fights the ORM.

**WHAT YOU SHOULD KNOW FIRST:** Stories IV–VI, XV — craft + tests. Phase 82 — upgrade-safe patterns.

**LEARNING RESOURCES:**

- [ORM API](https://www.odoo.com/documentation/18.0/developer/reference/backend/orm.html)
- [Testing](https://www.odoo.com/documentation/18.0/developer/reference/backend/testing.html)

## 93.1 Naming

**Naming** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Naming** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Naming** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Naming**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Naming** is concrete, continue — the next lesson in this phase still depends on it.

## 93.2 Small Methods

**Small Methods** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Small Methods** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Small Methods** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Small Methods**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Small Methods** is concrete, continue — the next lesson in this phase still depends on it.

## 93.3 Separation of Concerns

**Separation of Concerns** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Separation of Concerns** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Separation of Concerns** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Separation of Concerns**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Separation of Concerns** is concrete, continue — the next lesson in this phase still depends on it.

## 93.4 Cohesion

**Cohesion** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Cohesion** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Cohesion** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Cohesion**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Cohesion** is concrete, continue — the next lesson in this phase still depends on it.

## 93.5 Low Coupling

**Low Coupling** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Low Coupling** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Low Coupling** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Low Coupling**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Low Coupling** is concrete, continue — the next lesson in this phase still depends on it.

## 93.6 Reusable Mixins

**Reusable Mixins** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Reusable Mixins** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Reusable Mixins** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Reusable Mixins**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Reusable Mixins** is concrete, continue — the next lesson in this phase still depends on it.

## 93.7 Configuration over Hard-Coding

**Configuration over Hard-Coding** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Configuration over Hard-Coding** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Configuration over Hard-Coding** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Configuration over Hard-Coding**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Configuration over Hard-Coding** is concrete, continue — the next lesson in this phase still depends on it.

## 93.8 Constants

**Constants** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Constants** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Constants** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Constants**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Constants** is concrete, continue — the next lesson in this phase still depends on it.

## 93.9 Error Handling

**Error Handling** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Error Handling** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Error Handling** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Error Handling**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Error Handling** is concrete, continue — the next lesson in this phase still depends on it.

## 93.10 Logging

**Logging** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Logging** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Logging** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Logging**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Logging** is concrete, continue — the next lesson in this phase still depends on it.

## 93.11 Documentation

README: purpose, depends, config steps, known limits.  
Code comments: *why*, not *what*.  
Migration notes: when you bump versions.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Rename three vague methods on a sample module to business verbs. |
| Medium | Split a fat `write()` override into helpers with tests. |
| Hard | Review a module and produce a maintainability debt list prioritized by upgrade risk. |

**WHAT THIS UNLOCKS NEXT:** Maintainable code still needs a **Git workflow** so teams do not overwrite each other.

---

<a id="phase-89"></a>

## 93.12 Upgrade Safety

**Upgrade Safety** belongs to **Maintainable Odoo Code**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Upgrade Safety** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Upgrade Safety** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Upgrade Safety**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Upgrade Safety** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 93:** You can explain every lesson title in Phase 93 with a concrete Odoo or business example.

> **Phase 93 complete?** [Continue to Phase 94](#phase-94)


<a id="phase-94"></a>
# PHASE 94 - Git, Collaboration & Code Review

**Track:** Engineering Practice

**WHAT YOU WILL BE ABLE TO DO:** Review Odoo PRs for security, upgrade risk, performance, XML/data hazards, and missing tests — not just style.

**WHAT YOU SHOULD KNOW FIRST:** Phases 27–31 — security. Phase 82 — upgrade safety. Story XIV — performance.

**LEARNING RESOURCES:**

- [Security](https://www.odoo.com/documentation/18.0/developer/reference/backend/security.html)
- [`Interview.md`](../guides/Interview.md) — explaining trade-offs

## 94.1 Repository

**Repository** belongs to **Git, Collaboration & Code Review**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Repository** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Repository** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Repository**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Repository** is concrete, continue — the next lesson in this phase still depends on it.

## 94.2 Branches

**Branches** belongs to **Git, Collaboration & Code Review**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Branches** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Branches** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Branches**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Branches** is concrete, continue — the next lesson in this phase still depends on it.

## 94.3 Commits

**Commits** belongs to **Git, Collaboration & Code Review**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Commits** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Commits** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Commits**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Commits** is concrete, continue — the next lesson in this phase still depends on it.

## 94.4 Pull Requests

**Pull Requests** belongs to **Git, Collaboration & Code Review**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Pull Requests** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Pull Requests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Pull Requests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Pull Requests** is concrete, continue — the next lesson in this phase still depends on it.

## 94.5 Merge

**Merge** belongs to **Git, Collaboration & Code Review**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Merge** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Merge** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Merge**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Merge** is concrete, continue — the next lesson in this phase still depends on it.

## 94.6 Rebase Concepts

**Rebase Concepts** belongs to **Git, Collaboration & Code Review**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Rebase Concepts** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Rebase Concepts** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Rebase Concepts**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Rebase Concepts** is concrete, continue — the next lesson in this phase still depends on it.

## 94.7 Release Branches

**Release Branches** belongs to **Git, Collaboration & Code Review**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Release Branches** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Release Branches** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Release Branches**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Release Branches** is concrete, continue — the next lesson in this phase still depends on it.

## 94.8 Code Review for Correctness

**Code Review for Correctness** belongs to **Git, Collaboration & Code Review**. Treat it as a mechanism in Odoo 18: a model API, an XML record, a CLI flag, or an operational rule — something you can point at.

Work a small example on your local database. Prefer official docs and [odoo/odoo](https://github.com/odoo/odoo) over blog copy. When you can teach **Code Review for Correctness** with a concrete record or file path, you are done with this lesson.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Code Review for Correctness** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Code Review for Correctness**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Code Review for Correctness** is concrete, continue — the next lesson in this phase still depends on it.

## 94.9 Security Review

Ask, every PR:

1. **ACL / record rules** — new models covered? Portal-safe?
2. **`sudo()`** — justified? Still scoped?
3. **Multi-company** — `company_id` checked?
4. **`super()`** — called on overrides?
5. **Migrations** — needed for field renames?
6. **XML** — `noupdate`, xpath fragility?
7. **Performance** — searches in loops?
8. **Tests** — AccessError and business path covered?
9. **Dependencies** — manifest `depends` complete?

A polite “looks fine” without these checks is not a review.

## 94.10 ORM Performance Review

Ask, every PR:

1. **ACL / record rules** — new models covered? Portal-safe?
2. **`sudo()`** — justified? Still scoped?
3. **Multi-company** — `company_id` checked?
4. **`super()`** — called on overrides?
5. **Migrations** — needed for field renames?
6. **XML** — `noupdate`, xpath fragility?
7. **Performance** — searches in loops?
8. **Tests** — AccessError and business path covered?
9. **Dependencies** — manifest `depends` complete?

A polite “looks fine” without these checks is not a review.

## 94.11 Upgrade-Safety Review

Ask, every PR:

1. **ACL / record rules** — new models covered? Portal-safe?
2. **`sudo()`** — justified? Still scoped?
3. **Multi-company** — `company_id` checked?
4. **`super()`** — called on overrides?
5. **Migrations** — needed for field renames?
6. **XML** — `noupdate`, xpath fragility?
7. **Performance** — searches in loops?
8. **Tests** — AccessError and business path covered?
9. **Dependencies** — manifest `depends` complete?

A polite “looks fine” without these checks is not a review.

## 94.12 Test Review

Ask, every PR:

1. **ACL / record rules** — new models covered? Portal-safe?
2. **`sudo()`** — justified? Still scoped?
3. **Multi-company** — `company_id` checked?
4. **`super()`** — called on overrides?
5. **Migrations** — needed for field renames?
6. **XML** — `noupdate`, xpath fragility?
7. **Performance** — searches in loops?
8. **Tests** — AccessError and business path covered?
9. **Dependencies** — manifest `depends` complete?

A polite “looks fine” without these checks is not a review.

## 94.13 Documentation Review

Ask, every PR:

1. **ACL / record rules** — new models covered? Portal-safe?
2. **`sudo()`** — justified? Still scoped?
3. **Multi-company** — `company_id` checked?
4. **`super()`** — called on overrides?
5. **Migrations** — needed for field renames?
6. **XML** — `noupdate`, xpath fragility?
7. **Performance** — searches in loops?
8. **Tests** — AccessError and business path covered?
9. **Dependencies** — manifest `depends` complete?

A polite “looks fine” without these checks is not a review.


---


**CHECK YOUR UNDERSTANDING AFTER PHASE 94:** You can explain every lesson title in Phase 94 with a concrete Odoo or business example.

> **Phase 94 complete?** [Continue to Phase 95](#phase-95)


## STORY PROJECT — Design a Production ERP Solution

**Track:** Engineering Practice

Ship this story's closing deliverable before moving on. Keep the work under `odoo/` in your portfolio repo and tie it to [`Projects.md`](../guides/Projects.md) when a matching card exists.

**Acceptance:** another Odoo engineer can understand the business intent, open the module, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** The next story builds on this deliverable — do not skip it.

---
---

# STORY XXIII — GRAND ODOO CAPSTONE

**Track:** Capstone

*Stop learning isolated features. Engineer the whole ERP.*

---
<a id="phase-95"></a>
<a id="phase-95---production-erp-capstone"></a>
# PHASE 95 - Production ERP Capstone

**Track:** Capstone

**WHAT YOU WILL BE ABLE TO DO:** Ship a coherent portfolio of Odoo modules (with README, tests, screenshots/GIF, upgrade notes) that prove backend, extension, integration, and OWL skills — then complete the final business ERP capstone.

**WHAT YOU SHOULD KNOW FIRST:** Stories III–XVIII craft. [`Projects.md`](../guides/Projects.md) for structured builds.

**LEARNING RESOURCES:**

- [`Projects.md`](../guides/Projects.md)
- [`Interview.md`](../guides/Interview.md)
- [Testing reference](https://www.odoo.com/documentation/18.0/developer/reference/backend/testing.html)
- [odoo/docker](https://github.com/odoo/docker) / [source install](https://www.odoo.com/documentation/18.0/administration/on_premise/source.html) for demo environments

## 95.1 Problem Discovery

In the **Production ERP Capstone**, **Problem Discovery** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Problem Discovery
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Problem Discovery** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Problem Discovery**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Problem Discovery** is concrete, continue — the next lesson in this phase still depends on it.

## 95.2 Business Requirements

```text
CRM opportunity won
        |
        v
Sales quotation confirmed → Sales Order
        |
        +--→ Inventory delivery (pick/pack/ship)
        |
        +--→ Customer invoice (Accounting)
                |
                v
            Payment & reconciliation

Parallel: Purchase replenishment when stock rules fire
```

Practice narrating this without opening code. Then open `sale.order` / `stock.picking` / `account.move` and match the UI labels to models.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Define lead vs opportunity vs quotation in one sentence each. |
| Medium | Walk purchase→receipt→bill on a demo DB; list documents created. |
| Hard | Explain to a fictional CFO why customizing `account.move` posting needs extra care. |

**WHAT THIS UNLOCKS NEXT:** Functional maps help you **read existing Odoo code** with purpose instead of drowning in `addons/`.

---

<a id="phase-92"></a>

## 95.3 Process Maps

In the **Production ERP Capstone**, **Process Maps** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Process Maps
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Process Maps** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Process Maps**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Process Maps** is concrete, continue — the next lesson in this phase still depends on it.

## 95.4 Architecture

In the **Production ERP Capstone**, **Architecture** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Architecture
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Architecture** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Architecture**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Architecture** is concrete, continue — the next lesson in this phase still depends on it.

## 95.5 Module Boundaries

In the **Production ERP Capstone**, **Module Boundaries** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Module Boundaries
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Module Boundaries** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Module Boundaries**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Module Boundaries** is concrete, continue — the next lesson in this phase still depends on it.

## 95.6 Data Models

In the **Production ERP Capstone**, **Data Models** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Data Models
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Data Models** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Data Models**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Data Models** is concrete, continue — the next lesson in this phase still depends on it.

## 95.7 Relational Design

In the **Production ERP Capstone**, **Relational Design** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Relational Design
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Relational Design** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Relational Design**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Relational Design** is concrete, continue — the next lesson in this phase still depends on it.

## 95.8 ORM

In the **Production ERP Capstone**, **ORM** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── ORM
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ORM** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ORM**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ORM** is concrete, continue — the next lesson in this phase still depends on it.

## 95.9 Business Rules

```text
CRM opportunity won
        |
        v
Sales quotation confirmed → Sales Order
        |
        +--→ Inventory delivery (pick/pack/ship)
        |
        +--→ Customer invoice (Accounting)
                |
                v
            Payment & reconciliation

Parallel: Purchase replenishment when stock rules fire
```

Practice narrating this without opening code. Then open `sale.order` / `stock.picking` / `account.move` and match the UI labels to models.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Define lead vs opportunity vs quotation in one sentence each. |
| Medium | Walk purchase→receipt→bill on a demo DB; list documents created. |
| Hard | Explain to a fictional CFO why customizing `account.move` posting needs extra care. |

**WHAT THIS UNLOCKS NEXT:** Functional maps help you **read existing Odoo code** with purpose instead of drowning in `addons/`.

---

<a id="phase-92"></a>

## 95.10 Security

In the **Production ERP Capstone**, **Security** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Security
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Security** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Security**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Security** is concrete, continue — the next lesson in this phase still depends on it.

## 95.11 Multi-Company

In the **Production ERP Capstone**, **Multi-Company** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Multi-Company
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Multi-Company** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Multi-Company**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Multi-Company** is concrete, continue — the next lesson in this phase still depends on it.

## 95.12 Views

In the **Production ERP Capstone**, **Views** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Views
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Views** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Views**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Views** is concrete, continue — the next lesson in this phase still depends on it.

## 95.13 Workflows

In the **Production ERP Capstone**, **Workflows** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Workflows
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Workflows** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Workflows**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Workflows** is concrete, continue — the next lesson in this phase still depends on it.

## 95.14 Wizards

In the **Production ERP Capstone**, **Wizards** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Wizards
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Wizards** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Wizards**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Wizards** is concrete, continue — the next lesson in this phase still depends on it.

## 95.15 Chatter & Activities

In the **Production ERP Capstone**, **Chatter & Activities** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Chatter & Activities
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Chatter & Activities** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Chatter & Activities**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Chatter & Activities** is concrete, continue — the next lesson in this phase still depends on it.

## 95.16 Automation

In the **Production ERP Capstone**, **Automation** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Automation
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Automation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Automation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Automation** is concrete, continue — the next lesson in this phase still depends on it.

## 95.17 Reports

In the **Production ERP Capstone**, **Reports** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Reports
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Reports** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Reports**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Reports** is concrete, continue — the next lesson in this phase still depends on it.

## 95.18 Attachments

In the **Production ERP Capstone**, **Attachments** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Attachments
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Attachments** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Attachments**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Attachments** is concrete, continue — the next lesson in this phase still depends on it.

## 95.19 Translations

In the **Production ERP Capstone**, **Translations** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Translations
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Translations** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Translations**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Translations** is concrete, continue — the next lesson in this phase still depends on it.

## 95.20 Website

In the **Production ERP Capstone**, **Website** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Website
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Website** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Website**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Website** is concrete, continue — the next lesson in this phase still depends on it.

## 95.21 Portal

In the **Production ERP Capstone**, **Portal** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Portal
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Portal** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Portal**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Portal** is concrete, continue — the next lesson in this phase still depends on it.

## 95.22 API

In the **Production ERP Capstone**, **API** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── API
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **API** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **API**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **API** is concrete, continue — the next lesson in this phase still depends on it.

## 95.23 Webhooks

In the **Production ERP Capstone**, **Webhooks** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Webhooks
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Webhooks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Webhooks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Webhooks** is concrete, continue — the next lesson in this phase still depends on it.

## 95.24 External Integration

In the **Production ERP Capstone**, **External Integration** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── External Integration
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **External Integration** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **External Integration**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **External Integration** is concrete, continue — the next lesson in this phase still depends on it.

## 95.25 OWL Dashboard

In the **Production ERP Capstone**, **OWL Dashboard** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── OWL Dashboard
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **OWL Dashboard** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **OWL Dashboard**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **OWL Dashboard** is concrete, continue — the next lesson in this phase still depends on it.

## 95.26 PostgreSQL Optimization

In the **Production ERP Capstone**, **PostgreSQL Optimization** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── PostgreSQL Optimization
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **PostgreSQL Optimization** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **PostgreSQL Optimization**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **PostgreSQL Optimization** is concrete, continue — the next lesson in this phase still depends on it.

## 95.27 Automated Tests

In the **Production ERP Capstone**, **Automated Tests** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Automated Tests
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Automated Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Automated Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Automated Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 95.28 Security Tests

In the **Production ERP Capstone**, **Security Tests** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Security Tests
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Security Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Security Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Security Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 95.29 Performance Tests

In the **Production ERP Capstone**, **Performance Tests** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Performance Tests
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Performance Tests** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Performance Tests**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Performance Tests** is concrete, continue — the next lesson in this phase still depends on it.

## 95.30 Migration Strategy

In the **Production ERP Capstone**, **Migration Strategy** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Migration Strategy
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Migration Strategy** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Migration Strategy**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Migration Strategy** is concrete, continue — the next lesson in this phase still depends on it.

## 95.31 Backup Strategy

In the **Production ERP Capstone**, **Backup Strategy** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Backup Strategy
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Backup Strategy** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Backup Strategy**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Backup Strategy** is concrete, continue — the next lesson in this phase still depends on it.

## 95.32 Deployment

In the **Production ERP Capstone**, **Deployment** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Deployment
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Deployment** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Deployment**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Deployment** is concrete, continue — the next lesson in this phase still depends on it.

## 95.33 Monitoring

In the **Production ERP Capstone**, **Monitoring** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Monitoring
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Monitoring** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Monitoring**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Monitoring** is concrete, continue — the next lesson in this phase still depends on it.

## 95.34 README

In the **Production ERP Capstone**, **README** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── README
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **README** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **README**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **README** is concrete, continue — the next lesson in this phase still depends on it.

## 95.35 Architecture Diagram

In the **Production ERP Capstone**, **Architecture Diagram** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Architecture Diagram
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Architecture Diagram** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Architecture Diagram**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Architecture Diagram** is concrete, continue — the next lesson in this phase still depends on it.

## 95.36 Technical Documentation

In the **Production ERP Capstone**, **Technical Documentation** is a required slice of the complete business system — not a side note.

Write it down as a deliverable: what you will ship, which models/views/tests prove it, and what a reviewer should click. Reuse earlier story projects instead of inventing a parallel ERP. If this slice touches money, stock, or personal data, name the security and upgrade risk in the README.

```text
capstone ERP
   └── Technical Documentation
          ├── implementation
          ├── test/proof
          └── ops note
```

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Technical Documentation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Technical Documentation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Technical Documentation** is concrete, continue — the next phase still depends on it.


**CHECK YOUR UNDERSTANDING AFTER PHASE 95:** You can explain every lesson title in Phase 95 with a concrete Odoo or business example.

> **Phase 95 complete?** [Continue to Phase 96](#phase-96)


## STORY PROJECT — COMPLETE BUSINESS ERP

**Track:** Capstone

Ship one production-shaped ERP (extend official apps where they already own the fact; custom modules only for the gap). Keep the work under `odoo/` and the Phase 95 card in [`Projects.md`](../guides/Projects.md).

The system must cover:

- CRM and customers
- Sales and purchasing
- Inventory
- Employees
- Roles, permissions, and multi-company
- Approval workflows
- Chatter, activities, reports, scheduled automation, attachments
- Customer portal
- External API, webhooks, and one third-party integration
- OWL dashboard
- Translations
- Tests, security tests, and performance proof
- Migration notes, backups, monitoring, and a production-style deploy story

**Acceptance:** another Odoo engineer can understand the business intent, install the modules, and see that security, tests, and upgrade safety were not left for "later."

**WHAT COMES NEXT:** Story XXIV — interview, portfolio walkthrough, and job readiness.

---
---

# STORY XXIV — INTERVIEW, PORTFOLIO & JOB READINESS

**Track:** Job Ready

*Now prove that you can understand, build, debug and maintain Odoo.*

---
<a id="phase-96"></a>
<a id="phase-96---odoo-engineer-job-readiness"></a>
# PHASE 96 - Odoo Engineer Job Readiness

**Track:** Job Ready

**WHAT YOU WILL BE ABLE TO DO:** Answer Odoo interviews with mechanism + trade-offs: ORM, security, inheritance, upgrades, workers, config vs custom vs integrate — practicing with [`Interview.md`](../guides/Interview.md).

**WHAT YOU SHOULD KNOW FIRST:** Entire roadmap themes. Phase 93 — real incidents to narrate.

**LEARNING RESOURCES:**

- [`Interview.md`](../guides/Interview.md)
- [`Projects.md`](../guides/Projects.md) — portfolio talking points
- Official docs for any topic you claim

## 96.1 ERP Fundamentals

Job-readiness check: you must **explain and debug ERP Fundamentals** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ERP Fundamentals** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ERP Fundamentals**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ERP Fundamentals** is concrete, continue — the next lesson in this phase still depends on it.

## 96.2 Odoo Functional Knowledge

Job-readiness check: you must **explain and debug Odoo Functional Knowledge** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Odoo Functional Knowledge** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Odoo Functional Knowledge**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Odoo Functional Knowledge** is concrete, continue — the next lesson in this phase still depends on it.

## 96.3 Odoo Architecture

Job-readiness check: you must **explain and debug Odoo Architecture** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Odoo Architecture** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Odoo Architecture**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Odoo Architecture** is concrete, continue — the next lesson in this phase still depends on it.

## 96.4 Module Anatomy

Job-readiness check: you must **explain and debug Module Anatomy** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Module Anatomy** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Module Anatomy**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Module Anatomy** is concrete, continue — the next lesson in this phase still depends on it.

## 96.5 Python

Job-readiness check: you must **explain and debug Python** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Python** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Python**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Python** is concrete, continue — the next lesson in this phase still depends on it.

## 96.6 Models

Job-readiness check: you must **explain and debug Models** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Models** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Models**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Models** is concrete, continue — the next lesson in this phase still depends on it.

## 96.7 Fields

Job-readiness check: you must **explain and debug Fields** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 96.8 Relationships

Job-readiness check: you must **explain and debug Relationships** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Relationships** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Relationships**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Relationships** is concrete, continue — the next lesson in this phase still depends on it.

## 96.9 ORM

Job-readiness check: you must **explain and debug ORM** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **ORM** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **ORM**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **ORM** is concrete, continue — the next lesson in this phase still depends on it.

## 96.10 Recordsets

Job-readiness check: you must **explain and debug Recordsets** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Recordsets** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Recordsets**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Recordsets** is concrete, continue — the next lesson in this phase still depends on it.

## 96.11 Domains

Job-readiness check: you must **explain and debug Domains** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Domains** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Domains**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Domains** is concrete, continue — the next lesson in this phase still depends on it.

## 96.12 Computed Fields

Job-readiness check: you must **explain and debug Computed Fields** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Computed Fields** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Computed Fields**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Computed Fields** is concrete, continue — the next lesson in this phase still depends on it.

## 96.13 Constraints

Job-readiness check: you must **explain and debug Constraints** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Constraints** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Constraints**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Constraints** is concrete, continue — the next lesson in this phase still depends on it.

## 96.14 Workflows

Job-readiness check: you must **explain and debug Workflows** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Workflows** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Workflows**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Workflows** is concrete, continue — the next lesson in this phase still depends on it.

## 96.15 XML

Job-readiness check: you must **explain and debug XML** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **XML** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **XML**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **XML** is concrete, continue — the next lesson in this phase still depends on it.

## 96.16 Views

Job-readiness check: you must **explain and debug Views** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Views** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Views**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Views** is concrete, continue — the next lesson in this phase still depends on it.

## 96.17 Actions

Job-readiness check: you must **explain and debug Actions** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Actions** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Actions**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Actions** is concrete, continue — the next lesson in this phase still depends on it.

## 96.18 Security

Job-readiness check: you must **explain and debug Security** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Security** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Security**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Security** is concrete, continue — the next lesson in this phase still depends on it.

## 96.19 Multi-Company

Job-readiness check: you must **explain and debug Multi-Company** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Multi-Company** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Multi-Company**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Multi-Company** is concrete, continue — the next lesson in this phase still depends on it.

## 96.20 Inheritance

Job-readiness check: you must **explain and debug Inheritance** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Inheritance** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Inheritance**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Inheritance** is concrete, continue — the next lesson in this phase still depends on it.

## 96.21 Mixins

Job-readiness check: you must **explain and debug Mixins** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Mixins** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Mixins**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Mixins** is concrete, continue — the next lesson in this phase still depends on it.

## 96.22 Wizards

Job-readiness check: you must **explain and debug Wizards** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Wizards** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Wizards**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Wizards** is concrete, continue — the next lesson in this phase still depends on it.

## 96.23 Cron / Automation

Job-readiness check: you must **explain and debug Cron / Automation** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Cron / Automation** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Cron / Automation**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Cron / Automation** is concrete, continue — the next lesson in this phase still depends on it.

## 96.24 Chatter

Job-readiness check: you must **explain and debug Chatter** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Chatter** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Chatter**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Chatter** is concrete, continue — the next lesson in this phase still depends on it.

## 96.25 QWeb / Reports

Job-readiness check: you must **explain and debug QWeb / Reports** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **QWeb / Reports** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **QWeb / Reports**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **QWeb / Reports** is concrete, continue — the next lesson in this phase still depends on it.

## 96.26 Internationalization

Job-readiness check: you must **explain and debug Internationalization** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Internationalization** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Internationalization**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Internationalization** is concrete, continue — the next lesson in this phase still depends on it.

## 96.27 Controllers

Job-readiness check: you must **explain and debug Controllers** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Controllers** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Controllers**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Controllers** is concrete, continue — the next lesson in this phase still depends on it.

## 96.28 APIs

Job-readiness check: you must **explain and debug APIs** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **APIs** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **APIs**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **APIs** is concrete, continue — the next lesson in this phase still depends on it.

## 96.29 Webhooks

Job-readiness check: you must **explain and debug Webhooks** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Webhooks** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Webhooks**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Webhooks** is concrete, continue — the next lesson in this phase still depends on it.

## 96.30 Integrations

Job-readiness check: you must **explain and debug Integrations** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Integrations** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Integrations**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Integrations** is concrete, continue — the next lesson in this phase still depends on it.

## 96.31 OWL

Job-readiness check: you must **explain and debug OWL** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **OWL** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **OWL**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **OWL** is concrete, continue — the next lesson in this phase still depends on it.

## 96.32 JavaScript Framework

Job-readiness check: you must **explain and debug JavaScript Framework** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **JavaScript Framework** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **JavaScript Framework**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **JavaScript Framework** is concrete, continue — the next lesson in this phase still depends on it.

## 96.33 PostgreSQL

Job-readiness check: you must **explain and debug PostgreSQL** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **PostgreSQL** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **PostgreSQL**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **PostgreSQL** is concrete, continue — the next lesson in this phase still depends on it.

## 96.34 Performance

Job-readiness check: you must **explain and debug Performance** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Performance** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Performance**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Performance** is concrete, continue — the next lesson in this phase still depends on it.

## 96.35 Testing

Job-readiness check: you must **explain and debug Testing** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Testing** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Testing**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Testing** is concrete, continue — the next lesson in this phase still depends on it.

## 96.36 Debugging

Job-readiness check: you must **explain and debug Debugging** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Debugging** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Debugging**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Debugging** is concrete, continue — the next lesson in this phase still depends on it.

## 96.37 CLI

Job-readiness check: you must **explain and debug CLI** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **CLI** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **CLI**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **CLI** is concrete, continue — the next lesson in this phase still depends on it.

## 96.38 Deployment

Job-readiness check: you must **explain and debug Deployment** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Deployment** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Deployment**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Deployment** is concrete, continue — the next lesson in this phase still depends on it.

## 96.39 Odoo.sh

Job-readiness check: you must **explain and debug Odoo.sh** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Odoo.sh** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Odoo.sh**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Odoo.sh** is concrete, continue — the next lesson in this phase still depends on it.

## 96.40 Docker

Job-readiness check: you must **explain and debug Docker** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Docker** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Docker**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Docker** is concrete, continue — the next lesson in this phase still depends on it.

## 96.41 Upgrades

Job-readiness check: you must **explain and debug Upgrades** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Upgrades** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Upgrades**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Upgrades** is concrete, continue — the next lesson in this phase still depends on it.

## 96.42 Migrations

Job-readiness check: you must **explain and debug Migrations** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Migrations** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Migrations**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Migrations** is concrete, continue — the next lesson in this phase still depends on it.

## 96.43 Requirements Analysis

Job-readiness check: you must **explain and debug Requirements Analysis** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Requirements Analysis** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Requirements Analysis**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Requirements Analysis** is concrete, continue — the next lesson in this phase still depends on it.

## 96.44 Solution Architecture

Job-readiness check: you must **explain and debug Solution Architecture** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Solution Architecture** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Solution Architecture**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Solution Architecture** is concrete, continue — the next lesson in this phase still depends on it.

## 96.45 Live Module Design

Expect: “Design leave approval for multi-company” or “Here’s an AccessError — talk through it.” Use Phases 84–87 and 93 out loud. Mention tests and upgrade safety unprompted — it signals seniority.

Drill timed answers from [`Interview.md`](../guides/Interview.md); record yourself once.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | 90-second answer: what is a record rule? |
| Medium | 5-minute design: vendor bill approval. |
| Hard | Full mock: debug AccessError + propose module split + workers sizing. |

**WHAT THIS UNLOCKS NEXT:** Interviews ask for proof. Phase 95 — portfolio modules and the final capstone.

---

<a id="phase-95"></a>

## 96.46 Live Debugging

Expect: “Design leave approval for multi-company” or “Here’s an AccessError — talk through it.” Use Phases 84–87 and 93 out loud. Mention tests and upgrade safety unprompted — it signals seniority.

Drill timed answers from [`Interview.md`](../guides/Interview.md); record yourself once.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | 90-second answer: what is a record rule? |
| Medium | 5-minute design: vendor bill approval. |
| Hard | Full mock: debug AccessError + propose module split + workers sizing. |

**WHAT THIS UNLOCKS NEXT:** Interviews ask for proof. Phase 95 — portfolio modules and the final capstone.

---

<a id="phase-95"></a>

## 96.47 Code Review

Job-readiness check: you must **explain and debug Code Review** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Code Review** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Code Review**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Code Review** is concrete, continue — the next lesson in this phase still depends on it.

## 96.48 Portfolio Walkthrough

Job-readiness check: you must **explain and debug Portfolio Walkthrough** without opening this page.

Prepare one story from your modules (what you built, what broke, what you changed). If you cannot point to code, go back to the phase that taught it and add a tiny example to your portfolio addon.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Explain **Portfolio Walkthrough** in two sentences using a real company example. |
| Medium | Find it in Odoo 18 docs or [odoo/odoo](https://github.com/odoo/odoo) and note the file, XML id, or menu. |
| Hard | Describe one production failure caused by ignoring **Portfolio Walkthrough**, and the safer pattern. |

**WHAT THIS UNLOCKS NEXT:** Once **Portfolio Walkthrough** is concrete, continue — the next lesson in this phase still depends on it.

## 96.49 PORTFOLIO PROJECT 1 — Library / Asset Management

**PORTFOLIO PROJECT 1 — Library / Asset Management** is hire-facing proof. Package a small, complete addon: manifest, models, ACLs, a happy-path test, README, and a 90-second demo script.

Interviewers will ask why you customized vs configured, where `sudo()` appears, and how you would upgrade it. Answer from this repo, not from theory.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **PORTFOLIO PROJECT 1 — Library / Asset Management** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **PORTFOLIO PROJECT 1 — Library / Asset Management** is concrete, continue — the next lesson in this phase still depends on it.

## 96.50 PORTFOLIO PROJECT 2 — HR / Leave Approval

**PORTFOLIO PROJECT 2 — HR / Leave Approval** is hire-facing proof. Package a small, complete addon: manifest, models, ACLs, a happy-path test, README, and a 90-second demo script.

Interviewers will ask why you customized vs configured, where `sudo()` appears, and how you would upgrade it. Answer from this repo, not from theory.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **PORTFOLIO PROJECT 2 — HR / Leave Approval** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **PORTFOLIO PROJECT 2 — HR / Leave Approval** is concrete, continue — the next lesson in this phase still depends on it.

## 96.51 PORTFOLIO PROJECT 3 — Sales Customization

**PORTFOLIO PROJECT 3 — Sales Customization** is hire-facing proof. Package a small, complete addon: manifest, models, ACLs, a happy-path test, README, and a 90-second demo script.

Interviewers will ask why you customized vs configured, where `sudo()` appears, and how you would upgrade it. Answer from this repo, not from theory.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **PORTFOLIO PROJECT 3 — Sales Customization** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **PORTFOLIO PROJECT 3 — Sales Customization** is concrete, continue — the next lesson in this phase still depends on it.

## 96.52 PORTFOLIO PROJECT 4 — Inventory Extension

**PORTFOLIO PROJECT 4 — Inventory Extension** is hire-facing proof. Package a small, complete addon: manifest, models, ACLs, a happy-path test, README, and a 90-second demo script.

Interviewers will ask why you customized vs configured, where `sudo()` appears, and how you would upgrade it. Answer from this repo, not from theory.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **PORTFOLIO PROJECT 4 — Inventory Extension** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **PORTFOLIO PROJECT 4 — Inventory Extension** is concrete, continue — the next lesson in this phase still depends on it.

## 96.53 PORTFOLIO PROJECT 5 — API / SaaS Integration

**PORTFOLIO PROJECT 5 — API / SaaS Integration** is hire-facing proof. Package a small, complete addon: manifest, models, ACLs, a happy-path test, README, and a 90-second demo script.

Interviewers will ask why you customized vs configured, where `sudo()` appears, and how you would upgrade it. Answer from this repo, not from theory.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **PORTFOLIO PROJECT 5 — API / SaaS Integration** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **PORTFOLIO PROJECT 5 — API / SaaS Integration** is concrete, continue — the next lesson in this phase still depends on it.

## 96.54 PORTFOLIO PROJECT 6 — OWL Dashboard

**PORTFOLIO PROJECT 6 — OWL Dashboard** is hire-facing proof. Package a small, complete addon: manifest, models, ACLs, a happy-path test, README, and a 90-second demo script.

Interviewers will ask why you customized vs configured, where `sudo()` appears, and how you would upgrade it. Answer from this repo, not from theory.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **PORTFOLIO PROJECT 6 — OWL Dashboard** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **PORTFOLIO PROJECT 6 — OWL Dashboard** is concrete, continue — the next lesson in this phase still depends on it.

## 96.55 PORTFOLIO PROJECT 7 — Customer Portal

**PORTFOLIO PROJECT 7 — Customer Portal** is hire-facing proof. Package a small, complete addon: manifest, models, ACLs, a happy-path test, README, and a 90-second demo script.

Interviewers will ask why you customized vs configured, where `sudo()` appears, and how you would upgrade it. Answer from this repo, not from theory.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Write the acceptance criteria for **PORTFOLIO PROJECT 7 — Customer Portal** in five bullets. |
| Medium | Produce the artifact (module change, diagram, or trace) this item names. |
| Hard | Add one test or one hostile case that would fail if you skipped this item. |

**WHAT THIS UNLOCKS NEXT:** Once **PORTFOLIO PROJECT 7 — Customer Portal** is concrete, continue — the next lesson in this phase still depends on it.

## 96.56 FINAL MOCK INTERVIEW

**FINAL MOCK INTERVIEW** is a timed loop, not a reading assignment. Run rounds: functional flow, Python/ORM, XML/UI, security, integrations, OWL, PostgreSQL/performance, debugging, module design, portfolio walkthrough.

For each round: 8–12 minutes speaking, then notes on gaps. Restudy the matching phase the same day. Use [`Interview.md`](../guides/Interview.md) for drill structure.

### 96.56.1 Functional Round

Narrate CRM → Sales → Delivery → Invoice (and a purchase replenishment) without opening code. Name the documents, not the Python classes first — then map them to models.

### 96.56.2 Python / ORM Round

Write a domain, a batched `write`, and an override that calls `super()`. Explain empty vs singleton recordsets and when `ensure_one()` belongs.

### 96.56.3 XML / UI Round

Sketch a form + list + search view and one xpath inherit. Explain external IDs and `noupdate`.

### 96.56.4 Security Round

Design groups, ACLs, and a record rule for a multi-company document. Explain a `sudo()` you would reject in review.

### 96.56.5 Integration Round

Describe an inbound webhook with signature, idempotency key, and retries — plus which user the job runs as.

### 96.56.6 OWL Round

When would you patch a component vs register a client action? Name `useService("orm")` and one upgrade-safety reason not to patch.

### 96.56.7 PostgreSQL / Performance Round

Read a fake EXPLAIN, spot an N+1, and say whether the fix is prefetch, stored compute, or an index.

### 96.56.8 Debugging Round

Given an AccessError and a traceback, list the first five places you look (ACL, rule, `sudo`, company, XML group).

### 96.56.9 Module Design Round

Given a client request, choose configure vs custom module vs integration, and draw a depends graph.

### 96.56.10 Portfolio Walkthrough

Demo one addon in 90 seconds: problem, models, security, test, upgrade note. Then take hostile questions.

**PRACTICE:**

| Difficulty | Task |
| --- | --- |
| Easy | Time one round at 8 minutes and write three gaps. |
| Medium | Record a 90-second portfolio walkthrough and list the questions you could not answer. |
| Hard | Do a full ten-round mock with a peer or voice notes; restudy the weakest two phases the same day. |

**WHAT THIS UNLOCKS NEXT:** Once **FINAL MOCK INTERVIEW** is concrete, continue — the next phase still depends on it.

**CHECK YOUR UNDERSTANDING AFTER PHASE 96:** You can walk a mock interview and a portfolio demo without this page open.

> **Phase 96 complete?** You are at the end of the roadmap — finish the mock interview and portfolio walkthrough, then apply.

---

## Closing

You started with zero ERP vocabulary. You end able to design, build, secure, integrate, test, deploy, upgrade, and explain an Odoo system — and prove it with portfolio work.

Zero Knowledge → ERP Understanding → Odoo User → Odoo Backend Developer → Full-Stack Odoo Developer → Production Odoo Engineer → **Job Ready**

